# Clients And Chat — Feedback 2026-07-08

## Clients List KPI And Filters

TaskDB: `#540`.

Источник:
- Удалить дублирующие кнопки "Клиенты" / "Все" в левом блоке под строкой поиска.
- В KPI-карточке показывать не только отфильтрованное число, но и общий total через разделитель.
- KPI-фильтры должны поддерживать мультивыбор: например "С записями" + "С программой" + "С отменами".
- Разобраться с фильтром "С приложением": сейчас считает всех; нужен факт установки, если есть, или история входа через приложение.
- В блок "Каналы" добавить фильтр "Пуш-уведомления": фильтровать тех, у кого явно включены push / есть факт настройки.
- Доделать переход на ФИО везде; клиенту в приветствии показывать имя.
- Каналы связи (Telegram, телефон, почта и т.п.) сделать кликабельными.

Связанные задачи:
- `#24` — общий FIO cleanup.
- `#130`, `#131` — уже открытые мобильные/фильтровые хвосты страницы пациентов.

Критерии:
- Перед реализацией определить источник правды для "с приложением" и "push включен".
- Не смешивать `hasApp`, `hasConversation`, Telegram/MAX binding и web-push subscription.
- KPI total должен показывать полный размер сегмента до текущего набора фильтров, а рядом — текущее отфильтрованное значение.
- Мультивыбор KPI должен иметь понятную политику AND/OR. По feedback ожидается AND для выбранных KPI-признаков.

## Patient Card Header

TaskDB: `#541`.

Источник:
- В шапке карточки клиента показывать возраст.
- Пикер даты рождения заменить на такой же качественный, как выбор даты визита.
- Каналы связи в карточке сделать кликабельными.

Связанные факты:
- В БД уже есть `platform_users.birth_date` (migration `0123_platform_users_birth_date.sql`).
- В проекте есть `DoctorDatePicker` / date picker для визита.

Критерии:
- Возраст считается от `birth_date`, корректно обрабатывает пустую дату.
- Дата рождения редактируется без регресса сохранения профиля.
- Телефон/mail/messenger chips имеют понятное действие: copy, `tel:`, `mailto:`, открыть чат/профиль binding — уточнить по текущему UI и не ломать безопасность.

## Doctor Chat UX

TaskDB: `#542`.

Источник:
- Из чата нельзя открыть профиль клиента.
- При вставке ссылок проверить linkify: ссылки должны восприниматься как ссылки и открываться во внешнем браузере.
- По tap/right click добавить собственное меню с действием "Ответить".

Критерии:
- Из списка/панели чата есть переход в карточку клиента.
- URL в сообщениях безопасно linkify: `http(s)` открываются наружу, без XSS/HTML injection.
- "Ответить" не ломает текущую модель support-чата и read receipts.
- Для touch и desktop определить единый UX: context menu / long press / explicit action button.
