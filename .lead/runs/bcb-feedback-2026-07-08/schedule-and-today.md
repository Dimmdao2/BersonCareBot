# Schedule And Today — Feedback 2026-07-08

Обязательный контекст перед кодом/миграциями: [README.md](README.md) — `Mandatory Preflight For Every Task` и `SaaS Foundation Guardrail`.

## Schedule Work Grid

TaskDB: часть `#538`. Цвет филиалов не дублировать — `#530`.

Источник:

- "Расписание - график работы: не сбрасывается выбор дней по клику на пустом месте".
- "Цвет филиалов сделать настраиваемым" уже покрыт taskdb `#530`.

Новая задача:

- Проверить `ScheduleWorkTab`: выбор дней должен сбрасываться при клике по пустому месту/фону, без поломки мультивыбора дней и без сброса при клике по контролам формы.

Критерии:

- Клик по пустой области календаря графика снимает выбранные дни.
- Клик по выбранному дню, контролам времени, филиалу, шаблону, кнопкам сохранения не вызывает ложный сброс.
- Если после `#527` уже есть похожая логика, исправить регрессию, а не добавлять параллельный механизм.

## Schedule Calendar

TaskDB: `#538`.

Источник:

- Настраиваемый доктором дефолтный интервал календаря для страниц "Расписание" и "Сегодня".
- После изменения графика рабочие часы в сетке не обновляются без перезагрузки.
- Окно отображения часов выбирается хаотично; надо: дефолт 09-19 или настройка врача, расширение на час до/после записей/рабочих часов, не ужимать до узкого 14-17 при одной записи.
- Нет нижнего отступа календаря.
- В режиме "лист" список записей обрезан ниже высоты окна, появляется лишняя внутренняя прокрутка и пустое поле.
- В режиме "лист" правый блок с информацией обрезается без нормальной внутренней прокрутки.
- Статус записи показывает системное `confirmed`, а не человекочитаемый текст.
- Сегодняшний день показывать красным кругом вокруг даты во всех режимах: 3 дня, неделя, месяц. Для 3 дней/недели увеличить высоту шапки.

Связанные старые задачи:

- `#231` стандартное окно 9-19 + адаптация под записи — было принято, но свежий feedback говорит о регрессии/несоответствии ТЗ.
- `#237` настройка доктора для окна сетки — было принято.
- `#261` круг "сегодня" — было принято, но свежий feedback требует красный круг во всех режимах.

Критерии:

- Проверить фактический алгоритм видимого окна на `/app/doctor/schedule` и странице "Сегодня".
- Сохранение/изменение графика работы должно инициировать обновление календарной сетки без reload.
- Человеческие статусы записи должны использовать единый formatter, не raw enum.
- List-mode должен использовать доступную высоту окна без двойной/лишней прокрутки.
- Отступ снизу не должен ломать sticky/header behavior.

## Today KPI

TaskDB: `#539`.

Источник:

- KPI "Записи неделя" и "Записи месяц" разделить внутри карточки на две кликабельные половины: Всего и Будущие.
- Клик по половинам открывает соответствующий список записей.
- Если KPI = 0, пустую модалку не открывать.
- Удалить поиск по клиенту из KPI-модалок на странице "Сегодня".
- Для KPI "Комментарии" сделать единый шаблон списка комментариев: слева превью упражнения, справа ФИО, ниже начало сообщения, справа дата/время.

Связанные старые задачи:

- `#46` унификация KPI-модалок как на "Сегодня" — была принята, но новый feedback задает более точный UX.

Критерии:

- Не затрагивать аналитику, если она использует другой табличный список клиентов.
- Переиспользовать один компонент/формат элемента комментария там, где такой список нужен на разных страницах.
- Модалка не открывается при нулевом значении соответствующей половины KPI.
