# Тест или взгляд

The single repeatable route-grammar correction is tested through the retained public delivery-adapter contract;
the exact one-file product diff and restored clean tree are inspected. This is confirmation of one prior finding,
not a new general audit pass.

# Auditor-live brief — #915 encoded native tap route correction

Independently confirm only the correction from `88e9240df` to the exact committed candidate. Product code is
read-only. Do not reopen already-passed push architecture, migration, settings, delivery fan-out or copy questions;
do not fix code or touch Android/web runtime/media/data/services/PROD.

Run the AGENTS.md heading map; read the global decision method, §1/§1b, §5, §9–§12 and §24 fully, with §10a/§10b
before tests. Read M6-09/M7, the prior final report, its retained route contract and the exact one-file fix diff.

Источник оракула — `docs/_TODO/NATIVE_MOBILE_APP_INITIATIVE/MASTER_PLAN.md`: «Tap ведёт внутрь правильной
поверхности приложения; внешние и обманные маршруты отклоняются.»

Verify the backend sends a native target only for the same closed grammar accepted by the native tap gate. The
prior failing `/app/patient/%2F..%2Fadmin` case must be green. Add cases to the existing table only if needed to
prove encoded separator/dot, raw traversal normalization, backslash/protocol-relative and an otherwise valid
explicit `pushSurface` cannot bypass route validation; ordinary canonical patient/staff routes still reach only
their matching provider target. Assert provider observation, not private helpers/source strings.

Perform one temporary fault injection that removes or moves the new route validation after explicit surface
resolution; the retained/new oracle must go red, then restore production exactly. Run the one route-contract test,
integrator build/typecheck, scoped ESLint and `git diff --check`. Do not run full CI.

Commit only justified additions to the existing acceptance test and
`.lead/runs/mobile-push-route-fix-confirmation-20260909/90-final-report.md` with explicit paths, never `git add -A`;
do not push. Return binary PASS/MUST FIX for this correction only, with reachable scenario, exact assertions,
commands and candidate SHA. Restore every production mutation and do not finish while a foreground check runs.
