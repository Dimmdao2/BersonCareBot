# Быстрые задачи для юзера (quick wins) — 2026-06-17

> Подобраны под лупа «Минионы» (атомарные, преимущественно UI/UX поверх готового). Заземлены в коде.
> Масштаб: S ≤дни · M ~1–2 нед. Статус: ✅ есть · ◑ частично · 🆕 новое · 🐞 баг.
> Не редактировать отсюда `QUEUE.md`; владелец решает, что отдать лупу (копировать в `docs/_INBOX/`).

## A. Упражнение / программа — UX (главный кусок)
> Идея владельца: **всё уносим со списка на СТРАНИЦУ упражнения** (не модалки, не чат). В списке — только иконки.

1. **◑ Отметка выполнения + поля (повторы/подходы/вес) — из модалки в постоянный inline-блок** на странице упражнения.
   Над полями — рекомендация-цель врача; помнить прошлые значения; правка перед завершением.
   *Файлы:* `patient/treatment/PatientProgramStageItemPageClient.tsx` (страница есть), `diary/QuickAddPopup.tsx` (модалка — убрать). **M**
2. **◑ Из элемента списка убрать кнопки** «пометить выполненным» и «отправить видео» (видео, похоже, уже убрано ✅).
   Клик по элементу → открывает **страницу упражнения** (не модалку). *Файлы:* `treatment/PatientInstanceStageItemCard.tsx`, `PatientStageCompositionList.tsx`. **S–M**
3. **🆕 В элементе списка — только иконки снизу:** есть комментарии · есть новые · противопоказания · зелёные точки (сколько раз сегодня). *Файл:* `PatientInstanceStageItemCard.tsx` + `PatientProgramItemExecutionRow.tsx`. **S–M**
4. **🐞 Зелёные точки за день** — мульти-точки в UI уже есть (`PatientProgramItemExecutionRow` рисует `todayCount`), но при нескольких выполнениях/день показывается, похоже, не так. Проверить `resolveProgramItemExecutionDots` / откуда берётся `todayCount`. *Файл:* `treatment/programItemExecutionDisplay.ts`. **S (verify+fix)**
5. **◑ Комментарии врача — на странице упражнения** (добавлять/читать там же), а не в модалке/чате. *(discussion-модуль есть — разместить на itemPage).* **S–M**

## B. Напоминания / запись — быстрые
6. **🆕 «Добавить запись в календарь»** из интерфейса: Google / Yandex / `.ics` (телефон) / файлик на почту. **S–M**
7. **🆕 В push — кнопки «напомнить позже» / «пропустить».** *(PWA web-push actions.)* **S–M**
8. **🐞 «Не напоминать в боте» не отключает напоминания клиента** — проверить/исправить связку бот↔настройка. **S**
9. **◑ Напоминания по умолчанию по ВРЕМЕНИ, не интервал;** разминки — расписание в настройках через БД (доктор). *(ротация разминок ✅; время-vs-интервал доработать.)* **S–M**

## C. Медиа / видео — быстрые
10. **◑ Пациент заливает большое видео → серверный рендер 360/480 + удаление оригинала.** *(transcode-пайплайн есть; собрать client-upload→render flow.)* **M**
11. **🆕 Доктор добавляет индив. упражнение в ЛФК + заливает видео с пациентом** (снятое на приёме). **M**

## D. Надёжность UX — быстрая
12. **◑ Все фейлы — только в хинт (toast/inline), не ломать UI.** *(часть закрыта в salvage round-3.)* **S–M**

---
### Уже сделано / частично (не делать заново)
Страница упражнения (`PatientProgramStageItemPageClient`) ✅ · «отправить видео» из списка ✅ убрано · мульти-точки в UI ✅ (баг в данных) · ротация разминок ✅ · transcode-пайплайн ✅ · часть error-в-хинт ◑ (salvage).
