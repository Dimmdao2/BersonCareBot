# Agent auto-run — справка

Действующие правила оркестрации находятся только в [`AGENTS.md` §24](../AGENTS.md#24-оркестрация-субагентов).
Этот файл не является каноном и не содержит отдельной редакции ролей, audit/fix-циклов, лимитов или приёмки.

Команды запуска и их фактические гейты показывает:

```bash
tools/orch-launch.sh --help
```

История прежней схемы доступна в git; в обычный prompt агента она не загружается.
