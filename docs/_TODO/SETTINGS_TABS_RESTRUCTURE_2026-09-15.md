# Настройки: разбор на смысловые вкладки (владелец, 15.09.2026)

Владелец: «давай разделим настройки на смысловые блоки. а то у нас порядок полностью хаотичен».
И отдельно: «Я думаю надо перенести ВСЕ настройки для СОЛО в блок аккаунта (где имя и аватар) — все
в одно место. Для клиники там деление будет, но надо продумать, это позже».

Этот файл — единственный источник «todo» и «готово» по работе. Галочка ставится только со ссылкой
на доказательство (коммит + живая проверка), не по отчёту.

## Цель одной строкой

**У специалиста одно место со всеми настройками, и в нём девять вкладок по смыслу.** Контейнер —
ссылка с именем организации; заголовок экрана — «Профиль и настройки» (владелец дословно: «Аккаунт —
одна из вкладок. Контейнером становится ссылка с именем организации, заголовок в шапке „Профиль и
настройки“»).

## Что есть сейчас (замер 15.09, `feat/doctor-ui-rebuild`)

Настройки живут на ДВУХ страницах, и это половина беспорядка:

- `/app/settings` — пять вкладок (`settingsTabs.ts`): Организация, Онлайн-запись, Профиль
  специалиста, Команда, Тариф и биллинг. Вкладка «Организация» — свалка: в `page.tsx:520–615`
  подряд идут брендинг, домен, слаг, ссылка записи, визитка, рабочее пространство, второй фактор
  персонала, «Сегодня», напоминания, оплаты, каналы доставки, гугл-календарь.
- `/app/account` — четыре вкладки (`AccountTabs.tsx`): Профиль, Безопасность, Уведомления,
  Установить приложение.

Механика вкладок уже есть и переиспользуется: `?tab=` + `parseTab` в `page.tsx`, навигация —
`SettingsTabsNav.tsx`, список — `settingsTabs.ts`. Новую заводить не надо.

## Целевые вкладки (решение владельца, дословно по составу)

| # | Вкладка | Что внутри |
|---|---|---|
| 1 | **Аккаунт** | имейл логина; подключение ботов vk/тг/макс платформы therapysto для специалиста; безопасность — пароль и второй фактор (код на почту или TOTP), вход по ключу; устройства входа; тариф с возможностью выбрать новый; доступное место; отмена подписки и автоплатёж; удаление организации и аккаунта; выход |
| 2 | **Профиль** | название организации, слаг организации, ФИО специалиста, аватары и иконки |
| 3 | **Публичная страница** | превью и тексты (описание организации и специалиста), публичные контакты, что и где отображать: показывать ли визитку вообще, виджет записи на ней, ссылку на профиль специалиста, форму заявки (если по тарифу) — то, что доступно всем без брендирования |
| 4 | **Брендинг** | приложение, домен, боты, свои SMTP/SMS-провайдеры, иконка приложения, правка шаблонов брендированных сообщений |
| 5 | **Запись** | филиалы, услуги, настройки виджета записи и создание ссылки, правила отмены и переноса, напоминания клиентам |
| 6 | **Приём оплаты** (если в тарифе) | подключение провайдера, предоплата при записи, онлайн-оплата из карточки записи (QR), история платежей |
| 7 | **Рабочее место** | настройки интерфейса (что показывать), названия «клиенты/пациенты», «сеансы», «избранные», установка приложения на устройство |
| 8 | **Уведомления** | куда и что слать СПЕЦИАЛИСТУ: напоминания о задачах, сообщения от пациентов, комментарии к упражнениям; push в приложении и вебаппе |
| 9 | **Интеграции** | календари: Google, Яндекс, iOS |

### Границы, которые владелец назвал отдельно

- **Шаблоны — это два разных предмета.** «Шаблоны записи — это тексты. Шаблоны в брендинге — это
  оформление. Оно общее и для кодов входа пациентам, и для рассылок, и прочего, не только для
  уведомлений/напоминаний о записи». Тексты — во вкладке «Запись», оформление — в «Брендинге».
- **Уведомления о новой записи.** «Куда слать пациенту выбирает пациент. Специалист выбирает куда и
  что слать ему» — во вкладке «Уведомления» только адресат-специалист.
- **Вход по ключу (passkey)** — полноценный вход без пароля, не второй фактор. Стоит в «Аккаунте»
  рядом с паролем и вторым фактором, название — «Вход по ключу — без пароля и без кода».
  Персоналу клиники его не заводим.
- **Часовой пояс** — только у филиалов. В платформе Москва, отдельной настройки нет.
- **Журнал действий** — для соло не нужен; для клиники будет режим админа клиники, сейчас не
  продумывается.
- **Установка приложения на устройство** — это «Рабочее место», не «Аккаунт».

## Что удаляется (решения владельца 15.09)

- [ ] У СОЛО убрать **SMS fallback**, **«показывать мне врачебные экраны»**, **«требовать второй
      фактор у персонала»** — у одного человека этих понятий нет.
- [ ] **Статистика напоминаний** (`ReminderStatsSection.tsx`) — «точно не тут и вообще нахуй.
      Интересуют скорее ошибки напоминаний, а не сколько дошло, это самый бесполезный показатель и
      не тратить на него даже проц».
- [ ] **Ошибки напоминаний** в настройки НЕ добавляются вовсе (владелец 15.09): «никуда, админу
      платформы и так придёт, если это падения нашей системы». Отдельной поверхности не заводить.
- [ ] **«Откуда пришёл клиент»** (`BookingPublicAttributionSection.tsx`) — «точно не тут, пока у нас
      вообще выключено».
- [x] **Ручное ведение записи** — «судя по описанию не должно жить нигде… в таком виде по крайней
      мере. В интерфейсе у нас это сделано уже». Удалено, коммит `c1c63a4c2`.
- [ ] Для СОЛО убрать **«выбор специалиста»** из виджета записи и из остальных мест: выбирать не из
      кого.

## Этапы

Порядок такой, чтобы владелец увидел результат рано и мог развернуть, пока дёшево.

- [ ] **Н-1. Каркас.** Девять вкладок в `settingsTabs.ts`, заголовок «Профиль и настройки»,
      контейнер — ссылка с именем организации. Старые `?tab=` отвечают редиректом на новые, чтобы
      живые ссылки и закладки не умерли. Разделы пока переезжают как есть, без правки содержимого.
- [ ] **Н-2. Слияние с `/app/account`.** Личные разделы (имейл, безопасность, устройства, выход,
      установка приложения) уходят в соответствующие вкладки; `/app/account` остаётся редиректом.
- [ ] **Н-3. Чистка у СОЛО.** Удаления из списка выше.
- [ ] **Н-4. Приёмка владельцем на TEST.** Кликабельный проход по всем девяти вкладкам.
- [ ] **Н-5. Напоминания клиентам — до трёх настраиваемых.** Сейчас захардкожено три варианта;
      надо «добавлять до трёх любых напоминаний и отдельно выбирать их период. Просто по умолчанию
      ставить 24 и 2 часа при создании аккаунта».
- [ ] **Н-6. История платежей** открывается в правой панели или модалке, а не отдельной страницей.

## Баги, найденные владельцем по дороге (не этапы, но чинятся здесь)

- [ ] При включении выключенной услуги она не появляется в блоке «доступность услуг по филиалам»
      до перезагрузки страницы. Как ведёт себя при создании НОВОЙ услуги — не проверено.
- [ ] В режиме «профиль» пропадают иконки «создать запись» и «новый клиент» в верхней панели.

## Сделано до начала перестройки

- [x] Картинки по `mediaId` — один контрол вместо голого `<img>`; адрес записи в настройках стал
      каноническим `<хост>/<метка>/booking`. Коммит `98d20a945`.
