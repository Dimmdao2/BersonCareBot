# Решено, но не сделано — проверенный список

Заказ владельца, 19.08 (дважды): «у нас куча была обсуждений и решений, что из этого сделано, что нет, что
забыли, что переосмыслили» и, после того как выяснилось, что часовой пояс чинили по старому варианту при
записанном новом решении: «что еще мы обсудили и не сделали? — я же тебя про это и спрашивал».

**Почему этот файл есть.** 19.08 владелец записал правило §34 (пояс у человека не настраивается), а работа
в тот же день шла по старому варианту — селектору добавили подтверждающий тост. Никто не сверил работу с
решением. Разброс решений по трём реестрам это гарантирует: решение живёт в одном файле, работа идёт по
другому. Этот список — сверка решений с КОДОМ, а не с отчётами.

Каждая строка проверена командой. Пусто без доказательства не пишется.

## Проверено и НЕ сделано

| Решение | Где записано | Чем доказано, что не сделано | Состояние |
| --- | --- | --- | --- |
| Часовой пояс у человека не настраивается; настраивается у филиала | `OWNER_PRODUCT_RULES.md` §34 (19.08) | Селектор жив: `app/app/patient/profile/page.tsx:144` → `PatientCalendarTimezoneSection.tsx`; у специалиста `settings/DoctorTimezoneSection.tsx`. У филиала поля нет ни в одной форме при живой колонке `be_branches.timezone` | В работе, ветка `wt/timezone-34-20260819` |
| Выключателя публикации визитки нет — визитка есть всегда | `OWNER_PRODUCT_RULES.md` §33.6 (19.08) | `grep card_is_published\|cardIsPublished` по `apps/webapp/src`+`db` без тестов → **11 живых упоминаний**; ветка 404 по выключателю на месте | НЕ начато |
| Календарь — настройка КЛИНИКИ, учётка принадлежит клинике | `OWNER_PRODUCT_RULES.md` §30 (28.07) | В базе TEST нет ни одной таблицы с учётными данными календаря, привязанными к организации: `information_schema` по `%calendar%` даёт единственную `booking_calendar_map`. То есть разреза «платформа включает / клиника заводит свои креды» в данных нет | НЕ начато |
| Собственный домен клиники = брендирование = одна платная механика | `OWNER_DECISIONS.md` Т11 (19.08) | В реестре механик `modules/org-entitlements/types.ts` до сих пор ДВА отдельных ключа: `branding` и `custom_domain`. По решению это одна неделимая механика | Ждёт решения владельца по домену |
| Лимит клиентов убрать | `OWNER_DECISIONS.md` Т12 (19.08) | В текущем коде `patient_count` удалён из `MECHANICS`, конструктора и quota/write-door; миграция `20260819T210005_a_clinic_is_billed_for_seats_not_for_people.sql` чистит старые данные | Сделано; последствие вынесено в Т13 |
| «Только чтение» — одно место на все механики | `OWNER_DECISIONS.md` Т13 (19.08) | `isCabinetEntryBlocked` закрывает `disabled` и `unconfigured`, `read_only` пропускает; `POST /api/doctor/clients` после удаления `patient_count` не вызывает commercial resolver и остаётся достижимым в `read_only` (`PATIENT_COUNT_REMOVAL_AUDIT_2026-08-19.md`) | НЕ начато; открыто также в `TARIFFS_PAYMENTS_ADMIN_PLAN.md` Т13 |
| Счёт за место не отменяют; долг едет дальше в стоимость следующего периода | владелец 19.08 + `SEAT_UNPAID_PRACTICE_2026-08-19.md`, ⛔ уточнено владельцем 20.08 (Р-19) | Отказ отмены (Р-17) и перенос долга (Р-18) сделаны и влиты. **Перевыставление ОТМЕНЕНО**: владелец 20.08 — «короче перевыставление — бред, убирать»; у счёта один срок — конец периода, дальше долг включается в стоимость следующего периода. Снятие сделано в `wt/drop-reissue-20260820` (`34b0df9dd`), независимый гейт Opus — PASS (`08eddbe2f`), ждёт сведения | Решение изменено, перевыставления не будет |
| Порядок миграций — по имени файла, «применено» — по леджеру | владелец 19.08 | Сделано в ветке `wt/migration-order-20260819`, в `feat` не влито. На `feat` до сих пор ДВА файла `0047_*`, а `0048` пропущен | В работе |
| Своя почта клиники в письмах (адрес письма строится от домена клиники) | следствие Т11 | `grep APP_BASE_URL` по `apps/webapp/src` без тестов → **35 мест** строят адрес от одной платформенной переменной | НЕ начато |

## Проверено и НЕ подтверждено как дефект

- **Контакты владельца в коде (§19).** Найдено две ссылки на `dmitryberson.ru` — витрина лендинга и iframe
  адреса в кабинете пациента. Это не «контакты владельца константой» в смысле §19, а страницы конкретного
  сайта; отдельного решения по ним нет. Оставлено как наблюдение, не как задача.

## Открытые находки прошлых проходов, которые ждут своей очереди

- Видео на TEST не пересобирается больше суток — диспетчер очереди ходит арендной ролью
  (`OWNER_PATIENT_WALKTHROUGH_BUGS_2026-08-19.md`). В работе, ветка `wt/media-worker-root-20260819`.
- Двойная бронь на занятый слот; прод-алертинг не ловит отказ доставки; карточка пациента хардкодит Москву;
  расписания оператора на TEST не установлены.
- 26 разрывов «код есть, экрана нет» — `SILENT_CODE_CENSUS_2026-08-19.md`. Из них болят четыре: буфер между
  записями, блокировка времени, создание кабинета, пояс филиала (последнее закрывается работой по §34).

## Как этот список не протухнет

Правило, из-за отсутствия которого и появился этот файл: **правка решения не завершена, пока новое правило
не внесено в затронутые план-чек-листы.** Аудит сверяет с планом; правила, которого нет в плане, не
проверяет никто. Перед работой по теме — пройти три реестра решений (таблица в шапке `OWNER_DECISIONS.md`) и
назвать, где искал.
