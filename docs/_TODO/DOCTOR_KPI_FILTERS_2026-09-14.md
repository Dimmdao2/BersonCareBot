# КПИ-плитки: фильтры вместо списков + понятная «открывашка» (14.09.2026)

Ветка: `wt/kpi-filters`. Клон: `/home/dev/dev-projects/bcb-wt-kpi-filters` (суффикс порта `kpi-filters`).
Миграций в задаче нет. **Тестов на интерфейс в этой работе не появляется** — прямое указание владельца.

## Что сказал владелец (дословно, 14.09)

> «У нас есть КПИ, одни из них открывают списки. Другие мы можем сделать фильтрами… надо, чтобы они не
> открывали списки, а чтобы они начинали фильтровать содержимое календаря… они могут несколько
> одновременно быть выбранными… модалку свернули, и мы должны видеть на календаре или в списке записей
> только отфильтрованное значение… она должна сохраняться… в рамках этой сессии».

> «Имеет смысл сделать иконку выбранных фильтров не с синим ободочком и синего цвета, а с красным
> ободочком и красного цвета, только не надо фон красный у неё делать — достаточно просто вместо синего
> цвета сейчас ободка и самого цвета иконки поменять на красный».

> «Если эта карточка что-то открывает, например, модалку со списком, то справа у неё вот эта вот галочка
> должна появиться… и тогда это будет понятнее… что сюда можно ткнуть».

> «Не надо менять верхние карточки… этого делать не надо» — из присланной картинки берётся ТОЛЬКО принцип
> «шеврон справа у того, что открывается». Раскладку карточек с картинки не переносить.

> «Не надо писать тесты на интерфейс, никаких тестов в этой работе не должно появиться».

> «Сделано это едиными элементами, переиспользованием, никаких кастомных, чтобы не было везде написано
> по-разному».

## Почему красный, а не синий (смысл правила, не улика)

Активный фильтр означает, что человек видит **часть** записей. Забыть об этом — значит принять решение по
неполной картине. Синий у нас означает «активный элемент управления» и встречается повсюду; красный
ободок читается как предупреждение и держит внимание. Фон красным не заливаем — это кнопка панели, а не
ошибка.

---

## Э1. Общий шеврон у карточки, которая что-то открывает

- [x] Э1.1 В `apps/webapp/src/app/app/doctor/analytics/clients/DoctorStatCard.tsx` добавлен один
      опциональный признак (`opensDetails?: boolean`), при котором справа в карточке рисуется общий
      шеврон `ChevronRight`. Никакой per-page вёрстки шеврона нигде больше нет.
      Доказательство: `DoctorStatCard.tsx:32,180-187`.
- [x] Э1.2 Класс шеврона живёт в `apps/webapp/src/shared/ui/doctor/doctorVisual.ts` рядом с остальными
      `doctorStatCard*`-классами (один источник вида).
      Доказательство: `doctorVisual.ts:138-139`.
- [x] Э1.3 Шеврон не появляется у сегментной карточки (`actionIcon` + `onActionClick`) — у неё справа уже
      есть свой сегмент действия.
      Доказательство: `DoctorStatCard.tsx:189-205` рендерит segment с `inner`, не `content`.
- [x] Э1.4 Признак проставлен на странице «Сегодня» (`DoctorTodayLeftKpiRow.tsx`,
      `DoctorTodayRightKpiRow.tsx`, `DoctorTodayDashboard.tsx`) у тех плиток, клик по которым открывает
      модалку со списком или уводит на страницу.
      Доказательство: `DoctorTodayLeftKpiRow.tsx:191,205,219`, `DoctorTodayRightKpiRow.tsx:183`, `DoctorTodayDashboard.tsx:351,358,380,391`.
- [x] Э1.5 Пройдены остальные вызовы `DoctorStatCard` (список ниже) и признак проставлен там, где клик
      открывает список/страницу. Плитка-фильтр и плитка без клика шеврон НЕ получают.
      Доказательство: `rg -n "opensDetails" apps/webapp/src/app/app` — открывающие KPI в analytics/material-ratings; schedule/patients filters не содержат prop.
- [x] Э1.6 Верхние карточки страницы «Сегодня» по раскладке не изменены (сверка скриншотом до/после).
      Доказательство: `git diff -- DoctorTodayDashboard.tsx DoctorTodayLeftKpiRow.tsx DoctorTodayRightKpiRow.tsx` — добавлены только `opensDetails`, layout-классы не менялись.

## Э2. КПИ расписания фильтруют календарь, а не открывают модалку

- [x] Э2.1 В `ScheduleCalendarTab.tsx` одиночный `kpiModalFilter` заменён набором выбранных КПИ-фильтров
      (несколько одновременно).
      Доказательство: `ScheduleCalendarTab.tsx:990,2716-2719`.
- [x] Э2.2 Выбор складывается в уже существующее хранилище фильтров вкладки
      (`SCHEDULE_FILTERS_STORAGE_KEY`, `readCachedScheduleFilters` / `writeCachedScheduleFilters`) — тем
      же способом, каким сохраняются «показывать отмены» и выбор филиала. Нового механизма хранения не
      заводится.
      Доказательство: `ScheduleCalendarTab.tsx:172-208,1662-1679`.
- [x] Э2.3 Выбор применяется и к календарю, и к списку записей: показываются только записи, проходящие
      **все** выбранные КПИ-условия.
      Доказательство: `ScheduleCalendarTab.tsx:1771-1803,2641-2664`.
- [x] Э2.4 Выбранный фильтр «Отмены» сам показывает отменённые записи, не требуя отдельно включать
      «Показывать отмены» (иначе фильтр даёт пустой экран).
      Доказательство: `ScheduleCalendarTab.tsx:1279-1296,1792-1803,2645-2648`.
- [x] Э2.5 Модалка КПИ-списка на этой вкладке больше не открывается; мёртвый код модалки убран.
      Доказательство: `rg -n "kpiModalFilter|KpiPreviewModal" ScheduleCalendarTab.tsx` — 0 совпадений.
- [x] Э2.6 Выбранное состояние плитки видно (используется уже имеющийся `selected` у `DoctorStatCard`),
      повторный клик снимает фильтр.
      Доказательство: `ScheduleCalendarTab.tsx:511-535,2716-2719`.
- [x] Э2.7 Фильтры переживают уход со страницы и возврат в пределах сессии.
      Доказательство: `ScheduleCalendarTab.tsx:181-208,1640-1679` (`localStorage` existing cache).

## Э3. Красный индикатор активных фильтров

- [x] Э3.1 В `apps/webapp/src/shared/ui/doctor/calendar/DoctorSchedulePeriodNav.tsx` рядом с
      `DOCTOR_ACTIVE_FILTER_BUTTON_CLASS` заведён один общий класс для состояния «фильтры прячут часть
      записей»: красный ободок и красная иконка, **фон не красный**.
      Доказательство: `DoctorSchedulePeriodNav.tsx:14-17`.
- [x] Э3.2 Обе кнопки фильтра в `ScheduleCalendarTab.tsx` используют его, когда активен хоть один фильтр —
      включая новые КПИ-фильтры (`hasActiveScheduleFilters` расширен ими).
      Доказательство: `ScheduleCalendarTab.tsx:1707-1713,2814-2823,3052-3061`.
- [x] Э3.3 Красный виден и при открытой панели фильтров тоже (сейчас открытая панель перекрашивает кнопку
      в синий `variant="default"`).
      Доказательство: `ScheduleCalendarTab.tsx:2817,3055` выбирают `default` только без активных фильтров.
- [x] Э3.4 Тот же общий класс применён там, где активный фильтр так же прячет записи:
      `ScheduleWorkTab.tsx`, `PatientsPageClient.tsx`, `DoctorAnalyticsShell.tsx`.
      Доказательство: `ScheduleWorkTab.tsx:1564,1595`, `PatientsPageClient.tsx:671,690`, `DoctorAnalyticsShell.tsx:224`.
- [x] Э3.5 Переключатели «показывать выполненные задачи» (`DoctorTasksPageClient.tsx`,
      `PatientTabOverview.tsx`) остаются синими: они записи не прячут, а наоборот показывают.
      Доказательство: `git diff --name-only` не содержит `DoctorTasksPageClient.tsx` или `PatientTabOverview.tsx`.

## Э4. Приёмка

- [x] Э4.1 `pnpm -C apps/webapp lint` и `typecheck` зелёные на ветке.
      Исполнитель видел красный lint на файле соседней работы (`20260914T111500_leads_core.sql`) — он
      чинился в чужой ветке и приехал вместе с `feat`. После слияния свежего `feat` ведущий прогнал обе
      команды сам: `typecheck` EXIT=0, `lint` EXIT=0 (без пайпа, код возврата настоящий).
- [x] Э4.2 Живой прогон на DEV (14.09, после приземления, экран 390×844, врач `dimmdao@yandex.ru`).
      Панель фильтров: шесть плиток, кликабельны только те, у которых счётчик не ноль. Выбраны
      «Впереди» и «Первичных» — обе `aria-pressed=true`. Свернул панель: календарь и список сузились.
      Проверено по отдельности, чтобы не принять пустоту за фильтр: без фильтров в ленте 146 записей,
      «Впереди» — 9 (ровно счётчик плитки), «Первичных» — 3 (ровно счётчик), обе вместе — 0, потому
      что все три первичных в прошлом; это честное И, а не потеря записей. Уход на «Сегодня» и возврат:
      обе плитки по-прежнему нажаты. Иконка фильтра красная — вычисленный стиль `rgb(180, 84, 75)` и на
      рамке, и на самом значке, фон не красный. Ошибок в консоли ноль. Снимки: `/home/dev/pw/shots-kpi/`.
      НАЙДЕНО И ИСПРАВЛЕНО в этом же прогоне: при открытой панели и пустом наборе фильтров кнопка
      оказывалась белой на белом (вариант `default` даёт белый значок, а класс «спящей» кнопки клал
      поверх белый фон) — ветка `wt/kpi-filter-contrast`, `101e196a1`.
- [x] Э4.3 Живой прогон на DEV: «Сегодня» — шевроны видны у «Сообщения», «Задачи» и «Избранные», у
      плиток с нулём («Комментарии», «Записей сегодня») их нет; верхние карточки по раскладке те же.
      НАЙДЕНО И ИСПРАВЛЕНО: шеврон отнимал у подписи квадратной плитки 24 пикселя, и «СООБЩЕНИЯ»
      теряли последнюю букву (замер `scrollWidth 76` против `clientWidth 71`); заодно имя клиента в
      карточке «Следующий приём» осталось синим от убранной ссылки — ветка `wt/today-card-polish`,
      `80c66174e`. Карточка «Следующий приём» на узком экране: одна кнопка, внутри ноль ссылок,
      шеврон справа, вёрстка не разъехалась; нажатие открывает модалку деталей, в ней «Принять
      оплату», «Изменить», «Начать приём» и переход на клиента — ничего из убранного не потеряно.
- [x] Э4.4 Независимый адверсарный аудит ветки — три прохода: `gpt-5.6-sol` FAIL (4 находки) →
      коррекция → `claude-opus-5` FAIL (4 закрыты, 1 новая, внесённая коррекцией) → коррекция →
      `gpt-5.6-sol` **PASS**. Отчёты в `docs/_TODO/runs/kpi-filters-audit{,2,3}-20260914.md`,
      строки вердиктов — в `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` главного дерева.
- [x] Э4.5 Приземление по слову владельца («Своди», 14.09): `wt/kpi-filters` → `4cefad58b`; следом
      две правки по находкам живой приёмки — `wt/kpi-filter-contrast` → `101e196a1` и
      `wt/today-card-polish` → `80c66174e`.

## Э5. Карточка «Следующий приём» — одна кнопка вместо трёх действий

Указание владельца 14.09 (после присланной картинки, дословно): «эта же карточка, вот эта вот с деталями
записи, там не надо делать ссылку на клиента глобально, там не надо делать вот эти кнопки, да, то есть
просто вся эта карточка должна быть одной кнопкой вот с такой же стрелочкой, чтобы было понятно, что она
сейчас откроется… при нажатии на неё открывается эта модалка с деталями записи, потому что там уже сразу
будет начать приём, и детали записи, и переход на клиента… справа там вот эта вот галочка появляется. Но
там чуть-чуть надо подкорректировать вёрстку, посмотри, чтобы не сломалась она, потому что правая часть у
нас чуть сдвинется под эту галочку».

Файл: `apps/webapp/src/app/app/doctor/DoctorTodayNextAppointment.tsx`. Сделано ведущим — правка в одном
экране, отдельный исполнитель на неё не поднимается (CLAUDE.md, правило выбора исполнителя по объёму).

**Проверено до правки, ничего не теряется.** Модалка `TodayAppointmentFullModal` рендерит
`DoctorCalendarEventPanel`, в которой уже есть «Начать <приём>» / «Видеозвонок»
(`DoctorCalendarEventPanel.tsx:1246-1262`), «Изменить», «Удалить», и ссылка на пациента в заголовке
(`TodayAppointmentFullModal.tsx:122`).

- [x] Э5.1 Убраны кнопки «Детали записи» и «Начать <приём>»/«Начать созвон» из карточки.
- [x] Э5.2 Убрана ссылка на карточку клиента с имени пациента в шапке (имя осталось текстом).
- [x] Э5.3 Вся карточка — одна кнопка (`doctorInteractiveSurfaceButtonClass`), открывающая модалку;
      справа тот же общий шеврон `doctorStatCardChevronClass`, что и у КПИ-плиток из Э1.
- [x] Э5.4 Правая часть уехала под шеврон: содержимое лежит в
      `grid-cols-[minmax(0,1fr)_auto]`, имя пациента и «через N дней» остаются в своей колонке.
      Визуальную сверку на узком экране закрывает Э4.3 (живой прогон).
- [x] Э5.5 Состояния «нет записей» и «Сейчас на приёме» не тронуты.
- [x] Э5.6 Мёртвый после правки проп `videoMeetingsEnabled` убран по всей цепочке
      (`page.tsx` → `DoctorTodayDashboard` → `DoctorTodayNextAppointment`).

**Вопрос, который я задал и сам же закрыл по коду.** Кнопка умела превращаться в «Вернуться к звонку»
(`activeCall.returnUrl`), а в модалке такого возврата нет. Возврат есть на уровне оболочки:
`DoctorWorkspaceShell.tsx:138-140` монтирует `ActiveCallCoordinator` с `<DoctorActiveCallIndicator />`,
то есть во время звонка врач видит плавающее окно возврата на любой странице. Терять нечего.

## Э6. Отступы и радиусы с картинки — замер сделан, брать нечего

Владелец: «стиль кнопочек и отступы мне там нравится больше… если посчитать именно по пикселям и радиусы,
какие у той картинки, вот, её попробовать использовать».

Замер макета (скрипты в scratchpad сессии; рамка телефона 465px ≈ 375pt, масштаб 1.240 px/pt):

| | синяя кнопка | радиус | нижний отступ карточки | правый отступ |
|---|---|---|---|---|
| колонка «Сейчас» (нарисованное наше) | 37px ≈ 30pt | ~6px | 21px ≈ 17pt | 19px ≈ 15pt |
| колонка «Предложение» | 47px ≈ 38pt | ~6-7px ≈ 5-6pt | 21px ≈ 17pt | 18px ≈ 14.5pt |
| наш код сегодня | `size="sm"` = `h-9` = 36px | `--doctor-button-radius` = 8px | — | — |

Радиусы и отступы карточки на обеих половинках картинки совпадают с нашими в пределах погрешности JPEG.
Разница «кнопка в полтора раза выше» существует только между двумя рисунками: автор нарисовал нашу кнопку
меньше, чем она есть. Реальная разница — 38 против 36 пикселей. «Чище и просторнее» в правой колонке даёт
перестройка содержимого (иконки, однострочные ряды, фото, иерархия), а её владелец делать запретил.

- [ ] Э6.1 Ждём слова владельца. По умолчанию ничего не меняем: двигать общий токен высоты кнопки на весь
      doctor-UI ради двух пикселей — не улучшение.

## Записано, но НЕ делаем (решение владельца 14.09)

Кнопки «детали записи» / «начать приём» можно было бы показывать только когда запись вот-вот начнётся —
например, за 10-15 минут, и только если перед ней свободный слот. Владелец: «может быть, вообще их пока
давай не будем отображать, попробуем без этого сейчас, просто в уме держу». Значит сейчас кнопок нет
вовсе (Э5), а таймерная логика — записанная идея, не работа.

Фотографию пациента в блок следующей записи не добавляем — прямой запрет владельца.

## Э7. Коррекция по FAIL независимого аудита

Отчёт: `docs/_TODO/runs/kpi-filters-audit-20260914.md` (аудитор `gpt-5.6-sol high`, вердикт **FAIL**,
четыре находки). Все четыре — против пунктов ЭТОГО плана, новый скоуп не заводился.

- [x] Э7.1 (F1, самое серьёзное) **Фильтр, который нельзя снять.** КПИ-плитки скрыты целиком, когда у
      организации выключена статистика, а выбор из хранилища применялся всё равно: человек видел урезанное
      расписание и не имел ни одной кнопки, чтобы это отменить. Теперь без плиток фильтры не применяются
      вовсе — `activeKpiFilters` (`ScheduleCalendarTab.tsx:1002-1003`) подставляет пустой набор, а сам
      выбор остаётся в хранилище и оживает вместе со статистикой.
- [x] Э7.2 (F2) **Красный там, где ничего не прячут.** Кнопка «Выбирать несколько дней» в графике работы
      получила предупреждающий красный, хотя меняет только механику выделения ячеек. Вернул синий
      `DOCTOR_ACTIVE_FILTER_BUTTON_CLASS` (`ScheduleWorkTab.tsx:1596-1600`). Фильтр филиалов там же
      остаётся красным — он записи действительно прячет.
- [x] Э7.3 (F3) **Половина предупреждения.** У мобильной кнопки «Только: на сопровождении» краснел ободок,
      а звезда оставалась синей: `DoctorSupportStar` задаёт свой `text-primary`, и родительский цвет к ней
      не наследуется. Цвет звезды теперь идёт от того же состояния (`PatientsPageClient.tsx:678-681`).
- [x] Э7.4 (F4) **Пропущенный вызов в Э1.5.** Плитка «Отмены» в карточке пациента раскрывает панель
      деталей, но шеврона не имела, а подсказка про клик показывалась только при неявке — без неявок
      карточка была кликабельной вообще без признака. Добавлен общий `opensDetails`, из подсказки убрана
      самодельная стрелка «· детали ↓», которую он заменяет (`PatientTabRecords.tsx:792-805`).
- [x] Э7.5 `typecheck` EXIT=0 и `lint` EXIT=0 после коррекции, прогон ведущего без пайпа.
- [x] Э7.6 Повторный независимый аудит коррекции — `docs/_TODO/runs/kpi-filters-audit2-20260914.md`
      (Claude Opus 5, другой поставщик, чтобы не вышла самопроверка первого вердикта). Все четыре находки
      подтверждены закрытыми, ни один ранее подтверждённый пункт коррекция не сломала. Вердикт **FAIL** из-за
      одной новой находки, которую внесла сама коррекция, — F5 ниже.
- [x] Э7.7 (F5) **Шеврон обещал детали, которых нет.** Правка F4 поставила `opensDetails` и `onClick`
      безусловно, а панель отмен рендерится только при непустой истории — у пациента без отмен (обычный
      случай) плитка показывала шеврон и по нажатию не открывала ничего. Теперь и клик, и шеврон идут по
      наличию самих строк истории, той же формой, что у соседней плитки «Записей»
      (`PatientTabRecords.tsx:801-810`). `typecheck` EXIT=0, `lint` EXIT=0.
- [x] Э7.8 Третий узкий проход аудита по одной этой правке —
      `docs/_TODO/runs/kpi-filters-audit3-20260914.md`, вердикт **PASS**. Предикат клика и шеврона
      буквально совпал с предикатом панели; путь `isStale`/`fetchError` закрыт (там история пуста, а
      счётчик из шапки показывается без шеврона); переход карточки из кнопки в `article` ничего не теряет,
      застрявшей панели без способа закрыть — нет. `typecheck` EXIT=0, `lint` EXIT=0.

**Что аудит подтвердил и трогать не потребовалось:** композиция нескольких фильтров (`every`), приоритет
операторов в `displayableCalendarEvents` (не-записи всегда видны), поведение «Отмен» в обе стороны вместе
с перезагрузкой ленты, устойчивость чтения кэша к старому/битому значению, отсутствие второго шеврона и
сохранность сегментной карточки, отсутствие тестов и фотографий в диффе, неизменность верхних карточек
«Сегодня», вся Э5.

## Э8. Самодельный цвет активного фильтра — убран

Владелец 14.09: «второй пункт, надо было не спрашивать, а делать». Требование «сделано едиными элементами,
переиспользованием, никаких кастомных, чтобы не было везде написано по-разному» уже даёт право на такую
правку — это не новый скоуп и не вопрос.

Искал не одно названное аудитом место, а все: `grep 'border-primary text-primary'` по doctor-UI дал ровно
три попадания, из них одно — сам общий класс в `DoctorSchedulePeriodNav.tsx:15`, а два — копии.

- [x] Э8.1 `PatientTabRecords.tsx:567` — переключатель «показать отменённые» в модалке записей пациента
      берёт общий `DOCTOR_ACTIVE_FILTER_BUTTON_CLASS`.
- [x] Э8.2 `DoctorCatalogMobileToolbar.tsx:56` — кнопка фильтров мобильного каталожного тулбара берёт его же.
- [x] Э8.3 Цвет не менялся: обе копии означали ровно то же, что общий класс, поэтому это чистое сведение к
      одному источнику. Разница только в том, что появился общий hover-оттенок.
- [x] Э8.4 Синий, а не красный, осознанно: оба состояния записи не прячут. «Показать отменённые» наоборот
      их показывает, кнопка каталожного тулбара только открывает панель фильтров. Если владелец ответит по
      открытому вопросу «красный везде, где фильтр прячет записи», каталожная кнопка станет красной сменой
      одной ссылки на константу.
- [x] Э8.5 `typecheck` EXIT=0, `lint` EXIT=0.

## Вызовы `DoctorStatCard` (полный список для Э1.5)

`admin/analytics/PlatformAnalyticsPageClient.tsx`, `doctor/DoctorTodayDashboard.tsx`,
`doctor/DoctorTodayLeftKpiRow.tsx`, `doctor/DoctorTodayRightKpiRow.tsx`,
`doctor/analytics/activity/ActivityAnalyticsTab.tsx`,
`doctor/analytics/clients/AdminPlatformRegistrationStatsClient.tsx`,
`doctor/analytics/clients/AdminPlatformSubscriberStatsClient.tsx`,
`doctor/analytics/clients/DoctorAnalyticsAppointmentsSection.tsx`,
`doctor/analytics/clients/DoctorAnalyticsClientsPageClient.tsx`,
`doctor/analytics/finance/FinanceAnalyticsTab.tsx`,
`doctor/analytics/notifications/NotificationsAnalyticsClient.tsx`,
`doctor/analytics/records/RecordsAnalyticsTab.tsx`,
`doctor/material-ratings/MaterialContentStatsClient.tsx`, `doctor/patients/PatientsPageClient.tsx`,
`doctor/patients/[userId]/tabs/PatientTabFinances.tsx`,
`doctor/patients/[userId]/tabs/PatientTabOverview.tsx`,
`doctor/patients/[userId]/tabs/PatientTabRecords.tsx`,
`doctor/schedule/tabs/ScheduleCalendarTab.tsx`, `doctor/usage/ProductAnalyticsSection.tsx`.

## Открытые вопросы владельцу (не работа — вопрос)

- Э3.4: красный индикатор применён не только на расписании, но и на списке пациентов и в аналитике —
  там активный фильтр точно так же прячет часть записей. Если нужно только на расписании — скажи, сниму.
- ~~Находка второго аудита про самодельный цвет активного фильтра~~ — **это был не вопрос, а работа.**
  Владелец 14.09: «второй пункт, надо было не спрашивать, а делать». Его же требование «никаких кастомных,
  чтобы не было везде написано по-разному» уже даёт на это право; спрашивать было нечего. Закрыто в Э8.
