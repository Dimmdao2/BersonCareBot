# Кабинет пациента: карточка записи открывается, и из неё можно заплатить

**Источник скоупа — диктовка владельца 12.09.2026, 00:28, со снимком экрана.** Этот файл —
единственный источник «todo» и «done» по этому workstream. Находка аудитора, которой здесь нет
пункта, — вопрос владельцу, а не задача.

Дословно: «у нас говнище на странице пациента… мало того, что выглядит убого, так ещё и сломана.
Детали не открыть, то есть по идее я должен на неё ткнуть и открыть детали записи в модалке, как и у
врача. Но только те данные, которые нужны пациенту. А самое главное — как человек будет оплатить,
если он ссылку потерял, QR закрыл. Он записался, закрыл страницу, а там ссылка была или QR. А он в
кабинете или в приложении оплатить хочет. Очевидно, что нужен такой же путь, как у врача, только
оплатить. И у него должна быть пометка, что эта запись не оплачена, что бронь слетит через
столько-то. А сейчас у него этого прям в кабинете нету, там сразу написано типа бронь принята и всё.»

Владелец ушёл спать в 00:40 со словами: «делаешь сам, всё решаешь сам, что сделаешь неправильно —
переделаем утром. Но решай хорошо, если есть сомнения — иди смотри, как делают другие в мире».
Значит решения ниже приняты мной; каждое названо и обосновано, чтобы утром их было легко оспорить.

---

## Что уже есть (проверено по коду 12.09, не переделывать)

- Карточка предстоящей записи (`BookingUpcomingSection.tsx`) уже несёт бейдж статуса, ссылку
  «Оплатить» для `awaiting_payment`, действия «Перенести»/«Отменить» и выбор напоминаний.
- **Сегодня ночью приземлился S4** (`c0ec80e6b`): под карточкой `awaiting_payment` появился срок
  «Оплатить до …» в зоне филиала, а экраны `/book/pay` и `/app/patient/booking/pay` получили сумму,
  ссылку текстом, живой обратный отсчёт и QR на широком экране. На снимке владельца этого не видно
  потому, что обе его записи в статусе `confirmed`.
- Секция «Оплаты» переведена на человеческие слова (`0c9f80c66`) — она и была первым, на что
  владелец показал пальцем.
- У врача эталон — `DoctorCalendarEventPanel.tsx` и `AppointmentPaymentSection.tsx`: там уже решены
  срок, QR, «Отправить ссылку» и поведение истёкшего счёта.

## Что действительно сломано

1. **По карточке нельзя ткнуть.** Деталей записи у пациента нет вообще — ни модалки, ни страницы.
2. **QR из кабинета не достать.** Он есть только на экране оплаты, куда надо догадаться дойти по
   мелкой текстовой ссылке.
3. **Неоплаченность не кричит.** Бейдж «Ожидает оплаты» и текстовая ссылка одного веса с
   «Перенести»/«Отменить»; человек не понимает, что бронь слетит.
4. **Шапка врёт.** Над списком существующих записей стоит «Шаг 1 из 4» — это хром мастера новой
   записи, случайно накрывающий чужой контент.

## Решения (приняты мной, владельцу — как факт, оспаривать утром)

- **Детали — выезжающая снизу панель на телефоне и модалка на широком экране.** Так это устроено
  у всех, кто возит людей и лечит людей; у нас этот же приём уже применён в кабинете врача
  («Пациенты на телефоне», задача #130). Второго шаблона не заводим.
- **Деньги не прячутся за тап.** Кнопка «Оплатить» и срок остаются НА карточке, а не только внутри
  панели. Правило простое: действие, которое стоит человеку слота, не должно требовать лишнего
  касания.
- **QR — на широком экране показан, на телефоне под «Показать QR».** Владелец 11.09 решил «QR только
  на широком экране», а 12.09 сказал «он QR хочет открыть». Это не противоречие: по умолчанию QR не
  занимает телефонный экран, но всегда достаётся в одно касание. Разворачивать его на телефоне
  насильно — против первого решения; прятать совсем — против второго.
- **Внутри панели — только то, что нужно пациенту.** Когда, где (с адресом и ссылкой на карту), что
  за услуга, кто ведёт, сколько стоит, оплачено или нет. Ни внутренних статусов, ни чужих
  комментариев, ни идентификаторов.
- **Мёртвый счёт не платится ниоткуда.** Кнопка, ссылка и QR исчезают по тому же разбору, что уже
  стоит на экранах оплаты (`classifyPrepaymentBookingStatus`, `classifyPaymentIntentStatus`
  в `@/shared/lib/paymentStatusView.ts`). Третьего правила не заводим.

---

## Этапы

### P1. Карточка честно показывает неоплаченную бронь

- [x] P1.1 Неоплаченная запись видна как неоплаченная с первого взгляда: состояние, сумма и
      «осталось N» стоят вместе, а не бейджем в углу.
      — PASS живьём, аудит №2, телефон 390×844: «**Не оплачено** · 7 000,00 ₽ · осталось 1 ч 27 мин»
      одной строкой на тревожной подложке (`10-payable-page.png`).
- [x] P1.2 «Оплатить» — основное действие карточки, визуально сильнее «Перенести»/«Отменить».
      — PASS живьём: «Оплатить» — единственная залитая primary-кнопка карточки
      (`10-payable-page.png`).
- [x] P1.3 Счёт истёк → на карточке нет ничего платёжного, вместо этого причина. Источник разбора —
      существующий `paymentStatusView.ts`.
      — PASS живьём в трёх состояниях, платёжное исчезает во всех
      (`a[href*="/booking/pay"]` = 0): переход через дедлайн БЕЗ перезагрузки, отменённая запись,
      оплата наличными у врача (`20/22/23-*-card-page.png`).

### P2. Детали записи открываются по касанию

- [x] P2.1 Касание карточки открывает панель: снизу на телефоне, модалкой на широком экране.
      — PASS живьём: телефон — диалог `x=0 y=28 w=390 h=816` при вьюпорте 390×844 (панель снизу),
      широкий — `x=496 y=32 w=448` при 1440×900 (модалка). Измерено, а не на глаз.
- [x] P2.2 Внутри — когда, формат, услуга, специалист, адрес с ссылкой на карту (для очного),
      стоимость, состояние оплаты. Только пациентские данные.
      — PASS живьём + проверка на утечку: в HTML панели ноль внутренних статусов, все 46 UUID
      страницы прогнаны по трём таблицам — 0 строк чужого пациента.
- [x] P2.3 Действия внутри панели те же, что на карточке, без второго набора правил: оплатить,
      перенести, отменить, присоединиться к видеозвонку.
      **БЛОКЕР 12.09:** оплата/перенос/отмена переиспользованы, но доступный patient-контракт
      открывает видеозвонок только по `meetingId`, а загруженная запись несёт только
      `canonicalAppointmentId`; существующего чтения `appointmentId → meetingId` нет. Ограничения
      этого этапа запрещают новую дверь в БД/API, поэтому подставлять id записи как id встречи нельзя.
      Нужен выбор ведущего: отдельно расширить video-meeting read-контракт либо исключить действие из
      P2.3 до этапа связки записи со встречей.
      — PASS в исполнимой части (оплатить/перенести/отменить — тот же компонент и посимвольно
      тот же href, что на карточке). Видеозвонок остаётся объявленным блокером плана, см. развилку ниже.
- [x] P2.4 Клавиатура и скринридер: панель закрывается по Esc, фокус возвращается на карточку.
      — PASS живьём, только клавиатурой: Enter открывает, фокус внутри диалога, Esc закрывает,
      фокус возвращается на кнопку карточки (`50/51-keyboard-*.png`); то же на телефоне в пяти прогонах.

### P3. Ссылку и QR всегда можно достать заново

- [x] P3.1 В панели неоплаченной записи есть сумма, срок с живым отсчётом, кнопка «Оплатить» и сама
      ссылка текстом, которую можно скопировать.
      — PASS живьём: сумма, «Оплатить до …», убывающий отсчёт (1 ч 26 → 1 ч 25 мин), полная ссылка
      текстом и кнопка. Пройдено ДО КОНЦА: href отдал 307 на адрес счёта, а не заглушку.
- [x] P3.2 QR: на широком экране показан, на телефоне под «Показать QR».
      — PASS живьём числом: телефон — QR-картинка 0×0 до нажатия «Показать QR», 288×288 после;
      широкий — кнопки «Показать QR» нет вовсе (count=0), QR виден сразу.
- [x] P3.3 Ничего из перечисленного не показывается, если счёт истёк или запись отменена.
      — PASS живьём: дедлайн перейдён ПРИ ОТКРЫТОЙ панели без перезагрузки — секция, ссылка,
      кнопка, «Показать QR» и `img` исчезают разом (`21-deadline-panel-before/after.png`).

### P4. Шапка страницы перестаёт врать

- [x] P4.1 «Шаг 1 из 4» относится к мастеру новой записи, а не к списку существующих: хром мастера
      не стоит над чужими секциями.
      — PASS живьём: «Шаг 1 из 4» стоит НИЖЕ секций записей и непосредственно над выбором формата
      (`10-payable-page.png`, `40-wide-cabinet.png`).

---

## НЕ ДЕЛАЕМ в этом workstream

- Не переписываем мастер новой записи.
- Не трогаем экраны `/book/pay` и `/app/patient/booking/pay` — они приземлены сегодня и приняты
  независимым аудитом.
- Не заводим новых дверей в базу: панель показывает то, что уже отдают существующие маршруты.

## Проверка кандидата 12.09

- Webapp lint зелёный; typecheck не дошёл до общего зелёного из-за чужой ошибки
  `hlsStorageLayout.ts` → отсутствующий экспорт `isLegacyHotMediaSourceKey`.
- Live evidence не снят: единый `:5200` запущен из основного дерева
  `/home/dev/dev-projects/BersonCareBot`, а по `AGENTS.md` §1a воркер не переключает его на свою ветку и
  не поднимает второй Next. Снимки делаются после landing кандидата на общий runtime.

---

## Состояние на 12.09, 02:3x (ведущий)

Кандидат воркера `6acf89b00` лежит в `wt/patient-booking-card` и **не приземлён**. Независимый живой
аудит прошёл весь сценарий и снял полную матрицу скриншотов, но был оборван сторожем памяти
dev-сервера ровно на возврате runtime; отчёт спасён ведущим из сырого протокола —
`docs/_TODO/runs/patient-booking-card-audit-result.md`.

**Блокер приземления — не кандидат, а приземлённая дверь S4.** `/api/booking/payment-status` отвечает
500 любому пациенту (`Missing declared webapp port capability: tenant_service`), поэтому карточка
физически не может показать сумму и срок. Подтверждено ведущим дважды: внутрипроцессно и по живому
маршруту с настоящей пациентской сессией. Починка идёт этапом **S9** в
`docs/_TODO/APPOINTMENT_PREPAYMENT_VISIBILITY_2026-09-11.md`. Порядок: S9 приземляется → аудит
карточки перезапускается на том же кандидате → тогда закрываются P1–P4.

**Что уже видно живьём** (скриншоты аудитора, `/tmp/patient-booking-card-audit-20260912/`): при
живой двери карточка показывает «Не оплачено · 7 000,00 ₽ · осталось 1 мин» и сильную кнопку
«Оплатить»; касание открывает нижнюю панель на телефоне и модалку на широком экране со всеми
пациентскими данными, ссылкой текстом, кнопкой и «Показать QR»; «Шаг 1 из 4» больше не стоит над
списком записей.

**P2.3 частично невозможен как записано:** присоединения к видеозвонку из панели не будет без чтения
«запись → встреча», которого в коде нет (аудитор проверил порты, репозитории и API видеовстреч
отдельно). Остальная часть P2.3 — оплатить, перенести, отменить — сделана.

## Состояние на 12.09, 04:0x (ведущий): блокер снят, кандидат переведён на новую форму ответа

S9 приземлён в `feat` (`3a5ffb8f9`), дверь `/api/booking/payment-status` отвечает живому пациенту
`200`. Вместе с S9 форма успешного ответа стала ПЛОСКОЙ и объявлена один раз — тип
`BookingPaymentStatusOk` в `apps/webapp/src/shared/lib/paymentStatusView.ts`:
`intentId · amountMinor · currency · intentStatus · checkoutUrl · paymentDeadlineAt ·
appointmentStatus`. Полей `booking` и `summary.intent.*`, которые читал кандидат `6acf89b00`, больше
нет — на них карточка молча показывала бы «нет суммы, нет кнопки». Кандидат переведён на общий тип
ведущим в этой же ветке.

---

## Состояние на 12.09, 10:3x (ведущий): аудит №2 — PASS по всем пунктам

Независимый живой аудит (`claude-opus-5 high`, run `patient-booking-card-audit-2`, отчёт
`docs/audit/PATIENT_BOOKING_CARD_AUDIT_2_2026-09-12.md`) прошёл на собственном сервере клона
`:5303`, общий `:5200` не трогал. Вердикты: **P1.1–P4.1 — PASS живьём**, P2.3 — PASS в исполнимой
части. Уборка фикстуры доказана построчным `diff` с baseline (IDENTICAL по обеим строкам).

**Честная поправка аудитора к моему коммиту `e75b30461`** (вердиктов не меняет, но соврать здесь
нельзя): я мотивировал переход на `parseBusinessInstant` тем, что «строгий разбор даёт NaN». Форма,
которую реально отдаёт дверь — `2026-09-12 05:40:44.160439+03` — разбирается и `Date.parse`
(проверено аудитором в Node и в Chromium); NaN даёт только T-форма `…T05:40:44.160439+03`. Правка
по существу верна и поведение на экране правильное, но названный в сообщении отказ — не тот.

### Вопросы владельцу от аудита №2 (пунктов в плане нет — это НЕ задачи)

- [ ] **`/api/booking/payment-status` отвечает 500 на синтаксически неверном `bookingId`**
      (`?bookingId=not-a-uuid`). Стена цела: свой → 200, **чужой → 404**, несуществующий UUID → 404,
      пустой → 400. Течёт не данные, а форма отказа: тело 500 — страница ошибки Next, а не JSON.
      Маршрут приземлён этапом S9, к карточке отношения не имеет. Чинить или оставить?
- [ ] **Мастер новой записи не доходит до шага 3, и «Перенести» упирается в тот же тупик.**
      `/app/patient/booking/slot?...` отвечает 200 и сразу редиректится назад на список; экран выбора
      даты и времени человек не видит. Причину проглатывает пустой `catch {}` в
      `bookingCatalogRsc.ts:220` — в логе сервера нет ни строки. Кандидату не принадлежит (эти файлы
      он не трогал), в этом workstream пункта нет. Заводить отдельную работу? И убирать ли пустой
      `catch`, который прячет причину?
- [ ] **Порядок секций страницы изменился:** история посещений уехала выше мастера записи (кандидат
      перенёс все четыре секции в новый слот `beforeWizard`). План этого не просил и не запрещал.
      Так и оставить или вернуть историю вниз?
- [ ] **Со страницы убрана отдельная ссылка «Адрес кабинета»** — адрес теперь достаётся только
      изнутри панели. План просил адрес В ПАНЕЛИ и не просил убирать страничную ссылку. Оставляем?

### Что аудит НЕ проверял (перенесено сюда дословно, чтобы не считалось проверенным)

Видеозвонок в P2.3 (объявленный блокер); «мастер проходится до конца» (упёрлось в тупик шага 3);
настоящая оплата у провайдера (фикстура вела на безопасный `example.test`); завершение «Отменить»
(запись настоящего пациента, до `POST /api/booking/cancel` не доводилось); ветка панели для онлайн-
записи (у DEV-пациента все записи очные); настоящий скринридер (проверены клавиатура и фокус).
