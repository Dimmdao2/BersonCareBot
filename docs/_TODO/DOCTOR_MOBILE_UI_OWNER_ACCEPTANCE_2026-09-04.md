# Doctor mobile UI — owner acceptance checklist (2026-09-04)

**Ветка:** `feat/doctor-ui-rebuild`  
**Authority:** последовательные указания владельца в live-review 2026-09-04.  
**Статус:** active, требования продолжают дополняться владельцем.

Этот документ — единый чек-лист текущего прохода. В каждом пункте записано только последнее решение владельца:
несовместимые промежуточные варианты не сохраняются. Новая более поздняя коррекция владельца переписывает
соответствующий пункт здесь, а не добавляется рядом вторым вариантом.

`[ ]` — не принято; `[x]` — реализовано и доказано подходящей проверкой; визуальные требования закрываются только
живой проверкой mobile UI, а не тестами текста/классов. Общие компоненты расширяются вместо создания локальных
копий. Presentation-правки не покрываются новыми UI-тестами.

## A. Модалки упражнения и рекомендации

- [x] `EX-01` В верхнем блоке упражнения превью, первая строка рекомендаций и действия выровнены по верхнему краю,
      а не центрированы по высоте превью.
- [x] `EX-02` Кнопки статистики и редактирования находятся в первой строке, вертикально центрированы относительно
      неё и не перекрывают заметку специалиста.
- [x] `EX-03` Под верхним блоком есть одна лёгкая нижняя тень: заметнее текущего почти невидимого варианта, но
      слабее прежней выраженной тени; ориентир — тень верхней панели графика работы либо чуть бледнее неё.
- [x] `EX-04` Заметка специалиста ограничена двумя строками с многоточием.
- [x] `EX-05` Первая строка основной модалки: слева «Упражнение», справа ФИО пациента; название упражнения
      расположено отдельной второй строкой.
- [x] `EX-06` ФИО пациента в стандартной шапке модалки на 1 px меньше левого заголовка, имеет синий цвет и
      тонкое подчёркивание; это ссылка на «Обзор» карточки пациента.
- [x] `EX-06A` Статистика и рекомендации используют ту же шапку: тип модалки слева, ссылка-ФИО справа, название
      упражнения отдельной второй строкой.
- [x] `EX-07` Название упражнения во второй строке немного меньше заголовка модалки, но сохраняет текущее более
      плотное начертание.
- [x] `EX-08` Из списка упражнений открывается та же основная модалка, которая используется из комментариев на
      странице «Сегодня»; отдельного параллельного варианта деталей упражнения нет.
- [x] `EX-09` Старая промежуточная модалка с действиями «Прочитать все» и «Открыть обсуждение» удалена из пути
      пользователя после подтверждения отсутствия других нужных consumers.
- [x] `EX-10` В верхнем блоке основной модалки нет отдельной подписи «Рекомендации»: сразу показаны превью, строка
      параметров, заметка специалиста и действия.
- [x] `EX-11` Кнопки статистики и редактирования заметно крупнее прежнего варианта; для статистики используется
      иконка `chart-line`, визуально равная карандашу.
- [x] `EX-12` Рекомендации выводятся компактно: `15×2, 5 кг, Боль ≤ 6`; если подходы не заданы — `15 повт.`. Если
      нет ни параметров, ни заметки, выводится «Нет рекомендаций». Наличие заметки уже означает наличие рекомендаций.
- [x] `EX-13` Превью упражнения является рабочим видеопревью с play-состоянием; по нажатию открывается
      полноэкранное видео с обычным управлением воспроизведением.
- [x] `EX-14` В статистике над календарём по центру расположен заголовок «Отметки клиента» размером с имя клиента,
      ниже — календарь одного выбранного месяца и график за тот же месяц без отдельного заголовка «Динамика»;
      переключатели `7 / 30 дней` отсутствуют.
- [x] `EX-15` Переключение месяца едино для календаря и графика; календарь отмечает только дни выполнения именно
      этого упражнения.
- [x] `EX-16` График имеет читаемую левую шкалу значений и подписи дат; по нему понятно, что и в каком количестве
      измерено.
- [x] `EX-17` Во время загрузки календаря и графика по центру соответствующего блока показаны одинаково
      оформленные spinner и тексты «Загрузка календаря…» / «Загрузка графика…».
- [x] `EX-18` Из основной модалки удалён правый столбец с выполненной пациентом сложностью, повторами, подходами и
      весом; эти данные доступны через статистику.
- [x] `EX-19` В треде текст сообщения — 15 px; время — 12 px, более серое и снаружи пузыря у нижней границы: у
      своего правого пузыря слева, у чужого левого пузыря справа. Дата группы показана один раз по центру.
- [x] `EX-20` В чатах и обсуждениях нет постоянных кнопок «Ответить» под сообщениями; поле ответа использует общий
      мобильный chat composer.

## B. Непрочитанные сообщения и комментарии

- [x] `UNREAD-01` На плашке текущего этапа с новыми комментариями маленькая красная точка стоит точно на правом
      верхнем углу обводки.
- [x] `UNREAD-02` Точки непрочитанного компактнее прежних индикаторов.
- [x] `UNREAD-03` Кнопка сообщений при непрочитанных сохраняет красное состояние; дополнительная маленькая точка
      стоит на правом верхнем краю самой иконки-пузыря, а не на краю кнопки.
- [x] `UNREAD-04` В списке упражнений этапа название упражнения с новыми комментариями имеет более плотное
      начертание, а справа показан бейдж с точным количеством новых комментариев.
- [x] `UNREAD-05` KPI комментариев, группировка по пациентам, список упражнений и тред используют одну семантику
      unread и согласованные числа.
- [x] `UNREAD-06` Сценарий «у пациента 7 новых комментариев в нескольких упражнениях» показывает все затронутые
      упражнения, а сумма их unread равна 7; список не теряет упражнения после агрегации.
- [x] `UNREAD-07` Шапка обычного чата имеет заголовок «Сообщение» слева и стандартную ссылку-ФИО пациента справа.
- [x] `UNREAD-08` Открытие чата из KPI «Сообщения» помечает входящие прочитанными; после закрытия строка исчезает,
      а KPI пересчитывается без перезагрузки страницы.

## C. Визиты в обзоре пациента

- [x] `VISITS-01` Заголовок модалки использует общий двухчастный title contract: «Визиты» слева, ссылка
      `Фамилия Имя` справа, без двоеточия и без отчества.
- [x] `VISITS-02` Сводка отмен, переносов, поздних отмен и будущих визитов использует немного более плотный шрифт.
- [x] `VISITS-03` Под сводкой есть одна лёгкая нижняя тень.
- [x] `VISITS-04` Заголовки этой и соседних модалок используют общий `DoctorModal` title contract без локального
      размера/начертания.
- [x] `VISITS-05` KPI «Визитов» и «Абонемент / Без абонемента» используют одну компактную двухстрочную
      геометрию: верхние подписи выровнены, вторичная строка прижата к первой, а число центрировано по высоте.
- [x] `VISITS-06` Подпись ближайшей записи `След. ДД.ММ` использует semantic primary кабинета врача.
- [x] `VISITS-07` Модалки визитов, абонемента, заметок и задач, а также формы создания записи, абонемента,
      заметки и задачи из карточки пациента используют общую шапку с более плотной ссылкой `Фамилия Имя` справа.

## D. Страница «Сегодня»

- [x] `TODAY-01` Заголовки блоков «Следующий приём» и «Записи по неделям» используют одну общую роль заголовка.
- [x] `TODAY-02` Тап по графику не оставляет браузерный синий touch/focus-ободок.
- [x] `TODAY-03` Мобильные поля ввода кабинета врача имеют текст не меньше 16 px и не вызывают Safari zoom;
      высота полей следует общей шкале контролов.
- [x] `TODAY-04` Удаление видимого focus-ободка на touch не создаёт локальных разрозненных overrides и не лишает
      desktop keyboard navigation различимого `focus-visible` состояния.
- [x] `TODAY-05` В списке сопровождения на странице и в модалке удалены правые status-иконки прежнего варианта.
- [x] `TODAY-06` Если у пациента есть новые сообщения или комментарии, строка выделяется более плотным
      начертанием, а справа показан один бейдж с общим количеством новых элементов.
- [x] `TODAY-07` Нажатие на строку сопровождения открывает вкладку «Обзор» карточки этого пациента.
- [x] `TODAY-08` KPI сообщений, комментариев, задач и сопровождения обновляются в фоне в течение нескольких секунд
      и сразу при возврате фокуса в приложение.
- [x] `TODAY-09` В карточке ближайшей записи ФИО пациента расположено справа в первой строке как синяя
      подчёркнутая ссылка, дата/время и относительный срок делят вторую строку, а комментарий прижат ближе и
      ограничен двумя строками с многоточием. Доказательство: live Playwright 414×719,
      `/tmp/bcb-today-xr-final.png`; полное «Аминов Евгений» занимает `clientWidth=110`, `scrollWidth=110`.
- [x] `TODAY-10` В карточке «Записи по неделям» уменьшены верхнее и нижнее пустые пространства, а на коротком
      мобильном viewport вместо нечитаемо сплющенного графика используется существующая компактная сводка.
      Доказательство: live Playwright 414×719 — chart `display:block`; 375×548 — chart `display:none`, fallback
      `display:grid`, `/tmp/bcb-today-xr-final.png` и `/tmp/bcb-today-short.png`.

## E. Календарь: нижняя навигация и общая панель

- [x] `CAL-NAV-01` Прокручиваемая строка подразделов настроек удалена из верхней шапки.
- [x] `CAL-NAV-02` Порядок снизу вверх: глобальное нижнее меню → `Записи / График работы / Настройки` → белая
      прокручиваемая строка подразделов настроек.
- [x] `CAL-NAV-03` Над каждым уровнем нижней навигации есть своя лёгкая тень: глобальное меню, основные вкладки
      расписания и строка подразделов настроек; содержимое самих подразделов на этом этапе не переделывается.
- [x] `CAL-NAV-04` Кнопки выбора месяца в календаре и графике работы имеют одинаковые размер, шрифт, фон и радиус.
- [x] `CAL-NAV-05` Контролы графика работы белые, а не серые.
- [x] `CAL-NAV-06` Календарь, график работы и настройки используют стандартный doctor page container с теми же
      боковыми полями и верхней геометрией, что «Сегодня», «Обзор» и «ЛФК»; локальные full-bleed-компенсации их
      не растягивают и не обрезают.
- [x] `CAL-NAV-07` Прокрутка остаётся внутри стандартного контейнера и не проходит поверх содержимого панели.
- [x] `CAL-NAV-08` Текущая дата выделяется скруглённым прямоугольником канонической формы, не кругом и не овалом.
- [x] `CAL-NAV-09` Существующий вид одного дня сохраняется.

## F. График работы и форма расписания

- [x] `WORK-01` В форме «Задать расписание» подпись «Перерыв» находится над полями времени; начало, окончание и
      перерыв выровнены по одной сетке.
- [x] `WORK-02` В ячейке календаря нет текста `Обед 1`; остаются число, время и сокращённое название филиала.
- [x] `WORK-03` Плашка дня недели с сохранённым недельным шаблоном имеет заливку цветом филиала и показывает время
      работы; конкретные даты, унаследовавшие этот шаблон, имеют только обводку цветом филиала без заливки.
- [x] `WORK-04` Если на день заданы несколько филиалов, расписание отображается несколькими строками без потери
      времени или цвета каждого филиала.
- [x] `WORK-05` Конкретные даты, получившие расписание из недельного шаблона, показывают `12–19` строго одной
      строкой и сокращение филиала отдельной строкой; прямые настройки конкретной даты остаются с заливкой.
- [x] `WORK-06` Вместо ограниченного числа дат вроде `для всех сред (5 дней)` используется смысл постоянного
      расписания по выбранному дню недели.
- [x] `WORK-07` В футере шаблона «Очистить шаблон» слева, «Сохранить» справа; кнопки делят ширину поровну.
- [x] `WORK-08` Тап по сохранённому недельному шаблону открывает его модалку редактирования; промежуточного действия
      «Применить» в этом пути нет.

## G. Создание и редактирование записи — единая форма

- [x] `APPT-FORM-01` В модалке нет второго вложенного заголовка «Запись» и отдельного крестика.
- [x] `APPT-FORM-02` Подписи, поля и расстояния используют те же общие роли, что форма редактирования задачи.
- [x] `APPT-FORM-03` Справа от подписи «Пациент» есть заметное синее действие «Новый пациент».
- [x] `APPT-FORM-04` После выбора «Новый пациент» поиск по имени/телефону скрывается и вместо него показывается
      форма нового пациента.
- [x] `APPT-FORM-05` Имя нового пациента обязательно и отмечено `*`; отчество, телефон и email необязательны без
      приписки «если есть».
- [x] `APPT-FORM-06` Во вложенной форме пациента «Отмена» слева, «Сохранить и выбрать» справа; кнопки равной ширины.
- [x] `APPT-FORM-07` Выбор специалиста скрыт только когда серверные данные доказывают, что в клинике доступен ровно
      один специалист.
- [x] `APPT-FORM-08` У филиала и сеанса есть подписи; оба поля и остальные selectors занимают полную ширину.
- [x] `APPT-FORM-09` Длительность подставляется из выбранной услуги, остаётся редактируемой и подписана
      `Длительность, мин`.
- [x] `APPT-FORM-10` Заметка к записи необязательна без лишней поясняющей приписки.
- [x] `APPT-FORM-11` В стандартном футере «Отмена» слева, «Сохранить» справа; кнопки равной ширины.
- [x] `APPT-FORM-12` Режим «Изменить» переиспользует эту же форму с предзаполненными данными, а не отдельную
      параллельную форму.
- [x] `APPT-FORM-13` При редактировании доступны дата, время начала, длительность, филиал, услуга, статус и основной
      комментарий. Пациент уже существующей записи неизменяем, указан ссылкой-ФИО в шапке и не дублируется в теле
      формы.
- [x] `APPT-FORM-14` Новая запись, открытая из карточки пациента, уже привязана к этому пациенту: ссылка-ФИО
      находится в шапке, а поиск, смена и создание другого пациента в форме недоступны. Глобальная форма записи
      вне карточки сохраняет обычный выбор пациента.

## H. Список записей календаря

- [x] `APPT-LIST-01` Синяя отметка ближайшей записи состоит из одной верхней линии; нижней синей линии нет.
- [x] `APPT-LIST-02` Подпись «Следующее» полностью удалена.
- [x] `APPT-LIST-03` В строке показывается полное доступное ФИО пациента без искусственно различающегося вывода
      отчества.
- [x] `APPT-LIST-04` Реальный статус переноса или поздней отмены показывается справа; выдуманный статус ближайшей
      записи и подпись «Следующее» не выводятся.

## I. Детали записи

- [x] `APPT-DETAIL-01` В верхней строке дата и время с названием месяца словом; размер увеличен на 2 px без
      увеличения жирности.
- [x] `APPT-DETAIL-02` Иконки карандаша в верхней строке нет.
- [x] `APPT-DETAIL-03` Плашка фактического текущего статуса, включая «Подтверждена», сохраняется.
- [x] `APPT-DETAIL-04` Ниже даты показано имя пациента в текущем начертании; имя ведёт в карточку пациента.
- [x] `APPT-DETAIL-05` После пациента есть разделитель; ниже показаны филиал, услуга/сеанс и длительность.
- [x] `APPT-DETAIL-06` Кнопки звонка, чатов и других каналов связи из деталей записи удалены.
- [x] `APPT-DETAIL-07` Основной комментарий к записи один, расположен до блока оплаты и автоматически растёт по
      высоте; история отдельных staff-комментариев в этой карточке не создаётся.
- [x] `APPT-DETAIL-08` Нижний стандартный футер содержит три действия: «Изменить», «Отменить», «Создать визит».
- [x] `APPT-DETAIL-09` Отдельной кнопки «Перенести» нет; перенос выполняется изменением даты/времени через общую
      форму редактирования записи.
- [x] `APPT-DETAIL-10` Текст действия — «Создать визит», без слов «из записи».
- [x] `APPT-DETAIL-11` Первичный серверный payload деталей записи сразу содержит основной комментарий и сводку
      оплаты: модалка не показывает ложный прочерк и не догружает эти данные отдельными fetch, а немедленное
      открытие «Изменить» получает уже заполненный комментарий. Решение владельца 2026-09-05: «комментарий ... должен сразу формироваться, а не подгружаться дополнительным фетчем. Так же как и блок оплаты». Доказательство:
      merge `a86040b65`, интеграционная проверка `DoctorCalendarEventPanel.ui.test.tsx` — 11/11 PASS и
      `booking-calendar/service.unit.test.ts` — 8/8 PASS.

## J. Отмена записи и date/time picker

- [x] `CANCEL-01` Специальная половинчатая modal-вариация сейчас не вводится.
- [x] `CANCEL-02` «Отменить» открывает обычную модалку второго слоя без дополнительного blur/backdrop поверх уже
      открытого первого слоя.
- [x] `CANCEL-03` Заголовок — «Отмена записи»; ниже повторяется контекст записи: дата/время и пациент.
- [x] `CANCEL-04` Форма отмены содержит причину, отдельный комментарий к отмене и настройку отправки уведомления.
- [x] `CANCEL-05` Комментарий к отмене не заменяет основной комментарий записи.
- [x] `CANCEL-06` Отдельная форма переноса с обязательными «Начало/Окончание» удалена из пользовательского пути.
- [x] `PICKER-01` В общем date/time picker выбранная дата имеет каноническое скругление и semantic primary blue,
      согласованное с выбранным временем.
- [x] `PICKER-02` Стрелки выбора месяца используют semantic primary кабинета, без постороннего фиолетового цвета.
- [x] `PICKER-03` Сегодняшний день оформлен тем же приёмом, что календарь врача: канонический скруглённый маркер и
      согласованный цвет.
- [x] `PICKER-04` Нижняя кнопка «Применить» использует стандартную высоту, радиус и цвет doctor primary action.
- [x] `PICKER-05` В мобильном выборе времени часы `00–23` и минуты `00–55` с шагом 5 минут прокручиваются
      независимо; выбранные либо текущие значения при открытии находятся в центре видимой области.
- [x] `PICKER-06` На мобильном экране календарная часть и футер не прокручиваются вместе со временем;
      вертикальными scroll-owner остаются только две колонки часов и минут.

## J1. Задачи — последние owner-коррекции 2026-09-05

- [x] `TASK-01` В форме редактирования уже привязанный пациент неизменяем и показан только ссылкой-ФИО в шапке;
      у непривязанной задачи пациента можно добавить, а пустая шапка не получает заглушку.
- [x] `TASK-02` В редактировании вместо «Отмена» слева находится «Удалить»; удаление требует подтверждения с
      названием задачи. Закрытие модалки без сохранения оставляет задачу без изменений.
- [x] `TASK-03` Пустое название не сохраняется: поле получает красную границу, а «Заполните это поле» показано
      красным текстом на белом фоне.
- [x] `TASK-04` Срок без времени хранится как конец выбранного календарного дня и до `23:59:59.999` этого дня не
      становится просроченным и не исчезает из списка внимания.
- [x] `TASK-05` Для срока сегодня дата/время остаются обычного тёмного цвета; отдельная нижняя подпись «Сегодня»
      синяя. Это одинаково в списке задач, KPI-модалке и деталях задачи.
- [x] `TASK-06` После создания, изменения, выполнения или удаления задачи список, KPI и точки мобильной/desktop
      навигации обновляются без полной перезагрузки; наступление срока обновляет индикатор само.
- [x] `TASK-07` В списке справа максимум три строки метаданных в стабильном порядке: срок, «Просрочено»/«Сегодня»,
      «Важно!». При отсутствии срока «Важно!» остаётся нижней строкой; левая часть обрезает длинное описание.
- [x] `TASK-08` Разделители обычных задач совпадают с общими list-разделителями; у просроченной строки менее яркая
      красная граница есть и сверху, и снизу.
- [x] `TASK-09` Если просроченных задач нет, но есть задачи на сегодня, индикатор задач в мобильной и desktop
      навигации синий; при любой просроченной задаче он красный. KPI «Задачи» на странице «Сегодня» в состоянии
      только сегодняшних задач имеет синий бордер и синюю первую цифру без красной заливки; просрочка переводит
      KPI в красное состояние.
- [x] `TASK-10` KPI «Задачи» в карточке пациента открывает полный активный список задач этого пациента, а не
      срез «сегодня или просрочено» со страницы «Сегодня»; новая задача остаётся жёстко привязана к пациенту из
      шапки.

## J2. Список календаря и сегодняшний день графика работы — owner-коррекции 2026-09-05

- [x] `CAL-LIST-05` Кнопка «Сегодня» в режиме списка плавно прокручивает к сегодняшнему дню либо ближайшей будущей
      записи, насколько позволяет конец списка.
- [x] `CAL-LIST-06` Ближайшая запись имеет только синюю верхнюю линию; жёлтой заливки нет. Фон и граница обычной
      записи используют цвет её филиала, прошедшая запись лишь приглушается.
- [x] `CAL-LIST-07` Заголовок месяца расположен по центру на белом фоне, имеет увеличенные вертикальные поля и
      тонкий серый разделитель сверху; нижний остаток списка тоже белый.
- [x] `CAL-LIST-08` Пустые значения фильтров называются «Все локации» и «Все услуги».
- [x] `WORK-09` Сегодняшняя дата в графике работы полностью заменяет цвет филиала канонической кирпичной заливкой
      кабинета врача с непрозрачностью 90%; рамка филиала не просвечивает, весь текст белый.

## K. Деньги, абонементы и оплата записи — отдельный рискованный workstream

Этот раздел не смешивается с presentation-проходом. Реализация запускается через orchestration port отдельным
сильным worker после трассировки существующих contracts; деньги, entitlement и отправка платёжных ссылок требуют
независимого аудита до landing.

### K1. Цена и условия оплаты в записи

- [ ] `PAY-APPT-01` В форме создания записи врачом после услуги и длительности показана стоимость; исходное
      значение берётся из выбранной услуги и сохраняется снимком в самой записи.
- [ ] `PAY-APPT-02` Врач может изменить длительность и стоимость независимо для конкретной записи. Смена услуги
      подставляет цену новой услуги; уже сохранённый ручной снимок цены не меняется самопроизвольно.
- [ ] `PAY-APPT-03` В форме создания и редактирования врач видит условие оплаты: без предоплаты, процент
      предоплаты либо полная предоплата. Исходное значение берётся из политики услуги/клиники, но врач может
      переопределить его для конкретной записи.
- [ ] `PAY-APPT-04` Пациент при самостоятельной записи не может менять стоимость, длительность по финансовому
      расчёту или условие предоплаты; он видит зафиксированные клиникой значения.
- [ ] `PAY-APPT-05` Ссылка или QR-код для оплаты не создаются внутри формы врача: форма сначала сохраняет запись,
      а выставление счёта выполняется из деталей уже созданной записи.
- [ ] `PAY-APPT-06` Детали записи показывают общую стоимость, требуемую предоплату в рублях и процентах,
      фактически оплаченную сумму, текущий платёжный статус и срок оплаты. Из деталей врач может создать/повторно
      получить ссылку и QR-код на оплату.

### K2. Ожидание и завершение предоплаты

- [ ] `PAY-APPT-07` Если для записи требуется предоплата, и пациентская, и созданная врачом запись появляются в
      календаре со статусом `Ожидает оплаты`; слот на это время временно занят.
- [ ] `PAY-APPT-08` В настройках записи врача/клиники задаётся срок ожидания предоплаты. Для новой клиники значение
      по умолчанию — 20 минут; врач может установить любой требуемый срок без искусственного продуктового лимита.
      Пациент видит точный дедлайн либо оставшееся время.
- [ ] `PAY-APPT-09` После самостоятельной записи пациент сразу получает явное действие перехода к оплате; ссылка
      также отправляется только через реально доступные и подтверждённые каналы действующего механизма уведомлений.
- [ ] `PAY-APPT-10` Успешный webhook оплаты атомарно переводит запись из ожидания в оплаченный/подтверждённый
      статус. Повторный webhook идемпотентен и не создаёт вторую оплату.
- [ ] `PAY-APPT-11` Если предоплата не поступила до дедлайна, бронь автоматически освобождается, а запись получает
      однозначный истёкший/отменённый статус согласно действующей модели; просроченное ожидание не продолжает
      занимать слот.
- [ ] `PAY-APPT-12` Изменение услуги, стоимости или условия предоплаты до оплаты корректно обновляет снимок и
      платёжное требование. После состоявшейся оплаты финансовые значения не переписываются молча.

### K3. Статусы списка и календаря

- [ ] `PAY-APPT-13` В режиме списка справа в верхней строке статус ожидания показан фиолетовым как
      `● Ожидает оплаты`; используется заметный круглый маркер, а не текстовая точка.
- [ ] `PAY-APPT-14` В календарной сетке ожидающая оплату запись сохраняет основной цвет филиала, но получает
      единый фиолетовый status-border и круглый маркер у правого края; оформление одинаково во всех видах
      календаря и не заменяется локальными оттенками.
- [ ] `PAY-APPT-15` Отмена в верхней правой строке списка показана красным одним недублирующимся статусом:
      `Отменена пациентом`, `Отменена клиникой`, `Поздняя отмена пациентом` либо `Поздняя отмена клиникой`.
- [ ] `PAY-APPT-16` Нижняя строка отменённой записи выводит только реально случившееся финансовое последствие:
      `Предоплата возвращена`, `Предоплата удержана` либо конкретный штраф. Если денежного последствия не было,
      дополнительная строка не показывается.
- [ ] `PAY-APPT-17` Цвет текста и маркера каждого статуса берётся из общей semantic status-палитры; отмена —
      destructive, ожидание оплаты — единый payment-pending token. Цвета не хардкодятся отдельно по экранам.

### K4. Данные, права и приёмка

- [ ] `PAY-APPT-18` Снимок стоимости, условие/сумма предоплаты, дедлайн и платёжный статус имеют один канонический
      write-path; роли врача, пациента, системной процедуры и webhook имеют только необходимые права чтения и
      записи через центральную схему grants.
- [ ] `PAY-APPT-19` Денежные суммы хранятся и сравниваются без floating-point ошибок; изменение цены и частичная
      предоплата покрыты доменными тестами, а не тестами наличия кнопок или текста.
- [ ] `PAY-APPT-20` Приёмка проходит полные сценарии на TEST: запись врачом без предоплаты, с процентной и полной
      предоплатой; самостоятельная запись пациента; оплата ссылкой/QR; webhook; повтор webhook; истечение срока;
      изменение неоплаченной записи; отображение ожидания и вариантов отмены в списке и календаре.
- [ ] `PAY-APPT-21` Выбор платёжного провайдера и реквизиты YooKassa полностью перенесены из календаря во
      владеющий раздел `Настройки клиники → Платёжные настройки`; старый экран, дублирующий поля, удалён.
- [ ] `PAY-APPT-22` Секретный ключ провайдера хранится только серверно и после сохранения не возвращается в UI;
      интерфейс показывает лишь факт настройки и безопасные несекретные данные магазина.
- [ ] `PAY-APPT-23` В настройках записи нет платёжных ключей. Там остаются услуги и их стоимость/предоплата,
      специалисты и доступные им услуги, поля формы записи, срок ожидания предоплаты и остальные правила записи.

- [x] `MONEY-01` Воспроизвести и локализовать, почему добавление/сохранение абонемента сейчас не работает.
- [x] `MONEY-02` Проверить существующую модель цены, paid amount, способа оплаты и состояния абонемента; не создавать
      вторую модель рядом.
- [x] `MONEY-03` Две независимые строки «Цена» и «Оплачено» удалены. После создания абонемента пользователь явно
      выбирает поддержанный способ продажи/оплаты через существующий payment contract.
- [x] `MONEY-04` Наличная оплата использует существующий server-authorized cash contract.
- [x] `MONEY-05` Онлайн-оплата переиспользует существующие invoice/pay-link/provider contracts и определяет
      доступные QR-код и ссылку на оплату.
- [x] `MONEY-06` Блок оплаты записи виден только при доступности функции оплаты по тарифу/entitlement клиники.
- [x] `MONEY-07` В неоплаченной записи доступно единое действие «Принять оплату», открывающее следующий modal-layer.
- [x] `MONEY-08` В оплате доступны только реально поддержанные варианты: отметить наличную оплату, показать QR,
      скопировать платёжную ссылку и отправить её через доступный пациенту подключённый канал.
- [x] `MONEY-09` Каналы отправки не выдумываются UI: push, внутренний чат, SMS, Telegram, Max и email показываются
      только при существующем delivery contract и доступности конкретному пациенту.
- [x] `MONEY-10` Текущий серый нерабочий payment UI не объявляется готовым; worker даёт точный trace от действия до
      provider/payment state и обратно в карточку записи/абонемента.
- [x] `MONEY-11` Независимый аудитор проверяет реальные деньги, tenant isolation, entitlement, идемпотентность,
      корректность статусов и отсутствие ложного UI до landing.
- [x] `MONEY-12` Исправить отдельным изолированным workstream путь acquiring webhook: после фактического списания
      callback обновляет конкретный платёж из `pending` в итоговый статус через узкую server-authorized дверь,
      остаётся tenant-safe и идемпотентным; широкий relation-доступ классу `tenant_service` не выдаётся. Решение
      владельца 2026-09-05: «Запусти отдельный поток в отдельной ветке на фикс этой ошибки. Код аудит фикс по
      правилам». Доказательство: merge `a1e5828f2`, независимый audit `495044673` (kill-set 27/27,
      fault-injection 6/6), DEV `migrate-dev.sh --preflight` и `--execute` — PASS.
- [ ] `MONEY-13` Тот же дефект у ОБЩЕГО эквайрингового callback'а брони `/api/payments/webhook/[provider]`:
      успешная тестовая оплата ЮKassa на 7 000 ₽ по записи `f92ec4bb-2913-470a-a522-7851bb14ec2d` (TEST,
      2026-09-05) три раза получила HTTP 400, платёж записи навсегда остался `pending`. Обе половины
      маршрута не доходили до БД: резолвер клиники шёл отношением под bootstrap-принципалом (возможности
      `pre_session` в каталоге нет), проведение — под организационным (`tenant_service` без сквозной
      двери). Кандидат `wt/webhookfix`: резолвер переведён на объявленный pre-session корень, проведение —
      на один statement-атомарный корень `app.settle_booking_payment_webhook_event`, идемпотентный на
      повторе провайдера; широкий relation-доступ классу `tenant_service` не выдан. Открыто до
      независимого аудита и живой проверки: DEV rollback-only preflight PASS (`migrate-local.mjs
      --rollback-only`, pending=1), `migrate-dev.sh --execute` и живой платёж на TEST ещё не проводились.

## L. Быстрые действия по выделению времени в календаре

- [x] `CAL-ACTION-01` Одиночный тап, long press или drag по пустой сетке сначала сохраняет видимое выделение
      времени и открывает рядом контекстное меню; форма новой записи автоматически не открывается.
- [x] `CAL-ACTION-02` Контекстное меню переиспользует существующий мобильный popover/menu pattern (как действия
      телефона в карточке пациента) и размещается сверху или снизу выделения в зависимости от свободного места.
- [x] `CAL-ACTION-03` Если выделены только рабочие часы, меню содержит «Новая запись» и «Добавить перерыв».
- [x] `CAL-ACTION-04` Если выделен только интервал, недоступный для записи — перерыв, закрытый слот или нерабочее
      время, — меню содержит «Открыть для записи» и «Новая запись».
- [x] `CAL-ACTION-05` Если выделение одновременно пересекает рабочие и недоступные для записи интервалы, меню
      содержит «Добавить перерыв», «Открыть для записи» и «Новая запись».
- [x] `CAL-ACTION-06` «Добавить перерыв» записывает выбранный интервал в действующий график работы и обновляет
      календарь без второго механизма расписания.
- [x] `CAL-ACTION-07` «Открыть для записи» делает доступным только выбранный интервал, снимая перерыв либо
      расширяя рабочие часы через действующий scheduling contract.
- [x] `CAL-ACTION-08` «Новая запись» открывает общую форму записи с выбранными датой, временем начала и
      длительностью.
- [x] `CAL-ACTION-09` Слово «Перерыв» не выводится внутри календарной сетки; визуально перерыв совпадает с
      нерабочим временем до/после рабочего дня, без более тёмной заливки.
- [x] `CAL-ACTION-10` Нельзя создать противоречивый интервал: worker проверяет границы рабочего времени,
      существующие записи и пересечения до mutation.
- [x] `CAL-ACTION-11` Тап по сетке вне активного выделения закрывает контекстное меню и полностью снимает синее
      выделение; он не оставляет прежний интервал без меню.
- [x] `CAL-ACTION-12` В кабинете специалиста «Открыть для записи» всегда меняет график только текущего
      специалиста: специалист не показывается и не выбирается ни в каком виде. При нескольких доступных филиалах
      открывается стандартная модалка «Добавить рабочие часы» только с выбором филиала; при одном филиале часы
      сохраняются сразу. Настройка графиков других специалистов относится к отдельному интерфейсу администратора.
      Текущая модель сознательно ограничена одним филиалом и одним диапазоном рабочих часов специалиста на дату.

## M. Список клиентов и фильтры

- [x] `CLIENTS-01` Активный фильтр обозначается semantic-primary синей обводкой/состоянием той же системы, что
      активные фильтры календаря; после сброса кнопка возвращается в обычное состояние.
- [x] `CLIENTS-02` Из правой части строки клиента удалены иконки программы/упражнений и абонемента.
- [x] `CLIENTS-03` В строке остаётся календарная иконка с количеством записей.
- [x] `CLIENTS-04` Для пациента на сопровождении синяя звезда показана сразу после ФИО, как в коммуникациях.
- [x] `CLIENTS-05` Звезда вертикально центрирована относительно строки ФИО, а не прижата к нижнему краю текста.

## N. Вкладка «Файлы» карточки пациента

- [x] `FILES-01` Удалён текст-заглушка про будущий AI-разбор PDF, выписок и анализов.
- [x] `FILES-02` Под шапкой пациента расположен единый белый блок «Файлы и медиа» с общим section-title style.
- [x] `FILES-03` Если файлов нет, внутри блока по центру показано только «Файлов нет».
- [x] `FILES-04` В мобильном виде справа от заголовка находятся три понятных действия: камера, медиатека,
      документ. В desktop остаётся обычная кнопка «Загрузить файл».
- [x] `FILES-05` Камера открывает компактный выбор «Фото / Видео», затем соответствующий capture-mode устройства.
- [x] `FILES-06` Иконка изображения сразу открывает медиатеку без предложения использовать камеру.
- [x] `FILES-07` Иконка документа с плюсом открывает системный выбор файлов, а не медиатеку.
- [x] `FILES-08` Загруженные файлы и медиа отображаются общим прокручиваемым списком внутри блока.
- [x] `FILES-09` Вкладка занимает доступную высоту между шапкой/табами и нижними панелями; сама страница не
      прокручивается, прокручивается только внутренний список.
- [x] `FILES-10` Тап по строке открывает стандартную модалку предпросмотра и фактической информации о файле.
- [x] `FILES-11` Реализация переиспользует существующие upload/media/file contracts и desktop file block, если они
      уже существуют; отдельное хранилище или upload path не создаются.

## O. Вкладка «Учётка» карточки пациента

### O1. Общая композиция и личные данные

- [x] `ACCOUNT-01` Карандаш удалён из строки ФИО в общей шапке карточки пациента.
- [x] `ACCOUNT-02` Заголовки блоков вкладки используют тот же общий section-title style, что «Динамика симптомов»,
      «Программы ЛФК», блоки «Сегодня» и новый блок «Файлы и медиа».
- [x] `ACCOUNT-03` В блоке «Личные данные» текстовое действие «Редактирование» удалено; справа в заголовке остаётся
      одна иконка карандаша.
- [x] `ACCOUNT-04` Карандаш открывает стандартную модалку редактирования личных данных, а не inline-форму.
- [x] `ACCOUNT-05` В стандартном нижнем футере модалки «Отмена» слева и «Сохранить» справа.
- [x] `ACCOUNT-06` В личных данных показаны одно каноническое ФИО, дата рождения и пол; телефон, email, вес и
      дублирующее display-name представление отсутствуют.
- [x] `ACCOUNT-07` ФИО формируется из действующих структурированных фамилии, имени и отчества; скрытый второй
      вариант ФИО и пояснение о видимости для специалиста/пациента удалены.
- [x] `ACCOUNT-08` Date picker даты рождения сохраняет текущую точную дату; отдельный month-only режим не вводится.
- [x] `ACCOUNT-09` Подписи и значения вкладки приведены к общей мобильной шкале, не остаются в устаревшем мелком
      варианте.

### O2. Контакты и каналы

- [x] `CONTACTS-01` Текстовое действие «Добавить» в заголовке удалено как неработающее и непонятное.
- [x] `CONTACTS-02` Показываются только фактически существующие контакты/привязанные каналы; непривязанные Max,
      Telegram и другие каналы не рендерятся.
- [x] `CONTACTS-03` Основной телефон отображается как подпись «Основной телефон» и значение без фразы
      «не редактируется».
- [x] `CONTACTS-04` У существующего контакта подтверждение обозначается компактным статусом/галочкой; слово
      «подключён» не используется.
- [x] `CONTACTS-05` Telegram показывает полезный username/deep-link, если он известен; технический Telegram ID
      врачу не показывается.
- [x] `CONTACTS-06` Max использует принятую в кабинете букву `M`, а не иконку телефона; технический Max ID не
      показывается.
- [x] `CONTACTS-07` Статус blocked показывается только если он уже надёжно хранится в едином contract; новая
      эвристика блокировки не создаётся.
- [x] `CONTACTS-08` Легенда «подключён / активная и кликабельная / проблема подсвечена» удалена.
- [x] `CONTACTS-09` Дополнительный телефон и email можно добавлять существующими действиями; копирование телефона
      не дублируется отдельным новым механизмом.
- [x] `CONTACTS-10` Email считается подтверждённым после хотя бы одного успешного подтверждения/входа кодом,
      отправленным на этот адрес. До первого успешного использования кода состояние — «не подтверждён»;
      `unknown` в UI не показывается. Реализация трассируется до существующего auth/email-verification contract,
      а не вычисляется по одному факту отправки письма.
- [x] `CONTACTS-11` В строке email нет бессмысленной стрелки навигации.
- [ ] `CONTACTS-12` Действие внутреннего чата из обзора карточки пациента открывает тот же канонический чат,
      который используется в «Коммуникациях»; старая вложенная карточка переписки и отдельный legacy-вариант
      чата из этого пути удалены.
- [ ] `CONTACTS-13` Действия Telegram и Max не открывают внутренний чат. Они открывают соответствующий внешний
      мессенджер через достоверный deep link либо браузерную ссылку; технический ID не превращается в выдуманный
      публичный URL, а при отсутствии пригодного адреса действие не обещает недоступный переход.

### O3. Доступ к аккаунту

- [x] `ACCESS-01` Дубли Telegram-бота и Max удалены из нижнего блока; состояние каналов остаётся только в
      «Контактах и каналах».
- [x] `ACCESS-02` Отдельный заголовок нижнего action-блока не выводится.
- [x] `ACCESS-03` В белом блоке остаются две равные по ширине кнопки: «Заблокировать» слева и «В архив» справа.
- [x] `ACCESS-04` Действия переиспользуют существующие block/archive contracts и подтверждения; новый backend path
      не создаётся.

## P. Общий стек мобильных модалок

- [x] `MODAL-01` Blur/backdrop создаёт только первая открытая модалка; второй и последующие слои, включая
      обсуждение, статистику, рекомендации и отмену записи, не добавляют новое затемнение.
- [x] `MODAL-02` Вложенная модалка выезжает снизу тем же переходом и остаётся поверх предыдущей; закрытие
      возвращает пользователя к сохранённому состоянию нижнего слоя.
- [x] `MODAL-03` Нижние действия модалок используют единый footer с корректной mobile web-app safe area; локальные
      копии панели кнопок не создаются.

## P1. Комментарии ЛФК и единая модалка упражнения

- [x] `LFK-COMMENTS-01` В карточке пациента действие «Комментарии» на вкладке ЛФК открывает модалку с заголовком
      «Комментарии к ЛФК», синим ФИО пациента справа и названием этапа под шапкой.
- [x] `LFK-COMMENTS-02` В модалке перечислены только упражнения, где есть хотя бы одно сообщение; строка
      переиспользует представление KPI «Сегодня»: превью, название, последнее сообщение и unread-бейдж.
- [x] `LFK-COMMENTS-03` Название упражнения с непрочитанными сообщениями немного плотнее; прочитанное остаётся
      обычного начертания; unread-бейдж показывает точное число.
- [x] `LFK-COMMENTS-04` Тап по любой строке открывает существующую единую модалку упражнения со статистикой,
      рекомендациями, видео и чатом; старый desktop split/right-panel в мобильном пути не используется.
- [x] `LFK-COMMENTS-05` На странице «Комментарии» нижнего меню тап по пациенту открывает ту же модалку
      «Комментарии к ЛФК», а тап по упражнению — ту же единую модалку упражнения.
- [x] `LFK-COMMENTS-06` Отправка комментария из пути карточка пациента → ЛФК → комментарии успешно сохраняет
      сообщение в DEV и немедленно показывает его в треде без утечки внутренней ошибки.

## P2. Контейнер вкладки ЛФК

- [x] `LFK-LAYOUT-01` Вкладка ЛФК использует те же боковые поля, доступную ширину и базовую типографику, что
      «Обзор» и соседние вкладки карточки пациента, на iPhone XR и типовом Android viewport.
- [x] `LFK-LAYOUT-02` Переход на вкладку ЛФК и обратно не изменяет контейнер, поля или масштаб содержимого
      вкладки «Обзор»; layout-состояние не протекает между вкладками.

## P3. Карточка пациента: записи, заметки и контекст модалок

- [x] `PATIENT-OVERVIEW-01` KPI «Визиты» переименован в «Записи» и считает элементы истории записей; следующая
      запись остаётся синей подсказкой.
- [x] `PATIENT-OVERVIEW-02` Модалка «Записи» показывает компактную сводку и по умолчанию скрывает отменённые;
      `FunnelX` включает их, а отменённые дата и время отображаются красными и зачёркнутыми.
- [x] `PATIENT-OVERVIEW-03` Тап по строке открывает существующие канонические детали записи следующим слоем;
      добавление записи использует уже выбранного пациента и после изменения обновляет список без перезагрузки.
- [x] `PATIENT-OVERVIEW-04` Заметки идут от старых к новым, дата стоит слева сверху, текст — справа; действие
      «Новая заметка» находится в нижней панели, а форма не вызывает клавиатуру при открытии и растёт вверх на
      мобильном экране.
- [x] `PATIENT-OVERVIEW-05` В модалке задач пациента удалена ссылка «Все задачи», оставлено только действие
      «Новая задача».
- [x] `PATIENT-OVERVIEW-06` Модалки, открытые внутри карточки пациента, показывают ФИО чёрным полужирным текстом
      без ссылки обратно на уже открытую карточку; название этапа или клинической сущности в такой шапке синее.
- [x] `PATIENT-OVERVIEW-07` Название активной программы ЛФК выделено общим кирпичным цветом зоны доктора, без
      локального цветового значения.

## P4. Карта пациента: симптомы, анамнез и приёмы

### P4.1. Симптомы и диагнозы

- [ ] `CLINICAL-BLOCK-01` На вкладке «Карта» симптомы находятся в отдельном белом блоке «Симптомы».
- [ ] `CLINICAL-BLOCK-02` На вкладке «Карта» диагнозы находятся в следующем отдельном белом блоке «Диагнозы».
- [ ] `CLINICAL-BLOCK-03` На странице создания и редактирования приёма симптомы находятся в отдельном белом блоке
      «Симптомы».
- [ ] `CLINICAL-BLOCK-04` На странице создания и редактирования приёма диагнозы находятся в отдельном белом блоке
      «Диагнозы»; объединённого блока «Симптомы и диагнозы» нет.
- [ ] `CLINICAL-HEADER-01` В блоке симптомов заголовок «Симптомы» расположен слева в верхней строке блока.
- [ ] `CLINICAL-HEADER-02` Справа в заголовке симптомов расположена icon-only кнопка добавления `FilePlus2`;
      текстовой кнопки `+ Симптом` нет.
- [ ] `CLINICAL-HEADER-03` В блоке диагнозов заголовок «Диагнозы» расположен слева в верхней строке блока.
- [ ] `CLINICAL-HEADER-04` Справа в заголовке диагнозов расположена icon-only кнопка добавления `HeartPlus`;
      текстовой кнопки `+ Диагноз` нет.
- [ ] `CLINICAL-HEADER-05` Кнопка «История» в каждом блоке находится справа от заголовка и левее icon-only кнопки
      добавления.
- [ ] `CLINICAL-HEADER-06` Неактивная «История» имеет обычный нейтральный вид; активная получает стандартные синие
      обводку, текст и лёгкий фон общего doctor-фильтра.
- [ ] `CLINICAL-HEADER-07` Старые кнопки-плюсы у названий разделов и шильдики «Актуальные», «Текущие», «Снятые»
      удалены; переключение закрытых записей выполняет одна кнопка «История».
- [ ] `CLINICAL-ROW-01` Внутри каждого белого блока записи образуют плоский список от внутреннего левого края до
      внутреннего правого края блока.
- [ ] `CLINICAL-ROW-02` Соседние записи разделены одной стандартной серой линией flat-list.
- [ ] `CLINICAL-ROW-03` У отдельной записи нет собственной серой плашки, карточки, дополнительного бордера или
      двойного внутреннего отступа.
- [ ] `CLINICAL-ROW-04` В строке отдельного симптома или диагноза нет карандаша; вся строка является точкой входа
      в просмотр деталей.
- [ ] `CLINICAL-SYMPTOM-01` Название актуального симптома выводится первым обычным чёрным текстом и использует всю
      доступную ширину строки.
- [ ] `CLINICAL-SYMPTOM-02` Справа от названия симптома не резервируется пустая колонка под иконку или метаданные.
- [ ] `CLINICAL-SYMPTOM-03` У ключевого актуального симптома красный `!` расположен в фиксированном левом поле
      элемента списка, визуально относится ко всему элементу и не сдвигает начало основного текста.
- [ ] `CLINICAL-SYMPTOM-04` У обычного актуального симптома и у любого исторического симптома красного `!` нет.
- [ ] `CLINICAL-SYMPTOM-05` Сразу под названием, если текст существует, показана ровно одна строка анамнеза
      симптома мелким приглушённым шрифтом.
- [ ] `CLINICAL-SYMPTOM-06` Для превью анамнеза берётся первая строка исходного описания симптома; при пустом
      описании — первая строка самой ранней непустой записи его истории.
- [ ] `CLINICAL-SYMPTOM-07` Длинное превью анамнеза остаётся одной строкой и заканчивается многоточием; при полном
      отсутствии текста дополнительная пустая строка не создаётся.
- [ ] `CLINICAL-SYMPTOM-08` Ниже названия и превью находится одна нижняя строка метаданных симптома.
- [ ] `CLINICAL-SYMPTOM-09` Слева в нижней строке показаны дата появления симптома и в скобках количество дней с
      этой даты.
- [ ] `CLINICAL-SYMPTOM-10` Справа в той же нижней строке показана единая компактная динамика в порядке:
      начальная цифра, линия изменения, текущее значение `/10`.
- [ ] `CLINICAL-SYMPTOM-11` Начальная выраженность выводится только числом в компактном semantic-бейдже; подписи
      вида `4/10` слева нет.
- [ ] `CLINICAL-SYMPTOM-12` Линия динамики строится от исходной выраженности к текущей и не превращается в большой
      отдельный график внутри списка.
- [ ] `CLINICAL-SYMPTOM-13` Текущее значение расположено у правого края группы динамики и записано как `N/10`.
- [ ] `CLINICAL-SYMPTOM-14` Значения `0–3` используют общий синий primary-цвет.
- [ ] `CLINICAL-SYMPTOM-15` Значения `4–6` используют общий кирпично-оранжевый warning-цвет, без локального hex.
- [ ] `CLINICAL-SYMPTOM-16` Значения `7–10` используют общий destructive-красный цвет.
- [ ] `CLINICAL-SYMPTOM-17` При уменьшении выраженности линия динамики зелёная.
- [ ] `CLINICAL-SYMPTOM-18` При увеличении выраженности линия динамики destructive-красная.
- [ ] `CLINICAL-SYMPTOM-19` При неизменной выраженности линия динамики синяя primary.
- [ ] `CLINICAL-SYMPTOM-20` В режиме «История» закрытые симптомы сохраняют ту же геометрию списка, но основной и
      вторичный текст становятся приглушёнными.
- [ ] `CLINICAL-SYMPTOM-21` У закрытого симптома слева в нижней строке показан период от даты появления до даты
      закрытия.
- [ ] `CLINICAL-SYMPTOM-22` Динамика закрытого симптома может быть приглушена, но остаётся на том же месте и не
      создаёт отдельный тип карточки.
- [ ] `CLINICAL-DIAGNOSIS-01` Название актуального диагноза выводится первым обычным чёрным текстом на всю доступную
      ширину строки.
- [ ] `CLINICAL-DIAGNOSIS-02` Под названием диагноза в одной нижней строке дата/метаданные расположены слева, а
      клинический статус — справа.
- [ ] `CLINICAL-DIAGNOSIS-03` В строке диагноза нет графика выраженности.
- [ ] `CLINICAL-DIAGNOSIS-04` Ключевой актуальный диагноз получает тот же красный `!` в фиксированном левом поле,
      не сдвигающем основной текст.
- [ ] `CLINICAL-DIAGNOSIS-05` В режиме «История» закрытые диагнозы сохраняют ту же геометрию, отображаются
      приглушённо и не получают красный маркер.
- [ ] `CLINICAL-DETAIL-01` Нажатие на симптом открывает следующим слоем общую doctor-модалку его полных деталей;
      карта пациента остаётся под стеком.
- [ ] `CLINICAL-DETAIL-02` В модалке симптома видны исходное описание/анамнез появления и подробная история
      изменения выраженности.
- [ ] `CLINICAL-DETAIL-03` В модалке симптома доступно добавление нового текущего значения выраженности.
- [ ] `CLINICAL-DETAIL-04` В модалке симптома доступно добавление заметки к симптому.
- [ ] `CLINICAL-DETAIL-05` В модалке симптома есть одно действие редактирования исходных данных, открывающее форму
      следующим слоем.
- [ ] `CLINICAL-DETAIL-06` В модалке симптома есть действие закрытия/архивирования; после закрытия симптом уходит из
      актуального списка и появляется в «Истории».
- [ ] `CLINICAL-DETAIL-07` Нажатие на диагноз открывает следующим слоем общую doctor-модалку его полных деталей.
- [ ] `CLINICAL-DETAIL-08` В модалке диагноза доступны редактирование исходных данных и закрытие диагноза; закрытый
      диагноз уходит в «Историю».
- [ ] `CLINICAL-FORM-01` Добавление и редактирование симптома используют существующий patient-scoped contract и не
      создают вторую сущность, API или локальную форму.
- [ ] `CLINICAL-FORM-02` Добавление и редактирование диагноза используют существующий patient-scoped contract и не
      создают вторую сущность, API или локальную форму.
- [ ] `CLINICAL-FORM-03` В шапке модалок добавления, просмотра и редактирования справа показаны ФИО текущего пациента
      общим modal-title элементом.
- [ ] `CLINICAL-FORM-04` Выбора или создания другого пациента внутри этих форм нет.
- [ ] `CLINICAL-FORM-05` Симптомы и диагнозы можно добавлять и исправлять с карты пациента и со страницы приёма в
      любой момент; искусственной проверки «только во время приёма» нет.
- [ ] `CLINICAL-FORM-06` После сохранения симптома, диагноза, нового значения или заметки открытая поверхность
      обновляется сразу без полной перезагрузки страницы.

### P4.2. Анамнез заболевания

- [ ] `DISEASE-BLOCK-01` Сразу после отдельного блока диагнозов расположен отдельный белый блок «Анамнез
      заболевания».
- [ ] `DISEASE-BLOCK-02` Анамнез заболевания не смешивается с анамнезом жизни и хранится одним patient-scoped
      текстом.
- [ ] `DISEASE-HEADER-01` Заголовок «Анамнез заболевания» расположен слева в верхней строке блока.
- [ ] `DISEASE-HEADER-02` Справа в той же строке находится одно icon-only действие редактирования `SquarePen`.
- [ ] `DISEASE-PREVIEW-01` Непустой текст анамнеза начинается сразу под заголовком блока.
- [ ] `DISEASE-PREVIEW-02` В свёрнутом состоянии отображается максимум пять строк текста.
- [ ] `DISEASE-PREVIEW-03` Chevron вниз расположен по центру нижнего края блока только при фактическом переполнении
      пяти строк.
- [ ] `DISEASE-PREVIEW-04` Нажатие chevron вниз разворачивает весь текст; chevron меняет направление вверх.
- [ ] `DISEASE-PREVIEW-05` Нажатие chevron вверх возвращает представление к пяти строкам.
- [ ] `DISEASE-PREVIEW-06` Для текста не длиннее пяти строк и для пустого текста chevron не отображается.
- [ ] `DISEASE-EDIT-01` `SquarePen` открывает редактор следующим слоем общего `DoctorModal`; нижняя карта остаётся
      открытой и сохраняет состояние.
- [ ] `DISEASE-EDIT-02` В шапке редактора справа показано ФИО текущего пациента; выбора пациента нет.
- [ ] `DISEASE-EDIT-03` На мобильном редактор выглядит как безрамочный белый текстовый лист над общим footer.
- [ ] `DISEASE-EDIT-04` На мобильном ввод начинается у нижней части листа и при добавлении строк растёт вверх.
- [ ] `DISEASE-EDIT-05` На desktop/right-sheet тот же безрамочный текстовый лист ориентирован сверху.
- [ ] `DISEASE-EDIT-06` Сохранение обновляет текст блока без полной перезагрузки страницы.
- [ ] `DISEASE-EDIT-07` После успешного сохранения закрывается только редактор, а результат показывается общим
      toast-уведомлением.
- [ ] `DISEASE-EDIT-08` Закрытие редактора без сохранения не изменяет текст анамнеза.

### P4.3. Анамнез жизни

- [ ] `LIFE-BLOCK-01` После блока приёмов расположен отдельный белый блок «Анамнез жизни».
- [ ] `LIFE-BLOCK-02` Отдельного старого блока «Сопутствующие заболевания» на карте нет.
- [ ] `LIFE-SECTION-01` В анамнезе жизни отдельной подсекцией показаны сопутствующие заболевания.
- [ ] `LIFE-SECTION-02` В анамнезе жизни отдельной подсекцией показаны травмы.
- [ ] `LIFE-SECTION-03` В анамнезе жизни отдельной подсекцией показаны операции.
- [ ] `LIFE-SECTION-04` В анамнезе жизни отдельной подсекцией показаны болезни.
- [ ] `LIFE-SECTION-05` В анамнезе жизни отдельной подсекцией показаны стрессы.
- [ ] `LIFE-SECTION-06` В анамнезе жизни отдельной подсекцией показан образ жизни.
- [ ] `LIFE-SECTION-07` Все подсекции постоянно видны и не требуют отдельного раскрытия.
- [ ] `LIFE-SECTION-08` Заголовок заполненной подсекции отображается чёрным полужирным текстом на два пикселя
      меньше заголовка всего блока.
- [ ] `LIFE-SECTION-09` Заголовок пустой подсекции отображается тем же размером, но приглушённым серым цветом.
- [ ] `LIFE-SECTION-10` Значения заполненной подсекции расположены сразу под её заголовком обычным текстом того же
      размера, без отдельной плашки.
- [ ] `LIFE-SECTION-11` Пустая подсекция обозначается компактным прочерком либо отсутствием значения; фраз
      «записей нет» и «не внесено» нет.
- [ ] `LIFE-EDIT-01` В заголовке блока «Анамнез жизни» находится одно общее действие редактирования `SquarePen`.
- [ ] `LIFE-EDIT-02` Редактор открывается следующим слоем общей doctor-модалки с patient context в шапке и без
      выбора пациента.
- [ ] `LIFE-EDIT-03` В редакторе подсекции идут длинной последовательной простынёй без вложенной внешней карточки
      вокруг всей формы.
- [ ] `LIFE-EDIT-04` Справа от каждой подсекции находится её существующее действие добавления записи.
- [ ] `LIFE-EDIT-05` Внутри подсекции показан список существующих typed-записей.
- [ ] `LIFE-EDIT-06` Нажатие на существующую запись открывает следующий слой её редактирования.
- [ ] `LIFE-EDIT-07` Исторические записи анамнеза жизни не удаляются и не получают новую зависимость от конкретного
      приёма.
- [ ] `LIFE-EDIT-08` Сохранение обновляет видимый анамнез жизни без полной перезагрузки страницы.

### P4.4. Сводка и история приёмов

- [ ] `ENCOUNTERS-BLOCK-01` Блок приёмов расположен строго после «Анамнез заболевания» и строго перед «Анамнез
      жизни».
- [ ] `ENCOUNTERS-BLOCK-02` В заголовке блока написано `Приёмы: N`.
- [ ] `ENCOUNTERS-BLOCK-03` `N` является фактическим количеством клинических приёмов пациента, а не количеством
      календарных записей.
- [ ] `ENCOUNTERS-META-01` В сводке отдельно показано количество первичных приёмов.
- [ ] `ENCOUNTERS-META-02` В сводке отдельно показано количество повторных приёмов.
- [ ] `ENCOUNTERS-META-03` Несколько первичных приёмов допустимы и каждый учитывается в счётчике.
- [ ] `ENCOUNTERS-META-04` В сводке показана дата предыдущего, то есть последнего по времени, приёма.
- [ ] `ENCOUNTERS-META-05` Дата предыдущего приёма является кнопкой/ссылкой и открывает просмотр именно этого
      приёма.
- [ ] `ENCOUNTERS-ACTION-01` Внизу блока слева расположено действие «История приёмов» в общей doctor-панели
      действий.
- [ ] `ENCOUNTERS-ACTION-02` Внизу блока справа расположено primary-действие «Новый приём».
- [ ] `ENCOUNTERS-ACTION-03` Постоянной второй колонки или встроенной простыни истории на карте нет.
- [ ] `ENCOUNTERS-HISTORY-01` «История приёмов» открывает следующим слоем общую doctor-модалку.
- [ ] `ENCOUNTERS-HISTORY-02` В модалке показан полный хронологический список приёмов пациента.
- [ ] `ENCOUNTERS-HISTORY-03` Нажатие на строку списка открывает следующим слоем компактный просмотр выбранного
      приёма.
- [ ] `ENCOUNTERS-HISTORY-04` При открытии истории и просмотра карточка пациента остаётся под стеком, а затемнение
      остаётся одно.
- [ ] `ENCOUNTERS-VIEW-01` Просмотр существующего приёма остаётся компактной модалкой, а не полной страницей.
- [ ] `ENCOUNTERS-VIEW-02` Из компактного просмотра одно действие «Изменить» ведёт на полноценную страницу того же
      приёма.
- [ ] `ENCOUNTERS-VIEW-03` Создание нового приёма всегда ведёт на одну полноценную страницу создания; длинной
      вложенной формы внутри карты нет.

### P4.5. Страница приёма

- [ ] `ENCOUNTER-PAGE-01` Создание и редактирование приёма открываются в одной полноценной doctor-странице, не в
      длинной модалке.
- [ ] `ENCOUNTER-PAGE-02` Страница остаётся внутри одного doctor/patient shell и сохраняет контекст текущего
      пациента.
- [ ] `ENCOUNTER-PAGE-03` В верхнем информационном блоке показано ФИО пациента без возможности заменить пациента.
- [ ] `ENCOUNTER-PAGE-04` В верхнем информационном блоке показана дата приёма.
- [ ] `ENCOUNTER-PAGE-05` В верхнем информационном блоке показано время приёма в той же бизнес-таймзоне, что и
      связанная запись.
- [ ] `ENCOUNTER-PAGE-06` В верхнем информационном блоке показан филиал.
- [ ] `ENCOUNTER-PAGE-07` В верхнем информационном блоке показан специалист.
- [ ] `ENCOUNTER-PAGE-08` При наличии связи показана конкретная связанная календарная запись.
- [ ] `ENCOUNTER-PAGE-09` При отсутствии связи явно написано, что приём не связан с записью.
- [ ] `ENCOUNTER-PAGE-10` На странице можно выбрать тип приёма «Первичный» или «Повторный».
- [ ] `ENCOUNTER-PAGE-11` На странице есть поле анамнеза/истории появления жалобы.
- [ ] `ENCOUNTER-PAGE-12` Симптомы и диагнозы оформлены двумя отдельными блоками по `CLINICAL-BLOCK-03/04`.
- [ ] `ENCOUNTER-PAGE-13` Для существующих симптомов можно записать текущую выраженность и заметку этого приёма.
- [ ] `ENCOUNTER-PAGE-14` На странице есть отдельное поле осмотра.
- [ ] `ENCOUNTER-PAGE-15` На странице есть отдельное поле тестирования/результатов тестов, когда оно применимо.
- [ ] `ENCOUNTER-PAGE-16` На странице есть отдельное поле проведённых манипуляций.
- [ ] `ENCOUNTER-PAGE-17` На странице есть отдельное поле результатов приёма.
- [ ] `ENCOUNTER-PAGE-18` На странице есть отдельное поле рекомендаций/назначений.
- [ ] `ENCOUNTER-PAGE-19` Все поля сохраняются через существующий единый Visit contract; вторая сущность, API или
      repository для приёма не создаются.
- [ ] `ENCOUNTER-PAGE-20` Из блока симптомов можно открыть существующую patient-scoped форму добавления симптома.
- [ ] `ENCOUNTER-PAGE-21` Из блока диагнозов можно открыть существующую patient-scoped форму добавления диагноза.
- [ ] `ENCOUNTER-PAGE-22` После добавления симптома или диагноза соответствующий отдельный блок обновляется сразу,
      без ухода со страницы приёма.
- [ ] `ENCOUNTER-PAGE-23` Существующий приём можно открыть и редактировать после его даты; date guard не запрещает
      правку прошедшего приёма.
- [ ] `ENCOUNTER-PAGE-24` После сохранения изменённые данные видны на странице приёма.
- [ ] `ENCOUNTER-PAGE-25` После сохранения те же изменённые данные видны в карте пациента без полной перезагрузки.

### P4.6. Связь нового приёма с календарной записью

- [ ] `ENCOUNTER-LINK-01` При создании приёма из деталей календарной записи конкретная запись задана заранее.
- [ ] `ENCOUNTER-LINK-02` В prebound-сценарии selector другой записи скрыт и связь нельзя заменить.
- [ ] `ENCOUNTER-LINK-03` Сохранённый prebound-приём связан именно с переданной календарной записью.
- [ ] `ENCOUNTER-LINK-04` При создании из карты показан selector существующей ещё не связанной записи пациента.
- [ ] `ENCOUNTER-LINK-05` Уже связанная с другим приёмом запись не предлагается повторно.
- [ ] `ENCOUNTER-LINK-06` В selector есть явный вариант продолжить без существующей записи.
- [ ] `ENCOUNTER-CREATE-01` Для приёма без выбранной записи флажок «Создать запись» включён по умолчанию.
- [ ] `ENCOUNTER-CREATE-02` Специалист может явно выключить «Создать запись» и сохранить клинический приём без
      календарной записи.
- [ ] `ENCOUNTER-CREATE-03` При включённом флажке показано каноническое поле даты и времени записи.
- [ ] `ENCOUNTER-CREATE-04` При включённом флажке показан канонический выбор филиала.
- [ ] `ENCOUNTER-CREATE-05` При включённом флажке показан канонический выбор услуги.
- [ ] `ENCOUNTER-CREATE-06` При включённом флажке показана каноническая длительность.
- [ ] `ENCOUNTER-CREATE-07` При включённом флажке показана каноническая стоимость.
- [ ] `ENCOUNTER-CREATE-08` Новая запись создаётся через существующую manual booking door, а не через второй
      упрощённый appointment write-path.
- [ ] `ENCOUNTER-CREATE-09` Новый клинический приём связывается с appointment ID, который реально вернула manual
      booking door.
- [ ] `ENCOUNTER-CREATE-10` Созданная запись получает обычные snapshot price/payment/prepayment поля существующего
      booking contract.
- [ ] `ENCOUNTER-CONFLICT-01` При фактическом конфликте слота до создания клинического приёма открывается явное
      подтверждение «Время занято».
- [ ] `ENCOUNTER-CONFLICT-02` Нажатие «Отмена» закрывает подтверждение и не создаёт ни клинический приём, ни
      календарную запись, ни финансовую операцию.
- [ ] `ENCOUNTER-CONFLICT-03` Нажатие «Создать наложение» повторяет тот же manual request с явным consent-флагом.
- [ ] `ENCOUNTER-CONFLICT-04` После согласия физически создаётся перекрывающаяся обычная запись и новый приём
      связывается именно с ней.
- [ ] `ENCOUNTER-MONEY-01` OFF-сценарий не создаёт календарную запись.
- [ ] `ENCOUNTER-MONEY-02` OFF-сценарий не создаёт payment, payment intent или другую финансовую операцию.
- [ ] `ENCOUNTER-MONEY-03` Выбор существующей записи не создаёт вторую календарную запись и связывает приём с
      выбранной.

### P4.7. Приёмка нового клинического пути

- [ ] `CLINICAL-GATE-01` Stateful-потоки разделены по непересекающимся file-scope; каждый worker-коммит принят
      только после отдельного независимого аудита по относящимся к нему ID.
- [ ] `CLINICAL-GATE-02` Миграции и новые write-paths, если они понадобились, прошли schema/grant parity,
      owner-aware preflight и точную проверку под фактической runtime-ролью без временной базы.
- [ ] `CLINICAL-GATE-03` Интеграционный кандидат проверен живым Playwright-проходом на iPhone XR и desktop:
      пять строк/раскрытие анамнеза, редактор, сводка и история приёмов, создание и редактирование страницы приёма,
      оба варианта связи с записью и подтверждение конфликта; сохранены снимки ключевых состояний.
- [ ] `CLINICAL-GATE-04` После сведения веток выполнены целевые проверки, полный CI для накопленного
      межмодульного batch, `push:checked` и штатный deploy на TEST; живой TEST smoke подтверждает новый путь.

## Q. Приёмка и порядок исполнения

- [x] `GATE-01` Перед каждым slice выполнен current-code census и найдены reuse-кандидаты (`DoctorModal`, общий
      modal footer, appointment form, date/time picker, unread aggregation, calendar controls).
- [x] `GATE-02` Presentation-срезы принимаются живьём на mobile viewport; новые тесты на радиусы, размеры, текстовые
      классы и скриншоты не пишутся.
- [x] `GATE-03` Поведенческие тесты добавляются только для устойчивых contracts: unread aggregation, сохранение
      записи/отмены и money boundaries.
- [x] `GATE-04` Каждый отдельный worker получает только свой непересекающийся scope и полный текст соответствующих
      ID; worker коммитит в `wt/*`, не пушит и не занимает общий DEV-порт.
- [x] `GATE-05` Перед landing выполнен один независимый audit соответствующего риска; style-аудит не превращается
      в серийное придумывание новых требований.
- [x] `GATE-06` После интеграции всех принятых slices выполняются проверки по §9–§10; commit/push/TEST deploy —
      только по отдельной актуальной команде владельца.
- [x] `GATE-07` Последний интеграционный кандидат интерактивно проверен на iPhone XR `414×896` и типовом Android
      viewport: открытие обоих путей комментариев ЛФК, отправка сообщения, вложенная модалка упражнения,
      график работы и переход ЛФК ↔ Обзор; сохранены итоговые screenshots.
- [x] `GATE-08` После приёмки всех worker-веток полное дерево `feat/doctor-ui-rebuild` закоммичено, на этом SHA
      выполнен full CI, затем ветка отправлена в origin и штатно развёрнута на TEST.

## R. Доказательства текущего интеграционного кандидата

- `EX-03`, `EX-14`, `GATE-07`: итоговый интеграционный кандидат `b7a5b2894` проверен живым Playwright-проходом на
  DEV Turbopack в viewport iPhone XR `414×896` и Android `360×800` / `412×915`. У верхней панели упражнения
  вычисленная тень `0 1px 3px rgba(15,23,42,0.08)` визуально остаётся лёгкой и различимой; статистика имеет
  центрированный заголовок «Отметки клиента», единый месяц и одинаковые состояния «Загрузка календаря…» /
  «Загрузка графика…» без отдельного заголовка «Динамика». Во всех трёх viewport `scrollWidth = clientWidth`,
  браузерных ошибок нет. Итоговые снимки:
  `/home/dev/dev-projects/.lead/runs/doctor-mobile-owner-20260904/integration-current/`.
- `GATE-08`: full-CI для product HEAD `b7a5b2894` после изоляции конкурентно повреждённого generated
  `.next/dev/types` завершён каноническим `typecheck` и разрешённым `ci:resume:after-typecheck`: все workspace
  typecheck, тесты, обе production-сборки и SaaS/DB gates — PASS. `pnpm run push:checked` отправил
  `d1b686de6` и дождался `Security: success`; штатный
  `bash deploy/host/deploy-test.sh feat/doctor-ui-rebuild` применил одну ожидавшую TEST-миграцию, дважды доказал
  tenant wall `3/3`, завершил post-runtime gate с `status=okay` и выдал `deploy-test: PASS`. Runtime-проверка
  `curl --resolve test.bersoncare.ru:443:127.0.0.1 https://test.bersoncare.ru/api/me` вернула ожидаемый HTTP 401;
  все четыре TEST-unit (`webapp`, `api`, `scheduler`, `media-worker`) — `active`. Транскрипт deploy:
  `/var/log/bersoncarebot/deploy-test/deploy-test.20260904T155017Z.Ojkg3q.log`.

- `EX-13`, `UNREAD-03`, `MODAL-01`, `MODAL-02`: команда
  `node /tmp/bcb-orch-20260904/accept-integrated-modals.mjs` на Turbopack `127.0.0.1:5214`, viewport `390×844`,
  вывела `video: 1`, `afterVideoClose.contents: 2`, `discussionVisible: true`; на втором и третьем уровнях
  `overlays: 1`. Снимки: `.lead/runs/doctor-mobile-owner-20260904/integration-final/01–04-*.png`.
- `TODAY-06`: команда `bash /tmp/bcb-orch-20260904/accept-today06.sh` с одной временной DEV-строкой вывела
  `ariaLabel: "Берсон Дмитрий, новых сообщений и комментариев: 1"`, `fontWeight: "600"`; после trap команда
  `SELECT count(*) ... WHERE id = 'aa000006-0000-4000-8000-000000000001'` вернула `0`. Снимок:
  `.lead/runs/doctor-mobile-owner-20260904/integration-final/05-today-support-unread.png`.
- `APPT-FORM-01…13`, `APPT-LIST-01…04`, `APPT-DETAIL-01…10`, `CANCEL-01…06`, `PICKER-01…04`:
  отчёт и снимки живой проверки `390×844` находятся в
  `.lead/runs/doctor-mobile-owner-20260904/appointment-correction/REPORT.md`; targeted suite — `27/27`, отдельная
  fault injection — `5/5`.
- `MODAL-03`: тот же appointment live-pass проверил общий `DoctorModalFooter`; интеграционный код оставляет один
  footer-owner в `DoctorModal`, а контент передаёт только slot-содержимое.
- Миграционный preflight: `bash deploy/host/migrate-dev.sh --preflight --runtime-env-root
  /home/dev/dev-projects/BersonCareBot` — `PASS`, `pending=1`, транзакция завершена `ROLLBACK`.
- `GATE-05`: независимый Claude Opus 5/high проверил кандидат `c5d6330a1` против базы `66e72844c` и дал `PASS`;
  отчёт и 390×844 screenshots: `.lead/runs/doctor-mobile-owner-20260904/integration-final/FINAL_AUDIT.md` и
  `.lead/runs/doctor-mobile-owner-20260904/integration-final/audit/`. Targeted evidence аудитора: `145` тестов в
  `25` файлах и webapp typecheck PASS. После аудита ведущий отдельно прочитал тела DB-root и подтвердил
  `CONTACTS-10`: успешные OTP-root-функции выставляют `user_contacts.confirmed_at`, одна отправка письма — нет.
- Owner-requested аудит вредных UI-тестов: Claude Opus 5/high просмотрел `74` исполняемых TSX test-файла и `321`
  блок `it/test`, классифицировал `31` однозначную проверку наличия/текста/количества/DOM-формы на удаление и `9`
  смешанных случаев как OWNER QUESTION без автоматической правки. Отчёт:
  `/home/dev/brain/runs/agent-port/harmful-ui-tests-audit-20260904.json`. Все `31` кандидатов удалены; смешанные
  случаи сохранены. Проверка: `pnpm test:webapp` — `493 passed | 7 skipped` файлов, `2565 passed | 31 skipped`
  тестов; targeted `tariffMechanics.route.test.ts` — `42/42`; scoped ESLint — PASS.
- `FILES-04`: мобильный toolbar содержит раздельные действия «Камера / Медиатека / Документ», а desktop — одну
  обычную кнопку «Загрузить файл»; проверено scoped ESLint и webapp typecheck.
- `CAL-NAV-06/07`, `WORK-03/05/08`: независимый Claude Opus 5/high принял candidate `2374a6ee3` живым проходом
  на XR `414×896`, Android `360`/`412` и desktop-control. Full-bleed геометрия, внешний scroll-owner, различие
  filled/outline расписаний, однострочное время и прямой edit-path подтверждены; nested picker оставляет один
  backdrop. Targeted UI — `4` файла / `11` тестов, typecheck и scoped ESLint PASS. Артефакт:
  `.lead/runs/doctor-mobile-owner-20260904/work-schedule-audit/AUDIT.md`.
- `LFK-COMMENTS-01…06`, `LFK-LAYOUT-01/02`: независимый Claude Opus 5/xhigh принял candidate `38e9bee2a`
  живым проходом на XR `414×896`, Android `360`/`412`: обе точки входа открывают один patient-scoped список и
  одну exercise modal, вложенные слои выезжают снизу при одном backdrop, контейнер не меняется при ЛФК↔Обзор.
  Реальный DEV reply получил HTTP 200 и появился за `2.2s` при недоступном integrator; canonical Drizzle path,
  exact `id` privilege, generated parity и behavior test `4/4` PASS. Audit test `ac278bd48` не закрепляет UI
  shape. Артефакт: `/home/dev/dev-projects/.lead/runs/doctor-mobile-owner-20260904/lfk-comments-audit/AUDIT.md`.
- `GATE-06`: локальный full-CI прошёл lint, typecheck, integrator/webapp/media-worker/error-tracking tests,
  integrator/webapp production builds, SaaS DB regression audit; после удаления вредных UI-oracles полный
  `pnpm test:webapp` зелёный (`493 passed | 7 skipped` файлов, `2565 passed | 31 skipped` тестов). Локальный внешний
  `node scripts/registry-prod-audit.mjs` трижды завершился одинаковым `bulk advisories HTTP 503`; независимый
  GitHub Security check для `049c6c2552fb330b9fd6b17914cc0f3c52712171` завершился `success`, а
  `pnpm run push:checked` подтвердил все terminal checks. Штатный
  `bash deploy/host/deploy-test.sh feat/doctor-ui-rebuild` применил TEST migration (`pending=1`, `total=114`),
  повторно доказал tenant wall `3/3`, завершил post-runtime SaaS isolation gate с `status=okay` и выдал
  `deploy-test: PASS`; транскрипт:
  `/var/log/bersoncarebot/deploy-test/deploy-test.20260904T071028Z.2F9G7g.log`.
