# Независимый адверсарный аудит Л5 — почтовый канал DEV ведёт в ловушку

Дата: 2026-09-15. Аудитор: `claude-opus-5` (автор работы — `gpt-5.6-sol`, другая модель).
Предмет: клон `/home/dev/dev-projects/bcb-wt-alerting-truth`, ветка `wt/dev-mail-trap`,
коммит `13b1aba5e` (9 файлов, +282/−14).
Оракул: `docs/_TODO/LEADS_AND_COMMUNICATION_VISIBILITY_2026-09-14.md`, «Очередь до цели», пункт 5.

## ВЕРДИКТ: FAIL — MUST FIX 2

Все пять требований пункта 5 плана выполнены и доказаны (таблица ниже). FAIL ставится за два
отдельных нарушения, оба дешёвые в исправлении:

1. Стена, которую эта работа перестроила, осталась без единой проверки: два независимых способа снять
   список разрешённых получателей ТОЛЬКО для почты на стенде TEST проходят зелёными через ВЕСЬ набор
   интегратора (697 тестов).
2. Коммит оставил в силе противоречащую ему формулировку канона `AGENTS.md:595`, что прямо запрещено
   `AGENTS.md` §0 п.4.

Строка вердикта в `feat` мной не писалась (бриф; [[verdict-line-goes-in-main-not-in-the-branch]]).

---

## 1. Находка ведущего — первый ход

**Вопрос ведущего:** чем меряется, что ветка TEST и ветка продакшена остались дословно прежними?

**Ответ, полученный исполнением: ветку продакшена меряют ~30 утверждений в трёх файлах; ветку TEST
для канала `email` не меряет НИЧТО.**

### Ветка продакшена — измерена, и с запасом

Инъекция E: `if (!isTestDeployment()) return intent;` → `return SUPPRESS;`

```
###### E INJECTION: ветка продакшена глушит вместо пропуска
 Test Files  3 failed | 2 passed (5)
      Tests  30 failed | 30 passed (60)
```

Покраснели `dispatchPort.test.ts` (7 тестов) и `relayOutboundRoute.route.test.ts` (4 теста) целыми
блоками. Причина устройства, а не удачи: vitest гоняется без `TEST`, поэтому ветку продакшена
проходит практически каждый тест диспетчера. Эта ветка защищена надёжно.

### Ветка TEST для почты — не измерена ничем

Инъекция A (ровно инъекция ведущего): ранний выход для почты вынесен ИЗ ветки
`isLocalDevelopmentDeliverySuppressed()` наружу.

```
###### A INJECTION: TEST-ветка — вынести ранний выход email ИЗ dev-ветки
--- A: targeted ---
 Test Files  5 passed (5)
      Tests  60 passed (60)
--- A: ВЕСЬ набор интегратора ---
 Test Files  127 passed | 2 skipped (129)
      Tests  697 passed | 2 expected fail | 2 skipped (701)
```

**Ни один из 697 тестов интегратора не заметил.** Это не «мой targeted набор узкий» — это весь набор.

Инъекция H, вторая независимая форма того же класса (ранний выход не трогаем, вместо этого
освобождаем почту от самой стены):

```js
if (intendedChannel !== 'email' && !isTestDeliveryRecipientAllowed(intendedChannel, recipient)) {
```

```
###### H INJECTION: TEST-ветка теряет стену списка ТОЛЬКО для email
 Test Files  5 passed (5)
      Tests  60 passed (60)
```

Тоже зелено. Две разные правки, один и тот же молчаливый результат.

### Почему тестов нет — видно по самому файлу

В `apps/integrator/src/shared/testDeliverySafety.test.ts` ветку TEST проверяют ровно три теста:

| тест | канал | что утверждает |
|---|---|---|
| `TEST suppresses a real recipient instead of redirecting it` (:173) | **telegram** | неразрешённый получатель подавлен |
| `TEST delivers an allowlisted recipient unchanged` (:186) | **email** | РАЗРЕШЁННЫЙ получатель доходит |
| `TEST fails closed when account env is absent` (:228) | **telegram** | без env — подавлено |

То есть «почта + неразрешённый получатель + стенд TEST» — единственная клетка, которой нет, и она же
единственная, которую трогает эта работа. Проверка по всему репозиторию, что других кандидатов-ловцов
не существует:

```bash
grep -rln "TEST_ACCOUNT_\|isTestDeliveryRecipientAllowed\|PRE_FORK_TEST" --include=*.ts apps/
```

Восемь файлов, из них тест ровно один — `testDeliverySafety.test.ts`. Остальные: `dispatchPort.ts`,
`testDeliverySafety.ts`, `config/env.ts` (×2), `web-push/deliveryAdapter.ts` (комментарий),
`qa-test-send.ts`, `webapp/src/config/testAccounts.ts`. Пустой результат доказан перечислением.

### Какой проверки не хватает (сам не пишу — бриф)

Один `it` в том же файле, зеркало уже существующего `TEST suppresses a real recipient…`, но каналом
`email`: `NODE_ENV=production`, `TEST=true`, `TEST_ACCOUNT_EMAILS` настроен, intent на
`email` с получателем, которого в списке НЕТ → результат `{ suppressedByEnvironment: true }` и
`sent === []`, то есть адаптер не достигнут. Один этот тест краснеет и на A, и на H.

### Насколько это дорого сегодня — честно

Отказ **молчаливый** без оговорок: 697 зелёных. Про «дорого» — с оговоркой, и её надо знать ведущему:
на стенде TEST оба почтовых профиля сами смотрят в петлевую ловушку (замер ниже, §4), а mailpit
наружу не пересылает. Поэтому СЕГОДНЯ прямая цена инъекции — не письмо живому человеку, а: строки
очереди закрываются как `sent` вместо `dead`, здоровье канала `email` самоизлечивается на
недоставке, а проверка собственного канала клиники (`clinicCredentialProbe`) может включить канал по
доставке в ловушку.

Цена становится прежней ровно в том сценарии, которым план обосновывает саму петлевую проверку:
«если в базу DEV когда-нибудь вернётся боевой SMTP, канал снова заглохнет сам». Список
`TEST_ACCOUNT_EMAILS` — это та же страховка для стенда, и держать её должен тест, а не текущее
значение строки в базе. Поэтому — MUST FIX, а не наблюдение.

---

## 2. MUST FIX

### MUST FIX 1 — стена списка получателей на TEST для канала `email` не покрыта ничем

Место: `apps/integrator/src/infra/adapters/dispatchPort.ts:239-278`,
`apps/integrator/src/shared/testDeliverySafety.test.ts`.
Доказательство: инъекции A и H выше, 697/697 и 60/60 зелёных.
Что сделать: один поведенческий `it`, описанный выше. Больше ничего.

### MUST FIX 2 — коммит оставил в силе противоречащую ему строку канона

`AGENTS.md:595`, раздел §1b.2 «Dev никогда не инициирует реальную доставку»:

> В `development` доставка должна быть no-op/мок (логируем «отправили бы X», реально не шлём; без ошибок).

После `13b1aba5e` это неверно для почты: на DEV интегратор открывает настоящее SMTP-соединение на
`127.0.0.1:1025` и реально доставляет письмо в mailpit. Поведение санкционировано (владелец 15.09:
«на DEV почтовый канал делай тот же что на тест - у нас mailtip»), нормативный текст — нет.

`AGENTS.md` §0 п.4 требует дословно: «В том же изменении удали конфликтующие активные формулировки».
Это обязательное repo-rule, нарушение достижимое: следующий агент, читающий §1b.2 как канон, увидит
живую отправку на DEV как дефект и «починит» её обратно.

Что сделать: переписать буллет `AGENTS.md:595` в одном месте — на DEV доставка остаётся no-op для
всех каналов, кроме `email`, который допускается ТОЛЬКО когда разрешённый SMTP-хост петлевой; общий
запрет реальной отправки в Telegram/SMSC/MAX/S3 из dev не меняется. Второго зеркала нет: `CLAUDE.md`
этого правила не пересказывает (проверено чтением файла).

---

## 3. Пять требований пункта 5 — поимённо

| # | требование плана | вердикт | чем доказано |
|---|---|---|---|
| а1 | гейт среды на DEV пропускает `email`, остальное продолжает глушить | ✅ | инъекции D и G краснеют |
| а2 | петлевую проверку делает ровно ОДНО место в почтовом пути | ✅ | замер §5, одно место, и стоит после выбора clinic/platform профиля |
| а3 | поведенческий тест краснеет на подмене хоста на непетлевой | ✅ | инъекция B краснеет |
| б | профили DEV на ловушке, отправители `dev-staff@…` / `dev-patient@…` | ✅ | мой SELECT §4 |
| в | живое письмо в ловушке + telegram/sms на DEV молчат | ✅ | mailpit §4 + инъекции D/G + код |
| прод | ветка продакшена дословно прежняя | ✅ | diff + инъекция E (30 красных) |

### а1 — DEV пропускает только почту

Инъекция D (откат требования: убрать `if (intendedChannel === 'email') return intent;`):

```
     × on DEV sends email only through loopback SMTP and always suppresses telegram
AssertionError: expected { suppressedByEnvironment: true } to deeply equal {}
      Tests  1 failed | 59 passed (60)
```

Инъекция G (обратная сторона: дать пройти ВСЕМ каналам — `return intent;` без условия):

```
     × on DEV sends email only through loopback SMTP and always suppresses telegram
AssertionError: expected {} to deeply equal { suppressedByEnvironment: true }
      Tests  1 failed | 59 passed (60)
```

Обе стороны требования защищены.

### а3 — петлевая проверка

Инъекция B (`isLoopbackSmtpHost` → `return host.length >= 0;`):

```
AssertionError: expected {} to deeply equal { suppressedByEnvironment: true, …(1) }
      Tests  1 failed | 59 passed (60)
```

Обход подбором написания хоста я не нашёл: сравнение точное, `::1` и `localhost` через
`toLowerCase()`, `host` уже `.trim()`-нут zod-схемой (`config/smtpOutbound.ts:45`). Список узкий,
то есть ошибается в безопасную сторону (`[::1]`, `127.0.0.2` → подавление, а не отправка).

---

## 4. Замеры взглядом (качество разового действия, §24.4)

### Профили DEV — мой собственный SELECT, не отчёт исполнителя

```bash
sudo -n -u postgres psql -X -h /var/run/postgresql -p 5432 -d bcb_webapp_dev -P pager=off -c \
"SELECT key, scope, organization_id, value_json #>> '{value,host}' AS host,
        value_json #>> '{value,port}' AS port, value_json #>> '{value,secure}' AS secure,
        value_json #>> '{value,from}' AS sender, updated_at
   FROM public.system_settings WHERE key LIKE '%smtp%' ORDER BY key, organization_id;"
```

```
           key            | scope | organization_id |        host         | port | secure |           sender            |          updated_at
--------------------------+-------+-----------------+---------------------+------+--------+-----------------------------+-------------------------------
 smtp_outbound            | admin |                 | mail.hosting.reg.ru | 465  | true   | no-reply@bersoncare.ru      | 2026-08-17 02:55:25.080243+03
 therapygo_smtp_outbound  | admin |                 | 127.0.0.1           | 1025 | false  | dev-patient@therapygo.local | 2026-09-15 13:52:20.556892+03
 therapysto_smtp_outbound | admin |                 | 127.0.0.1           | 1025 | false  | dev-staff@therapysto.local  | 2026-09-15 13:52:20.556892+03
```

Требование (б) выполнено дословно. Клинических (`clinic_smtp_outbound`) записей на DEV нет вовсе.

### Прежний профиль, смотревший наружу — действительно недостижим

Вопрос ведущего: правда ли новый гейт делает боевой профиль безопасным. Ответ сильнее: этот профиль
не выбирается НИКАКИМ живым путём почты, гейт для него — второй рубеж, а не единственный.

Интроспекция живой двери (не grep по миграциям — [[migration-grep-is-not-live-object-state]]):

```bash
sudo -n -u postgres psql ... -c "SELECT pg_get_functiondef('app.read_integrator_provider_runtime_setting(text)'::regprocedure);"
```

Тело функции содержит закрытый белый список ключей, и в нём из почтовых есть только
`therapygo_smtp_outbound`, `therapysto_smtp_outbound`. Ключа `smtp_outbound` в списке нет — функция
вернёт `NULL` даже если его попросить.

Вторая дверь к нему в коде существует, но мертва:

```bash
grep -rn "readSmtpOutboundSettingValueJson" --include=*.ts apps/
```

Единственное вхождение — само объявление в `infra/db/publicRestrictedSettings.ts:13`. Вызовов ноль.

Остаётся `app.read_operator_health_smtp_outbound_setting(text)` для планировщика — он тоже принимает
audience и читает те же два профиля (`config/smtpOutbound.ts:91-95`).

Итог: `smtp_outbound` на DEV живёт только как запись, которую показывает экран настроек админа.

### Ловушка общая с TEST, письма различимы

```bash
curl -s 'http://127.0.0.1:8025/api/v1/messages?limit=60'
```

Всего 36 писем. По отправителям:

```
test-staff@therapysto.local   28
test-patient@therapygo.local   6
dev-patient@therapygo.local    1
probe@localhost                1
```

- Живое письмо Л5 на месте: `0TQ70CStqV6XrP3YKNaOxI`, `dev-patient@therapygo.local` →
  `dimmdao@gmail.com`, «Тест SMTP — Therapysto», `2026-09-15T14:12:02.871+03:00` — совпадает с тем,
  что видел ведущий.
- **Стенд TEST не изменился:** на момент постановки задачи план зафиксировал 35 писем,
  28 `test-staff@` + 6 `test-patient@`. Сейчас ровно те же 28 и 6, плюс одно новое письмо DEV.
  То есть работа не переслала ни одного письма TEST и не переименовала его отправителей.
- Письма DEV отличимы префиксом `dev-` против `test-` — требование «чтобы письма DEV отличались
  в общей ловушке от писем TEST» выполнено.
- Настройки стенда я не читал и не менял; `git show --stat 13b1aba5e` показывает, что коммит не
  трогает ни `deploy/`, ни env-файлы.

### Живое письмо шло настоящим путём, а не в обход

Проверил, что `/api/admin/smtp-test`, которым исполнитель делал живую пробу, — не отдельный
почтовый путь: `apps/webapp/src/app/api/admin/smtp-test/route.ts:36` зовёт `relayOutbound`, то есть
идёт webapp → integrator `relay-outbound` → `dispatchPort` → `EmailDeliveryAdapter`. Именно тот путь,
в котором стоит проверка.

---

## 5. Обходит ли адаптер какой-нибудь путь почты

Требование а2 — «ровно одно место». Замер:

```bash
grep -rn "sendMail(\|nodemailer\|createTransport" --include=*.ts --include=*.tsx apps/ scripts/ tools/
grep -rn "isLoopbackSmtpHost" --include=*.ts apps/integrator/src
```

Сетевую границу `sendMail` в продуктовом коде зовут **два** места:

1. `integrations/email/deliveryAdapter.ts:148` — единственный зарегистрированный email-адаптер
   (`app/di.ts:229`, других регистраций нет). Петлевая проверка стоит на строке 139, то есть
   ПОСЛЕ `clinicSmtp ?? resolveSmtpOutboundConfig(...)` (строки 125-130) — значит она видит и
   платформенный, и клинический профиль. Место ровно одно: `isLoopbackSmtpHost` объявлен один раз
   и вызывается один раз.
2. `app/operatorHealthProbeRunner.ts:356` — проба здоровья почты из планировщика. Она в адаптер
   НЕ заходит. Но на DEV она вообще не запускается: `operatorHealthProbeRunner.ts:549`
   `if (process.env.NODE_ENV === 'development') { details.email = 'skipped_development'; }`.
   Путь существует до этой работы и ею не затронут; дыры на DEV не даёт.

Все остальные вхождения — тесты, `index.ts`-реэкспорт и `isResolvedMailerConfigured` (проверка
конфигурации, не отправка). В `apps/webapp` своего SMTP нет.

Отдельно проверил, что подавление не превращается в ложное «канал работает»: адаптер ВОЗВРАЩАЕТ
подавление, а не бросает, поэтому `dispatchPort.ts:434-444` не уходит в fallback на платформенный
профиль (fail-closed), а `relayOutboundRoute.ts:355` отвечает `status: 'skipped'`. Проверка канала
клиники включает канал только по `status === 'accepted'`
(`webapp/src/app/api/admin/clinic-delivery-test/route.ts:113-116`), то есть инвариант C5(б) держится
и без прежнего `throw CLINIC_CHANNEL_PROBE_SUPPRESSED`.

---

## 6. Тесты по §10a / §10b

Изменён один тест-файл, добавлен один `it`:
`testDeliverySafety.test.ts:127` «on DEV sends email only through loopback SMTP and always suppresses telegram».

| вопрос §10b | ответ |
|---|---|
| Что сломается? | «На DEV в базе оказался непетлевой SMTP — платформа отправила настоящее письмо настоящему человеку из прод-налитых данных» |
| Дорого и молчаливо? | да и да: адресаты на DEV — реальные люди из прод-дампа, а отказ ничем не виден |
| Откуда oracle? | план владельца, п. 5 — независим от реализации |
| Дешёвый публичный слой? | да: настоящий `dispatchPort` + настоящий адаптер, подделаны только резолвер конфига и сетевая граница. Ни БД, ни UI, ни E2E |
| Дубль? | нет, единственный |
| Виден ли выход цепочки? | да: `sendMail` достигнут/не достигнут, `telegram.sent === []` |

Линейка владельца (15.09): (1) само не упадёт — стен на это нет; (2) при честной правке кода править
не придётся — тест не знает внутренней формы; (3) выход цепочки виден. Проходит.

`expect(mailFakes.sendMail).toHaveBeenCalledOnce()` — не антипаттерн §10a п.7: предметом проверки
является сам факт обращения к внешней границе, что §10a разрешает дословно.

Автоматических UI-тестов, тестов текста исходника, `.sql`-текста и заявок про DB/RLS в изменении нет.
Ни один существующий тест не удалён и не ослаблен: два `vi.mock` добавлены в шапку общего файла, но
прежние тесты работают через `recordingAdapter()` и настоящего адаптера почты не касаются — это
подтверждается тем, что они краснеют при инъекции E (значит живые).

**Замечание, НЕ findings:** тест утверждает дословный токен `'development_non_loopback_smtp_host'`,
скопированный из нашего же объявления `kernel/contracts/ports.ts:93`, которое меняется тем же
коммитом — формально это то, о чём предупреждает §10a («expected скопирован из нашего же
объявления»). Зубы теста от этого не зависят: инъекция B краснеет уже на
`suppressedByEnvironment`. Правки не требую.

---

## 7. Счёт инъекций

**8 инъекций, 4 непойманных (3 независимых класса).**

| # | что сломано | покрасневшее утверждение | итог |
|---|---|---|---|
| A | ранний выход email вынесен из dev-ветки (инъекция ведущего) | — | **НЕ ПОЙМАНО** (697/697 зелёных) |
| B | `isLoopbackSmtpHost` → всегда истина | `expected {} to deeply equal { suppressedByEnvironment: true, …(1) }` | поймано |
| C | снят `sendResult?.suppressedByEnvironment !== true` в `providerConfirmed` | — | **НЕ ПОЙМАНО** (60/60) |
| D | dev-гейт снова глушит email | `expected { suppressedByEnvironment: true } to deeply equal {}` | поймано |
| E | ветка продакшена глушит вместо пропуска | 30 красных в 3 файлах | поймано |
| F | worker теряет конкретную причину подавления | — | **НЕ ПОЙМАНО** (135/135) |
| G | dev-гейт пропускает ВСЕ каналы | `expected {} to deeply equal { suppressedByEnvironment: true }` | поймано |
| H | стена списка снята только для email (вторая форма A) | — | **НЕ ПОЙМАНО** (60/60) |

Про C и F, по §10a («непойманная поломка сама по себе задачей не становится» — сначала назови отказ
и приложи к критерию ступени 2):

- **C.** Отказ: «на DEV подавленное письмо считается подтверждённой доставкой провайдера, и здоровье
  интеграции `email` самоизлечивается на недоставке». Молчаливый — да. Дорогой — нет: только DEV,
  только показатель здоровья. Ступень 2 не проходит → факт в отчёт, не работа. Сама правка в коде
  верная и нужная: `platformIntegrationIdForChannel('email')` возвращает `'email'`
  (`dispatchPort.ts:219`), то есть без неё `onProviderDeliveryConfirmed` действительно бы сработал.
- **F.** Отказ: «в `last_error` строки очереди вместо `development_non_loopback_smtp_host` лежит общий
  `environment_delivery_suppressed`». Молчаливый — да, дорогой — нет: это диагностика на DEV.
  Ступень 2 не проходит → факт в отчёт. Проверил заодно, что новое значение безопасно: на
  `public.outgoing_delivery_queue` из CHECK-ограничений живёт только
  `outgoing_delivery_queue_status_check` по `status`, на `last_error` ограничений нет, а рядом уже
  пишутся такие же свободные токены (`rate_limited`, `workspace_mailings_disabled`).

Все инъекции откачены, дерево чистое:

```bash
git status --porcelain   # пусто
```

`npx tsc --noEmit` EXIT=0, `npx eslint` по восьми затронутым файлам EXIT=0 (прогон под замком хоста).

---

## 8. ВОПРОСЫ ВЛАДЕЛЬЦУ

Обе позиции — вне плана, поэтому это вопросы, а не работа.

1. **Почта клиники на DEV теперь глохнет молча, и увидеть её живой нельзя.** На DEV собственные
   SMTP-креды клиник налиты из прода, то есть хост непетлевой, и гейт их закрывает — правильно
   и по плану. Следствие: сценарий «письмо уходит от имени клиники» на DEV не проверяется в принципе.
   Перевести клинические профили на DEV в ту же ловушку (действие над данными, как пункт (б)),
   или так и оставить и смотреть этот сценарий только на TEST? Сегодня на DEV записей
   `clinic_smtp_outbound` нет вовсе, так что действие ничего не ломает.
2. **Прежний профиль `smtp_outbound` на DEV хранит боевое `mail.hosting.reg.ru` / `no-reply@bersoncare.ru`.**
   Живой код его не читает (§4), но экран настроек админа его показывает, и §1b.1 канона говорит,
   что реальных прод-значений в dev-env быть не должно. Чистить его или оставить как есть? Работа
   Л5 его намеренно не трогала, и это правильно — он вне двух ключей пункта (б).

---

## 9. Наблюдения (не findings, работой не становятся)

- `docs/ARCHITECTURE/SERVER CONVENTIONS.md:195` называет ловушку «Почтовый перехватчик TEST», хотя
  теперь в неё ходит и DEV. Факт не стал ложным, но строка неполна.
- Проба здоровья почты (`operatorHealthProbeRunner.ts:356`) — второй путь к `sendMail` мимо адаптера,
  со своим отдельным правилом среды на строке 549. Пускать её на DEV в ловушку смысла нет: круг
  замыкается через IMAP, а у mailpit IMAP нет. Трогать нечего, но знать про второе место полезно.
- `readSmtpOutboundSettingValueJson` (`infra/db/publicRestrictedSettings.ts:13`) и живая функция
  `app.read_integrator_smtp_outbound_setting()` — мёртвая пара без вызовов. Вне скоупа Л5.

---

## 10. НЕ СДЕЛАНО

- Полный CI (`pnpm run ci`, `scripts/ci-record.mjs`) не запускался — запрещён брифом. Запускает
  ведущий после приземления.
- Продуктовый код не исправлялся: MUST FIX 1 и 2 отданы текстом, тест за исполнителя не написан
  (бриф), канон `AGENTS.md` не правился.
- Прод не читался и не трогался. Настройки стенда TEST не читались и не менялись, режим ТЕСТ не
  снимался.
- Миграции не применялись и не создавались; `migrate-dev.sh --preflight` не гонялся, потому что
  коммит не несёт ни одной миграции и ни одной правки прав (`git show --stat`: ни `deploy/`, ни
  `db/drizzle-migrations/`).
- Второй Next-сервер из клона не поднимался. Живое письмо принято по уже лежащему в ловушке
  сообщению и отпечатку в `system_settings`, повторной отправки я не делал.
- Строка вердикта в `feat` не писалась — отдана ведущему текстом.
- Автоматических UI-тестов не создавалось.

---

## Строка вердикта для ведущего (занести в `feat`, не в ветку)

`FAIL — Л5 (13b1aba5e): пять требований п.5 выполнены и доказаны 8 инъекциями, но стена TEST_ACCOUNT_EMAILS для канала email не покрыта ничем (две инъекции зелёные на 697/697), и коммит оставил в силе противоречащую ему строку AGENTS.md:595 вопреки §0 п.4. MUST FIX 2, вопросов владельцу 2.`
