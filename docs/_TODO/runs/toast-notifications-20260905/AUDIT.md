# Независимый аудит: результат действия — общее всплывающее уведомление

Кандидаты: `05706d9b5`, `5b4b98d80` на `wt/toast-notifications-20260905` (49 файлов: 30 + 19).
Authority: `AGENTS.md` §9–§10b, §15–§17, §21, §24; требование владельца — успех/отказ действия
показывается всплывающим уведомлением сверху, а не постоянной строкой внутри формы/модалки;
валидация поля остаётся на месте; тесты на литеральную форму кнопки/текста/элемента вредны.

**Вердикт обоих коммитов: PASS — READY FOR LAND.**
Blind kill-set: **10 классов, закрыто 10 / непойманных 0.** Findings по §24.6 — **нет**.

## Слепой kill-set (составлен по authority ДО чтения изменённых тестов)

| # | Названная поломка | Метод | Итог |
|---|---|---|---|
| K1 | Правку записи приняли, но успех нигде не объявлен | тест | **убит** — новый acceptance-тест, fault injection A → красный |
| K2 | В теле панели осталась постоянная строка «Изменения сохранены» | взгляд (§24.4 — разовое действие) | **закрыт** |
| K3 | Успех объявлен, но слой правки не закрылся / детали не перечитаны | тест | **убит** — fault injection B → красный |
| K4 | Отказ сервера проглочен: врач считает, что сохранилось | живой прогон + тест | **убит** — живой отказ даёт `toast.error`, форма остаётся открытой с набранным текстом |
| K5 | В уведомление утекают машинные детали | взгляд + структурный гейт | **закрыт** — новых утечек нет |
| K6 | Заведена вторая система уведомлений | взгляд | **закрыт** — один `<Toaster>`, один монтаж |
| K7 | Валидация поля/формы превращена во всплывающее | взгляд + живой прогон | **закрыт** — канал валидации сохранён |
| K8 | Постоянный индикатор связи/состояния превращён во всплывающее | взгляд | **закрыт** |
| K9 | Отказ первичной загрузки превращён во всплывающее | взгляд | **закрыт** |
| K10 | Уведомление перекрывает управление модалки / нечитаемо на мобильном | живой прогон | **убит** — снимок 390×844 |

## Доказательства по пунктам гейта

**1. Переиспользован единственный тостер, второй системы нет.** В дереве ровно один `<Toaster>` —
`apps/webapp/src/components/ClientToaster.tsx:13`, смонтирован один раз в `apps/webapp/src/app/layout.tsx:63`.
Ни провайдера, ни хука, ни контекста, ни компонента не добавлено: диффом в `ClientToaster` изменены только
`toastOptions.success` и `toastOptions.error`. `react-hot-toast` импортируют 100 непроверочных модулей, и все
100 используют один и тот же дефолтный `toast` — параллельного API нет.

**2. Мотивирующий путь.** `DoctorCalendarEventPanel.tsx:676-678`: приём всех шагов правки → `toast.success('Изменения сохранены')`,
`setMode('view')`, `onChanged()`. Постоянная строка результата из тела панели удалена; `message` теперь уходит
только в `DoctorAppointmentForm` (`DoctorAppointmentForm.tsx:208`), то есть остаётся каналом валидации формы.
Из `DoctorAppointmentCancelModal` проп `message` убран вместе с его строкой.

**3. Отказ действия — то же уведомление, без машинных деталей.** Все ветки `!json.ok` в панели ушли в
`toast.error(panelErrorLabel(...))`. Новых утечек нет: репозиторный структурный гейт
`scripts/check-safe-user-error-door.mjs` (8 leak-фикстур красные, 5 безопасных зелёные) проходит.
Оставшиеся сырые проброски (`PatientMessagesClient.tsx:124`, `PatientTabFinances.tsx:285`,
`SaasBillingProviderSettings`, `OperatorHealthProbeSettingsSection`) перенесены дословно из прежней inline-строки —
предсуществуют, не регрессия этих коммитов.

**4. Валидация, индикаторы и первичная загрузка не тронуты.**
- Валидация: `AdminSettingsSection`, `AppParametersSection`, `AuthProvidersSection`, `EmailSmtpSection`,
  `NotificationsTopicsSection`, `OperatorHealthAlertsSection`, `WebPushVapidSection`, `DoctorCourseEditForm`,
  `PatientTabFinances` — состояние `error`/`cashError` сохранено и по-прежнему рендерится на месте.
- Постоянные индикаторы: `DoctorChatPanel.tsx:311` и `PatientMessagesClient.tsx:157` держат `error` для отказа
  чтения; во всплывающее ушёл только отказ отправки. `SecretSettingInput` сохранил постоянный признак
  «credential настроен».
- Первичная загрузка: `AppointmentPaymentSection.tsx:100-103` оставляет `setError` для первого чтения и рисует
  `<p role="alert">`; во всплывающее ушёл только путь действия `run()`. `CommercialConstructorClient` сохранил
  `loadError`. `ScheduleSetupTab` стал строже: отказ сохранения больше не сносит секцию в `phase:'error'`.

**5. Изменённые тесты держат поведение, а не форму.** 7 файлов: предмет проверки прежний (пользователь видит
авторский текст отказа, а не машинный код и не молчание), изменился только канал — с присутствия узла в DOM на
факт доставки. Литеральная вёрстка, количество и источник не заморожены; `SettingsForm.ui.test.tsx` вместо
`findByText('Сохранено')` проверяет сам факт одного успешного уведомления, что ближе к §10a.

**6. Геометрия на мобильном.** Живой прогон на изолированном `:5301`, вьюпорт 390×844, DPR 2, сессия владельца-доктора.
Отказ записи комментария смоделирован перехватом ответа В БРАУЗЕРЕ (`route.fulfill` 500) — продуктовый код не менялся.
Снимок `50-fail-toast.png`: уведомление «Комментарий не сохранён.» садится сверху по центру, целиком на экране,
поверх модалки; destructive-палитра (`rgb(180,84,75)` на светлой заливке с рамкой), 16px, радиус 8px, читается.
Управление модалки НЕ перекрыто: `Отмена` и `Сохранить` остаются видимыми и кликабельными (`box y=796`, `visible=true`).
Перекрывается только заголовок «Детали записи» — не элемент управления. Гаснет само в пределах 4.5 с.
Форма правки при отказе осталась открытой с набранным текстом — результат не теряется.

## Прогоны

- `pnpm --dir apps/webapp exec vitest run` по 7 изменённым файлам — **7 файлов / 39 тестов PASS**.
- Тот же прогон после добавления acceptance-теста: `DoctorCalendarEventPanel.ui.test.tsx` — **16/16 PASS**.
- `pnpm --dir apps/webapp typecheck` — exit 0.
- `pnpm --dir apps/webapp lint` — exit 0 (включая `check-safe-user-error-door`, `media upload door`, остальные структурные гейты).
- Fault injection (по одному на независимый класс, §24.5): **A** — убрать `toast.success('Изменения сохранены')` → 1 failed;
  **B** — убрать `setMode('view')` + `onChanged()` → 1 failed. Обе правки откачены, дерево чистое.

Full CI не запускался: изменения — одно приложение, без shared-пакетов, корневых конфигов, lockfile и
межприложенческих контрактов (§9, §10 «Уровни» → phase-level).

## Рекомендации и вопросы владельцу (НЕ findings, работой автоматически не становятся)

1. `EmailSmtpSection` — «Укажите корректный email получателя» (ответ `invalid_body` на пробную отправку) раньше
   стоял строкой под самим полем, теперь гаснет за 3 с. Это исход действия, но текст называет поле: стоит ли
   такие «валидация через сервер» оставлять у поля?
2. `PatientTabFiles.tsx:740` — отказ удаления файла остался inline (`deleteError`) внутри диалога подтверждения,
   тогда как успех ушёл в уведомление. Диалог не закрывается, поэтому результат не теряется, но канал разный.
3. Успех без уведомления там, где у соседних действий оно появилось: `AppointmentPaymentSection.run()` (принятая
   оплата), `PayTariffButton.saveBillingEmail`, `SecretSettingInput`. Прежде успеха не показывали и там — не
   регрессия, но единообразие неполное.
4. `AppointmentPaymentSection.run()` показывает отказ, если упал только последующий перечит `load()` — принятая
   оплата будет объявлена неудачей. Предсуществует, перенесено дословно из `setError`.
5. `CommercialConstructorClient.tsx:1361,1510` — «Выберите действие после триала/после оплаченного периода» ушли
   во всплывающее. Это валидация формы, но путь недостижим: кнопка отправки заблокирована при пустом селекте
   (`:1496`), а неявная отправка формы отключённой кнопкой не срабатывает. Поэтому не finding.
6. Косметика: удаление блоков оставило по лишней пустой строке в `ClinicPublicCardSection.tsx` и
   `AdminMergeAccountsPanel.tsx`. Линт чистый.

## Незакрытое живое наблюдение (не относится к этим коммитам)

В правке записи изменение ТОЛЬКО поля «Длительность, мин» с последующим «Сохранить» в 2 попытках из 2 на
DEV `:5301` не дало ни запроса, ни уведомления, ни inline-сообщения — форма молча осталась открытой
(`60-ok-aftersave.png`). При этом каждый предзапросный guard `submitEdit` ставит inline-сообщение, а путь
«ничего не изменилось» заканчивается `toast.success`, то есть обработчик, похоже, не выполнялся вовсе.
Отделить это от артефакта браузерной автоматизации за этот проход не удалось. Поведение одинаково до и после
изменения (прежний успех шёл через тот же обработчик), поэтому регрессией `05706d9b5`/`5b4b98d80` не является.
Стоит отдельной проверки.

## Гигиена

Аудитом закоммичены только `DoctorCalendarEventPanel.ui.test.tsx` (acceptance-тест), этот артефакт и строка
очереди. Продуктовый код не изменялся; обе временные поломки откачены (`git status` чист). Общий процесс `:5200`
не трогался — живая проверка шла на отдельном `:5301` из этого worktree.
