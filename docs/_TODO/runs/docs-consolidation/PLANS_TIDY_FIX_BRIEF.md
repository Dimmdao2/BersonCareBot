# Закрыть находки аудита порядка в планах + сверить схему с цитатником владельца
(run: worker-plans-tidy-fix)

**Аудит:** `docs/_TODO/runs/docs-consolidation/PLANS_TIDY_AUDIT.md` — вердикт «доработать»: из 1121
удалённой строки правилом оказалась одна. Задача маленькая и точная.

## Часть 1 — вернуть потерянное (F1–F3)

- **F1 (обязательно).** При сведении пяти копий блока «перед стартом этапа» в раздел 0 из шести элементов
  канона перенесено пять: потерян пункт **«правила ведения документации и логов»**. Вернуть его в §0
  `WORK_ORDER.md` дословно, на прежнее место в перечислении.
- **F2 (обязательно).** Из удалённого `D12B_CENSUS_RESULT.md` (строка 261 в `38b331392^`) пропала оговорка:
  **недостижимость по контенту ≠ недостижимость в рантайме** — у людей остались старые сообщения с уже
  выданными кнопками, поэтому вырезанный обработчик может получить нажатие. Перенести смысл в
  `D12B_REACHABLE_CENSUS.md`. Это «как не делать», а не история.
- **F3 (по возможности).** Вернуть в `WORK_ORDER.md` указатели на разделы «Универсальный режим исполнения
  многоэтапного плана» и «Урок 2026-07-22» — одна строка навигации.

F4 и F5 аудитор сам назвал низкой тяжести — не трогать, чтобы не раздувать.

## Часть 2 — сверка схемы с цитатником (главное)

Аудитор в разделе «вне области» перечислил **восемь указаний владельца, которые не читаются в схеме
идентичности**, и отдельно отметил: удалением их не потеряли — их там не было. Проверить и закрыть.

Источник — `docs/_TODO/runs/integrator-cleanup/OWNER_QUOTE_2026-07-31_IDENTITY.md`, **читать целиком, все
три порции.** По каждому указанию владельца ответить: где оно читается в
`IDENTITY_AND_MERGE_SCHEME.md` как ДЕЙСТВУЮЩЕЕ правило. Если нигде — вписать, дословно по смыслу цитаты,
в подходящий раздел схемы. Названные аудитором примеры (проверить в первую очередь):

- «при вводе email — на email» (куда уходит код по введённому идентификатору);
- «надо двухфакторку заводить»;
- «„контакт для входа" и „контакт для доставки" — мне нравится» (разделение назначения контакта);
- «насчёт срока годности подтверждения — кто так делает? я не видел» (**запрет**: срока годности у
  подтверждения контакта мы не вводим).

Остальные четыре найти самому, пройдя цитатник построчно.

## Границы

- Только `WORK_ORDER.md` и файлы прогона `integrator-cleanup`. Код, архив и `docs/ARCHITECTURE/` не трогать.
- **Цитатник не редактировать вообще** — он источник истины.
- Не изобретать правил: в схему попадает только то, что сказал владелец. Своё понимание, не подтверждённое
  цитатой, — строка в «развилки», вопросом ему.
- Не наращивать объём: правило — одна-две строки, а не абзац рассуждений.

## Приёмка

`docs/_TODO/runs/docs-consolidation/PLANS_TIDY_FIX_REPORT.md`: F1–F3 (что вернул, куда) + таблица
«указание владельца → где теперь читается в схеме → было ли уже». Раздел «развилки» обязателен.
