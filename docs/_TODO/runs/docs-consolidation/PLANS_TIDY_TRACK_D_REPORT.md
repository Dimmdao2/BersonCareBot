# Порядок в рабочих планах Track D — отчёт

Run: `worker-plans-tidy-trackd`. Правки сделаны, не предложены. Код не тронут, `docs/_ARCHIVE/` и
`docs/ARCHITECTURE/` не тронуты. Цитатник владельца `OWNER_QUOTE_2026-07-31_IDENTITY.md` не редактировался.

## 1. Что сделано по файлам

| файл | что сделано | было → стало |
|---|---|---|
| `UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` | Полная пересборка. Пятикратно повторённый блок «⛔ перед стартом этапа» сведён к одному разделу 0. Четыре одинаковых абзаца SUPERSEDED в D3/D4/D5/D7 заменены одним правилом 5.1.1. Заведён раздел 5.1 «правила Track D» — девять правил в форме «как делать / как не делать», вытащенных из историй отдельных пунктов. Из D1/D2/D5/D12/D13a/D13b/D18a убран пересказ заходов и откатов, оставлены правило + коммит-доказательство. Удалён абзац «Execution order… D10 is always last», противоречивший единому порядку. D10a переписан по ответу владельца 31.07. Пункты D20–D30 упорядочены по номерам. Добавлена навигационная таблица 5.5 по семействам уведомлений. | 628 → 489 |
| `runs/integrator-cleanup/D12B_CENSUS_RESULT.md` | **Удалён целиком.** Перепись состояния, которого больше нет (писалась до `bf45f5853` и до D13a/D13b/D14), заменена файлом `D12B_REACHABLE_CENSUS.md`; пути внутри указывают на чужой клон `bcb-wt-tariff`. Из живых планов на неё не ссылается никто; три ссылки остались в брифах завершённого прогона D12 (`D12_DEAD_BRIEF.md`, `D12_D18A_AUDIT_BRIEF.md`, `D12_D18A_FIX_BRIEF.md`) — брифы завершённых прогонов я по постановке не трогал, ссылки в них теперь битые. | 267 → 0 |
| `runs/integrator-cleanup/D12B_REACHABLE_CENSUS.md` | Из шапки убран рассказ «заменяет устаревший файл, который писался до…». §4 «Спорные — вопросы» превращён в таблицу «вопрос → ответ владельца 31.07 → пункт плана»: девять вопросов закрыты, повторно их задавать запрещено. В сводной таблице сняты три ячейки «нет решения владельца». | 354 → 329 |
| `runs/integrator-cleanup/D15A_IDENTITY_RESEARCH.md` | Раздел 6 «Развилки, которые закрывает владелец» (восемь развилок с вариантами и рекомендациями) свёрнут в таблицу «о чём была развилка → действующее правило и где оно записано». Статус исправлен: развилки закрыты, документ описывает текущее поведение интегратора и порядок переноса. | 476 → 436 |
| `runs/integrator-cleanup/IDENTITY_AND_MERGE_SCHEME.md` | Убран список «закрыто владельцем (вторая порция ответов)» — перечень отменённых развилок. Исправлена нумерация развилок: ссылки на несуществующие №6/№13/№15 переведены на действующие №2/№3/№4. Снят пересказ провенанса («уточнение владельца, меняющее прежнюю формулировку», «закрывает дыру D7/D8/D10 аудита»). Раздел «владелец пометил правило как нестандартное» заменён на §11: исследование `IDENTITY_MODEL_RESEARCH.md` сделано, приведён его вывод, названо, что решает владелец. Добавлена шапка «это авторитет для D25–D29». | 320 → 324 |
| `runs/integrator-cleanup/IDENTITY_SCHEME_AUDIT.md` | Добавлена шапка из трёх строк: прогон завершён, 17 вопросов раздела 3 владелец закрыл 31.07, действующий список развилок — только в схеме. Тело отчёта не тронуто. | 152 → 156 |
| `runs/integrator-cleanup/D16_LOOP_CENSUS.md` | Из таблиц А и Б удалены колонки «остаётся или уходит / причина»: вердикты теперь живут в D30 и противоречили ему (например, B1 — «остаётся» против «переезжает»). Разделы «что мешает свести» и «итог арифметики» переписаны по действующему состоянию: снята запись «D10a блокирован неотвеченным вопросом» (владелец ответил 31.07), снят как отдельный вопрос сюжет про судьбу процесса `scheduler` — он стоит развилкой №1 в D30. | 144 → 118 |
| `runs/integrator-cleanup/D30_SCHEDULER_REVERSAL_PLAN.md` | Снята развилка №6 и пункт «до ответа сносить журналы нельзя» — владелец ответил 31.07. Из шапки убрана ссылка на номер строки плана (ломается при любой правке) и служебная строка «это шаг 1». Поправлена ссылка на переписанный раздел D16. | 416 → 416 |
| `runs/integrator-cleanup/D20_INTEGRATOR_MAP.md` | Две развилки из двенадцати закрыты ссылкой на уже принятое решение: №8 (судьба процесса `scheduler`) — это развилка №1 D30; №11 (какая очередь целевая) — решено D10a/D30 §3. Ссылка на номера строк плана заменена на ссылку на пункт. | 687 → 686 |

**Итого по тронутым файлам: 3444 → 2953 строки (−491).**

Файлы прогонов, которые я НЕ трогал: 35 файлов `*_BRIEF.md` и 8 `*_REPORT.md` завершённых прогонов — по
постановке «архив и завершённые прогоны не трогать вообще». Единственное исключение — трёхстрочная шапка у
`IDENTITY_SCHEME_AUDIT.md`, потому что без неё следующий агент понесёт владельцу 17 уже отвеченных вопросов.

## 2. Удалённое правило-в-мусоре — построчно

| что убрано | почему это можно убрать |
|---|---|
| Блок «⛔ ПЕРЕД СТАРТОМ ЭТАПА» ×5 в WORK_ORDER | Один и тот же текст пять раз; правило не исчезло — оно в разделе 0. |
| Абзац SUPERSEDED ×4 (D3, D4, D5, D7) | Один и тот же текст четыре раза; стал правилом 5.1.1 с тем же сценарием отказа. |
| WORK_ORDER, D1: «HISTORY (reverted grant attempt)» + «REMAINING for a true tick» | История отменённого захода. Правило из неё («права не выдаются ролью — грант роняет TEST») сохранено как 5.1.5. |
| WORK_ORDER, D5: «AUDIT ROUND 2 FIX» — рассказ о раунде аудита | Правило («писать под явным org-принципалом, иначе RLS молчит») сохранено как 5.1.4 с тем же коммитом. |
| WORK_ORDER, D5: «Note found, not fixed here: D3/D4 чекбоксы всё ещё `[ ]`» | Бухгалтерия чужого пункта, к D5 отношения не имеет; на момент правки уже неверна. |
| WORK_ORDER, D12: «Первый заход срезал 1189 строк… отменён лидом» | Рассказ о заходе. Урок сохранён как правило 5.1.6 (у исполнителя нет тестовой сети). |
| WORK_ORDER, D13a: разбор «аудит опроверг посылку, на которой пункт стоял изначально» | Опровергнутая посылка + её опровержение. Осталось действующее решение: решает вебапп, доставляет интегратор. |
| WORK_ORDER, D13b: «Прежняя формулировка: …» | Дословно отменённая редакция рядом с действующей. Запрет «не резать раньше потребителя» сохранён. |
| WORK_ORDER, D18a: «История двух неудачных кругов», «отдельный аудит не запускался осознанно» | История заходов. Действующее устройство сторожа (смотрит на файл, а не на форму аргумента) сохранено. |
| WORK_ORDER: «Execution order: D0 first… D10 is always last» | Прямо противоречил разделу «единый порядок исполнения», который сам же объявлял такие пометки недействительными. |
| WORK_ORDER: пометки про taskdb `#981`, `#635/#621` и «конкурировавший вариант D1» в трёх местах | Сведено в один абзац про taskdb-привязку. |
| Схема: «Закрыто владельцем 31.07 (вторая порция ответов): …» — 9 позиций | Перечень закрытых развилок. Сами правила стоят в §1–§10; список нужен был только для сверки с аудитом. |
| Схема: «(закрывает дыру D7/D8/D10 аудита)», «(ответ 31.07, вторая порция)» ×6 | Провенанс правки, а не правило. Запрет («экран не подсказывает, какие каналы есть у владельца номера») переписан как правило. |
| D15a §6: восемь развилок с вариантами (a)/(b)/(c) и «безопасными умолчаниями» | Владелец ответил: «семь развилок по идентичности — это не вопрос». Варианты, из которых выбор уже сделан, читаются как живые альтернативы. |
| D12b §4: девять вопросов «не решались самостоятельно» | Все девять — это «девять вопросов переноса» из цитаты, на которые владелец ответил дословно. |
| D16 census: колонки вердиктов в таблицах А и Б | Прямо противоречили вердиктам D30 по тем же строкам (B1, B2, B4, B5b, B5c). Одно решение — одно место. |
| D16 census + D30 + D20 map: «вопрос про `outgoing_delivery_queue` записан и ответа не получил» ×3 | Ответ получен 31.07: выбор таблицы инженерный. Три документа держали работу заблокированной задним числом. |

«CANON-NNN» и подобных выдуманных исполнителями требований в области не найдено (`grep -rn "CANON-[0-9]"` — пусто).

## 3. Пять тем владельца — где каждая теперь читается

| тема | где действующее правило | где встроено в работу |
|---|---|---|
| **Идентичность и слияние аккаунтов** | `IDENTITY_AND_MERGE_SCHEME.md` §1, §2, §5 (с шапкой «авторитет для D25–D29») | WORK_ORDER: правило 5.1.1, пункты **D25** (интегратору остаётся доставка входа), **D26** (слияние как инструмент поддержки, автослияние только без медистории), **D15a/D15b** (порядок переноса) |
| **Экран входа по номеру** | `IDENTITY_AND_MERGE_SCHEME.md` §3 (куда уходит код, повтор, «подтвердить другим способом», несколько почт) | WORK_ORDER, пункт **D27** — состав экрана и запрет подсказывать состав каналов |
| **Кириллица в ФИО** | `IDENTITY_AND_MERGE_SCHEME.md` §6 | WORK_ORDER, пункт **D29** — включая отмену автоподстановки имени из мессенджеров и OAuth; открытое (накопленные латинские имена) стоит развилкой №1 схемы |
| **Триггеры уведомлений** | Навигационная таблица WORK_ORDER §5.5 — семь семейств с адресом правила по каждому | **D21** самопомощь · **D22** таксономия категорий · **D26** тревога о взломе · **D27** код входа · **D13a/D13b/D14** напоминания о приёме · **D30** расчётные триггеры периода · §6 кнопка «отписаться» |
| **Разворот архитектуры запуска (D30)** | `D30_SCHEDULER_REVERSAL_PLAN.md` — вердикт по каждой cron-точке, слияние очередей, шаги Ш0–Ш9 | WORK_ORDER, пункт **D30** (решение, две оси, два условия-гейта) + шаг 9 порядка исполнения: D10a, D16 и D30 идут одной работой |

## 4. Развилки — что я вынес вопросом, а не решил

1. **«Пять семейств триггеров уведомлений»** из постановки — я не нашёл в документах области списка ровно из
   пяти семейств. Собрал таблицу §5.5 из семи семейств, каждое подтверждено словами владельца 31.07.
   **Вопрос:** имелся в виду именно этот набор, или пять — это список из
   `QUOTAS_AND_MECHANICS_DESIGN_2026-07-28.md` (уведомления периода тарифа), который я в область не брал?
2. **Порядок исполнения для D21–D24 и D26–D29.** В тексте плана прямая зависимость записана только у D25/D26
   («по схеме, шагами D15b, до D17»). Для D21–D24 я поставил их шагом 7 — после переноса доменов D3–D8 и до
   сноса транспорта, потому что это тот же класс работы «продуктовое решение уезжает в вебапп».
   **Вопрос:** порядок верен, или D21–D24 должны идти раньше/позже?
3. **D2, хвост:** дневник пациента при нескольких клиниках сегодня fails-closed. Вопрос владельцу, нужен ли
   мультиклинический дневник, был записан ещё 24.07 и ответа не имеет — оставил его в тексте пункта.
4. **Развилки, которые я НЕ трогал и которые продолжают ждать ответа:** пять развилок схемы идентичности,
   шесть развилок D30 (топология процессов, лестница каналов, медиа-cron, сторож B5a, частота проб),
   двенадцать развилок карты D20 (из них две закрыл ссылкой на уже принятое решение), три вопроса в
   `runs/owner-questions.md`.

## 5. Чего я не смог выяснить

1. **Совпадает ли моё разделение «журнал / очередь» в D10a с тем, что владелец имел в виду 30.07.** Он сказал
   «`public.outgoing_delivery_queue` убирать», но сказал это про описание, которое сам план 31.07 признал
   неверным (её описали как второй журнал вебаппа, тогда как это единственная живая очередь доставки, которую
   крутит воркер интегратора). Ответ 31.07 («какую именно — мне насрать, лишь бы работало, лишнее вырезать»)
   я прочитал как снятие прежней формулировки, и D30 уже построен на том, что очередь остаётся. Если это
   прочтение неверно — рушится и D10a, и раздел 3 плана D30.
2. **Точный список того, что владелец считает «рабочими планами» за пределами двух названных путей.** Я
   ограничился `WORK_ORDER.md` и `runs/integrator-cleanup/*.md`. В `docs/_TODO/` лежит ещё около 50 план-файлов
   (в том числе `OWNER_PUNCHLIST_2026-07-28.md`, `SAAS_FOUNDATION/*`), где те же правила могут быть записаны
   третий раз своими словами — я их не проверял, это вне области.
3. **Все ли ссылки на удалённый `D12B_CENSUS_RESULT.md` учтены.** В `docs/` их три, все — в брифах
   завершённого прогона D12 (перечислены в таблице §1); трогать эти брифы постановка запрещает, поэтому
   ссылки остались битыми осознанно. В taskdb, в логах прогонов и в `/home/dev/dev-projects/.lead/` я не
   искал. Если битые ссылки в завершённых брифах неприемлемы — файл восстанавливается из git одной командой.
4. **Верны ли сегодня номера строк кода**, которые я перенёс из старых редакций (`bookingLifecycleRoute.ts`,
   `writePort.ts:1634`, `executeAction.ts:811`). Я их не сверял с кодом: правки были текстовые, а сверка
   279 ссылок — отдельная работа. Ссылки на файлы верны, номера строк могли уехать.
