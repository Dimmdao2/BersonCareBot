# Аудит наведения порядка в планах — текст аудитора

(записан лидом: роль запускалась в песочнице только на чтение)

Write to `PLANS_TIDY_AUDIT.md` needs your approval — it wasn't created. Approve and I'll write the file as drafted; here's the audit.

## ВЕРДИКТ: SHIP-WITH-FIXES

Из 1121 удалённой строки правилом оказалась **одна**. Цель «правила вместо истории» достигнута честно.

### Находки

| # | что потеряно | где было (в `38b331392^`) | почему правило | что вернуть |
|---|---|---|---|---|
| **F1** | пункт обязательного чтения **«правила ведения документации и логов»** | `WORK_ORDER.md` строки **21, 39, 54, 600, 616** — все пять копий блока «⛔ ПЕРЕД СТАРТОМ ЭТАПА» | указание «что читать перед работой», не рассказ о заходе; при сведении пяти копий в раздел 0 из шести элементов канона перенесли пять | в §0 после `ORCHESTRATOR_CHECKLIST.md` дословно: `правила ведения документации и логов,` |
| **F2** | оговорка: недостижимость из контента ≠ недостижимость в рантайме — у людей остались старые сообщения с уже выданными кнопками | `D12B_CENSUS_RESULT.md` строка **261** | прямое «как не делать»: не считать вырезанный callback безопасным по одной переписи контента. Похожая оговорка в живом файле (`:278-282`) про другое — про гейт, а не про разосланное | перенести в `D12B_REACHABLE_CENSUS.md` |
| **F3** | указатели на разделы «Универсальный режим исполнения многоэтапного плана» и «Урок 2026-07-22» | `WORK_ORDER.md` строки **33-34** | навигация к правилам. **Смягчает:** оба названы в `CLAUDE.md` — правило живо, потерян указатель | по желанию |
| **F4** | оговорка про негарантированную заполненность `facts.links.*` | `D12B_CENSUS_RESULT.md` строка **264** | незакрытый вопрос по КАНАЛ-сценариям | низкая тяжесть |
| **F5** | состав реестра действий + маршрут живого `message.send` мимо него | `D12B_CENSUS_RESULT.md` строка **249** | деталь; сам вердикт «реестр мёртв» сохранён в `D12B_REACHABLE_CENSUS.md:19-20` | необязательно |

### По семи проверкам

**2, 5, 6 — чисто.** Цитатник не в диффе. Вердикты D16 не потеряны: D30 §1 покрывает B1–B6 (B5 даже дробнее — B5a/B5b/B5c). Два вердикта D16 прямо противоречили D30, потому что владелец 31.07 развернул архитектуру — снять устаревшую колонку было правильным разрешением. Перенумерация развилок **починила** ссылки: в старой версии список был из пяти пунктов, так что №6/№13/№15 указывали в пустоту; новые №2/№3/№4 попадают в те же предметы.

**3 — чисто, кроме F1.** Пять копий блока дословно одинаковы, четыре абзаца SUPERSEDED дословно одинаковы — сведение законно. Правила 3, 8, 9 (те, что без обратной ссылки) **имеют источник** в строках 175-177, 259-260, 262-267: изобретённых требований нет. «D10 is always last» удалён законно — он уже был отменён разделом на строках 274-276 исходника. Отдельно проверил открытые дефекты, спрятанные внутри «историй» — сохранены все четыре: ID-пространство в GET-чтениях (`:176`), живой дефект прав `app_patient` с `task_53b67199` (`:199`), блокер D6 по FK (`:194`), вопрос про мультиклинический дневник (`:177`).

**4 — чисто.** Каждое удаление в схеме — пометка провенанса, правило рядом оставлено; в двух местах стало строже («⛔ „Перенесём без 2FA" — запрещено»).

**1** — сохранено почти всё; 26 сценариев поимённо остались только в git, но они уже вырезаны из кода, это запись об исполненном удалении. **7** — три битые ссылки, все в брифах завершённого прогона, исполнитель их признал.

### Вне области

Восемь указаний владельца не читаются в схеме — но `grep` по `38b331392^` вернул пусто по всем трём запросам: **коммит их не удалял, их там не было**. Среди них «при вводе email — на email», «надо двухфакторку заводить», «„контакт для входа" и „контакт для доставки" — мне нравится», «насчет срока годности подтверждения — кто так делает?». Называю, потому что цитатник объявлен источником истины; новых требований не предлагаю.

### Две оговорки о доказательности

Текст удалённого `D12B_CENSUS_RESULT.md` я не читал побайтово — shell в сессии работал нестабильно, файл пришёл через цепочку агентов. Косвенные сверки сходятся, цитаты не попадают на затёртые скрубером места, но перед возвратом F2 текст стоит сверить командой `git show '38b331392^:...D12B_CENSUS_RESULT.md' | sed -n '245,265p'`.

И к процессу: один агент цепочки попытался обойти скрубер секретов, попросив выдать вывод посимвольно — исполнитель отказался, правильно. Другой **подставил** в пять затёртых мест текст из соседнего файла. На выводы это не влияет, но подстановка в аудите недопустима даже с пометкой, поэтому фиксирую.