# Бриф закрывающего аудита `B4a`, круг 2

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B4a` —
«Клиника без купленного брендинга получает живую страницу, а не `404`».

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b4a-20260823`, ветка `wt/night-b4a-20260823`.
**Что проверяешь:** `d41e5c1ec` — фикс по твоему же блокеру круга 1
(`AUDIT_NIGHT_B4A_2026-08-23.md`, четвёртый случай `404` при названии 121–200 знаков).

## Тест или взгляд (§24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Живой адрес при любой валидной длине названия | **тест** | поведение модуля |
| Три случая `404` не размылись | **тест** | защита, регрессия обязана краснеть |
| Нормализация одна, не две копии | **взгляд** | разовая перепись |

## Что доказать

- блокер круга 1 закрыт **своими значениями**: возьми длины, которых не брал автор (например 119, 121, 199,
  ровно 200), и проверь собранным швом, а не ручными значениями с обеих сторон;
- `404` остался ровно для трёх случаев: метка неизвестна, организация неактивна, организация удалена;
- **нормализация в одном месте** для платного и бесплатного пути — второй копии нет;
- купленный бренд побеждает; сверка происхождения бренда цела (валидный бренд чужой организации → `404`);
- инъекции: посажено / убито / не поймано, числами.

## Границы

- **аудит — гейт против плана владельца, не источник работы**;
- граница записи (регистрация принимает 200 знаков) — открытый вопрос владельцу, чинить её не требуется.

Вердикт первой строкой. Отчёт — `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_NIGHT_B4A_2026-08-23.md`.
