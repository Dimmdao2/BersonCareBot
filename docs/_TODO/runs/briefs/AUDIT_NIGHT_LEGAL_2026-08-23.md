# Аудит слайса «юридические документы»: один комплект на обе поверхности

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2d —
«Комплект один на всю платформу, а не по поверхностям.»

**Что проверяешь:** коммит `3d5d74e65` на ветке `wt/night-legal-20260823`, автор — Codex `gpt-5.6-terra`/high
по брифу `docs/_TODO/runs/briefs/NIGHT_LEGAL_ONE_KIT_BRIEF_2026-08-23.md`.
**Клон:** `/home/dev/dev-projects/bcb-wt-audit-legal-20260823`.

## Что проверить под лупой

Автор заявил: один legal-маршрут на обе поверхности; `title`, Apple title и manifest согласованы; заведён
единый источник реквизитов со статусом `awaiting-owner-input`; тексты документов не менялись; 4/4 теста.

1. **Тексты документов действительно не тронуты.** Это прямой запрет владельца 23.08 («тексты я буду
   переписывать не в этом проходе»). Докажи `git diff` по содержательным файлам, а не поверь на слово.
   Правка текста — блокирующая находка.
2. **Дефект `S-3` закрыт по-настоящему:** одна страница не может отдавать `title` одной поверхности, а
   `apple-mobile-web-app-title` и manifest — другой. Проверь ЖИВЬЁМ на поднятом dev-сервере (свой порт, не
   5200), а не только тестом: запроси `/legal/terms` с обеих поверхностей и сравни всё, что видно.
3. **Инъекция:** верни расхождение идентичности руками и докажи, что тест краснеет. Не краснеет — гейт
   бумажный, это находка.
4. **Второго дерева юридических страниц нет** — перепроверь своим поиском.
5. **Реквизиты**: место одно, значения пустые и это ВИДНО пользователю честно, а не выглядит готовой
   страницей с пустыми полями. Скажи, что увидит человек сегодня.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_LEGAL_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
