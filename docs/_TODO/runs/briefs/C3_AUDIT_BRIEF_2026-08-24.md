# Audit brief — `C3`, круг 1 (независимый, адверсарный)

Repo canon: `AGENTS.md` — read §10a, §10b, §24.4, §24.5 in full before classifying anything.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C3`,
дословно: «удалить любой достижимый platform fallback для них.»

Under audit: commit `0277423d0` on branch `wt/therapysto-c3-20260824`, clone
`/home/dev/dev-projects/bcb-wt-therapysto-c3-20260824`. Author: Codex `gpt-5.6-terra`/high. His claim: семь
интентов (OTP, подтверждение контакта, жизненный цикл записи, уведомление о созданной записи, напоминание о приёме,
повторяющееся напоминание пациенту, ответ доктора) переведены на `clinic_required` через существующий dispatch port;
три инъекции красные; 68 тестов зелёные.

## Классификация «тест или взгляд» (§24.4) — обязательна, по пунктам

| Пункт | Природа | Способ доказательства |
| --- | --- | --- |
| Перепись интентов ПОЛНАЯ — нет восьмого, который автор пропустил | разовое действие | собственный обход кода (`rg` по dispatch/sender/бот-вызовам), а не сверка с таблицей автора |
| Каждый брендированный интент уходит как `clinic_required` | повторяемое поведение | слепой kill-set по оракулу ДО чтения авторских тестов |
| Достижимый platform fallback УДАЛЁН, а не просто не выбирается | повторяемое поведение | тест краснеет, если fallback вернуть |
| Клиника без своего бота получает внятный отказ, а не тихую отправку через платформенный | повторяемое поведение | свой тест на своих значениях |
| Небрендированная (standard) пациентская поверхность НЕ изменилась | повторяемое поведение | регрессия на своих значениях |
| Доставка staff/admin НЕ изменилась | повторяемое поведение | регрессия |
| Один chokepoint, а не второй параллельный отправитель | разовое действие | взгляд на diff; если два места делают одно — это вопрос «свести в одну функцию с параметром?», запиши его |

## Жёсткие факты этого бокса

- В `system_settings` на TEST лежит НАСТОЯЩИЙ прод-токен бота. Никогда не ставь `telegram_mode=long_polling` —
  это украдёт обновления живого прод-бота. Не отправляй ничего реальным получателям.
- Не трогай `apps/webapp/src/modules/auth/authDeliveryGate.ts` — им владеет соседняя ветка `F2`.
- Не трогай `app.read_integrator_clinic_delivery_credential` и обе миграции `20260823T030000…` / `20260823T043206…`;
  обе роли не разрешать. В `feat/doctor-ui-rebuild` не вливать.

## Уровень и объём

Уровень `app`, но затронуты ДВА приложения (`apps/integrator` и `apps/webapp`) — гоняй таргетные наборы обоих.
Full CI не гонять: корневые конфиги, lockfile и контракты между приложениями не менялись.

## Дедлайн

Уложись в 25 минут. Отчёт и свои тесты закоммить в клоне ДО этого. `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_C3_2026-08-24.md`, первой строкой вердикт
`PASS, FOR LAND` или `FAIL, NOT FOR LAND`, затем строка на каждый пункт: `ID → PASS|FAIL|BLOCKED → evidence`,
счёт инъекций и ответ на вопрос автора: надо ли сводить OTP и request-contact в один параметризованный конверт.
