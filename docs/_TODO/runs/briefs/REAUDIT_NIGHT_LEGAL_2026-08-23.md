# Повторный аудит слайса «юридические документы», круг 2

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2d —
«Реквизиты юрлица подставляются, тексты — нет.»

**Что проверяешь:** коммит `25a528d8e` на ветке `wt/night-legal-20260823` — фикс по единственной блокирующей
находке круга 1. Круг 1: аудит `58ca95ce7`, вердикт `FAIL`, отчёт
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_LEGAL_2026-08-23.md`.
**Клон:** `/home/dev/dev-projects/bcb-wt-reaudit-legal-20260823`.

## Один вопрос круга

Круг 1 нашёл: единый источник реквизитов заведён, но страницы его не читают. Фиксер заявил, что теперь
`terms` и `privacy` берут данные из `LEGAL_DOCUMENT_OPERATOR.requisites`, а при пустых значениях человек
видит: «Реквизиты оператора ожидают уточнения владельцем. Не указаны: Наименование юридического лица, Адрес,
ИНН, ОГРН». Инъекция `test-legal-entity-from-source` доехала до обеих страниц на порту 5210, источник
восстановлен пустым. 6/6 тестов, typecheck и scoped ESLint зелёные.

Проверь это ЖИВЬЁМ на своём порту (не 5200 и не 5210) и **своей** инъекцией, а не повтором авторской:

1. Подставь значение в источник — доехало ли оно до обеих страниц.
2. Верни пусто — виден ли честный статус, и не выглядит ли страница готовой с молча пустыми полями.
3. **Сломай связь** (убери чтение источника) — краснеет ли тест. Не краснеет — гейт бумажный, это находка
   того же класса, что круг 1 нашёл у соседнего слайса.

## Что подтверждено кругом 1 и НЕ переоткрывается

Тексты документов не изменены, второго дерева legal-страниц нет, обе поверхности отдают одинаковые `title`,
Apple title, manifest и видимый текст, инъекция идентичности ловится. Перепроверь только то, что мог задеть
фикс, — и скажи прямо, если задел.

**Отдельно проверь запрет владельца:** реквизиты не должны быть придуманы — ни наименования, ни адреса, ни
ИНН, ни ОГРН, даже как пример или как тестовое значение, оставшееся в коде. Найдёшь придуманное значение —
блокирующая находка.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/REAUDIT_NIGHT_LEGAL_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
