# Бриф: коррекция этапа A по вердикту аудита FAIL

- **План-файл (единственный источник todo и done):**
  `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, этап `A`, требования `TPB-01`,
  `TPB-08`, `TPB-09`, `TPB-15`, `TPB-16`. Твой DONE = закрытые пункты этого брифа.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел.
- **Отчёт аудита целиком:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_STAGE_A_2026-08-22.md`
  (коммит `d37390274`). Прочитай его до правок — там способы воспроизведения каждой находки.
- **Клон:** `/home/dev/dev-projects/bcb-wt-therapysto-stage-a-20260822`, ветка
  `wt/therapysto-stage-a-20260822`. Правишь поверх `c3136e23e` + `d37390274`.

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-08` — «Branding влияет только на patient-facing surface; staff/admin видят Therapysto. Доказательство:
cross-surface metadata/UI tests.» — и требование `TPB-09` — «Standard patient name/origin меняются deploy
config без data migration; clinic domain/integrations остаются org-scoped DB settings. Доказательство: config
test и settings ownership tests.»

Все три находки ниже соответствуют пунктам плана владельца — поэтому их чиним, а не обсуждаем.

## Делать

- [ ] `X1` **`F1`: экраны входа доктора и админа клиники показывают `Therapygo`.** Воспроизведение:
  `curl -s <хост>/app/doctor/login | grep -o "<title>[^<]*</title>"` даёт `Therapygo`; то же на
  `/app/admin/login`, и там же видимая брендовая строка шапки и `apple-mobile-web-app-title`.
  Причина: route-группа `app/app/(role-login)/**` не имеет своего layout и наследует корневой fallback,
  который теперь пациентский. Это прямое нарушение `TPB-08`: «staff/admin видят Therapysto».
  Корневой fallback ОСТАВИТЬ пациентским — он правильно обслуживает лендинг, `/book`, `/join`, `/legal`,
  `/app/patient`. Чинить надо тем, что staff-экраны входа заявляют свою идентичность сами, а не наследуют
  чужую. Способ выбираешь ты, но он обязан быть тем же seam'ом, а не третьим местом с именем.

- [ ] `X2` **`F2`: переменные окружения не долетают до клиентских компонентов.** Воспроизведение аудитора:
  подняв dev с `PATIENT_APP_NAME=QA-Renamed`, экран `/app/patient/login` показывает `QA-Renamed`, а
  `/app/contact-support` — `Therapygo`. Это два экрана в один клик друг от друга. Нарушено `TPB-09`:
  «name/origin меняются deploy config».
  Все 12 продуктовых `'use client'` call-site'ов читают литерал из `productSurfaceNames.ts` и override
  игнорируют. Чинить **через существующий choke point, а не заводя новый** (`TPB-16`): в дереве уже есть
  `@/shared/ui/PlatformProvider`, подключённый в корневом layout — разумный кандидат, чтобы серверное
  значение доезжало до клиента одним путём. Проверь, подходит ли он, и если нет — обоснуй выбор в отчёте.
  `productSurfaceNames.ts` после этого остаётся дефолтом для схемы env, а не источником для UI.

- [ ] `X3` **`F3`: два user-visible вхождения вне `apps/webapp/src`.**
  - `apps/webapp/public/maintenance.html:8,75` — эту страницу отдаёт nginx по `error_page 502/503/504`
    и на PROD, и на TEST. Она общая для обеих поверхностей, хост в ней не известен, поэтому имя платформы
    (`Therapysto`) — правильный выбор; но она статическая, конфиг в неё не импортируется. Так и оставь
    статикой, значение впиши строкой, и **добавь рядом короткий комментарий, что это дубль имени, который
    надо править руками** — иначе следующий rename её снова забудет.
  - `apps/webapp/public/sw.js:55` — `showNotification(title || 'BersonCare')`. Сначала **докажи**, какая
    поверхность реально использует этот service worker и кто получает уведомление с пустым заголовком
    (аудитор указывает на `patientWebPushNotify.ts:232`), и только потом ставь имя. Обоснование — в отчёт.
  - Повтори инвентаризацию **по всему репозиторию**, а не только по `apps/webapp/src`: команда плана шире,
    и исполнитель первого круга сузил её. Найденное раздели на runtime/product, technical и history.

- [ ] `X4` **`F4`: описать новые переменные в `.env.example`.** `PATIENT_APP_NAME` и `PATIENT_APP_ORIGIN`
  сейчас не описаны нигде, кроме схемы. Дефолты у них ОСТАЮТСЯ — это санкционированное ведущим отступление
  от буквы `A1` (владелец выбрал значения 22.08.2026), переигрывать его не надо.

## НЕ делать

- Не трогать: `modules/auth/passkeyAuth.ts`, `modules/staff-security/totp.ts`, `apps/integrator/**`,
  npm-имена, имена таблиц/ролей/колонок БД, `bersoncare-tweakcn-theme.css`.
  **Привязка passkey к домену не меняется ни на йоту** — аудит подтвердил, что она цела, не сломай.
- Не менять `id`, `scope`, `start_url` ни у одного манифеста — это контракт уже установленных приложений.
- Не трогать подписи в письмах пациентам (`sendBookingConfirmationEmail.ts`) — это этап C.
- Не начинать этапы B и C: резолвер по Host, клиничный брендинг, OAuth, рассылки — не твоё.
- Не удалять тесты, добранные аудитором в `staffPwaManifest.unit.test.ts`.
- PROD, TEST, деплой, БД, push — не трогать. Коммитить только свои файлы поимённо, `git add -A` запрещён.

## Отдельно: тихая утечка, которую нашёл аудитор

Аудитор проверил инъекцией: если клиентский компонент импортирует `config/productSurfaces`, сборка НЕ падает,
но `config/env.ts` уезжает в браузерный чанк вместе с секретами (`grep -rl
"dev-session-secret-change-me-min-16" .next/static/` → 1 файл). Сегодня этого не происходит, но ничто не
мешает следующему агенту сделать такой импорт.

Если твой способ решения `X2` закрывает эту дыру заодно — хорошо, скажи об этом в отчёте. Если нет — **не
изобретай под неё отдельную машинерию**: напиши строкой в отчёт как наблюдение для владельца. Это не пункт
плана, и разгонять из него скоуп запрещено.

## Приёмка

- [ ] `V1` Воспроизвести команды аудитора из находок `F1` и `F2` и показать, что теперь ответ другой:
  `/app/doctor/login` и `/app/admin/login` дают `Therapysto`; `PATIENT_APP_NAME=QA-Renamed` меняет имя
  на ВСЕХ пациентских экранах, включая `/app/contact-support`.
- [ ] `V2` Инвентаризация по всему репозиторию: каждое оставшееся вхождение перечислено с причиной.
- [ ] `V3` Имя по-прежнему задаётся в одном месте; третьего места не появилось.
- [ ] `V4` typecheck, scoped lint, `next build` и затронутые тесты зелёные.

Отчёт: что изменено с путями, что найдено но не тронуто и почему, что осталось вопросом владельцу. Числа —
вместе с командой, которой получены.
