# Audit brief — `C1` + `C2`, круг 1 (независимый, адверсарный)

Repo canon: `AGENTS.md` — прочитай §10a, §10b, §24.4, §24.5 целиком до классификации.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункты `C1` и `C2`,
дословно: «Сохранить одну global patient Yandex config в `system_settings` с secret envelope и существующим»
и «resolver допускает одну global config только на включённых patient-поверхностях, а signed state и exact».

Под аудитом: `69f69a6dc` на `wt/therapysto-c12-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-f2-20260824`
(имя каталога от прежней ветки — это нормально, проверь `git branch --show-current`). Автор: Codex `gpt-5.6-terra`/high.

Заявление автора: один резолвер Yandex OAuth для start/callback по `ResolvedSurface`; разрешены только пациентские
поверхности, включённый провайдер и точный DB-allowlist callback URL; state связывает поверхность, нормализованный
origin и organization ID, callback сверяет повторно; `yandex_oauth_redirect_uri` стал global `string_list` с чтением
прежнего одиночного значения; прежние отдельные getters удалены; 5 тестов зелёные.

## Красный флаг, проверить первым

Автор пишет: «`tsc --noEmit` блокируется отсутствующими workspace-зависимостями `luxon`, `drizzle-orm`, `pg`».
Это значит, что typecheck у него по-настоящему НЕ прошёл. Прогони его сам как положено (при необходимости
`pnpm install --frozen-lockfile` в клоне) и скажи прямо: типы целы или нет. Зелёные пять тестов при неработающем
typecheck — не доказательство.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ доказательства |
| --- | --- | --- |
| `C1-1` конфигурация ОДНА и глобальная, per-org клиничных конфигураций не заведено | разовое действие | взгляд на diff и `rg` по хранилищу настроек |
| `C1-2` секрет идёт через существующий secret envelope и существующий settings write service | разовое действие | взгляд: какой сервис пишет, каким конвертом |
| `C2-1` вход по Yandex доступен только на ВКЛЮЧЁННЫХ пациентских поверхностях | поведение | слепой kill-set ДО чтения авторских тестов |
| `C2-2` подмена host / org / provider отбивается signed state и точным allowlist | поведение | свои инъекции на КАЖДЫЙ из трёх векторов отдельно |
| `C2-3` staff и platform_admin вход не изменился; пациентский вход на обычной поверхности не изменился | поведение | регрессия на своих значениях |
| `C2-4` один резолвер, а не второй параллельный путь; прежние getters действительно удалены, а не осиротели | разовое действие | взгляд на diff |
| `C2-5` совместимость: прежнее одиночное значение `yandex_oauth_redirect_uri` продолжает читаться | поведение | тест на старом формате значения |
| `C2-6` типы и линт | разовое действие | собственный прогон, см. красный флаг выше |

Отдельно проверь: что происходит при ПУСТОМ allowlist и при значении с лишним слэшем/портом — точное сравнение
не должно превращаться в «почти совпало».

## Границы

Не трогай доставку и `senderScope` (соседняя ветка `C3`), `app.read_integrator_clinic_delivery_credential` и обе
миграции `20260823T030000…` / `20260823T043206…`. Настоящих секретов в репозиторий не класть. В `feat` не вливать.
Finding вне owner scope — вопрос владельцу, не работа. Уровень `app` (`apps/webapp`), full CI не гонять.

## Дедлайн

Уложись в 25 минут. Отчёт и свои тесты закоммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_C1_C2_2026-08-24.md`, первой строкой вердикт,
строка на каждый пункт, счёт инъекций.
