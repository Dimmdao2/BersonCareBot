# Аудит слайса B3: один резолвер поверхности и место, где он стоит

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B3` —
«Реализовать один `RequestSurfaceResolver` и подключить к существующему request choke point.»

**Что проверяешь:** коммит `d1a46d880` (плюс задел `a1602a74e`) на ветке `wt/night-b3-20260823`.
Автор — Codex `gpt-5.6-sol`/high в два захода: первый остановлен портом в фазе `blocked_system` на 26-й
минуте без отчёта, второй доводил чужой задел. **Это делает аудит важнее обычного: часть кода писалась
вслепую поверх незавершённой работы, и проверки на первом заходе не гонялись ни разу.**
**Клон:** `/home/dev/dev-projects/bcb-wt-audit-b3-20260823`.

## Что заявил автор

Резолвер оставлен в `proxy.ts`; matcher расширен так, что proxy теперь проходит `/`, clinic slug, `/legal`,
`/book`, `/join`, `/setup`, `/app`, `/api`, манифесты и service worker, а статика и Next internals остаются
снаружи. Единственный production-вызов `resolveRequestSurface({` — `1`. Matcher — один литерал. Host-матрица
покрывает `staff`, `patient_default`, `patient_branded`; unknown/duplicate/inactive Host → hard 404. Устранён
циклический импорт metadata ↔ manifest. 57 тестов в трёх файлах + 3 теста host-only cookie зелёные.

## Что проверить под лупой

1. **Расширение matcher'а — самое опасное место этой работы.** Теперь proxy выполняется на публичных
   страницах, которых раньше не касался. Проверь ЖИВЬЁМ на своём порту (не 5200): не сломались ли `/`,
   визитка клиники, `/legal`, `/book`, `/join`; не появилось ли лишних редиректов и не просела ли статика.
   Отдельно проверь, что service worker и манифесты отдаются корректно.
2. **`next build` обязан быть зелёным.** Литерал matcher'а — доказанное ограничение Next: собери сам через
   общий замок хоста и покажи `BUILD_ID`.
3. **Hard 404 без отката на платформу** — инъекция: подсунь неизвестный, дублирующийся и неактивный Host,
   докажи, что каждый даёт 404, а не платформенную страницу.
4. **Единственность резолвера** (`TPB-16`): перепроверь своим поиском, не появился ли второй источник
   поверхности; автор считал `git grep`, посчитай иначе.
5. **Инвариант cookie host-only** не сломан — тест из ветки аудита схемы обязан остаться зелёным.
6. **Задел `a1602a74e` писался вслепую.** Пройди по нему отдельно: удалён `shared/lib/http/getRequestOrigin.ts`
   — докажи, что ни один живой вызывающий не остался; переписаны оба манифеста — сравни `id`, `scope`,
   `start_url` с тем, что было ДО, потому что их смена ломает уже установленные приложения.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B3_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
