# Вход по коду из почты сломан: `permission denied for table platform_users` при проверке кода

**СРОЧНО: это блокирует владельца и живой TEST-гейт D15b/6.**

**Роль:** worker. **Канон:** `AGENTS.md` — сначала карта заголовков
(`grep -n "^## \|^### " AGENTS.md`), затем §6 (PostgreSQL/роли), §«Миграции schema B: generated
snapshot + timestamp forwards», **§«⛔ Миграция не выдаёт и не отзывает права. Никогда»**,
§5 (границы), §10/§10a (тест проверяет ПОВЕДЕНИЕ), §24.
Поиск — сначала `node /home/dev/brain/tools/code-search.mjs "<q>" --repo bcb`, потом grep.

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «D15b/6 — контакты в `user_contacts`». Живой гейт этого чекбокса — обычный вход существующего владельца по коду; сейчас он падает, значит чекбокс закрыть нельзя.

## Симптом и уже собранные доказательства (ведущий, не перепроверяй заново)

Владелец получает письмо с кодом, вводит его — интерфейс отвечает «неверный код». На самом деле
проверка кода падает ДО сравнения, с отказом прав. Две его попытки, TEST, `journalctl -u
bersoncarebot-webapp-test`:

```
Aug 22 01:40:11  [cause]: error: permission denied for table platform_users
                 severity: 'ERROR',  code: '42501'
Aug 22 01:41:20  то же самое
```

Место отказа названо в самом сообщении:

```
where: 'SQL statement "SELECT 1
  FROM public.platform_users AS candidate
  WHERE candidate.id IN (
    SELECT challenge.user_id FROM public.email_challenges AS challenge
    WHERE challenge.email = v_email_normalized)
  ORDER BY candidate.id
  FOR UPDATE"
PL/pgSQL function app.email_otp_public_consume_latest_challenge(text,text) line 28 at PERFORM'
```

Функция `SECURITY DEFINER`, владелец — `app_seam_email_otp_owner`. Фактические права на таблицу
(`information_schema.role_table_grants`, обе именованные базы дают ОДИНАКОВЫЙ ответ, то есть это не
дрейф TEST, а декларация):

```
app_object_owner              | DELETE,INSERT,REFERENCES,SELECT,TRIGGER,TRUNCATE,UPDATE
app_seam_email_otp_owner      | DELETE
app_seam_identity_lookup_owner| DELETE
app_seam_password_auth_owner  | DELETE
app_staff                     | DELETE,SELECT
```

То есть у владельца функции на `platform_users` есть только `DELETE`, а строке нужен как минимум
`SELECT`, и из-за `FOR UPDATE` — ещё и `UPDATE` (Postgres требует UPDATE для блокировки строки).

Функция появилась миграцией `apps/webapp/db/drizzle-migrations/20260821T040000_cut_over_canonical_contacts.sql`
(cutover D15b/6 от 21.08). До 20.08 вход по почте работал — в очереди доставки есть успешная
отправка 20.08 23:02 со статусом `sent`, а сломалось после этой выкатки. **Это регрессия вчерашнего
дня, а не древний дефект.**

## Что сделать

1. **Сначала реши, нужен ли этот `FOR UPDATE` вообще.** Прочитай тело функции целиком и пойми, что
   она защищает этой блокировкой. Возможны два честных исхода, выбери обоснованно и напиши почему:
   - блокировка нужна → владельцу функции выдаётся МИНИМАЛЬНЫЙ набор прав, которого ей хватает;
   - блокировка лишняя/избыточная (например, дублирует блокировку самой `email_challenges`) → убрать
     её timestamp-forward миграцией, и тогда прав добавлять меньше или не надо вовсе.
   ⛔ Не расширяй права «на всякий случай» и не выдавай роли больше, чем требует названная строка.
2. **Права — ТОЛЬКО через декларацию и генератор** (`deploy/postgres/privileges/declaration.ts` →
   штатный generate). ⛔ `GRANT`/`REVOKE` внутри миграции запрещены каноном, это отдельное жёсткое
   правило §«Миграция не выдаёт и не отзывает права. Никогда».
3. **Проверь двух соседей.** `app_seam_identity_lookup_owner` и `app_seam_password_auth_owner` имеют
   ровно тот же подозрительный набор `DELETE`. Установи по их функциям, сломаны ли они так же
   (вход по паролю, резолв личности). Если да — почини тем же проходом, это один класс дефекта.
   Если нет — напиши, почему им хватает.
4. **Докажи поведением, а не рассуждением.** Нужен тест, который краснеет на текущем продукте и
   зеленеет после правки. Уровень — существующий механизм проверки прав/RLS из §10b
   (`pnpm test:db-privileges` и соседние наборы), не новая машинерия.
5. **Живое доказательство на именованной DEV, rollback-only:** прогнать реальный путь
   «запросить код → ввести код → вход» под существующей учёткой, без создания новых пользователей.
   Если это невозможно rollback-only — скажи прямо, живую часть сделает ведущий.

## Запрещено

`GRANT`/`REVOKE` в миграции; расширение прав сверх необходимого; выдача широкой роли;
`migrate-dev.sh --execute`, применение миграций, деплой, TEST/PROD-запись, остановка сервисов;
фикстуры, новые пользователи/клиники, disposable-базы; `git add -A`; push; полный CI.
Preflight на DEV — rollback-only, и его делает ведущий после твоего коммита.

## Формат сдачи

Отчёт: причина своими словами, какой вариант из п.1 выбрал и почему, точный список добавленных прав
с обоснованием каждого, вердикт по двум соседним ролям, пара «красный/зелёный» из п.4 с реальным
выводом. Обязательная секция `НЕ СДЕЛАНО:`.
**Коммить до конца хода.** Не заканчивай ход в ожидании фонового процесса.
