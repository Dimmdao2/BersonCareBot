# Бриф аудитора: круг 4 этапа A — финальный гейт

## Классификация «тест или взгляд» (AGENTS.md §24.4)

| Пункт | Класс | Почему |
|---|---|---|
| 1 — гейт краснеет на инъекции в matcher | **поведение** | инъекция неисправности, не чтение |
| 2 — лид-адрес клиники и staff-фактор отдают Therapysto | **поведение** | живой прогон и клик, не дифф |
| 3 — класс закрыт: обход ОТ staff-входов | **поведение** | доказательство должно идти от входов, а не от таблицы |
| 4 — единственность источника matcher'а | **взгляд** | разовое суждение по графу импортов |
| 5 — регрессия по кругам 1–3 | **поведение** | прогон прежних команд |
| 6 — скоуп не разогнан | **взгляд** | сравнение диффа с брифом |

Постоянных тестов «в коде нет такой-то строки» не создавать.

Ты **независимый адверсарный аудитор**. Это ЧЕТВЁРТЫЙ круг и последний: три предыдущих закрылись `FAIL`.
Отчёт исполнителя и зелёные тесты доказательством НЕ являются.

- **План-файл:** `IMPLEMENTATION_PLAN.md` (папка `THERAPYSTO_PATIENT_BRANDING_INITIATIVE`), этап `A`,
  `TPB-08`, `TPB-16`, Gate A. 🔴 В шапке — режим владельца: этапы в `feat` сводит только он.
  **Приземления не предлагать ни при каком вердикте.**
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.
- **Прошлый вердикт:** `AUDIT_STAGE_A_ROUND3_2026-08-22.md`. **Бриф круга:**
  `docs/_TODO/runs/briefs/THERAPYSTO_STAGE_A_FIX3_BRIEF_2026-08-22.md`.
- **Что проверяешь:** клон `/home/dev/dev-projects/bcb-wt-therapysto-stage-a-20260822`, коммит `1fe5f5660`.

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-08` — «Branding влияет только на patient-facing surface; staff/admin видят Therapysto. Доказательство:
cross-surface metadata/UI tests.»

## Что проверить

1. **Гейт перестал быть слепым.** Круг 3 показал: `config.matcher` был вторым литералом, и инъекция
   «убрать `/` из matcher'а» оставляла гейт 33/33 зелёным. Воспроизведи эту инъекцию сам. Плюс придумай
   СВОИ инъекции в тот же шов — цель в том, чтобы найти инъекцию, которую гейт всё ещё пропускает.
2. **Обход ОТ staff-входов, а не от дерева маршрутов.** Круг 3 провалился на круговом доказательстве:
   ожидаемая поверхность бралась из той же таблицы, которую обход проверял. Исполнитель заявляет список из
   16 staff-входов, полученный тремя измерениями (ссылки лендинга, общий `AuthFlowV2`, пути в письмах
   персоналу и приглашениях). **Собери список СВОИМ способом и сравни**: если твой шире — это находка.
3. **Две закрытые страницы:** `/app/contact-support?from=clinic-demo` и `?from=staff-factor`. Второй
   исполнитель нашёл сам и закрыл сверх брифа — оцени, законная ли это правка того же класса или выход за
   узкий круг.
4. **Единственность источника matcher'а.** Исполнитель утверждает: вынести список даже в константу того же
   файла нельзя, `next build` падает («matcher needs to be a static string»), поэтому источник — литерал в
   `proxy.ts`, а гейт импортирует `config`. Проверь утверждение сборкой, а не чтением.
5. **Регрессия по кругам 1–3:** staff-экраны входа, `PATIENT_APP_NAME=<чужое>`, контракт манифестов,
   passkey rp-id, юридические страницы, запрещённые файлы, секрет в бандле, soft-navigation.
6. **Скоуп.** Всё ли ложится на `Z1`/`Z2` плюс названное исполнителем исправление сверх.

## Границы

- Находка вне плана владельца — **вопрос владельцу, а не работа**.
- Ничего не чинить самому. PROD, TEST, деплой, БД, push — не трогать. Порт 5200 не трогать.
- **Не находки, закрыты владельцем или брифом:** голый `/app` при одном хосте («никак, я сразу дам оба
  домена»); подделка `x-bc-pathname` вне matcher'а (место — этап `B`); установка PWA с лендинга даёт
  приложение персонала (владелец: приложение выбирает хост).
- Оговорка: при живом `next dev` (Next 16.2.11) `tsc` падает на сгенерированном
  `.next/dev/types/routes.d.ts` — это не код, лечится `rm -rf .next/dev`.

## Вердикт

`PASS` или `FAIL`, у каждой находки — путь, строка, способ воспроизведения. Отдельной секцией: что проверил
и проблем не нашёл. Числа — вместе с командой.
