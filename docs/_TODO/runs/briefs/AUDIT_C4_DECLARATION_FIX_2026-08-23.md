# Аудит фикса регрессий `C4`: утечка шаблона между арендаторами и корень bootstrap

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C4` —
«`C4` Расширить existing SMTP config только sender display data, добавить один org-scoped transactional template».

**Что проверяешь:** коммит `e61afcf69` на ветке `wt/therapysto-night-20260823`.
**Клон:** `/home/dev/dev-projects/bcb-wt-therapysto-night-20260823`.
**Автор:** Codex `gpt-5.6-sol`/high. Ты не слабее автора — проверяй скептически, отчёт автора доказательством не является.

## Что заявил фиксер

- `FC-1`: корень `app.read_integrator_clinic_delivery_credential(text,uuid)` при чужом `organization_id`
  возвращал шаблон чужой клиники (`returned_foreign_template=t` на живой DEV, rollback-only). В миграцию
  `20260823T010000_mail_profile_reaches_auth_delivery.sql` добавлен предикат
  `setting.organization_id = app.current_org_id()`.
- `FC-2`: рантайм зовёт 11-аргументную `app.email_auth_start_challenge`
  (`apps/webapp/src/infra/repos/pgEmailAuth.ts:71`); каталог bootstrap в
  `deploy/postgres/privileges/port-context-catalog.test.mjs:224` исправлен на неё.
- Инъекции: 2 посажено, 2 убито, 0 не поймано. Полный CI `rc=0`, 486 с.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Утечка шаблона чужой клиники через definer-корень | **Тест** | Стена арендатора; регрессия молчаливая и уходит в письма живым людям |
| Какую сигнатуру зовёт рантайм на пути bootstrap | **Взгляд** | Разовая сверка вызывающего с каталогом, `файл:строка` |
| Полнота правки: нет ли ВТОРОГО такого же незащищённого чтения в том же слайсе | **Взгляд** | Структурный обход definer-корней, изменённых `C4` |
| Соответствие пункту `C4` плана владельца | **Взгляд** | Сверка с owner-регистром |

## Что проверить

1. **Главное: правка закрывает утечку, а не прячет её.** Воспроизведи утечку сам на живой DEV
   (rollback-only, своими значениями, не переписывая замер автора): принят принципал клиники X, аргумент —
   `organization_id` клиники Y. До правки корень отдавал шаблон Y; после правки обязан не отдавать ничего.
   Проверь и обратное: **своя** клиника по-прежнему получает свой шаблон — предикат не должен сломать
   штатный путь `C4` (иначе письма клиники останутся без шаблона и слайс бесполезен).
2. **Нет ли второго такого же корня.** `C4` трогал не одну функцию. Обойди все definer-корни, изменённые
   слайсом, и проверь каждое чтение стенованной таблицы. Если найдёшь второй незащищённый — это блокер.
3. **`FC-2` — правка теста, а не кода: докажи, что это правда.** Найди вызывающего сам. Если где-то в
   репозитории остался вызов шестиаргументной сигнатуры — она сегодня падает
   `email_auth_start_challenge: mail_profile_required`, и это живой дефект, а не устаревший каталог.
4. **Инъекции — свои, минимум по одной на каждый фикс, плюс две сверх заявленных автором.** Числа
   красных до и после приведи.
5. **Полный CI по ветке — обязателен**, только через общий замок хоста:
   `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`. Приведи `rc`, длительность, `runs/ci-last.json`,
   сверь `HEAD` до и после. Автор упоминает нестабильный `PhoneMessengerAuthFlow.ui.test.tsx` — проверь
   это утверждение отдельно, а не принимай на слово: нестабильность, выданная за нестабильность, чтобы
   спрятать регрессию, — ровно то, что аудит обязан ловить.
6. **Миграция:** `GRANT`, `REVOKE`, `CREATE POLICY` в ней быть не должно (проверь грепом, не глазами).

## Границы

- **Не чинить.** Находка без пункта в плане владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы тесты и временные инъекции, откатываемые до конца прогона.
- PROD и TEST не трогать. Живая DEV — только rollback-only. Порт `5200` не занимать.

## Канон

- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел.
- Поиск по коду — `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, не слепой grep.

## Отчёт

Вердикт `PASS, FOR LAND` или `FAIL, NOT FOR LAND` первой строкой. Числа: блокирующих, неблокирующих,
инъекций посажено / убито / не поймано. Отчёт положи в
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_C4_DECLARATION_FIX_2026-08-23.md` и закоммить.
