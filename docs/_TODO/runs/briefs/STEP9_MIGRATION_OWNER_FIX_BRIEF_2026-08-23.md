# Бриф-фиксер: секция Ш9 идёт под владельцем, у которого нет доступа к схеме `app`

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D15b/7a** (шаг Ш9).
  Отказ держит сведение DEV и выкатку TEST.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`; §1 (миграции НИКОГДА не выдают гранты — права
  идут декларацией и reconcile), §5, §6, §10a, §24.

**Источник оракула:** `apps/webapp/db/drizzle-migrations/20260822T200000_patient_demographics_leave_the_actor_root.sql` — «-- BCB-MIGRATION-OWNER: app_seam_public_booking_owner»

## Что сломано (замер ведущего, живой отказ)

`bash deploy/host/migrate-dev.sh --execute` на DEV:

```
NOTICE:  D15b/7a Ш9 backfill counts: height_cm=0, weight_kg=0, gender=1, birth_date=0
ERROR:  permission denied for schema app
EXIT=3
```

Перенос данных прошёл, падает СЛЕДУЮЩАЯ секция — та, что начинается со строки 132 и объявляет владельцем
`app_seam_public_booking_owner`: она переписывает тело `app.resolve_public_booking_client_by_phone(...)`,
то есть работает в схеме `app`, а у этого владельца туда прав нет.

Заметь: `--preflight` у автора шага проходил. Значит preflight этот случай не ловит — это отдельная находка,
назови её в отчёте (чинить preflight в этом заходе НЕ надо).

## Задание

1. **Замерь, а не предполагай:** какие права у `app_seam_public_booking_owner` на схему `app` сегодня, и
   какой владелец в репозитории штатно правит тела функций схемы `app` (посмотри соседние миграции того же
   дня — там несколько секций с разными владельцами, и они проходят).
2. **Почини секцию так, чтобы она шла под правильным владельцем**, не расширяя ничьих прав. Если окажется,
   что владелец выбран верно, а не хватает именно права — **права выдаются декларацией и reconcile, НИКОГДА
   миграцией** (§1); тогда правь декларацию и объясни, почему это право законно.
3. **Проверь ВСЕ остальные секции этой миграции** тем же способом — не только упавшую. Если упадёт
   следующая, прогон встанет снова; перечисли проверенное.
4. **Смысл шага не менять:** четыре демографических поля уже уехали в клинический профиль, чтение и запись
   называют одну колонку; правка только про владельца секции.
5. **Доказательство:** прогони `bash deploy/host/migrate-dev.sh --preflight` — EXIT=0 — и, поскольку
   preflight этот класс не ловит, **дополнительно материализуй секции в откаченной транзакции на DEV под
   теми же владельцами** и покажи, что ни одна не падает по правам.
6. `pnpm test:db-privileges`, typecheck, оба `--check` побайтно. **`--execute` ЗАПРЕЩЁН** — его гоняет ведущий.

## Границы
Деплой, TEST, `push`, full CI — не твои. Прод не трогать. Preflight не чинить — только назвать находку.
Отчёт: `docs/_TODO/runs/integrator-cleanup/STEP9_MIGRATION_OWNER_FIX_2026-08-23.md`.
