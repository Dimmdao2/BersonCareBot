# Audit brief — `C1` + `C2`, круг 2 (закрывающий)

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C1`,
дословно: «Сохранить одну global patient Yandex config в `system_settings` с secret envelope и существующим».

Под аудитом: `7cccd0650` на `wt/therapysto-c12-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-f2-20260824`
(каталог назван по прежней ветке — проверь `git branch --show-current`). Правку делал ведущий (Opus 5), не воркер.

Круг 1 (`AUDIT_C1_C2_2026-08-24.md`): `C2` — PASS, 5 из 5 инъекций пойманы; блокер один и не в резолвере, а в
записи — реестр объявил `yandex_oauth_redirect_uri` как `string_list`, а форма осталась однострочным полем с
`validateHttpUrl`, поэтому allowlist из двух и более origin через интерфейс записать было нельзя.

Правка: поле стало многострочным (по адресу в строке), значения разбираются существующим `parseIdTokens`,
каждый проверяется `validateHttpUrl`, сохраняется массив; чтение (`adminSettingsData.ts`) научилось показывать
и массив, и прежнюю одиночную строку. Регрессия — `AuthProvidersYandexAllowlist.ui.test.tsx` (3 теста; при
возврате однострочной записи краснеют 2).

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `R2-1` список из двух и более адресов доходит до базы и до резолвера целиком | поведение | свой сквозной тест: запись формы → значение настройки → решение резолвера |
| `R2-2` прежнее одиночное значение продолжает работать после правки чтения | поведение | свои значения в обоих форматах |
| `R2-3` мусорная строка в списке не сохраняется и не ломает остальные | поведение | своя инъекция |
| `R2-4` регрессия ведущего краснеет при откате правки | разовое действие | сними правку записи — тест обязан упасть |
| `R2-5` вход staff/platform_admin и пациентский вход на обычной поверхности не изменились | поведение | регрессия |
| `R2-6` типы и линт затронутых файлов | разовое действие | свой прогон; про состояние клона см. круг 1 |

## Границы

Не трогать доставку и `senderScope` (ветка `C3`), `app.read_integrator_clinic_delivery_credential` и обе миграции.
Настоящих секретов в репозиторий не класть. В `feat` не вливать. Уровень `app`. Full CI не гонять.
Finding вне owner scope — вопрос владельцу, не работа.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_C1_C2_2026-08-24.md`: первой строкой вердикт,
строка на каждый пункт, счёт инъекций.
