# Календарный корень отдаёт refresh-токен ЧУЖОЙ клиники — закрыть стену

Повторный аудит круга 3 дал **FAIL**. Отчёт — `docs/_TODO/runs/integrator-cleanup/D17_REAUDIT_2026-08-23.md`,
прочитай целиком.

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «узкая роль интегратора не мешает доставке»

Канон — `AGENTS.md` (`grep -n "^## \|^### " AGENTS.md`), §1, §5, §10a.

## Что найдено

`app.read_integrator_google_calendar_setting(text,uuid)` выбирает строку **по аргументу**, а не по
принятому контексту. Живьём: под контекстом клиники A узкая роль прочитала `google_refresh_token`
клиники B.

**Это НЕ регрессия D17.** Аудитор доказал двумя независимыми снимками до-D17-состояния, что тело
выбирает строку по аргументу с самого своего появления. То есть дыра жила до сегодняшних правок, и
круг 3 её не создавал — он закрыл соседний credential-корень, за который получил FAIL круг 2.

## Что сделать

1. Закрыть календарный корень так же, как закрыт credential-корень в круге 3: организация берётся из
   принятого контекста, аргумент только СВЕРЯЕТСЯ, несовпадение — `42501`. Форма — та же, что у соседа,
   чтобы двери не разъезжались.
2. **Проверь остальные тела ещё раз этим же критерием** — «откуда берётся организация». Круг 3 заявил,
   что проверил все четыре, и одно всё же осталось. Не повторяй ту же проверку тем же способом.
3. Права не откатывать: узкая роль `app_integrator_tenant_service` должна проходить все три двери.
4. Права — только через `declaration.ts` + генерация (`--all`, `--all --port-context-only`,
   `--all --check` побайтово). В миграции нет `GRANT`/`REVOKE`/`CREATE POLICY`.

## Доказательство

- Тест: под контекстом клиники A вызов с аргументом клиники B получает `42501` и НЕ возвращает строку.
  Инъекция — вернуть выборку по аргументу, тест обязан покраснеть.
- Прежние зелёные круга 3 остаются зелёными.
- `typecheck`, `lint`, `definer-tenant-predicate.test.mjs` — зелёные.

## Границы

`--execute`, TEST, PROD, push — запрещены. Галочки плана не ставить.
Отчёт: раздел «Круг 4» в `docs/_TODO/runs/integrator-cleanup/D17_INTEGRATOR_LOST_EXECUTE_2026-08-23.md`.
