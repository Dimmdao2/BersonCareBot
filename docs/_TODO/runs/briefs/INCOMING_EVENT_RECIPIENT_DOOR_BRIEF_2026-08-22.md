# Бриф: опознание получателя во входящем событии молча отказывает под двумя принципалами из трёх

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D17**.
  Твой DONE = закрытые пункты ЭТОГО брифа. Пункты плана закрывает ведущий.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`. Особенно §1 (миграция не несёт
  `GRANT`/`REVOKE`), §5, §6, §10a, §24.

**Источник оракула:** `docs/_TODO/runs/integrator-cleanup/D17_RELATION_READERS_2026-08-22.md`,
раздел «ВОПРОС ВЕДУЩЕМУ (один)» — «`handleIncomingEvent` читает получателя под тремя разными принципалами
(§1.1 п.2), а работает это только под организационным — и до этой ветки, и после.»

## Решение ведущего (принято 22.08, исполнять)

Воркер предыдущей ветки нашёл это и правильно не стал чинить сам: изменение поведения, в плане владельца
его нет. **Ведущий решил делать:** это не новый объём, а починка живого дефекта — человек, пишущий в бота
по маршруту, идущему под «неправильным» принципалом, сегодня НЕ опознаётся, и отказ МОЛЧАЛИВЫЙ.
Приём для починки уже приземлён сегодня и доказан: гейт корня ветвится ПО ДВЕРИ, каждая ветка называет
ровно одну роль (образец — `app.record_reminder_occurrence_finalized_projection`).

## Задание

1. **Сначала замер, потом код.** Назови `path:line` всех трёх мест в `handleIncomingEvent`, под каким
   принципалом каждое исполняется, что происходит при отказе (проглатывается ли ошибка и где),
   и **что именно НЕ получает живой человек** из-за неопознания — это обязательный пункт отчёта, а не
   украшение. Если окажется, что два из трёх маршрутов мертвы — удаляй, а не переводи, доказав
   недостижимость перечислением попыток поиска.
2. **Починка — вторая дверь класса `integrator` у существующего корня** (роль та же,
   `app_integrator_request`), гейт ветвится по двери. **Второго корня не заводить, роль не расширять,
   форму «одна дверь принимает две роли» не вводить** — каждая ветка называет ровно одну роль.
3. **Права — только `declaration.ts` + генератор**, оба `--check` побайтно; в миграции ни `GRANT`,
   ни `REVOKE` (§1). Разбор прав — в шапке миграции.
4. **Стена арендатора обязана остаться:** корень `SECURITY DEFINER` обходит RLS, поэтому предикат
   организации в теле проверь отдельно и докажи инъекцией, что без него чужая строка достаётся.
   Это тот же класс, на котором предыдущая ветка чуть не расширила доступ вместо сужения.
5. **Живое доказательство, `--execute` на DEV ЗАПРЕЩЁН** (DEV ведёт главное дерево): `--preflight` с
   откатом, живой прогон в откаченной транзакции — входящее событие опознаёт получателя под ВСЕМИ
   маршрутами, которые остались живыми; под чужой дверью отказ; чужая организация не достаётся.
   Инъекции неисправности на каждый вывод.
6. `pnpm test:db-privileges`, затронутые сюиты интегратора, typecheck, lint.

## Границы
Деплой, запись на TEST, `push`, full CI — НЕ твои. Прод не трогать.
Членство в `app_tenant_service` не снимать. `app_operational_delivery_worker` не трогать.
Отчёт: `docs/_TODO/runs/integrator-cleanup/INCOMING_EVENT_RECIPIENT_DOOR_2026-08-22.md`.
