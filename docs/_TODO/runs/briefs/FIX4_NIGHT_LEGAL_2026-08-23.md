# Фикс legal, круг 5: unit-набор красный на чистом дереве

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2d —
«Реквизиты юрлица подставляются, тексты — нет.»

**Клон:** `/home/dev/dev-projects/bcb-wt-night-legal-20260823`, ветка `wt/night-legal-20260823`.

## Блокер круга 4 — простой и стыдный

Аудит Opus (коммит `a591c21de`, отчёт
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT4_NIGHT_LEGAL_2026-08-23.md`) подтвердил: **блокер
круга 3 закрыт по-настоящему** — путь владельца пройден руками, `tsc --noEmit` чистый, все четыре комбинации
Host × страница показывают готовый документ без баннера, инъекция, бывшая входом в блокер, теперь краснеет.

Но появился новый и элементарный: **unit-набор красный прямо сейчас, на чистом дереве, без единой инъекции.**

```
npx vitest run --project=unit  →  Tests  1 failed | 755 passed (756)
```

Фикс круга 4 удалил поле `status` из источника и не тронул `apps/webapp/src/shared/lib/pwa/staffPwaManifest.unit.test.ts:26`,
где `toEqual` это поле утверждает.

**Почему три «зелёные» проверки фиксера это пропустили** — аудитор проверил и воспроизвёл все три: route-тест
не включает этот файл, ESLint его не исполняет, а лишний ключ в `toEqual` для TypeScript легален. Цепочка до
CI при этом целая: `ci:steps` → `test:webapp` → `vitest --run` → проект `unit`. То есть полный CI упал бы.

## Что сделать

1. Привести `staffPwaManifest.unit.test.ts` в соответствие с источником, из которого убрано `status`.
   **Не возвращай `status` обратно** — производная готовность и есть решение круга 4.
2. **Прогнать весь проект `unit`, а не точечные файлы.** `npx vitest run --project=unit` обязан быть зелёным
   целиком. Точечная проверка — ровно то, что скрыло дефект.
3. Убедиться, что решение круга 4 не задето: заполненный источник — без баннера, пустой — с баннером и
   списком, частичное заполнение — баннер и только недостающие поля.

## Что подтверждено кругом 4 и НЕ переоткрывается

Путь владельца работает, `TS2367` исчез вместе с полем `status`, придуманных реквизитов нет (три независимых
среза), тексты документов не изменены, три инъекции прямо в новую производную готовность краснеют все три.
Частичное заполнение показывает и вписанное, и недостающее поимённо — аудитор счёл это разумным.

Две инъекции остаются непойманными (`NODE_ENV`, `display:none`) — аудитор назвал это измеренным пределом
проверки по markup-строке, а не обходом в коде. **В скоуп не бери,** это неблокирующее.

## Границы

- Реквизиты не придумывать, тексты документов не переписывать.
- Не трогать `src/proxy.ts`, `src/config/surfaceRoutes.ts`, доставку писем, каталог `admin/`.
- Ничего не приземлять в `feat`. Пушить `git push dimmdao wt/night-legal-20260823`, в `main` никогда.
- Полный прогон только через `/home/dev/brain/host-orch/run-tests.sh`. Порт 5200 не занимать.
- Коммитить только свои файлы поимённо. PROD и TEST не трогать.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.

## Приёмка

- [ ] `F4-1` `npx vitest run --project=unit` зелёный целиком — показать итоговую строку.
- [ ] `F4-2` `status` не возвращён; производная готовность работает во всех трёх состояниях.
- [ ] `F4-3` typecheck и scoped ESLint зелёные; ветка запушена.
