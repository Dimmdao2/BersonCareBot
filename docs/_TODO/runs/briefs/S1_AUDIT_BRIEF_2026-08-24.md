# Audit brief — находка `S-1` (`TPB-15`), круг 1 (независимый, адверсарный)

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `S1-1` перепись мест «имя отправителя» ПОЛНАЯ — девятого места нет | разовое действие | СВОЙ исчерпывающий обход (`rg` по `BersonCare` в непроверочном коде обоих приложений и в `deploy/postgres/*.sql`), не сверка с таблицей автора |
| `S1-2` персонал видит Therapysto: регистрация специалиста и приём приглашения | поведение | слепой kill-set ДО чтения авторских тестов |
| `S1-3` пациент общего входа видит Therapygo | поведение | свои значения |
| `S1-4` пациент клиники с купленным брендом видит имя клиники | поведение | своя организация, свои значения |
| `S1-5` дефолта-подстановки у получателя не осталось: отсутствие параметра — ошибка, а не «BersonCare» | поведение | инъекция возврата дефолта обязана покраснеть |
| `S1-6` технические идентификаторы и история НЕ переименованы | разовое действие | взгляд на diff; `TPB-15` это прямо запрещает |
| `S1-7` SMS-имени не заведено (§1.2e — SMS выключен) | разовое действие | взгляд |
| `S1-8` вход и письма не сломаны ни у кого | поведение | регрессия на своих значениях |

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `TPB-15`,
дословно: «Нужен параметр от вызывающего, а не дефолт у получателя, то есть механизм этапа `C`.»
Имена по месту вызова закрыты владельцем в §1.2f: «Два: Therapysto персоналу, Therapygo пациентам».

Под аудитом: `c8e72a1f7` на `wt/therapysto-s1-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-s1-20260824`.
Автор: Codex `gpt-5.6-sol`/high, 17.6 минут. Он утверждает, что definer-функция
`app.email_auth_start_challenge` правки НЕ потребовала — она уже требовала профиль и передавала его в очередь.
**Проверь это отдельно и своими руками:** если это так, то четыре круга аудита и сам план описывали причину
неверно, и это надо сказать прямо.

## Границы

Прод не трогать, `--execute` на DEV не запускать, реальным адресатам ничего не слать, в `feat` не вливать.
Не трогать `app.read_integrator_clinic_delivery_credential`, миграции `20260823T030000…` / `20260823T043206…`,
`app.enqueue_outbound_message` и гейт готовности каналов (`C5`) — там соседняя ветка.
Finding вне owner scope — вопрос владельцу, не работа. Уровень `app`, два приложения. Full CI не гонять.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_S1_2026-08-24.md`: первой строкой вердикт,
строка на каждый пункт, счёт инъекций посажено/убито/не поймано.
