# Бриф: независимый аудит `B3`, круг 3 — однохостовость по умолчанию

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B3` —
«Реализовать один `RequestSurfaceResolver` и подключить к существующему request choke point.»

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b3-20260823`, ветка `wt/night-b3-20260823`.
**Кандидат:** `517c53aab` (плюс предыдущие `1e8a86b4b`, `6aeab03c4` того же слайса).
**Автор:** Codex `gpt-5.6-terra`/high; ты сильнее — аудитор не слабее автора.

## Вход

- круг 1 (`FAIL`): `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B3_2026-08-23.md`;
- круг 2 (`FAIL`, блокер `R-1`): `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/REAUDIT_NIGHT_B3_2026-08-23.md`;
- отчёт кандидата: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/CORRECTION_NIGHT_B3_ROUND3_2026-08-23.md`.

**Прогон автора оборван портом (`blocked_system`) сразу после записи отчёта.** Коммит сделал ведущий.
Значит: полного CI не было вообще, и последняя фраза отчёта («полный CI приведён после этого отчёта») —
незакрытое обещание, а не результат. Отчёт автора — заявка, не доказательство; проверяй своими руками.

## Что проверить

1. **Блокер круга 2 закрыт по-настоящему.** Собери production и прогони на конфигурации, которая реально
   существует на DEV и TEST: **`PATIENT_APP_ORIGIN` в окружении отсутствует**. Шесть путей круга 1
   (`/app/patient/login`, `/app/patient/cabinet`, `/book`, `/book/embed.js`, `/join/start`, `/clinic-a`)
   плюс `/manifest.webmanifest`, `/maintenance.html`, `/api/health`. Пустой гейт-`404` отличай от app-`404`
   по размеру тела, как это делали круги 1 и 2. Значения бери СВОИ, не переписывай таблицу автора.
2. **Обратная сторона не сломана.** При двух различимых Host гейт обязан работать: пациентское дерево
   отбивается на staff-хосте, staff-дерево на пациентском.
3. **Явно заданный `PATIENT_APP_ORIGIN` по-прежнему побеждает** — это конфигурация TEST и PROD после
   появления домена. Проверь живьём, а не по коду.
4. **`FB-2`: тест спрашивает окружение, а не подменяет поверхность присваиванием.** Верни дефолт
   `https://therapygo.ru` — тест обязан покраснеть. Посади ещё минимум две СВОИ инъекции этого шва.
5. **`FB-3`:** `/manifest.webmanifest` на `platform_admin` — определённый ответ, не исключение. Проверь,
   что `500` не вернётся другим путём (например при незаданном `PATIENT_APP_ORIGIN` и админском Host).
6. **Полный CI по ветке — обязателен**, только через общий замок хоста:
   `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`. Приведи `rc`, длительность и `runs/ci-last.json`;
   сверь `HEAD` до и после. Из worktree он теперь работает (`b81a82614`). Красный CI — блокер.
   Известная нестабильность: `PhoneMessengerAuthFlow.ui.test.tsx` — если краснеет он один, скажи об этом
   отдельно и перепроверь файл, не выдавая нестабильность за регрессию слайса.
7. **Границы `TPB-16`:** второго резолвера, второго геттера поверхности и второго места, где решается
   пациентский origin, появиться не должно.

## Чем это НЕ является

Аудит — гейт против плана владельца, а не источник работы. `R-2`, `R-4` и `N-2` кругов 1–2 закрыты
решениями владельца или вынесены ему вопросом: в скоуп не заводить. Находка без пункта в плане — вопрос
владельцу, а не задача. Скоуп сам не расширяй.

## Канон

- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел.
- Поиск по коду — `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, не слепой grep.
- PROD и TEST не трогать, порт `5200` не занимать, свои dev-серверы гасить за собой.

## Отчёт

Вердикт `PASS, FOR LAND` или `FAIL, NOT FOR LAND` первой строкой. Числа: блокирующих, неблокирующих,
инъекций посажено / убито / не поймано, непойманного (дефектов мимо проверок автора). Таблицы живых
замеров. Отдельная секция «НЕ СДЕЛАНО». Отчёт положи в
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT3_NIGHT_B3_2026-08-23.md` и закоммить.
