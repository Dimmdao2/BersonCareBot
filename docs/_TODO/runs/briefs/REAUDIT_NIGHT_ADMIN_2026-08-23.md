# Повторный аудит слайса «админская поверхность», круг 2

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-16` — «Реализация расширяет перечисленные choke points и не создаёт параллельных getters/resolvers/stores.
Доказательство: dependency/architecture audit по diff.»

**Что проверяешь:** коммит `95224044f` на ветке `wt/night-admin-20260823` — фикс по двум блокирующим находкам
круга 1 (аудит `dd4bc4bab`, вердикт `FAIL`, отчёт `AUDIT_NIGHT_ADMIN_2026-08-23.md`).
**Клон:** `/home/dev/dev-projects/bcb-wt-reaudit-admin-20260823`.

## Что заявил фиксер

1. Тест переписан на проверку фактического `metadata` export. Инъекция `staffPwaLayoutMetadata` дала `exit 1`
   с `/manifest-staff.webmanifest` и непустым `appleWebApp`; после отката — `1 passed`.
2. Каталог `admin/` удалён — `10` файлов. Сняты `build:admin`, записи `admin/dist`/`admin/node_modules` из
   `.gitignore` и ESLint, legacy-route из обоих runbook.
3. Webapp build под host-lock: `409/409`, `BUILD_ID=1GslmaJtc2-dBBrB8myLh`, standalone `server.js` найден.

## Что проверить

1. **Тест больше не бумажный — проверь СВОЕЙ инъекцией**, не повтором авторской. Круг 1 поймал ровно этот
   класс: убито `0`, не поймано `1`. Придумай другой способ вернуть PWA админке и убедись, что тест краснеет.
2. **Ничего живого не отвалилось после удаления каталога.** Перечисли своим поиском, не осталось ли ссылок на
   `admin/` в конфигах, CI, скриптах, доках; и не сломался ли `pnpm install`/workspace.
3. **Сборка** — собери сам под общим замком и покажи свой `BUILD_ID`, не доверяй чужому.
4. **Операторский остаток**: фиксер пишет, что на `151.241.228.122` остаётся stale
   `/etc/nginx/sites-available/tgcarebot.conf` с `/admin/ → :8080`, enabled-ссылки и слушателя нет, а PROD
   `135.106.162.170` не проверялся. **Живой nginx не трогай.** Проверь только утверждение про отсутствие
   enabled-ссылки и слушателя, и скажи, согласен ли с формулировкой остатка для оператора.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/REAUDIT_NIGHT_ADMIN_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
