# Бриф независимого аудита `F1`: способы входа как свойство поверхности

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `F1` —
«Расширить резолвер поверхности типизированной auth policy».
Прочитай §1.6 плана («Политика входа по поверхностям») целиком до первой проверки.

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-f1-20260823`, ветка `wt/night-f1-20260823`.
**Что проверяешь:** `ae8cd73f8` («feat(auth): add surface auth policy matrix»), автор — Codex `gpt-5.6-sol`/high.

## Тест или взгляд (§24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Поведение входа не изменилось ни на одной поверхности | **тест** | это главное требование пункта |
| Включение способа настройкой не требует правки кода | **тест** | заявленное свойство типа |
| Источник решения один | **взгляд** | разовая перепись мест |

## Главное требование — ничей вход не изменился

`F1` — несущая конструкция, а НЕ переключение поведения. `F2`–`F5` (выключить OAuth у персонала, второй фактор
доктора, свести пациентский вход) — отдельные пункты, их в этой правке быть не должно.

Автор заявил, что значения по умолчанию совпадают с прежними на всех трёх поверхностях. **Проверь это не по
его таблице, а по продукту**: возьми поведение до `ae8cd73f8` и после, перечисли способы входа на `staff`,
`platform_admin` и patient списком в обе стороны. Любое расхождение — блокер, даже «улучшение».

Дальше:

- **включение способа настройкой действительно работает** без правки типа и кода — покажи прогоном;
- **источник один** (`TPB-16`): после правки принадлежность механики поверхности решается в одном месте.
  Автор назвал пять прежних мест (`passwordEligibility.ts`, `authChannelPolicy.ts`, роут TOTP-фактора,
  `publicAuthSnapshot.ts`, `AuthFlowV2.tsx`) и утверждает, что они остались проверками общей готовности, а не
  вторым surface-решением. **Проверь это утверждение поимённо** — именно здесь появляется второй источник правды;
- **passkey не включён по умолчанию нигде** и при этом включаем настройкой — решение владельца;
- инъекции: посажено / убито / не поймано, числами.

## Границы

- **аудит — гейт против плана владельца, не источник работы.** Находка без пункта в плане — вопрос владельцу;
- `F2`–`F5` не проверяем и не требуем: их ещё не делали.

Вердикт первой строкой: `PASS, FOR LAND` или `FAIL, NOT FOR LAND`. Отчёт —
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_F1_2026-08-23.md`.
