# Бриф Ш9: субъектные колонки уезжают с акторского корня

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D15b/7a**.
  Твой DONE = закрытые пункты ЭТОГО брифа.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`; §1 (миграции НИКОГДА не выдают гранты и не
  создают политик), §5 (один проход), §6 (роли и RLS), §10a, §24.

**Источник оракула:** `docs/_TODO/runs/integrator-cleanup/D15B7A_ACTOR_SUBJECT_SPLIT_SCHEME_2026-08-22.md` — «`height_cm`, `weight_kg`, `gender`, `birth_date` из `platform_users` — в клиническую таблицу; врачебные»

## Зачем
`platform_users` — акторский корень (учётная запись человека). Рост, вес, пол и дата рождения — это данные
о ПАЦИЕНТЕ, а не об учётке; пока они лежат в корне, разделение актора и субъекта проведено в ссылках, но не
в данных. Заодно исчезает живое расхождение: **пишем в `platform_users.birth_date`, читаем
`user_identity.birth_date`.**

## Задание

1. **Сначала замер, потом переезд.** Сколько строк на DEV несут непустое значение в каждой из четырёх
   колонок; кто их пишет и кто читает (перепись по коду и по телам функций). **TEST не трогай** — его
   замеряет ведущий сам.
2. **Перенести четыре колонки в клиническую таблицу** отдельной миграцией с отдельным разбором прав.
   Куда именно — реши по существующей схеме и НАЗОВИ причину выбора; новой таблицы не заводить, если
   подходящая есть (§5).
3. **Перевести врачебных писателей** `apps/webapp/src/infra/db/repos/pgDoctorClients.ts` (около строк
   1416-1436 и 1481-1503 — проверь номера сам, они могли сдвинуться) на новое место и **устранить
   расхождение чтения и записи**: читатель и писатель обязаны называть одну колонку.
4. **Права разбираются отдельно и явно:** новая колонка в клинической таблице обязана попасть под ту же
   стену арендатора и те же политики, что соседние клинические поля. Миграция прав не выдаёт (§1) —
   права идут декларацией и reconcile. Покажи диффом артефактов, что новых табличных грантов не
   появилось сверх необходимого, и назови каждый появившийся.
5. **Доказательство поведением на живой DEV, в откаченных транзакциях:** врач своей клиники видит и правит
   поле у своего пациента; врач ЧУЖОЙ клиники не достаёт ни строки; пациент видит своё.
   **Инъекции обязательны:** снять предикат стены — чужая клиника достаётся; вернуть — не достаётся.
6. **Данные не теряются:** перенос обязан переносить значения, а не только колонки. Докажи сверкой числа
   непустых значений до и после на DEV.
7. `--preflight` EXIT=0; `pnpm test:db-privileges`, затронутые тесты, typecheck, оба `--check` побайтно.
   **`--execute` на DEV ЗАПРЕЩЁН** — его гоняет ведущий.

## Границы
Деплой, TEST-запись, `push`, full CI — НЕ твои. Прод не трогать. Ш7 не трогать.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D15B7A_STEP9_2026-08-22.md`.
