# D15b/7a — разделение actor/subject внутри одного Postgres: исследование и схема (БЕЗ реализации)

**Роль:** researcher (взгляд + проектирование, без правки продукта). **Канон:** `AGENTS.md` — сначала карта
заголовков (`grep -n "^## \|^### " AGENTS.md`), затем §5 (Clean Architecture), §6 (PostgreSQL/роли),
§1 (миграции, «⛔ Миграция не выдаёт и не отзывает права», «Перед приземлением миграции — разбор её прав»),
§12 (стандарт плана), §24.2/§24.6. Поиск — `node /home/dev/brain/tools/code-search.mjs "<q>" --repo bcb`.

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт D15b — дословно:
«D15b/7a (разделение actor/subject внутри одного Postgres) идёт после TEST-закрытия D15b/6; только
физическое разнесение баз (вариант I) остаётся отложенным.» Там же дословно про ожидаемый результат:
«единый порт аутентификации, вход в ОДИН аккаунт по любому его подтверждённому контакту, и всё работает».

Обоснование и замеры, которые надо прочитать до выводов:
`docs/_TODO/runs/integrator-cleanup/IDENTITY_DB_SPLIT_RESEARCH_2026-08-03.md` §6 и
`docs/_TODO/runs/integrator-cleanup/IDENTITY_AND_MERGE_SCHEME.md` §2b.

## Задача

Живая проверка D15b/6 на TEST ещё не сделана и по решению владельца 22.08 **не блокирует эту работу** —
она блокирует только закрытие этапа в документах. Поэтому здесь делается проектная часть целиком.

Дай **один документ** в `docs/_TODO/runs/integrator-cleanup/` со схемой разделения actor/subject внутри
одного кластера Postgres. Обязательные разделы:

1. **Что сегодня.** Перепись против реальности, не против старых документов: какие таблицы и колонки несут
   «кто действует» (учётка, контакты, подтверждения, сессии, привязки каналов), какие — «о ком данные»
   (медицинские/продуктовые сущности), и где эти две роли сегодня смешаны в одной таблице. Каждое утверждение —
   `path:line` или интроспекция каталога с командой и её выводом.
2. **Целевая схема.** Как проходит граница, какие объекты переезжают, какие остаются, что становится
   именованным корнем. Соблюдай принцип владельца — один общий проход, а не второй параллельный путь.
3. **Права.** По §1 «Перед приземлением миграции — разбор её прав»: под какой ролью исполняется каждое новое
   тело и какие права ему нужны, чтобы оно **исполнилось**. Отдельно `SELECT … FOR UPDATE`/`FOR SHARE` —
   поколоночного `SELECT` там не хватает. Права только через декларацию + генератор.
4. **Порядок шагов и обратимость.** Разбей на шаги, каждый из которых приземляется и проверяется отдельно;
   по каждому — как откатить. Ни один шаг не должен требовать одномоментного переключения всего.
5. **Что этот объём НЕ включает.** Физическое разнесение баз (вариант I) остаётся отложенным — назови явно.
6. **Открытые вопросы владельцу** — отдельным коротким списком, каждый с рекомендацией и безопасным
   умолчанием, чтобы он закрыл их за один присест.

## Границы

- ⛔ Продуктовый код, миграции, права, схема БД, тесты — в этом ходе НЕ трогать. Результат — один документ.
- ⛔ Любая запись в базу, `--execute`/`--apply`, деплой, TEST/PROD, push, full CI — запрещены.
- ⛔ Фикстуры, disposable-базы, новые тестовые базы, любая новая машинерия — запрещены прямым словом владельца.
- Галочки в WORK_ORDER не ставь.
- Не выдумывай требований, которых нет в оракуле. Всё, что кажется нужным сверх него, — в раздел 6 вопросом.
