# Повторный аудит: снятие legacy-sweep, круг 2

Ветка `wt/d30-sweep-removal-20260823`, коммит `fc86fd727`, клон
`/home/dev/dev-projects/bcb-wt-d30-sweep-removal-20260823`. Отчёт исполнителя —
`docs/_TODO/runs/integrator-cleanup/D30_SWEEP_REMOVAL_FIX2_2026-08-23.md`. Твой же прошлый вердикт —
`D30_SWEEP_REMOVAL_AUDIT_2026-08-23.md` (`FAIL`, три блокера). Канон — `AGENTS.md`.
Authority — `WORK_ORDER.md`, D30.

## Тест или взгляд

| Пункт | Что проверяем | Тест или взгляд | Почему |
|---|---|---|---|
| 1 | сторож действительно краснеет при снятии постановки в очередь на каждом из 4 путей | **тест** | это ровно то свойство, отсутствие которого было блокером |
| 2 | `enqueueDueReminders`/`listDueReminders` удалены везде, мёртвого не осталось | **взгляд** | перепись по дереву |
| 3 | `codePaths` в декларации указывают на существующие файлы | **взгляд** | сверка путей |
| 4 | ничего лишнего не удалено | **взгляд** | разбор diff |

## Что доказать

1. **Сторож не бумажный.** Сам сними постановку в очередь на каждом из четырёх путей по очереди и
   покажи, какой тест краснеет. Твоя прошлая инъекция «снять все четыре» оставляла полный набор
   зелёным — повтори её: теперь она обязана покраснеть.
2. **Блокер 2 закрыт по существу:** обеих функций нет ни в порту, ни в сервисе, ни в обеих
   реализациях, ни в тестах; кросс-арендного скана без организации не осталось.
3. **Блокер 3 закрыт:** каждый `codePath` в затронутых записях `relation-access.ts` указывает на
   существующий файл. Проверь сам, а не по отчёту.
4. **Долг, который автор назвал внешним** (strict typecheck каталога прав падает на двух литералах в
   неизменённом `declaration.ts:3981,7147`): подтверди или опровергни, что он действительно
   существовал ДО этой ветки. Если он появился в ней — это блокер.

## Границы

Продуктовый фикс не делаешь. `--execute`, PROD, push — запрещены. Временные поломки откатывай.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D30_SWEEP_REMOVAL_REAUDIT_2026-08-23.md`.
Вердикт одной строкой: `PASS, FOR LAND` или `FAIL, NOT FOR LAND` + блокеры.
