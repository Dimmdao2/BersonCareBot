# Аудит слайса «юридические документы», круг 5 — закрывающий

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2d —
«Реквизиты юрлица подставляются, тексты — нет.»

**Что проверяешь:** коммит `b9366efa1` на ветке `wt/night-legal-20260823` — фикс по единственному блокеру
твоего круга 4 (красный unit-набор на чистом дереве).
**Клон:** `/home/dev/dev-projects/bcb-wt-audit5-legal-20260823`.

## Что заявил фиксер

Устаревшее ожидание `status` удалено из `staffPwaManifest.unit.test.ts`, само поле не возвращено.
`npx vitest run --project=unit` через host runner: `756 passed (756)`. Legal route-проверка `6 passed` —
пустой, заполненный и частичный источник на обеих страницах. typecheck и scoped ESLint зелёные.

## Что проверить

Круг маленький, но это ПЯТЫЙ заход по слайсу — смотри не только на заявленное.

1. **Весь проект `unit` зелёный** — прогони сам, не по слову автора.
2. **Полный CI по ветке** через общий замок хоста: `ci:steps`. Круг 4 показал, что точечные проверки скрывают
   поломку — закрой этот вопрос целиком, чтобы слайс можно было сводить.
3. **Решение круга 4 не задето:** производная готовность работает во всех трёх состояниях — пусто, заполнено,
   заполнено частично. Проверь живьём, а не тестом.
4. **`status` не вернулся** ни в источник, ни в тесты.
5. Придуманных реквизитов нет; тексты документов не изменены по всей истории слайса.

**Если блокеров нет — так и скажи вердиктом `PASS, FOR LAND`:** слайс ждёт сведения, и держать его дальше
без причины дороже, чем свести.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT5_NIGHT_LEGAL_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
