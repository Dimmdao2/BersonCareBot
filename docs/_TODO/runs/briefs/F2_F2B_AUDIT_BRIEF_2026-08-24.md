# Audit brief — `F2` + `F2b`, круг 1 (независимый, адверсарный)

Repo canon: `AGENTS.md` — read §10a, §10b, §24.4, §24.5 in full before classifying anything.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункты `F2` и `F2b`,
дословно: «Выключить OAuth-вход на staff/admin surface значением в матрице механик.» и «Passkey НЕ удалять.
Подключить его к матрице механик как переключаемую опцию, по умолчанию».

Under audit: commit `6dc5b260f` on branch `wt/therapysto-f2-20260824`, clone
`/home/dev/dev-projects/bcb-wt-therapysto-f2-20260824`. Author: Codex `gpt-5.6-terra`/high. His claim: OAuth
start/callback and every passkey entrance route now pass through the resolver-backed `authDeliveryGate`;
passkey default OFF for staff; 20 tests green; three fault injections went red.

## Классификация «тест или взгляд» (§24.4) — обязательна, по пунктам

| Пункт | Природа | Способ доказательства |
| --- | --- | --- |
| `F2` — отказ OAuth на staff/admin происходит на уровне РЕЗОЛВЕРА, а не скрытием в UI | повторяемое поведение | поведенческий тест по слепому kill-set (§24.5), составленному ДО чтения авторских тестов |
| `F2` — включение настройкой возвращает путь в строй БЕЗ правки кода | повторяемое поведение | тест: перевести значение матрицы и получить рабочий вход |
| `F2` — ничего не удалено: UI, start/callback, provider config остались в коде | разовое действие | взгляд на diff и итоговое состояние (`rg`), тест на отсутствие строк НЕ писать |
| `F2b` — passkey стал механикой матрицы, дефолт выключен у докторов | повторяемое поведение | тест на дефолт и на включение настройкой |
| `F2b` — код и маршруты passkey сохранены, PIN не вернулся | разовое действие | взгляд на diff и `rg` |
| Регрессия — вход НИ У КОГО больше не изменился (пациент, staff, platform_admin) | повторяемое поведение | собственные значения, не авторские фикстуры |

Сначала составь kill-set по оракулу, потом читай реализацию, потом ломай. Свои проверки пиши на СВОИХ значениях;
авторские тесты не переиспользуй как доказательство. Продуктовый код в конце откати — `git diff` должен быть пуст,
кроме твоего отчёта и, если писал, своих тестовых файлов.

## Границы (решения владельца, не инженерные развилки)

- Не трогай `app.read_integrator_clinic_delivery_credential` и обе миграции `20260823T030000…` / `20260823T043206…`;
  обе роли в одном списке не разрешать.
- В `feat/doctor-ui-rebuild` не вливать.
- Finding вне owner scope — это regression/repo-rule, owner question или recommendation, но НЕ новая работа.

## Уровень и объём

Уровень `app`: затронут только `apps/webapp`. Full CI не гонять — признаков repo-уровня нет
(нет корневых конфигов, lockfile, контрактов между приложениями).

## Дедлайн

Уложись в 25 минут: агенты на боксе обрываются примерно на 30-й. Отчёт и тесты закоммить в клоне ДО этого.
`git add -A` запрещён — только свои файлы поимённо.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_F2_F2B_2026-08-24.md`, первой строкой вердикт
`PASS, FOR LAND` или `FAIL, NOT FOR LAND`, затем строка на каждый пункт таблицы: `ID → PASS|FAIL|BLOCKED → evidence`,
и счёт инъекций: посажено / убито / не поймано.
