# Бриф: `TPB-09` — имя и origin пациентского приложения из деплой-конфига, настройки клиники в БД

## Классификация «тест или взгляд» (§24.4)

Первым шагом раздели требование на две половины и запиши решение в отчёт: «имя/origin меняются деплой-конфигом
без миграции данных» — это ПОВЕДЕНИЕ (нужен config-тест, который краснеет, если значение перестанет читаться
из окружения); «домен и интеграции клиники остаются org-scoped настройками БД» — тоже поведение (тест
владения настройкой), а не взгляд на файлы.

## Источник оракула

Файл: `/home/dev/dev-projects/bcb-wt-therapysto-night-20260823/docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`

Дословно, `TPB-09`: «Standard patient name/origin меняются deploy config без data migration; clinic domain/integrations»

Канон репозитория: `AGENTS.md` — перед действием `grep -n "^## \|^### " AGENTS.md` и читать свой раздел.

## Что доказать

Ветка `wt/therapysto-night-20260823`, клон `/home/dev/dev-projects/bcb-wt-therapysto-night-20260823`.

1. **Имя пациентского приложения меняется одним значением окружения.** `PATIENT_APP_NAME` (дефолт —
   `PATIENT_DEFAULT_SURFACE_NAME`) обязан доходить до КАЖДОГО места, где имя видит пациент. Обойди потребителей
   САМ (метаданные страниц, PWA-манифест, профиль отправителя писем, заголовок push, `.ics`), а не по списку
   из плана. Где имя берётся литералом мимо окружения — это FAIL с `файл:строка`.
2. **Origin меняется тем же способом**: `APP_BASE_URL` — единственный источник хоста, никакой второй константы
   и никакой записи в БД для этого не нужно.
3. **Без миграции данных**: смена имени/origin не требует ни одной строки в БД — покажи это обходом, а не
   утверждением (нет таблицы/настройки, которая дублирует имя платформенной пациентской поверхности).
4. **Домен и интеграции клиники — наоборот, org-scoped настройки БД**: они живут в настройках организации, их
   нельзя задать окружением, и одна организация не видит настройку другой.

Каждый пункт — тест, который КРАСНЕЕТ при откате: сажаешь инъекцию, показываешь красноту, откатываешь.
Недостающую проверку дописывай ОДИН раз, в существующий соседний файл, а не заводи новый набор.

## Границы

- НЕ трогать `app.read_integrator_clinic_delivery_credential` и миграции `20260823T030000…`, `20260823T043206…`;
  обе роли в одном списке не разрешать (решение владельца 24.08, §1.2k плана).
- Никаких `--execute`, деплоя, PROD, TEST, `telegram_mode`, push в `feat`, слияний в `feat`, full CI.
- Регистрация `therapygo.ru` не сделана — реальные домены не проверяются, только конфигурационный шов.
- Находка, которой нет строки в план-файле владельца, — ВОПРОС владельцу в «Findings», работой не становится (§24.6).
- Все инъекции откатить; `git status --porcelain` после прогона обязан быть пуст.

**Уложись в 25 минут.** Не успел — пиши, что успел и что осталось, в секции «НЕ СДЕЛАНО».
