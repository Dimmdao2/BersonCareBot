# Бриф: `B4` — продолжение оборванного прогона, доказательства

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B4` —
«Расширить существующий org branding service: optional patient app name и один accent token; anonymous
branded projection отдаёт только published/entitled safe fields. Public card/contacts читаются через
существующий clinic-public-card port.»

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b4-20260823`, ветка `wt/night-b4-20260823`.
**Состояние:** прогон предыдущего агента оборван портом (`blocked_system`) на 32-й минуте. Его правки
закоммичены ведущим как `9a07bed69` (17 файлов + миграция `20260823T064034`, +311/−66). **Отчёта нет.
Проверок не гонялось НИ ОДНОЙ: ни инъекций, ни scoped-набора, ни полного CI.**

## Твоя работа — доказать или опровергнуть уже написанное, а не переписывать его

Сначала прочитай `git show 9a07bed69` целиком и скажи в отчёте, что там сделано. Меняй только то, что
доказательство покажет сломанным. Расширять скоуп запрещено: конструктора тем, второго токена, UI —
не делать.

## Что доказать

1. **Главное — утечка неоплаченного и неопубликованного.** Анонимная брендированная проекция обязана
   отдавать ТОЛЬКО published/entitled safe fields. Инъекции, обе обязаны краснить:
   сними проверку entitlement; сними проверку published. Покажи числа красных до и после.
   Отдельно проверь, что неоплаченное/неопубликованное не «просвечивает» через `null` или пустое поле,
   по которому видно, что оно существует.
2. **Второго резолва бренда нет.** `ResolvedSurface` уже несёт `effectivePatientBrand`; докажи `git grep`,
   что второго места, где бренд достаётся, не появилось (`TPB-16`).
3. **Организация без брендинга работает как раньше:** дефолтное имя, дефолтный акцент, никакого пустого
   экрана. Проверь живьём, а не только юнитом.
4. **Ограничения миграции реальны:** акцент вне формата `^#[0-9a-f]{6}$` и пустое имя обязаны отбиваться
   базой. Проверь на живой DEV rollback-only, а не чтением файла миграции.
5. **`GRANT`/`REVOKE`/`CREATE POLICY` в миграции — `0`** (грепом). Владелец миграции объявлен.
6. **Полный CI по ветке** только через общий замок хоста:
   `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"` (напрямую `pnpm run ci` нельзя). Перед прогоном
   проверь, что `.next` клона не протух — на этом за ночь споткнулись три прогона.

## Границы

- PROD и TEST не трогать. Живая DEV — только rollback-only. Порт `5200` не занимать.
- `B5`, `B5a`, `B7`, `B8` не брать.
- Коммить только свои файлы поимённо. `push` и приземление в `feat` не делать.

## Канон

- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел до правок.
- Поиск по коду — `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, не слепой grep.

## Отчёт

Что было в салваже и что из этого подтвердилось; таблица «оплачено / не оплачено / не опубликовано → что
видит аноним»; числа инъекций (убито / не поймано); итог полного CI с кодом возврата и `runs/ci-last.json`;
отдельная секция «НЕ СДЕЛАНО».
