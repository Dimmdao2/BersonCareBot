# Бриф `F1`, круг 2: матрица обязана описывать сегодняшнее состояние, а не желаемое

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `F1` —
«Расширить резолвер поверхности типизированной auth policy».

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-f1-20260823`, ветка `wt/night-f1-20260823`.
**Вход:** независимый аудит Opus 5, `FAIL, NOT FOR LAND` —
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_F1_2026-08-23.md` (`592643b95`).
Прочитай отчёт целиком до первой правки.

## Блокер: значения матрицы расходятся с продом по трём механикам из шести

Аудит сверил не таблицу автора, а живые значения (`deploy/postgres/generated/prod-to-target/runtime-settings.sql`):

| механика | в проде | в матрице |
| --- | --- | --- |
| passkey | **включён** | выключен везде |
| oauth | **выключен** | включён на staff/admin/patient |
| phone_bot | **выключен** | включён везде |

Сегодня ничего не сломано: `authPolicy` в продуктовом коде никто не читает. Но в коде стоит утверждение
«здесь сегодняшнее поведение», и следующий исполнитель (`F4`) возьмёт эти значения за исходное состояние —
тогда у всех пропадёт работающий passkey, а у персонала **включится** OAuth, прямо против `OG-5`.

**Что сделать: привести значения матрицы к тому, что есть в проде на самом деле** — passkey включён,
OAuth выключен, телефон/бот выключены; остальные три совпадают и не трогаются. `F1` не меняет поведение —
значит и матрица обязана описывать состояние, а не намерение.

**Отдельно почини само утверждение:** в комментарии у матрицы должно быть написано, ОТКУДА взяты значения
(файл живых настроек и дата), чтобы следующий не сверял их на глаз.

## Границы

- `F2`–`F5` не брать: выключение OAuth у персонала, второй фактор доктора и сведение пациентского входа —
  отдельные пункты плана;
- подход не переделывать: аудит подтвердил, что конструкция верна, второго источника правды не появилось;
- **находка вне плана владельца — вопрос в отчёт, а не работа.**

## Доказательства

- построчная сверка «прод против матрицы» после правки: шесть механик, три поверхности, расхождений ноль;
- тест, который краснеет при расхождении матрицы с заявленным источником;
- scoped lint, typecheck, числа прогонов. **Полный CI не гоняй.**
