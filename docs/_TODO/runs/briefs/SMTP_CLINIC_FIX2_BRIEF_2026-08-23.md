# SMTP клиники, круг 2: дверь объявлена, но не попала в точный каталог способностей

Независимый аудит дал **FAIL**. Отчёт —
`docs/_TODO/runs/integrator-cleanup/CLINIC_SMTP_INTEGRATION_DISABLED_AUDIT_2026-08-23.md`, прочитай.

**Источник оракула:** `docs/_TODO/OWNER_WALKTHROUGHS/2026-08-23_TEST_FULL_WALK.md` — «Сохранить SMTP»

Канон — `AGENTS.md` (`grep -n "^## \|^### " AGENTS.md`), §1, §5, §10a.

## Блокер

**Логика правки верна** — аудитор проверил её независимо и претензий к ней не имеет. Блокер механический:
декларация объявляет способность `clinic_platform_integration_availability_read`, но из четырёх
генерируемых на базу артефактов обновлены только два. Точный каталог `port-context-capabilities.*.sql`
остался прежним, поэтому `app.require_accepted_context` откажет на КАЖДОМ вызове новой двери.

И это хуже сегодняшнего симптома: правка завязала на дверь весь блок «Каналы доставки клиники» — вместо
«форма есть, сохранение даёт 403» врач увидит красную плашку и ни одного канала вообще.

`--all --check`, которым проверялся круг 1, этого не ловит: он сверяет `privileges` и `allowlist`.
Каталог способностей пересобирает ОТДЕЛЬНАЯ команда.

## Что сделать

1. `node deploy/postgres/privileges/generate-cli.mjs --all`
2. `node deploy/postgres/privileges/generate-cli.mjs --all --port-context-only` ← **пропущенный шаг**
3. `node deploy/postgres/privileges/generate-cli.mjs --all --check` — побайтово
4. Убедись, что в коммит попали ВСЕ изменившиеся сгенерированные файлы, включая
   `port-context-capabilities.bcb_webapp_dev.sql` и `port-context-capabilities.bersoncarebot_test.sql`.
5. **Отдельно оцени связность.** Аудитор прав: завязывать весь блок каналов доставки на одну новую дверь
   — хрупко. Если её отказ обязан гасить весь блок, объясни почему; если нет — пусть падает только то,
   что от неё действительно зависит.

## Доказательство

- Живая проверка на именованной DEV: вызов новой двери под врачом ПРОХОДИТ (а не отказывает
  `accepted port context required`), сохранение SMTP клиники срабатывает.
- Инъекция: откати каталог способностей к прежнему состоянию — тест обязан покраснеть именно на этом,
  а не «упасть вообще».
- `typecheck`, `lint`, затронутые тесты — зелёные.

## Границы

`--execute`, TEST, PROD, push — запрещены.
Отчёт: раздел «Круг 2» в `docs/_TODO/runs/integrator-cleanup/CLINIC_SMTP_INTEGRATION_DISABLED_2026-08-23.md`.
