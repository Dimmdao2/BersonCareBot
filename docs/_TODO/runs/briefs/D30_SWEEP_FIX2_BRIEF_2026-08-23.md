# D30 Ш3, круг 2: доснять sweep и поставить сторожа вместо снятого тика

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «sweep/route/registry/host cron для закрытых code-side шагов тем же коммитом, что и пост-деплойные проверки»

Канон — `AGENTS.md` (`grep -n "^## \|^### " AGENTS.md`), §1, §5, §10a, §24.
Работаешь в клоне `/home/dev/dev-projects/bcb-wt-d30-sweep-removal-20260823` поверх `871bd8754`.

Круг 1 (`3f4378d62`) снял роут, реестр, csrf-allowlist и доки — это принято. Независимый аудит на
Opus (`docs/_TODO/runs/integrator-cleanup/D30_SWEEP_REMOVAL_AUDIT_2026-08-23.md`) дал
`FAIL, NOT FOR LAND` по трём блокерам. Закрываешь ровно их, ничего сверх.

## Блокер 1 — покрытие write-time пути мнимое

Аудитор снял ВСЕ четыре вызова корня очереди (`create`/`update`/`complete`/`delete`) — полный набор
вебаппа остался зелёным: 430 файлов, 1990 тестов. То есть страховка снята, а сторожа взамен нет:
завтра кто-нибудь уберёт постановку в очередь, и ни один тест не покраснеет, а напоминания просто
перестанут приходить живым людям.

Поставь сторожа: набор, который краснеет при снятии постановки в очередь на КАЖДОМ из четырёх путей.
Аудитор оценил объём примерно в 80 строк и оставил рецепт своего временного прогона в приложении А
своего отчёта — прочти его и сделай постоянным. Доказательство обязательно инъекцией: снимаешь вызов
на каждом из четырёх путей по очереди и показываешь, какой именно тест краснеет.

## Блокер 2 — снята дверь, а не sweep

`enqueueDueReminders` (`apps/webapp/src/infra/repos/pgSpecialistTasks.ts:271`) и `listDueReminders`
(`:236`) остались в порту, сервисе и in-memory реализации без единого нетестового вызывающего.
План Ш3 называл `enqueueDueReminders` в снимаемом составе поимённо. Отдельно: `listDueReminders`
фильтрует без организации — это кросс-арендный скан, описанный в
`docs/_TODO/runs/integrator-cleanup/DOCTOR_TASK_COMPLETE_ENQUEUE_2026-08-22.md` §В2.

Снять обе вместе со всем, что становится мёртвым: объявления в порту, реализации (pg и in-memory),
методы сервиса, их тесты. Перед удалением каждого — перепись вызывающих, а не предположение.

## Блокер 3 — декларация ссылается на удалённые файлы

`deploy/postgres/privileges/relation-access.ts:8210` указывает на `dispatchDueReminders.ts`, удалённый
тем же коммитом; рядом `:8209` `reconcileJobKeys.ts`, где упоминаний задач специалиста уже нет.
Гейт этого не ловит: `access-census.mjs:98` проверяет только непустоту `codePaths`. Почини записи
генерацией, а не рукой. **Отдельной строкой в отчёте назови**, стоит ли усилить сам гейт проверкой
существования пути — но САМ гейт в этом круге не переписывай, это решение ведущего.

## Границы

- Write-time producer и очередь не трогать — они целевая архитектура.
- Права: только через `declaration.ts` и генерацию (`--all`, `--all --port-context-only`,
  `--all --check` побайтово). В миграциях нет `GRANT`/`REVOKE`/`CREATE ROLE`/`CREATE POLICY`.
- `--execute`, TEST, PROD, push — запрещены. Галочки плана ставит ведущий.

Отчёт: `docs/_TODO/runs/integrator-cleanup/D30_SWEEP_REMOVAL_FIX2_2026-08-23.md`.
