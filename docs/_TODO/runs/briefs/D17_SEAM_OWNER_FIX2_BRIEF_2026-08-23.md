# Права владельцев швов, круг 2: узкое право вместо табличного

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «узкая роль интегратора не мешает доставке»

Канон — `AGENTS.md` (`grep -n "^## \|^### " AGENTS.md`), §1, §5, §10a.
Клон `/home/dev/dev-projects/bcb-wt-d17-seam-owner-20260823`, поверх `ac1a68fa4`.

Круг 1 нашёл настоящую дыру, но выдал слишком широкое право. Независимый аудит на Opus
(`docs/_TODO/runs/integrator-cleanup/D17_SEAM_OWNER_AUDIT_2026-08-23.md`) дал `FAIL, NOT FOR LAND`.
Закрываешь ровно его блокеры, ничего сверх.

## Блокеры, которые надо закрыть

1. **Грант на `user_channel_preferences` убрать полностью.** Аудитор доказал живьём: там уже был
   колоночный `SELECT` ровно на пять читаемых колонок, разрыва не было (122 строки читаются без
   табличного гранта). Правка круга 1 просто открыла всю таблицу — это регресс, а не починка.
2. **Грант на `user_contacts` сузить до двух колонок.** Настоящий разрыв — `confirmed_at` и
   `is_primary`. Аудитор прогнал три состояния rollback-only: до правки `42501`; только две колонки —
   `"ok": true`; табличный грант — то же самое. Цена табличного: 16 definer-корней владельца
   получают чтение `source_origin`, `id`, `created_at`, которые под колоночными правами им закрыты.
   Обоснование из отчёта круга 1 («корень выполняет `count(*)`, значит нужна вся таблица») неверно —
   под колоночными правами `count(*)` отработал и вернул 338.
3. **Инъекцию в тесте переписать.** `REVOKE SELECT ON TABLE` сносит и колоночные права (замерено:
   было пять колонок, стало `<none>`), поэтому `42501` после такого отзыва не доказывает, что нужен
   именно табличный грант. Отзывать надо ИМЕННО проверяемые колонки, и тест обязан краснеть на этом.
4. Прочитай отчёт аудитора целиком — если блокеров там больше трёх, закрой и их.

## Границы

- Права — только через `declaration.ts` и генерацию (`--all`, `--all --port-context-only`,
  `--all --check` побайтово). `GRANT`/`REVOKE`/`CREATE POLICY` в миграциях запрещены.
- Перепись владельцев швов из круга 1 (43 роли, 631 требование) переделывать не надо — аудитор
  проверил её отдельно; исправь только те её строки, которые становятся неверными после сужения прав.
- `--execute`, TEST, PROD, push — запрещены.
- **Уложись в 25 минут работы**: агенты на этом боксе гибнут около 30-й минуты. Не успеваешь —
  пиши отчёт и коммить готовое.

Отчёт: `docs/_TODO/runs/integrator-cleanup/D17_SEAM_OWNER_FIX2_2026-08-23.md`.
