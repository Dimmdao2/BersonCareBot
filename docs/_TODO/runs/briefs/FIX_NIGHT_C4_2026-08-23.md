# Фикс C4: пациент с паролем получает чужое имя, и слайс залез в SMS

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C4` —
«Расширить existing SMTP config только sender display data, добавить один org-scoped transactional template
setting и один mail-profile resolver/renderer.»

**Клон:** `/home/dev/dev-projects/bcb-wt-night-c4-20260823`, ветка `wt/night-c4-20260823`.

## Две блокирующие находки независимого аудита

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_C4_2026-08-23.md`, коммит `f23876e7b`
на `wt/audit-c4-20260823` (Codex `gpt-5.6-sol`/high). Убито `3`, непойманного `1`.

### `C1` Пациент получает имя персонала

**Пациент с парольным доступом, проходящий через `/email-password/forgot`, получает `Therapysto` вместо
`Therapygo`.** Это ровно тот дефект, ради которого делался весь слайс, только в другой ветке цепочки.
Аудитор написал воспроизводящий acceptance-тест: сейчас `1 failed / 16 passed`. Задача — `17 passed`,
**не ослабляя тест**.

Причина, судя по всему, в том, что профиль выбирается по точке входа, а не по тому, кто на самом деле
получатель. Разберись и почини так, чтобы правило было ОДНО и покрывало все ветки, а не добавляй ещё одну
развилку под этот случай.

### `C2` Слайс залез в SMS вопреки решению владельца

C4 изменил SMS payload, маршрут и текст через mail renderer. Владелец 23.08 (§1.2e): SMS остаётся как есть и
выключен; §1.5 — SMS в брендинг не включается. **Откати правки SMS полностью.** Не «сделай аккуратнее» —
верни как было: канал выключен, трогать его в этом проходе не просили.

## Что подтверждено и НЕ переоткрывается

Письма аутентификации больше не показывают `BersonCare`; брендированное письмо без owner copy действительно
fail-closed до SMTP; профиль обязателен по конструкции и на уровне SQL; резолвер и рендерер одни; тексты писем
не переписаны; права и гранты корректны; alpha-имя SMS не добавлено.

## Границы

- Не трогать `src/proxy.ts`, `src/config/surfaceRoutes.ts`, юридические страницы, каталог `admin/` — там
  параллельно работают другие агенты.
- Тексты писем не сочинять и не править: владелец 23.08 пишет их сам.
- Ничего не приземлять в `feat`. Пушить `git push dimmdao wt/night-c4-20260823`, в `main` никогда.
- Полный прогон только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`. Порт 5200 не занимать.
- Коммитить только свои файлы поимённо. PROD и TEST не трогать; SQL — только DEV, откат описать.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.

## Приёмка

- [ ] `FC-1` Тест аудитора `17 passed`, текст теста не ослаблен — доказать `git diff` по тестовому файлу.
- [ ] `FC-2` Перечислены ВСЕ ветки цепочки доставки и показано, какое имя получает каждая; правило одно.
- [ ] `FC-3` Правки SMS откачены полностью — доказать `git diff` по SMS-маршруту, payload и рендереру.
- [ ] `FC-4` Инъекция: сломай выбор профиля в ДРУГОЙ ветке, чем проверял аудитор, и докажи, что тест краснеет.
- [ ] `FC-5` typecheck + scoped lint + затронутые тесты зелёные; ветка запушена.
