# Бриф `F4`, круг 2: живой замер на DEV

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `F4` —
«Заменить единственный global-admin переключатель входа на политики `staff`, `platform_admin` и patient;
миграция существующего значения детерминирована».

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-f4-20260823`, ветка `wt/night-f4-20260823`.
**Вход:** круг 1, `e700b0f35` — код и миграция готовы, тесты `13/13`, typecheck и lint зелёные.

## Что осталось: одно — доказать на живой базе

Круг 1 не смог применить миграцию: `migrate-dev.sh --preflight` отказал с `DEV API env path guard failed`,
потому что в клоне не было канонических `.env` и `apps/webapp/.env.dev`. **Ведущий положил оба файла в клон** —
защиту обходить не нужно и нельзя, она теперь просто проходит.

Требуется:

- `bash deploy/host/migrate-dev.sh --preflight`, затем `--execute` — штатным путём, без ручного SQL;
- **замер до и после**, числами: сколько было прежних значений, сколько появилось строк по поверхностям,
  и построчная сверка с таблицей «было → стало» из круга 1. Если хоть одна строка легла иначе, чем обещано, —
  это блокер, а не мелочь;
- **доказательство, что поведение не изменилось**: перечисли доступные способы входа на каждой из трёх
  поверхностей до и после миграции, списком, а не словом «совпадает»;
- если общий сверочный прогон упадёт на `pre-session exact gate`
  (`app.email_auth_find_email_challenge_for_confirm`) — **это известный чужой дефект**, разобран в
  `docs/_TODO/runs/PRE_SESSION_GATE_CONFLICT_2026-08-23.md`. Не чини его и не обходи: отметь в отчёте и иди дальше.

## Границы

- кода круга 1 не переписывать: он готов, задача — доказать;
- ничего, кроме применения миграции и замеров, не делать;
- `.env`-файлы не коммитить, не копировать дальше и не печатать их содержимое в отчёт.
