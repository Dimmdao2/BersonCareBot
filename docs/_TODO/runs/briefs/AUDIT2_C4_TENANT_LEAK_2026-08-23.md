# Аудит `C4`, круг 2 — доставка предиката арендатора в живые базы

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C4` —
«`C4` Расширить existing SMTP config только sender display data, добавить один org-scoped transactional template».

**Что проверяешь:** коммит `c06a2daa1` на ветке `wt/therapysto-night-20260823`.
**Клон:** `/home/dev/dev-projects/bcb-wt-therapysto-night-20260823`.
**Автор:** Codex `gpt-5.6-sol`/high. Ты не слабее автора.
**Вход:** твой же круг 1 — `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_C4_DECLARATION_FIX_2026-08-23.md`.

**Прогон автора оборван портом (`blocked_system`) на 35-й минуте. Файла-отчёта НЕТ.** Всё, что он заявляет,
лежит только в сообщении коммита: `migrate-dev` owner preflight PASS; DEV rollback-proof `foreign=null`,
`own=ROUND2-X-OWN`, без контекста `42501`; две инъекции снятия предиката красные; полный CI `exit 0`.
Это заявка, а не доказательство: проверяй каждое своими руками.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Утечка шаблона чужой клиники через обе копии корня | **Тест** | Стена арендатора; регрессия молчаливая и уходит в письма живым людям |
| Доставка исправленного тела в уже существующую базу | **Взгляд** | Разовая проверка состояния DEV после прогона миграций |
| Проба `VERIFY` отвергает уязвимое тело | **Взгляд** | Разовое применение пробы к двум версиям тела |
| Соответствие пункту `C4` плана владельца | **Взгляд** | Сверка с owner-регистром |

## Что проверить

1. **Обе копии.** Твой блокер 1 круга 1: корень определён дважды. Проверь тем же способом, каким ловил —
   примени блок из `deploy/postgres/integrator-server-runtime-config.sql` дословно поверх исправленного
   тела (rollback-only) и покажи, что чужой шаблон больше НЕ возвращается.
2. **Доставка.** Твой блокер 2: правка в применённой миграции до живых баз не доезжает. Прогони миграции
   на DEV и покажи состояние живого тела ПОСЛЕ:
   `strpos(pg_get_functiondef(…), 'current_org_id') > 0` для корня и обеих перегрузок. Если тело осталось
   уязвимым — блокер остаётся, как бы ни выглядел файл миграции.
3. **Проба `VERIFY` работает как гейт.** Примени её к уязвимому телу и к исправленному: на уязвимом обязана
   давать `false`. Прежняя проба этого не делала — убедись, что новая делает.
4. **Штатный путь не сломан.** Своя клиника по-прежнему получает свой шаблон; без контекста — `42501`.
   Замеряй своими значениями, не переписывай ни круг 1, ни автора.
5. **Инъекции — свои, минимум три**, включая снятие предиката в каждой из двух копий.
6. **Полный CI по ветке** только через общий замок хоста:
   `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`. Приведи `rc`, длительность, `runs/ci-last.json`,
   сверь `HEAD` до и после. Красный CI — блокер.
7. **Миграция:** `GRANT`, `REVOKE`, `CREATE POLICY` — `0` (грепом, не глазами). Владелец миграции объявлен.
8. **Постороннее расхождение**, названное автором (`email_auth_find_email_challenge_for_confirm`,
   exact-gate drift на DEV reconcile) — проверь, наше это или соседней ветки, и скажи прямо. В скоуп не бери.

## Границы

- **Не чинить.** Находка без пункта в плане владельца — вопрос владельцу, не задача.
- Покрытие корня гейтом `D17` — вопрос владельцу, названный тобой в круге 1; в скоуп не заводить.
- PROD и TEST не трогать. Живая DEV — только rollback-only. Порт `5200` не занимать.

## Канон

- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел.
- Поиск по коду — `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, не слепой grep.

## Отчёт

Вердикт `PASS, FOR LAND` или `FAIL, NOT FOR LAND` первой строкой. Числа: блокирующих, неблокирующих,
инъекций посажено / убито / не поймано. Отчёт положи в
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_C4_TENANT_LEAK_2026-08-23.md` и закоммить.
