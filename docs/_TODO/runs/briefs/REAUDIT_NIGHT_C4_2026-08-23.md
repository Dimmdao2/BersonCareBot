# Повторный аудит слайса C4, круг 2

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C4` —
«Расширить existing SMTP config только sender display data, добавить один org-scoped transactional template
setting и один mail-profile resolver/renderer.»

**Что проверяешь:** коммиты `904b03f7e` + `c16ec75a1` на ветке `wt/night-c4-20260823` — фикс по двум
блокирующим находкам круга 1 (аудит `f23876e7b`, вердикт `FAIL`).
**Клон:** `/home/dev/dev-projects/bcb-wt-reaudit-c4-20260823`.

## Что заявил фиксер

`forgot` выбирает отправителя по роли реального получателя: `client → Therapygo`, `doctor/admin → Therapysto`.
Все ветки C4 идут через один `platformMailProfileForRecipientRole`; прямых вызовов `platformMailProfile(...)`
в production auth-коде не осталось. SMS полностью возвращён к состоянию до C4 — без `mailProfile`, без
зависимости от БД, без mail renderer; доказано `git diff --exit-code` по трём файлам. Тест аудитора не
ослаблен — `git diff --exit-code` по тестовому файлу пуст. `17 passed`, инъекция в ДРУГОЙ ветке
(`/api/auth/email/start`, подмена `client → Therapysto`) дала `1 failed`. Scoped webapp `25 passed`,
integrator `20 passed`, оба typecheck и ESLint зелёные.

## Что проверить

1. **Перечисли ВСЕ ветки доставки кода СВОИМ способом** и скажи, какое имя получает каждая. Круг 1 нашёл
   пропущенную ветку именно так; ищи следующую пропущенную, а не подтверждай авторский список.
2. **Своя инъекция в третьей ветке** — не в той, что проверял автор, и не в той, что проверял круг 1.
3. **SMS действительно откачен** — сверь с состоянием до `7b1ef9ba6` сам, включая тексты и payload.
4. **Роль получателя берётся надёжно.** Что происходит, если роль не определилась или получатель есть и в
   персонале, и среди пациентов: какое имя он получит и не утечёт ли чужая идентичность.
5. **Один резолвер** (`TPB-16`) — перепроверь своим подсчётом.
6. **Права и гранты** после отката SQL — `node scripts/check-migration-privileges.mjs`.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/REAUDIT_NIGHT_C4_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
