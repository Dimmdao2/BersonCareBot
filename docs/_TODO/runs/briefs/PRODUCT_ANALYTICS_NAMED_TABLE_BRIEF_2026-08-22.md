# Бриф: экран «Приложение» — именная таблица людей уходит, экран остаётся на агрегатах

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, решение **Р-АДМИН** (§2.3).
  Твой DONE = закрытые пункты ЭТОГО брифа. Пункты плана закрывает ведущий.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`. Особенно §1, §5, §6, §10a, §24.

**Источник оракула:** `docs/_TODO/OWNER_QUESTIONS_2026-07-26.md`, пункт #1019-Q1 — «**Включать можно только
после того, как уберут переход к пациентам** с вкладок «Клиенты» и «Приложение».»

## Что решено и кем (не переоткрывать)

Решение принято ДВАЖДЫ и с двух сторон:
1. **26.07, #1019-Q1:** patient-level drill-down платформенного админа закрыт fail-closed
   (`/api/admin/doctor-analytics-metric-accounts` → `409 platform_patient_drilldown_disabled`), и включение
   разрешено только после снятия перехода к пациентам с вкладок «Клиенты» и «Приложение».
2. **22.08, `Р-АДМИН` (владелец):** «глобальный админ читает данные аккаунтов и открытые данные… Мед данные —
   не читает», «мне нахуй не нужны какие-то их данные, кроме того, что нужно для моей работы». Там же
   агентское уточнение: где админу нужно ЧИСЛО про людей — экран ходит в корень за агрегатом, а табличных
   грантов принципалу не выдаётся.

Экран «Приложение» (`/api/admin/product-analytics`, `ProductAnalyticsSection.tsx:267-310`, наполняется
`pgProductAnalytics.ts:440-458` через `platform_users ⋈ user_identity`) показывает таблицу «Клиент» с ФИО,
«был в сети» и минутами активности — то есть строки людей поимённо. Она и есть тот самый «переход к
пациентам» с вкладки «Приложение», из-за которого соседний drill-down стоит закрытым. **Таблица снимается.**

## Задание

1. **Снять именную таблицу с экрана** вместе с её загрузчиком и путём чтения `platform_users ⋈ user_identity`.
   Не прятать флагом, не оставлять «на всякий случай» — удалить, вместе с осиротевшим кодом. Если у неё есть
   вызывающие вне этого экрана — назови их, тогда это другая работа.
2. **Экран остаётся живым:** KPI-часть, которая и так агрегат, обязана продолжать работать и отдавать 200
   под платформенным принципалом. Если после снятия таблицы 500 остаётся — значит KPI тоже читает строки,
   и его надо перевести на корень тем же способом, каким переведены два соседних экрана в
   `docs/_TODO/runs/integrator-cleanup/ADMIN_STATS_AGGREGATES_2026-08-22.md`. **Существующий
   `app.read_platform_user_stats` переиспользуй, если он даёт нужные величины; второй корень не заводи** (§5).
3. **Ни одного нового табличного гранта роли `app_platform_settings`** — это условие приёмки, проверяется
   диффом артефактов. Права — только `declaration.ts` + генератор, оба `--check` побайтно; в миграции ни
   `GRANT`, ни `REVOKE` (§1).
4. **Если снятие таблицы делает достижимым включение соседнего drill-down** — НЕ включай его. Это условие
   владельца из #1019-Q1, включение — отдельное решение, не следствие твоей правки (§24.6). Скажи об этом
   в отчёте одной строкой.
5. **Доказательство:** `/api/admin/product-analytics` отдаёт 200 под платформенным принципалом; в ответе нет
   ни одного поля с именем, фамилией или контактом человека (проверить утверждением по ответу, а не глазами);
   инъекция неисправности красит проверку. `--execute` на DEV ЗАПРЕЩЁН — `--preflight` с откатом и живой
   прогон в откаченной транзакции.
6. `pnpm test:db-privileges`, затронутые сюиты вебаппа, typecheck, lint. Два пред-существующих отказа `tsc`
   в `declaration.ts` (строки ~3642/6731) — НЕ твои, не чинить, проверено `git stash` предыдущей веткой.

## Границы
Деплой, запись на TEST, `push`, full CI — НЕ твои. Прод не трогать. Другие вкладки аналитики не трогать.
Отчёт: `docs/_TODO/runs/integrator-cleanup/PRODUCT_ANALYTICS_NAMED_TABLE_2026-08-22.md`.
