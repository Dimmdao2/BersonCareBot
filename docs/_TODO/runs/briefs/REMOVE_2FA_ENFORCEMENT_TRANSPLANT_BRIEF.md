# Worker: перенести удаление платформенного принуждения 2FA на свежую feat — #1082

Роль: worker. Работать только в `wt/2fa-enforcement-removal`, созданной от актуальной
`feat/doctor-ui-rebuild` (`72b18c4b7`). Прочитать `AGENTS.md`: маршрут, §1 миграции/DEV,
§5, §9–§10b, §16–§17, §21, §24; `apps/webapp/src/modules/auth/auth.md`,
`apps/webapp/src/app-layer/guards/guards.md`, `apps/webapp/src/app/app/account/account.md`.

## Authority

План: `docs/_TODO/SINGLE_ENTRY_CLEANUP_2026-08-01.md`, Ч7 и запись в
`docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` по `92388d1df`.

**Источник оракула:** прямое распоряжение владельца 01.08 — «переключатель
`auth_2fa_enabled` требует второй фактор от всего персонала — удали». Добровольный TOTP пользователя
остаётся: требование проверки возникает только у человека, который сам завёл фактор.

## Задача

1. Перенести **только** продуктовый смысл коммита `92388d1df` из доступного объекта git на свежую ветку.
   Начать с `git cherry-pick 92388d1df`; конфликты решить по актуальному коду, не возвращая старое состояние
   `wt/settings-to-db` и не затрагивая тарифы/биллинг.
2. Миграцию `0294_remove_platform_staff_2fa_enforcement.sql` перенумеровать в `0303`: соседний тарифный
   workstream уже зарезервировал `0298` и `0299`, а перенос кругов Ч7 занимает `0300`–`0302`. Перед коммитом
   повторно проверить, что `0303` всё ещё
   свободен. Обновить `_journal.json` согласованно,
   сохранив строгую монотонность `idx`/`when`. Не редактировать и не переименовывать чужие миграции.
3. Удалить только платформенное принуждение и его admin toggle/key. Сохранить добровольный TOTP,
   enrollment/status/verify и `securityFactorRequired` для пользователя с уже заведённым фактором.
4. Сохранить актуальные изменения auth/settings, уже находящиеся в fresh feat. Не переносить отчёты и плановые
   редакции старой ветки, кроме минимального обновления строки Ч7-з с новым SHA/evidence в том же коммите.
5. Не применять миграцию к DEV и не поднимать сервер: stateful live-проверка выполняется лидом после аудита.

## Тесты и проверки

- Таргетированные auth route/unit tests по затронутым файлам, включая два наблюдаемых сценария:
  staff без заведённого фактора не отправляется на `/app/account`; staff с заведённым фактором по-прежнему
  требует собственную верификацию.
- `pnpm --dir apps/webapp typecheck`.
- `pnpm --dir apps/webapp lint` либо узкий ESLint, если полный app lint занят; назвать фактическую команду.
- `bash apps/webapp/scripts/check-drizzle-journal-sync.sh` и подходящий migration self/preflight без записи в БД.
- Полный repo CI не запускать.

## Scope

Разрешены только файлы, реально необходимые для семантики `92388d1df`, соответствующие auth/settings/guard UI,
новая migration + journal, targeted tests, одна строка плана и новый bounded report
`docs/_TODO/runs/testsuite-v2/DROP_2FA_ENFORCEMENT_TRANSPLANT_REPORT.md`.

Запрещено: остальные Ч7-круги, integrator, media-worker, квоты, RLS, тарифы/биллинг, PROD/TEST/DEV data,
`feat`, taskdb, push.

## Готово

- Дифф против fresh feat содержит только удаление принуждения 2FA и чистящую миграцию с уникальным номером.
- Все проверки выше зелёные с точными командами/exit codes в report.
- Коммит содержит `#1082`, почему, доказательства, пункт плана и что не сделано.
- Ветка чистая; worker не пушит.
