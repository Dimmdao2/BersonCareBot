# Бриф Ш7: снятие совместимой сигнатуры — проход по-настоящему один

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D15b/7a**.
  Твой DONE = закрытые пункты ЭТОГО брифа.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`; §1 (миграции НИКОГДА не выдают гранты),
  §5, §6, §10a (тест доказывает ПОВЕДЕНИЕ, не текст исходника), §24.

**Источник оракула:** `docs/_TODO/runs/integrator-cleanup/D15B7A_ACTOR_SUBJECT_SPLIT_SCHEME_2026-08-22.md` — «Миграция: `DROP FUNCTION app.pre_session_resolve_identity(uuid)` и приватные однопараметрические варианты.»

## Задание

1. **Сначала перепись живых вызовов, потом снятие.** Найди ВСЕ вызовы однопараметрических вариантов —
   в коде приложений, в телах функций БД, в тестах, в миграциях. Пустой результат доказывается
   перечислением того, ЧЕМ ты искал (точный поиск по имени, `code-search.mjs`, поиск по `pg_proc.prosrc`),
   а не словом «нет».
2. **Снять совместимую сигнатуру** `app.pre_session_resolve_identity(uuid)` и приватные однопараметрические
   варианты отдельной миграцией. Гранты миграцией НЕ выдаются и не снимаются — только тело/объект (§1).
3. **Если хоть один живой вызов найдётся** — СТОП, не переписывай его молча: назови поимённо и спроси
   ведущего. Шаг про снятие, а не про перевод вызывающих.
4. **Доказательство поведением:** вход всеми тремя учётками владельца на DEV работает ДО и ПОСЛЕ;
   двухпараметрический проход остаётся единственным. Инъекция: временно вернуть снятую сигнатуру и
   показать, что ни один продуктовый маршрут её не выбирает.
5. `--preflight` EXIT=0; `pnpm test:db-privileges`, затронутые тесты, typecheck, оба `--check` побайтно.
   **`--execute` на DEV ЗАПРЕЩЁН** — его гоняет ведущий.

## Границы
Деплой, TEST, `push`, full CI — НЕ твои. Прод не трогать. Ш9 не трогать.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D15B7A_STEP7_2026-08-22.md`.
