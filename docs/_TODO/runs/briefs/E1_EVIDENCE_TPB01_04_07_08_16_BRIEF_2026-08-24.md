# Бриф: `E1` — бинарное доказательство по `TPB-01`, `TPB-04`, `TPB-07`, `TPB-08`, `TPB-16`

## Классификация «тест или взгляд» (§24.4)

Первым шагом по КАЖДОМУ из пяти пунктов реши и запиши в отчёт: это утверждение о ПОВЕДЕНИИ модуля
(→ нужен тест, который краснеет при откате) или разовый факт о состоянии дерева (→ достаточно пойти
и посмотреть, приложив команду и её вывод). Не смешивай: «в коде нет строки» — взгляд, «staff-поверхность
показывает Therapysto» — поведение.

## Источник оракула

Файл: `/home/dev/dev-projects/bcb-wt-therapysto-night-20260823/docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`

Дословно, `E1`: «Закрыть каждый checkbox §2 только его бинарным evidence; синхронизировать активные docs и runbook.»

Оракулы по пунктам — дословные строки §2 того же файла:

- `TPB-01`: «User-visible имя staff/platform surface — **Therapysto**. Доказательство: inventory diff + UI/»
- `TPB-04`: «Не созданы `staff.therapysto.ru` и `patient.therapysto.ru`; единственный отдельный поддомен»
- `TPB-07`: «Остаются один repo, один webapp, одна DB и общие mechanics. Доказательство: architecture diff не»
- `TPB-08`: «Branding влияет только на patient-facing surface; staff/admin видят Therapysto. Доказательство:»
- `TPB-16`: «Реализация расширяет перечисленные choke points и не создаёт параллельных getters/resolvers/stores.»

Канон репозитория: `AGENTS.md` — перед действием `grep -n "^## \|^### " AGENTS.md` и читать свой раздел.

## Что сделать

Ветка `wt/therapysto-night-20260823`, клон `/home/dev/dev-projects/bcb-wt-therapysto-night-20260823`.
По каждому из пяти пунктов дай ОДИН из двух исходов и ничего между:

1. **PASS** — с доказательством ровно того класса, что назван в §2 пункта. Для поведенческих пунктов
   доказательство обязано КРАСНЕТЬ при откате: сажаешь инъекцию, показываешь красноту, откатываешь.
   Нет теста, который краснеет, — значит пункт НЕ доказан, как бы правильно код ни выглядел; тогда
   допишешь недостающую проверку сам (§24.5), один раз, в существующий файл рядом.
2. **FAIL** — с точным местом (`файл:строка`) и тем, что человек увидит по факту.

Границы диффа: `TPB-01`/`TPB-08` — имена поверхностей (`STAFF_SURFACE`, `PATIENT_DEFAULT_SURFACE`,
`mailProfileForResolvedSurface`, metadata страниц, passkey `rpName`, TOTP `issuer`); `TPB-04` — deploy-конфиг
и активные доки; `TPB-07`/`TPB-16` — обход диффа ветки против `feat/doctor-ui-rebuild` на предмет второго
приложения, второго стора, второго диспетчера, параллельного резолвера поверхности.

## Границы

- НЕ трогать `app.read_integrator_clinic_delivery_credential` и миграции `20260823T030000…`, `20260823T043206…`;
  обе роли в одном списке не разрешать (решение владельца 24.08, §1.2k плана).
- Никаких `--execute`, деплоя, PROD, TEST, `telegram_mode`, push в `feat`, слияний в `feat`, full CI.
- Находка, которой нет соответствующей строки в план-файле владельца, — ВОПРОС владельцу в секции «Findings»,
  работой не становится (§24.6).
- Продуктовый код правишь только если это устранение FAIL по названному пункту; инъекции откатываешь все,
  `git status --porcelain` после прогона обязан быть пуст.

**Уложись в 25 минут.** Не успел — пиши, что успел, и что осталось, в секции «НЕ СДЕЛАНО».
