# Фикс B1a: цифровая метка проходит мимо ограничения БД

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B1a` —
«Зарезервировать служебные метки (`www`, `app`, `api`, `mail`, `admin` и т.п.), чтобы slug клиники не
мог их занять. Проверка — на записи slug, а не только на чтении.»

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b1-20260823` (тот же, где лежит работа), ветка
`wt/night-b1-20260823`.

## Находка независимого аудита — блокирующая

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B1_2026-08-23.md`, коммит `72bca1699`
на ветке `wt/audit-b1-20260823` (Codex `gpt-5.6-sol`/high).

**Ограничение БД пропускает прямую запись чисто цифровой метки `123`, хотя политика приложения резервирует
`^[0-9]+$`.** То есть защита стоит только в коде, а требование пункта — «на записи», значит в БД.
Аудитор написал воспроизводящий тест, он уже в дереве аудита:

```
RUN_CLINIC_DOMAIN_WRITE_CONSTRAINTS_DB=1 node --test deploy/postgres/privileges/clinic-domain-write-constraints.devDbProof.test.mjs
```
→ сейчас `2 pass, 1 fail`. Задача: сделать `3 pass`, **не ослабляя тест**.

Остальное аудит подтвердил: hard 404, единый seam, уникальность hostname, гранты не менялись, 4/4 инъекции
пойманы. Это не трогай.

## Что сделать

1. Довести ограничение БД так, чтобы **чисто цифровая метка не записывалась**, и чтобы правило совпадало с
   политикой приложения, а не расходилось с ней. Одно место истины, не второй список.
2. Перенести тест аудитора в свою ветку и держать его зелёным.
3. Миграция — только DEV, откат описать. Гранты не менять; если правка их задевает, назвать отдельной строкой.

## Отдельно, НЕ твоя работа — вопрос владельцу

Аудитор заметил, что в списке служебных меток нет `mta-sts`, `mx`, `mta`, `relay`, `webmail`, `ns`, `www1`,
`www2`. Пункт плана перечисляет метки словами «и т.п.», прямого требования на этот список нет — значит это
**вопрос владельцу, а не задача**. В список их НЕ добавляй, запиши строкой в отчёт.

## Границы

- Не трогать `src/proxy.ts`, `src/config/surfaceRoutes.ts`, юридические страницы, каталог `admin/`, доставку
  писем — там параллельно работают другие агенты.
- Ничего не приземлять в `feat`. Пушить `git push dimmdao wt/night-b1-20260823`, в `main` никогда.
- Полный прогон только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`. Порт 5200 не занимать.
- Коммитить только свои файлы поимённо. PROD и TEST не трогать.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.

## Приёмка

- [ ] `F-1` Тест аудитора `3 pass`, текст теста не ослаблен — доказать `git diff` по тестовому файлу.
- [ ] `F-2` Правило одно; `git grep` доказывает, что второго списка меток не появилось.
- [ ] `F-3` Инъекция: сними ограничение и докажи, что тест краснеет.
- [ ] `F-4` Миграция применена на DEV, откат описан, гранты не тронуты.
- [ ] `F-5` typecheck + scoped lint + затронутые тесты зелёные; ветка запушена.
