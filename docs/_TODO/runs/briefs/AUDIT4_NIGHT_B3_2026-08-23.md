# Аудит `B3`, круг 4 — закрывающий: матрица конфигураций хостов

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B3` —
«Реализовать один `RequestSurfaceResolver` и подключить к существующему request choke point.»

**Что проверяешь:** коммит `594f20d3f` на ветке `wt/night-b3-20260823` — фикс по единственному блокеру
твоего круга 3 (`A-1`, набор подогнан под код).
**Клон:** `/home/dev/dev-projects/bcb-wt-night-b3-20260823`.
**Автор:** Codex `gpt-5.6-terra`/high.
**Вход:** твой отчёт круга 3 — `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT3_NIGHT_B3_2026-08-23.md`.

## Что заявил фиксер

Правка в один файл `proxy.route.test.ts` (+133/−54), продуктовый код не тронут. Матрица: общий origin —
пациентские маршруты живут; различимые origin — чужие деревья hard-404 в обе стороны; через choke point
проверены `staff`, `patient_default`, `platform_admin`. Инъекция «предохранитель всегда `false`» — `4 red`;
безусловный гейт — `4 red`; набор с выставленным `PATIENT_APP_ORIGIN` — `0 red` (было `3 red из 67`).
Scoped Vitest `49 passed`, полный CI `rc=0`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Гейт аудитории маршрутов в обеих конфигурациях | **Тест** | Ровно то, что круг 3 нашёл незакрытым: молчаливая регрессия |
| Продуктовый код круга 3 не изменён | **Взгляд** | Сверка diff'ом по всей истории слайса |
| Имена тестов соответствуют тому, что они проверяют (`A-4`) | **Взгляд** | Разовое чтение набора |
| `Gate B`: пять поверхностей резолвятся через choke point | **Взгляд** | Сверка с owner-регистром |

## Что проверить

1. **Твои же две ноги блокера — своими инъекциями, не по отчёту.** Посади «предохранитель всегда `false`»
   и убедись, что набор краснеет; посади безусловный гейт — тоже. Плюс минимум две СВОИ инъекции этого
   шва, которых у автора не было.
2. **Конфигурация после переезда на домены.** Прогони набор с выставленным `PATIENT_APP_ORIGIN`,
   отличным от `APP_BASE_URL`: `0 red` — иначе merge-gate сломается в день, когда владелец даст домены.
3. **Матрица задаётся в тесте, а не наследуется из окружения.** Проверь, что конфигурация не подтягивается
   из модульного состояния процесса: подмени переменные окружения перед прогоном и покажи, что результат
   набора от этого не зависит.
4. **Продуктовый код действительно не тронут.** Сверь diff всей ветки слайса против принятого тобой
   состояния круга 3: если `proxy.ts`, `requestSurface.ts` или `env.ts` изменились — назови чем и почему.
5. **`A-4`:** имена тестов не должны обещать покрытия, которого нет.
6. **`Gate B`:** какие поверхности реально резолвятся через choke point хотя бы одним тестом — перечисли
   поимённо; чего не хватает, назови.
7. **Полный CI по ветке** только через общий замок хоста:
   `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`. Приведи `rc`, длительность, `runs/ci-last.json`,
   `HEAD` до и после. Автор упоминает stale `.next/types` в этом worktree — проверь, что дерево чистое и
   кэш не влияет на результат.

## Границы

- **Не чинить.** Находка без пункта в плане владельца — вопрос владельцу.
- `A-2` (staff-манифест сохранил необработанный throw) и `A-3` (`R-2` стал конфигурацией по умолчанию) —
  вопросы владельцу, названные тобой же; в скоуп не заводить.
- PROD и TEST не трогать, порт `5200` не занимать, свои dev-серверы гасить.

## Канон

- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел.
- Поиск по коду — `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, не слепой grep.

## Отчёт

Вердикт `PASS, FOR LAND` или `FAIL, NOT FOR LAND` первой строкой. Числа: блокирующих, неблокирующих,
инъекций посажено / убито / не поймано. Отчёт положи в
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT4_NIGHT_B3_2026-08-23.md` и закоммить.
