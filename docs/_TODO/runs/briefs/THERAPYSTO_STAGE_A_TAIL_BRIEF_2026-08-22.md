# Бриф: пересмотр всего объёма этапа A и закрытие остатка

Задача из двух частей: сначала **пересмотреть весь этап `A` целиком** — что в нём действительно закрыто, а
что только выглядит закрытым, — и лишь потом доделывать. Отчёты четырёх предыдущих кругов читай как версию
исполнителей, а не как истину: три круга из четырёх заявляли закрытие, которое аудит опровергал.

- **План-файл (единственный источник todo и done):**
  `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, этап `A`, требования `TPB-01`,
  `TPB-03`, `TPB-04`, `TPB-15`, `TPB-16`, Gate A.
  🔴 В шапке плана — режим владельца: этапы в `feat` сводит только он. **Приземления не предлагать.**
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свои разделы.
- **История этапа:** четыре круга и четыре вердикта — в `NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` (искать
  «Therapysto этап A»), отчёты — в папке инициативы (`AUDIT_STAGE_A_*`, `CORRECTION_STAGE_A_*`).
- **Клон:** `/home/dev/dev-projects/bcb-wt-therapysto-stage-a-20260822`, ветка
  `wt/therapysto-stage-a-20260822`.

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-15` — «User-visible BersonCareBot/platform BersonCare и понятие BersonCare Bot заменены на Therapysto;
technical IDs и history не переименованы. Доказательство: scoped exact inventory before/after с явным
allowlist technical/history.»

## Карта имён, по которой классифицируешь (решения владельца 22.08.2026)

| Что | Имя |
|---|---|
| Специалисты, админы клиники, платформенные админы | **Therapysto** (`therapysto.ru`) |
| Общий вход пациентов | **Therapygo** (`therapygo.ru`) |
| Пациенты конкретной клиники | бренд клиники, приезжает данными на этапах `B`–`D` |

`BersonCare` перестал быть именем продукта: он стал первой клиникой-арендатором. В коде продукта его как
имени приложения быть не должно, но **branding `if BersonCare` запрещён требованием `TPB-06`** — значит и
подставлять «Therapysto» туда, где по смыслу должен быть бренд клиники, тоже нельзя. Такие места —
находка для этапа `C`, а не правка.

## Делать

- [ ] `T1` **Пересмотр объёма.** Пройди этап `A` по пунктам плана и скажи по каждому: закрыт / закрыт
  частично / не закрыт — со ссылкой на доказательство. Особое внимание тому, что четыре круга объявили
  сделанным: единый конфиг, отсутствие вторых мест с именем, гейт. Если найдёшь, что заявленное закрытие
  дырявое, — это важнее любой новой правки, доложи первым.

- [ ] `T2` **`A3`: инвентаризация по ВСЕМУ репозиторию, а не по `apps/webapp/src`.** Прежние круги мерили
  узко и дважды на этом попались. Раздели найденное на четыре корзины:
  1. **правим сейчас** — user-visible имя продукта в staff-периметре;
  2. **не трогаем, техническое** — npm-имена (`berson-care-bot`, `@bersoncare/*`), имена таблиц, ролей,
     колонок, переменные окружения, имена веток и worktree, директория
     `apps/integrator/src/integrations/bersoncare/`, файл `bersoncare-tweakcn-theme.css`;
  3. **не трогаем, история** — `docs/_TODO/runs/`, очередь аудита, архивы, отчёты;
  4. **этап `C`, только доложить** — тексты, которые читает ПАЦИЕНТ: подписи писем, тема и тело письма и
     SMS с кодом входа, уведомления. Там правильное имя — бренд клиники или Therapygo, и решается это
     механизмом брендинга, а не заменой строки. **Ничего из этой корзины не переименовывать.**

  Известная зацепка от круга 3: текст кода входа в SMS и почте до сих пор говорит «BersonCare», источник —
  в интеграторе и в двух SQL-файлах `deploy/postgres/**`. Разберись и положи в правильную корзину с
  обоснованием; в SQL и миграции не лезь.

- [ ] `T3` **`A4`: активные документы.** В действующих owner/contract/runbook документах заменить описание
  имени платформы и адресов, которое теперь противоречит карте выше. Исправлять на месте, не добавлять
  сноску рядом со старым вариантом. Архивы и историю не трогать. Если документ большой и правка
  неочевидна — вынеси списком в отчёт, а не переписывай на своё усмотрение.

- [ ] `T4` **Хвосты, названные аудитом:** `apps/webapp/src/config/config.md:12` ссылается на удалённый хук
  `usePatientSurfaceName`. Проверь, нет ли других ссылок на удалённое.

## НЕ делать

- Не трогать: `modules/auth/passkeyAuth.ts`, `modules/staff-security/totp.ts` (исключены владельцем),
  `sendBookingConfirmationEmail.ts` и прочие пациентские тексты (этап `C`), `apps/integrator/**` кроме
  чтения для инвентаря, миграции и SQL, `id`/`scope`/`start_url` манифестов.
  **Привязка passkey к домену не меняется ни на йоту.**
- Не перестраивать механизм идентичности — он прошёл аудит. Если найдёшь в нём дефект, это находка в
  отчёт, а не правка на своё усмотрение.
- Не начинать этапы `B`, `C`, `F`. Подделка `x-bc-pathname` вне matcher'а сознательно отложена в `B`.
- PROD, TEST, деплой, БД, push — не трогать. Коммитить свои файлы поимённо, `git add -A` запрещён.
  Порт 5200 не трогать, свой dev-сервер — на свободном альтернативном порту.
- Оговорка: при живом `next dev` (Next 16.2.11) `tsc` падает на сгенерированном
  `.next/dev/types/routes.d.ts` — это не твой код, лечится `rm -rf .next/dev`.

## Приёмка

- [ ] `V1` Таблица «пункт этапа `A` → закрыт/частично/нет → доказательство».
- [ ] `V2` Инвентаризация по всему репозиторию с числами и командой, которой они получены; каждое
  оставшееся вхождение лежит в названной корзине с причиной.
- [ ] `V3` Живой прогон: staff-периметр показывает Therapysto, пациентский — Therapygo, ничего не сломано.
- [ ] `V4` typecheck, scoped lint, затронутые тесты зелёные.

Отчёт: что пересмотрено и что оказалось не тем, чем заявлено; что изменено с путями; что положено в корзину
`C` и почему; что осталось вопросом владельцу. Числа — вместе с командой. Пустой результат доказывается
перечислением мест, где искал.
