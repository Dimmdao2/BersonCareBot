# Повторный аудит слайса B3, круг 2

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B3` —
«Реализовать один `RequestSurfaceResolver` и подключить к существующему request choke point.»

**Что проверяешь:** коммиты `1e8a86b4b` (задел прерванного фикса) + `6aeab03c4` на ветке `wt/night-b3-20260823`
— фикс по трём блокерам твоего же круга 1 (аудит `bbfb40d7c`, вердикт `FAIL`, непойманного `4`).
**Клон:** `/home/dev/dev-projects/bcb-wt-reaudit-b3-20260823`.

## Что заявил фиксер

Гейт маршрутов включается только при различимых Host; при одном Host пациентские маршруты не режутся. Добавлен
regression-тест манифеста на общем Host. Инъекция удаления PWA fallback дала `rc=1`, после восстановления
прошла. Production build `rc=0` (142 с, через общий замок). Живой прогон кандидата на `:5210` при Host
`127.0.0.1:5200`: patient login `200`, cabinet `307`, `/book` `200`, валидный `/join/*` `200`, manifest `200`
с `id=/app`, `scope=/app`, `start_url=/app/patient`, `/api/health` `200` с БД `up`, а `/clinic-a` даёт
содержательный app-level `404`, а не пустой `404` гейта.

## Что проверить

1. **Все шесть путей из таблицы твоего круга 1** — своим прогоном production-сборки на своём порту. Тогда они
   отдавали `404, 0 байт`. Проверь и отличие пустого гейт-404 от настоящего app-404 по размеру тела, как ты
   это делал в круге 1.
2. **`F-2`:** совпадение `PATIENT_APP_ORIGIN` и `APP_BASE_URL` больше не гасит приложение — включая
   `/api/health`. Проверь именно этой конфигурацией.
3. **`F-3`:** установленные пациентские PWA получают манифест; `id`, `scope`, `start_url` не изменились.
4. **Обратная сторона — важнее прямой.** Гейт обязан РАБОТАТЬ, когда Host различимы: сделай origin разными и
   докажи, что пациентское дерево на staff-хосте отбивается. Иначе мы просто выключили проверку.
5. **Задел `1e8a86b4b` писался вслепую** после третьего обрыва порта — пройди по нему отдельно.
6. **Твоя находка `N-2`** (посаженный второй Host→surface резолвер оставлял тесты зелёными) — проверь, изменилось
   ли что-то. В скоуп не заводи: это неблокирующее и было классифицировано как «взгляд».
7. **Полный CI** по этой ветке через общий замок: круг 1 показал `ci:steps` rc=0. Подтверди, что он остался
   зелёным после правки.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/REAUDIT_NIGHT_B3_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
