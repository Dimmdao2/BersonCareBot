# Аудит слайса «админская поверхность»: своя идентичность без PWA

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-16` — «Реализация расширяет перечисленные choke points и не создаёт параллельных getters/resolvers/stores.
Доказательство: dependency/architecture audit по diff.»

**Что проверяешь:** коммит `96e716ad3` на ветке `wt/night-admin-20260823`, автор — Codex `gpt-5.6-terra`/medium
по брифу `docs/_TODO/runs/briefs/NIGHT_ADMIN_CLEANUP_BRIEF_2026-08-23.md`.
**Клон:** `/home/dev/dev-projects/bcb-wt-audit-admin-20260823`.

## Что проверить под лупой

Автор заявил: `/app/admin` получил Therapysto metadata без `manifest` и `appleWebApp`, добавлен unit-тест;
правка `F1` внесена в две строки плана; целевой тест, typecheck и scoped ESLint прошли.

**Автор оставил два незакрытых места, и оба надо проверить, а не принять на слово:**

1. **`admin/` НЕ удалён** — автор нашёл живые ссылки: `package.json:15` (`build:admin`) и legacy nginx
   `/admin/ → 127.0.0.1:8080` в `SERVER CONVENTIONS.md:349` и `HOST_DEPLOY_README.md:376`.
   Проверь это утверждение сам и назови **полный** список того, что держит каталог живым. Владелец 22.08
   распорядился каталог удалить, значит вопрос не «удалять ли», а «что снять перед удалением».
   Отдельно скажи: nginx-правило описано в доке или реально стоит на живом хосте.
2. **`pnpm run build:webapp` не дошёл до артефактов** (`BUILD_ID`/standalone отсутствуют) — автор объявил это
   блокером и зелёной сборки не показал. **Это главное, что ты проверяешь.** Собери сам через общий замок
   хоста и скажи прямо: сборка сломана правкой автора, сломана до неё, или просто не дождались.
   Если сломана правкой — это блокирующая находка.
3. **Метаданные админки.** Инъекция: верни `staffPwaLayoutMetadata` на место и докажи, что тест краснеет.
   Не краснеет — гейт бумажный.
4. **Правка `F1` в плане** — сверь обе строки с owner-регистром §1.2–§1.2g. Расхождение блокирующее.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_ADMIN_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
