# Полный CI красный на аудите зависимостей — четыре пакета

Rules: `AGENTS.md` — Маршрут, CORE rules, §9 (full CI gate), §24. Internal work in English.

⚠️ Один ход, следующего не будет; всё на переднем плане; коммит до конца хода.

Authority: этот бриф (`ORCH_OPS`). Блокирует деплой: без зелёного CI выкатка не идёт.

Источник оракула: `AGENTS.md` §9 — полный `pnpm run ci` обязателен перед deploy/merge; красный аудит
зависимостей останавливает выкатку так же, как красный тест.

## Измеренное состояние

`/home/dev/brain/host-orch/run-tests.sh 'pnpm run ci'` на `feat/doctor-ui-rebuild` (04.08 05:49) → `exit 1`,
`441s`. **Тесты и типы прошли** — падает только `registry-prod-audit`, лог
`full-ci-0549.log` в scratchpad сессии.

Затронуты четыре пакета, все закреплены пинами в `pnpm.overrides` корневого `package.json`:

- `fast-uri@3.1.4` — high;
- `undici@8.5.0` — один high и четыре moderate;
- `hono@4.12.27` — moderate;
- `ip-address@10.2.0` — high (`GHSA-mwp4-54f8-5fhr`, затронуты `<=10.3.0`), плюс два moderate, у одного
  диапазон `>=10.1.1 <=10.2.1`.

## Работа

1. Для каждой уязвимости найти **точную исправленную версию** по её advisory, а не «взять последнюю».
2. **Поднять пины до исправленных версий, оставаясь внутри своего мажора**, если это возможно: именно так
   чинилась предыдущая волна (`brace-expansion`, коммит `82ddaf06e`) — переход через мажор ломал eslint.
3. Если для какой-то уязвимости патча не существует: расширить существующий allowlist
   (`scripts/registry-prod-audit-allowlist.json`) в той же форме, что уже используется, с честным обоснованием
   — почему в проде недостижимо — и датой пересмотра. Новый механизм подавления не изобретать, уровень аудита не
   понижать, сам `registry-prod-audit` не ослаблять.
4. После правки прогнать **полный** `pnpm run ci`, а не только аудит: пины трогают дерево зависимостей, и
   поднятая версия может сломать сборку или lint — именно это случилось при прошлой попытке поднять
   `brace-expansion` через мажор.
5. В отчёте по каждому пакету одной строкой: подняли до версии X / подавили с обоснованием Y.

## Границы

- Продуктовый код, миграции, БД и деплой не трогать. **PROD не трогать.** Push не делать.

## Готово значит

`/home/dev/brain/host-orch/run-tests.sh 'pnpm run ci'` → `exit 0`, в отчёте путь к логу и длительность.
Один коммит на своей ветке, включая `pnpm-lock.yaml`.
