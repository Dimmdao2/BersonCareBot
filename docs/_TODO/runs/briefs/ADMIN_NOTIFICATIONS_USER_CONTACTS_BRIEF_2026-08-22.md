# Бриф: страница «Уведомления» в админке отдаёт 500 — чтение почты мимо двери

- **План-файл (единственный источник todo и done):**
  `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **Б2** и живой обход TEST 22.08.
  Твой DONE = закрытые пункты ЭТОГО брифа, а не «миссия выполнена». Пункты плана закрывает ведущий.
- **Канон правил репозитория — `AGENTS.md`.** Перед действием: `grep -n "^## \|^### " AGENTS.md`,
  найди раздел по своей теме и прочитай его. Особенно §1 (миграция НИКОГДА не несёт `GRANT`/`REVOKE`/
  `CREATE POLICY` и разбор её прав перед приземлением), §5 (единственный chokepoint), §6 (роли и RLS),
  §10a (тест доказывает ПОВЕДЕНИЕ, никогда не текст исходника), §24.

**Источник оракула:** `AGENTS.md:732` — «**Что считается выполненным.** Есть единственная точка, через которую
операция проходит, и есть механическая» — правило единственного прохода; тот же файл, раздел §5.

## Что измерено (ведущий, живой обход TEST 22.08.2026, голова `daa08c2ed`)

Кликовый обход всех тринадцати страниц админки под учёткой глобального админа `dimmdao@gmail.com` на
`https://test.bersoncare.ru`. Двенадцать страниц отвечают 200. Одна — нет:

    /app/admin/notifications  →  500  (Server Components render, digest 3859192223)

Журнал `bersoncarebot-webapp-test` в ту же секунду (16:04:11):

    [cause]: error: permission denied for table user_contacts
    code: '42501', routine: 'aclcheck_error'
    at async Object.getProfileEmailFields (...)

Запрос — реляционный, из `platform_users` двумя коррелированными подзапросами в `public.user_contacts`
(`value_normalized` и `confirmed_at`, `contact_kind='email' AND is_primary=true`).

## Что это по классу

Это хвост цутовера почты 21.08 (`20260821T040000`): почта уехала из скалярной колонки `platform_users.email`
в `public.user_contacts`, а этот ЧИТАТЕЛЬ двери не получил и ходит в таблицу напрямую — под ролью, у которой
на `user_contacts` грантов нет. Тот же класс, что и уже закрытые 22.08 стены: право видно только при
ИСПОЛНЕНИИ, деплой и reconcile при этом зелёные.

## Задание

1. **Сначала замерь, не решай.** Найди `getProfileEmailFields`, назови `path:line`, под каким принципалом
   исполняется страница и почему у роли нет права. Проверь по декларации
   (`deploy/postgres/privileges/declaration.ts`), объявлен ли уже корень, который отдаёт первичную почту
   человека — **если он есть, второго не заводи**, дай читателю существующую дверь (§5).
2. **Перепиши соседей.** `getProfileEmailFields` почти наверняка не единственный такой читатель после
   цутовера. Перечисли ВСЕ реляционные чтения `public.user_contacts` из `apps/webapp/**`, для каждого скажи,
   достижим ли он из живого маршрута и под каким принципалом. Чинить в этой ветке — только достижимые;
   недостижимые назови списком, это вопрос ведущему, а не работа (§24.6).
3. **Права — только через декларацию и генератор** (`--all` и `--all --port-context-only`), артефакты
   пересобрать, оба `--check` обязаны быть побайтными. В миграции ни `GRANT`, ни `REVOKE` (§1).
4. **Доказательство — живое, не тест формы.** Прогон на `bcb_webapp_dev` из своего worktree: страница
   `/app/admin/notifications` отдаёт 200 и показывает содержимое; инъекция неисправности (снять объявленное
   право) красит проверку, продукт возвращается побайтно.
5. `bash deploy/host/migrate-dev.sh --preflight` (только preflight, `--execute` этой ветке ЗАПРЕЩЁН — DEV
   ведёт главное дерево), `pnpm test:db-privileges`, typecheck и lint затронутых пакетов.

## Границы

Полный деплой, запись на TEST, `push` и full CI — НЕ твои. Страницы админки, кроме этой, не трогать.
Отчёт положить в `docs/_TODO/runs/integrator-cleanup/ADMIN_NOTIFICATIONS_USER_CONTACTS_2026-08-22.md`.
