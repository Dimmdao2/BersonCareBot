# Бриф независимого аудита: одно ограничение названия клиники — 100 знаков

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2i —
решение владельца 23.08.2026: «конечно же, сто знаков, конечно. Зачем больше?».

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b4a-20260823`, ветка `wt/night-b4a-20260823`.
**Что проверяешь:** `a7af0c99a` («fix: limit clinic names to 100 characters»).

## Тест или взгляд (§24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| 100 принимается, 101 отбивается на каждом вводе | **тест** | поведение модуля |
| Второго числа нигде не осталось | **взгляд** | разовая перепись констант |
| Старые длинные названия не убивают адрес | **тест** | это был блокер `B4a` |

## Что доказать

- **ограничение одно и стоит на вводе.** Найди все места, где название организации попадает в базу, — сам,
  не по списку автора (регистрация, настройки бренда, приглашения, админка, импорт, сиды, миграции). В каждом
  либо стоит 100, либо ввода названия нет. Место без ограничения — блокер;
- **чисел 200 и 120 не осталось нигде**: ни в схемах, ни в санитайзере, ни в тестах как ожидание;
- **обрезания больше нет**: что ввели, то и показывают. Проверь, что длинное СТАРОЕ название (в базе такие
  возможны) не роняет адрес в `404` — это был исходный блокер;
- отказ типизирован и доходит до клиента как код, а не как брошенная строка;
- инъекции: посажено / убито / не поймано, числами.

## Границы

- **аудит — гейт против плана владельца, не источник работы**;
- это **не адрес**: `<метка>.therapygo.ru` — 3…30 знаков, не путать и не трогать;
- политику для существующих названий длиннее 100 не выдумывать: на DEV их `0`, для прода это вопрос владельцу.

Вердикт первой строкой. Отчёт — `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_CLINIC_TITLE_LIMIT_2026-08-23.md`.
