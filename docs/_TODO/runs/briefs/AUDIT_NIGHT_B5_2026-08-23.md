# Бриф независимого аудита `B5`: одно пациентское дерево

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B5` —
«Переподключить root/login/recovery/clinic card/booking/patient cabinet к одному patient tree с разным
resolved context. Therapysto home и therapist directory недостижимы на patient origins.»
Плюс §1.2b: корень брендированного адреса показывает визитку клиники.

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b5-20260823`, ветка `wt/night-b5-20260823`.
**Что проверяешь:** `9a1b32bf6` («feat(webapp): unify patient surface routing»), автор — Codex `gpt-5.6-sol`/high.

## Тест или взгляд (§24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Каждый маршрут пункта на обеих пациентских поверхностях | **тест** | поведение модуля |
| Therapysto home и каталог специалистов недостижимы | **тест** | это требование пункта, не осмотр |
| Дерево одно, визитка переиспользована | **взгляд** | разовая перепись |

## Что доказать

- **каждый маршрут пункта** — корень, вход, восстановление, визитка, запись, кабинет — ведёт себя верно
  на `patient_default` и на `patient_branded`. Проверь своими значениями, не авторскими;
- **корень брендированного адреса отдаёт визитку той самой клиники** — и это ТА ЖЕ реализация, что
  `app/[clinicSlug]`, а не похожая. Докажи переиспользованием, а не совпадением вида;
- **Therapysto home и каталог специалистов на пациентских адресах недостижимы.** Попробуй обойти: прямой путь,
  другой регистр, хвостовой слэш, кодированный путь, `rewrite`-цель напрямую;
- **второго дерева нет**: новых page/layout ноль, вторая сборка метаданных не появилась (`TPB-16`);
- **`B4a` цел**: три случая `404`, живой адрес без купленного бренда, сверка происхождения бренда;
- инъекции: посажено / убито / не поймано, числами.

## Известное, в объём НЕ входит

- на голове интеграционной ветки `proxy.route.test.ts` даёт 2 красных из-за формы `authPolicy`: `F1` сделал её
  объектом, ожидания `B4a` остались строкой. Это столкновение слияния, оно чинится отдельно и не является
  находкой по `B5`;
- `B5a` (флаг «сразу вход, визитку не показывать») — отдельный пункт, не требовать;
- боевой шов арендатора — заглушка, свои домены отложены.

## Границы

- **аудит — гейт против плана владельца, не источник работы.** Находка без пункта в плане — вопрос владельцу.

Вердикт первой строкой. Отчёт — `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B5_2026-08-23.md`.
