# Audit brief — находка `F-1` (`C3`), закрывающий круг

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `F1-1` `clinic_if_configured` доходит через `app.enqueue_outbound_message` до потребителя | поведение | слепой kill-set ДО чтения авторских тестов, свои значения |
| `F1-2` клиника СО своим включённым ботом отката на платформенный не получает и на этом продюсере | поведение | своя инъекция возврата отката |
| `F1-3` клиника БЕЗ своего бота получает прежнюю доставку | поведение | регрессия |
| `F1-4` миграция соответствует канону: без `GRANT`/`REVOKE`/`CREATE ROLE`/`ALTER DEFAULT PRIVILEGES`/`CREATE POLICY`, порядок и watermark целы | разовое действие | взгляд на файл миграции и гейты |
| `F1-5` определение функции в ЖИВОЙ DEV-базе и в дампе схемы сходятся | разовое действие | `pg_get_functiondef` своими руками |
| `F1-6` авторский тест не «зелёный всегда» | разовое действие | сними правку — тест обязан покраснеть |
| `F1-7` ничего кроме этого не изменилось | разовое действие | взгляд на diff |

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C3`,
дословно: «удалить любой достижимый platform fallback для них.»

Под аудитом: `04e8a8c9f` на `wt/therapysto-c3-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-c3-20260824`.
Автор: Codex `gpt-5.6-sol`/high. Предыстория — `AUDIT3_C3_2026-08-24.md`, раздел `F-1`.

Автор заявляет: timestamp-миграция сохраняет `clinic_if_configured` в `delivery.senderScope` наравне с
`clinic_required`; сквозной rollback-only тест на живой `bcb_webapp_dev` исполняет миграцию, читает созданный
payload очереди и передаёт intent в настоящий `createDefaultDispatchPort`; инъекция возврата старого условия
покраснела; живую функцию сверял через `pg_get_functiondef`; постоянно на DEV миграцию НЕ применял;
`migrate-dev.sh --preflight` упёрся в отсутствие канонического `.env` в worktree.

Отдельно проверь: тест выполняется только при `RUN_ENQUEUE_CLINIC_SCOPE_DB=1` — то есть в обычном прогоне он
не идёт вообще. Скажи прямо, чего стоит такая защита и ловит ли что-нибудь регрессию без переменной окружения.

## Границы

Прод не трогать. `--execute` на DEV не запускать. Не трогать `app.read_integrator_clinic_delivery_credential`
и миграции `20260823T030000…` / `20260823T043206…`; обе роли не разрешать. В `feat` не вливать.
Finding вне owner scope — вопрос владельцу, не работа.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_C3_F1_2026-08-24.md`: первой строкой вердикт
`PASS, FOR LAND` / `FAIL, NOT FOR LAND`, строка на каждый пункт, счёт инъекций посажено/убито/не поймано.
