# Бриф: круг 4 этапа A — узкий, последний перед остановкой

Это ПОСЛЕДНИЙ круг по этапу `A`. Три круга подряд закрылись `FAIL`. Правило владельца от 28.07: после
эскалации допускаются два таких прохода, потом стоп и вопрос владельцу. Круг 3 был первым эскалированным,
этот — второй и последний. Поэтому объём здесь **сознательно узкий**: два конкретных дефекта, ничего сверх.

- **План-файл:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, этап `A`,
  `TPB-08`, `TPB-16`, Gate A. 🔴 В шапке — режим владельца: этапы в `feat` сводит только он, приземления нет.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.
- **Вердикт, который закрываешь:** `AUDIT_STAGE_A_ROUND3_2026-08-22.md` (коммит `d1556e259`).
- **Клон:** `/home/dev/dev-projects/bcb-wt-therapysto-stage-a-20260822`.

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-16` — «Реализация расширяет перечисленные choke points и не создаёт параллельных getters/resolvers/stores.
Доказательство: dependency/architecture audit по diff.»

## Делать — ровно два пункта

- [ ] `Z1` **`R3-2`: гейт слеп к шву, на котором стоит весь механизм.** `config.matcher` в `proxy.ts` и его
  копия в `surfaceRoutes.ts:201` — два литерала, тест покрытия читает копию. Инъекция «убрать `/` из
  matcher'а» оставляет гейт зелёным, а живой лендинг возвращает пациентский манифест и иконки.
  Сведи к ОДНОМУ источнику (`TPB-16`: не плодить параллельные копии) и добейся, чтобы гейт краснел на этой
  инъекции. Проверь инъекцией, а не рассуждением.

- [ ] `Z2` **`R3-1`: `/app/contact-support?from=clinic-demo` отдаёт пациентскую идентичность**, хотя туда
  ведут четыре CTA «Демо для клиники» со staff-лендинга. Механизм правил `query` уже умеет — это одна строка
  правила, а не перестройка.

  **Урок метода, обязательный к применению.** Круг 3 заявил «обход 149 маршрутов, 0 расхождений», и это
  доказательство оказалось КРУГОВЫМ: ожидаемая поверхность бралась из той же таблицы, которую обход
  проверял. Оно доказывает согласованность рантайма с таблицей и молчит о том, верна ли таблица.
  Поэтому проверяй **от staff-входов, а не от дерева маршрутов**: возьми все ссылки и CTA, ведущие со
  staff-поверхности (лендинг, письма персоналу, приглашения, экраны входа), пройди по каждой и посмотри,
  куда человек попадает и что он там видит. Список ссылок получи измерением и приведи в отчёте.

## НЕ делать

- Не перестраивать механизм заново — он признан аудитом настоящим, гейт признан живым.
- Не чинить подделку заголовка `x-bc-pathname` вне matcher'а (`SHOULD FIX` аудита): последствие косметическое,
  третьим лицом не эксплуатируется, а правильное место для этого — этап `B`, где поверхность будет
  резолвиться по Host. Отметь строкой в отчёте, что оставлено сознательно.
- Не трогать: `passkeyAuth.ts`, `totp.ts`, `apps/integrator/**`, npm-имена, имена таблиц/ролей/колонок БД,
  `bersoncare-tweakcn-theme.css`, `sendBookingConfirmationEmail.ts`. Привязка passkey к домену не меняется.
- Не менять `id`, `scope`, `start_url` манифестов.
- Не делать `A2b`/`A3`/`A4` и не начинать `B`/`C`.
- PROD, TEST, деплой, БД, push — не трогать. Коммитить свои файлы поимённо. Порт 5200 не трогать.
- `tsc` при живом `next dev` падает на сгенерированном `.next/dev/types/routes.d.ts` (Next 16.2.11) — это не
  твой код, лечится `rm -rf .next/dev`; не «чини» это правками в исходниках.

## Приёмка

- [ ] `V1` Инъекция «убрать `/` из matcher'а» краснит гейт; после восстановления гейт зелёный.
- [ ] `V2` `/app/contact-support?from=clinic-demo` отдаёт Therapysto во всех брендозависимых полях и в
  видимых строках; пациентский `/app/contact-support` без параметра не задет.
- [ ] `V3` Перечень staff-входов, полученный измерением, и результат прохода по каждому.
- [ ] `V4` typecheck, scoped lint, `next build`, затронутые тесты зелёные.

Отчёт: что изменено с путями; какие staff-входы проверены и как получен их список; что оставлено сознательно.
Числа — вместе с командой.
