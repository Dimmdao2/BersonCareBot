# D19 — перепроверить целевую схему ПОСЛЕ реализации и актуализировать правило

**Роль:** worker (перепись против реальности + правка документа). **Канон:** `AGENTS.md` — сначала
карта заголовков (`grep -n "^## \|^### " AGENTS.md`), затем §5 (Clean Architecture, границы),
§6 (PostgreSQL/роли), §10a, §24.2/§24.6, §0 (как исправлять правила).
Поиск — сначала `node /home/dev/brain/tools/code-search.mjs "<q>" --repo bcb`, потом grep.

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «D19 — перепроверить правило и схему ПОСЛЕ реализации». Там же дословно назван список проверки и известная находка: «`apps/webapp/ARCHITECTURE.md:50` всё ещё пишет «same DATABASE_URL and DB role for both services» — уже не так».

## Задача

Чекбокс D19 закрывается **правкой документа против реальности** (или явной записью «расхождений нет»),
а НЕ устным «всё сошлось». Правило подгоняется под реальность, реальность под текст — **никогда**.

Пройди ровно пять пунктов из чекбокса, по каждому дай доказательство (`path:line`, команда + её
реальный вывод, интроспекция каталога):

1. **Остался ли в интеграторе хоть один путь записи канона.** Уже измерено независимым аудитом
   22.08 — `docs/_TODO/runs/integrator-cleanup/D25_WRITER_REMOVAL_INDEPENDENT_AUDIT_2026-08-22.md`,
   раздел K1: перечисление непустое ровно на одну запись,
   `apps/integrator/src/infra/db/repos/userChannelBotBlocked.ts:41-50` (`bot_blocked` INSERT в
   `user_channel_bindings`), и аудит классифицировал её как **вопрос владельцу**, не как находку.
   Перепроверь это перечисление на ТЕКУЩЕЙ голове (D25 уже приземлён) и зафиксируй результат.
   Своей работы из этого вопроса НЕ заводи (§24.6).
2. **Ведёт ли к базе один путь.**
3. **Сошлось ли число вечных циклов.**
4. **Выдана ли узкая роль и совпадает ли она с тем, что ассертит деплой.** ⚠️ Это предмет **D17**,
   который ещё ОТКРЫТ. Если ответ зависит от незакрытого D17 — так и напиши, не выдавай желаемое за
   факт и не закрывай этот пункт.
5. **Не появилось ли новых прямых импортов между деревьями приложений вместо пакета.** D19a закрыт,
   гейт есть — проверь, что он живой и красит нарушение.

Затем **актуализируй `apps/webapp/ARCHITECTURE.md`** там, где записанная целевая схема разошлась с
тем, что получилось. Минимум — строка 50 про «same `DATABASE_URL` and DB **role** for both services»:
опиши, как на самом деле, со ссылкой на источник истины. Не переписывай документ шире расхождений.

## Границы

- ⛔ Продуктовый код, тесты, миграции, права, база на запись, деплой, TEST/PROD — НЕ трогать. Эта
  работа — перепись и документ.
- Если перепись обнаружит расхождение, которое требует правки КОДА, — это СТОП и вынос ведущему
  отдельным пунктом отчёта, а не работа в этом ходе.
- Чекбокс D19 в WORK_ORDER **не закрывай сам**: галочку ставит ведущий. Допиши в него фактическое
  состояние и что осталось (в частности зависимость от D17).
- `git add -A`, push, полный CI, фикстуры, disposable-базы — запрещены.

## Формат сдачи

Отчёт файлом `docs/_TODO/runs/integrator-cleanup/D19_ARCHITECTURE_REVERIFY_2026-08-22.md`: пять
пунктов, по каждому вердикт + доказательство; отдельно список внесённых правок в `ARCHITECTURE.md` с
обоснованием каждой; обязательные секции `ВОПРОСЫ ВЛАДЕЛЬЦУ:` и `НЕ СДЕЛАНО:`.
**Коммить до конца хода.** Не заканчивай ход в ожидании фонового процесса.
