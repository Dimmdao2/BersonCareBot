# Повторный аудит слайса B1/B1a/B2, круг 2

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B1a` —
«Зарезервировать служебные метки (`www`, `app`, `api`, `mail`, `admin` и т.п.), чтобы slug клиники не
мог их занять. Проверка — на записи slug, а не только на чтении.»

**Что проверяешь:** коммит `beae913b4` на ветке `wt/night-b1-20260823` — фикс по единственной блокирующей
находке круга 1 (аудит `72bca1699`, вердикт `FAIL`, отчёт `AUDIT_NIGHT_B1_2026-08-23.md`).
**Клон:** `/home/dev/dev-projects/bcb-wt-reaudit-b1-20260823`.

## Что заявил фиксер

Миграция `20260823T011000_reject_numeric_organization_slug_claims.sql` добавила numeric CHECK; `123` теперь
отклоняется. Тест круга 1 — `3 pass, 0 fail`, не ослаблен. Инъекция: снятие CHECK через rollback-only вернуло
`2 pass, 1 fail`, затем rollback восстановил ограничение. Гранты не менялись, 18 unit B1 зелёные.

## Что проверить

1. **Тест не ослаблен** — сравни его текст с версией круга 1 построчно, а не по слову автора.
2. **Своя инъекция:** обойди приложение и попробуй записать напрямую в БД цифровую метку, служебную метку и
   занятый `org_custom_domain_hostname`. Все три обязаны отбиться на уровне БД.
3. **Правило одно.** Проверь, что numeric CHECK и политика приложения не разъехались: не появился ли второй
   список или второе регулярное выражение, которые завтра начнут расходиться.
4. **Гранты** — проверь сам, тихая правка ломает выкатку.
5. **Круг 1 подтвердил** hard 404, единый seam, уникальность hostname, `4/4` инъекции — перепроверь только то,
   что мог задеть фикс.

**Не расширяй список служебных меток.** Отсутствие `mx`, `ns`, `webmail`, `mta-sts` — записанный вопрос
владельцу, а не задача: прямого требования на них в плане нет.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/REAUDIT_NIGHT_B1_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
