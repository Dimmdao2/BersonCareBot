# Аудит перед приземлением: третья дверь pre-session и перепись путей входа (`9ecec7598`)

## Тест или взгляд — классификация по пунктам (AGENTS.md §24.4)

| # | Пункт | Природа | Как доказывать |
|---|---|---|---|
| 1 | новая дверь не отдаёт данные чужого человека | повторяемое поведение | **тест** + fault injection |
| 2 | тело двери не потеряло ничего из прежней логики выбора канала | разовое действие | **взгляд**: построчно «было → стало» |
| 3 | `POST /api/auth/phone/start` доходит до постановки кода целиком | повторяемое поведение | **тест** сквозной, не один вызов |
| 4 | перепись 18 оставшихся точек полна и честна | разовое действие | **взгляд**: свой независимый поиск, не повтор команды автора |
| 5 | стена не расширена, гейт первым оператором, в миграции нет `GRANT` | разовое действие | **взгляд** |

**Источник оракула:** `docs/_TODO/OWNER_WALKTHROUGHS/2026-08-23_TEST_FULL_WALK.md` — «Войти по номеру телефона»

Ветка `wt/presession3-20260823`, отчёт автора —
`docs/_TODO/runs/integrator-cleanup/PRESESSION_THIRD_CALLER_CENSUS_2026-08-23.md`. Канон — `AGENTS.md`.

Живой дефект, ради которого всё делалось: человек жмёт «Войти по номеру телефона», вводит номер и
получает «Не удалось запросить код» (`500`, `Missing declared webapp port capability: pre_session`).

## Что проверить скептически

1. **Это третий круг подряд, и первые два «чинили» ровно этот же 500 и промахивались.** Твоя главная
   задача — сказать, чинит ли эта правка путь ЦЕЛИКОМ. Пройди `POST /api/auth/phone/start` от входа до
   постановки кода и найди следующее реляционное чтение, если оно есть.
2. **Перепись 18 точек.** Сделай свой поиск и скажи: 18 — это всё, больше или меньше? Пропущенная
   точка здесь дороже всего, потому что она вскроется живым отказом у человека.
3. **Дверь отдаёт только своё.** Может ли pre-session-принципал получить канал ЧУЖОГО человека,
   подставив другой `uuid`? Если аргумент не сверяется с опознанным владельцем — это блокер.
4. **Пометку `crossesTenantWall` на эту дверь ставить нельзя** (арендного вызывающего нет) — проверь,
   что её нет и что `definer-tenant-predicate.test.mjs` зелёный.

## Границы

Ничего не чинить, в чужую ветку не коммитить. `--execute`, TEST, PROD, push — запрещены.
Вердикт первой строкой: `PASS` или `FAIL` + причина.
Отчёт: `docs/_TODO/runs/integrator-cleanup/PRESESSION_THIRD_CALLER_AUDIT_2026-08-23.md`.
