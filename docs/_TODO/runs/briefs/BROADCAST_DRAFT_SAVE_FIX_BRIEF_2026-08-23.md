# Бриф-фикс: «Сохранить черновик» в рассылках отвечает 500 на живом TEST

**Найдено обходом кабинета врача 23.08** (`docs/_TODO/OWNER_WALKTHROUGHS/2026-08-23_TEST_FULL_WALK.md`).
Чинить ровно это; соседнее не трогать.

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`; §1 (миграции НЕ выдают прав и НЕ создают
  политик), §5, §6 (роли и RLS), §10a, §24.

**Источник оракула:** `docs/_TODO/OWNER_WALKTHROUGHS/2026-08-23_TEST_FULL_WALK.md` — «`Сохранить черновик` отвечает `500`»

## Что измерено (повтори сам)

Кнопка `Сохранить черновик` на `/app/doctor/broadcasts` и `/app/doctor/broadcasts/archive` отвечает `500`.
В журнале вебаппа TEST:

```
⨯ Error: Failed query:
  [cause]: error: new row violates row-level security policy for table "broadcast_drafts"
```

Снимок каталога TEST на 23.08:

- права на отношении: `{app_object_owner=arwdDxt/app_object_owner,app_staff=r/app_object_owner}` — у
  арендной роли персонала только чтение;
- политики: `rev10_context_gate_67` и `rev10_saas_org_dormant_p0_8_3`, обе `polcmd='*'`, у обеих
  `WITH CHECK` отсутствует (значит проверкой записи служит выражение `USING`), органичение —
  `organization_id = app.current_org_id()`.

## Что сделать

1. **Сначала назови ПРИЧИНУ, а не симптом:** под какой ролью идёт запись черновика, какое право у неё
   есть, и какое выражение отказывает. «RLS отказал» — это симптом; ответ должен назвать роль, право и
   предикат. Докажи пробой в откаченной транзакции на DEV.
2. Почини так, как в этом репозитории чинят подобное: права — ТОЛЬКО через
   `deploy/postgres/privileges/declaration.ts` и генерацию (`generate-cli.mjs --all`, затем
   `--all --port-context-only`, затем `--all --check` побайтово). **Миграция не содержит
   `GRANT`/`REVOKE`/`CREATE POLICY`.** Если запись должна идти именованным корнем — заведи корень с
   гейтом первым оператором после `BEGIN`, как у соседей.
3. Стена клиники ослабляться не должна: черновик одной клиники не должен становиться видимым или
   записываемым для другой. Это надо доказать тестом, а не заявить.
4. `bash deploy/host/migrate-dev.sh --preflight`; `--execute` НЕ запускать.

## Доказательство

- Поведенческий тест: сохранение черновика своей клиникой проходит, попытка записать черновик чужой
  клиники отказывает. Fault injection на оба утверждения с показанным красным и зелёным.
- `typecheck` и `lint` зелёные.

## Границы

TEST и PROD не трогать, `--execute` и `push` не делать, галочки плана не ставить.
Отчёт: `docs/_TODO/runs/integrator-cleanup/BROADCAST_DRAFT_SAVE_2026-08-23.md`.
