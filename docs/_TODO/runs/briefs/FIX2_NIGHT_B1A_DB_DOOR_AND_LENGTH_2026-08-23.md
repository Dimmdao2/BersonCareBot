# Бриф `B1a`, круг 2: те же границы адреса — в двери базы, плюс длина по решению владельца

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B1a` —
«Зарезервировать служебные метки (`www`, `app`, `api`, `mail`, `admin` и т.п.), чтобы slug клиники не мог их
занять; отказ — типизированной ошибкой существующего slug-порта.»

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b1a-20260823`, ветка `wt/night-b1a-20260823`.
**Вход:** круг 1, коммит `9617172c5` — список вырос 146 → 225 имён, тесты 7/7, полный CI `rc=0`.

## `FA-1` (блокер). Новые 79 имён знает только приложение, а дверь базы — нет

`B1` уже установил канон этого места: запреты адреса клиники стоят ДВУМЯ слоями — в порте приложения и
CHECK-ограничением на `public.organization_slug_claims`. Смотри
`apps/webapp/db/drizzle-migrations/20260823T010000_patient_subdomain_slug_and_custom_domain_uniqueness.sql`
(перечень зарезервированных) и `20260823T011000_reject_numeric_organization_slug_claims.sql` (запрет цифр).

Круг 1 тронул только два файла TypeScript. Значит, 79 новых меток — включая `mx`, `ns`, `_domainkey`,
`webmail` — база пропустит при любой записи мимо кода приложения: миграция, скрипт, ручной INSERT.
Приведи перечень в двери базы в соответствие со списком в коде **прямой миграцией** (не правкой уже
применённой — правка применённого файла до живых баз не доедет, это в репозитории уже ловили).

## `FA-2` (блокер). Длина адреса: решение владельца 23.08.2026

- минимум — **3 знака**;
- максимум — **30 знаков** (было 63 — это предел DNS-метки; владелец сузил сознательно).

Сегодня обе границы живут только в приложении (`organizationSlug.ts:200-201`), причём максимум там ещё 63.
Требуется: поправить максимум на 30 в приложении И поставить обе границы CHECK-ограничением в той же
двери базы, рядом с именами и цифрами.

**Перед тем как ставить ограничение — померь, есть ли уже выданные адреса вне новых границ:**
`select count(*) filter (where length(slug) < 3), count(*) filter (where length(slug) > 30), max(length(slug))
from public.organization_slug_claims;` на DEV. Если такие строки есть — ограничение не встанет; тогда НЕ
выдумывай политику миграции сам, а верни это вопросом в отчёт и поставь остальное.

## Доказательства в отчёте

- живой прогон миграции на DEV через штатный `migrate-dev.sh` (preflight + execute), с выводом;
- доказательство двери: прямая запись каждого класса мимо приложения отбита базой — по одному примеру на
  имя из нового списка, на короткий адрес и на длинный. Числа до/после;
- тесты `reservedNamespace.test.ts` и всё, что тронул; scoped lint; `typecheck`;
- полный CI через общий замок — **можно, ты один в очереди**.

## Границы

- список имён из круга 1 не пересматривать и не расширять: он принят;
- политику для уже выданных адресов вне границ не выдумывать — это вопрос владельцу;
- новых модулей не заводить, второй список не создавать: источник имён остаётся один.
