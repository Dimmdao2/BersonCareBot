# Аудит `B6`: cookies host-only и CSRF под матрицей Host

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B6` —
«Оставить cookies host-only и текущий CSRF request-origin seam; добавить только regression tests для
нескольких Host. Unknown Host и cross-org попытки fail closed.»

**Что проверяешь:** коммит `f0d7224b2` на ветке `wt/night-b6-20260823`.
**Клон:** `/home/dev/dev-projects/bcb-wt-night-b6-20260823`.
**Автор:** Codex `gpt-5.6-terra`/high. Ты не слабее автора.

## Что заявил автор

Только тесты (+176/−0), продуктовый код не тронут. Матрица: `Set-Cookie` без `Domain` и неотправка куки на
чужие поверхности; чужой `Origin`/`Referer`/отсутствие обоих → `403` на четырёх поверхностях; неизвестный
Host → `404` без редиректа и без куки; org A + branding org B → `404`. Инъекций 4, убито 4. Scoped Vitest
`74/74`, полный CI `exit 0`, 504 с. Настоящих дыр не найдено.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Границы куки и CSRF на каждой поверхности | **Тест** | Обязано остаться зелёным навсегда; регрессия молчаливая и означает угон сессии |
| Продуктовый код не изменён | **Взгляд** | Сверка diff'ом всей ветки |
| Полнота матрицы против `Gate B` | **Взгляд** | Сверка перечня поверхностей с owner-регистром |

## Что проверить

1. **Заявление «дыр не найдено» — главное, что надо перепроверить, а не принять.** Автор сам себе
   выставил зачёт по механике, которую ему запретили менять. Посади минимум **шесть** своих инъекций, из
   них минимум три таких, каких у автора не было: попробуй пронести куку между поверхностями через
   `Set-Cookie` с `Path`, через поддомен арендатора, через запрос без `Host` вовсе; попробуй CSRF с
   `Origin: null` и с совпадающим `Referer` при чужом `Origin`.
2. **Матрица задаётся в тесте, а не наследуется из ambient-окружения** — тот же дефект, который сожрал два
   круга в `B3`. Подмени переменные окружения перед прогоном и покажи, что результат набора не меняется.
3. **Полнота против `Gate B`:** «host matrix tests (`staff`, `patient_default`, `patient_branded`,
   unknown)» — перечисли поимённо, что реально покрыто, и назови пустые ячейки.
4. **Продуктовый код действительно не тронут** — сверь diff всей ветки, а не только последнего коммита.
5. **Полный CI по ветке** только через общий замок хоста:
   `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`; приведи `rc`, длительность, `runs/ci-last.json`,
   `HEAD` до и после. Перед прогоном убедись, что `.next` клона не протух.

## Границы

- **Не чинить.** Находка без пункта в плане владельца — вопрос владельцу, не задача.
- Если найдёшь настоящую дыру в механике cookies/CSRF — это находка с доказательством и вопрос владельцу,
  а не повод править продуктовый код.
- PROD и TEST не трогать, порт `5200` не занимать, свои dev-серверы гасить.

## Канон

- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел.
- Поиск по коду — `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, не слепой grep.

## Отчёт

Вердикт `PASS, FOR LAND` или `FAIL, NOT FOR LAND` первой строкой. Числа: блокирующих, неблокирующих,
инъекций посажено / убито / не поймано. Отчёт положи в
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B6_2026-08-23.md` и закоммить.
