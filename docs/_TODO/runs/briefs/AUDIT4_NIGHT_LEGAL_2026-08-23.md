# Аудит слайса «юридические документы», круг 4

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2d —
«Реквизиты юрлица подставляются, тексты — нет.»

**Что проверяешь:** коммит `cdd0fdcbe` на ветке `wt/night-legal-20260823` — фикс по блокеру `F1` твоего же
круга 3 (аудит `749b9610d`).
**Клон:** `/home/dev/dev-projects/bcb-wt-audit4-legal-20260823`.

## Что заявил фиксер

Литеральный `status` удалён: готовность выводится из заполненности четырёх реквизитов. Частичное заполнение
сохраняет баннер и перечисляет только недостающие поля. Регрессионный тест до исправления `2 failed | 2 passed`,
после — `6 passed`. Живая проверка `terms` и `privacy` под Host `therapysto.ru` и `therapygo.ru`: заполненный
источник — баннера нет, пустой — баннер и полный список отсутствующего. Тексты не менялись, придуманных
реквизитов и временных маркеров нет.

## Что проверить

1. **Путь владельца целиком, своими руками.** Впиши реквизиты так, как это сделает он, и убедись: typecheck
   зелёный, обе страницы под обоими Host показывают готовый документ, баннера нет нигде.
2. **Частичное заполнение** — заполни два поля из четырёх и посмотри, что видит человек. Скажи, разумно ли это.
3. **Три инъекции, которые ловил и не ловил твой круг 3:** те, что краснели, обязаны краснеть и сейчас; те три,
   что не были пойманы, проверь заново — фикс мог закрыть часть из них, а мог и нет. Назови число.
4. **Придуманных реквизитов нет** — свой срез, не повтор авторского.
5. **Тексты документов не изменены** по всей истории слайса.

**Это четвёртый круг.** Если найдёшь блокер того же класса, что круг 3 (гейт слеп к состоянию «заполнено»),
скажи это прямо — тогда работа встаёт и уходит владельцу вопросом, а не пятым кругом.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT4_NIGHT_LEGAL_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
