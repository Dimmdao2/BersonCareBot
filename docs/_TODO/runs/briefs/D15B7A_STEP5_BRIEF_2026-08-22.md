# Бриф: D15b/7a Ш5 — fail-closed по виду ссылки

- **План-файл (единственный источник todo и done):**
  `docs/_TODO/runs/integrator-cleanup/D15B7A_ACTOR_SUBJECT_SPLIT_SCHEME_2026-08-22.md`, раздел 4, шаг **Ш5**.
  Твой DONE = закрытые пункты ЭТОГО брифа. Пункты плана закрывает ведущий.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`. Особенно §1 (миграция НЕ несёт
  `GRANT`/`REVOKE`/`CREATE POLICY`; разбор прав перед приземлением), §5, §6, §10a, §24.
- **Шапку плана-файла читать до работы** — там поправка 22.08 про слово «миграция» в шагах Ш3/Ш5/Ш7/Ш9.

**Источник оракула:** `docs/_TODO/runs/integrator-cleanup/D15B7A_ACTOR_SUBJECT_SPLIT_SCHEME_2026-08-22.md`,
раздел 4, шаг Ш5 — «Миграция: `resolve_variant_a_physical(uuid, text)` поднимает `42501` при несовпадении вида;»

## Состояние на входе (проверено ведущим, не пересказ)

Ш1–Ш4 приземлены и живут на DEV. Ш3 научил резолверы ПРИНИМАТЬ вид вторым аргументом, но вид НЕ
проверяется — это и есть Ш5. Ш4 сделал вывод ссылки зависящим от вида
(`sha256(uuid_send(id) || <разделитель вида>)`, у акторского разделитель пуст, поэтому 24 ранее выданные
акторские ссылки воспроизводятся дословно). Приложение уже просит нужный вид: класс `patient` берёт
`actorRef` видом `actor` и `subjectRef` видом `subject`, `staff` и `platform` — только `actor`.

## Задание

1. **Обратный резолвер `app_ext.resolve_variant_a_physical(uuid, text)` поднимает `42501` при
   несовпадении вида:** ссылка, выданная как `actor`, предъявленная как `subject`, и наоборот — отказ,
   а не молчаливое разрешение. Текст ошибки — в стиле уже существующих отказов порта, не выдумывай новый
   класс. `CREATE OR REPLACE`, OID/владелец/сигнатура/возврат/волатильность/`SECURITY DEFINER`/
   `search_path`/гейт — дословно прежние.
2. **`assert_port_context_claim` передаёт вид явно:** `'actor'` для `actor_ref`, `'subject'` для
   `subject_ref`. Именно это делает подмену полей отвергаемой базой, а не приложением.
3. **Прав это не меняет** — новых отношений и колонок нет; если по замеру окажется иначе, правь
   `declaration.ts` и пересобери артефакты, оба `--check` побайтно. **В миграции ни `GRANT`, ни `REVOKE`** (§1).
4. **Живое доказательство, `--execute` на DEV ЗАПРЕЩЁН** (DEV ведёт другая ветка) — `--preflight` с откатом,
   живой прогон в откаченной транзакции, в духе `port-context-tenant-claim.devDbProof.test.mjs`:
   установка контекста с actor-ref в поле `subject_ref` даёт `42501`; правильная пара проходит; вход тремя
   учётками владельца не ломается. Инъекция (убрать проверку вида — одна строка) красит пробу, продукт
   возвращается побайтно.
5. **Границу объёма держи:** Ш6 (перевод девяти политик на `current_actor_user_id()`) и Ш7 (снятие
   совместимой сигнатуры) в этот шаг НЕ входят. Совместимую однопараметрическую сигнатуру не трогать —
   она живёт до Ш7.
6. `pnpm test:db-privileges`, затронутые сюиты, typecheck, lint.

⛔ **Условие выкатки, назвать в отчёте:** код этой ветки без reconcile ломает вход — дескриптор порта против
каталога базы. Код и reconcile обязаны ехать ОДНОЙ выкаткой (на TEST это обеспечивает `deploy-test.sh`).

## Границы
Деплой, запись на TEST, `push`, full CI — НЕ твои. Прод не трогать.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D15B7A_STEP5_2026-08-22.md`.
