# Audit brief — `S-1`, круг 2 (закрывающий): снятие осиротевшей привилегированной функции

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `R2-1` вызывающих у снятого пути действительно НЕ было | разовое действие | СВОЙ обход (индекс `code-search` + точный `rg` по всему репозиторию), не сверка со списком автора |
| `R2-2` вход по коду на почту работает как прежде | поведение | слепой kill-set ДО чтения авторских тестов, свои значения |
| `R2-3` снятие полное и последовательное: порт, DI-привязка, адаптер, функция, перепись функций, overlay прав, гейт владения | разовое действие | взгляд на diff и на артефакты привилегий |
| `R2-4` миграция не содержит `GRANT`/`REVOKE`/`CREATE ROLE`/`ALTER DEFAULT PRIVILEGES`/`CREATE POLICY` и проходит гейты | разовое действие | свой прогон гейтов миграций |
| `R2-5` артефакты привилегий пересобираются байт-в-байт (`--all --check`) | разовое действие | свой прогон |
| `R2-6` авторский тест не «зелёный всегда» | разовое действие | сними правку — тест обязан покраснеть |
| `R2-7` «BersonCare» больше не собирается ни в одном живом пути аутентификации | разовое действие | свой исчерпывающий `rg` по коду и `deploy/postgres/*.sql` |

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `TPB-15`,
дословно: «User-visible BersonCareBot/platform BersonCare и понятие BersonCare Bot заменены на Therapysto».

Под аудитом: `0f8cf4af6` на `wt/therapysto-s1-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-s1-20260824`.
Автор: Codex `gpt-5.6-sol`/high. Круг 1 и его находки — `AUDIT_S1_2026-08-24.md`.

Заявление автора: production-вызовов у `enqueueEmailOtpDelivery` нет; удалены порт, DI-привязка, PostgreSQL-адаптер,
функция (миграция `20260824T021309_retire_orphaned_email_otp_enqueue.sql`, выполняется от `app_seam_email_otp_owner`,
только `DROP`, без операций с правами), записи в переписи функций, grant/revoke overlays и гейт владения на TEST.
Durable queue 2/2; инъекция (заменить атомарный `email_auth_start_challenge` на старый `insertEmailChallenge`)
красит оба теста; privilege-тесты 162 passed / 0 failed; webapp typecheck и scoped lint зелёные.

**Проверь особо:** удаление привилегированного объекта — это операция, где ошибка стоит дорого. Убедись сам,
что снятый объект не нужен ни одному живому пути, что артефакты привилегий согласованы, и что после удаления
не осталось висящих ссылок в списках прав или в гейтах развёртывания.

## Границы

Прод не трогать, `--execute` на DEV не запускать, в `feat` не вливать, реальным адресатам не слать.
Не трогать `app.read_integrator_clinic_delivery_credential`, миграции `20260823T030000…` / `20260823T043206…`
и `app.enqueue_outbound_message`. Finding вне owner scope — вопрос владельцу, не работа. Уровень `app`.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_S1_2026-08-24.md`: первой строкой вердикт,
строка на каждый пункт, счёт инъекций посажено/убито/не поймано.
