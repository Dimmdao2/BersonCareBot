# Бриф D19: сверить записанную архитектуру с тем, что получилось — и править ТЕКСТ, а не реальность

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D19**.
  Твой DONE = закрытые пункты ЭТОГО брифа. Галочку плана ставит ведущий.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`; §1, §5, §6, §10a, §24.

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «актуализировать правило, а не подгонять реальность под текст»

## Зачем сейчас

D11–D18 закрыты, и вчера (22–23.08) устройство доступа интегратора изменилось СИЛЬНО: у него появилась
собственная узкая роль, снято широкое арендное членство `app_tenant_service` вместе с capability, восемь
отношений под `FORCE RLS`, медицинских таблиц у роли нет. Записанная архитектура за этим не поспела.

## Задание — по списку самого плана, каждый пункт с замером

1. **Остался ли в интеграторе хоть один путь записи канона?** Перепись, а не мнение. Пустой результат
   доказывается перечислением способов поиска.
2. **Ведёт ли к базе один путь?** Проверь chokepoint по-настоящему: есть ли обходы мимо него.
3. **Сошлось ли число вечных циклов?**
4. **Выдана ли узкая роль и СОВПАДАЕТ ЛИ ОНА С ТЕМ, ЧТО АССЕРТИТ ДЕПЛОЙ.** Это самое важное: скрипт
   выкатки жёстко проверяет точный набор прав каждой роли, и вчерашние изменения могли его рассинхронить.
   **Если разойдётся — выкатка на TEST упадёт**, поэтому назови расхождение ЯВНО и первым.
5. **Не появилось ли новых прямых импортов между деревьями приложений** вместо пакета.
6. **Известная находка 20.08, не поправлена:** `apps/webapp/ARCHITECTURE.md:50` всё ещё утверждает «same
   DATABASE_URL and DB role for both services» — это уже неправда. Поправить этой же работой.

## Как закрывать

Правкой документа `apps/webapp/ARCHITECTURE.md` (и `ARCHITECTURE.md` в корне, если расхождение там) —
**или явной записью «расхождений нет» с доказательством**. Устное «всё сошлось» не принимается.
**Реальность под текст НЕ подгонять:** нашёл расхождение — правь текст; считаешь, что неправа реальность —
это ВОПРОС ведущему (§24.6), а не переделка кода.

## Границы
Кода не менять (кроме документации), миграций не заводить, `--execute` запрещён, TEST и прод не трогать,
`push` не делать. Если по пункту 4 найдёшь расхождение — СТОП по нему и вопрос ведущему, чинить сам не надо.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D19_ARCHITECTURE_RECONCILE_2026-08-23.md`.
