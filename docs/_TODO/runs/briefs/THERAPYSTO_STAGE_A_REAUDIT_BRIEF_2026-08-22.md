# Бриф аудитора: ПОВТОРНАЯ проверка этапа A после коррекции

## Классификация «тест или взгляд» (AGENTS.md §24.4)

| Пункт | Класс | Почему |
|---|---|---|
| 1 — три находки прошлого круга закрыты | **поведение** | каждая была доказана командой на живом сервере; закрытие доказывается тем же способом, а не чтением диффа |
| 2 — коррекция не сломала то, что прошлый круг признал чистым | **поведение** | регрессия проверяется прогоном, включая контракт манифестов |
| 3 — не появилось третьего места с именем | **взгляд** | разовое суждение по графу импортов |
| 4 — не разогнан ли скоуп | **взгляд** | разовое сравнение диффа с брифом коррекции |
| 5 — тихая утечка `env.ts` в клиентский бандл | **поведение** | проверяется сборкой, как в прошлом круге |

Постоянных тестов «в коде нет такой-то строки» не создавать.

Ты **независимый адверсарный аудитор**. Прошлый круг ты (или твой предшественник) закрыл вердиктом `FAIL`.
Сейчас проверяешь коррекцию. Отчёт исполнителя и зелёные тесты доказательством НЕ являются.

- **План-файл:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, этап `A`,
  требования `TPB-01`, `TPB-03`, `TPB-04`, `TPB-06`, `TPB-08`, `TPB-09`, `TPB-15`, `TPB-16`.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.
- **Прошлый вердикт:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_STAGE_A_2026-08-22.md`.
- **Отчёт коррекции:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/CORRECTION_STAGE_A_2026-08-22.md`.
- **Бриф коррекции:** `docs/_TODO/runs/briefs/THERAPYSTO_STAGE_A_FIX_BRIEF_2026-08-22.md`.
- **Что проверяешь:** клон `/home/dev/dev-projects/bcb-wt-therapysto-stage-a-20260822`, коммиты коррекции
  `f8778803c` (воркер) и `f3361a4f3` (ведущий), поверх `c3136e23e` + `d37390274`.

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-08` — «Branding влияет только на patient-facing surface; staff/admin видят Therapysto. Доказательство:
cross-surface metadata/UI tests.»

## Что проверить

1. **Три находки прошлого круга закрыты по факту.** Воспроизведи свои же команды: заголовок и брендовая
   строка `/app/doctor/login` и `/app/admin/login`; поведение `PATIENT_APP_NAME=<чужое имя>` на ВСЕХ
   пациентских экранах, включая `/app/contact-support`; отсутствие старого имени в `public/maintenance.html`
   и `public/sw.js`.
2. **Ведущий доложил остаток `F1`, найденный уже после коррекции:** staff-зоны переопределяли `title` и
   `apple-title`, но не `description`, и несли корневое пациентское «Patient web application for …».
   Исправлено коммитом `f3361a4f3`. Проверь, что закрыто, и — главное — **поищи, не остались ли ДРУГИЕ поля
   метаданных того же класса**, которые staff-зоны молча наследуют от пациентского корня (OpenGraph,
   twitter, `applicationName`, `keywords`, что угодно ещё). Это уже второй остаток одной находки.
3. **Регрессия.** Всё, что прошлый круг признал чистым, должно остаться чистым: контракт манифестов
   (`id`, `scope`, `start_url` обеих поверхностей), привязка passkey к домену, юридические страницы,
   запрещённые файлы, отсутствие секрета в клиентском бандле.
4. **Не появилось ли третьего места с именем.** Способ решения `F2` — контекст в существующем
   `PlatformProvider`. Проверь, что это расширение существующего choke point, а не новый параллельный
   store (`TPB-16`), и что staff-имя через него не переопределяется.
5. **Не разогнан ли скоуп.** Сравни дифф коррекции с брифом коррекции: всё ли изменённое соответствует
   `X1`–`X4`, не появилось ли машинерии, которой никто не просил.

## Границы

- Находка, которой нет соответствующего пункта в плане владельца, — **вопрос владельцу, а не работа**.
- Ничего не чинить самому. Только проверять и докладывать.
- PROD, TEST, деплой, БД, push — не трогать. Свой dev-сервер — только на свободном альтернативном порту,
  5200 не трогать.

## Вердикт

`PASS` или `FAIL` с перечнем находок, у каждой — путь, строка и способ воспроизведения. Отдельной секцией:
что проверил и проблем не нашёл. Числа — вместе с командой, которой получены.
