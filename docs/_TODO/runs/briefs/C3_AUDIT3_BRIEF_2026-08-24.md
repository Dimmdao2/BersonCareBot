# Audit brief — `C3`, круг 3 (закрывающий)

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2h, дословно:
«На пути по умолчанию клиника не настраивает НИЧЕГО».

Под аудитом: `6ea163195` на `wt/therapysto-c3-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-c3-20260824`.
Правку делал ведущий (Opus 5), а не воркер: круг 2 получил FAIL по единственному блокеру — восьмой продюсер
`doctorSupportMessagingService.ts:129` (legacy-ветка `sendAdminReply`) ставил безусловный `clinic_required`.
Отчёты кругов 1 и 2: `AUDIT_C3_2026-08-24.md`, `AUDIT2_C3_2026-08-24.md`.

Правка: маркер сменён на `clinic_if_configured` плюс регрессия
`apps/webapp/src/modules/messaging/doctorSupportLegacyChannelScope.test.ts` (3 теста; при возврате
`clinic_required` краснеют 2).

## Что проверить (классификация §24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `R3-1` девятого продюсера НЕТ | разовое действие | СВОЙ обход кода обоих приложений, не сверка с таблицами прошлых кругов |
| `R3-2` legacy-ветка действительно доходит до пациента у клиники без своего бота | поведение | слепой kill-set, свои значения |
| `R3-3` у клиники со своим ботом отката по-прежнему нет | поведение | инъекция возврата отката |
| `R3-4` регрессия ведущего не «зелёная всегда» | разовое действие | сними правку — тест обязан покраснеть |
| `R3-5` ничего больше не изменилось | разовое действие | взгляд на diff круга 3 |

Отдельно: `.catch(logger.error)` вокруг отправки означает, что ЛЮБОЙ отказ доставки виден врачу как
«отправлено». Скажи прямо, остались ли ещё места с таким молчанием на пациентском пути.

## Границы

Не трогать `app.read_integrator_clinic_delivery_credential` и миграции `20260823T030000…` / `20260823T043206…`;
обе роли не разрешать. `telegram_mode=long_polling` никогда. Реальным получателям не слать. В `feat` не вливать.
Уровень `app`, два приложения. Full CI не гонять. Finding вне owner scope — вопрос владельцу, не работа.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT3_C3_2026-08-24.md`: первой строкой вердикт,
строка на каждый пункт, счёт инъекций.
