# Бриф закрывающего аудита `F1`, круг 2

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `F1` —
«Расширить резолвер поверхности типизированной auth policy».

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-f1-20260823`, ветка `wt/night-f1-20260823`.
**Что проверяешь:** `42b65cecc` — фикс по твоему же блокеру круга 1
(`AUDIT_NIGHT_F1_2026-08-23.md`, три механики из шести расходились с продом).

## Тест или взгляд (§24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Матрица совпадает с живыми настройками | **тест** | обязано краснеть при расхождении |
| Утверждение в коде честно | **взгляд** | разовая проверка текста и источника |
| Поведение входа не изменилось | **тест** | главное требование `F1` |

## Что доказать

- все 18 ячеек совпадают с живыми настройками — сверь **сам, из источника**, а не по тесту автора;
- тест действительно краснеет при расхождении: подмени одну ячейку — должен упасть;
- в коде написано, откуда взяты значения, и это правда;
- **ничей вход не изменился**: `F1` по-прежнему несущая конструкция, `F2`–`F5` не заехали;
- инъекции: посажено / убито / не поймано, числами.

## Границы

- **аудит — гейт против плана владельца, не источник работы**;
- `F2`–`F5` не проверяем и не требуем.

Вердикт первой строкой. Отчёт — `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_NIGHT_F1_2026-08-23.md`.
