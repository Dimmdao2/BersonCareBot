# Аудит: схема доменов и поверхностей против живого кода

Владелец 22.08.2026: «нарисовать один раз наконец-то… реально на этой схеме уже проходить и смотреть, как будет
это соответствовать, не будет, что у нас в коде». Схема нарисована `sol`; проверяешь её ты, независимо.

## Тест или взгляд (`AGENTS.md` §24.4) — классификация проверок этого аудита

| # | Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- | --- |
| `A` | Резолвер поверхности по Host: неизвестный/неактивный Host → hard 404, platform fallback запрещён | **Тест** | Это поведение модуля на входных данных, оно обязано остаться зелёным навсегда |
| `B` | Cookie host-only, cross-domain SSO нет | **Тест** | Поведение, регрессия молчаливая и дорогая |
| `C` | Таблица «схема → есть в коде» из раздела 5 карты: 25 расхождений | **Взгляд** | Разовая сверка документа с деревом, тест тут не нужен |
| `D` | Матрица «поверхность × механика входа» существует ли вообще | **Взгляд** | Ответ да/нет по коду, не поведение |
| `E` | Единственность резолвера: параллельных getters/resolvers/stores нет | **Взгляд** | Структурная проверка diff'ом и поиском |
| `F` | Правки, внесённые `sol` в план, не противоречат решениям владельца | **Взгляд** | Сверка текста с owner-регистром |

Тесты пишешь только там, где стоит «тест», и только если такого теста ещё нет. Взгляд оформляется абзацем с
командой и путями, а не новым тестовым файлом.

- **Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
  `TPB-16` — «Реализация расширяет перечисленные choke points и не создаёт параллельных getters/resolvers/stores.
  Доказательство: dependency/architecture audit по diff.»
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.
- **Проверяемый документ:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/SURFACE_AND_DOMAIN_MAP_2026-08-22.md`
  (ветка `wt/therapysto-surface-map-20260822`, коммит `655650a5c`).
- **Клон:** `/home/dev/dev-projects/bcb-wt-surface-map-audit-20260823`.

## Что именно проверить

1. **Пересчитай 25 расхождений сам.** Совпало число — хорошо; не совпало — назови, где `sol` ошибся в обе
   стороны: и «объявлено есть, а нет», и «объявлено нет, а на самом деле есть». Второй класс важнее: он создаёт
   работу, которой не нужно.
2. **Инварианты схемы — самое ценное, что в ней есть.** Проверь каждый против кода отдельно:
   hard 404 на неизвестном Host; `organizationId` только после точного совпадения с активной клиникой и только
   как контекст, а не авторизация; cookie host-only; `bersoncare.ru` мимо резолвера. Инвариант, которого код не
   держит, — это находка, даже если схема его лишь декларирует на будущее.
3. **Правки `sol` в `IMPLEMENTATION_PLAN.md`** (коммит `655650a5c` трогал план: `platform_admin`, Yandex, OAuth,
   custom domains, сертификат apex, passkey, BersonCare). Убедись, что ни одна не переписала решение владельца.
   Решения владельца лежат в §1.2–§1.2g и §2.1. **Расхождение здесь — блокирующая находка.**
4. **Свежие решения владельца 23.08** (§1.2e, §1.2f, §1.2g) в схеме ещё не отражены: SMS выключен, alpha-имена
   не покупаем, TOTP админа не обязателен, вход специалиста переделывается после домена, реквизиты не наша
   работа, sender брендированного письма — пара «клиника и мы». Назови места схемы, которые из-за этого устарели.
5. **Passkey:** замер `sol` — на DEV ключей `0`. Перемерь своим способом.

## Границы

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без соответствующего пункта
  в плане владельца — вопрос ему, не задача.
- Продуктовый код не менять. PROD и TEST не трогать, deploy и push не делать. Порт 5200 не занимать.
- Вердикт `PASS`/`FAIL` по схеме в целом; блокирующие и неблокирующие находки разделить явно.

## Приёмка

- [ ] `V1` Число расхождений перемеряно своей командой; расхождения с `sol` названы в обе стороны.
- [ ] `V2` Каждый инвариант схемы проверен против кода отдельно, с путём и строкой.
- [ ] `V3` Правки `sol` в план сверены с owner-регистром; противоречий нет либо они названы поимённо.
- [ ] `V4` Названы места схемы, устаревшие после решений владельца 23.08.
- [ ] `V5` Passkey перемерен независимо.
- [ ] `V6` Вердикт с разделением блокирующего и неблокирующего.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_SURFACE_MAP_2026-08-23.md`.
Строку вердикта заведи в `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в том формате, что там уже принят.
