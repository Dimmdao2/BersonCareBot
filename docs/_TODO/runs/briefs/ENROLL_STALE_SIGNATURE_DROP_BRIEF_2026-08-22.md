# Бриф: снять устаревшую однопараметрическую сигнатуру двери публичной записи

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D15b/7a**
  (отказ держит сведение DEV и выкатку TEST).
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`; §1 (миграции НИКОГДА не выдают гранты), §5, §10a.

**Источник оракула:** `deploy/postgres/privileges/migrate-local.mjs` — «records ${holders.length} migration(s) as applied whose objects are not in the catalog, »

## Что сломано (замер ведущего)

`bash deploy/host/migrate-dev.sh --execute` падает:

```
absent: function app.enroll_current_patient_in_public_booking_clinic(uuid)
        (from 20260819T170216_a_public_visitor_becomes_a_client_when_identified)
```

Разбор: миграция `20260819T170216` создала дверь с ОДНИМ параметром; позже `20260819T182039` завела
двухпараметрическую (`uuid,text`) — и только она объявлена в `declaration.ts:4883`. Старую сигнатуру
**никто не снял миграцией**, её убрал reconcile как незадекларированную. Страж прав: запись о применённых
миграциях обещает объект, которого нет.

## Задание

1. **Сначала перепись:** убедись, что однопараметрическую сигнатуру не зовёт НИКТО — ни код приложений, ни
   тела функций БД, ни тесты. Пустой результат доказывается перечислением способов поиска.
2. **Новой миграцией снять устаревшую сигнатуру** (`DROP FUNCTION IF EXISTS
   app.enroll_current_patient_in_public_booking_clinic(uuid)`), чтобы снятие стало ЯВНЫМ и видимым стражу.
   Двухпараметрическую НЕ трогать. Грантов в миграции нет (§1).
3. **Проверь, нет ли других таких же расхождений:** объект создан миграцией, не объявлен в декларации и
   потому снят reconcile-ом, а снятия миграцией нет. Перечисли найденное; чинить в этом заходе только
   названный случай, остальные назвать ведущему (§24.6).
4. Затронутые тесты, typecheck, оба `--check` побайтно. **`--execute` ЗАПРЕЩЁН** — его гоняет ведущий.

## Границы
Деплой, TEST, `push`, full CI — не твои. Прод не трогать.
Отчёт: `docs/_TODO/runs/integrator-cleanup/ENROLL_STALE_SIGNATURE_DROP_2026-08-22.md`.
