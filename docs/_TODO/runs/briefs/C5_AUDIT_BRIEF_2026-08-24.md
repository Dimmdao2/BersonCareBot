# Audit brief — `C5(б)`, круг 1 (независимый, адверсарный)

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `C5-1` свой канал включается ТОЛЬКО после успешной живой отправки, не по сохранению настроек | поведение | слепой kill-set по оракулу ДО чтения авторских тестов, свои значения |
| `C5-2` «успешная» значит доставленная: подавленная/перенаправленная/`skipped` отправка не включает канал | поведение | своя инъекция на каждый из этих статусов отдельно |
| `C5-3` до проверки работает путь по умолчанию, никто не остаётся без сообщений | поведение | регрессия, прямая буква §1.2h |
| `C5-4` адресат проверки — сотрудник клиники, не пациент | разовое действие | взгляд на код отправки пробы |
| `C5-5` второго отправителя и второй очереди не заведено | разовое действие | взгляд на diff |
| `C5-6` поломка канала ПОСЛЕ включения адрес клиники не гасит | поведение или взгляд — реши сам | §1.2h; автор пишет, что адрес не гасится, а отдельного постоянного сигнала о поломке он не добавлял |
| `C5-7` числа автора воспроизводятся | разовое действие | свой прогон названных наборов |

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `C5`,
дословно: «не включается, пока не прошла успешная ЖИВАЯ отправка — не по галочке «настроил», а по факту доставки».

Под аудитом: `111a5f7e7` на `wt/therapysto-c5-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-f2-20260824`
(каталог от прежней ветки — проверь `git branch --show-current`). Автор: Codex `gpt-5.6-sol`/high, 27 минут.

Заявление автора: кредентиал сохраняется в `pending`, форма сама канал не включает; включает только живая
отправка со статусом `accepted` через существующий relay/adapter; адресат — текущий сотрудник клиники; на DEV
сохраняется redirect-шов и подавленная отправка успешной не считается; до проверки работает платформенный канал;
в существующих настройках видны ожидание, успех и ошибка. Пять инъекций красные, webapp 25 тестов, интегратор 36,
оба typecheck и scoped ESLint зелёные.

## Границы

Ничего не слать реальным адресатам, redirect-шов на DEV не снимать. `telegram_mode=long_polling` не ставить
никогда — в настройках настоящий прод-токен. Не трогать `app.read_integrator_clinic_delivery_credential`,
миграции `20260823T030000…` / `20260823T043206…` и `app.enqueue_outbound_message`. Прод не трогать,
`--execute` на DEV не запускать, в `feat` не вливать. Finding вне owner scope — вопрос владельцу, не работа.
Уровень `app`, два приложения. Full CI не гонять.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_C5_2026-08-24.md`: первой строкой вердикт
`PASS, FOR LAND` / `FAIL, NOT FOR LAND`, строка на каждый пункт, счёт инъекций посажено/убито/не поймано.
