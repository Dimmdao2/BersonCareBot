# Ночь: удалить каталог `admin/` и развести идентичность админской поверхности

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-16` — «Реализация расширяет перечисленные choke points и не создаёт параллельных getters/resolvers/stores.
Доказательство: dependency/architecture audit по diff.»

**Клон:** `/home/dev/dev-projects/bcb-wt-night-admin-20260823`, ветка `wt/night-admin-20260823`.

## Что сделать

1. **Удалить каталог `admin/` в корне репозитория.** Решение владельца 22.08.2026: «каталог admin — это
   какая-то древняя хуйня, нам это не надо». Установлено предыдущим заходом: он не входит в
   `pnpm-workspace.yaml`, не собирается в CI, не деплоится, содержит заглушку и упоминает четвёртый домен
   `admin.bersonservices.ru`.
   **Перед удалением докажи, что он мёртв:** перечисли, кто на него ссылается (workspace, CI, deploy-скрипты,
   nginx, доки), и покажи, что ничего живого не отвалится. Найдёшь живую ссылку — не удаляй, назови её в отчёте.
2. **Развести идентичность админской поверхности** (находка аудита `N5`): `app/app/admin/layout.tsx:24`
   отдаёт `staffPwaLayoutMetadata`, то есть админка носит staff-имя вкладки и **staff-PWA**. Владелец 23.08
   решил: платформенному админу приложение на телефон не нужно, «я просто зайду через браузер».
   Значит админской поверхности нужны свои metadata без PWA-части: ни `manifest`, ни `appleWebApp`.
   Имя вкладки — Therapysto (это его продукт), но приложение с неё не ставится.
3. **Довести правку `F1` в плане** (находка аудита `N6`): две строки сохранили отменённую редакцию. Найди их
   по отчёту аудита и приведи в соответствие с решением владельца. Это правка ТЕКСТА плана, не кода.

## Границы

- Не трогать `src/proxy.ts`, `src/config/surfaceRoutes.ts`, `modules/system-settings`, схему БД, юридические
  страницы — там параллельно работают другие агенты.
- Хост `admin.therapysto.ru` НЕ поднимать и маршруты под него не переносить: это отдельный пункт этапа `B`,
  и он ждёт домена.

## Приёмка

- [ ] `AD-1` Мёртвость `admin/` доказана перечислением проверок; каталог удалён либо названа живая ссылка.
- [ ] `AD-2` Админская поверхность больше не отдаёт staff-PWA; тест это фиксирует.
- [ ] `AD-3` Две строки плана приведены к решению владельца.
- [ ] `AD-4` typecheck + scoped lint + затронутые тесты зелёные; `next build` не сломан.
- [ ] `AD-5` Ветка запушена.

## Правила, общие для ночного прогона (владелец, 23.08.2026)

- **Ничего не приземлять в `feat`.** Работа живёт в своей ветке; сведение делает владелец отдельной командой.
- **Пушить свою ветку для бэкапа** после каждого содержательного коммита: `git push dimmdao <своя ветка>`.
  В `main` не пушить никогда.
- **Полный прогон — только через общий замок хоста:** `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
  Точечные проверки (один файл vitest, typecheck, scoped lint) можно напрямую. Порт 5200 не занимать.
- Коммитить только свои файлы поимённо: в дереве параллельно работают другие агенты. `git add -A` запрещён.
- PROD не трогать вообще. TEST не деплоить и не править.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.
- Владелец спит. Упёрся в его решение — не гадай: запиши вопрос в отчёт и сделай всё, что от этого решения
  не зависит.

