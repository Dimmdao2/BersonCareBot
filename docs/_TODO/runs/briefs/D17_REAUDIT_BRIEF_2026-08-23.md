# Аудит D17, круг 4: стена календарного корня (`ae2039ece`)

## Тест или взгляд — классификация по пунктам (AGENTS.md §24.4)

| # | Пункт | Природа | Как доказывать |
|---|---|---|---|
| 1 | вызов из клиники A с аргументом клиники B отказывает | повторяемое поведение | **тест** + fault injection |
| 2 | все 4 переписанных тела ничего не потеряли при смене языка | разовое действие | **взгляд** построчно «было → стало», свой diff |
| 3 | узкая роль сохранила доступ, права не откачены | повторяемое поведение | **тест** |
| 4 | гейт первым оператором, `DECLARE` после него | разовое действие | **взгляд** |

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «узкая роль интегратора не мешает доставке»

Круг 2 этой же ветки получил `FAIL` — отчёт
`docs/_TODO/runs/integrator-cleanup/D17_INTEGRATOR_LOST_EXECUTE_AUDIT_2026-08-23.md`. Прочитай его:
блокером было то, что смена языка `plpgsql` → `sql` тихо унесла сверку организации с принятым
контекстом, и клиника читала бы креды чужой. Круг 3 (`c787f1c49`) заявляет, что вернул стену.

Канон — `AGENTS.md`. Ветка `wt/d17-integrator-20260823`, клон `d17-integrator-20260823`.

## Главное

Ты проверяешь ту же поверхность, на которой прошлый круг спрятал снятие стены. Поэтому **не верь
заявлению «проверены все 4 тела»** — сделай свой построчный diff каждого тела и скажи по каждому, что
именно в нём делает организационную проверку и откуда берётся организация: из контекста или из
аргумента. Если хоть в одном она из аргумента — блокер.

Отдельно: убедись, что при возврате стены не откатились права, ради которых круг 1 и делался, —
узкая роль `app_integrator_tenant_service` должна проходить все три двери.

## Границы

Ничего не чинить. `--execute`, TEST, PROD, push — запрещены.
Вердикт первой строкой: `PASS` или `FAIL` + причина.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D17_REAUDIT_2026-08-23.md`.
