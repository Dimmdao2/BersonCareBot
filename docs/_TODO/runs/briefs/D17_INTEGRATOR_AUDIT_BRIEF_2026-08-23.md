# Аудит перед приземлением: узкая роль интегратора достаёт корни доставки (`8e49a07f6`)

## Тест или взгляд — классификация по пунктам (AGENTS.md §24.4)

| # | Пункт | Природа | Как доказывать |
|---|---|---|---|
| 1 | перепись 15 корней верна: каждый «зовёт только вебапп» действительно не зовётся интегратором | разовое действие | **взгляд**: свой независимый поиск, не повтор команды автора |
| 2 | два суженных корня не отобраны у вебаппа | разовое действие | **взгляд** по коду обоих приложений |
| 3 | узкая роль проходит, чужой принципал отказывает | повторяемое поведение | **тест** + fault injection |
| 4 | стена не расширена: медицинских отношений узкая роль не получила | повторяемое поведение | **тест** |
| 5 | цепочка материализации напоминаний проходит целиком, а не один вызов | повторяемое поведение | **тест** |

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «узкая роль интегратора не мешает доставке»

Ветка `wt/d17-integrator-20260823` в клоне `d17-integrator-20260823`, коммиты `132de6191` (салваж) и `8e49a07f6`.
Отчёт автора — `docs/_TODO/runs/integrator-cleanup/D17_INTEGRATOR_LOST_EXECUTE_2026-08-23.md`.
Канон — `AGENTS.md` (`grep -n "^## \|^### " AGENTS.md`), §1, §5, §10a.

Живая регрессия, ради которой всё делалось: пациент записывается, а подтверждение и напоминания не
создаются — `42501 permission denied for function read_integrator_google_calendar_setting` под
`bcb_test_integrator`.

## Что проверить скептически

1. **Сужение — не выдача.** У двух settings-корней автор ЗАМЕНИЛ роль, а не добавил. Найди своим
   способом (не повторяя его команду), зовёт ли эти корни вебапп хоть откуда-нибудь, включая
   динамические имена, обёртки и конфиг. Один найденный вызывающий — блокер.
2. **Классификация «узкая дверь не нужна» у остальных 13.** Возьми хотя бы четыре из них и проверь
   сам. Особое внимание: `replace_appointment_reminder_generation` и
   `record_reminder_occurrence_finalized_projection` — они на пути доставки, и «зовёт только вебапп»
   там надо доказать, а не принять.
3. **Не расширена ли стена.** `--all --check` побайтово; в миграции нет `GRANT`/`REVOKE`/`CREATE POLICY`;
   узкая роль не получила прав ни на одно медицинское отношение (проверь шире трёх, которые взял автор).
4. **Гейт первым оператором** после `BEGIN` во всех изменённых телах, без `DECLARE`-инициализаторов до него.
5. **Тест автора не ослаблен** и краснеет по своей причине; проверь, что он падает при снятии КАЖДОГО
   из добавленных прав по отдельности, а не только целиком.

## Границы

Ничего не чинить, в чужую ветку не коммитить. `--execute`, TEST, PROD, push — запрещены.
Вердикт первой строкой: `PASS` или `FAIL` + причина.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D17_INTEGRATOR_LOST_EXECUTE_AUDIT_2026-08-23.md`.
