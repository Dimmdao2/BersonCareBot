# D17, круг 3: вернуть стену арендатора, которую вырезали вместе с починкой прав

Независимый аудит дал **FAIL**. Отчёт — `docs/_TODO/runs/integrator-cleanup/D17_INTEGRATOR_LOST_EXECUTE_AUDIT_2026-08-23.md`,
прочитай целиком ПЕРЕД работой.

**Источник оракула:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — «узкая роль интегратора не мешает доставке»

Канон — `AGENTS.md` (`grep -n "^## \|^### " AGENTS.md`), §1, §5, §10a.

## Блокер

Миграция `20260823T030000_integrator_tenant_role_reaches_delivery_roots.sql` переписала тело
`app.read_integrator_clinic_delivery_credential(text,uuid)` с `plpgsql` на `sql` и вместе с этим
выбросила сверку организации с ПРИНЯТЫМ КОНТЕКСТОМ.

Было: `v_organization_id := app.current_org_id()`, явный отказ `42501`, если аргумент не совпал с
контекстом, и выборка `WHERE setting.organization_id = v_organization_id`.
Стало: выборка `WHERE setting.organization_id = p_organization_id` — **по аргументу вызывающего**.

Следствие: узкая роль, войдя с контекстом своей клиники, читает SMTP/SMSC/бот-токены ЧУЖОЙ.
Аудитор доказал это живьём на именованной DEV в откате.

## Что сделать

1. Вернуть стену: организация берётся из принятого контекста, аргумент только СВЕРЯЕТСЯ и при
   несовпадении даёт `42501`. Права при этом не откатывать — узкая роль должна сохранить доступ.
2. **Проверь ВСЕ тела, которые эта миграция переписала**, а не только названное. Смена языка
   `plpgsql` → `sql` роняет `DECLARE`-переменные, а с ними — всё, что на них держалось. По каждому
   переписанному телу приведи в отчёте построчное «было → стало» и явно скажи, что не потерялось.
3. Гейт остаётся ПЕРВЫМ оператором после `BEGIN`; `DECLARE`-инициализаторов до гейта нет.
4. Права — только через `declaration.ts` + генерация (`--all`, `--all --port-context-only`,
   `--all --check` побайтово). В миграции нет `GRANT`/`REVOKE`/`CREATE POLICY`.

## Доказательство

- Тест обязан ловить ИМЕННО этот класс: под контекстом клиники A вызов с аргументом клиники B
  отказывает `42501`, а не возвращает строку. Инъекция: верни выборку по аргументу — тест должен
  покраснеть.
- Прежние доказательства круга 2 (узкая роль проходит, без контекста `42501`, медицинских прав ноль)
  должны остаться зелёными.
- `typecheck`, `lint`, `definer-tenant-predicate.test.mjs` — зелёные.

## Границы

`--execute`, TEST, PROD, push — запрещены. Галочки плана не ставить.
Отчёт: раздел «Круг 3» в `docs/_TODO/runs/integrator-cleanup/D17_INTEGRATOR_LOST_EXECUTE_2026-08-23.md`.
