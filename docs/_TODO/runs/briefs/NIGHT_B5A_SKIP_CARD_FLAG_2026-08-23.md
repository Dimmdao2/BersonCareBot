# Бриф `B5a`: один флаг «сразу вход, визитку не показывать»

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B5a` —
«Один org-scoped флаг «сразу вход, визитку не показывать» (§1.2b). Дефолт — показывать визитку.
Флаг живёт в существующих настройках организации, отдельного store не заводится; конструктора страниц и
выбора типа страницы НЕ делается.»

Прочитай §1.2b плана и **обязательно** `docs/_TODO/CLINIC_PUBLIC_PAGE_AND_URL_FLIP_2026-08-19.md` — пункт прямо
требует согласовать эти две работы, чтобы они не разъехались. Если увидишь противоречие между ними,
**это вопрос владельцу в отчёт, а не повод выбрать сторону самому.**

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b5a-20260823`, ветка `wt/night-b5a-20260823`
(отведена от головы интеграционной ветки: `B5`, `B4a`, `F1` и сведение формы `authPolicy` уже в ней).

## Что сделать

- **один** org-scoped флаг в **существующих** настройках организации. Своей таблицы, своего store, своего
  модуля настроек не заводить;
- **дефолт — показывать визитку.** Клиника, которая ничего не трогала, ведёт себя ровно как сегодня;
- флаг включён → корень брендированного адреса ведёт сразу на вход, минуя визитку;
- решение принимается там же, где `B5` строит проекцию корня, — **вторая развилка в другом месте не заводится**
  (`TPB-16`).

## Чего НЕ делать

- конструктора страниц, выбора типа страницы, набора шаблонов — пункт это прямо запрещает;
- не трогать саму визитку и её содержимое;
- не менять способы входа: это `F`-этап;
- не ломать `B5`: маршруты обеих пациентских поверхностей, `B4a` с тремя случаями `404` и сверку происхождения
  бренда оставить как есть. Их регрессия — блокер.

## Доказательства в отчёте

- тесты: флаг выключен (и не задан вовсе) → визитка, как сегодня; флаг включён → вход; обе пациентские
  поверхности; чужая организация флагом не управляет;
- доказательство, что развилка одна: перепись мест, где решается, что показывать на корне;
- доказательство, что нового хранилища нет: где именно живёт значение;
- сверка с `CLINIC_PUBLIC_PAGE_AND_URL_FLIP_2026-08-19.md` — что совпало, что разошлось, вопросы владельцу;
- scoped lint, typecheck, числа прогонов. **Полный CI не гоняй** — он у ведущего.
