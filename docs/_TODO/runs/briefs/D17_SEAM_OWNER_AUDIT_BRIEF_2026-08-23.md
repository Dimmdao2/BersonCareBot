# Независимый аудит: права владельцев швов (D17)

Ветка `wt/d17-seam-owner-20260823`, коммит `1fdb79cfa`, клон
`/home/dev/dev-projects/bcb-wt-d17-seam-owner-20260823`. Отчёт —
`docs/_TODO/runs/integrator-cleanup/D17_SEAM_OWNER_PRIVILEGES_2026-08-23.md`.
Канон — `AGENTS.md`. Authority — `WORK_ORDER.md`, D17 (перемер 23.08 внутри пункта).

Правки писал агент, оборвавшийся в `blocked_system`; отчёт и правки закоммичены ведущим как салваж.
**Отчёт исполнителя доказательством не является.**

## Тест или взгляд

| Пункт | Что проверяем | Тест или взгляд | Почему |
|---|---|---|---|
| 1 | два добавленных `SELECT` действительно снимают живой `42501` | **тест** | это поведение корня, оно должно краснеть при отзыве права |
| 2 | перепись полна: «2 из 631» — правда | **взгляд** | разовый пересчёт своим способом |
| 3 | стена арендатора не ослаблена выданными правами | **взгляд** | разбор тел и политик |
| 4 | права выданы минимально | **взгляд** | сверка diff декларации |

## Что доказать

1. **Живой симптом.** Исходный отказ: `42501 permission denied for table user_contacts` внутри
   `SELECT app.read_integrator_delivery_target_snapshot(…)` под ролью вебаппа, из-за чего на
   `booking.created` не материализуются напоминания. Докажи на именованной DEV, что после правки
   корень отдаёт данные, а при отзыве нового права снова `42501`. Инъекция обязательна.
2. **Перепись пересчитай сам.** Автор утверждает: 43 владельца шва, 631 требование, неполны ровно 2,
   ни одна роль не получает права через членство. Проверь своим способом, а не его скриптом
   (скрипт `seam-owner-access-census.mjs` разбери отдельно: не подгоняет ли он ответ — например,
   не считает ли `partial` за `owned`).
3. **Ничего лишнего не выдано.** Новые права — только `SELECT` и только на те отношения, которые
   корни реально читают. `GRANT`/`REVOKE` в миграциях нет; артефакты пересобраны генератором
   (`--all`, `--all --port-context-only`, `--all --check` побайтово).
4. **Стена цела.** В теле корня организация по-прежнему берётся из принятого контекста, аргумент
   только сверяется. Выданный `SELECT` не должен давать чтение чужой клиники — проверь поведением,
   а не рассуждением.
5. **Отдельно назови**, почему расхождение «код читает — права нет» не поймал ни один гейт: есть ли
   такой гейт вообще, и что нужно, чтобы завтра это краснело до выкатки, а не в журнале TEST.

## Границы

Продуктовый фикс не делаешь. `--execute`, TEST, PROD, push — запрещены; выкатку и живую
перепроверку делает ведущий. Отчёт:
`docs/_TODO/runs/integrator-cleanup/D17_SEAM_OWNER_AUDIT_2026-08-23.md`.
Вердикт одной строкой: `PASS, FOR LAND` или `FAIL, NOT FOR LAND` + блокеры.
