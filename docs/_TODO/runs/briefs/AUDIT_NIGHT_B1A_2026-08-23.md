# Бриф независимого аудита `B1a`: служебные метки и границы длины адреса клиники

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B1a` —
«Зарезервировать служебные метки (`www`, `app`, `api`, `mail`, `admin` и т.п.), чтобы slug клиники не мог их
занять; отказ — типизированной ошибкой существующего slug-порта.»
Плюс решение владельца 23.08.2026: длина адреса — минимум 3 знака, **максимум 30**.

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b1a-20260823`, ветка `wt/night-b1a-20260823`.
**Что проверяешь:** круг 1 `9617172c5` (список 146 → 225 имён) и круг 2 `0d161a686` (тот же перечень и
границы длины в двери базы, максимум приложения 63 → 30).

## Тест или взгляд (§24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Отказ порта на зарезервированное имя и на длину | **тест** | это поведение модуля, оно обязано краснеть от регрессии |
| Дверь базы отбивает запись мимо приложения | **тест** (живой DEV) | поведение базы, воспроизводимо |
| Полнота перечня: код и база совпадают | **взгляд** | разовая сверка двух списков |
| Отброшенные 361 имя блоклиста | **взгляд** | разовое суждение об уместности |

## Что доказать

- **два слоя совпадают**: множество имён в `organizationSlug.ts` и в CHECK-ограничении таблицы
  `public.organization_slug_claims` идентичны. Расхождение в любую сторону — блокер;
- **дверь базы реально закрыта**: прямой INSERT мимо приложения отбивается для зарезервированного имени,
  для 2 знаков, для 31 знака, для чисто цифрового. Проверь **своими** значениями, не теми, что в отчёте автора;
- **граница 30 не сломала уже выданные адреса**: автор намерил на DEV `max = 24`. Перемерь сам;
- **регрессия ловится**: посади инъекции (сними имя из списка, сними CHECK, верни максимум 63, ослабь
  нормализацию перед проверкой) и покажи, что набор краснеет. Числа: посажено / убито / не поймано;
- **отказ типизирован** — код ошибки порта, а не брошенная строка.

## Границы

- **аудит — гейт против плана владельца, а не источник работы.** Находка, которой нет соответствующего
  пункта в плане, — вопрос владельцу, а не задача. Так и помечай в отчёте;
- список из 225 имён не пересматривать по вкусу: спорное имя — вопрос, а не правка;
- политику для уже выданных адресов вне границ не выдумывать.

Вердикт первой строкой отчёта: `PASS, FOR LAND` или `FAIL, NOT FOR LAND`. Отчёт положи в
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B1A_2026-08-23.md`.
