# Аудит слайса «юридические документы», круг 3 — после эскалации

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2d —
«Реквизиты юрлица подставляются, тексты — нет.»

**Что проверяешь:** коммит `28645a566` на ветке `wt/night-legal-20260823`.
**Клон:** `/home/dev/dev-projects/bcb-wt-audit3-legal-20260823`.

## Почему ты Opus, а не Codex

Два круга подряд закрывались находкой одного класса — бумажный гейт. По правилу владельца 28.07 это
эскалация: фиксер поднят до `sol`/high, аудитор — до Opus, обоим шире свобода. **Если и этот круг закроется
той же причиной, работа встаёт и уходит владельцу вопросом.** Поэтому смотри не только на то, что заявлено,
но и на то, каким ещё способом этот гейт можно обойти.

## Что заявил фиксер

Способ переделан: изолированный компонентный тест заменён на `LegalDocuments.route.test.tsx`, который рендерит
реальные exports страниц `terms` и `privacy`. Инъекции в каждую страницу по отдельности дали `2 failed | 2 passed`,
`exit=1`; после восстановления `8/8`. Придуманные реквизиты вычищены, значения представлены маркерами
`__from_source__`; три поиска по ИНН/ОГРН-подобным строкам дали `0`. Живой прогон на `:5217` для двух Host ×
двух страниц: заполненный источник — каждый маркер по `2` раза, пустой — статус и список отсутствующих по `2`
раза, временных маркеров `0`.

## Что проверить

1. **Своей инъекцией, третьим способом.** Круг 1 ломал wiring, круг 2 удалял его из обеих страниц. Придумай
   ЧЕТВЁРТЫЙ способ обойти гейт: подмени источник пустым объектом, верни компонент, но не отрендерь его,
   отрендерь под условием, которое в тесте всегда истинно. Гейт обязан краснеть на каждом.
2. **Придуманных реквизитов нет нигде** — ни в тестах, ни в фикстурах, ни в комментариях, ни в снапшотах.
   Поищи своим способом, не повтором авторских трёх команд.
3. **Живьём на своём порту** (не 5200, не 5217): значение из источника доезжает до обеих страниц под обоими
   Host; пустой источник даёт честный видимый статус, а не молча пустое место.
4. **Тексты документов не изменены** — `git diff --word-diff` по всей истории слайса, а не по последнему коммиту.
5. **Второго дерева legal-страниц нет.**

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT3_NIGHT_LEGAL_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
