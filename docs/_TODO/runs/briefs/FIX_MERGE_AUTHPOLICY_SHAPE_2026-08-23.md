# Бриф: свести форму `authPolicy` после слияния `F1` и `B4a`

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `F1` —
«Расширить резолвер поверхности типизированной auth policy».

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-merge-fix-authpolicy-20260823`, ветка
`wt/merge-fix-authpolicy-20260823` (отведена от головы интеграционной ветки).

## Что случилось

Полный CI интеграционной ветки: `rc=1`, `2 failed | 2125 passed`. Оба падения — `src/proxy.route.test.ts`,
набор `B4a`. `F1` сделал `authPolicy` объектом (`availableMethods` + `enabledMethods`), а ожидания `B4a`
остались строкой `'patient'`. Обе ветки по отдельности зелёные — дефект родился на слиянии.

Это **второе такое столкновение за сутки** (первое — форма `effectivePatientBrand` после `B3`+`B4`).

## Что сделать

- привести ожидания `B4a` к действующей форме `authPolicy`;
- **не ослаблять проверку ради зелёного**: если тест проверял, что поверхность несёт политику, он обязан
  проверять её и дальше — с реальными значениями, а не «любой объект». Ожидание вида
  `expect.objectContaining({})` или снятие поля — это потеря проверки, так делать нельзя;
- поискать ту же рассинхронизацию в других местах: `grep -rn "authPolicy" apps/webapp/src`;
- **отдельно, коротко в отчёт:** можно ли поймать этот класс дешевле — оба раза «зелёный по отдельности,
  красный на слиянии» ловил только полный прогон. Предложение, не работа.

## Доказательства

- `pnpm --dir apps/webapp exec vitest --run src/proxy.route.test.ts` — было `2 failed`, стало зелёным, с числами;
- перечисли, какие именно утверждения изменил и что каждое проверяет теперь;
- scoped lint, typecheck. **Полный CI не гоняй** — он у ведущего.
