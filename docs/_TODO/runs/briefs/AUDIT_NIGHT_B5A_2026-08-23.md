# Бриф независимого аудита `B5a`: флаг «сразу вход, визитку не показывать»

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B5a` —
«Один org-scoped флаг «сразу вход, визитку не показывать» (§1.2b). Дефолт — показывать визитку.»

**Канон репозитория:** перед действием выполни `grep -n "^## \|^### " AGENTS.md`, найди раздел по своей теме
и прочитай его целиком.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b5a-20260823`, ветка `wt/night-b5a-20260823`.
**Что проверяешь:** `9861f8672` («feat(webapp): add branded root login flag»), автор — Codex `gpt-5.6-terra`/high.

## Тест или взгляд (§24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Три состояния флага на обеих поверхностях | **тест** | поведение модуля |
| Изоляция: флаг одной клиники не влияет на другую | **тест** | это стена арендатора |
| Своего хранилища нет, развилка одна | **взгляд** | разовая перепись |

## Что доказать

- **дефолт безопасен**: клиника, которая ничего не трогала (значение не задано вовсе, не только `false`),
  ведёт себя ровно как до правки — визитка. Проверь именно «не задано», это отдельный случай от «выключено»;
- **изоляция**: включённый флаг у клиники A не меняет корень клиники B. Проверь своими организациями;
- **развилка одна**: другого места, решающего, что показывать на корне, не появилось (`TPB-16`);
- **своего хранилища нет**: значение живёт в существующих настройках, новой таблицы и нового модуля нет;
- **`B5` и `B4a` целы**: маршруты обеих поверхностей, три случая `404`, сверка происхождения бренда;
- инъекции: посажено / убито / не поймано, числами.

## Границы

- **аудит — гейт против плана владельца, не источник работы**;
- конструктор страниц и выбор типа страницы пункт прямо запрещает — их отсутствие не находка;
- боевой шов арендатора не подключён: известно, в объём не входит.

Вердикт первой строкой. Отчёт — `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B5A_2026-08-23.md`.
