# Ночь B1/B1a/B2: организация по метке поддомена, служебные метки, уникальность своего домена

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B1` —
«Резолвить организацию по метке поддомена нашего patient-домена через существующий slug-резолвер и»
approved DB seam.

**Клон:** `/home/dev/dev-projects/bcb-wt-night-b1-20260823`, ветка `wt/night-b1-20260823`.

## Что сделать

1. **`B1`** — резолв организации по метке поддомена `<клиника>.therapygo.ru` через **существующий**
   slug-резолвер и approved DB seam. Не из route и не из proxy напрямую. Неизвестная метка — hard 404.
   Резолв обязан быть функцией, которую параллельный агент вызовет из `RequestSurfaceResolver`; сам резолвер
   поверхности НЕ пиши — это чужой слайс.
2. **`B1a`** — зарезервировать служебные метки (`www`, `app`, `api`, `mail`, `admin` и подобные), чтобы slug
   клиники не смог их занять. **Проверка стоит на записи slug, а не только на чтении** — это прямое требование
   пункта, и тест обязан доказать именно запись.
3. **`B2`** — защита от дубля `org_custom_domain_hostname` **на записи**: узкий partial unique expression index.
   Новой таблицы не заводить. Два арендатора не должны суметь объявить один и тот же хост.

## Границы

- Не трогать `src/proxy.ts`, `src/config/surfaceRoutes.ts`, `src/config/productSurface*` — там параллельно
  работает агент `B3`.
- Не трогать юридические страницы и каталог `admin/`.
- Миграция: только DEV. Dry-run описать, на TEST не применять.
- Права ролей БД жёстко ассертятся деплоем — если миграция задевает гранты, назови это в отчёте отдельно.

## Приёмка

- [ ] `B1-1` Резолв идёт через существующий slug-резолвер; `git grep` доказывает, что второго пути нет.
- [ ] `B1-2` Тест: неизвестная метка → 404, без отката на платформу.
- [ ] `B1-3` Тест на ЗАПИСЬ: служебная метка не принимается как slug клиники.
- [ ] `B1-4` Тест на ЗАПИСЬ: второй арендатор не может объявить занятый `org_custom_domain_hostname`.
- [ ] `B1-5` Миграция применена на DEV, откат описан; typecheck + scoped lint + затронутые тесты зелёные.
- [ ] `B1-6` Ветка запушена.

## Правила, общие для ночного прогона (владелец, 23.08.2026)

- **Ничего не приземлять в `feat`.** Работа живёт в своей ветке; сведение делает владелец отдельной командой.
- **Пушить свою ветку для бэкапа** после каждого содержательного коммита: `git push dimmdao <своя ветка>`.
  В `main` не пушить никогда.
- **Полный прогон — только через общий замок хоста:** `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
  Точечные проверки (один файл vitest, typecheck, scoped lint) можно напрямую. Порт 5200 не занимать.
- Коммитить только свои файлы поимённо: в дереве параллельно работают другие агенты. `git add -A` запрещён.
- PROD не трогать вообще. TEST не деплоить и не править.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.
- Владелец спит. Упёрся в его решение — не гадай: запиши вопрос в отчёт и сделай всё, что от этого решения
  не зависит.

