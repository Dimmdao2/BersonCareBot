# Независимый аудит: снятие legacy-sweep напоминаний о задачах специалиста (D30 Ш3, пункт 3)

Ветка `wt/d30-sweep-removal-20260823`, коммит `3f4378d62`, клон
`/home/dev/dev-projects/bcb-wt-d30-sweep-removal-20260823`. Отчёт исполнителя —
`docs/_TODO/runs/integrator-cleanup/D30_SWEEP_REMOVAL_2026-08-23.md`. Канон — `AGENTS.md`
(`grep -n "^## \|^### " AGENTS.md`). Authority — `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт D30.

**Отчёт исполнителя доказательством не является.** Проверяй против реальности сам.

## Тест или взгляд

| Пункт | Что проверяем | Тест или взгляд | Почему |
|---|---|---|---|
| 1 | напоминание материализуется без снятого тика | **тест** | это поведение модуля, оно должно краснеть завтра, а не только сегодня |
| 2 | мёртвых упоминаний снятого роута не осталось | **взгляд** | разовая перепись по дереву, тесту тут места нет |
| 3 | декларация прав пересобрана генератором, а не дописана рукой | **взгляд** | сверка артефактов и их происхождения |
| 4 | write-time producer и очередь не тронуты | **взгляд** | это про diff: что именно изменено |
| 5 | host-cron | **взгляд** | разовая проверка `cronport list` |

## Что доказать

1. **Главное: снятие ничего не потеряло.** Докажи своим прогоном, что после удаления тика
   `create`/`update` задачи специалиста по-прежнему кладут строку в `public.outgoing_delivery_queue`,
   а `complete`/`delete` её терминализуют. Инъекция обязательна: сломай write-time producer и покажи,
   что набор краснеет — иначе покрытие мнимое.
2. **Не удалено лишнее.** Проверь, что снятые константы (`OPERATOR_SPECIALIST_TASK_REMINDERS_TICK_JOB_KEY`,
   `OPERATOR_SPECIALIST_TASKS_JOB_FAMILY` и прочее) не использовались где-то ещё — включая экраны
   операторского здоровья: не появилось ли пустое место там, где раньше показывалась джоба.
3. **Права.** В коммите нет `GRANT`/`REVOKE`/`CREATE ROLE`/`CREATE POLICY` в миграциях; изменения
   в `deploy/postgres` сделаны генерацией и `--all --check` побайтово чист.
4. **Документы не врут.** После снятия в `api.md` и `deploy/HOST_DEPLOY_README.md` не осталось
   инструкции заводить cron на несуществующий путь.

## Границы

Продуктовый фикс аудитор не делает. Временные поломки откатывай. `--execute`, PROD, push — запрещены.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D30_SWEEP_REMOVAL_AUDIT_2026-08-23.md`. Вердикт одной строкой:
`PASS, FOR LAND` или `FAIL, NOT FOR LAND` + список блокеров.
