# Повторный аудит: SMTP клиники, круг 2 (`d18a6970f`)

## Тест или взгляд — классификация по пунктам (AGENTS.md §24.4)

| # | Пункт | Природа | Как доказывать |
|---|---|---|---|
| 1 | оба точных каталога способностей пересобраны и полны | разовое действие | **взгляд** + свой прогон генератора |
| 2 | новая дверь под врачом ПРОХОДИТ, а не отказывает | повторяемое поведение | **тест** на живой DEV |
| 3 | отказ двери больше не гасит весь блок каналов доставки | повторяемое поведение | **тест** |
| 4 | логика круга 1, которую ты уже принял, не поехала | разовое действие | **взгляд** на diff круга 2 |

**Источник оракула:** `docs/_TODO/OWNER_WALKTHROUGHS/2026-08-23_TEST_FULL_WALK.md` — «Сохранить SMTP»

Круг 1 получил от тебя `FAIL` — отчёт
`docs/_TODO/runs/integrator-cleanup/CLINIC_SMTP_INTEGRATION_DISABLED_AUDIT_2026-08-23.md`. Блокером был
непересобранный каталог `port-context-capabilities.*.sql`, из-за которого `require_accepted_context`
отказал бы на каждом вызове. Круг 2 заявляет, что каталоги пересобраны и что страница настроек больше
не зависит от новой двери. Канон — `AGENTS.md`.

## Что проверить

Проверяй ТОЛЬКО дельту круга 2 и её последствия — логику круга 1 ты уже разобрал и принял, заново её
разбирать не нужно. Главное: убедись своим прогоном, что каталог теперь полон, и что автор не «дописал
строку руками» вместо генерации.

## Границы

Ничего не чинить. `--execute`, TEST, PROD, push — запрещены.
Вердикт первой строкой: `PASS` или `FAIL` + причина.
Отчёт: `docs/_TODO/runs/integrator-cleanup/CLINIC_SMTP_REAUDIT_2026-08-23.md`.
