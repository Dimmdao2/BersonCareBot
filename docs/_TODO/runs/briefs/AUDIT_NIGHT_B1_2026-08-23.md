# Аудит слайса B1/B1a/B2: аренда по хосту

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B1` —
«Резолвить организацию по метке поддомена нашего patient-домена через существующий slug-резолвер и»
approved DB seam.

**Что проверяешь:** коммит `021cdea1e` на ветке `wt/night-b1-20260823`, автор — Codex `gpt-5.6-terra`/high
по брифу `docs/_TODO/runs/briefs/NIGHT_B1_TENANT_HOST_BRIEF_2026-08-23.md`.
**Клон:** `/home/dev/dev-projects/bcb-wt-audit-b1-20260823`.

## Что проверить под лупой

Автор заявил: adapter `resolvePatientSubdomainOrganization` поверх существующего
`ClinicDirectoryService.resolveOrganizationIdBySlug`; unknown → 404; служебные метки заблокированы «до
write-port и DB constraint»; partial unique expression index на непустой `org_custom_domain_hostname`;
гранты миграцией не менялись; 18/18 unit, 2/2 живых доказательства на DEV.

1. **Проверка на ЗАПИСЬ, а не на чтении** — прямое требование `B1a` и `B2`. Докажи инъекцией, что попытка
   записать служебную метку и попытка записать занятый хост отбиваются **на уровне БД**, а не только в коде
   приложения. Обойди приложение и попробуй вставить строку напрямую.
2. **Второй путь к резолву.** Автор ссылается на `git grep`. Перепроверь своим способом: не появился ли
   параллельный резолвер организации мимо `app.resolve_public_organization_by_slug` (`TPB-16`).
3. **Неизвестная метка → hard 404 без отката на платформу.** Проверь, что отката действительно нет.
4. **Гранты.** Автор утверждает, что миграция их не меняла. Права ролей жёстко ассертятся деплоем — проверь
   утверждение сам, тихая правка грантов ломает выкатку.
5. **Полнота списка служебных меток**: `www`, `app`, `api`, `mail`, `admin` и подобные. Назови, чего не хватает.

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_B1_2026-08-23.md`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Заявленное поведение модуля на входных данных | **Тест** | Обязано остаться зелёным навсегда; регрессия здесь молчаливая |
| Единственность источника, отсутствие второго пути | **Взгляд** | Структурная проверка `git grep` и diff'ом, тест не нужен |
| Соответствие сделанного пунктам плана владельца | **Взгляд** | Сверка текста с owner-регистром |

Тест пишешь только там, где стоит «тест», и только если его ещё нет.

## Границы аудита

- **Не чинить.** Аудит — гейт против плана владельца, а не источник работы. Находка без пункта в плане
  владельца — вопрос владельцу, не задача и не новый скоуп.
- Продуктовый код не менять; допустимы только тесты и временные инъекции, откатываемые до конца прогона.
- Ничего не приземлять в `feat`. PROD и TEST не трогать. Порт 5200 не занимать.
- Полный прогон — только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
- Вердикт `PASS`/`FAIL`, блокирующее и неблокирующее разделить явно. Строку вердикта завести в
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в принятом там формате, с числом непойманного и убитых.
- **Инъекции обязательны:** сломай проверяемое поведение и докажи, что тесты краснеют. Гейт, который не ловит
  собственную поломку, — находка.
