# TEST: почему деплой упал и оставил окружение мёртвым на 9 часов — root cause

**Роль:** worker. **Канон:** `AGENTS.md` — сначала карта заголовков
(`grep -n "^## \|^### " AGENTS.md`), затем §1 (онбординг, server conventions), §1b (безопасность
dev-среды, границы TEST/PROD), §6 (PostgreSQL/DATABASE_URL), §7–§9 (коммит/CI/деплой),
§10/§10a (тест проверяет поведение), §24 (оркестрация).
Плюс: `docs/ARCHITECTURE/SERVER CONVENTIONS.md`, `deploy/HOST_DEPLOY_README.md`.
Поиск по коду — сначала `node /home/dev/brain/tools/code-search.mjs "<q>" --repo bcb`, потом grep.

**Authority:** прямое распоряжение владельца 22.08: «разберись, пожалуйста, почему не работает тест.
Никак не запускается сайт… сервисы не стартуют». Связанный план, который этим разблокируется —
`docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, чекбокс **D15b/6** (живой TEST-гейт
логина/привязки/доставки; ему нужен работающий деплой на TEST).

## Факты, уже измеренные ведущим (не перепроверяй заново, отталкивайся от них)

- Сейчас все четыре юнита `bersoncarebot-*-test.service` **active running**, поднялись 21.08 22:37;
  `http://127.0.0.1:6300/api/health` → `{"ok":true,"db":"up"}`, integrator `:3300/health` → то же.
- **Была реальная авария:** юниты остановлены 21.08 **09:04:44** и подняты только **18:17:03** — TEST
  лежал больше девяти часов. Остановил их сам `deploy-test.sh`.
- Упавший прогон: `/var/log/bersoncarebot/deploy-test/deploy-test.20260821T091336Z.MMF5DX.log`.
  Последняя строка: `FATAL: сверка прав reconcile-access: отказал (код 1)`, следом
  `TEST writers remain stopped after failed migration/deploy; inspect the transcript before recovery.`
- В том же транскрипте раньше, строки ~1623–1660: `Error: psql failed (3)` и
  `ERROR: column "email" of relation "platform_users" does not exist`.
- Замерено ведущим: колонки `platform_users.email` **нет ни в `bersoncarebot_test`, ни в
  `bcb_webapp_dev`** (`information_schema.columns`, обе базы вернули пусто).
- Последний успешный прогон: `deploy-test.20260821T192843Z.CZkYt7.log` →
  `deploy-test: PASS branch=feat/doctor-ui-rebuild head=92cf34ffa4bf`.
- Ещё два прогона умерли на входном гейте, не тронув TEST: `FATAL: staged source changes must be
  committed before TEST deploy` и `FATAL: local branch does not exist: --help`.

## Что требуется (ровно это, не больше)

1. **Назови точную причину падения 09:13** — какой шаг, какая проверка, какой объект. Свяжи
   `ERROR: column "email" … does not exist` с `reconcile-access`: это одна причина или две разные?
   Откуда вообще берётся ссылка на `platform_users.email` — декларация прав, миграция, генератор?
   Ответ обязан опираться на строки транскрипта и на реальное состояние каталога, а не на догадку.
2. **Ответь на главный вопрос: пройдёт ли деплой текущей `feat` на TEST сейчас?** Проверяй только
   безопасными средствами: собственные preflight/dry-режимы деплоя и read-only интроспекция каталога.
   ⛔ **Сам деплой на TEST НЕ ЗАПУСКАТЬ** — владелец прямо сейчас пытается пользоваться TEST, а
   упавший деплой снова его положит. Если для ответа нужен именно живой деплой — это блокер, так и
   напиши, не запускай.
3. **Если причина в репозитории** (декларация/миграция/генератор ссылается на несуществующую колонку,
   рассинхрон DEV↔TEST) — почини её в своей ветке, минимально и по канону: миграции
   timestamp-forward, права только через декларацию/генератор (§«Миграция не выдаёт и не отзывает
   права»). Если причина в состоянии хоста/базы — не чини руками, опиши точный шаг восстановления.
4. **Отдельный вопрос, ответ обязателен:** упавший деплой оставляет TEST лежать и НИКОМУ об этом не
   сообщает — владелец узнал через сутки. Найди, есть ли уже в репозитории готовый механизм алерта
   оператору (он есть, `operator_alert` — не изобретай новый), и назови минимальную точку, куда
   встроить сообщение о «деплой упал, TEST остановлен». **Само встраивание в этом ходе НЕ делай** —
   сначала ведущий утвердит место. Найди и назови, не строй.

## Запрещено

- Запускать `deploy-test.sh`, любые миграции с `--execute`/apply, `migrate-dev.sh --execute`.
- Трогать PROD (`135.106.162.170`) — он вне этой машины и вне работы.
- Останавливать/перезапускать любые `bersoncarebot-*-test.service` — TEST сейчас живой, владелец в нём работает.
- `git add -A`, push, полный CI, фикстуры, disposable-базы, новые пользователи/клиники.

## Формат сдачи

- Отчёт файлом в `docs/_TODO/runs/integrator-cleanup/TEST_DEPLOY_FAILURE_ROOT_CAUSE_2026-08-22.md`:
  причина, доказательства (команда + её реальный вывод), ответ на п.2 да/нет с доказательством,
  что починено в ветке, что осталось. Обязательная секция `НЕ СДЕЛАНО:`.
- Коммить **до конца хода** — незакоммиченное пропадёт. Не заканчивай ход в ожидании фонового
  процесса: долгие команды гони на переднем плане либо отсоединённо через `setsid` и назови лог.
