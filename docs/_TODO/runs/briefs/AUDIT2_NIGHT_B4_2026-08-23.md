# Аудит `B4`, круг 2 — закрывающий: бренд вернулся пациенту?

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B4` —
«Расширить существующий org branding service: optional patient app name и один accent token; anonymous
branded projection отдаёт только published/entitled safe fields. Public card/contacts читаются через
существующий clinic-public-card port.»

**Что проверяешь:** коммит `fabc7bcbe` на ветке `wt/night-b4-20260823` — фикс по единственному блокеру твоего круга 1 (`B-1`, регрессия бренда) плюс закрытие четырёх твоих непойманных инъекций.
(продолжение, audit-only) на ветке `wt/night-b4-20260823`.
**Клон:** `/home/dev/dev-projects/bcb-wt-night-b4-20260823`.
**Авторы:** Codex `gpt-5.6-sol`/high (оборван портом на 32-й минуте, коммит сделал ведущий) и
`gpt-5.6-terra`/high (продолжение). Ты не слабее обоих.

## Что заявлено

Два необязательных поля в существующем `org_brand_revisions`; `AnonymousPatientBrand` отдаёт только
`effectiveDisplayName`, `patientAppName`, `accentToken` и опционально `logoUrl`, а для неоплаченного и
неопубликованного — **`null`**, не пустой объект. Второго резолва бренда нет. Инъекций 2, убито 2.
Scoped `60/60`, migration/privilege `53/53`, полный CI `exit 0`.

**Чего авторы НЕ сделали и сказали честно:** живых DEV-проб нет — `migrate-dev.sh --preflight` падает в
клоне (`FATAL: DEV API env path guard failed`, нет `.env`); **на DEV миграция ещё не применена**, двух
колонок в базе нет, ledger count тега `0`.

## Тест или взгляд (`AGENTS.md` §24.4)

| Что проверяем | Тест или взгляд | Почему |
| --- | --- | --- |
| Утечка неоплаченного/неопубликованного анонимному читателю | **Тест** | Платный признак и приватность арендатора; регрессия молчаливая |
| Ограничения миграции (формат акцента, непустое имя) | **Взгляд** | Разовая проверка на живой базе, постоянный тест не нужен |
| Единственность резолва бренда (`TPB-16`) | **Взгляд** | Структурная проверка `git grep` и diff'ом |
| Соответствие пункту `B4` плана владельца | **Взгляд** | Сверка с owner-регистром: два поля, один токен, без темы и конструктора |

## Что проверить

0. **Блокер `B-1` — своими значениями.** Автор заявляет: при `patient_default` было «Точка Здоровья»,
   стало «Дмитрий Берсон»; при `patient_branded` имя с Host побеждает. Перемерь сам, взяв ДРУГУЮ
   организацию и другие значения, и проверь третий случай, которого у автора нет: организация вообще без
   опубликованной ревизии. Плюс убедись, что новый тест `layout.branding.test.ts` краснеет от возврата
   регрессии, а не просто существует.

1. **Главное — утечка.** Посади СВОИ инъекции (минимум четыре, из них две новых класса относительно
   авторских): не только снятие проверок entitlement и published, но и попытку протащить неопубликованную
   ревизию через кэш/фолбэк, и попытку отдать поле-маркер вместо `null`. Числа красных до и после.
2. **Живая база — то, чего у авторов нет.** Примени миграцию на DEV (rollback-only, как это делали
   аудиторы `C4` в эту же ночь: скрипт замера рядом с их отчётами) и проверь ограничения ПОВЕДЕНИЕМ:
   акцент вне `^#[0-9a-f]{6}$` и пустое имя обязаны отбиваться базой, а не только зодом. Если применить
   миграцию невозможно — скажи прямо, чем именно заблокирован, и не выдавай чтение файла за проверку.
3. **Организация без брендинга** — дефолтное имя, дефолтный акцент, пустого экрана нет.
4. **Скоуп не разъехался:** ровно два поля и ОДИН токен; ни темы, ни конструктора, ни второго store.
   Проверь diff всей ветки, а не последнего коммита.
5. **`TPB-16`:** второго места, где резолвится бренд, нет.
6. **Полный CI по ветке** только через общий замок хоста:
   `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`; приведи `rc`, длительность, `runs/ci-last.json`,
   `HEAD` до и после. **Отдельно сверь, что `runs/ci-last.json` называет именно проверяемый коммит** —
   в слайсе `B6` этой ночи артефакт называл незакоммиченное дерево.

## Границы

- **Не чинить.** Находка без пункта в плане владельца — вопрос владельцу, не задача.
- `B5`, `B5a`, `B7`, `B8` — не брать.
- PROD и TEST не трогать. Живая DEV — только rollback-only. Порт `5200` не занимать.

## Канон

- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`, читай свой раздел.
- Поиск по коду — `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, не слепой grep.

## Отчёт

Вердикт `PASS, FOR LAND` или `FAIL, NOT FOR LAND` первой строкой. Числа: блокирующих, неблокирующих,
инъекций посажено / убито / не поймано. Таблица «оплачено / не оплачено / не опубликовано → что видит
аноним» своими значениями. Отчёт положи в
`docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_NIGHT_B4_2026-08-23.md` и закоммить.
