# Аудит перед приземлением: админские экраны перестали стучаться в чужую дверь (`08f0fc3c7`)

## Тест или взгляд — классификация по пунктам (AGENTS.md §24.4)

| # | Пункт | Природа | Как доказывать |
|---|---|---|---|
| 1 | граница «платформенное против арендного» проведена, а не проверка ослаблена | разовое действие | **взгляд** на diff гвардов |
| 2 | арендные шаблоны по-прежнему защищены механикой `branding` | повторяемое поведение | **тест** + fault injection |
| 3 | админ не получил доступа к пациентским отношениям | разовое действие | **взгляд**: гранты и гейты |
| 4 | отказ показывается человеку как объяснённый отказ, а не `500` | повторяемое поведение | **тест** |

**Источник оракула:** `docs/_TODO/OWNER_WALKTHROUGHS/2026-08-23_TEST_FULL_WALK.md` — «Сохранить оформление»

Ветка `wt/admin-doors-20260823`. Бриф автора — `runs/briefs/ADMIN_WRONG_DOOR_500S_BRIEF_2026-08-23.md`.
Решение владельца о границах глобального админа — `Р-АДМИН` в
`docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` §2.3. Канон — `AGENTS.md`.

## Что проверить скептически

1. **Самый вероятный способ «починить» здесь — ослабить гвард.** Автор трогал `requireRole.ts` и
   `requireEntitlement.ts` — файлы, через которые проходит доступ ВСЕГО приложения. Проверь своим
   разбором, что ни один посторонний путь не стал доступнее: возьми несколько несвязанных маршрутов и
   покажи, что их поведение не изменилось.
2. **Арендная защита не должна была пропасть.** Клиника без механики `branding` по-прежнему не должна
   править брендированные шаблоны — докажи прогоном, а не чтением.
3. **Пациентские данные админу — никогда** (`Р-АДМИН`). Если админский web-push теперь ходит своей
   дверью, проверь, что она не читает пациентских отношений.
4. `500` не должен остаться ни на одном из двух путей.

## Границы

Ничего не чинить, в чужую ветку не коммитить. `--execute`, TEST, PROD, push — запрещены.
Вердикт первой строкой: `PASS` или `FAIL` + причина.
Отчёт: `docs/_TODO/runs/integrator-cleanup/ADMIN_WRONG_DOOR_500S_AUDIT_2026-08-23.md`.
