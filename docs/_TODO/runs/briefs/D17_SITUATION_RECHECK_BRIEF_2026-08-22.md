# Бриф-перепроверка: посмотри на всю ситуацию D17 свежими глазами и скажи, где мы неправы

**Это НЕЗАВИСИМАЯ АДВЕРСАРНАЯ ПЕРЕПРОВЕРКА по прямому распоряжению владельца 22.08.**
Кода не писать, ничего не чинить. Результат — отчёт. Твоя работа — искать, где ведущий и исполнители
ошиблись, а не подтверждать их.

- **План-файл:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункты **D17** и **D15b**.
- **Канон — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`; §1, §5, §6, §10a, §24.

**Источник оракула:** `docs/_TODO/runs/integrator-cleanup/D17_RELATION_READERS_2026-08-22.md` — «принятый контекст один на транзакцию»

## Что происходило (кратко, проверяй — не верь)

Цель D17: у интегратора (кода, торчащего наружу и принимающего вебхуки) не должно остаться широкого
арендного членства `app_tenant_service`, дающего реляционный доступ ко всему под организационным
принципалом. Читатели переводятся на именные `SECURITY DEFINER`-корни, у каждого в теле выписан предикат
стены арендатора. За 22.08 приземлено: собственная роль интегратора, десять дверей, семь реляционных
читателей PII на трёх корнях, вторая дверь опознания получателя, гейт «definer-корень обязан нести предикат
организации» и шесть корней, у которых замок стоял в вызывающем.

**Членство `app_tenant_service` НЕ снято.** Его держат последние места, три из которых — структурный барьер:
чтение зовётся внутри уже открытой реляционной транзакции, куда именной корень войти не может.

Отчёты этих работ лежат в `docs/_TODO/runs/integrator-cleanup/` (файлы за 2026-08-22), вердикты — в
`docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` (последние строки).

## Вопросы, на которые нужен ответ

1. **Направление верное?** `SECURITY DEFINER` обходит RLS, то есть стена переехала из базы в тела функций.
   Это выигрыш или размен одной опасности на другую? Скажи прямо, если считаешь, что мы сделали хуже.
2. **Не наплодили ли мы машинерии.** Посчитай, сколько новых функций, гейтов, маркеров и тестов появилось
   22.08 и сколько из них реально предотвращают вред, а сколько обслуживают сами себя. Владелец отдельно
   ненавидит машинерию ради машинерии.
3. **Есть ли дыры В ТОМ, ЧТО УЖЕ ПРИЗЕМЛЕНО.** Ищи скептически: корень без предиката, предикат не на том
   отношении, дверь, доступная не той роли, `crossesTenantWall`-пометка без причины, тест, зеленеющий по
   пустоте. Каждую находку доказывай живой пробой на DEV в откаченной транзакции.
4. **Барьер транзакции — действительно ли он структурный?** Или его выдумали, и есть способ проще, который
   никто не заметил. Проверь по коду.
5. **Что осталось до снятия членства** — поимённо, с оценкой объёма, и **что случится, если членство просто
   снять сегодня**: какие маршруты упадут и на чём именно. Проверь пробой, а не рассуждением.
6. **Самое важное: что мы делаем зря.** Если какая-то из 22.08-работ была не нужна или решала выдуманную
   проблему — назови её. Это ценнее, чем список того, что хорошо.

## Правила доказательства
Число без команды — не число. «Нет» без перечня мест, где искал, — не «нет». Отчёт исполнителя и зелёные
тесты сами по себе доказательством не являются.

## Границы
Не писать код, не заводить миграций, `--execute` запрещён, TEST и прод не трогать, `push` не делать.
Отчёт: `docs/_TODO/runs/integrator-cleanup/D17_SITUATION_RECHECK_2026-08-22.md`.
