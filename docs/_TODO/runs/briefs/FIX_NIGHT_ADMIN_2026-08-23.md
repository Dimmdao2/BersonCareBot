# Фикс слайса «админская поверхность»: бумажный тест и невынутый каталог

**Источник оракула:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, требование
`TPB-16` — «Реализация расширяет перечисленные choke points и не создаёт параллельных getters/resolvers/stores.
Доказательство: dependency/architecture audit по diff.»

**Клон:** `/home/dev/dev-projects/bcb-wt-night-admin-20260823`, ветка `wt/night-admin-20260823`.

## Две блокирующие находки независимого аудита

Отчёт: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_NIGHT_ADMIN_2026-08-23.md`, коммит `dd4bc4bab`
на `wt/audit-admin-20260823` (Codex `gpt-5.6-sol`/high).

### `A1` Тест бумажный — это важнее самой правки

Аудитор вернул `staffPwaLayoutMetadata` на место, и
`pnpm --dir apps/webapp exec vitest run src/app/app/admin/layout.unit.test.ts` **остался зелёным**:
`1 passed`. Kill-set: убито `0`, не поймано `1`.

Значит гейт не ловит собственную поломку и завтра её пропустит молча. **Сделай тест настоящим:** он обязан
краснеть, когда админская поверхность снова начинает отдавать `manifest` и `appleWebApp`. Проверь это САМ той
же инъекцией — верни `staffPwaLayoutMetadata`, убедись, что тест красный, откати.

### `A2` Каталог `admin/` вынуть по-настоящему

Владелец 22.08 распорядился каталог удалить. Аудитор нашёл, чем он держится: `build:admin` в `package.json`,
записи в ignore/ESLint, девять файлов самого каталога, два runbook с правилом `/admin/ → :8080`, и stale
nginx-конфиг на `151.241.228.122`.

**Важное измерение аудитора: на DEV правило лежит только в `sites-available`, enabled-ссылки нет и слушателя
на `:8080` нет.** То есть в вебе оно ничего не обслуживает.

Сними всё, что держит каталог в РЕПОЗИТОРИИ, и удали его: скрипт сборки, записи ignore/ESLint, упоминания в
runbook (правь текст доки, а не выдумывай новую). **Конфиг nginx на живом хосте не трогай** — это операторское
действие: опиши в отчёте, что оператору надо снять, и на каком хосте. **На PROD правило не проверялось —
так и напиши, не утверждай за него.**

## Границы

- Не трогать `src/proxy.ts`, `src/config/surfaceRoutes.ts`, `modules/system-settings`, юридические страницы,
  доставку писем — там параллельно работают другие агенты.
- Хост `admin.therapysto.ru` не поднимать: он ждёт домена.
- Ничего не приземлять в `feat`. Пушить `git push dimmdao wt/night-admin-20260823`, в `main` никогда.
- Полный прогон только через `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`. Порт 5200 не занимать.
- Коммитить только свои файлы поимённо. PROD и TEST не трогать. Живой nginx не трогать.
- **Канон правил репозитория — `AGENTS.md`.** `grep -n "^## \|^### " AGENTS.md`.

## Приёмка

- [ ] `FA-1` Инъекция `staffPwaLayoutMetadata` краснит тест — показать вывод до и после.
- [ ] `FA-2` Каталог `admin/` удалён; перечислено поимённо всё снятое, что его держало.
- [ ] `FA-3` Сборка зелёная через общий замок хоста: `next build` доходит до `BUILD_ID` и standalone.
- [ ] `FA-4` Операторский остаток (nginx на живом хосте, статус PROD) назван отдельным абзацем отчёта.
- [ ] `FA-5` typecheck + scoped lint + затронутые тесты зелёные; ветка запушена.
