# Audit brief — остаток `TPB-15`, круг 2 (закрывающий)

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `R2-1` литерала «BersonCare» в живых путях доставки не осталось | разовое действие | СВОЙ исчерпывающий `rg` по обоим приложениям, не сверка с таблицей автора |
| `R2-2` fail-closed реальный: без имени вызов ОТКАЗЫВАЕТ, а не молча шлёт | поведение | слепой kill-set ДО чтения авторских тестов, свои значения, на каждом из пяти мест |
| `R2-3` ни один живой вызывающий не сломался этим ужесточением | поведение | СВОЙ обход вызывающих обоих relay-маршрутов и push-адаптера: каждый ли задаёт `subject`/`title` |
| `R2-4` персонал получает Therapysto, пациент — Therapygo или имя клиники | поведение | свои значения, своя организация |
| `R2-5` контракт интегратора описывает новое требование, а не расходится с кодом | разовое действие | взгляд на `INTEGRATOR_CONTRACT.md` против кода |
| `R2-6` тесты аудита круга 1 целы и зелёные | разовое действие | `git diff` по файлу и прогон |
| `R2-7` технические идентификаторы и история не переименованы | разовое действие | взгляд на diff |

**Главный риск этого круга — не бренд, а отказ доставки.** Ужесточение «нет имени → 400» ломает любой путь,
где имя не задавалось. Автор утверждает, что все вызывающие его задают; проверь это САМ и назови, где смотрел.
Если найдётся вызывающий без имени — это блокер: пациент или персонал перестанет получать сообщения.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `TPB-08`,
дословно: «Branding влияет только на patient-facing surface; staff/admin видят Therapysto.»

Под аудитом: `a84e195eb` на `wt/therapysto-s1-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-s1-20260824`.
Автор: Codex `gpt-5.6-sol`/high. Круг 1 и его FAIL — `THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_TPB15_2026-08-24.md`.
Автор выбрал fail-closed: общий push-адаптер поверхность не знает, поэтому любой его fallback ошибочен хотя бы
для одной стороны, и вместо fallback стоит отказ `WEB_PUSH_PAYLOAD_INVALID`.

## Границы

Прод не трогать, `--execute` на DEV не запускать, в `feat` не вливать, реальным адресатам ничего не слать.
Не трогать `app.read_integrator_clinic_delivery_credential`, миграции `20260823T030000…` / `20260823T043206…`,
`app.enqueue_outbound_message` и `dispatchPort.ts`. Finding вне owner scope — вопрос владельцу, не работа.
Уровень `app`, два приложения. Full CI не гонять.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_TPB15_2026-08-24.md`: первой строкой вердикт,
строка на каждый пункт, счёт инъекций посажено/убито/не поймано.
