# Audit brief — `C3`, круг 2 (закрывающий, независимый, адверсарный)

Repo canon: `AGENTS.md` — прочитай §10a, §10b, §24.4, §24.5 целиком до классификации.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, §1.2h, решение
владельца 23.08.2026 (новее текста `C3`), дословно: «На пути по умолчанию клиника не настраивает НИЧЕГО»;
и пункт `C3`: «удалить любой достижимый platform fallback для них.»

Под аудитом: `328aac31b` на `wt/therapysto-c3-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-c3-20260824`.
Автор круга 2 — Codex `gpt-5.6-sol`/high. Круг 1 (`0277423d0`) получил FAIL, отчёт
`THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_C3_2026-08-24.md` — прочитай его, блокер был один: `clinic_required`
ставился безусловно и гасил путь по умолчанию.

Заявление автора: семь путей передают `clinic_if_configured`; единый `clinicSenderScope` выбирает платформенного
бота при отсутствии включённого кредентиала и `clinic_required` при его наличии; две инъекции покраснели;
integrator 29 passed, webapp 19 passed, оба typecheck зелёные.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ доказательства |
| --- | --- | --- |
| `R2-1` клиника БЕЗ своего бота — пациент получает сообщение прежним путём, а не отказ | поведение | слепой kill-set по §1.2h ДО чтения авторских тестов, свои значения |
| `R2-2` клиника СО своим включённым ботом — платформенного отката нет даже при ошибке её бота | поведение | свой тест, краснеющий при возврате отката |
| `R2-3` решение о scope принимается в ОДНОМ месте, семь продюсеров только сообщают контекст | разовое действие | взгляд на diff, теста на отсутствие строк не писать |
| `R2-4` «включён» значит включён по-настоящему: тарифная механика И наличие кредентиала | поведение | проверь ОБА условия по отдельности, а не одно |
| `R2-5` небрендированная пациентская поверхность и доставка staff/admin не изменились | поведение | регрессия на своих значениях |
| `R2-6` аудиторский `phoneStartBrandedOtpSender.audit.test.ts` цел и не ослаблен | разовое действие | `git diff` по файлу |
| `R2-7` числа автора воспроизводятся | разовое действие | перезапуск названных наборов своими руками |

Смотри отдельно, не поверив автору на слово: в каком порядке проверяются механика тарифа и кредентиал, и что
происходит, если механика включена, а кредентиал пуст (и наоборот). Молчаливая отправка через платформенный бот
там, где клиника считает, что пишет своим, — это отказ, а не мелочь.

## Границы

Не трогай `app.read_integrator_clinic_delivery_credential` и миграции `20260823T030000…` / `20260823T043206…`;
обе роли не разрешать. Никогда не ставь `telegram_mode=long_polling` — в настройках настоящий прод-токен бота.
Ничего не слать реальным получателям. В `feat` не вливать. Finding вне owner scope — вопрос владельцу, не работа.

Уровень `app`, два приложения (`apps/integrator`, `apps/webapp`) — гоняй таргетные наборы обоих. Full CI не гонять.

## Дедлайн

Уложись в 25 минут. Отчёт и свои тесты закоммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_C3_2026-08-24.md`, первой строкой
`PASS, FOR LAND` или `FAIL, NOT FOR LAND`, строка на каждый пункт `ID → PASS|FAIL|BLOCKED → evidence`,
счёт инъекций посажено/убито/не поймано.
