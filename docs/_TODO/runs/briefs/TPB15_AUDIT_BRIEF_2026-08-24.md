# Audit brief — остаток `TPB-15`, круг 1 (независимый, адверсарный)

Repo canon: `AGENTS.md` — §10a, §10b, §24.4, §24.5 целиком до классификации.

## Классификация «тест или взгляд» (§24.4)

| Пункт | Природа | Способ |
| --- | --- | --- |
| `T-1` перепись ПОЛНАЯ: пятого места, где человек видит «BersonCare», нет | разовое действие | СВОЙ исчерпывающий `rg` по обоим приложениям и `deploy/postgres/*.sql`, не сверка с таблицей автора |
| `T-2` персонал видит Therapysto в диалоге passkey и в приложении-аутентификаторе | поведение | слепой kill-set ДО чтения авторских тестов |
| `T-3` пациент видит Therapygo, а пациент клиники — имя клиники, в письме записи и в push | поведение | свои значения, своя организация |
| `T-4` `rpID` и секреты не тронуты: уже выданные passkey и привязанные аутентификаторы живы | разовое действие | взгляд на diff; автор заявляет, что менял только отображаемые имена |
| `T-5` имя берётся из существующего механизма, а не новой константы; второго источника имён нет | разовое действие | взгляд |
| `T-6` отсутствие поверхности у вызывающего — ошибка вызова, а не подстановка платформенного имени | поведение | своя инъекция |
| `T-7` технические идентификаторы, ключи настроек и история НЕ переименованы | разовое действие | взгляд на diff; `TPB-15` это прямо запрещает |

**Автор инъекции НЕ прогонял — не успел до дедлайна и честно об этом сказал.** Значит вся проверка на
«не зелёное всегда» лежит на тебе: посади свои инъекции на каждое из четырёх мест и скажи, что покраснело,
а что прошло молча.

Источник оракула: `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `TPB-15`,
дословно: «User-visible BersonCareBot/platform BersonCare и понятие BersonCare Bot заменены на Therapysto».
Разделение имён по поверхностям — §1.2f: «Два: Therapysto персоналу, Therapygo пациентам»; запрет показывать
пациенту имя платформы — `TPB-08`.

Под аудитом: `e30e78522` на `wt/therapysto-s1-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-s1-20260824`.
Автор: Codex `gpt-5.6-terra`/high. Предыстория — `THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT2_S1_2026-08-24.md`,
пункт `R2-7` и «Вопросы владельцу» №2.

## Границы

Прод не трогать, `--execute` на DEV не запускать, в `feat` не вливать, реальным адресатам ничего не слать.
Не трогать `app.read_integrator_clinic_delivery_credential`, миграции `20260823T030000…` / `20260823T043206…`,
`app.enqueue_outbound_message` и `dispatchPort.ts`. Finding вне owner scope — вопрос владельцу, не работа.
Уровень `app`, два приложения. Full CI не гонять.

## Дедлайн

Уложись в 25 минут, коммить ДО этого, `git add -A` запрещён.

## Выход

Отчёт `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_TPB15_2026-08-24.md`: первой строкой вердикт
`PASS, FOR LAND` / `FAIL, NOT FOR LAND`, строка на каждый пункт, счёт инъекций посажено/убито/не поймано.
