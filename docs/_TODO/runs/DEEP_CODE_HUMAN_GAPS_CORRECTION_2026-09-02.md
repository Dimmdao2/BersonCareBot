# N1 human gaps — correction acceptance, 2026-09-02

## Result

**PASS AFTER FIX.** The independent audit in
`DEEP_CODE_HUMAN_GAPS_AUDIT_2026-09-02.md` accepted the diary purge surface and rejected the
email-change surface because a newer OTP challenge of another purpose could shadow the valid
`patient_email_change` row.

The correction makes purpose part of the existing latest-pending lookup instead of adding another
OTP consumer. Both the patient profile and the clinic-admin pending view request exactly
`patient_email_change`; confirmation receives only the matching challenge.

## Migration and access analysis

- Changed object: the existing latest-pending email-challenge function moves from the two-argument
  signature to the same function with a third `purpose` argument. No table, column, policy or role is
  added.
- Owner/runtime: the function remains owned by the existing narrow email-OTP seam owner and remains
  executable only by the patient runtime role.
- Relation access: the body still only selects the already-declared challenge columns. The new
  predicate uses the already-declared `purpose` column; no additional table operation is required.
- Declaration: the function identity and typed arguments were updated in the single function census;
  both generated DEV/TEST privilege artifacts were regenerated byte-for-byte from it. The migration
  contains no grant, revoke, role or policy statement.
- Owner-aware named-DEV preflight executed the pending migration under the declared owner and rolled
  the transaction back: `pending=1`, `ROLLBACK`, `PASS`.

## Validation

- Independent acceptance suite after correction: 4 files / 7 tests passed, including the competing
  purpose case and diary user-bound purge route.
- Migration privilege/order gates passed for 114 active migrations.
- Generated privilege `--check` passed for DEV and TEST.
- Scoped ESLint and webapp typecheck passed.
- `git diff --check` passed.

No migration was applied permanently and DEV/TEST/PROD data was not changed.
