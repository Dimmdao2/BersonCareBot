# T-A · Организация выбирает, как называется событие записи

План: [`MEDICAL_WELLNESS_TERMINOLOGY_MODE_2026-09-02.md`](../MEDICAL_WELLNESS_TERMINOLOGY_MODE_2026-09-02.md),
раздел «Решение владельца 12.09.2026» и таблица блоков T-A…T-F. Он — единственный источник «готово».
Задача №1094 в общей taskdb.

## Что решил владелец (дословно, не пересказ)

> «Короче, мы меняем там клиент и пациент. Просто у нас уже есть такая штука, да? Надо будет просто
> добавить выбор названия, типа, сеанс, прием, тренировка, сессия… Вы записаны на сеанс, вы записаны
> на прием… И внутри кабинета специалиста, и внутри кабинета клиента надо просто везде перевести на
> вот этот вот механизм выдачи нужного слова, так же, как по принципу выдача клиент-пациент».

> «Прием или сеанс тогда, окей, пока». · «Занятие, занятия, занятия, оно как бы как есть, так и
> есть. Занятие всегда». · «Встреча, блядь, ну, встреча не уверен я. Встречу не надо». ·
> «Медицинские шаблоны менять точно не надо». · «И то не срочно».

## Скоуп ЭТОГО прохода — только T-A

Механизм и его хранение. Ни одна надпись в интерфейсе этим проходом не переписывается: переписывание
идёт блоками T-B…T-F, каждый отдельным проходом, и начинается только после того, как T-A принят.

1. Второй per-org выбор рядом с существующим `patient_label` — как называется событие записи.
   Набор ровно четыре значения: **приём · сеанс · тренировка · сессия**. Дефолт — **приём**: это
   сегодняшнее поведение продукта, и смена дефолта переименовала бы всё у всех уже работающих клиник.
2. Тот же путь, что у `patient_label`, а не второй параллельный: одно место хранения, одна дверь
   чтения, один контекст на клиенте. Второго resolver-а, второго контекста и page-level подстановок
   быть не должно — это записано в MWT-03 и проверяется приёмкой.
3. Экран настройки специалиста: выбор стоит рядом с выбором «Клиенты / Пациенты», тем же элементом.
4. Слой обязан отдавать не одну строку, а ГРАММАТИЧЕСКИЕ ФОРМЫ, которые реально нужны существующим
   надписям: «Очный приём», «Формат приёма», «Новое время приёма», «Журнал прошедших приёмов», «Вы
   записаны на приём». Родительный и винительный падежи у «приём/сеанс/тренировка/сессия» разные, и
   род тоже (тренировка и сессия — женский). Если отдать одну строку, блоки T-B…T-F начнут склеивать
   падежи руками в каждом файле — ровно то, от чего слой и строится. Формы задаются данными в одном
   месте, не выводятся алгоритмом из основы.

## Границы — за них не выходить

- **«Занятие» не участвует.** Оно закреплено за самостоятельной практикой (отметка ЛФК) и в набор
  выбора не входит. Не переименовывать.
- **Медицинские шаблоны справочников не трогаются.**
- Остальные 24 пункта D-матрицы (§4.2 инвентаря) и развилки Q1-Q7, Q9 владелец НЕ решал. Не
  реализовывать и не додумывать по рекомендациям агента.
- Ветка отдельная, не основная — решение владельца дословно: «точно делать не в основной ветке».

## Чем доказывается «готово»

- новая колонка объявлена в `deploy/postgres/privileges/declaration.ts` (GRANT в миграции не
  работает — reconcile его снимает), артефакты перегенерированы, `generate-cli.mjs --check` чистый;
- миграция по схеме B с маркерами и `BCB-MIGRATION-VERIFY`;
- `migrate-dev.sh --preflight`, затем `--execute`;
- живой прогон: на DEV в кабинете специалиста выбор переключается и переживает перезагрузку страницы;
- затронутые тесты зелёные, `tsc --noEmit` и eslint по изменённым файлам чистые.

Отчёт заканчивается обязательной секцией «НЕ СДЕЛАНО», даже если она пустая.
