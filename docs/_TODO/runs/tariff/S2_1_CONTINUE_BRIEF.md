# Этап 2, пункты 2.1a–2.1c — ДОВЕСТИ начатое (run: worker-tariff-2-1-continue)

**План (authority):** `docs/_TODO/SAAS_FOUNDATION/TARIFFS_PAYMENTS_ADMIN_PLAN.md`, §5a, пункты **2.1a, 2.1b, 2.1c**.
Прочитать все три дословно.
**Канон:** `QUOTAS_AND_MECHANICS_DESIGN_2026-07-28.md`, §4a и §5.

**Правила репозитория, обязательные к прочтению до правки:**
`.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/webapp-tests-lean-no-bloat.mdc`,
`.cursor/rules/clean-architecture-module-isolation.mdc`.

## Это ПРОДОЛЖЕНИЕ, не работа с нуля

Предыдущий прогон снят на середине (не по своей вине — я снял его по указанию владельца). Его работа
закоммичена как есть: `37e7fc58c`, 22 файла, 649 строк. Что уже сделано:

- миграция `0284_organization_cabinet_access_ladder_local.sql` — доступ в кабинет как отдельный предмет лестницы;
- `resolveCabinetAccess` в порту прав (`ports.ts`, `pgOrgEntitlements.ts`, `service.ts`, `types.ts`);
- страж кабинета `app-layer/guards/cabinetAccessGate.ts` и подключение в `requireRole.ts`;
- расширено доказательство лестницы `check-access-ladder-transitions.mjs` (+207 строк);
- тесты маршрута оплаты клиникой своего тарифа (`api/clinic/billing/route.test.ts`) — это инвариант 2.1c;
- правки в `saas-billing/service.ts` и его тестах.

**⛔ Не переделывай сделанное.** Сначала прочитай diff коммита `37e7fc58c` целиком и пойми, что там уже есть.

## Что доделать — по порядку

1. **ТИПЫ КРАСНЫЕ, это блокер.** `pnpm --filter webapp typecheck` падает: в порт добавлен `resolveCabinetAccess`,
   но тестовые заглушки порта его не реализуют — 5+ ошибок `TS2741` в `org-entitlements/service.test.ts` и
   соседних. Довести до зелёного.
2. **Пройти три пункта приёмки и закрыть недостающее** (см. ниже). Что из этого уже покрыто — доказать
   ссылкой на тест, что нет — дописать.
3. Миграция уже занимает номер `0284`; если понадобится ещё одна — брать следующий свободный, метка `when`
   уникальная и строго возрастающая, номер файла и `idx` в журнале совпадают.

## Приёмка (из плана, без изменений)

- доступ в кабинет настраивается теми же тремя величинами и проходит три ступени — тест на каждую;
- при блоке кабинета данные целы и возвращаются при возобновлении;
- **2.1b**: тест падает, если в коде появится агентский список механик, исключённых из лестницы; критичные
  механики в тариф не попадают вовсе — доказать. ⚠️ Класс «никогда» из канона — это НЕ агентский список,
  а решение владельца; снимать его нельзя;
- **2.1c**: организация в конечном состоянии «блок» открывает и оплачивает СВОЙ тариф (путь, которым клиника
  платит НАМ, лестницей не затрагивается ни в одном состоянии), и отдельно — приём денег ОТ ПАЦИЕНТОВ погашен;
- каждый тест краснеет при снятии соответствующей проверки — дословный вывод до и после;
- `pnpm --filter webapp lint` и `typecheck` зелёные;
- галочки плана не ставить, push и merge не делать.
