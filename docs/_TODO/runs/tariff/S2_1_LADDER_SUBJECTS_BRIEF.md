# Этап 2, пункты 2.1a–2.1c — предметы лестницы и инвариант двух потоков денег (run: worker-tariff-2-1)

**План (authority):** `docs/_TODO/SAAS_FOUNDATION/TARIFFS_PAYMENTS_ADMIN_PLAN.md`, §5a, пункты **2.1a, 2.1b, 2.1c**.
Прочитать все три дословно.
**Канон:** `QUOTAS_AND_MECHANICS_DESIGN_2026-07-28.md`, §4a и §5. Читать целиком.

**Правила репозитория, обязательные к прочтению до правки:**
`.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/webapp-tests-lean-no-bloat.mdc`,
`.cursor/rules/clean-architecture-module-isolation.mdc`.

## Три требования, все от владельца 30.07

**2.1a — доступ В КАБИНЕТ есть ОТДЕЛЬНЫЙ предмет лестницы.** Дословно: «и сам доступ к кабинету, и доступ к
механикам внутри тарифа». Настраивается теми же тремя величинами: сколько терпения с полным доступом, сколько
только чтения, когда блок. Блок кабинета закрывает вход в продукт целиком, а не отдельные разделы. **Данные при
этом не удаляются и возвращаются при возобновлении.**

**2.1b — исключений НЕТ ни у одной механики, включённой в тариф.** Агент не выбирает, какая механика подчиняется
лестнице. Механическая проверка: в коде **нет ни одного списка механик, исключённых из лестницы по решению
агента**. Критичные механики (те, что владелец велел не трогать) — не тарифные опции, они в тариф не попадают
вовсе; их закрывает только блок кабинета как следствие уровня системы.

**2.1c — ИНВАРИАНТ ДВУХ ПОТОКОВ ДЕНЕГ, критично не перепутать.** Механика «приём оплат» гасит приём денег
клиникой **от пациентов** (предоплата при записи, онлайн-оплата на приёме). Путь, которым клиника платит **НАМ**
за тариф, лестницей не затрагивается **ни в одном состоянии, включая блок**. Тест обязателен: организация в
конечном состоянии «блок» по-прежнему может открыть оплату своего тарифа и оплатить его.

## Что уже есть — опереться, не переписывать

Сведено в ветку и проверено: лестница состояний и единая дверь доступа, две ручки (неоплата и понижение тарифа),
три числовые механики с доказательствами гонки, реестр защищённых точек, класс «никогда» для критичных
(карточка пациента, приложение пациента, дневники — последние 31.07 по уточнению владельца не отбираются
**ни у пациента, ни у клиники**).

⚠️ Для 2.1b учти: класс «никогда» — это НЕ «список исключений по решению агента», а прямое решение владельца из
канона §4a. Отличие важно: 2.1b запрещает агентские списки, а не канонические классы. Если найдёшь агентский
список — снимай.

## Приёмка

- доступ в кабинет настраивается теми же тремя величинами и проходит три ступени — тест на каждую;
- тест: при блоке кабинета данные целы и возвращаются при возобновлении;
- **2.1b — механическая проверка**: тест падает, если в коде появится агентский список механик, исключённых из
  лестницы; критичные механики при этом в тариф не попадают вовсе — доказать;
- **2.1c — обязательный тест**: организация в состоянии «блок» открывает и оплачивает СВОЙ тариф; и отдельно —
  приём денег от пациентов при этом погашен;
- каждый тест краснеет при снятии соответствующей проверки — дословный вывод до и после;
- `pnpm --filter webapp lint` и `typecheck` зелёные;
- галочки плана не ставить, push и merge не делать.
