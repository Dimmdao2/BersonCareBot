VERDICT: FAIL

## Матрица A

`PRESENT` означает наличие отдельного проверяемого checkbox. Упоминание только внутри общего слайса считается `MISSING`.

| Owner ruling | Plan checkbox ID | Статус |
|---|---:|---|
| §1.1: «повышение — сразу… понижение — с начала следующего расчётного периода» | 4.5 | PRESENT |
| §1.1: «просто вкл/выкл механику cms. менять код» | 2.3 | PRESENT |
| §1.1: «страница профиля и страница букинга + виджеты» не являются CMS | 2.3 | PRESENT |
| §1.1: «предупреждения, период терпения, потом read only» | 4.2 | PRESENT |
| §1.2: «перевести на read-only специалистов сверх тарифа, но сначала предупреждения и период терпения» | 4.2 | PRESENT |
| §1.2: «не может понизить тариф, не архивировав или удалив лишних пациентов» | 4.5 | PRESENT |
| §1.2: «За специалистов можно доплатить… за клиентов — нет» | 4.1, 4.3 | PRESENT |
| §1.2: «Курсы — возможность, не число» | 2.1, 2.2 | PRESENT |
| §1.2: «Рассылки — вкл/выкл саму возможность» | 2.1 | PRESENT |
| §1.2: «для рассылок… SMTP провайдера своего и ботов своих» | 2.7 | PRESENT |
| §1.3: «ты мне главное дай выключатели корректные»; цифры и упаковка не задача агента | — | MISSING |
| §1.3: «Сегодня… блок не должен видеть при выключенной настройке» | 5.1, только внутри общего слайса | MISSING |
| §1.3, следствие: «журнал событий расхода, якорь периода… СЕЙЧАС НЕ НУЖНЫ» | 3.2, но 4.5 возвращает операцию нового периода | MISSING |
| §9.11: внешний календарь вообще, а не только Google | 5.3, общий слайс | MISSING |
| §9.11: дневники пациента | 5.1, общий слайс | MISSING |
| §9.11: клинические тесты; системные группы исчезают и из программы лечения | 5.2, общий слайс | MISSING |
| §9.11: онлайн-анкета | 5.2, общий слайс | MISSING |
| §9.11: поддержка — рубильник при постройке системы обращений | 5.5 | PRESENT |
| §9.11: статистика вместе с UTM-источниками, одна механика | 5.3, общий слайс | MISSING |
| §9.11: проактивные подсказки | — | MISSING |
| §9.11: задачи специалиста — рубильник, не число | 5.2, общий слайс | MISSING |
| §9.11: предоплата при записи | 5.3, общий слайс | MISSING |
| §9.11: шаблоны уведомлений — часть брендирования | 2.6 | PRESENT |
| §9.11: «приложение не трогай… карточка пациента… тоже не трогай» | 2.4 | PRESENT |
| §9.11: «филиалы — числом» | 4.4 | PRESENT |
| §9.11: шаблоны программ лечения и ЛФК «не резать» | — | MISSING |
| §9.11: «НЕ делаем: переписка с пациентом» | — | MISSING |
| §9.11: «НЕ делаем: правила отмены» | — | MISSING |
| §9.11: оценки материалов выключаются для платформы целиком | 2.8 | PRESENT |
| §9.11: напоминания и уведомления не ограничиваются | — | MISSING |
| §9.11: двухфакторная аутентификация «нужна всем» | — | MISSING |
| §9.11: экстренная помощь — без отдельного рубильника | — | MISSING |
| §9.11: журнал операций платформенный, рубильник не нужен | — | MISSING |
| §9.11: экспорт своих данных не ограничивается, включая read-only | — | MISSING |

## MUST FIX

1. **Checkbox 5.1–5.3: отсутствует проактивная аналитика.** Слайсы перечисляют десять других механик, но `doctor-proactive-insights` нигде не реализуется; при исполнении плана эта функция останется доступной всем тарифам. Impact: прямое невыполнение положительного вердикта §9.11. Нарушены owner ruling и требование отдельного atomic checkbox из `plan-authoring-execution-standard.mdc:26-28`.

2. **Checkbox 5.1–5.3 и отсутствующие IDs: построчные решения сведены в мегачекбоксы либо потеряны.** Например, исполнитель предоплаты может закрыть весь `booking-policies`, включая запрещённые к ограничению правила отмены; исполнитель клинических тестов может затронуть запрещённые шаблоны программ/ЛФК. Impact: владелец получает отключение функций, которые прямо приказал не ограничивать, а аудитор не имеет отдельной строки для выявления этого. Нарушены дизайн §9.11 и atomic-checklist rule.

3. **Checkbox 5.5 делает план формально незакрываемым.** Он намеренно остаётся `[ ]` до будущего построения системы обращений, хотя эта система объявлена вне scope. Impact: либо #1069/LOG нельзя честно закрыть, либо строку закроют без реализации. Нарушены `plan-authoring-execution-standard.mdc:45-57` и правило трёх состояний чекбокса; owner-deferred работа должна иметь трассируемое состояние, а не вечный открытый checkbox текущего плана.

4. **Checkbox 2.2, 2.3, 5.1–5.3: поведение выключенной feature противоречит канону.** План требует ворота «на всех путях записи и чтения» (`TARIFF_MECHANICS_PLAN:173-179`), тогда как дизайн §9.3 требует блокировать только создание нового, а уже созданное оставлять работающим, видимым и выгружаемым. Reachable failure: выключение курсов, дневников или тестов скрывает уже назначенное пациенту. Impact: потеря доступа к клиническим данным/контенту. Нарушен `QUOTAS_AND_MECHANICS_DESIGN:298-304`.

5. **Checkbox 1.1–1.2: план самовольно меняет канонические пять классов.** Канон §9.1 содержит `возможность | места | расход за период | объём | никогда`; план заменяет `расход за период` на неутверждённый `count` (`TARIFF_MECHANICS_PLAN:99-102`) и не задаёт для него допустимые единицы/период. Impact: исполнитель типизирует другую модель и может снова разрешить бессмысленные комбинации числа и периода. Нарушен точный canonical model §9.1 и запрет агента самостоятельно закрывать продуктовую развилку.

6. **Checkbox 2.5, 4.6: лимит файлов не реализуется и не имеет race-proof.** 2.5 лишь удаляет единицу «штуки», а 4.6 требует гонку только для пациентов и филиалов. Нет шага, который атомарно суммирует байты и запрещает конкурентные загрузки. Reachable failure: две параллельные загрузки проходят последний остаток и превышают тариф. Impact: неконтролируемые расходы хранилища. Нарушены DoD 2 (`TARIFF_MECHANICS_PLAN:202-204`), дизайн §9.3.4 и требование «каждый числовой лимит имеет race proof».

7. **Checkbox 4.1–4.2: read-only за неоплаченную доплату невозможно построить при объявленном scope.** План исключает биллинг #1057 (`TARIFF_MECHANICS_PLAN:57-58`), но требует определять неоплату дополнительного специалиста и запускать от неё grace/read-only (`:143-150`). Без счёта/статуса оплаты нет авторитетного сигнала. Impact: read-only либо никогда не наступит, либо будет включаться по выдуманному локальному состоянию; сумма счёта станет неверной. Нарушен punchlist 10.16: «делать согласованно, не двумя ветками».

8. **Checkbox 4.5 содержит invented scope.** Операция «начать новый период сейчас» возвращена после прямого вывода §1.3 и punchlist 10.6–10.7, что сейчас нет расхода за период, якоря и сбрасываемой величины. Impact: строится операция без предмета, способная случайно обойти правила понижения/уборки. Нарушены owner ruling «СЕЙЧАС НЕ НУЖНЫ» и принцип «необходимо и достаточно».

9. **Checkbox 3.3 ссылается на несуществующие и противоречивые образцы.** В §9.5 нет «четырёх строк для четырёх чисел»: числовых строк только две — места и файлы (`QUOTAS_AND_MECHANICS_DESIGN:336-342`). Более того, пример мест требует освободить место, тогда как позднейшее решение разрешает доплату сверх базы. Impact: для пациентов/филиалов нет acceptance-текста, а места могут быть ошибочно реализованы жёстким запретом. Нарушены §9.5 и §9.9.

10. **Проблема 0.1 содержит ложный factual claim «реально стоят только три запрета».** `quotaEnforcement` относится к числовой квоте, но feature-гейт уже реально закрывает прямые API для `booking` (`branches/route.ts:26-30`), `branding` (`notification-templates/route.ts:65-72`), `files` (`files/route.ts:139-140`), `subscriptions` (`patient-packages/route.ts:70-78`) и `mailings` (`broadcasts/actions.ts:100-103`). Impact: исполнители исходят из неверной карты и могут дублировать или удалить действующие ворота. Нарушено требование D проверять claims по коду.

11. **Checkbox 5.4 приписывает `check-s4-entitlement-coverage` гарантию, которой нет.** Проверка guard-вызова намеренно удалена (`check-s4-entitlement-coverage.ts:64-68`); скрипт проверяет существование экспорта и строки реестра, но не фактический guard или правильную механику. Reachable failure: добавить mapping, не вызвать `requireEntitlement*`, получить зелёный check и оставить API-обход. Impact: тарифный рубильник не защищает прямой запрос. Нарушены owner requirement «корректные выключатели» и behaviour-test policy.

12. **Checkbox 4.7 не покрывает обязательный deploy-контракт новых DB-функций.** В плане есть только ролевая проверка права, но нет обновления `expected_secdef_count`, двух contract tests и exact ACL assertions. Deploy реально падает при несовпадении счётчика (`deploy-test-saas.sh:1514-1534`) и сравнивает точные ACL через двусторонний `EXCEPT` (`:1607-1636`). Impact: TEST deploy падает после миграции либо лишний grant нарушает role wall. Нарушен `ORCHESTRATOR_CHECKLIST:18-19` и execution hazard E.

13. **Checkbox 4.3, 4.4, 6.1–6.2 не фиксирует FORCE-RLS principal contract.** Без установленного principal repository-read возвращает `[]`, а не ошибку (`RLS_UNPRINCIPLED_READ_FIX_PLAN:20-28`). Race-скрипт без настоящих RLS/ролей этого не выявит. Impact: пересчёт видит ноль пациентов/филиалов, разрешает превышение или строит ложный отчёт «превысивших нет». Нарушено обязательное требование E.

14. **Декларация параллельности этапов 4/5 и слайсов A/B/C фактически неверна.** Все они добавляют ключи в `types.ts` и mappings в `protectedActionRegistry.ts`; этап 4 добавляет пациентов/филиалы, каждый слайс 5 — свои mechanics и protected actions. В одном клоне параллельные воркеры редактируют одни файлы. Impact: merge-конфликты или потеря registry/mapping строки; один слайс проходит проверку против уже изменившегося файла другого. Нарушено правило параллелить только непересекающийся file-scope. Общие registry-файлы должны редактироваться последовательно; доменные файлы слайсов могут идти параллельно.

15. **Checkbox 7.1 задаёт неверный финальный CI gate.** На общем хосте полный CI разрешён только через `/home/dev/brain/host-orch/run-tests.sh "<cmd>"` (`ORCHESTRATION_BINDINGS:26-37`), а merge-gate включает `pnpm install --frozen-lockfile` перед CI (`pre-push-ci.mdc:39-44`). Reachable failure: параллельный Next build падает на build-lock либо gate проходит на несинхронных зависимостях. Impact: финальная проверка недействительна. Частота «один раз в конце» при этом выбрана правильно.

## Что в плане верно

- Проблема, scope, запрет PROD и Definition of Done сформулированы явно.
- `cms_pages` действительно считает физические строки, включая архив: `0270_cms_pages_snapshot_quota.sql:6-20`.
- Оба триггера существуют: courses — `0225_saas_tariff_quotas_trial.sql:302-385`, CMS — `0270_cms_pages_snapshot_quota.sql:30-133`.
- Новые mechanic keys хранятся в JSONB и сами по себе не требуют schema migration: `saasEntitlements.ts:35-42`.
- Миграционный номер правильно оставлен лиду на merge — checkbox 7.2.
- Один full CI в конце, targeted-проверки по этапам, запрет source-text assertions и расширение существующих тестов соответствуют тестовой политике.
- Race-proof для пациентов и филиалов и отдельная проверка DB-прав заложены правильно, хотя files и deploy ACL-контракт пропущены.
- Ловушка `.next/types` неприменима: план не требует удалять route/page. Если исполнитель начнёт удалять их ради скрытия, это уже выход за scope.

## Claims I could not verify

- Не выполнялся живой DEV/TEST прогон, поэтому нельзя подтвердить наличие и состояние демо-клиник A/B и будущие UI-smoke пункты.
- Статически подтверждены кодовые поверхности девяти кандидатов и отсутствие их ключей в текущем `MECHANIC_REGISTRY`, но их фактическая доступность на запущенном DEV не проверялась.
- `ORCHESTRATOR_CHECKLIST` требует обновлять «два contract tests» вместе со счётчиком `SECURITY DEFINER`, но их точные имена в прочитанных authority-файлах не названы; план в любом случае не содержит такого шага.

Файлы не изменялись; тесты, миграции и deploy не запускались.