## 0. Главное, что меняет постановку задачи

Отчёт сверял 53 пункта **только с §5a**. Но §5a — чек-лист механик и лимитов (#1069). Биллинг, аналитика и библиотека живут в **том же файле плана в Phase 4/5 и в DoD**, а часть — в пунш-листе (§4, карточки #844/#845/#1057) и в #854. При этом:

- В Phase 4/5 требования стоят **строками без галочки** с шапкой `ВЕДЁТСЯ В SAAS_S4… §9 / S4-4 — «…»` (например `TARIFFS_PAYMENTS_ADMIN_PLAN.md:504, 510, 522, 526, 552, 573, 618, 623, 633`). Пункт 6a.4 архивирует именно тот файл → **эти строки останутся текстом без чекбокса, указывающим в архив**. Механическое действие при архивации: вернуть им `- [ ]` и снять шапку `ВЕДЁТСЯ В`. Это спасает ~15 из 53 без единого нового пункта в §5a.
- **Статус Phase 4 в плане устарел.** Там написано «modules/saas-billing не существует, подтверждено дважды». Существует: `apps/webapp/src/modules/saas-billing/{ports,service,settings}.ts`, `db/schema/saasBilling.ts` (+`0259`), `infra/repos/pgSaasBilling.ts`, ключ `saas_billing_payment_provider` (`system-settings/registry.ts:310`), две ручки (`api/clinic/billing/route.ts`, `api/admin/organizations/[organizationId]/billing/route.ts`), экран (`SaasBillingOverview.tsx`). Пунш-лист §4.1–4.5 закрыт коммитом `9bfa4303c`; открыт только 4.6 (checkout+вебхук).
- **§0 плана (строки 56–59) прямо говорит**, что этот документ берёт из S4 только S4‑0/1/2 и урезанный S4‑4, а S4‑3 (магазин) и S4‑5 (аналитика) **не берёт** — они «отложенная, не отменённая работа». Значит для отложенного нужен не пункт в §5a, а имя в надгробии 6a.4.

Правило, которым я резал: `ORCHESTRATION_BINDINGS.md:86-89` — «аудит стал генератором скоупа… 5.3k строк машинерии, 0 задач владельца». Пункт без достижимого сценария поломки — `НЕ НУЖНО`.

---

## 1. Счёт

| Вердикт | Кол-во | Номера |
|---|---:|---|
| **НУЖНО СЕЙЧАС** | 6 | 2, 14, 16, 22, 49, 52 |
| **НУЖНО ПОЗЖЕ** | 12 | 3, 4, 7, 19, 21, 24, 25, 28, 29, 35, 41, 47 |
| **НЕ НУЖНО** | 35 | 1, 5, 6, 8–13, 15, 17, 18, 20, 23, 26, 27, 30–34, 36–40, 42–46, 48, 50, 51, 53 |
| **Всего** | **53** | |

---

## 2. НУЖНО СЕЙЧАС (6)

### 1) Пункт 2 — изоляция own-only библиотеки → **Этап 3, новый 3.5**

```
- [ ] **3.5** Кросс-организационная приёмка библиотеки: клиника B не получает упражнения, шаблоны и медиа
      клиники A ни списком, ни поиском, ни счётчиком, ни прямым ID, ни воспроизведением медиа; тест краснеет
      при снятии org-предиката в `pgLfkExercises`/`pgLfkTemplates`/`s3MediaStorage`.
```
**Что предотвращает.** Изоляция сегодня держится **только на предикате в приложении** (`pgLfkExercises.ts:544-546, 698-703`, `pgLfkTemplates.ts:418-420, 611-615`, `s3MediaStorage.ts:827-851, 919-961`): FORCE RLS с `lfk_*`/`media_files` снят миграцией `0177_phase4_no_force_rls_compat.sql:1-6`, cutover-скрипт `deploy/postgres/phase4-force-rls-cutover.sql` не применён, **автотестов кросс-тенантного отказа на эту поверхность ноль**. А Этапы 3.1a (снятие раннего `return { ok: true }` в `requireEntitlement.ts:42-49`) и 3.1b (адаптер видимости) правят ровно эти read-path. Одна потерянная строка предиката = клиника B видит упражнения и видео клиники A, и никто не краснеет.
**Практика.** Изоляция библиотеки контента в мультитенанте принимается негативным тестом на **прямой ID и выдачу медиа** (IDOR), а не на список: скрытая навигация изоляцией не считается.

### 2) Пункт 16 — лестница не задевает соседнюю клинику → **Этап 7, новый 7.5**

```
- [ ] **7.5** Прогон лестницы на TEST делается на двух клиниках: перевод A в «терпение», «только чтение» и
      «выключено» (по неоплате и по понижению тарифа) не меняет ступень у B, не удаляет ни одной строки данных A,
      и пересчёт состояния выполняется под установленным принципалом.
```
**Что предотвращает.** 7.3 сегодня гоняет **одну** клинику. Канон §2: «под FORCE-RLS чтение без принципала возвращает пустоту, а не ошибку» — пакетный пересчёт лестницы без принципала либо выключит не тех, либо не выключит никого; а «выключено» по 2.4 скрывает разделы у специалиста и у его пациентов. Цена ошибки — чужая клиника, потерявшая доступ.
**Практика.** Dunning/lifecycle-джобы в мультитенанте гоняются per-tenant с явным scope; глобальный batch — классический источник кросс-тенантных инцидентов.

### 3) Пункт 22 — платформенная ручка аналитики отдаёт персональные строки → **вне §5a: отдельный security-пункт (пунш-лист / преflight #854)**

```
- [ ] `GET /api/admin/product-analytics` приводится к уже существующему в репозитории fail-closed контракту
      `/api/admin/doctor-analytics-metric-accounts` (409 `platform_patient_drilldown_disabled`) либо перестаёт
      отдавать `clientActivity`; тест краснеет, если ручка снова начнёт возвращать персональные строки.
```
**Что предотвращает.** Ручка смонтирована под платформенным гейтом (`route.ts:8-16`), **org-blind** (в `pgProductAnalytics.ts:312-346, 412-451` нет предиката организации) и по контракту возвращает `clientActivity[].userId/displayName` из `platform_users` (`types.ts:161-171`, `api.md:86`). В приложении нет ни фильтра, ни редакции — единственный барьер — **отсутствие гранта** `SELECT ON platform_users` роли `app_platform_settings` (`c5a-platform-operations-runtime.sql:639-641, 796`), ничем не проверенное тестом. UI к ручке нет (`DoctorAnalyticsShell` осиротел), поэтому регрессию никто не заметит. Это прямо против рулинга владельца «персональная аналитика пациентов чужих клиник… не входит в platform view» (`OWNER_RULINGS_2026-07-15:50-63`).
⚠️ Честно: сегодня вызов, вероятно, падает на отсутствии грантов (живьём не проверял) — но код и контракт написаны на выдачу, барьер один и снимается одним грантом.
**Практика.** Платформенный оператор в мультитенантном SaaS получает агрегаты, а drill-down до персоны закрывают на уровне контракта ручки, а не на уровне прав БД «на всякий случай».

### 4) Пункт 14 — успех платежа только по серверной проверке → **Phase 4 / пунш-лист §4.6**

```
- [ ] Успешная оплата подтверждается серверной проверкой статуса у провайдера: payload callback/redirect сам по
      себе доступ не активирует; provider order ref и transaction ref имеют проверенный mapping; адаптеры с
      provider-specific контрактом (Alfa-Bank — `S4_0_S4_1_CONTRACT_INVENTORY.md:78`) не активируются до отдельной проверки.
```
**Что предотвращает.** Подделанный redirect/callback = бесплатная активация тарифа. В плане описана проверка подписи **вебхука** (`TARIFFS_PAYMENTS_ADMIN_PLAN.md:552-556`) и безопасная return-страница (`:560-564`), но ref-mapping и «не верить payload'у callback» не названы нигде живом; исходные gaps лежат в `S4_0_S4_1_CONTRACT_INVENTORY.md:72-83` — при архивации S4 ссылка на них должна остаться.
**Практика.** Ни одна PSP-интеграция не доверяет return-URL: подтверждение — серверный запрос статуса или подписанный вебхук.

### 5) Пункт 49 — отказ обычному специалисту на биллинг не доказан → **Phase 4 / пунш-лист §4.3 (переоткрыть доказательство)**

```
- [ ] Отказ обычному приглашённому специалисту доказан тестом: `GET /api/clinic/billing` → 403
      `billing_admin_required`, вкладка «Тариф и биллинг» не отдаётся; тест краснеет при снятии ролевого предиката.
```
**Что предотвращает.** Гейт есть (`api/clinic/billing/route.ts:9-11`, `app/app/settings/page.tsx:79-82, 95-96`), но **теста нет**: в каталоге `api/clinic/billing/` только `route.ts`, а комментарий `page.tsx:94` ссылается на несуществующий тест. При этом пунш-лист 4.3 помечен ✅ формулировкой «Доказательство: тест 403 для не-владельца» — галочка стоит на отсутствующем доказательстве. Ручка отдаёт суммы счетов и provider-поля (`route.ts:21-25`).
**Практика.** Доступ к финансовым данным арендатора закрывают ролью **внутри** арендатора (owner/billing admin), и это единственная роль, для которой держат явный negative-тест.

### 6) Пункт 52 — пять mock-подтверждений оплаты не закрыты тестом → **Phase 4 / #1057**

```
- [ ] Пять ручек `*/payments/mock-complete` закрыты тестом: вне development/test каждая отвечает 404;
      тест краснеет при ослаблении `isMockPaymentConfirmEnabled`.
```
**Что предотвращает.** Единственная защита — `mockPaymentGatePolicy.ts:19-21`, тестов ноль (нет ни одного `*.test.*` на `mock-complete`), а `env.ts:28` даёт `NODE_ENV` дефолт `development`; одна из ручек — **неаутентифицированная** (`api/booking/public/payments/mock-complete/route.ts:17-19`). Потеря переменной окружения = аноним помечает брони, абонементы и товары оплаченными.
**Практика.** Тестовые платёжные подтверждения закрывают allowlist по окружению **и** тестом — цена ошибки бесплатные заказы.

---

## 3. НУЖНО ПОЗЖЕ (12)

| № | Предусловие | Где ведётся |
|---|---|---|
| 3 | Базовая библиотека платформы **пуста** (`0250_c4d_platform_library_read_staff_scope.sql:41` — «live platform row count today: 0») и экрана курирования нет; нужно решение владельца, наполняем ли её вообще | Надгробие 6a.4 (часть C4D §8A) + план §2 «Вне scope» строка 189 |
| 4 | То же (композиция base library). Половина «тариф даёт base-library без покупки и грантов» **уже работает**: `pgLfkAssignments.ts:30-34` открывает platform-контент по механике `exercise_catalog`, без грантов | Там же |
| 7 | Состояние «own+base» нечем показать до наполнения base library (0 строк) | Там же |
| 19 | Выбор PSP, юрлица-получателя и модель ККТ/54-ФЗ — развилка владельца; канон §12: «прямого источника не нашлось, вопрос к бухгалтеру» | S4 §13 п.3 → надгробие; пунш-лист «Развилки»; Phase 4 фискализация `TARIFFS_PAYMENTS_ADMIN_PLAN.md:588-609` |
| 21 | Нет источников метрик (биллинг/usage) — сама проекция строится после них | #854 («аналитика, отложена до биллинга», `OWNER_PUNCHLIST_2026-07-28.md:529`) + план §2:190, §«НЕ СДЕЛАНО»:1008 |
| 24 | То же | #854 |
| 25 | PII-чекер/канарейка строятся вместе с проекцией; живой риск закрыт пунктом по №22 | #854 |
| 28 | OWNER GATE на метрики — по рулингу «точный набор метрик определяется в конце» (`OWNER_RULINGS_2026-07-15:60-63`) | #854 |
| 29 | После 28 | #854 |
| 35 | Негативы аналитики — после появления самой аналитики | #854 |
| 41 | DoD-строка про platform analytics + PII gate | #854 |
| 47 | Design/spec под выбранный PSP и legal/cash-register — та же развилка, что 19 | S4 §13 п.3 → надгробие; #844 |

---

## 4. НЕ НУЖНО (35)

**Уже реализовано в коде (7):**
- **1** — ownership-инвентаризация фактически исполнена: `owner_kind` + CHECK «`organization`⇔org NOT NULL / `platform`⇔NULL» (`schema.ts:1743` и ещё пять таблиц), opt-in `includePlatformBase` на каждом read-path, политики `c4d_platform_library_read` (`0250:44-66`). Повтор «инвентаризации» — машинерия.
- **5** — публикация clinic→platform невозможна уже сейчас: все вставки жёстко пишут `'organization'` (`pgLfkExercises.ts:718-721, 743-744`, `pgLfkTemplates.ts:649-650`), `0217_platform_lfk_ownership.sql:11-22` отказывается мигрировать NULL-строки («never promoted to platform automatically»), кода публикации нет.
- **8** — `modules/saas-billing/{ports,service,settings}.ts` существует (коммиты 27.07 `53dd848c2`/`f773c5d8c`, пунш-лист §4).
- **9** — `db/schema/saasBilling.ts` + `0259`: аккаунты, подписки (`source` = `manual|paid_subscription`, `:84,108,132`), счета с `provider_idempotency_key` (`:178,189`), provider events (`:237`).
- **11** — ключ зарегистрирован как restricted с редакцией: `system-settings/registry.ts:310`, `api/admin/settings/route.ts:120,289`, читается через `buildAppDeps.ts:749`; `booking_payment_providers` не трогает.
- **23** — org в ingest уже берётся из доверенного контекста: `pgProductAnalytics.ts:204` (`getCurrentDbPrincipalOrganizationId`), пациентский путь через SECURITY DEFINER по GUC (`0200_current_patient_product_analytics.sql:37-66`), в клиентской схеме поля org нет (`ingestSchemas.ts:4-13`). Утверждение reality-lock «ingest его не передаёт» устарело.
- **27** — «безопасный preview без объявления KPI окончательными» уже есть буквально: `app/app/(global-admin)/doctor/analytics/page.tsx:1-16` — «Аналитика платформы появится после C6», `.../clients/page.tsx:12`, `.../usage/page.tsx:9-12`.

**Уже стоит в этом же плане — нужен не новый пункт, а возврат галочки при архивации S4 (14):**
- **10** → `TARIFFS_PAYMENTS_ADMIN_PLAN.md:522-525` (и половина сделана: `pgSaasBilling.ts:143-198` пишет `source='manual'` и compatibility-проекцию в одной транзакции; открыт только mismatch-чекер).
- **12** → `:560-564` («никогда не берёт сумму/tariff/target org от клиента»). **13** → `:552-556`. **15** → `:573-576`. **20** → `:548-551` + заголовок Phase 4 «keyless-safe».
- **30** → `:618-622`. **31** → `:623-629` (плюс `/app/admin/clinics` уже показывает тариф и состояние, пунш-лист §9). **32** → `:630-632`. **33** → `:631` + DoD `:931-933`; entitlement-часть — живой открытый пункт Phase 2 `:358-363` («override A не течёт на B»). **34** → `:633-639`.
- **37** → DoD `:934-939` держит провенанс открытым явно; §0 `:66-67` уже разделяет рулинг и инженерное решение. **39** → DoD `:928-930` + 4.6. **42** → DoD `:932-933`.
- **38** → ядро (own/base приватность) закрывается предложенным 3.5; store-часть отложена владельцем (S4 §8B, все `⏳`).

**Ведётся отдельной карточкой, дублировать в §5a нельзя (9):**
- **17, 45, 46** → #844 (пунш-лист §4, «биллинг-операции»); 4.1/4.2 закрыты `9bfa4303c`, остальное — тело карточки.
- **18, 48** → #845; вкладка и ручка организации уже сделаны (4.3/4.4), остаток (история, документы, upgrade/downgrade) — карточка.
- **40** — раскол авторизации сделан (платформенная ручка + org-ручка + роль `app_clinic_billing`, `c5a-platform-operations-runtime.sql:230-273`); «reconciled ledger» = #844.
- **44** → пунш-лист §4 + #844; A/B-часть закрывается предложенным 7.5.
- **43** — мутаций биллинга не существует вовсе (только GET-ручки); чтение закрыто гейтом, доказательство — пункт по №49.
- **50** — это формулировка самого workstream #1057, а не атомарное требование (пунш-лист §4).

**Требование выполнено проверкой или уже связано другим пунктом (5):**
- **6** — живая половина = предложенный 3.5; «no copied rows/object keys» — инвариант магазина (S4 §8B, отложен), проверять нечего: platform-строк 0.
- **26** — clinic-порт уже строго single-org (`api/doctor/analytics-metric-accounts/route.ts:84` пинит org из ctx); живая половина — пункт по №22; остальное — конструирование будущего порта (#854).
- **36** — уже обязывает канон процесса: `ORCHESTRATION_BINDINGS.md:92-93` («не закрывать визуальную фичу по одному общему скриншоту… построчная runtime/screenshot matrix») и `:169,185` (единый checklist executor/audit/fixer). Чекбокс, повторяющий общее правило, — машинерия.
- **51** — проверка выполнена этим отчётом: **онлайн-оплата пациентом — mock везде** (`PatientPackagePayClient.tsx:42-58,78` «Оплатить (тест)»; то же в `PatientBookingPayClient`, `PatientProductPayClient`, `PublicBookingPayClient`, `PublicProductPayClient`); doctor memberships закрыты фактом (S4 §10, все `[x]`).
- **53** — преамбула «⛔ ПЕРЕД СТАРТОМ ЭТАПА — перечитать, не по памяти» стоит в **каждом** разделе плана и пунш-листа; отдельный чекбокс «перечитать рулинги» ничего не добавляет.

---

## 5. «Не смог оценить» — 0. Остаточные наблюдения (не чекбоксы)

1. **Два чтения `media_files` без предиката организации**: `pgMediaTranscodeJobs.ts:27-38` (внутренний роут транскодинга) и `pgMediaFileIntakeResolve.ts:33-46` (авторизация по `uploaded_by`, то есть по пользователю, не по организации). Раскрытия контента не даёт, но при неприменённом FORCE RLS это единственные пути, опирающиеся на защиту БД, которой сейчас нет.
2. **`GET /api/clinic/billing` отдаёт клиенту `providerCheckoutUrl`, `providerInvoiceRef`, `providerIdempotencyKey`** (`route.ts:21-25`, `saas-billing/ports.ts:25-42`) — в пределах своей организации, но полезнее не отдавать.
3. **`CLINIC_TEAM_FAIL_CLOSED_SEAT_BASELINE = 1`** жив в двух местах — TS (`org-entitlements/types.ts:137`, `service.ts:297-309`) и SQL (`organization-member-invites-rls.sql:309`). Пункт 2.6a упоминает только TS; правки должны идти одним куском, иначе accept-путь останется с константой.
4. **Метод.** Отчёт read-only, файлы не менялись. Для проверки кода я запускал read-only исследовательских субагентов (без прав записи), а не через `tools/orch-launch.sh` — это не пайплайн-воркеры, но фиксирую, чтобы аудитор не считал это скрытым обходом правила.