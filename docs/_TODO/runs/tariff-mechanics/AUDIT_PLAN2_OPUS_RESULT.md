Both agents are in. Here is the audit.

---

# VERDICT: **FAIL**

Механизм §4a описан верно и порядок этапов (механизм → одна дверь → механики) правильный. Но план нельзя брать в работу в текущем виде: правило, которое §5a объявляет выброшенным, продолжает **исполняться** в коде единственного chokepoint'а и не имеет ни одного пункта на снятие; главное требование владельца («ни он не видит раздела, ни его клиенты») сегодня не исполняет ничто, а адаптер, который должен был это делать, — пустая заглушка; и тот же файл в Phase 4 держит вторую, конфликтующую «действующую лестницу» 7/21 день без пометки SUPERSEDED, что прямо запрещено `plan-authoring-execution-standard.mdc:29-30`.

---

## A. Матрица «слова владельца → где реализовано → статус»

| Слова владельца (дословно) | Где в плане | Статус |
|---|---|---|
| «ты вообще не должен решать что ограничивать… дай мне механизм… какой период терпения… какой период read-only» | §5a 2.1–2.3, 2.7; канон §4a | **ИСКАЖЕНО.** Механизм описан верно, но в **том же файле** строки 478 и 490 объявляют «Итого действующая лестница: 7 дней grace с 3 попытками → 21 день `read_only` → `blocked`», а строка 486-487 сама признаёт, что 7/3 — «**предложение агента**». Два несовместимых active-требования в одном документе |
| «если у специалиста нет в тарифе разминок и cms — то **ни он не видит в кабинете этого раздела, ни его клиенты не увидят** у себя разминок и статей его» | §5a 2.4, преамбула Этапа 4 («на видимости раздела у обеих сторон») | **ИСКАЖЕНО/НЕ ЗАКРЫТО.** В прозе есть, в чек-листе нет ни одного пункта, снимающего то, что этому мешает: ранний `return` для чтения (`requireEntitlement.ts:49`), заглушка `requireEntitlementForPage` и ~20 исключений реестра с обоснованием «read route»/«read action» |
| «как настрою то и входит, ты мне главное дай выключатели корректные» · «сами цифры — тебя не касаются» | §5a Этап 5, 2.1, 2.7 | **ИСКАЖЕНО.** Живы числа, выбранные агентом: 80 % (`service.ts:226`), `CLINIC_TEAM_FAIL_CLOSED_SEAT_BASELINE = 1`, `MECHANIC_DEFAULT_ENABLED`, `no_trial → false`, seed 7/3/21 в `0259`. Ни одно не названо в §5a |
| «главное — не переусложнить. Делать НЕОБХОДИМО И ДОСТАТОЧНО» | «Как проверяем», Этап 2 | present |
| «перевести на read-only специалистов сверх тарифа… **кого перевести — выбирает администратор клиники, не выбрал → последние добавленные, выбор можно менять**» (владелец: «делай так») | — | **MISSING.** §5a 5.1 — только «база и цена за дополнительного; превышение разрешено и оплачивается». Механизма выбора нет нигде |
| «поддержка — тоже рубильник» | — | **MISSING.** Канон §4:131 отсылает в `OWNER_PUNCHLIST §11`, а §11 (пп. 11.1–11.6) рубильника не содержит. Пункт не отслеживается ни одним чек-листом |
| CMS: «иметь свой сайт — не путать с "иметь свою страницу профиля и страницу букинга + виджеты для сайта"» | — | **MISSING.** 4.7 гейтит CMS, но границы (профиль, `/book/{slug}`, `embed.js` остаются при выключенной CMS) нет ни в одном пункте |
| «Курсы — возможность, не число» | 4.7 | present |
| «Рассылки — вкл/выкл саму возможность»; каналы клиника настраивает сама | 4.8 (по контракту #1071) | present |
| «шаблоны программ и лфк давай тоже не резать» | 6.4 | present |
| «а вот филиалы — числом» | 5.3 | present |
| число пациентов — проверка только при появлении нового | 5.2 | present |
| «приложение не трогай… карточка пациента… тоже не трогай» | 2.5, 6.4 | present |
| «Сегодня» — админ не видит блок при выключенной настройке | 4.9 | present |
| UTM — «пусть будет в скопе аналитики для клиник» | 4.4 («одна механика, не две») | present |
| понижение — со следующего цикла и только после уборки | 5.6 | present |
| не ограничиваем никогда: экспорт, 2FA, журнал, экстренная помощь, уведомления | 2.5 + 6.4 | present (классом) |

---

## B. Что до сих пор решено за владельца или зашито

1. **Лестница 7 дней grace → 21 день read-only → blocked** — `TARIFFS_PAYMENTS_ADMIN_PLAN.md:478,490`. Решил: агент (7/3 — «предложение агента», строка 486), владелец сказал только «Только чтение ещё на три недели». Форма поля: те же три поля §4a на уровне системы; строки 474-491 должны получить `SUPERSEDED — 30.07, replaced by §5a/§4a`.
2. **Seed `graceDays 7, chargeAttempts 3, readOnlyDays 21`** — `db/drizzle-migrations/0259_saas_billing_foundation.sql:238-242`, тип `SaasBillingLifecyclePolicy` (`modules/saas-billing/settings.ts:5-9`). Значения записаны в БД миграцией, потребителя нет. Решил агент. Форма поля: те же поля конструктора, без seed'а.
3. **Порог предупреждения 80 %** — `org-entitlements/service.ts:226` (`currentUsage * 5 >= quota.limit * 4`) и канон §5.4 «Предупреждения на 80% и 100%». Решил агент (следом за `QUOTAS_RESEARCH:742` «Safe default»). Форма поля: порог предупреждения в тарифе, рядом с числом.
4. **«Два предупреждения»** — канон §1:31, §5.2:198, §7:240. Владелец назвал число, но §4a перечисляет только три поля (терпение, read-only, конечное состояние) — **поля под количество предупреждений нет**, значит число неизбежно станет константой. Форма поля: четвёртое поле лестницы.
5. **`CLINIC_TEAM_FAIL_CLOSED_SEAT_BASELINE = 1`** — `types.ts:137`. Решил агент. Форма поля: `includedSeats` обязателен у тарифа, отсутствие → отказ, а не «1».
6. **`MECHANIC_DEFAULT_ENABLED`** — `types.ts:119-129`, шесть механик выключены списком в коде. Решил агент; канон §8 на этом строит «механики владельца». Форма поля: значение тарифа/исключения, а не литерал.
7. **`access.source === 'no_trial' ? false`** — `service.ts:172`. Это ровно «что происходит, когда X выключен», выбранное агентом: нет триала → всё выключено, минуя лестницу. Форма поля: конечное состояние системного уровня.
8. **`start_event` зафиксирован как `organization_provisioned`** — CHECK в `saasEntitlements.ts:150-153` + `service.ts:81`. Системный уровень лестницы владелец описал «при неоплате», но якорь другого события запрещён схемой. Решил агент.
9. **Брендирование: «нейтральное платформенное оформление, но само сообщение уходит»** — канон §5.7. Слов владельца нет; это выбор поведения при выключенной механике, отличный от §4a («раздел не показывается»).
10. **«Деньги клиники не блокируем никогда» (канон §5.5) против 4.7**, где «приём оплат» подчиняется лестнице и в конечном состоянии исчезает. Развилка не разрешена.
11. **`TariffQuotaMap = Partial<Record<'files', TariffQuota>>`** (`types.ts:110`) + `normalizeQuotaMap` бросает на любом другом ключе (`service.ts:39`). Тип запрещает владельцу поставить число пациентам и филиалам — решение агента, а не настройка.
12. **Живые предписания в справочнике, противоречащие канону:** `QUOTAS_RESEARCH_2026-07-28.md:595,749` рекомендует термин «Период лимита», запрещённый каноном §7:231; `:702` — «не активировать downgrade, пока seats не приведены к target» против «превышение разрешено и оплачивается» (§5.2); `:319` — «Safe default до этого — unlimited patients»; `:742` — 80/100 %; `:16-17,342` — четырёхклассовая таксономия; `:134,201,219,347,349,365,534,554` — семейство «чтение/export/delete всегда доступны», то есть отозванное правило. Файл объявлен «справочником без решений», но содержит два листа «РАЗВИЛКИ ВЛАДЕЛЬЦУ» в форме «Рекомендация / Safe default».
13. **`OWNER_REVIEW_2026-07-18.md:33,95` и `OWNER_DECISION_PACKET.md:69,73`** до сих пор утверждают «warning на 80 %, hard-block только нового growth/consumption» — отозванное каноном §5.1 правило, доступное по цепочке авторитета, которую сам план цитирует в §0a.

---

## C. MUST FIX — корректность и полнота

**C1. Отозванное правило «чтение не ограничиваем никогда» не выброшено, а исполняется.** `requireEntitlement.ts:42-49`:
```ts
if (access === 'read') return { ok: true };
// A tariff mechanic controls writes only. Existing clinic/patient data remains readable
// and exportable after a mechanic is switched off; resolving it here would turn a read into a hide.
```
Строка 827 плана утверждает, что правило выброшено, но это проза без чекбокса. Следствие: `requireEntitlementForRead`/`...ReadAction` **не могут отказать никогда**, а реестр помечает ими 7 точек, включая пациентскую `courses.patient.list` (`protectedActionRegistry.ts:89-98`) — ровно ту, которой владелец требовал «его клиенты не увидят». Нужен атомарный пункт: снять ранний return, снять комментарий, перевести ~20 исключений «read route»/«read action» в решение по лестнице, с тестом на скрытие у пациента.

**C2. Механизм выбора «кого перевести в read-only» отсутствует.** Владелец утвердил его словом «делай так» (канон §1:31). В §5a 5.1 его нет. Пункт нужен: выбор администратором клиники, дефолт «последние добавленные», изменяемость выбора.

**C3. Рубильник поддержки не отслеживается нигде.** Канон §4:131 отправляет его в `OWNER_PUNCHLIST §11`; §11 (677-687) его не содержит. Либо пункт в §5a, либо явный пункт в §11 со ссылкой — иначе owner requirement исчез во «вне scope», что запрещено `plan-authoring-execution-standard.mdc:49-50`.

**C4. Граница CMS не закрыта.** «Профиль, страница записи и виджеты остаются» (канон §1:25-26, §7:245) не имеет пункта. При выключенной CMS `app/book/embed.js/route.ts` и публичная страница записи должны выжить — сейчас это не проверяется ничем.

**C5. Статусные аннотации Phase 4 ложны.** План (строки 499-501, 510-513, 537-539) утверждает: «`find apps/webapp/src/modules -iname "*saas-billing*"` — пусто», «0 совпадений», «только dormant-заготовка». Фактически существуют `apps/webapp/src/modules/saas-billing/settings.ts` (163 строки, разбор провайдеров, redaction, retain-секретов) и миграция `0259_saas_billing_foundation.sql`. Нарушен `ORCHESTRATOR_CHECKLIST.md:11` («галочку ставит оркестратор, проверив сам») и `plan-authoring-execution-standard.mdc:66-68`.

**C6. Definition of Done не закрывает план.** Шесть пунктов DoD не покрывают Этап 6.1 (витрина у клиники), 6.2 (отчёт по всем организациям), 6.3 (порядок включения) и весь Этап 6a (сведение документации, включая открытый 6a.4). По `plan-authoring-execution-standard.mdc:45-46` каждый пункт обязан входить в DoD либо иметь трассируемое основание для отмены.

**C7. Этап 4 пропускает две механики.** Внешний календарь и дневники пациента появляются только в 3.4 («переведены на порт»), но не проходят чек-лист Этапа 4 — «видимость раздела у обеих сторон, видимый отказ, строка в реестре, тест со снятием защиты». Дневники — пациентская поверхность, то есть ровно тот случай, о котором говорил владелец.

**C8. Реестр защищённых точек врёт уже сейчас, и 3.3 сформулирован мимо.** `DECLARED_NO_SURFACE.branding = 'code-search: no branding write action'` (`protectedActionRegistry.ts:1266`) — при существующем `'use server' saveOrgBranding` (`app/app/settings/brandingActions.ts:33`); то же для `exercise_catalog` при `doctor/exercises/actions.ts:25,39,58,84`. Эти записи работают как подавители проверки покрытия. Пункт 3.3 говорит только про «исключения, прикрывающие реальную запись» — формулировка не ловит ложные `DECLARED_NO_SURFACE` и не ловит исключения-«чтения».

**C9. Нет приёмки владельца в середине плана.** Единственная — 7.4, в самом конце. `plan-authoring-execution-standard.mdc:118` требует приёмку в СЕРЕДИНЕ. Естественная точка — после Этапа 2 (владелец смотрит поля лестницы до того, как 28 механик на неё сядут).

---

## D. MUST FIX — инженерная проработка

**D1. У лестницы нет якоря времени, и 2.3 запрещает его завести.** 2.3 объявляет входы: тариф, персональное исключение, коммерческое состояние — «выдуманных локальных флагов нет». Но «терпение N дней» требует ответа на вопрос «с какого момента». Сегодня единственный якорь в данных — `saas_organization_trials.started_at/ends_at/grace_ends_at` (`saasEntitlements.ts:172-174`), и он существует только для триала; `start_event` зажат CHECK'ом в `organization_provisioned` (`:150-153`). Момент, когда механику убрали из тарифа или когда не прошла оплата, нигде не фиксируется. Пункт 2.4 («предупреждение **с датой**») невыполним. Нужен явный пункт: где хранится дата входа в ступень для пары (организация, механика) и для системного уровня.

**D2. `requireEntitlementForPage` — пустая заглушка, вызываемая четырьмя страницами.** `requireEntitlement.ts:140-146`: `void ctx; void mechanic;` при JSDoc'е «fail-closed `notFound()`». Вызывают: `app/app/doctor/courses/page.tsx:59`, `courses/[id]/page.tsx:22`, `courses/new/page.tsx:28`, **`app/app/patient/courses/page.tsx:31`**. То есть заявленная защита видимости у пациента не делает ничего. При этом DoD плана (строка 842) утверждает: «`checkEntitlement()` — единственный внутренний резолвер за 6 адаптерами (… + Page)» — пруф неверен. Нужен пункт на реализацию адаптера + тест, краснеющий при его опустошении.

**D3. «Реестр перестаёт врать» (3.3) не имеет исполнимого механизма.** Проверка вызова guard намеренно удалена 29.07 решением владельца («сноси машинерию»), `check-s4-entitlement-coverage.ts:64-68`, потому что матчила текст исходника. Следствие уже реализовалось: `protectedActionRegistry.ts:569-628` объявляет шесть действий clinical-tests под `requireEntitlementForMutationAction`, а `app/app/doctor/clinical-tests/actionsShared.ts:81,216,254` содержит только `requireDoctorWorkspaceContext()` — **проверено мной лично, вызова entitlement в каталоге нет вообще**. 3.2/3.3 обязаны назвать замену, совместимую с `tests-check-behaviour-not-circumstances.mdc:40-48,136-138`: разбор дерева TypeScript либо поведенческий тест, но не regex по тексту.

**D4. Интегратор — вторая дверь в ту же базу, и «один порт» её не закрывает.** `apps/integrator/package.json:34-53` не зависит от webapp; импортов `requireEntitlement`/`org-entitlements` нет. Он пишет `public.*` напрямую под эскалацией принципала (`writePort.ts:137-140` — `runWithOrganizationPrincipal`), поэтому RLS не подстрахует. Правило entitlement реализовано там **второй раз, сырым SQL, ровно для одной механики**: `writeDiaryLfkDirect.ts:172-235` с именем механики в двух литералах (`:210`, `:218`). Не гейтится: `public.reminder_rules` (`writeReminderRulesDirect.ts:152`, механика `warmups`, которую webapp считает защищённой — `protectedActionRegistry.ts:931-939`), `public.broadcast_audit` (`outgoingDeliveryWorker.ts:205,221,749`, механика `mailings`). §5a называет бота в списке дверей (проблема №3), но ни один пункт Этапа 3 не распространяет порт на второе приложение.

**D5. Подписка на пуш создаёт данные механики мимо ворот.** `app/api/patient/web-push/subscribe/route.ts:27` — единственный гейт `requirePatientApiBusinessAccess`; строка 98 вызывает `ensureWarmupsReminderOnFirstPwaPush`, который на `ensureWarmupsReminderOnFirstPwaPush.ts:72-84` создаёт reminder-правило `warmups`. Роут отсутствует и в mappings, и в exemptions. Это же — открытый MUST FIX №1 последнего аудита слайса A (`STAGE5_SLICE_A_AUDIT_R3_RESULT.md:17`, вердикт FAIL).

**D6. Этапы 5.2 и 5.3 не могут работать как написано.** `TariffQuotaMap` допускает только `files` (`types.ts:110`), `normalizeQuotaMap` бросает `tariff_quota_mechanic_invalid` на любом другом ключе (`service.ts:39`). Хуже: `entitlementsFromSnapshot` для классов `запас`/`объём` возвращает `false`, когда тариф назначен, но числа нет (`service.ts:162-167`) — то есть у любой организации с тарифом `patient_count` и `branches` сейчас **выключены**, и включить их владелец не может ничем. Нужен пункт на расширение типа/схемы до 5.2/5.3.

**D7. Витрина 6.1 не на чем стоять.** `getEnforcedQuotaUsage` в боевом адаптере отдаёт только `courses` и `clinic_team` (`pgOrgEntitlements.ts:227-238`), а `resolveOrgQuotaProjections` пропускает лишь классы `места` и `объём` (`service.ts:196`) — при этом `files` в usage не приходит вовсе. Практически единственная возможная проекция — места. «Использовано из включённого **по всем числам**» требует правки порта, которой в плане нет.

**D8. Инвентарь семейств дырявый по построению.** `PROTECTED_ACTION_FAMILIES` — все четыре с `recursive: false` (`protectedActionRegistry.ts:1088-1113`), при собственном комментарии «a new exported action file cannot evade the inventory». `[redacted-token].ts` (5 экспортируемых пишущих действий) в скан не попадает. 3.2 опирается ровно на эту гарантию.

**D9. Скрытая зависимость в порядке этапов.** 3.4 берёт слайс A как «уже сделанные точки контроля», но последний вердикт слайса A — `STAGE5_SLICE_A_AUDIT_R3_RESULT.md:1` **FAIL** с двумя достижимыми обходами `warmups` и ложным исключением `toggleReminderCategory`. Переносить на порт незакрытый слайс — значит перенести и обходы; пункт должен требовать сначала закрытия R3.

---

## E. Комплект документов

**Не сведён.**

- **Внутри плана два конфликтующих active-требования:** §5a/§4a (лестница — поля владельца) против строк 474-491 («действующая лестница: 7 дней grace → 21 день `read_only` → `blocked`»), без пометки SUPERSEDED. Прямое нарушение `plan-authoring-execution-standard.mdc:29-30`.
- **§0a:122-126 устарел:** «Phase 1-2 сознательно строят только бинарный `entitlement_required` — этого достаточно для scope #751 (тариф либо даёт механику, либо нет)». §5a Этап 2 это отменяет; пометки нет.
- **§4 (таблица реестра) устарела:** 14 механик, статусы «❌ не гейтится», датировано 2026-07-17. В коде 28 ключей (`types.ts:54-84`). DoD рядом говорит «реестр вырос до 15». Трёх разных чисел в одном файле быть не должно.
- **Справочник не нейтрален:** `QUOTAS_RESEARCH_2026-07-28.md` при заявлении «решений здесь нет» держит два листа «РАЗВИЛКИ ВЛАДЕЛЬЦУ» и живые рекомендации, противоречащие канону (см. B-12). Это и есть «старые ошибочные утверждения», которые владелец просил убрать.
- **Ложные статусы Phase 4** (C5) — «старые ошибочные утверждения» уже внутри действующего плана.
- **6a.4 обработан честно** — предупреждение в шапке `SAAS_S4_TARIFFS_STORE_ENTITLEMENTS.md:5-9` реально стоит, формулировка точная («чтение тоже ограничивается скрытием раздела»), 83 открытых чекбокса подтверждаются подсчётом. Скрытия нет.
- **6a.3 подтверждён:** дублирующий чек-лист из `OWNER_PUNCHLIST §10` действительно удалён, остались только `10.1`/`10.2` с галочками. Мелочь к правке: заголовок §10 «14 из 15 не проверяются никем» устарел, и два баннера расходятся в диапазоне (10.3–10.18 против 10.3–10.19), причём ни один диапазон в файле не существует.
- **Отозванное правило живёт за пределами комплекта:** `OWNER_REVIEW_2026-07-18.md:33,95` и `OWNER_DECISION_PACKET.md:69,73` («hard-block только нового growth», 80 %), при том что `SAAS_S4:14-15` называет `OWNER_REVIEW_2026-07-18` «последней обязательной delta».

---

## Что в плане верно — не ломать

1. **Порядок «механизм → одна дверь → механики»** правильный и обоснован причиной, а не вкусом.
2. **Раздел «Проблема, ради которой это делается»** стоит первым — выполняет `ORCHESTRATOR_CHECKLIST.md:20-23`, и все четыре пункта проблемы подтверждаются кодом (в частности «правило живёт в десятках мест»: `warmupsContentMutationGuard.ts`, `promoMaterializationGate.ts`, `patientWarmupReminderMutationGuard.ts`, `readMaterializationGate.ts` — четыре самодельных гейта).
3. **3.1: числовая проверка остаётся внутри пишущей транзакции под блокировкой**, а не в воротах — совпадает с работающим образцом `pgOrganizationInvites.ts:110-120` (`pg_advisory_xact_lock`).
4. **5.7: доказательство гонки на каждое число + пересчёт под принципалом**, и FORCE-RLS-требование зафиксировано и в «Как проверяем», и в каноне §2 — это ровно тот риск, который иначе даёт молчаливое превышение.
5. **5.8: контракт деплоя под `SECURITY DEFINER`** со счётчиком и двумя контрактными тестами — на этом дважды падал деплой.
6. **5.5: освобождение объёма как предусловие включения предела** — редкий случай, когда план отказывается включать механику до появления штатного способа освободить место. Сохранить дословно.
7. **7.2: гейт слияния** (номер миграции только при слиянии, тесты обеих сторон после ручного конфликта, снос `.next/types`) — покрывает три реальных прошлых падения.
8. **«Как проверяем»** честно ссылается на `check-s4-entitlement-coverage.ts:64-68` и не приписывает чекеру гарантию вызова ворот — прошлый аудит ловил ровно это.
9. **6.4: проверка, что лишнего не появилось** (у критичных механик нет ни лестницы, ни числа) — правильный обратный контроль.
10. **Этап 1 закрыт по-настоящему:** класс механики типизирован, недопустимые комбинации не компилируются (`types.ts:16-47`, `TariffQuotaMap`), 28 ключей в реестре — сверено с кодом.

---

## Не смог проверить

- **Живое поведение на DEV/TEST** — миссия read-only; ни одного теста, миграции или прогона не запускал. Все выводы — из кода и документов.
- **Применена ли миграция `0259` на test/dev** — читал только файл; seed 7/3/21 может ещё не лежать в базе.
- **Второй аудитор (Sol)** — `PLAN_AUDIT_2_RESULT.md` в `runs/tariff-mechanics/` отсутствует, сверить нечего.
- **83 чекбокса `SAAS_S4`** построчно не читал — только шапку и разделы 697-719; вывод по 6a.4 сделан по наличию баннера и совпадению счётчика.
- **Эффективность `section.isVisible`** как фактического заменителя гейта в `ensureWarmupsReminderOnFirstPwaPush.ts:59` — поведенчески не проверял; классифицирую как обход по отсутствию entitlement-вызова, а не по доказанному сценарию.
- **Полнота обхода интегратора** — считал по grep'у `INSERT/UPDATE/DELETE public.*`; писать он может и через хранимые функции, которые этот шаблон не ловит.