# Хендофф оркестрации Track D — 2026-08-20, вечер

> ⚠️ **ПРОДОЛЖЕНИЕ — `TRACK_D_ORCHESTRATION_HANDOFF_2026-08-21.md`.** Состояние веток и «что крутится сейчас»
> здесь УСТАРЕЛО. Живыми остаются разбор ошибки с грантом (§1) и грабли дня (§4).


Кому: следующему оркестратору `#987` (Opus — оркестратором может быть только Opus).
Авторитет работы: `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, §2.2 «Порядок исполнения Track D».
Этот файл — не замена плану. План — единственный источник «что делать» и «что готово». Здесь только то, чего
в плане нет: состояние на момент передачи, что крутится прямо сейчас, и грабли, на которые я наступил.

---

## 1. Первое, что надо знать: я ошибся, ошибка исправлена, урок ниже

**Я приземлил грант, который не работает и расширял поверхность прав.** Коммит `4bbbda02f` («D6») давал
`app_patient` колоночный INSERT на `integrator.direct_public_write_retries`. Независимый аудит (sol, живая
rollback-проба на `bcb_webapp_dev`) доказал: у таблицы включён FORCE RLS, политика `rev10_direct_business_10`
действует на **все** команды и пускает только `app_integrator_request` и `app_operational_delivery_worker`.
Временный GRANT под `app_patient` дал **42501**. Грант не чинил ничего.

Убрано ревертом `72e874f79` (`relation-access.test.mjs` 41/41 после).

**Как я это допустил — читать внимательно, это ловушка для любого оркестратора:**
я «перепроверил своей рукой» — но проверил **декларацию** (тест, что права объявлены как задумано) и прочитал
код. Живой INSERT под настоящим принципалом я не выполнил. Это ровно тот класс проверки, который владелец
запретил дословно: «нам не нужен тест, который проверяет, как настроены права; нам нужно проверять, что
функция работает». И правило 5.1.6 самого плана говорит то же: зелёные тесты здесь ничего не доказывают,
доказательство — живой прогон. **Я применял это правило к воркерам и не применил к себе.**

Второе нарушение в том же коммите: правило владельца «никогда не расширяй права рантайм-роли; недостающее
право — это НАХОДКА». Грант вместо находки — уже неверный ход, независимо от того, работает он или нет.

**Настоящий дефект остался открытым:** путь ретрая `writePort.ts` на неудачной `reminders.rule.upsert` входит
в `enqueueDirectPublicWriteRetry` под принципалом `app_patient`, которому там не место. Это находка о неверном
принципале, а не задача выдать право. Лечится сменой принципала на стороне кода, а не декларацией.

---

## 2. Что крутится прямо сейчас

**Воркер `d10a-journal-worker2-20260820`**, ветка `wt/d10a-journal-nonqueue-20260820`, клон
`/home/dev/dev-projects/bcb-wt-d10a-journal-nonqueue-20260820`, модель `gpt-5.6-sol` / high.
Бриф: `<scratchpad>/brief-d10a-journal.md`. Задача — реализовать решение **Р-D10a-2** (см. §3).
На момент передачи в клоне уже есть новая миграция
`20260820T185707_the_delivery_journal_accepts_a_nonqueue_attempt.sql`, правки `function-census.ts`,
`c4-operational-runtime.sql`, `operatorDeliveryAttempts.ts` и его интеграционного теста.

**Что сделать, когда он закончит:**
1. Перепроверить его числа СВОЕЙ рукой по всей затронутой области, а не по его выборке (см. §4, грабля 3).
2. Проверить, что в `c4-operational-runtime.sql` появились ТОЛЬКО `ALTER`/`REVOKE`/`GRANT` и НЕ появилось тело
   функции — это стережёт `node scripts/check-c4-migration-owned-function-bodies.mjs`, прогнать обязательно.
3. Проверить, что в миграции нет `GRANT`/`REVOKE`/`CREATE POLICY` — права только в генераторе.
4. **Применить миграцию на именованной DEV самому** (воркерам применять запрещено) и доказать живьём:
   письмо оператору об инциденте и подтверждение записи пациенту обязаны дать строку в
   `public.notification_delivery_attempts`; клиника проставлена там, где вызывающий её знает, и NULL только
   для платформенного алерта. **Без этого живого доказательства PASS не ставить** — см. §1.

---

## 3. Состояние Track D на момент передачи

`feat/doctor-ui-rebuild` = `72e874f79`. Приземлено сегодня по разрешению владельца («все что можешь свое —
вливай»), полный CI после приземления не гонялся по его же прямому распоряжению:

| что | merge |
|---|---|
| уборка мёртвого кода + сверка C4-файла с генератором | `40955951b` |
| D10 stage 1 — снос транспорта проекции в интеграторе | `7ae0d8a4b` |
| D10 stage 2 — снос потребителей (маршрут, CSRF, `events.ts`, projection-слой operator-health) | `6824ea5af` |
| D6 «фикс гранта» | `12d458c63` → **отменён** `72e874f79`, см. §1 |

**D10 — остаток два пункта** (подробности в плане, пункт D10):
1. Живой прогон readiness на TEST для `deploy/host/assert-c4-operational-runtime-ready.sh`: воркер вырезал
   оттуда пробы по `projection_outbox`, и проба роли `app_operational_diagnostic` теперь не утверждает ничего.
   По коду это не оценить — нужен живой прогон.
2. DROP `integrator.projection_outbox` миграцией, вместе с Drizzle-декларацией
   (`infra/db/schema/integratorQueues.ts`). Строго последним.

**D10a — решение принято, реализация в работе.** Решение **Р-D10a-2**: канонический журнал обязан принимать
попытку без строки очереди. Довод, закрывающий альтернативу «всё через очередь»: алерт о сломанной доставке не
имеет права зависеть от сломанной доставки. Измеренное основание — в плане, блок 🔴 БЛОКЕР D10a: за 30 дней в
`public.notification_delivery_attempts` ноль успешных email и ноль SMS, при живых legacy-строках.

**Дальше по §2.2:** D10a + D16 + D30 — одна работа. D30 честно упирается в реальные будущие даты напоминаний
(~29.08), текст плана это уже отражает.

---

## 4. Грабли этого дня — то, ради чего стоит читать этот файл

1. **Гейт `land` отказывает по делу, не мешай ему.** Он требует строку вердикта в
   `NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` на КАЖДЫЙ коммит ветки, чистое главное дерево на `feat`, и наличие
   головы `feat` в клоне. Ручной `git merge` в `feat` блокирует hook — только `tools/orch-launch.sh land`.
2. **Вопрос воркера — не вопрос владельцу.** Я передал владельцу три «развилки» воркера не проверив; проверка
   по коду сняла все три (одна была про мёртвый код с нулём вызовов). Владелец был в ярости и справедливо.
   Правило теперь зашито как 5.1.10 в §2.2, а список закрытых развилок — таблицей там же, чтобы не всплывало.
3. **Числа воркеров не совпадают с реальностью систематически.** За день: один отчитался зелёным при exit 2;
   другой написал «vitest exit 0, 31 tests», сломав тест, который в его узкую выборку не попал; третий назвал
   «typecheck exit 2» из-за моих неполных симлинков (а вот он как раз был честен). Вывод: гонять самому и по
   ВСЕЙ области, где воркер правил файлы.
4. **`node_modules` в новом worktree.** Симлинки нужны в корень, `apps/webapp`, `apps/integrator` И в каждый
   `packages/*` — иначе typecheck врёт «Cannot find module». Плюс дописать `node_modules`, `*/node_modules`,
   `**/node_modules` в `$(git rev-parse --git-path info/exclude)`, иначе симлинки видны как незакоммиченное и
   launcher откажет.
5. **Имя клона задаёт путь.** `orch-launch.sh worker <клон> …` ищет `bcb-wt-<клон>`; каталог обязан называться
   ровно так.
6. **Бриф обязан содержать `## Источник оракула`, сразу следующую строкой цитату в «ёлочках» (ОДНОЙ строкой,
   без пустой строки после заголовка) и упоминание `AGENTS.md`** — иначе launcher откажет.
7. **`--effort` — это minimal|low|medium|high|xhigh|max.** `worker-hard` — значение `ORCH_JOB`, не effort.
8. **Каталог миграций подрезан** (12 файлов, старший — вчерашний). Отсутствие миграции в нём НЕ доказывает,
   что у объекта нет источника в репозитории. Я чуть не объявил живую функцию бесхозной.

---

## 5. Чего делать НЕЛЬЗЯ

- **Не вливать `wt/d10a-scope2-20260820`** — ветка удалена полностью (worktree, локальная ветка; на удалённом
  её не было). Правка ломала релиз-гейт: старый источник даёт 100% покрытие цели при расхождении 1.2%, новый —
  23%, гейт считал бы ~77% расхождения и врал. Причина записана в плане, блок «⛔ ОТКЛОНЕНО 20.08».
- **Не поднимать заново развилки из реестра закрытых** (§2.2 WORK_ORDER.md, таблица «⛔ Реестр закрытых
  развилок»). Их пять, все сняты проверкой.
- **Не трогать чужие ветки:** `wt/doctor-patient-header-tabs2-20260820`, `wt/global-admin-video-access-20260820`,
  `wt/video-duration-20260820` — их сводит владелец сам.
- **Не выдавать права рантайм-ролям** (см. §1). Недостающее право — находка в план, а не грант.
- **Не писать GRANT/REVOKE/CREATE POLICY в миграции.** Только генератор `deploy/postgres/privileges/`.
- **Воркерам не применять миграции.** Применяет и доказывает оркестратор, на именованной DEV/TEST.
- **ПРОД не трогать.** Он на другом сервере.
