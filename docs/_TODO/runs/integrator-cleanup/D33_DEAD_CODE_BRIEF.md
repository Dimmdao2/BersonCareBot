# D33 + D31-часть — вырезать мёртвое: заглушка Instagram и провайдер бэкфила
(run: worker-d33-dead-code)

## Решения владельца 31.07

> «делать API для VK, инсту удалять» (развилка №4 карты)

Развилка №9 снята фактом, а не решением: скрипта `stage6-historical-time-backfill.ts` в репозитории **уже
нет**, остался только его провайдер соединений `infra/scripts/stage6HistoricalBackfillPoolProvider.ts`, и у
него **ноль импортёров** — проверено поиском по `apps/integrator/src`.

## Что вырезать

1. **Instagram целиком** — `apps/integrator/src/integrations/instagram/*` (дескриптор и плейсхолдеры,
   доставки нет). Плюс все упоминания: реестр каналов, типы кода канала, конфиг, контент, доки.
2. **`stage6HistoricalBackfillPoolProvider.ts`** — мёртвый провайдер. Проверить, не сторожит ли его гейт
   доступа к базе (`scripts/check-db-chokepoint.mjs` содержит список именованных провайдеров) — если да,
   снять оттуда запись тем же коммитом, иначе гейт покраснеет.

**VK НЕ трогать** — он остаётся и будет доводиться до настоящего канала отдельной работой (D31). Случайно
вырезать его вместе с Instagram — самая вероятная ошибка этого прогона.

## Доказательство недостижимости (обязательно, до реза)

Правило репозитория, выученное на откате прошлого реза (вырезали живой обработчик, тесты промолчали):
**недостижимость доказывается тремя источниками** — контент сценариев обоих каналов, код обоих приложений,
смысловой поиск (`node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`). Приложить по каждому
удаляемому куску.

Отдельно проверить **базу и настройки**: не хранится ли где-то код канала `instagram` в данных (привязки
пользователей, настройки клиники, шаблоны). Если хранится — рез кода не должен ломать чтение старых строк;
сказать об этом в отчёте.

## Гейт приёмки

- Полный прогон интегратора (сейчас 154 теста) и вебаппа по затронутым модулям — до и после.
- `pnpm run lint` и `typecheck` обоих приложений (гейт доступа к базе гоняется в линте — он и покажет, если
  провайдер был в списке).
- Тест-сторож: код канала `instagram` больше не принимается реестром каналов; обязан краснеть, если его вернуть.

## Границы

- Не трогать VK, MAX, Telegram, веб-пуш, почту, смс.
- Не «чистить заодно» соседнее мёртвое: область — ровно два куска выше.
- Не изобретать требований: чего нет в решении владельца — строка в «развилки».

## Правила тестов — прочитать целиком

`.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/webapp-tests-lean-no-bloat.mdc`.

## Приёмка

`docs/_TODO/runs/integrator-cleanup/D33_REPORT.md`: что удалено (файлы, строки), доказательство
недостижимости по трём источникам на каждый кусок, прогоны, разделы «развилки» и «чего не смог».
Писать по ходу; фоновых задач и подзапусков не использовать.
