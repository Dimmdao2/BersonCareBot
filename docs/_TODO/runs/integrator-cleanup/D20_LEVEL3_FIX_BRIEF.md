# D20 уровень 3 · закрыть находки аудита F1–F5 (доказательство конкуренции не было гонкой)
(run: worker-d20-level3-fix)

**Аудит:** `docs/_TODO/runs/integrator-cleanup/D20_LEVEL3_AUDIT.md` — прочитать целиком, он сделан
прогонами: каждая находка получена внесением поломки и полным прогоном.

Главное: **скрипты, которые мы считали доказательством конкуренции, конкуренцию не проверяют.** Снятие
`FOR UPDATE SKIP LOCKED` из захвата строки оставляет их зелёными, а двухфазный захват без единой блокировки
проходит три попытки из трёх. То есть защита от двойной отправки сегодня не гарантирована ничем.

## Что закрыть

- **F2 (дороже всех).** Сделать захват строки настоящей гонкой: аудитор проверил, что помогает —
  **прогрев обоих соединений пула до гонки** (четыре строки перед постановкой). С прогревом двухфазный
  захват падает три из трёх, снятие `SKIP LOCKED` — два из трёх. Прогрева недостаточно: добавить **барьер**,
  чтобы оба соединения стартовали одновременно, и добиться устойчивого падения на обеих поломках.
- **F5.** Скрипт, который не проверяет ничего, сегодня даёт зелёный CI. Добавить в каждый скрипт-доказательство
  самопроверку: если ни одна проверка не выполнилась — выход с ошибкой. Тот же приём, что у гейта констант.
- **F1.** Освобождение ключа идемпотентности не покрыто ничем: можно снести **всю** таблицу ключей — всё
  зелено. Добавить: занять два разных ключа, освободить один, убедиться, что второй жив и не переиспользуем.
- **F3.** Возврат зависших строк можно расширить на уже **отправленные** — доставленное уедет повторно.
  Добавить контрольную строку в состоянии «отправлено», которую возврат не имеет права трогать.
- **F4 — сначала проверить, не закрыто ли уже.** Аудит шёл на клоне до слияния D34; в ветке порт
  идемпотентности уже обязателен на уровне типа (`bookingLifecycleRoute.ts:43`). Если снять проводку молча
  больше нельзя — записать это в отчёт как закрытое и **не делать работу повторно**.

## Гейт приёмки

По каждой находке: внести ровно ту поломку, что названа в аудите, показать красное, откатить, показать
зелёное. Скрипты гонять по-настоящему (они поднимают свой временный PostgreSQL), вывод приложить.
Полный прогон интегратора (сейчас 161) до и после.

**Отдельное требование:** после правки скриптов повторить обе поломки аудитора — снятие `SKIP LOCKED` и
двухфазный захват — и показать, что теперь падает каждая. Без этого работа не принимается: именно здесь
прошлый круг и промахнулся.

## Границы

- Продуктовый код не менять — все находки про доказательства и тесты. Исключение: если F3 требует правки
  условия возврата зависших строк, это правка продуктового кода — обосновать отдельной строкой.
- Не расширять область на другие уровни.
- Не изобретать требований: чего нет в аудите — строка в «развилки».

## Правила тестов — прочитать целиком

`.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/webapp-tests-lean-no-bloat.mdc`.

## Приёмка

`docs/_TODO/runs/integrator-cleanup/D20_LEVEL3_FIX_REPORT.md`: таблица F1–F5 (закрыто / уже закрыто / не
закрыто и почему · файл · поломка · вывод прогона), обязательный раздел «повтор обеих поломок аудитора».
Писать по ходу; фоновых задач и подзапусков не использовать.

---

## ПРОДОЛЖЕНИЕ (запуск 2, 01.08) — работа уже начата, не начинай с нуля

Прошлый прогон снят владельцем на середине. Салваж — коммит `33005b007` в этом же клоне: прогрев соединений
и барьер частично внесены в три скрипта-доказательства (`check-d30-scheduler-lock-concurrency.ts`,
`check-d30-outgoing-delivery-claim-concurrency.ts`, `check-d30-idempotency-key-concurrency.ts`), начата правка
`bookingLifecycleRoute.dedup.test.ts`, в `outgoingDeliveryQueue.ts` снята одна строка.

**Первый шаг: `git show 33005b007` — понять, что уже сделано, и продолжить, а не переписать.**
Гейт приёмки и границы выше не меняются. Отдельно проверь, не сломана ли снятая строка в
`outgoingDeliveryQueue.ts` (продуктовый код — обосновать или вернуть).
