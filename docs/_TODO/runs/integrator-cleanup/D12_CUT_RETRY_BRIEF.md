# D12, повтор — рез недостижимых веток с ДОКАЗАТЕЛЬСТВОМ по каждому типу (run: worker-d12-cut-2)

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D12**.

**Правила репозитория, обязательные к прочтению до правки:**
`.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/clean-architecture-module-isolation.mdc`,
`.cursor/rules/webapp-tests-lean-no-bloat.mdc`.

## Что случилось в прошлый заход — читать обязательно

Прошлый прогон (`worker-d12-cut`, коммит `ea7353d71`) вырезал 1189 строк и **срезал ЖИВУЮ ветку**:
`message.edit` используется в живых сценариях MAX — `apps/integrator/src/content/max/user/scripts.json`
(строки 738, 783, 850, 895 и далее, всего 14 вхождений в контенте), плюс живой путь в
`integrations/telegram/deliveryAdapter.ts:217`. Обработчик при этом был удалён.

**Тесты этого НЕ поймали.** Я проверил отдельно: отключение живой ветки рассылки напоминаний
(`reminders.dispatchDue`) не роняет ни одного теста — тестовой сети у этой подсистемы нет вовсе.
Значит «зелёные тесты» здесь ничего не доказывают, и опираться на них нельзя.

Коммит отменён (`debd1b5cd`). Начинаем заново, с другим порядком доказательства.

## Порядок, обязательный

**По КАЖДОМУ типу, который собираешься вырезать, приложи доказательство недостижимости из ТРЁХ источников:**

1. **Контент сценариев** — `grep -rn "<тип>" apps/integrator/src/content/` даёт ноль. Это главный источник:
   именно оттуда приходят живые действия. Прошлый заход эту проверку пропустил.
2. **Код обоих приложений** — `grep -rn "'<тип>'" apps/integrator/src apps/webapp/src --include=*.ts` без тестов:
   ноль производителей (упоминания в схемах и перечислениях типов — не производители, но их назови отдельно).
3. **Смысловой поиск** — `node /home/dev/brain/tools/code-search.mjs "<тип> <смысл>" --repo bcb`: ноль живых мест.

Тип, у которого хотя бы один источник даёт живое вхождение, **НЕ режется**. Выноси строкой в отчёт — это
правильный исход, а не недоработка.

⛔ **НЕ резать (вопрос владельцу без ответа):** `reminders.rules.get`, `reminders.rule.toggle`,
`reminders.rule.cyclePreset`.

⛔ **Особое внимание к семейству `message.*` и `intent.*`** — это ядро доставки, ради которого интегратор и
существует. Прошлый заход срезал оттуда живое. Если сомневаешься — не режь.

## Приёмка

- таблица: тип → результат по КАЖДОМУ из трёх источников → вырезано / оставлено;
- дословный вывод команд по каждому вырезанному типу (не пересказ);
- три исключения перечислены и не тронуты;
- `pnpm --filter @bersoncare/integrator lint` и `typecheck` зелёные — но помни, что зелёные тесты здесь
  доказательством НЕ являются, доказательство — три источника выше;
- галочки плана не ставить, push и merge не делать.
