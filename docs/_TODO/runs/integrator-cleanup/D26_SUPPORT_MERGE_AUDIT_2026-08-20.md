# D26 — независимый первичный аудит (гейт мед-истории + инструмент поддержки)

**Кандидат:** `cb336e4fc` (`wt/d26-support-merge-20260820`)
**Аудитор:** `auditor-live`, codex gpt-5.6-sol/high, `d26-audit-20260820-run1`
**Authority:** `WORK_ORDER.md` D26, решение Р-D26, `IDENTITY_AND_MERGE_SCHEME.md` §5.

## Вердикт: FAIL

Слепой список поломок составлен по §5 схемы до чтения тестов кандидата. Итог: **2 достижимых дефекта**
(target-side bypass, неполный перенос истории), **1 архитектурная находка** (route держит DB/business-логику
напрямую, нарушая §5 thin-route), **3/3 fault injection убиты, 0 непойманных** на тех классах, что проверялись
целевой мутацией отдельно от acceptance-тестов.

### Находка 1 — автогейт обходится, если эвристика выбирает `target` неверно

Гейт медицинской истории проверяет только `duplicateId` (старую сторону пары), не обе стороны. Если эвристика
слияния выбрала аккаунт С историей как `target` (не `duplicate`), проверка истории у `target` не проводится, и
автослияние проходит вопреки §5.2. Новый acceptance-тест (`accountMergeMedicalHistory.unit.test.ts`) красный на
исходной реализации.

### Находка 2 — support-merge не переносит полную медицинскую историю

Обратный (support) перенос данных не переносит `clinical_visit` и ряд связанных clinical/anamnesis,
discussion, clinic-link таблиц — то есть «перенести все данные в любом направлении» (§5.8) не выполнено
полностью. Новый acceptance-тест красный на исходной реализации.

### Находка 3 (частично) — §5.4 выполнен не полностью

Auto-path (без истории) переносит ВСЕ channel/OAuth-привязки без разбора, не реализует условие 2FA-переноса и
кириллический приоритет имени по §5.4(б). Route-тесты подтвердили корректность account id для revoke/ban/unban
и отсутствие удаления истории при отзыве контакта — это НЕ сломано.

### Архитектурная находка — не блокирующая, для отдельного рассмотрения

И `merge/route.ts`, и `support-account/route.ts` держат DB/business-оркестрацию прямо в route handler,
нарушая §5 «route handlers thin». Разделение на два route признано оправданным (revoke/ban ≠ merge семантически,
transfer переиспользует существующий merge-route) — находка не про дублирование, а про толщину route.

### Категории (§5.2/§5.2a) — проверено взглядом

Запрос гейта НЕ ограничен организацией (проверяет по всем клиникам — верно по §5.2 «за любой срок, в любой
клинике»). Блокирует appointments, doctor-programs, patient-authored messages — верно. НЕ включает
promo-программы/отметки самочувствия/разминок/полученные рассылки как блокеры — верно (это НЕ-блокеры по §5.2).

## Проверки аудитора

- `pnpm --dir apps/webapp exec vitest --run --project=unit src/infra/accountMergeMedicalHistory.unit.test.ts`
  → **2 failed, 1 passed** (находки 1 и 3 подтверждены красным).
- route-тест (revoke/ban/unban id корректность, отсутствие удаления истории) → **2 passed**.
- Fault injection (отключённый автогейт, перепутанный revoke-id, перепутанный ban-id) → **3/3 убиты, 0
  непойманных**.
- `pnpm run typecheck` после сборки workspace-пакетов (`@bersoncare/db-principal`, `@bersoncare/platform-merge`)
  → **PASS**. Утверждение воркера о несвязанной причине падения typecheck — подтверждено, не было дефектом D26.

Тесты закоммичены аудитором: `bdc1f11c8` (`test(d26): expose support merge gaps`).

## NOT DONE

Продуктовые исправления НЕ вносились — это handoff воркеру: закрыть находки 1–3 по уже написанным красным
acceptance-тестам как oracle, не переписывая их ожидание.
