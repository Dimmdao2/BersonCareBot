# Track D / D30 owner-decision plan-hygiene — mechanical correction result, 2026-08-21

## Источник оракула

`docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`, строка «Bounded docs worker removes incompatible
drain/29.08/DROP mechanics from current D30/handoff/active DB-privilege plans instead of preserving two
formulations» (`brief c71422131` + oracle corrections `0d74cbacc`, `c6d40fa8c`); FAIL против первого результата
`647c672d4` в том же файле. См. `AGENTS.md` §«Как решать, что делать».

## Что сделано

### 1. `docs/_TODO/runs/integrator-cleanup/D30_SCHEDULER_REVERSAL_PLAN.md`

- Раздел 3 («Слияние двух очередей в одну») — снят баннер `УСТАРЕЛО/ЗАМЕНЕНО`, который держал старую
  формулировку («раздел описывал перенос…») рядом с новой. Осталась одна формулировка: целевая очередь —
  `public.outgoing_delivery_queue`, выбор закреплён Р-D10a; сравнительная таблица оставлена как инженерное
  обоснование выбора (не executable-инструкция).
- Пункт **Ш7** — заголовок с `[-] ~~…~~ — ОТМЕНЕНО ВЛАДЕЛЬЦЕМ 21.08.2026` (уже был в правильном формате трёх
  состояний чек-листа) сохранён; **вырезан весь последующий текст пункта** (~260 строк: `CURRENT PARTIAL
  03.08`, `CANDIDATE DRAIN`, `STALE NOTE 04.08`, `RE-MEASURE 04.08`, `DEV-worker unblock`,
  `READINESS-PROBE FIX + DEV LIVE DRAIN` — перенос/дренаж/PROD-замер/ожидание due-строк до 29.08/точка
  невозврата `DROP TABLE`). Заменён на 12-строчный абзац: что отменено, чем доказано (`docs/OWNER_DECISIONS.md`
  → «Track D — текущий scope»), и backreference на git-хеши (`25a6c11a7`, `a521ca4d2`, `638b7b9cb`,
  `1f9b2f22f`) и три отдельных отчёта с той же историей (`DELIVERY_WORKER_READINESS_FIX_BRIEF_2026-08-04.md`,
  `TEST_DEPLOY_CRASHLOOP_BRIEF_2026-08-04.md`, `DEV_LEDGER_REPAIR_AND_WORKER_UNBLOCK_REPORT_2026-08-04.md`) —
  эти отчёты не менялись, история осталась в git, здесь не воспроизводится.
- Раздел 1 (перепись A1 `jobQueueLoop`) — убрана ссылка «уходит вместе со слиянием очередей, раздел 3, шаг Ш7»
  (Ш7 отменён и это больше не действующая причина); заменена на прямую ссылку на текущее состояние
  (`message_retry_jobs` на именованной DEV отсутствует, Ш7 отменён 21.08.2026).
- Раздел «Чего я не смог выяснить», пункт 3 (объём `message_retry_jobs` на проде, «от этого зависит длительность
  дренажа на Ш7») — помечен снятым: вопрос был привязан к отменённому шагу.
- Раздел 5 (риски/точки невозврата) уже был в правильном трёхсостоянии формате (`~~…~~ — УСТАРЕЛО/ЗАМЕНЕНО`,
  короткие однострочники без исполняемых деталей) — не трогался.

### 2. `docs/_TODO/runs/integrator-cleanup/TRACK_D_ORCHESTRATION_HANDOFF_2026-08-21.md`

Не менялся: §5 уже в правильном формате (`~~D30 честно упирается…~~ УСТАРЕЛО/ЗАМЕНЕНО 21.08.2026: …`), активная
29.08-механика уже снята первым результатом `647c672d4`, оставшаяся работа D30 названа явно (Ш9 + открытые
Ш1–Ш6/Ш8). Второй активной формулировки нет.

### 3. `docs/_TODO/DB_PRIVILEGE_LAYER_REBUILD/FINDINGS_TABLES.md`

Три stale active backreference на отменённый Ш7/дату 29.08 исправлены на факт («таблица уже отсутствует на
именованной DEV», без даты/счётчика pending-строк):

- Д14, строка `integrator.message_retry_jobs` (таблица вердиктов): `**СНОСИТЬ после 2026-08-29** — … остались
  10 pending; пункт плана D30 Ш7` → `**СНЕСЕНА** — на именованной DEV таблицы больше нет (…, 21.08.2026); …
  D30 Ш7 (перенос/дренаж) отменён владельцем`.
- Итог по девяти таблицам Д14: `message_retry_jobs сносится не раньше 2026-08-29 17:00 MSK (10 строк pending)`
  → `message_retry_jobs на именованной DEV уже отсутствует — снос не ждёт даты`.
- И16 (`integrator.*` очереди): `message_retry_jobs сносится (D30 Ш7, после 2026-08-29)` → `message_retry_jobs
  уже снесена (на именованной DEV отсутствует; D30 Ш7 отменён владельцем 21.08.2026)`.

`AUDIT_LOG.md`, `evidence/*`, архивы — не открывались и не менялись (вне скоупа брифа).

### 4. `docs/_TODO/DB_PRIVILEGE_LAYER_REBUILD/PLAN.md`

Проверен построчным грепом (`message_retry_jobs`, `D30`, `29.08`, `DROP TABLE`, `дренаж`) — единственное
вхождение (строка 142, `[x]` в прошедшем времени: «код очереди доставки переведён с удалённой
`integrator.message_retry_jobs` на `public.outgoing_delivery_queue`») уже не несёт активной drain/29.08/DROP
механики. Файл не менялся.

### 5. `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`

- **D39 закрыт `[x]`.** Существующее PASS-доказательство, без нового аудита: census `1b18259eb`
  (`docs/REPORTS/D39_DELIVERY_SEAM_CENSUS_2026-08-20.md`), независимый sweep + аудит `dc3ab3e0c`/`1c1a93a87`
  (`docs/REPORTS/D39_REMAINING_SEAMS_SWEEP_2026-08-20.md`) подтвердивший двумя независимыми путями поиска, что
  census полон (единственный найденный пропуск `operator-alert-relay` уже duplicate-safe), фикс D39-F1
  `362655450` (`docs/REPORTS/D39_FIX_F1_2026-08-20.md`) с независимым аудитом `e4f6ca310`
  (`AUDIT_D39_DELIVERY_SEAMS_2026-08-20.md`, PASS), плюс `fb1655884`/`499f2efd6`/`decc3937f` (три коммита
  дедупликации + owner-решение по transactional email). Все восемь SHA проверены командой
  `git merge-base --is-ancestor <sha> feat/doctor-ui-rebuild` — все PASS (предки `feat/doctor-ui-rebuild`).
  Убраны две стейл-формулировки «пункт остаётся открыт»/«чекбокс не закрыт — требуется независимый аудит» и
  добавлена строка закрытия со ссылками на evidence. Повторный аудит НЕ запускался.
- Остальные `29.08`/`message_retry_jobs`-упоминания (§2.3 Р-D10a/Р-D30, §3.4 статус-баннер 21.08, D30/D15b
  прогресс-заметки) уже в форме «ожидания/дренажа до 29.08 нет» — одна текущая формулировка, второй не
  сосуществует; не менялись.
- Остальные галочки WORK_ORDER.md не трогались — точного противоречия для них не найдено.

## Проверки

- `git diff --check` (по staged-изменениям) — чисто, конфликт-маркеров/пробельных ошибок нет.
- Точный `rg -n "29\.08"` и `rg -n "DROP TABLE"` по всем пяти scoped-файлам — единственные оставшиеся
  совпадения формулируют отсутствие ожидания/отмену, ни одного активного «жди до 29.08 / DROP» не осталось
  (полные списки приведены выше построчно).
- `git merge-base --is-ancestor <sha> feat/doctor-ui-rebuild` для всех восьми D39-хешей — PASS для каждого
  (команды выполнены выше, не по памяти).
- Docs-only: код/миграции/AGENTS/canon/DB/DEV/TEST/PROD/deploy/ветки не менялись; full CI не гонялся.

## Оставшиеся exact-совпадения — исторические, не active

- `docs/_TODO/runs/integrator-cleanup/TRACK_D_ORCHESTRATION_HANDOFF_2026-08-21.md:127-128` —
  `~~D30 честно упирается…~~ УСТАРЕЛО/ЗАМЕНЕНО` (страйкthrough + причина, единственная формулировка).
- `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md:412,780,797,1459` — «ожидания/дренажа до 29.08
  нет» (отрицание, не инструкция ждать).
- `docs/_TODO/runs/integrator-cleanup/D30_SCHEDULER_REVERSAL_PLAN.md:394,416` — «гейт и точка невозврата
  больше не входят в маршрут» / `~~Ш7, DROP TABLE…~~ — УСТАРЕЛО/ЗАМЕНЕНО».
- Полные `CURRENT PARTIAL/CANDIDATE DRAIN/RE-MEASURE/DEV-worker unblock/READINESS-PROBE FIX` нарративы 03–04.08
  остаются только в git-хешах (`25a6c11a7`, `a521ca4d2`, `638b7b9cb`, `1f9b2f22f`) и в трёх отдельных
  завершённых отчётах, перечисленных выше — не переписывались.

## Актуальный следующий шаг D30

Из `TRACK_D_ORCHESTRATION_HANDOFF_2026-08-21.md` §5, п.4 (не менялось этим проходом): пакет D10a+D16+D30 —
ближайший открытый шаг **Ш9** (свести `worker`/`scheduler` в один резидентный процесс, Р-D30а от 20.08,
`D30_SCHEDULER_REVERSAL_PLAN.md` шаг Ш9), плюс реально открытые Ш1–Ш6/Ш8 того же плана. Ш7 закрыт как
отменённый владельцем, работы по нему нет.

## NOT DONE

D30 implementation / landing / TEST / deploy / push / full CI — не выполнялись и не входили в мандат. Новый
аудит по D39 не запускался (запрещено брифом). Новых сущностей/планов не заведено.
