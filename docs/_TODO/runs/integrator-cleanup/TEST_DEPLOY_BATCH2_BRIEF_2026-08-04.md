# Деплой на TEST: VK, выключатель врачебных экранов, миграции CI-волны

Rules: `AGENTS.md` — Маршрут, CORE rules, §6, §9, §24. Language: internal work is English.

⚠️ Один ход, следующего не будет; всё на переднем плане; коммит до конца хода.

Authority: пять веток приземлены и прошли независимый аудит; полный `pnpm run ci` на главном дереве —
`exit 0`, 453 c, на свежей установке. `ORCH_OPS`.

Источник оракула — правило репозитория, дословно: «Полный `pnpm run ci` — только перед deploy/merge/repo-level
изменением». Он пройден, значит выкатка — следующий шаг.

## Что едет

Миграции `0361` (грант под применение тарифа), `0362` (переиздание триггера проекций OAuth), `0365` (связь
пациент↔специалист + колонка выключателя врачебных экранов), плюс правки харнесса тестов и CI-цепочки.

⚠️ **Важно про `0362`:** её содержимое уже применено на TEST вручную предыдущим ходом (через временную
элевацию, снятую после). Значит миграция обязана быть идемпотентной на TEST — проверь это ДО выкатки и скажи,
что увидел; если она не идемпотентна, останови выкатку и доложи, а не «попробуй и посмотри».

## ⛔ Как НЕ надо: прошлый прогон этого же брифа погиб именно так

Предыдущий агент запустил деплой в фоне и завершил ход словами «подожду, пока закончится» — его процесс умер
вместе с ходом, сборка оборвалась на середине, TEST остался невыкаченным. Твой ход — единственный: **никаких
`&`, `nohup`, `setsid` и «подожду». Деплой запускается на переднем плане и ты дожидаешься его кода возврата
внутри своей команды.** Если он длиннее твоего терпения — это всё равно единственный способ.

## Работа

1. **Выкатить `feat/doctor-ui-rebuild`** штатным `deploy/host/deploy-test.sh`, foreground, лог в отчёт.
   Перед стартом убедись, что оборванная сборка прошлого прогона не оставила окружение в промежуточном
   состоянии: юниты TEST должны быть живы и на СТАРОМ коде (сборка обрывалась до переключения).
2. **Гейты закрытия** зелёные. Счётчик-константа, разошедшаяся из-за новой таблицы `patient_specialist_links`
   или новых грантов, — книжная поправка, чини. Содержательное падение — НЕ подгонять, описать.
3. **Проверить живьём три вещи, которые владелец увидит:**
   - VK-кнопка на экране входа есть, `start` даёт редирект на `id.vk.com` (не `501`);
   - выключатель врачебных экранов в кабинете присутствует и по умолчанию экраны ВКЛЮЧЕНЫ;
   - воркер доставки жив, crash-loop не вернулся (`NRestarts`, лог Postgres).
4. **Новая таблица под стеной:** после выкатки убедись, что `patient_specialist_links` имеет RLS и политику на
   TEST так же, как на DEV, — новые org-таблицы обязаны приезжать со стеной, а не без неё.

## Границы

- **PROD (`135.106.162.170`) не трогать.** Полного дампа не делать — code-only выкатка.
- Дальше клика по VK не идти: вход завершает владелец.

## Готово значит

- Деплой завершён, юниты активны и здоровы, гейты зелёные.
- Три пункта живой проверки названы результатом, а не «должно работать».
- Идемпотентность `0362` на TEST подтверждена явно.
