# D30 Ш9 — independent audit, 2026-08-21

**Candidate:** `7cf580712`  
**Base:** `5662a9f57`  
**Oracle:** `docs/OWNER_DECISIONS.md` Track D; `WORK_ORDER.md` Р-D30 (one resident process/unit/lock/top-level loop, `SKIP LOCKED`); `D30_SCHEDULER_REVERSAL_PLAN.md` Ш9; `D30_CURRENT_RESIDENT_PROCESS_FINISH_BRIEF_2026-08-21.md`.

Blind kill-set written before reading tests: lose the leader lock; overlap a slow body; omit either former worker tick; retain/start a second worker service; rewrite delivery claim/idempotency/retry/reclaim behavior.

- **FAIL — one-process topology / retired worker lifecycle:** source removal is complete (`worker/main.ts`, `bersoncarebot-worker-prod.service`, worker package launchers and compose service are deleted), but an upgrade from the base can retain the already installed and active old unit. `git show 5662a9f57:deploy/systemd/bersoncarebot-worker-prod.service` shows its `Restart=always` executable unit; `git show 7cf580712:deploy/host/deploy-prod.sh | rg -n 'systemctl (restart|is-active|stop|disable).*worker|WORKER_SERVICE'` and the equivalent bootstrap search have no retired-worker stop/disable/removal. The documented ordinary deploy leaves installed units in place and restarts only API/scheduler. Thus the old worker continues while the new scheduler begins its embedded delivery tick: duplicate delivery is reachable, violating Р-D30's one unit/process/lock/cycle requirement.
- **PASS — lock ownership, lock loss, single-flight and shutdown:** `schedulerLockedTick.ts` checks `assertLockStillHeld()` before starting every body and each body shares the single-flight constructor; `scheduler/main.ts` releases the lock, closes DB/error tracking, then exits for both SIGINT/SIGTERM and lock loss. Focused command `pnpm --dir apps/integrator exec vitest --run src/infra/runtime/scheduler/schedulerLockedTick.unit.test.ts` passed **6/6**. Fault injection `await deps.assertLockStillHeld()` removed -> **3 failed** (including “starts neither … when the leader lock is lost”); `if (inFlight !== null) return` removed -> **3 failed** (slow outgoing delivery ran twice).
- **PASS — both former worker ticks are hosted by the existing coordinator:** `7cf580712:apps/integrator/src/infra/runtime/scheduler/main.ts` wires `runOutgoingDeliveryWorkerTick` and `runDirectPublicWriteRetryWorkerTick` into `createSchedulerLockedTickCoordinator`; it retains separate scheduler and worker DI graphs. Fault injection removing `directPublicWriteRetryTick.startIfIdle()` -> **1 failed**, “does not start another body after a later lock-loss observation” expected the retry tick once and observed zero.
- **FAIL — deploy/systemd cannot yet prevent a second active worker on upgrade:** local descriptors are internally valid (`bash -n deploy/host/bootstrap-systemd-prod.sh deploy/host/deploy-prod.sh deploy/host/install-systemd-sandbox.sh deploy/host/prod/bcb-bluegreen-lib.sh`; `bash deploy/host/bootstrap-systemd-prod.sh --self-test` -> `OK`; package JSON parse and `docker compose -f deploy/docker/docker-compose.yml config --no-interpolate` passed), but none retires an installed legacy worker before starting scheduler. Static descriptor deletion alone cannot change systemd state.
- **PASS — delivery semantics not rewritten:** `git diff --quiet 5662a9f57..7cf580712 -- apps/integrator/src/infra/runtime/worker/outgoingDeliveryWorker.ts apps/integrator/src/infra/runtime/worker/directPublicWriteRetryWorker.ts apps/integrator/src/infra/runtime/worker/operatorDeliveryAttemptWritePort.ts` exited 0; only their hosting changed. `git diff --check 5662a9f57..7cf580712` also exited 0.

No acceptance test was added: the failure is an upgrade-state transition in installed systemd, not a repeatable product behavior suitable for a source-text/unit-count gate. No PROD, deploy, migration, database, fixtures, or full CI run occurred. Temporary production-code mutations were reverted.
