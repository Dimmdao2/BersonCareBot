# ПОВТОРНЫЙ АУДИТ уровня 2 — теперь С ПРОГОНАМИ (роль с доступом к дереву)
(run: auditor-live-d20-level2)

Проверяемое: коммиты **`9bbad9dcb`** (37 тестов уровня 2) и **`297392877`** (закрытие семи находок аудита).
Клон `/home/dev/dev-projects/bcb-wt-testsuite-g0`, тестов сейчас 130 (было 88 до уровня 2).

**Почему аудит повторный.** Первый прогон запускался ролью только для чтения: аудитор не смог ни выполнить
свои поломки, ни записать отчёт, и честно пометил все находки как предсказания по коду. У тебя доступ к
дереву есть — **центральная проверка обязана быть исполнена прогонами**.

## ⛔ Границы роли

**Ты аудитор, а не исполнитель.** Менять код и тесты как результат работы ЗАПРЕЩЕНО. Ломать — обязан, но
каждую поломку возвращаешь. В конце `git status` обязан показать чистое дерево на коммите `297392877`;
вывод приложить. Единственный файл, который ты создаёшь, — свой отчёт.

## Что проверить прогонами

1. **Семь закрытых находок — воспроизвести.** Отчёт `D20_LEVEL2_FIX_REPORT.md` по каждой называет поломку и
   красный вывод. Внести ту же поломку и убедиться, что тест действительно краснеет **и что краснеет именно
   тот тест**, а не соседний. Расхождение с отчётом — находка.
2. **Свои поломки, а не из отчётов.** Придумай не менее восьми собственных, там же — и таких, которые
   выгодно пропустить: пропуск канала записан, но с пустой причиной · финализация возвращает успех, ничего
   не записав · ключ дедупликации собирается из полей в другом порядке · принципал арендатора берётся не
   тот · попытка увеличивается, но статус не меняется. По каждой: покраснел ли тест, какой, с чем.
   **Поломка, которую не поймал никто, — находка.**
3. **Тест на моках.** Для каждого из шести файлов: удалить проверяемую функцию целиком — покраснеет или
   пройдёт мимо?
4. **Полный прогон интегратора** до и после твоих поломок — числа привести.

## Как отвечать

Вердикт **PASS / SHIP-WITH-FIXES / FAIL** + таблица находок: что · файл и строка · вывод прогона · что чинить.
Каждая находка — с выводом прогона. Без прогона — помечать как гипотезу и объяснять, почему прогнать не смог.
**Не расширять область:** требования из карты D20 и слов владельца; идеи «как ещё улучшить» — раздел
«вне области», работой они не становятся.

## Приёмка

Файл `docs/_TODO/runs/integrator-cleanup/D20_LEVEL2_REAUDIT.md` + чистый `git status` в конце.
