# Хендофф оркестрации Track D — 2026-08-21, ночь

Кому: следующему оркестратору `#987`. Оркестратором может быть только Opus.
Авторитет работы: `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, §2.2 «Порядок исполнения Track D».
Предыдущий хендофф — `TRACK_D_ORCHESTRATION_HANDOFF_2026-08-20.md`; он не отменён, там грабли дня и разбор моей
ошибки с грантом. Здесь — только то, что изменилось после него, и то, чего в плане нет.

`feat/doctor-ui-rebuild` на момент передачи — **`8e8e26bab`**.

---

## 1. ПЕРВОЕ ДЕЛО СЛЕДУЮЩЕГО: живой дефект в уже приземлённой работе

**Канонический журнал доставки не объявлен в каталоге port-context, а код теперь зовёт только его.**

После моего D10a-пакета («единственный писатель», merge `c5440424a`) весь `delivery.attempt.log` идёт в
`app.record_operator_delivery_attempt(text,text,text,uuid,text,text,integer,text,text,timestamp with time zone)`.
Но `app_ext.port_context_capabilities` **на обеих живых базах** объявляет для доставки только СТАРУЮ дверь:

```
app.record_operational_delivery_attempt_audit(...)  role=app_operational_delivery_worker  purpose=delivery.attempt-audit
```

Проверено мной запросом к `bcb_webapp_dev` и к `bersoncarebot_test` — записи под новый корень нет ни там, ни там.
Резолвер принципала интегратора требует РОВНО ОДНО точное совпадение function identity и падает громко ещё до SQL:

```
Error: Missing unique declared integrator port capability for
app.record_operator_delivery_attempt(text,text,text,uuid,text,text,integer,text,text,timestamp with time zone)
```

Эту строку получил воркер `d20-portcontext-harness-20260820`, когда впервые за долгое время реально ЗАПУСТИЛ
opt-in тест (см. §3). То есть дефект вскрылся ровно потому, что тесты перестали молча пропускаться.

**Честная граница доказанного:** каталог пуст — доказано запросом; падение резолвера — доказано сообщением
воркера. Живого падения приложения я НЕ наблюдал: DEV-интегратор не запущен, а на TEST крутится код ДО D10.
То есть это дефект, который выстрелит на первой же выкатке, а не сегодняшний инцидент.

**Куда чинить:** объявить capability под канонический корень в контракте port-context
(`deploy/postgres/port-context/contract.sql` — обрати внимание, этот файл лежит в дереве изменённым и
незакоммиченным ещё с начала моей смены, я его НЕ трогал; разберись, чей он, прежде чем править).
⛔ Чинить надо объявлением capability, а НЕ грантом роли и НЕ правкой теста под сломанный код.

---

## 2. Что крутится прямо сейчас

**Воркер `d10-outbox-drop-20260821`** — codex `gpt-5.6-sol`/high, ветка `wt/d10-outbox-drop-20260821`, клон
`/home/dev/dev-projects/bcb-wt-d10-outbox-drop-20260821`, pid `2062540`, бриф
`<scratchpad>/brief-d10-outbox-drop.md`. Задача — D10, остаток п.2: полностью убрать
`integrator.projection_outbox` (миграция DROP + Drizzle-декларации в обоих приложениях + права в генераторе +
deploy-SQL + четыре CLI-хвоста в вебаппе + устаревшая строка в `api.md`).
Владелец сказал «или не запускай снос, просто дай хендофф» — но воркер к тому моменту уже шёл пять минут.
Я его не убивал: он пишет только в свою ветку, а в `feat` без строки вердикта и без порта ничего не попадёт.
Хочешь остановить — `kill 2062540`; хочешь принять — проверяй по §5.

**Что проверить в его работе особо:** он режет ОБЩИЕ строки `REVOKE`/`GRANT`, где рядом стоят
`integrator.idempotency_keys` и очередь доставки. Сломать соседу грант, редактируя общую строку, — самый
вероятный способ провалить этот пакет. В брифе это запрещено явно; проверь построчно сам.

**Воркер `d20-portcontext-harness-20260820` ЗАКОНЧИЛ, но НЕ ПРИЗЕМЛЁН** — см. §3.

---

## 3. Незакрытый пакет: оживление «живых» тестов

Ветка `wt/d20-portcontext-harness-20260820`, коммит `7495016c8`, отчёт
`docs/_TODO/runs/integrator-cleanup/D20_PORTCONTEXT_TEST_HARNESS_2026-08-20.md`.

Что сделано: **молчаливый `skipped` устранён** — четыре opt-in теста на настоящем Postgres впервые исполняются
(16 тестов), гейт больше не требует `DB_PRINCIPAL_SIGNING_SECRET` от снятого механизма, оснастка
`realPostgresIntegrationTestHarness.ts` знает про port-context.

Что НЕ сделано, и воркер это честно написал в `## NOT DONE`: общей зелени нет, инъекции поломки нет, потому что
нет зелёной базовой линии. Два реальных finding оставлены красными:

1. **FINDING 1:** фикстура под port-context получает роль `app_tenant_service`, у которой нет
   `INSERT`/`DELETE` на `public.outgoing_delivery_queue` — `42501` раньше поведения, ради которого тест написан.
   Живой замер: ни у `app_tenant_service`, ни у `app_operational_delivery_worker` этих прав нет.
2. **FINDING 2:** это и есть дефект из §1.

**Моё суждение, передаю как рекомендацию, а не как решение:** пакет правильный по направлению и не должен
лендиться «как есть» — он оставляет четыре красных теста в дереве. Сначала §1 (объявить capability), потом
перепроверить, сколько красноты останется, и только потом принимать. ⛔ Ни в коем случае не «чинить» это
расширением прав роли или ослаблением утверждений теста — оба хода запрещены и оба уже стоили этому треку суток.

---

## 4. Что я приземлил за смену (всё через `orch-launch.sh land`, со строкой вердикта на каждый коммит)

| что | merge |
|---|---|
| D10a — журнал принимает попытку без строки очереди (миграция, десятиаргументный корень) | `1617f53ae` |
| D10a — канонический журнал стал ЕДИНСТВЕННЫМ писателем, legacy-писатель удалён из кода | `c5440424a` |
| хвост D10 stage 2 — контрактный сторож падал ENOENT на снесённом каталоге | `4e2c1adf8` |
| хвост D10a — гейт готовности звал снесённые подписи функций | `8e8e26bab` |

Плюс док-коммиты в план и очередь аудита напрямую в `feat` (кодом `feat` я не трогал ни разу).

**Мои ветки — все влиты, вершины входят в `feat`, проверено:** `wt/d5-deadcode-c4-20260820`,
`wt/d10-transport-stage1-20260820`, `wt/d10-transport-stage2-20260820`, `wt/d20-level4-remainder-20260820`,
`wt/d10a-journal-nonqueue-20260820`, `wt/d10a-single-writer-20260820`, `wt/d10-contract-test-tail-20260820`,
`wt/d10-readiness-identity-20260820`. Не влиты и переходят к тебе: `wt/d20-portcontext-harness-20260820` (§3)
и `wt/d10a-scope2-20260820` — **последняя УДАЛЕНА целиком по требованию владельца**, не воскрешать.

---

## 5. Остаток Track D — что делать дальше, по порядку

1. **§1** — capability под канонический журнал. Блокирует всё остальное в D10a и портит выкатку.
2. **Принять или доработать пакет сноса `projection_outbox`** (§2). Замер перед сносом уже сделан и лежит в
   плане: DEV — 3759 `done` + 9 `cancelled`, TEST — 4668 + 9, **ноль `pending`/`dead` на обеих**, то есть
   неприменённых событий нет и переносить нечего.
   ✅ Решение владельца 21.08 дословно: «Ты же сносишь на dev а потом на test одновременно с миграцией и
   деплоем кода, ты похоже перебдел» — ждать выкатки НЕ надо, миграция едет вместе с кодом.
   **Миграцию применяешь ТЫ, на именованной `bcb_webapp_dev`. Воркерам применять запрещено.**
3. **Живой прогон readiness на TEST** — D10, остаток п.1. Сейчас НЕВОЗМОЖЕН по причине из §6, и причина старше
   Track D.
4. **Пакет D10a+D16+D30.** Ближайший шаг D10a — снять два CLI-читателя cutover'а и их вызов из релиз-гейта,
   потом DROP `integrator.delivery_attempt_logs` (~6248 строк — там история ЕСТЬ, в отличие от outbox, сначала
   перенос). D30 честно упирается в будущие даты напоминаний (~29.08).

---

## 6. Находки, которые НЕ мой скоуп и НЕ повод заводить работу самому

Обе доказаны живьём, обе записаны в строку вердикта `8e8e26bab`, обе относятся к старшей инстанции
`docs/_TODO/DB_PRIVILEGE_LAYER_REBUILD/` — не чини их из Track D, вынеси владельцу отдельно.

1. **`app.release_principal_context()` не существует ни на одной живой базе** (вместе с
   `install_signed_context` и `reset_principal_context` — все три `pg_proc` → 0). Это не поломка: собственный
   `deploy/postgres/privileges/function-census.ts` называет их «obsolete context roots ... intentionally
   absent». Но deploy-сторона за этим не пошла: `declaration.ts:720` до сих пор их декларирует с широким
   EXECUTE, `c5a-platform-operations-runtime.sql:51-55` делает по ним голый `GRANT EXECUTE ... TO
   app_clinic_billing` и ассертит это право (:1705), а `deploy-test-saas.sh:688` безусловно зовёт гейт
   готовности, чей первый же оператор их вызывает. **Вывод, требующий отдельной проверки: выкатка на TEST
   сегодня упёрлась бы в это.**
2. **`dev-c3-app-function-owners.sql` требует владельца `app_owner` для всей телеметрической семьи**, а живьём
   и на DEV, и на TEST они принадлежат `app_seam_telemetry_*_owner`. Моя миграция это не вносила — на TEST
   старая пятиаргументная функция тоже под seam-владельцем.

---

## 7. Решения и распоряжения владельца за эту смену — держи в голове

- «все что можешь свое — вливай» (20.08) и уточнение 21.08: **«вливать в feat разрешаю свои только ветки после
  полного pass. В чужую работу не лезть, лендить через порт чтобы не толкаться с соседями»**.
- «после приземления full ci не гоняй» (20.08) — проверка идёт ДО слияния, не после.
- «Свои ошибки все править» (21.08).
- Про снос — цитата в §5, п.2.
- Чужие ветки, которые сводит владелец сам: `wt/doctor-patient-header-tabs2-20260820`,
  `wt/global-admin-video-access-20260820`, `wt/video-duration-20260820`.
- ⛔ Отклонённая ветка `wt/d10a-scope2-20260820` удалена полностью: её правка ломала релиз-гейт (старый источник
  даёт 100% покрытия цели при расхождении 1.2%, новый — 23%, гейт врал бы про ~77%).

---

## 8. Практика, которая за смену себя оправдала

- **Числа воркеров не совпадают с реальностью систематически.** Гоняй сам и по ВСЕЙ области, где воркер правил
  файлы, а не по его выборке. За две смены: один отчитался зелёным при exit 2; другой написал «vitest exit 0»,
  сломав тест вне своей выборки.
- **Живое доказательство, а не декларация.** Мой провал 20.08: приземлил грант, проверив ТЕСТ ДЕКЛАРАЦИИ вместо
  живого INSERT. Владелец запретил такие тесты дословно. Сегодняшний пример правильного хода: старый зонд
  готовности падал `42883 does not exist` (то есть не проверял НИЧЕГО), новый на тех же данных даёт
  `42501 permission denied` — упирается в стену прав, ради которой написан.
- **Молчаливый `skipped` хуже красного.** Именно снятие молчаливого пропуска вскрыло дефект §1.
- **Гейты `orch-launch.sh` отказывают по делу.** Не обходи: клон обязан содержать голову `feat`; бриф обязан
  нести `## Источник оракула`, цитату в «ёлочках» ОДНОЙ строкой, имя файла-источника в том же абзаце и
  упоминание `AGENTS.md`; каталог клона — ровно `bcb-wt-<имя>`; `land` требует строку вердикта на КАЖДЫЙ
  коммит, включая чужие и merge-коммиты. `ORCH_PROVIDER=codex` обязателен для моделей `gpt-*`.
- **Симлинки в новом worktree:** `node_modules` в корень, оба приложения И каждый `packages/*`, плюс три
  строки в `$(git rev-parse --git-path info/exclude)` — иначе typecheck врёт, а launcher откажет на
  «незакоммиченном».
- **Сосед-оркестратор работал параллельно и ничего моего не сломал** — проверено сверкой вершин моих веток и
  файл-в-файл по контрольным суммам. Единственный его коммит в моей области (`26a94a822`) только добавляет:
  независимый аудит со слепой инъекцией, «убито 3, не поймано 0».
