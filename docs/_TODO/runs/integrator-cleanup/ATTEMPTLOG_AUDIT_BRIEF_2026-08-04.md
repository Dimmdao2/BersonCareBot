# Аудит: журнал попыток доставки терял 100% записей (`660c37c01`)

Rules: `AGENTS.md` — Маршрут, CORE rules, §5, §6, §9, §24. Language: internal work is English.

Authority: гейт перед сведением `wt/delivery-attempt-log`. `ORCH_OPS`.

**Тест или взгляд:** ТЕСТ. Проверяемое — «запись о попытке действительно появляется» — поведение на живой базе.

Источник оракула — отчёт исполнителя, дословно: «из 6319 строк в `delivery_attempt_logs` за всё время таблицы
(с 04.03) — **0** относятся к этому пути воркера… За последние 30 дней только по `reminder_dispatch` потеряно
минимум 658 успешных + 34 неуспешных доставок».

## Что проверяешь, скептически

Исполнитель утверждает: `CHECK (attempt > 0)` ни при чём и остался как есть; неправ был предикат происхождения
внутри функции — он написан под один узкий вид (`operator_alert`), а вызывается для всей очереди; функция
расширена, пишет реальный `kind`.

1. **Перемерь потерю сам.** Число «ноль записей из этого пути» — сильное утверждение; проверь своим запросом.
2. **Не ослаблен ли предикат происхождения.** Это главный риск правки: функция подтверждает, что запись
   соответствует реальной строке очереди. Попробуй записать попытку для несуществующего события, для чужого
   канала, для чужой организации — отказ должен остаться.
3. **Записи появляются** для обычной доставки: успех, неуспех, повтор с ростом счётчика.
4. **Регрессия на `operator_alert`** — прежний узкий случай обязан работать как раньше.
5. ⚠️ **Исполнитель применил `CREATE OR REPLACE` напрямую на живой TEST.** Проверь, что состояние TEST
   соответствует файлу в ветке и что ничего лишнего там не осталось. Скажи отдельно, расходится ли теперь TEST
   с тем, что даст следующий штатный деплой.

## Ответ

Вердикт `PASS` или `FAIL` первой строкой, затем по пунктам — команда и что увидел. Ничего не чини.
