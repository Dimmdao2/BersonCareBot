# Исследование под D13a — кто должен читать настройки напоминаний (run: research-d13a)

Read-only. Ничего не менять, только выяснить и доказать.

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункты D13a → D14 → D13b.
**Правило поиска:** сначала индекс `node /home/dev/brain/tools/code-search.mjs "<запрос>" --repo bcb`, слепой grep — потом.
**Правила репозитория к сверке:** `.cursor/rules/clean-architecture-module-isolation.mdc` (доступ к БД только через
порт своего приложения), `.cursor/rules/tests-check-behaviour-not-circumstances.mdc`.

## Установленный факт, от которого отталкиваться

Настройки `doctor_appointment_reminder_enabled` и `doctor_appointment_reminder_offsets_minutes` существуют как
запись в реестре настроек и экран в кабинете, но их НЕ ЧИТАЕТ ни один планировщик. Единственная работающая
реализация напоминаний о приёме — константы 24ч и 2ч внутри интегратора. Это подтверждено прошлым аудитом;
перепроверь и назови файлы, где это видно.

## Вопросы

1. **Где именно сейчас живут константы 24ч/2ч** и какой код их применяет: файл, строка, путь вызова от события
   записи на приём до постановки доставки.
2. **Кто в вебаппе может стать потребителем настроек:** есть ли уже планировщик/воркер/крон-точка в вебаппе,
   которая ходит по будущим записям. Если есть — назвать; если нет — назвать ближайшее место, куда это ложится
   по архитектуре, со ссылкой на документ, который так велит (`apps/webapp/ARCHITECTURE.md`, конвенции сервера).
3. **Через какой порт читать настройки** — назвать существующий порт и функцию, а не предлагать новый.
4. **Что случится с уже поставленными доставками**, когда врач поменяет смещения: перепланировать, отменить,
   оставить. Как это решают в мире — коротко, со ссылками; не изобретать своё.
5. **Границы:** что из D13a можно сделать, не трогая интегратор вообще. Резать константы (D13b) нельзя, пока
   потребитель не работает.

## Формат ответа

Список ответов на пять вопросов, каждый — с файлами и строками. В конце — предлагаемая последовательность шагов
D13a на 3–6 пунктов, каждый проверяемый. Никаких правок кода.
