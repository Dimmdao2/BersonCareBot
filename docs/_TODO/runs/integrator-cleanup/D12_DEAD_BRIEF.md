# MISSION: D12 — вырезать мёртвое, названное переписью поимённо

Механическая работа: перепись уже назвала каждый объект и доказала недостижимость. Ничего не решай сам — что не в
списке, то не трогай.

## Authority

- **План:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, «Track D-полный», пункт **D12**, шаг 3 порядка.
- **Что именно резать — из переписи:** `docs/_TODO/runs/integrator-cleanup/D12B_CENSUS_RESULT.md`: 10 недостижимых веток
  старого `switch` (у каждой назван перехватчик с файлом и строкой) и 27 недостижимых сценариев контента.
- **Правила тестов** (обязательны к прочтению, даже если новых тестов не пишешь):
  `.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/webapp-tests-lean-no-bloat.mdc`.
- **Доступ к базе, если случайно заденешь:** `.cursor/rules/clean-architecture-module-isolation.mdc`, раздел наверху.

## Что делать

1. Удалить 10 недостижимых веток `switch` в исполнителе действий — ровно те, что названы в переписи. Для каждой сверь
   перед удалением, что перехватчик на месте: если перехватчика нет, ветка живая — не трогай и вынеси в отчёт.
2. Удалить 27 недостижимых сценариев контента, названных переписью, вместе с их шаблонами, если те больше никем не
   используются. Шаблон, который используется хоть где-то ещё, остаётся.
3. Ничего не рефакторить попутно. Файлы, которых нет в списке, не открывать.

## Приёмка — командная, без «убедиться»

- Поиск по именам удалённых веток и сценариев возвращает ноль.
- `typecheck` и `lint` интегратора зелёные; существующие тесты исполнителя зелёные (новых не требуется — это удаление).
- Проверка `check-no-new-raw-sql` зелёная (ты ничего не добавляешь, но пусть подтвердится).
- В отчёте — число удалённых строк и по каждой из 10 веток строка «перехватчик подтверждён: файл:строка».

## Ограничения

- Не трогать: живые сценарии, реестр доменных действий (у него отдельная судьба), семь файлов `directPublic`, вебапп.
- План не редактировать — галочки ставит лид.
- Никаких `git add -A`. Коммит в этом клоне, без push и merge.

## Отчёт

`что удалено (ветки, сценарии, шаблоны) → подтверждение перехватчика по каждой ветке → вывод поиска → результаты
typecheck/lint/тестов → сколько строк убрано`.
