# Проверить TEST после выкатки: цепочка кода входа и журнал доставки

Rules: `AGENTS.md` — Маршрут, CORE rules, §6, §9, §24. Language: internal work is English.

⚠️ Один ход, следующего не будет; всё на переднем плане; коммит до конца хода (если правки будут).
**Это проверка, а не выкатка. Деплой уже прошёл — заново не запускать.**

Authority: деплой `deploy-test-20260804T102935Z-1512740.log` завершён, все гейты закрытия OK. Агент выкатки был
убит сигналом до того, как выполнил живые проверки, — они остались несделанными. `ORCH_OPS`.

Источник оракула — бриф выкатки, дословно: «запросить код входа по почте на TEST и показать, что строка встала
в очередь и дошла до `sent`, а воркер её разобрал. Это первая выкатка всей цепочки — до сегодняшнего дня
отправка шла синхронно внутри запроса».

## Работа

1. **Цепочка кода входа целиком на TEST:** запрос → строка в `outgoing_delivery_queue` с видом
   `auth_email_otp` → воркер её взял → `status=sent`. Покажи запросами каждый шаг.
2. **Журнал попыток доставки** `integrator.delivery_attempt_logs`: до сегодняшнего дня в нём было НОЛЬ строк
   этого пути за всё время таблицы. Покажи, что после реальной доставки появляются записи.
3. **Тело функции журнала** на TEST обязано совпадать с файлом ветки — её применяли туда вручную до деплоя.
   Сверь `pg_get_functiondef` с `deploy/postgres/c4-operational-runtime.sql` и скажи прямо, совпадает ли.
4. **Воркер доставки:** `NRestarts` не растёт, циклы держатся.
5. **Оверлей и миграция.** В первом прогоне деплоя лог показывал ошибку: оверлей
   `organization-member-invites-rls.sql` звал старую пятиаргументную сигнатуру
   `app.email_auth_enqueue_otp_delivery`, удалённую третьим кругом D27-C. Финальный прогон прошёл — выясни,
   что именно изменилось, и осталась ли где-то ещё ссылка на снятую сигнатуру.

## Границы

- **PROD не трогать. Деплой не перезапускать.**
- Реальные адреса живых людей не использовать — только тестовые.
- Правки, если понадобятся, — на своей ветке; push и merge в `feat` не делать.

## Готово значит

- Каждый шаг цепочки показан выводом запроса, а не выводом.
- Сказано число записей в журнале попыток до и после.
- Сказано, совпадает ли тело функции с файлом ветки.
- Сказано, осталась ли ссылка на снятую сигнатуру.
