# D12 — вырезать недостижимые ветки исполнителя, найденные переписью (run: worker-d12-cut)

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D12**.
**Основание:** перепись `docs/_TODO/runs/integrator-cleanup/D12B_REACHABLE_CENSUS.md` (сведена 31.07), §3 —
21 недостижимый тип действия, обнаруженный ЗАНОВО (не пересекается с тем, что резал `bf45f5853`).

**Правила репозитория, обязательные к прочтению до правки:**
`.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/clean-architecture-module-isolation.mdc`,
`.cursor/rules/webapp-tests-lean-no-bloat.mdc`.

## Что резать

Недостижимые типы действий из §3 переписи. Решение владельца 28.07 действует: **«не надо держать ради истории
устаревшее»** — убирать целиком, а не помечать «устаревшим».

⛔ **ТРИ ИСКЛЮЧЕНИЯ — НЕ РЕЗАТЬ.** Перепись задала по ним вопрос владельцу, ответа пока нет
(`runs/owner-questions.md`, пункт про D12b, вопрос 4): `reminders.rules.get`, `reminders.rule.toggle`,
`reminders.rule.cyclePreset` — недостижимы сегодня, но возможно планируется новый экран правил напоминаний,
который до них снова дойдёт. До ответа владельца они остаются. Если тронешь — вернёшь работу назад.

## Порядок

1. **Перепроверь недостижимость САМ** по каждому типу перед резом: кто мог бы его произвести, есть ли живой
   сценарий. Не верь переписи на слово — она делалась другим прогоном. Искать индексом
   (`node /home/dev/brain/tools/code-search.mjs "<q>" --repo bcb`), не только грепом.
2. **Режь только доказанно недостижимое.** Нашёл живого потребителя — не режь, вынеси строкой в отчёт: это
   правильный исход, а не недоработка.
3. Вместе с веткой убирай то, что становится мёртвым следом (её обработчик, схемы, тесты на неё саму), но
   ничего сверх — соседние файлы «раз уж мы тут» не трогаем.

## Приёмка

- построчно: тип действия → доказательство недостижимости (чем проверял) → вырезано / оставлено с причиной;
- три исключения перечислены отдельно и НЕ тронуты;
- существующие тесты интегратора остаются зелёными — это защита от «отрезал вместе с живым»;
- `pnpm --filter @bersoncare/integrator lint` и `typecheck` зелёные;
- если после реза какой-то файл целиком опустел — удалить файл, а не оставлять пустышку;
- галочки плана не ставить, push и merge не делать.
