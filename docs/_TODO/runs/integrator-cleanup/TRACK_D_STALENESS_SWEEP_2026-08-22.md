# Track D — сверка оставшихся этапов на устаревание (22.08.2026)

**Роль:** `auditor-live`, read-only. Ни одна строка кода/теста/миграции/базы/деплоя не изменена.
**Authority:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`. Открытые верхнеуровневые
чекбоксы на момент старта (`rg -n "^- \[ \] \*\*D[0-9]" WORK_ORDER.md`): **D15b, D17, D19, D25, D30**.
D25 — на независимом аудите, здесь не разбирается, только зависимости от него.

Метод: по каждому этапу — 4 вопроса брифа, каждый с цитатой + `path:line` + командой/выводом. Исследование
велось 4 параллельными read-only агентами (по одному на D15b/D17/D19/D30), их сырые отчёты синтезированы
ниже; конфликтующие показания между агентами отдельно помечены.

---

## D15b — перенос идентичности отдельными шагами (`WORK_ORDER.md:575-757`)

### 1. Требование ещё живо?

Да. `docs/OWNER_DECISIONS.md:332-367` («Track D — текущий scope, владелец 04.08; обновлено 21.08»)
воспроизводит формулировку WORK_ORDER дословно, включая снятие 20.08 отсрочки D15b/7 («сколько можно
говорить про „не сейчас" — устаревшая запись и её надо удалить») и подтверждает, что D15b/7a — уже в
объёме, отложен только физический разнос баз (вариант I). Ни в `.cursor/rules` (по `D15b`/`D25` — 0
совпадений), ни в хвосте `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` более позднего/противоречащего
решения не нашлось. `WORK_ORDER.md` в этом разделе последний раз правился `b5e0f4e1d` (21.08 23:25,
переоткрытие оставшихся D25-writer'ов) — это уже прочитанный командой текст, расхождений нет.

### 2. Не сделано ли это уже — и иначе?

Нет, ни один из открытых под-шагов не закрыт побочным путём. Проверено: `grep -n "D15b\|D25"
docs/OWNER_DECISIONS.md` (только уже учтённый блок), `.cursor/rules` (0 совпадений), `git log --oneline -5
-- WORK_ORDER.md` (верхний `b5e0f4e1d`, содержимое совпадает).

### 3. Что реально осталось — по под-шагам

**D15b/2 — интегратор перестаёт писать идентичность (= требование D25).**
Открыт даже острее, чем показывает текст WORK_ORDER. Текущий текст (`b5e0f4e1d`, 21.08 23:25) уже переоткрыл
чекбокс `[x]→[ ]` после докс-аудита, но описывает ПРОБЛЕМУ, а не исход попытки чинить её.
**Критический факт, которого ещё нет ни в WORK_ORDER.md, ни в OWNER_DECISIONS.md** (`grep -n "22\.08"
docs/OWNER_DECISIONS.md` → 0 совпадений): независимый аудит `9208262e9` (22.08) кандидата на закрытие D25
вынес **FAIL**, зарегистрирован в очереди коммитом `00217cbaa` (`docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`,
последняя строка) и уже породил бриф-коррекцию `docs/_TODO/runs/briefs/D25_CORRECTION_K5_K8_BRIEF_2026-08-22.md`.
Находка K5: кандидат зовёт `app.integrator_upsert_channel_identity` /
`app.integrator_bind_bootstrap_channel_phone` напрямую, а обе способности объявлены
`contextClass=integrator`/`app_integrator_resolver` и принимаются ТОЛЬКО под bootstrap — реальный вебхук
(`telegram/webhook.ts:372-378`) при уже разрешённой клинике зовёт `runWithIntegratorPrincipal`/
`runWithOrganizationPrincipal` и ловит отказ, наружу уходящий как `phoneLinkIndeterminate`/
`db_transient_failure` (вечные ретраи). Находка K8: `messenger_phone_bind_blocked` теперь несёт один id
вместо обоих столкнувшихся кандидатов — человек поддержки не видит контрагента, и `conflict_key` (sha256
отсортированных id) схлопывает два разных конфликта с одним источником в одну строку.
Живая проверка в этом рабочем дереве подтверждает, что фикс НЕ приземлён здесь: `writePort.ts:37-47`
по-прежнему импортирует `writeIdentityAndPreferencesDirect`/`mergeCandidatesDirect` из `./directPublic/*`
(до-D25 состояние); `git merge-base --is-ancestor ef42f0129 HEAD` → `fatal: Not a valid object name` — объект
даже не в этом дереве. Фикс живёт в отдельном клоне `/home/dev/dev-projects/bcb-wt-d25-remove-integrator-identity-writers-20260821`
(свой `.git`, `origin` указывает на локальный main-репо — то есть это независимый клон, не linked worktree).
Осталось: приземлить K5 (сделать корни достижимыми под реальными принципалами — через существующий шов,
без второго writer-пути/HTTP-hop/нового хранилища) и K8 (новая timestamp-forward миграция, вторая
OUT-колонка у `app.integrator_bind_bootstrap_channel_phone`, права миграция не выдаёт), прогнать зелёными оба
acceptance-теста аудитора (`writePort.identityRootReachability.audit.test.ts`,
`writePort.phoneLinkConflictContract.audit.test.ts`), затем повторить живую двухвебхуковую проверку — аудит
Part C был **BLOCKED**, не пройден: упёрся в предсуществующий `permission denied for schema public` в
`resolveCanonicalPlatformUserIdByChannel`, вне diff кандидата, помечен отдельным пунктом.

**D15b/6 — контакты в `user_contacts`.**
Код/миграция/привилегийная часть physical cutover (`#987`) задеплоены на TEST в merge `92cf34ffa4bf`,
единственное недостающее — «обычный existing-owner TEST-гейт логина/привязки/доставки» (текст WORK_ORDER
подтверждён живым чтением, `:731-734`: email-OTP challenge стартован для аккаунта врача Dmitry Berson,
код от владельца не введён — старт challenge не значит успешный вход). `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md:5-50`
(«Track D continuation — 21.08») показывает длинную цепочку named-DEV preflight FAIL→fix→retry именно по
этой миграции (маркеры schema-CREATE, language-usage, порядок source-origin constraint — 4+ повтора),
заканчивающуюся `STATIC PASS`/«повторить точный rollback-only preflight перед приземлением» — то есть
даже rollback-only цепочка ещё донастраивалась ДО TEST-деплоя, который цитирует WORK_ORDER. Более поздней
записи о завершённом existing-owner TEST-прогоне нет. Осталось: довести до конца уже стартованный email-OTP
челлендж владельца и зафиксировать аутентифицированную мутацию + provider-delivery proof. По коду других
хвостов не осталось — evidence: `docs/_TODO/DB_PRIVILEGE_LAYER_REBUILD/evidence/42-d15b6-canonical-contacts-cutover.md`.

**D15b/7 — псевдоним.** Снят с отсрочки 20.08, подтверждено `OWNER_DECISIONS.md:353-367` тем же текстом
(«не отложен»); порядок (начать после TEST-закрытия именно D15b/6, а не после code-cutover) — явный выбор
владельца, повторён в `TRACK_D_ORCHESTRATION_HANDOFF_2026-08-21.md:129-135`. Отчётов `D15B7_*` в
`docs/_TODO/runs/integrator-cleanup/` нет (есть только D15B1…D15B6) — этап по факту не начинался, целиком
зависит от завершения D15b/7a.

**D15b/7a — разделение actor/subject ref.** Швы УЖЕ существуют, но НЕ разделены: `packages/db-principal/src/portContext.ts:18-19`
уже несёт `actorRef?: string; subjectRef?: string;`, но `apps/webapp/src/infra/db/portContextRuntime.ts:355-356`
заполняет оба ОДНИМ И ТЕМ ЖЕ `opaqueIdentityRef` — ровно то стартовое состояние, которое описывает план
(«на совместимом первом этапе оба UUID разрешаются в текущий `platform_users.id`»). Отчёта о завершении
нет. Осталось (по плану, `WORK_ORDER.md:741-751`): разнести opaque actor/identity ref и opaque
medical-subject ref внутри уже существующего seam (`app_ext.variant_a_identity_refs`,
`app.pre_session_resolve_identity`, `PortContextPrincipal.actorRef/subjectRef`, `portContextRuntime`), без
второго linkage-сервиса/HTTP-hop/параллельного хранилища; type-aware resolver обязан fail-closed на путанице
actor↔subject; аудит только на пересечении границы, не на каждый запрос; массовый перенос клинических FK — не
в этот срез.

### 4. Чем заблокирован / зависимости

- D15b/2 не в полёте нигде, кроме брифа-коррекции — фикс НЕ приземлится сам, если никто не запустит его
  исполнение (бриф написан, воркер по нему ещё не запускался — `git log --oneline 9208262e9..HEAD` в клоне
  D25 показывает только мержи, кода нет).
- D15b/6 зависит только от живого действия владельца (ввод OTP-кода на TEST), это НЕ технический блокер.
- D15b/7 зависит от TEST-закрытия D15b/6 — очерёдность владельца, не техническая невозможность.
- D15b/7a ничем не заблокирован технически прямо сейчас.
- D15b в целом блокирует D17 (список гейтов в D17 явно называет D15b).

**Возможные конфликты file-scope, которые нашёл агент:** K5-фикс (D25/D15b/2) трогает `portContextRuntime.ts`
и объявления способностей port-context — те же файлы, что расширяет D15b/7a. Сегодня строк-пересечений нет,
но координация желательна, если оба пойдут одновременно.

---

## D17 — узкая роль в базе (`WORK_ORDER.md:996-1007`)

### 1. Требование ещё живо?

Да, и чекбокс уже сам себя поправил: зачёркнутое `~~Сегодня у него та же роль, что у вебаппа~~` с врезкой
«⚠️ ФАКТ УСТАРЕЛ 20.08» переносит владение механикой/статусом узкой роли в
`docs/_TODO/DB_PRIVILEGE_LAYER_REBUILD/PLAN.md`, а сам D17 переопределён не как «создать роль», а как
«подтвердить отсутствие писателей канона к моменту дальнейшего сужения». Правило 5.1.5 того же WORK_ORDER
(«права интегратору не выдаются ролью, недостающие чтения — fail-open») отдельно помечено `⚠️ УСТАРЕЛО 20.08`
той же логикой. `docs/OWNER_DECISIONS.md:376-379` (владелец 08–09.08, «Права БД, роли и стены») требует не
дублировать решения — они живут один раз в OWNER_DECISIONS.md. PLAN.md (562 строки, Ф0–Ф9) НЕ содержит
отдельного раздела «D17» — весь документ (стадии Ф4/Ф6/Ф7/Ф8) и есть то, во что «узкая роль» превратилась;
это подтверждает, а не опровергает актуальность D17-текста. Более позднего owner-решения, переоткрывающего
цель узкой роли, не найдено (хвост `NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`, блок «Track D continuation —
21.08» — только про D15b/6, не про D17 саму по себе).

### 2. Не сделано ли это уже — и иначе?

ЧАСТИЧНО — именно так, как утверждает чекбокс. `deploy/postgres/privileges/declaration.ts:1851-1855`:
`bcb_dev_integrator: { canonicalRole: 'app_integrator_request', memberships: [...six specific roles...],
login: true, connect: ['bcb_webapp_dev'] }`, идентично для `bcb_test_integrator` (`:1871-1875`) — против
`bcb_dev_webapp_staff` (`:1838-1842`) с ДРУГИМ набором членства (`app_pre_session, app_staff,
app_clinic_billing, app_worker, app_tenant_service, app_operational_media_worker,
app_operational_maintenance, saas_telemetry_operator`). Интегратор-логин уже исключает `app_staff`,
`app_clinic_billing`, `app_worker`, `app_operational_maintenance`, `saas_telemetry_operator` — материально
уже ỳже, отдельно объявлен, а не «та же роль». `deploy/host/deploy-test.sh:320-325` гоняет
`reconcile-access.mjs`, который ассертит именно этот набор и валит выкатку при расхождении — соответствует
собственному тексту D17 «деплой ассертит точный набор прав».

Чего НЕ хватает (это и есть остаток D17): роль отдельная, но ещё не достаточно узкая — живые пути записи
канона всё ещё реальны под её членством. Код подтверждает: `apps/integrator/src/infra/db/writePort.ts:279`
зовёт `writeIdentityAndPreferencesDirect(...)`, `:365` — `applyMessengerPhonePublicBind(...)` — оба пишут
канонические идентичность/контакты из-под роли интегратора. Механизм = готов; предусловие «нет писателей
канона» = открыто.

### 3. Что реально осталось

- **D15b/6 не закрыт** (см. выше) — живой блокер.
- **D15b/2 (=D25)** — два достижимых writer-пути в `writePort.ts:279,365`, подтверждены живым чтением.
- **D15b/7/7a** — секвенированы строго после D15b/6.
- **PLAN.md Ф8 (TEST-репетиция)** сама заблокирована требованием «OWNER-REPLACED 16.08.2026: TEST запрещено
  трогать до полного зелёного DEV-прохода» (`PLAN.md:526`) — то есть даже после закрытия D15b/6 на DEV нужен
  полный зелёный проход по матрице роль/страница/действие/воркер/scheduler/доставка ПЕРЕД TEST, прежде чем
  D17 может быть выдан.
- **Сам акт D17** после снятия всех писателей: сузить членство `bcb_dev_integrator`/`bcb_test_integrator`
  (`declaration.ts:1851-1855,1871-1875`), перегенерировать/прогнать `reconcile-access.mjs`, получить зелёный
  деплой.
- **D19** — пост-проверка ПОСЛЕ D17, явно секвенирована следующей, не частью D17.

### 4. Чем заблокирован / зависимости

Проверена вся цепочка гейта D17 по факту (не на слово):

| Пункт | Статус в WORK_ORDER.md | Строка |
|---|---|---|
| D3–D8 | `[x]` все закрыты | 440,450,454,470,475,484 |
| D11, D12 | `[x]` закрыты | 527,534 |
| D13a, D13b | `[x]` закрыты | 539,548 |
| D14 | `[x]` закрыт | 554 |
| **D15b** | **`[ ]` ОТКРЫТ** — под-шаги 1,3,4,5 `[x]`, **2, 6, 7 `[ ]`** | 575 (595,661,735) |
| D16 | `[x]` закрыт | 775 |
| D18 | `[x]` закрыт | 781 |
| **D17** | `[ ]` открыт (по конструкции, ждёт D15b) | 996 |

Вывод: **D15b — единственный оставшийся открытый блокер в списке гейта**; все семь остальных названных
предусловий реально `[x]`, не только заявлены. Внутри D15b живой блокер — именно D15b/6 (mid-fixer-cycle на
момент замера).

**Коллизия file-scope, важная для параллели:** `deploy/postgres/privileges/declaration.ts` НЕ дизъюнктен с
активной работой D15b/6 — его правил тот же файл коммитом `995144016` (репарация privilege-metadata после
canonical contacts cutover, убраны сброшенные `platform_users` contact-колонки из 11 блоков
`relationSurfaces`, плюс `app_staff`/`app_tenant_service` гранты в `relation-access.ts`) в рамках починки
причины падения TEST 21.08 (см. D30 ниже — тот же коммит). **Начинать сужение D17 на этом же файле сейчас —
реальный риск merge-коллизии**, не только концептуальный.

---

## D19 — перепроверить правило и схему ПОСЛЕ реализации (`WORK_ORDER.md:1008-1020`)

### 1. Требование ещё живо?

Да, известная находка ещё НЕ починена. `git log -- apps/webapp/ARCHITECTURE.md` показывает: файл не
трогали с `48c97f389` (30.07, «уточнить правило импортов и описать целевую схему»); ни один doc-sync коммит
после 20.08 его не касался. WORK_ORDER же активно правился до 21.08 — то есть строка `ARCHITECTURE.md:50`
реально осталась непочиненной, а не «забыли отметить фикс». D19a (под-пункт «после D18 заново перемерить
import allowlist») закрыт корректно и проверяемо: `docs/_TODO/runs/integrator-cleanup/D19A_IMPORT_BOUNDARY_EVIDENCE_2026-08-03.md`
показывает census 3→0 production-совпадений, текущий `apps/webapp/eslint.config.mjs:27-73` держит
`no-restricted-imports` на `@/infra/db/*`/`@/infra/repos/*` для модулей и route-хендлеров, allowlist в файле
не осталось — этот чекбокс достоверен.

### 2. Не сделано ли это уже — и иначе?

Нет — устаревшая строка буквально ещё на месте: `apps/webapp/ARCHITECTURE.md:50` — «same `DATABASE_URL` and
DB **role** for both services». Прямо противоречит собственному тексту D17 (`declaration.ts:520-527`,
`bcb_test_integrator_login … canonicalRole: 'app_integrator_resolver'`, отдельно от webapp-ролей) — «одна и
та же роль» уже неверно СЕГОДНЯ, независимо от того, завершён ли полный cutover D17. Остальная часть
`ARCHITECTURE.md` (строки 22-99, «Целевая схема») в основном уже совпадает с реальностью и правки не
требует: единственный цикл доставки (~69) соответствует закрытому D16; «один путь пишет канон» (:50, второе
предложение) в целом согласуется с закрытым D18; запрет прямых импортов (:40-49) уже отражает уточнение
30.07 и совпадает с чистым census D19a; формулировка про будущую узкую роль (:90-93, «до этого момента
изоляция обеспечивается только договорённостью в коде») ОСТАЁТСЯ верной, поскольку D17 реально ещё открыт —
это не устаревший, а корректный прогноз. То есть единственная найденная стухшая строка — именно line 50,
пункт про DB role.

### 3. Что реально осталось

- Починить `ARCHITECTURE.md:50`: заменить «same `DATABASE_URL` and DB role for both services» на
  формулировку «общий `DATABASE_URL`/база, но РАЗНЫЕ DB-роли» (интегратор: `app_integrator_resolver`/узкая,
  `declaration.ts:520-527`; webapp: `app_staff`/`app_patient`/… `:533-543`) — чисто текстовая правка, ровно
  та, что прописана в `WORK_ORDER.md:1013-1015`.
- Перепроверить «остался ли путь записи канона» — ответ сейчас «да, частично»: D15b/2 (`writePort.ts:279,365`)
  ещё пишет идентичность/контакты из интегратора; это нужно переподтвердить как часть финального прохода
  D19, а не считать закрытым по одному только D18.
- Перепроверить «выдана ли узкая роль и совпадает с тем, что ассертит деплой» — нельзя закрыть, пока D17
  открыт (гейт `D15b/2, D15b/6, D15b/7 → D17`).
- Формальная запись закрытия правкой документа или явным «расхождений нет» (`WORK_ORDER.md:1015`) — устного
  подтверждения недостаточно.

### 4. Чем заблокирован / зависимости

D19 заблокирован не только D17: он сидит внутри диапазона «D11–D18» (порядок исполнения, шаг 10,
`WORK_ORDER.md:182`), и D15b (часть этого диапазона по факту, т.к. D17 сам ждёт D15b) — тоже реальный
предшественник. Но **текстовая правка `ARCHITECTURE.md:50` — чисто документная и дизъюнктна по file-scope**
с кодом D15b/D17 (`apps/integrator/src/infra/db/repos/*`, `packages/platform-merge/*`,
`deploy/postgres/privileges/declaration.ts`, миграции) — может идти СЕЙЧАС как частичный проход D19, без
претензии на полное закрытие («частично: текст поправлен; проверка узкой роли/писателей канона всё ещё ждёт
D15b/D17»).

---

## D30 — разворот архитектуры запуска по расписанию (`WORK_ORDER.md:1356-1372`)

### 1. Требование ещё живо?

Да. Последнее решение владельца — Р-D30 (`WORK_ORDER.md:303-321`, 31.07 и 20.08): слияние worker+scheduler в
один резидентный процесс, один замок/цикл, `outgoing_delivery_queue` — единственная очередь повторов,
`integrator.message_retry_jobs` не мигрируется/не дренируется. Более позднего разворота не найдено. Ш9
(слияние топологии) подтверждённо закрыт 21.08 — `D30_SCHEDULER_REVERSAL_PLAN.md:469` `[x]`, независимо
проаудирован PASS (`NIGHT_WAVE...md:1626-1633`).

**Найден реальный дрейф ВНУТРИ самого «свежеправленного» текста WORK_ORDER.** Абзац `WORK_ORDER.md:1356-1372`
последний раз правился `6ca11f8a3` (21.08, 16:23) и до сих пор описывает шаг **(1)** — read-only сверку
`specialist_tasks` на именованных DEV/TEST — как «предстоит». Но ПОЗЖЕ В ТОТ ЖЕ ДЕНЬ эта сверка реально была
выполнена и задокументирована: `docs/_TODO/runs/integrator-cleanup/D30_SPECIALIST_TASK_TEST_LIVE_FAILURE_2026-08-21.md`,
принята PASS в очереди аудита (`NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md:1660`, «CENSUS PASS»). Абзац D30 в
WORK_ORDER.md после этого больше не правился (последующие коммиты `10a06bccd` и `b5e0f4e1d` трогают только
разделы D15b/6, D15b/7, D25 — не абзац D30). То есть в рамках одного и того же дня чек-лист D30 в WORK_ORDER
уже на шаг отстаёт от собственного детального плана.

Проверены оба названных коммита на отсутствие противоречий: `91c101429` (снятие устаревшего dual-host/PROD
cronport-гейта) правит только `D30_SCHEDULER_REVERSAL_PLAN.md`, не WORK_ORDER.md — WORK_ORDER изначально не
упоминал dual-host/PROD требование, сверять нечего; `10a06bccd` (синхронизация D15b/6, D15b/7, D30 Ш3 с
пост-деплойным статусом на `92cf34ffa`) правит D30 только внутри `D30_SCHEDULER_REVERSAL_PLAN.md` (подраздел
Ш3), не абзац D30 в WORK_ORDER.

### 2. Не сделано ли это уже — и иначе?

**Шаг (1) — read-only сверка DEV/TEST — УЖЕ СДЕЛАН**, вопреки формулировке WORK_ORDER. Из
`D30_SPECIALIST_TASK_TEST_LIVE_FAILURE_2026-08-21.md`: для обеих именованных баз (`bcb_webapp_dev`,
`bersoncarebot_test`) все четыре метрики — `future open`, `future without queue`, `all open with remind_at`,
`all without queue` — равны **0**. «Легаси-бэклога специалист-задач для миграции/дренажа нет.» Далее там же
зафиксирован провал живого гейта создания (`SQLSTATE 42501 permission denied for table specialist_tasks`),
который потом починен (`b254ec1bf`, `NIGHT_WAVE:1664/1666`) и задеплоен на TEST в том же merge `92cf34ffa`
(отмечено `УСТАРЕЛО/ЗАМЕНЕНО 21.08.2026` в `D30_SCHEDULER_REVERSAL_PLAN.md:359-366`), с прошедшим
`reconcile-access` на этом деплое — но сама последовательность create/update/complete/delete/резидентная
доставка ПОВТОРНО против починенного гранта ещё не прогонялась.

**Шаг (4) — снятие host cron через `cronport` для DEV/TEST — фактически уже no-op.** По телу коммита
`91c101429` и принятой строке очереди (`NIGHT_WAVE:1673`): `node /home/dev/brain/tools/cronport.mjs list`
уже выполнен на DEV/TEST-хосте (`151.241.228.122`) и не нашёл НИ ОДНОЙ записи
specialist-task/operator-health-digest/system-health-guard/operator-health-probe — только посторонние
brain/backup/hygiene строки. Снимать на DEV/TEST для этих семейств задач нечего; PROD остаётся отдельным
owner-gated будущим действием.

**Код-сторона снятия cron/route для Ш5/Ш6 уже сделана** — подтверждено прямым чтением каталога:
`deploy/host/cron.d/` содержит только `media-preview`, `operator-health-critical` (намеренный выживший —
исключение Р-D30, «внешняя тревога»), `saas-billing-renewal`, `test-operator-health-critical` — без
digest/guard/probe шаблонов (`adc9b32f2`/`6882e6568`, `NIGHT_WAVE:1638`). `cronJobRegistry.ts:91,111,151`
по-прежнему несёт `system-health-guard`, `operator-health-digest`, `specialist-task-reminders/tick` как
намеренно оставленные legacy-reconciliation строки до прохождения живого гейта, по тексту плана; запись
`operator-health-probe` уже удалена. Текст `deploy/HOST_DEPLOY_README.md:463,560,888` пока корректно (НЕ
устарел) описывает `specialist-task-reminders/tick` как «временное, снимается после census Ш3» — раз живое
доказательство ещё не прошло.

### 3. Что реально осталось

Сопоставлено с собственной последовательностью (1)-(4) WORK_ORDER, исправлено по факту:

- **(1) DEV/TEST census — СДЕЛАНО**, не осталось; текст WORK_ORDER нуждается в докс-правке (у
  `D30_SCHEDULER_REVERSAL_PLAN.md` Ш3 это уже отражено, у абзаца D30 в WORK_ORDER.md — нет).
- **(2) Живая TEST-проверка create/update/complete/delete + резидентная доставка для Ш3** — реально открыто.
  Первая попытка упала на `42501`; починка (`b254ec1bf`) задеплоена на TEST (`92cf34ffa`), `reconcile-access`
  прошёл, но САМА последовательность create/update/complete/delete/доставка против починенного гранта не
  повторена. Указатель: `D30_SCHEDULER_REVERSAL_PLAN.md:307-366` (блок Ш3).
- **(2b) Живая проверка Ш4** — материализация web-push/email код-complete (land `775d900b4`, миграция `0338`
  применена на DEV), но «TEST-доказательство на безопасном провайдере со сходящимися счётчиками
  planned/dueClaimed/sent» не прогонялось. Указатель: `:391-397`.
- **(2c) Живой probe получателей Ш5 + живое наблюдение `lastRunAt` Ш6** — код-сторона закрыта для обоих
  (digest/guard/probe вызовы эквивалентны прежней cron-кадентности), но окно «до/после снятия cron» на живом
  TEST/PROD не наблюдалось. Указатель: `:399-461`.
- **(3) Снятие sweep/route/registry/host cron для закрытых code-side шагов тем же коммитом, что и
  пост-деплойные проверки** — заблокировано доказательствами (2)/(2b)/(2c); по явному порядку плана снимать
  раньше нельзя (cron остаётся до доказательства, чтобы не образовать разрыв между снятием и cutover).
- **(4) TEST-наблюдение, затем фактическое `cronport`-снятие для Ш4/Ш5/Ш6** — для DEV/TEST-хоста уже no-op по
  census (см. §2); остаётся именно живая половина наблюдения (период схождения `lastRunAt`) плюс
  registry/route/doc-правка после закрытия (2)/(2c). PROD прямо вне этого объёма до отдельного разрешения
  владельца.
- **Ш1** (`organizationId` в очереди доставки) код-complete, но его собственный живой dev-run гейт
  (`reminder_dispatch: pending→processing→sent` + схождение `/api/admin/system-health`) отдельно на этом
  дереве не переподтверждался (`:548-552`).

### 4. Чем заблокирован / зависимости

Заявленная в брифе посылка («TEST сейчас ненадёжен, `deploy-test.sh` падал 21.08 на 9 часов, отдельный
воркер занят root cause») подтверждена, но уже **в основном решена на ветке, которой нет в этом рабочем
дереве**: `docs/_TODO/runs/briefs/TEST_DEPLOY_FAILURE_ROOT_CAUSE_BRIEF_2026-08-22.md:19-20` — TEST-юниты
легли 21.08 в 09:04:44, поднялись только в 18:17:03 (~9 часов), распоряжение владельца 22.08 «разберись,
почему не работает тест». Причина найдена и уже ПОЧИНЕНА в дереве `feat/doctor-ui-rebuild` (в этом
worktree её нет — `git merge-base --is-ancestor bdb88451d HEAD` → не предок; `--is-ancestor bdb88451d
feat/doctor-ui-rebuild` → да): коммит `995144016` починил три источника устаревшей privilege-метадаты
(`declaration.ts`, `function-census.ts`, `relation-access.ts`), всё ещё объявлявших пять `platform_users`
contact-колонок, сброшенных миграцией cutover D15b/6 (`38b917aba`); отчёт `bdb88451d` подтвердил, что
`995144016` — предок текущего `feat`-HEAD, 0 оставшихся устаревших ссылок, живой каталог TEST не содержит ни
одной из пяти сброшенных колонок — принят PASS в очереди аудита («следующий деплой TEST должен пройти этот
шаг»). **Живого повторного прогона `deploy-test.sh` ещё не было** (намеренно не запускался — граница), и
открыт следующий бриф (`59cb18308`, последний коммит на той ветке) про алертинг на будущие сбои деплоя —
значит уверенность в TEST-деплое сейчас «причина понята + точечный фикс приземлён», а не «свежедоказано
end-to-end».

Отсюда: шаг (2) (живая TEST-проверка) действительно требует здорового TEST — по определению это живой
create/update/complete/delete + резидентная доставка через существующий аккаунт владельца на активно
работающих, корректно привилегированных TEST-сервисах. Начинать его из этого рабочего дерева без
предварительного подтягивания фикса `995144016`/`bdb88451d` и подтверждения ведущим свежего успешного
`deploy-test.sh` не стоит. Шаг (1), напротив, здорового TEST не требовал вовсе — сделан read-only
`BEGIN READ ONLY` транзакцией по обеим именованным базам и уже закрыт.

**Коллизия file-scope, важная для параллели:** D30's остаточные точки касания — `deploy/host/cron.d/*`,
`apps/webapp/src/modules/operator-health/cronJobRegistry.ts`, `deploy/HOST_DEPLOY_README.md`,
`D30_SCHEDULER_REVERSAL_PLAN.md`/абзац D30 в WORK_ORDER.md — дизъюнктны с файлами D15b/6
(`packages/platform-merge`/`user_contacts`) и с D19 (не стартует раньше времени). Но D17 (узкая роль) тоже
трогает `deploy/postgres/privileges/declaration.ts`/`relation-access.ts`/`function-census.ts` — те же файлы,
только что починенные `995144016` для TEST-краша — поэтому D17 и дальнейшая privilege-уборка D30 Ш5
(снятие digest/guard route-строк, `D30_SCHEDULER_REVERSAL_PLAN.md:421-429`) не должны идти по этим трём
файлам нескоординированно одновременно.

---

## Что можно запускать параллельно прямо сейчас

| Работа | File-scope | Требует здорового TEST? | Коллизии |
|---|---|---|---|
| D25/D15b/2 коррекция K5/K8 (бриф `D25_CORRECTION_K5_K8_BRIEF_2026-08-22.md`, отдельный клон `wt/d25-remove-integrator-identity-writers-20260821`) | `apps/integrator/src/infra/db/writePort.ts`, `directPublic/*`, новая timestamp-forward миграция на `app.integrator_bind_bootstrap_channel_phone` | Нет (DEV) | Координировать с D15b/7a — обе трогают `portContextRuntime.ts`/объявления способностей, строк-пересечений пока нет |
| D15b/6 — довести email-OTP челлендж владельца на TEST | нет кода, живое действие владельца | Да (уже живой, работает) | Нет |
| D15b/7a — разделение actor/subject ref | `packages/db-principal/*`, `portContextRuntime.ts`, `app_ext.variant_a_identity_refs` | Нет (DEV) | См. выше про D25/K5 |
| D19 — текстовая правка `apps/webapp/ARCHITECTURE.md:50` | только сам файл | Нет | Нет — чисто докс |
| D30 шаг (1) → докс-правка абзаца WORK_ORDER (шаг уже закрыт, текст отстал) | `WORK_ORDER.md` (абзац D30) | Нет | Нет |
| D30 — переподтверждение, что `995144016`/`bdb88451d` фикс в целевой ветке ПЕРЕД новым TEST-деплоем | read-only | Нет | Нет |
| D17 — сужение членства `declaration.ts`/`reconcile-access` | `deploy/postgres/privileges/declaration.ts`, `relation-access.ts`, `function-census.ts` | Да (гейт Ф8: TEST запрещён до полного зелёного DEV) | **НЕ запускать сейчас** — ждёт закрытия D15b (2 и 6), и коллидирует по файлам с любой параллельной privilege-уборкой D30 Ш5 |

**Нельзя параллелить:** D30 шаг (2)/(2b)/(2c) (живые TEST-проверки Ш3/Ш4/Ш5/Ш6) — ждут подтверждённо
здорового TEST-деплоя после `995144016`; D17 сужение роли — ждёт закрытия D15b/2 и D15b/6; D19 полное
закрытие (кроме докс-правки строки 50) — ждёт D17.

---

## ВОПРОСЫ ВЛАДЕЛЬЦУ

- `apps/integrator/src/infra/db/repos/userChannelBotBlocked.ts:41-50` вставляет каноническую строку
  `user_channel_bindings` на событии bot-blocked, если её ещё нет. Это «факт доставки» (в объёме доставки
  доктора) или «синхронизация идентичности» под Р-D25? Сейчас это НЕ входит в перечень WORK_ORDER (там
  названы ровно два writer-пути для D15b/2) — это третий достижимый канонический writer со стороны
  интегратора, найденный независимым аудитом D25, не изобретённый здесь.
- K8-фикс (вторая OUT-колонка у `app.integrator_bind_bootstrap_channel_phone`) — должен ли жить внутри
  `apps/integrator`, или изменение корня пересекает границу пакета и требует отдельного согласия владельца?
  Сам аудитор D25 поднял это как открытый вопрос, не как решённое.
- `apps/integrator/src/infra/db/messengerPhonePublicBind0380.unit.test.ts` теперь тестирует функцию
  (`applyMessengerPhonePublicBind`), которую после удаления D25-writer'ов зовёт только вебапп — переносить
  файл или оставить? Не решение владельца, а нерешённая уборка, зафиксирована здесь для триажа.

## НЕ СДЕЛАНО

- Не разбирался D25 сам по себе (по прямому распоряжению брифа — он на независимом аудите), кроме как
  зависимость D15b/2/D17 от его исхода.
- Не проверялись Track A/B/C/E и остальные закрытые `[x]` пункты Track D — вне объёма (только D15b, D17,
  D19, D30).
- Не запускался `node /home/dev/brain/tools/cronport.mjs list` живьём этим агентом — использован уже
  зафиксированный в репозитории вывод (`91c101429`, `NIGHT_WAVE:1673`), т.к. это живая host-команда, а не
  чтение репозитория; свежий повторный прогон не входит в read-only сверку плана.
- Правка `apps/webapp/ARCHITECTURE.md:50` НЕ выполнена (запрещено брифом трогать код/доки продукта) — только
  зафиксирована как готовый к немедленному запуску, дизъюнктный по file-scope атомарный пункт.
- Текстовая рассинхронизация абзаца D30 в WORK_ORDER.md (шаг (1) описан как «предстоит», хотя уже сделан и
  принят в очереди аудита) НЕ исправлена — только зафиксирована находкой; правка чек-листа плана владельца
  не входит в read-only-объём этого свипа.
