# Аудит: postgres-job доведён до зелёного (`0ba4224b5`)

Rules: `AGENTS.md` — Маршрут, CORE rules, §1, §6, §9, §24. Language: internal work is English.

Authority: гейт перед сведением ветки `wt/ci-pg-green`. `ORCH_OPS`.

**Тест или взгляд:** ТЕСТ. Проверяется, действительно ли прогон зелёный по существу, а не потому, что ожидания
подогнали под красное. Это видно только прогоном.

Источник оракула — бриф исполнителя, дословно: «Не отключать и не помечать `skip` ни один тест ради зелёного…
чинить код под тест, а не наоборот».

## Что проверяешь, скептически

Исполнитель починил семь вещей и утверждает, что прогон зелёный целиком (38 файлов, 125 тестов).

1. **Прогони сам.** Зелёный или нет; лог назови.
2. **Главный риск — подогнанные ожидания.** По каждому месту, где ИЗМЕНЁН ТЕСТ, а не код, скажи независимо:
   тест действительно устарел, или его подвинули под текущее поведение? Мест таких по отчёту четыре:
   путь `9995_…`→`0339_…` (два теста), ожидание нулевого доступа `app_owner` к
   `integrator.user_reminder_occurrences`, вставки в `be_organizations` в purge-тесте.
3. **Грант в новой миграции.** Исполнитель утверждает, что `0350` выдал `UPDATE` без `SELECT` на колонки
   предиката — то есть пробел настоящий. Проверь это сам: воспроизведи отказ до гранта и успех после.
4. **Правка харнесса про `BYPASSRLS`.** Утверждение: одноразовый кластер создавал `app_owner` без BYPASSRLS,
   а на живых окружениях он с ним. Сверь с тем, что реально делает деплой, — если харнесс теперь ВРЁТ в другую
   сторону (даёт больше прав, чем на проде), тесты станут зеленее реальности, и это FAIL.
5. **Обработчик ошибок пула.** Исполнитель говорит, что проблема реальна и для прода. Подтверди или опровергни.
6. **Ни один тест не выключен** — проверь своим поиском `skip`/`todo`/`only` в диффе, а не по отчёту.

## Ответ

Вердикт `PASS` или `FAIL` первой строкой, затем по пунктам — команда и что увидел. Ничего не чини.
