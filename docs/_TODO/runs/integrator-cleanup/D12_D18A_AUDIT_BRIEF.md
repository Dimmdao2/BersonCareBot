# MISSION: аудит двух шагов вычистки — запрет на сырой SQL (`4ab706c14`) и вырезание мёртвого (`dead-d12`)

Read-only, тесты запускать можно. Дерево клона должно остаться чистым.

## Authority

- **План:** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, «Track D-полный», пункты **D18a** и **D12**,
  порядок — «ЕДИНЫЙ ПОРЯДОК ИСПОЛНЕНИЯ».
- **Перепись, на которой стоит удаление:** `docs/_TODO/runs/integrator-cleanup/D12B_CENSUS_RESULT.md`.
- **Отчёты исполнителей (проверять, не верить):** `D18A_GUARD_REPORT.md`, `D12_DEAD_REPORT.md` там же.
- **Правила:** `.cursor/rules/tests-check-behaviour-not-circumstances.mdc`,
  `.cursor/rules/clean-architecture-module-isolation.mdc` (раздел наверху).

## Вопросы

1. **Ни одна живая ветка не удалена.** По каждой из десяти удалённых веток проверь САМ, что перехватчик существует и
   реально перехватывает раньше `switch` — исполнитель назвал их по файлам и строкам, сверь независимо. Если хоть у
   одной перехватчика нет или он условный, это блокер: удалили живое.
2. **Ни один живой сценарий не удалён.** 27 сценариев контента должны быть недостижимы: ни меню, ни колбэк, ни команда,
   ни другой сценарий на них не ведут. Проверь выборочно, но не менее чем по трети, и назови, что именно проверял.
3. **Шаблоны.** Удалены только те, что больше нигде не используются; оставшиеся действительно кем-то используются.
4. **Запрет на сырой SQL честный.** Проверь, что его нельзя обойти: (а) новый файл с сырым вызовом роняет проверку;
   (б) удаление строки из списка при живом сыром вызове тоже роняет; (в) обёртка drizzle `sql`…`.execute()` не считается
   нарушением. Прогони проверку сам и приведи вывод.
5. **Список долга соответствует реальности:** 17 файлов интегратора и 30 вебаппа — пересчитай своей командой и скажи,
   сходится ли; расхождение назови поимённо.
6. **Ничего лишнего.** `git diff --stat` по обоим коммитам: только целевые файлы; вебапп, семь файлов `directPublic`,
   реестр доменных действий и миграции не тронуты.
7. **Галочка в плане.** Исполнитель D18a сам отметил пункт в плане, хотя это работа лида. Скажи, соответствует ли
   отметка реальности — лид будет её переставлять сам.

## Прогоны

Интегратор `typecheck`, `lint`, его тесты; `check-no-new-raw-sql`; корневой `lint`. Полный CI не гонять. Приведи числа.

## Output

`VERDICT: PASS | PASS WITH FIXES | FAIL`, ответы по семи вопросам с доказательствами, нумерованный MUST FIX (пустой —
допустимый ответ), и подтверждение, что дерево клона чистое.
