# D10a, часть 1 — перевести наблюдение с `outgoing_delivery_queue` (run: worker-d10a-observers)

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D10a**. Прочитать дословно.

**Правила репозитория, обязательные к прочтению до правки:**
`.cursor/rules/tests-check-behaviour-not-circumstances.mdc`, `.cursor/rules/clean-architecture-module-isolation.mdc`,
`.cursor/rules/webapp-tests-lean-no-bloat.mdc`.

## Границы задачи — прочитать раньше, чем начнёшь

Решение владельца 30.07: журналов доставки должно остаться ОДИН — `public.notification_delivery_attempts`.
`integrator.delivery_attempt_logs` сносится, `public.outgoing_delivery_queue` убирается.

⛔ **Здесь НИЧЕГО НЕ СНОСИТСЯ.** План задаёт порядок прямо: сначала перевести потребителей метрик и
сердцебиения с `outgoing_delivery_queue` на оставшуюся очередь интегратора — «иначе снос ослепит наблюдение», —
и только ПОТОМ сносить таблицы миграцией, причём снос идёт вместе с D16. Твоя задача — ровно первая половина:
**перевести наблюдателей**. Миграций не писать, таблицы не трогать, `DROP` не добавлять.

## Что сделать

1. **Перепись потребителей.** Найти ВСЕ места, которые читают `outgoing_delivery_queue`: метрики, сердцебиение,
   админские экраны, скрипты, алерты. Искать индексом — `node /home/dev/brain/tools/code-search.mjs "<запрос>"
   --repo bcb`, — и по смыслу, а не только по точному имени таблицы. Перечислить найденное списком в отчёте:
   путь, что именно считает, куда выводит.
2. **Перевести каждого** на очередь доставки интегратора, сохранив СМЫСЛ показателя, а не форму запроса.
   Если показатель на новой очереди не воспроизводится один в один — не подгонять молча: назвать расхождение
   в отчёте и оставить пункт нерешённым.
3. **Ничего не удалять.** Таблица остаётся на месте и продолжает жить; меняются только читатели.

## Приёмка

- построчный список потребителей: было → стало, с путями;
- тест на каждый переведённый показатель: считает по новой очереди и даёт ожидаемое число;
- тест краснеет, если показатель начинает считать не то — приложить дословный вывод до и после;
- доказать, что старых чтений `outgoing_delivery_queue` в переведённых местах не осталось (вывод поиска);
- `pnpm --filter webapp lint`, `pnpm --filter @bersoncare/integrator lint`, оба `typecheck` зелёные;
- галочки плана не ставить, push и merge не делать, миграций не создавать.
