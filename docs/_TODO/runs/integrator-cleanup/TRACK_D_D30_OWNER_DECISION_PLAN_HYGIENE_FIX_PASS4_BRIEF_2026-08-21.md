# Track D / D30 — current-plan contradiction closure, pass 4

Роль: same-branch mechanical docs fixer в `wt/trackd-plan-hygiene-20260821`. Это продолжение одного
приёмочного прохода после коммита `a7973760f`; нового аудита и нового scope нет.

До действия прочитать карту `AGENTS.md`, §«Как решать, что делать», §0, §1 checkbox rules, §7, §12 и §24.
Повторить `code-search` и точный поиск более поздних owner-решений. Текущий oracle:
`docs/OWNER_DECISIONS.md` → «Track D — текущий scope» → решение 21.08.2026: таблицы
`integrator.message_retry_jobs` на named DEV нет; D30 не включает её перенос/дренаж/ожидание 29.08.
Более позднее owner-решение, если найдётся, заменяет этот brief и должно быть явно названо в результате.

## Exact finding приёмки

Коммит `a7973760f` неполон: активный `D30_SCHEDULER_REVERSAL_PLAN.md` всё ещё хранит отменённый Ш7 и старую
механику как зачёркнутый checkbox, `DROP TABLE` как зачёркнутую точку невозврата, строку риска про старый
`plan[]/onFail`, снятый PROD-вопрос и рассказ о дренажах 03–04.08. Handoff всё ещё показывает сначала старое
«упирается до 29.08», затем новое. Это прямо нарушает исходный brief: несовместимое старое описание не должно
оставаться в current plan/handoff даже под `УСТАРЕЛО/ЗАМЕНЕНО`; история уже есть в Git и завершённых reports.

## Scope

- `docs/_TODO/runs/integrator-cleanup/D30_SCHEDULER_REVERSAL_PLAN.md`
- `docs/_TODO/runs/integrator-cleanup/TRACK_D_ORCHESTRATION_HANDOFF_2026-08-21.md`
- `docs/_TODO/runs/integrator-cleanup/TRACK_D_D30_OWNER_DECISION_PLAN_HYGIENE_FIX_RESULT_2026-08-21.md`

Остальные файлы, код, миграции, AGENTS/canon, DB, DEV/TEST/PROD, deploy и ветки не менять.

## Исправление

1. Полностью удалить из current D30 plan отменённый checkbox Ш7 вместе с пояснением и ссылками на старый
   drain; не заменять его второй исторической заметкой. Нумерацию реально открытых Ш8/Ш9 сохранять: их IDs
   принадлежат текущему плану.
2. Удалить из current D30 plan зачёркнутую `DROP TABLE` точку невозврата и перенумеровать оставшуюся точку.
3. Удалить строку риска про потерю `plan[]/onFail`, снятый вопрос про PROD-объём старой таблицы и любые другие
   prose-блоки, которые существуют только для объяснения отменённой механики. Не удалять нейтральное техническое
   сравнение старой и целевой очередей, если оно нужно для объяснения уже принятого архитектурного выбора и не
   является инструкцией переносить/дренировать.
4. В handoff убрать старую зачёркнутую фразу «D30 честно упирается…». Оставить только положительную текущую
   формулировку: отсутствующую очередь не переносим; реальная работа — Ш9 и действительно открытые Ш1–Ш6/Ш8.
5. Исправить result-файл: он не должен утверждать, что оставшиеся зачёркнутые старые формулировки были допустимы.
   Перечислить фактически удалённые этим pass совпадения и честный итоговый census.

Не менять трёхсостояние чекбоксов других owner-requirements. D39 уже закрыт в `a7973760f` и повторно не трогать.

## Gate и commit

- `git diff --check`.
- Exact `rg` по двум current-файлам: не остаётся старого утверждения «ждать/упирается до 29.08», отменённого
  checkbox Ш7, старого `DROP TABLE` route, PROD re-measure или narrative дренажа 03–04.08.
- Один текущий D30 route остаётся: scheduler/topology, включая Ш9; Ш1–Ш6/Ш8 не закрывать.
- Явно stage только три scoped path, без `git add -A`; commit до завершения хода.
- В финале назвать SHA, команды и `NOT DONE: D30 implementation / landing / TEST / deploy / push / full CI`.
