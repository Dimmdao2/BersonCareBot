# АУДИТ: ловят ли тесты уровня 2 то, ради чего написаны
(run: auditor-d20-level2)

Проверяемая работа — один коммит **`9bbad9dcb`** в клоне `/home/dev/dev-projects/bcb-wt-testsuite-g0`:
+37 тестов интегратора (88 → 125), шесть новых файлов. Отчёт исполнителя —
`docs/_TODO/runs/integrator-cleanup/D20_TESTS_LEVEL2_REPORT.md`.
**Отчёт исполнителя и зелёный прогон САМИ ПО СЕБЕ доказательством не являются.** Именно так в этом
репозитории появились «6/6 зелёных» поверх бага, ломавшего каждый ответ.

## Постановка владельца 31.07 (дословно)

> «У исполнителя интегратора НЕТ ТЕСТОВ ВООБЩЕ — потому что были нерабочие пустые тесты на строки кода.
> Задача будет такая — пройти по каждому модулю, понять что он делает и какое поведение должны ловить
> тесты, и написать их.»

Уровень 2 по карте `D20_INTEGRATOR_MAP.md` — про потерю, невидимую вообще ничем: человек написал, ответа
нет, в логах чисто.

## Что проверять

1. **Поломкой, своей собственной.** Отчёт перечисляет внесённые поломки (T-A4…T-A10 и другие) — НЕ
   повторять их. Придумай СВОИ, в тех же семи продуктовых файлах, и в первую очередь такие, которые
   исполнителю невыгодно было выбирать: не «убрать проверку», а **сдвинуть поведение**: снять
   освобождение dedup-ключа при падении pipeline · дать финализации молча вернуться без записи ·
   потерять причину пропуска канала, оставив сам пропуск · вернуть `null` вместо ошибки. По каждой:
   покраснел ли тест, какой именно, с каким сообщением. **Поломка, которую не поймал ни один тест, —
   находка.** Работать в клоне `bcb-wt-testsuite-g0`; всё сломанное вернуть (`git diff` в конце пуст).
2. **Тест на моках, зелёный при удалении реального кода.** Правило `.cursor/rules/
   tests-check-behaviour-not-circumstances.mdc`. Для каждого нового файла ответить: что произойдёт, если
   удалить проверяемую функцию целиком — тест покраснеет или пройдёт мимо?
3. **Соответствие пунктам 9–13 карты.** По каждому: ловит ли тест ИМЕННО названную там потерю. Пункт 12
   («пропуск канала записывается с причиной») и 13 («канал пропущен с ИМЕНОВАННОЙ причиной») — проверить
   особо: если сегодня причина нигде не записывается, тест обязан быть про отсутствие поведения, а не
   про нынешнее молчание.
4. **Правка среды.** Исполнитель создал `.pnpm-store-local/` и собрал четыре воркспейс-пакета, утверждая,
   что `package.json`, lockfile и конфиг pnpm не тронуты. Проверить это по диффу против `HEAD~1`.
5. **Не сузил ли он задачу.** Все ли пять модулей уровня 2 покрыты, или часть тихо осталась.

## Как отвечать

Вердикт **PASS / SHIP-WITH-FIXES / FAIL** + таблица: находка · файл и строка · чем доказана (текст
поломки и вывод прогона) · что чинить.

**Не расширять область.** Требования берутся из карты D20 и слов владельца. Идеи «как ещё улучшить
тесты» — отдельный раздел «вне области», работой они не становятся.

## Приёмка

Файл `docs/_TODO/runs/integrator-cleanup/D20_LEVEL2_AUDIT.md`. Продуктовый код и тесты после аудита
обязаны вернуться в состояние коммита `9bbad9dcb` — проверить `git status` и приложить его вывод.
