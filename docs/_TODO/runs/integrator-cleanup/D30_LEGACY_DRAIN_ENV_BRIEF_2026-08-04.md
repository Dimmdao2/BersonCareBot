# D30 Ш7 — расшить DEV-воркер и слить остаток старой очереди

Rules: `AGENTS.md` — Маршрут, CORE rules, §1, §6, §9, §24. Internal work in English.

⚠️ Один ход, следующего не будет; всё на переднем плане; коммит до конца хода; ~25 минут.

Authority: `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — **D30**, и
`runs/integrator-cleanup/D30_SCHEDULER_REVERSAL_PLAN.md` §Ш7. Владелец 04.08 назвал D30 в числе доделок:
«остальное напрямую влияет на работу системы».

Источник оракула: план D30, §Ш7 — legacy consumer и таблица снимаются только «после штатного дренажа и
доказанного post-cutover zero-write периода».

## Что уже доказано (не переизмерять, использовать)

Замер 04.08: состав старой очереди `113` строк — `20 pending` со сроком `06–29.08`, `4 stale processing` от
`25–28.07`, `22 dead`, `67 done`. `max(created_at)` по всей таблице — `2026-07-24`, то есть **zero-write период
10+ суток подтверждён измерением**, а не заявлением. Producer-код закрыт статически до webapp-стороны.

## Чем заблокировано — это окружение, а не код

Фикс reclaim (`a521ca4d2`/`638b7b9cb`, сведён `657b9b3bb`) корректен, но **DEV-воркер не может стартовать**:
единственная DEV-роль приложения `bcb_webapp_dev_user` не состоит в `app_operational_delivery_worker`, которая
нужна SECURITY DEFINER-пробам `assertDeliveryWorkerPoolReady`. На TEST узкие операционные логины по семьям
процессов есть, на DEV — ни одного.

## Работа

1. **Выдать DEV недостающее членство каноническим путём** — тем же провижининг-скриптом, что делает это на TEST
   (`deploy/postgres/c4-operational-runtime.sql` и его обёртка), а не ручным `GRANT` из сессии. Если канонический
   путь на DEV не предусмотрен — сказать это прямо и предложить минимальное расширение скрипта, а не обходной
   путь.
2. **Запустить штатный дренаж** и слить `4 stale processing`. Показать состав очереди до и после запросом.
3. **Доказать, что в старую очередь больше не пишут** — после дренажа `max(created_at)` не сдвинулся.
4. ⛔ **Точку невозврата не проходить.** `DROP TABLE integrator.message_retry_jobs` в этом прогоне НЕ делать —
   по плану это отдельный необратимый шаг. Сказать в отчёте, что осталось до него.

## Границы

- **PROD не трогать.** Только DEV; TEST трогать не нужно.
- Политики и гранты сверх недостающего членства не менять; тела функций не трогать.
- Push не делать.

## Готово значит

- DEV-воркер стартует; членство выдано каноническим путём, названным в отчёте.
- `4 stale processing` слиты, состав очереди до/после показан запросами.
- Записано, что именно осталось до необратимого шага.
- Один коммит на своей ветке.
