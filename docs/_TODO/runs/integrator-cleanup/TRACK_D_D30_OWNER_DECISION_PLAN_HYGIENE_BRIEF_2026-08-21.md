# Track D / D30 — привести активный план и handoff к решениям владельца 21.08

Роль: docs worker в `wt/trackd-plan-hygiene-20260821`. Это один цельный проход по активным инструкциям и
чек-листам Track D/D30; исторические reports, evidence, audit records, логи и архивы не переписывать.

Authority по убыванию приоритета:

1. `docs/OWNER_DECISIONS.md`, раздел `Track D — текущий scope (владелец, 04.08; обновлено 21.08)`.
2. `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` §2.3 `Р-D30` и §3.4 Track D.
3. `AGENTS.md` §«Как решать, что делать», §0, §12 и §24.

Источник оракула: `docs/OWNER_DECISIONS.md` — «`integrator.message_retry_jobs` на именованной DEV нет; ожидания будущих строк до 29.08 нет.»

Перед правками прочитать карту `AGENTS.md`, названные разделы authority и повторить поиск более поздних
owner-решений. Сначала `code-search`, затем точный `rg` по известным строкам. Если найдено более позднее
owner-решение, оно заменяет этот бриф; не смягчать и не оставлять два активных варианта.

## Scope

- `docs/_TODO/runs/integrator-cleanup/D30_SCHEDULER_REVERSAL_PLAN.md`
- `docs/_TODO/runs/integrator-cleanup/TRACK_D_ORCHESTRATION_HANDOFF_2026-08-21.md`
- `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md` — только если фактическая сверка обнаружит
  оставшуюся активную противоречащую строку или неверный чекбокс.
- этот бриф.

## Что исправить

1. В D30 plan убрать из активного маршрута перенос, дренаж, ожидание, PROD-замер и `DROP TABLE` отсутствующей
   `integrator.message_retry_jobs`. Не удалять историю молча: заменённые активные формулировки пометить
   `УСТАРЕЛО/ЗАМЕНЕНО 21.08.2026 →` либо, если это owner-checkbox отменённого действия,
   `[-] ~~...~~ — ОТМЕНЕНО ВЛАДЕЛЬЦЕМ 21.08.2026: ...` по AGENTS.md §1.
2. D30 оставить открытым ровно по фактической оставшейся работе: scheduler/worker в один резидентный процесс,
   один systemd-unit, один замок/цикл, `SKIP LOCKED`, перенос действительно существующих scheduled jobs.
   Не объявлять этот код выполненным без evidence.
3. В handoff убрать активный запрет/ожидание до 29.08 и ложное описание отсутствующей очереди. Явно назвать
   handoff снимком состояния, а каноническое продолжение направить в current WORK_ORDER и owner decisions.
   Остальные фактические SHA/исторические результаты не переписывать.
4. Проверить все активные D30/Track D чекбоксы в этих трёх файлах: готовое оставить `[x]` с существующим
   evidence; реально открытое `[ ]`; отменённое владельцем — только трёхсоставная `[-]` форма. Не оставлять
   метки `не сейчас`, `после 29.08`, `blocked by pending reminders` или два несовместимых active next-step.
5. Точным поиском сделать census других активных планов/чек-листов с `message_retry_jobs`, `2026-08-29`,
   `29.08`, `future reminders`, `будущие напоминания`. Исторические evidence/reports/logs/archives перечислить
   в отчёте, но не менять. Если найден ещё один активный владеющий план вне scope — остановиться с точным списком,
   не расширять scope самовольно.

## Проверки и commit

- `git diff --check`.
- Точный `rg` по активным scoped-файлам доказывает, что ни один исполняемый/открытый пункт не ждёт 29.08 и не
  предлагает мигрировать/дренировать отсутствующую очередь; исторические абзацы явно маркированы заменёнными.
- Сверить ссылки и статусы checkbox вручную; docs-only, без full CI.
- Не менять код, миграции, AGENTS/canon, DB, DEV/TEST/PROD, deploy, ветки или чужие файлы.
- Staging только явных scoped paths, без `git add -A`. Коммит содержит `#987`, почему, чем доказано, какой пункт
  закрывает и `NOT DONE: D30 implementation / landing / TEST / deploy / push / full CI`.
- В конце сообщить SHA, точный список изменённых файлов, команды/exit codes и оставшиеся historical-only matches.
