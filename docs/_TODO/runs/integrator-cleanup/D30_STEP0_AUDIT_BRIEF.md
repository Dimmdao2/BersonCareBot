# АУДИТ: замок планировщика D30 Ш0 — держит ли защита то, что обещает
(run: auditor-d30-step0)

Проверяемая работа — один коммит **`c3922ef85`** в клоне `/home/dev/dev-projects/bcb-wt-fmtcut`.
Отчёт исполнителя — `docs/_TODO/runs/integrator-cleanup/D30_STEP0_REPORT.md`.
**Отчёт и зелёный прогон сами по себе не доказательство.**

Зачем работа: по решению владельца 31.07 задания переезжают с крона в планировщик. **После переезда цена
потери замка — двойная отправка живым людям.** Аудит проверяет не «написан ли код», а «сработает ли защита».

## Что проверять — по убыванию цены ошибки

1. **Потеря замка на живом соединении.** Добавлена `assertStillHeld()` и проверка в каждом тике
   (`schedulerLockedTick.ts`, `main.ts`). Ответить фактами: что произойдёт, если соединение оборвётся между
   проверкой и работой тика · если `pg_try_advisory_lock` вернёт true на ДРУГОМ соединении пула (advisory
   lock сессионный — привязан ли handle к тому же физическому соединению, или пул может подсунуть другое?)
   · если `assertStillHeld` сама бросит по сетевой ошибке — процесс выйдет или продолжит без замка.
   **Это главный вопрос аудита.** Пул соединений — самое вероятное место, где защита окажется мнимой.
2. **Доказательства живут вне CI.** Конкуренция доказана двумя скриптами (`check:d30-*`), которые поднимают
   одноразовый PostgreSQL и запускаются руками. Значит, регресс завтра никто не поймает. Проверить: верно ли
   исполнитель сослался на `.cursor/rules/test-execution-policy.md` (запрет новой DB-механики в vitest), или
   правило допускало обычный тест. Если сослался верно — назвать, чем гейтится регресс, и это строка в
   «развилки», а не работа.
3. **Гейт unit-файлов.** `deploySystemdSchedulerUnitGate.ts` проверяет три свойства. Внести СВОИ поломки в
   реальные `deploy/systemd/*.service` (не в фикстуры) и убедиться, что каждая краснеет: второй unit той же
   среды под другим именем · `Restart=always` вместо `on-failure` · снятый `ConditionHost` при сохранённом
   `ExecCondition` и наоборот. Всё вернуть.
4. **Смысл `process.exit(1)`.** Исполнитель НЕ менял поведение, а переписал комментарий: ненулевой выход
   объявлен механизмом выбора лидера (перезапуск каждые 5 с — это перегонка за замком). Проверить, что это
   правда для сегодняшнего unit-файла, и назвать цену: сколько записей в журнале в сутки даёт standby,
   и не забьёт ли это журнал/алерты. Если цена заметна — находка.
5. **Правка `apps/integrator/package.json`** (две новые команды). Убедиться, что ничего больше не менялось
   и что команды не попадают в сборочные/деплойные цепочки.
6. **Не сузил ли задачу.** Пять кусков брифа: тест захвата · проверка владения в тике · решение противоречия
   с `Restart` · конкурентный `claim` строки очереди · гейт unit-файлов. Что из этого сделано формально.

## Как отвечать

Вердикт **PASS / SHIP-WITH-FIXES / FAIL** + таблица: находка · файл и строка · чем доказана · что чинить.
Каждая находка сопровождается либо выводом прогона, либо цитатой кода. Гипотеза без доказательства
помечается как гипотеза.

**Область — только этот коммит и то, что он затрагивает.** Идеи по устройству планировщика вообще — раздел
«вне области». Работой они не становятся: скоуп заводит владелец.

## Приёмка

Файл `docs/_TODO/runs/integrator-cleanup/D30_STEP0_AUDIT.md`. Всё сломанное вернуть, приложить `git status`.
