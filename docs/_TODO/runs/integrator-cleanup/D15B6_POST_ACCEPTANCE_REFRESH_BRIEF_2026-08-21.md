# D15b/6 — обновление принятого fixer-кандидата до текущей интеграции

Роль: worker. Ветка: `wt/d15b6-audit-20260821`. Принятый продуктовый кандидат:
`63ce08b99933e1445423b7db992a65540d976919`; lead-acceptance:
`acdd5dc9238587d5a2198924613a22afd1d8eeda`. Текущая интеграционная вершина перед запуском:
`a72170ba4d6f6cc26559376b55e8efd37e04485f`.

Перед любым действием прочитать карту `AGENTS.md`, затем §1, §5, §7, §9–§10b и §24. Повторить поиск более
поздних owner-решений в `docs/OWNER_DECISIONS.md`, `docs/ARCHITECTURE/OWNER_PRODUCT_RULES.md`, актуальном
`WORK_ORDER.md` и датированных owner-строках. Если найдено решение позднее 21.08.2026, несовместимое с этим brief,
остановиться с точным `OWNER QUESTION`, не выбирать мягкий вариант.

## Источник оракула

Владелец 21.08.2026: «если контакты все уже существуют, значит ничего переносить не надо, надо удалять просто
дубли ... и проверять, что интегратор правильно обращается к нужной таблице ... напрямую ... через свой порт».
Полный критерий D15b/6 находится в
`docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`; независимый PASS и точные уже зелёные команды — в
`docs/_TODO/runs/integrator-cleanup/D15B6_CONTACTS_PHYSICAL_CUTOVER_FIXER_ACCEPTANCE_2026-08-21.md`.

## Scope

1. Слить в эту ветку текущую `feat/doctor-ui-rebuild` обычным merge-коммитом. Не rebase, не force, не писать в
   интеграционную ветку.
2. Разрешить только реальные конфликты D15b/6; не расширять продуктовый scope и не переписывать уже принятый
   fixer. Сначала проверить, нельзя ли сохранить существующие canonical DB-порты без новой функции/обёртки.
3. На итоговом HEAD повторить только проверки, дающие новый сигнал после интеграции:
   - оба strict typecheck;
   - три D15b/6 integrator test-файла и девять webapp test-файлов из acceptance;
   - migration/generator проверки из acceptance;
   - scoped lint затронутых merge-конфликтом файлов, если конфликты были.
4. Проверить `git diff --check` и чистоту дерева. Отдельный product commit допустим только если merge реально
   вскрыл совместимый дефект; тогда назвать достижимый сценарий и точное owner-требование.

## Жёсткие границы

- Не применять миграцию и не обращаться к DEV/TEST/PROD.
- Не создавать одноразовую базу, fixture/seed/test-account machinery или исторический replay.
- Не запускать full CI, deploy или push.
- Не менять чекбоксы: named-DEV migration и live login/bind/delivery/parity gate ещё не выполнены.
- Не заканчивать ход до завершения foreground-проверок; итоговое дерево должно быть чистым.

Итог: новый HEAD, список merge/conflict изменений, точные команды/exit codes и готовность кандидата к отдельному
lead-owned named-DEV gate.
