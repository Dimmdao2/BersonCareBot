# D12b — перепись ДОСТИЖИМЫХ сценариев исполнителя (run: worker-d12b-census)

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D12b**. Прочитать дословно.

**Правила репозитория, обязательные к прочтению до правки:**
`.cursor/rules/clean-architecture-module-isolation.mdc`. Тестов здесь нет — это перепись, код не меняется.

## Зачем этот пункт существует

Аудит 30.07: удаление десяти мёртвых веток (D12) **не закрывает главную цель** — в исполнителе остаются живые
продуктовые решения. Пока не перечислено, какие именно, заголовок «интегратор = приём и доставка» остаётся
декларацией. Вся цель Track D — довести интегратор до блока доставки сообщений и приёма вебхуков.

## Что сделать

Документ `docs/_TODO/runs/integrator-cleanup/D12B_REACHABLE_CENSUS.md`: каждый ДОСТИЖИМЫЙ сценарий и действие
исполнителя (`apps/integrator/src/kernel/domain/executor/`, начиная с `executeAction.ts`) классифицирован как:

- **«канал/доставка — остаётся»** — работа с транспортом: отправить, повторить, разобрать вебхук, привязать
  адресата, отметить исход доставки;
- **«продуктовое решение — уезжает в вебапп»** — что написать, кому, когда, при каком условии, что считать
  успехом с точки зрения продукта.

Таблица: сценарий/действие → файл и строка → класс → причина отнесения → если «уезжает», то куда именно
(в какой модуль вебаппа) и что для этого нужно.

Требования:

1. **Только ДОСТИЖИМЫЕ.** Недостижимые ветки — предмет D12, не этой работы. Достижимость доказывать: кто
   вызывает, откуда приходит. Искать индексом (`node /home/dev/brain/tools/code-search.mjs "<q>" --repo bcb`),
   не только грепом; пустой результат доказывать перечислением попыток.
2. **Учесть уже сделанное:** D14 закрыт — шесть решений жизненного цикла записи уже уехали (отмена напоминаний,
   вариант пуша, текст пациенту, уведомление врача, календарь) и вебапп их шлёт; D13a/D13b закрыты — план
   напоминаний строит вебапп, константы 24ч/2ч вырезаны. Эти решения в переписи должны быть помечены как
   УЖЕ ПЕРЕЕХАВШИЕ, а не как «уезжает».
3. **Ничего не менять.** Код не трогать, ничего не резать, миграций не писать.

## Приёмка

- таблица покрывает все достижимые сценарии и действия, у каждого класс и причина;
- перечислены попытки поиска, чтобы полнота была доказана, а не заявлена;
- отдельной секцией: сколько продуктовых решений осталось в исполнителе и что мешает перенести каждое —
  со ссылками на пункты плана;
- дерево клона чистое, кроме нового документа;
- галочки плана не ставить, push и merge не делать.
