# D16, перепись — пересчитать вечные циклы поимённо (run: worker-d16-census)

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D16**. Прочитать дословно.

**Правила репозитория, обязательные к прочтению до правки:**
`.cursor/rules/clean-architecture-module-isolation.mdc`. Тестов здесь не пишем — это перепись, см. ниже.

## Что требует план и почему это отдельный шаг

D16 говорит: у воркера должен остаться ОДИН вечный цикл — доставка. И там же прямо записано требование аудита
30.07: «арифметика "ровно один" сегодня не сходится, потому что циклов и точек планирования больше, чем три;
**перед закрытием пункта пересчитать их поимённо и назвать каждый, который остаётся, с причиной**».

⛔ **Это ТОЛЬКО перепись. Ничего не резать, не сливать циклы, код не менять.** Снос транспорта проекции (D10)
ещё не сделан и вдобавок упирается в открытый вопрос владельца (см. пометку у D10a в плане — описание
`outgoing_delivery_queue` там оказалось неверным). Резать до ответа нельзя.

## Что сделать

Документ `docs/_TODO/runs/integrator-cleanup/D16_LOOP_CENSUS.md` с построчной таблицей:

| цикл / точка планирования | где в коде | что обрабатывает | остаётся или уходит | причина |

Требования к переписи:

1. **Поимённо и исчерпывающе.** Начать с `apps/integrator/src/infra/runtime/worker/main.ts` (там несколько
   `while (true)`), но не ограничиваться им: искать индексом — `node /home/dev/brain/tools/code-search.mjs
   "<запрос>" --repo bcb`, — и по смыслу тоже. Пустой результат доказывается перечислением попыток, а не одной
   командой.
2. **Точки планирования — тоже.** План говорит именно «циклов И ТОЧЕК ПЛАНИРОВАНИЯ больше, чем три».
3. **Для каждого — вердикт с причиной.** «Остаётся, потому что …» или «уходит вместе с …». Целевое состояние по
   плану — один цикл: очередь исходящей доставки с попытками, отступами и «мёртвой полкой». Очередь остаётся в
   интеграторе (повторы и лимиты у каждого канала свои — в этом его польза). Отдельного модуля-планировщика не
   заводить.
4. **Что уже сделано, учесть:** решения жизненного цикла записи уехали в вебапп (D14 закрыт), план напоминаний
   шлют все пути (D13a добор), возврат зависших и уборка очереди работают (D10b), постановка принимает
   абсолютное время (D10c).

## Приёмка

- таблица покрывает ВСЕ найденные циклы и точки планирования, у каждого назван вердикт и причина;
- перечислены попытки поиска (чем искал), чтобы «больше не нашлось» было доказано, а не заявлено;
- отдельной секцией: что мешает свести к одному прямо сейчас, по пунктам, со ссылками на блокирующие пункты плана;
- код не изменён — показать чистое дерево, кроме нового документа;
- галочки плана не ставить, push и merge не делать.
