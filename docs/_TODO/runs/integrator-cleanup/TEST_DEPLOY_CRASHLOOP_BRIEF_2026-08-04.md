# Деплой на TEST: снять суточный crash-loop воркера доставки

Rules: `AGENTS.md` — Маршрут, CORE rules, §6, §9, §24. Language: internal work is English.

⚠️ Один ход, следующего не будет; всё на переднем плане; коммит до конца хода.

Authority: живой инцидент; правка приземлена в `feat/doctor-ui-rebuild` (`f82503dc3`). `ORCH_OPS`.

Источник оракула — замер прогона D30, дословно: воркер доставки TEST «в crash-loop непрерывно с 2026-08-03
05:41 (после `1f9b2f22f`), 8080+ повторов в логе Postgres на момент замера».

## Работа

1. **Выкатить `feat/doctor-ui-rebuild` на TEST** штатным путём (`deploy/host/deploy-test.sh`), в foreground,
   лог назвать в отчёте.
2. **Доказать, что crash-loop прекратился:** воркер доставки поднят и держит циклы; в логе Postgres новых
   повторов `cannot execute SELECT FOR UPDATE in a read-only transaction` после деплоя нет. Покажи это
   выводом, а не выводом деплой-скрипта.
3. **Проверить открытый вопрос предыдущего хода:** на `app.read_integrator_platform_integration_availability()`
   у роли `app_operational_delivery_worker` может не быть гранта в каноническом `c4-operational-runtime.sql` —
   на DEV это чинилось оверлеем, на TEST оверлеев нет. Если воркер упрётся в это — почини каноническим путём,
   это тот же инцидент, а не новый скоуп.
4. **Проверить вход через VK** — ключи владелец уже завёл, провайдер включён: кнопка должна появиться на
   экране входа. Дальше клика по кнопке идти не нужно (это шаг владельца), но факт появления и корректный
   `start`-редирект покажи.
5. **Гейты закрытия** должны быть зелёными. Если упадёт счётчик-константа — это книжная поправка, чини; если
   упадёт что-то содержательное — НЕ подгоняй константу под сломанное состояние, а опиши.

## Границы

- **PROD (`135.106.162.170`) не трогать** ни на одном шаге.
- Полный дамп прода не делать: это code-only выкатка.

## Готово значит

- Деплой завершён, все юниты TEST активны и здоровы.
- Crash-loop прекращён — показано логом, а не предположением.
- Состояние VK-кнопки на экране входа названо.
- Один коммит, если что-то правилось; если правок нет — так и скажи.
