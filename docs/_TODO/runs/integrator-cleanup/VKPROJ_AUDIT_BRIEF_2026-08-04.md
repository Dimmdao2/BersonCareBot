# Аудит: починка застрявшей проекции `oauth_vk_enabled` (`74efcf336`)

Rules: `AGENTS.md` — Маршрут, CORE rules, §1, §6, §9, §24. Language: internal work is English.

Authority: гейт перед сведением ветки `wt/vk-projection`. `ORCH_OPS`.

**Тест или взгляд:** ТЕСТ. Проверяется состояние живой схемы TEST и поведение входа через VK — это прогон, а не
чтение: ровно потому баг и прожил незамеченным, что в файлах миграции всё выглядело правильно.

Источник оракула — отчёт исполнителя, дословно: «на TEST тело `public.sync_registered_app_runtime_setting()`
было побайтово равно версии из миграции 0193 — правки функции из 0210 и 0352 никогда не попали в живую схему».

## Что проверяешь, скептически

1. **Правда ли это причина.** Исполнитель говорит, что мигратор решает по водяному знаку `created_at`, хэш
   после записи в леджер не сверяется, поэтому `0352` не переприменится никогда. Проверь механику сам.
2. **Исполнитель ПРАВИЛ ЖИВОЙ TEST** — временно выдал `BYPASSRLS`+`app_owner` роли `bersoncarebot_test` и, по
   его словам, отозвал. **Проверь, что отозвано на самом деле**: текущие атрибуты роли и членства. Оставленная
   лишняя привилегия на TEST — FAIL сама по себе.
3. **VK работает:** проекция `true`, `/api/auth/oauth/providers` отдаёт `vk:true`, `start` даёт редирект на
   `id.vk.com`, а не `501`. Проверь своими запросами.
4. **Не сломаны ли остальные провайдеры.** Пересчёт был «всех четырёх разом» — убедись, что Яндекс/Google/Apple
   не изменили состояние вопреки настройкам владельца (Google выключен тумблером сознательно).
5. **Переживёт ли следующий деплой.** Главный вопрос: не затрёт ли выкатка починку снова — этот же класс уже
   ловили с владельцами SECURITY DEFINER функций (`eaafe46d9`). Прогони цепочку деплойных SQL и скажи, кто
   после миграций трогает эту функцию.
6. **Секреты** VK нигде не попали в файлы, тесты, лог коммита.

## Ответ

Вердикт `PASS` или `FAIL` первой строкой, затем по пунктам — команда и что увидел. Ничего не чини.
