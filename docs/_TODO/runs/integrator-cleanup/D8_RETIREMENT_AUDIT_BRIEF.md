# Независимый аудит снятия мёртвой проекции рассылок (D8) — run: auditor-d8-retirement

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункт **D8**.
**Очередь аудита:** `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` — три коммита ниже стоят «ОЖИДАЕТ АУДИТА».

Ты — независимый аудитор. Работу делал другой исполнитель, его отчёт тебе не показывают намеренно.
Твоя задача — не подтвердить, а **опровергнуть**. Отчёт исполнителя и зелёные тесты сами по себе
доказательством не являются (этот репозиторий уже ловил «6/6 зелёных» поверх бага, ломавшего каждый ответ).

## Что проверять — три коммита

- `3e0645306` — снятие мёртвого домена проекции рассылок: 47 файлов, −1809 строк, включая удаление
  `pgSubscriptionMailingProjection.ts` и вырезание кусков `modules/integrator/events.ts`;
- `1552b4d06` — удаление устаревшего смоук-теста проекции (−12 строк);
- `fa7481533` — усиление сторожа снятия (`check-d8-mailing-retirement.mjs`, +206/−68) и правка
  `deploy/DATA_MIGRATION_CHECKLIST.md`.

## Вопросы, на которые нужен ответ доказательством, а не рассуждением

1. **Точно ли удалённое было мёртвым?** Для каждого удалённого экспорта и таблицы — доказать отсутствие живых
   потребителей. Искать через индекс: `node /home/dev/brain/tools/code-search.mjs "<имя>" --repo bcb`, а не
   слепым grep, и не только по точному имени — по смыслу тоже. Пустой результат доказывается перечислением
   попыток, а не одной командой.
2. **Не удалено ли лишнее?** Особая цель — `pgPatientHomeLegacyContent.ts` (−74) и
   `patientHomeLegacyContentPort.ts` (−18): это ПАЦИЕНТСКАЯ поверхность. Если оттуда ушло что-то, что пациент
   реально видел, это регресс, а не уборка. Проверить по коду потребителей, а не по названию файла.
3. **Сторож `check-d8-mailing-retirement.mjs` — он вообще ловит?** Проверить внесением поломки: вернуть кусок
   снятого кода и убедиться, что сторож краснеет. Сторож, который зелёный всегда, — хуже отсутствия сторожа.
   Приложить дословный вывод до и после.
4. **`notificationsTopics.ts` (−4) и `repository.ts` (−4)** — не выпала ли из тем уведомлений живая тема.
5. **Правки доков** (`DB_STRUCTURE.md`, `DATA_MIGRATION_CHECKLIST.md`) — соответствуют ли они фактическому
   состоянию базы, или описывают желаемое.

## Форма ответа

Построчная матрица: пункт → что проверил (команда/файл) → вывод (PASS / FAIL / UNPROVEN) → доказательство.
`UNPROVEN` — честный и ожидаемый исход там, где доказать не удалось; не подменяй его словом PASS.
Отдельной строкой — итоговый вердикт и список того, что НЕ проверял.

Ничего не чинить и не менять: ты read-only. Дерево клона обязано остаться чистым.
