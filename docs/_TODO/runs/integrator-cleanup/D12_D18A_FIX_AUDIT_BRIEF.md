# Аудит корректирующего прохода D12+D18a (run: audit-d12-d18a-fix)

Read-only. Ничего не править. Проверяется коммит `8d7208518` в клоне `bcb-wt-tariff`.

**План (authority):** `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, пункты D12 и D18a.
Требования прохода: `docs/_TODO/runs/integrator-cleanup/D12_D18A_FIX_BRIEF.md`.
Предыдущий вердикт: `audit-d12-d18a-2` (FAIL, 3 must-fix) в `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`.

**Правила репозитория, обязательные к сверке:** `.cursor/rules/tests-check-behaviour-not-circumstances.mdc`
(проверка обязана падать, если снять то, что она стережёт), `.cursor/rules/clean-architecture-module-isolation.mdc`.

## Что проверить

1. **Сторож сырого SQL действительно глухой.** Не верить самопроверке автора — придумать СВОИ обходы и предъявить
   их дословный вывод: комментарий перед запросом (`--` и `/* */`), перенос строки, шаблонная строка с
   интерполяцией, `.query(` через переменную/алиас, вызов не на `pool`, а на другом объекте, запрос, собранный
   конкатенацией. Для каждого — прошёл или упал. Отдельно: честный ``db.execute(sql`…`)`` обязан проходить,
   удаление живой строки манифеста — падать.
2. **Самопроверка не фиктивная:** сломай саму проверку (например, верни ей старое поведение) — падает ли self-test.
3. **Девять шаблонов.** Подтвердить, что удалённые ключи действительно никем не читаются, включая resolver и
   runtime-map. Отдельно разобрать асимметрию, которую заметил лид: `describeQuestion` удалён из Telegram, но
   ОСТАЛСЯ в MAX и читается из `handleMessage.ts:89` через контракт `webhookContent.ts:18`. Вопрос к проверке:
   достижим ли этот путь в проде хоть для одного канала. Если достижим — это регресс, называй файл и маршрут.
4. **Числа.** Отчёт D12 и перепись D12b говорят 26 и возвращают `telegram.cabinet.open.callback` в живые. Сверить
   с фактическим диффом `bf45f5853`.
5. **Лишнего не привнесено:** коммит трогает только сторож, шаблоны и документы; галочки плана не двигались.

## Формат ответа

`VERDICT: PASS | FAIL` + по каждому пункту факт и доказательство (файл:строка либо дословный вывод команды).
Прогоны: lint, typecheck, tests интегратора и сам сторож — с фактическим результатом.
