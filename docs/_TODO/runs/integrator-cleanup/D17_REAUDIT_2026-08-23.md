# FAIL — D17 круг 3 (`c787f1c49`): стена credential-корня восстановлена, стены calendar-корня нет

**Вердикт: FAIL.** Причина: из четырёх тел организация берётся из **аргумента** в одном —
`app.read_integrator_google_calendar_setting(text,uuid)`. Живьём под принятым контекстом клиники A
узкая роль прочитала `google_refresh_token` клиники B. Критерий брифа («если хоть в одном она из
аргумента — блокер») сработал.

**Важная оговорка о природе блокера.** Это **не регрессия D17 и не поломка круга 3** — тело
calendar-корня выбирает строку по аргументу с самого своего появления (доказано ниже по двум
независимым снимкам до-D17-состояния). Круг 3 закрыл ровно то, за что круг 2 дал FAIL, и закрыл
полностью. Блокер здесь — по критерию этого брифа, а не по невыполненной работе круга 3.

Аудитор: независимый (Opus, оркестратор), клон `bcb-wt-d17-integrator-20260823`, ветка
`wt/d17-integrator-20260823`, HEAD `52f3299eb`, проверяемый коммит `c787f1c49`.
Оракул: `docs/_TODO/UI_FINISH_AND_REAUDIT_2026-07-22/WORK_ORDER.md`, D17 — «узкая роль интегратора не
мешает доставке». Круг 2: `D17_INTEGRATOR_LOST_EXECUTE_AUDIT_2026-08-23.md`.
`--execute`, TEST, PROD, push не выполнялись. Все прогоны — `BEGIN … ROLLBACK` на именованной DEV
`bcb_webapp_dev`. Чужих файлов не трогал; ничего не чинил.

---

## Пункт 2 брифа — свой построчный diff каждого из четырёх тел

Ниже не пересказ отчёта автора, а мой собственный разбор: что в каждом теле делает организационную
проверку и **откуда берётся организация**.

### 1. `app.read_integrator_clinic_delivery_credential(text,uuid)` — организация ИЗ КОНТЕКСТА ✅

Источник: `apps/webapp/db/drizzle-migrations/20260823T030000_integrator_tenant_role_reaches_delivery_roots.sql:12-49`.
Мой diff `8e49a07f6 → c787f1c49` по этому телу, построчно:

| Было (круг 2, FAIL) | Стало (круг 3) | Что это делает |
| --- | --- | --- |
| `LANGUAGE sql` | `LANGUAGE plpgsql` (:16) | появилась возможность `RAISE`; тело стало fail-closed |
| — | `DECLARE v_organization_id uuid; v_value jsonb;` (:21-23) | **инициализаторов в `DECLARE` нет** — см. пункт 4 |
| `SELECT app.require_attested_context_for_roles(…app_integrator_tenant_service…)` | `PERFORM app.require_attested_context_for_roles(…app_integrator_tenant_service…)` (:25-28) | узкая роль D17 сохранена; гейт — первый оператор после `BEGIN` |
| `current_org_id()` в теле отсутствовал (`grep -c` → 0) | `v_organization_id := app.current_org_id();` (:30) | **организация возвращена из принятого контекста** |
| сверки аргумента с контекстом не было | `IF p_organization_id IS NULL OR p_organization_id <> v_organization_id THEN RAISE … 42501` (:31-33) | аргумент только **сверяется**, доступом не управляет |
| allowlist ключей: 5 + `clinic_transactional_mail_template`, работал как фильтр `WHERE` | `IF p_key NOT IN (5 ключей) THEN RAISE … 42501` (:34-39) | лишний ключ убран; запрещённый ключ снова даёт 42501, а не тихий `NULL` |
| `AND setting.organization_id = p_organization_id` | `AND setting.organization_id = v_organization_id` (:45) | **выборка по контексту, не по аргументу** |
| `SELECT … LIMIT 1` как результат sql-функции | `SELECT … INTO v_value … ; RETURN v_value;` (:40-47) | семантика чтения не изменилась |

Ничего не потеряно: все семь смысловых элементов тела от 21.08 (гейт, чтение контекста, сверка
организации, allowlist ключей, три предиката выборки, `LIMIT 1`) присутствуют. Удалён ровно один
элемент — незаявленный ключ `clinic_transactional_mail_template`; **вызывающих у него нет во всём
репозитории** (мой `grep -rn` по `*.ts,*.tsx,*.mjs,*.sql` без `node_modules` → ни одной строки),
и в TS-типе `IntegratorClinicDeliveryCredentialKey`
(`apps/integrator/src/infra/db/publicSystemSettings.ts:120-125`) его тоже нет. Удаление безопасно.

### 2. `app.read_integrator_google_calendar_setting(text,uuid)` — организация ИЗ АРГУМЕНТА ❌ **БЛОКЕР**

Источник: та же миграция, `:55-83`. Тело:

```sql
      (p_organization_id IS NOT NULL
        AND p_key IN ('google_calendar_enabled', 'google_calendar_id', 'google_refresh_token')
        AND setting.organization_id = p_organization_id)   -- :78  ← ОРГАНИЗАЦИЯ ИЗ АРГУМЕНТА
```

**Организационной проверки в теле нет вообще.** `app.current_org_id()` в теле не вызывается;
`require_attested_context_for_roles` организацию не сверяет по построению (у неё нет такого
аргумента). Единственное, что стоит между вызывающим и строкой, — совпадение `organization_id` со
**значением, которое передал сам вызывающий**.

**Это предсуществующее состояние, а не работа круга 3.** Два независимых снимка до-D17-тела:

- `deploy/postgres/generated/prod-to-target/schema-pre.sql` (снимок прод-схемы): то же
  `AND setting.organization_id = p_organization_id`, гейт был на `app_tenant_service`;
- `deploy/postgres/integrator-server-runtime-config.sql:235-259` (легаси-оверлей): то же тело
  вообще без гейта.

D17 поменял в этом теле **только роль в гейте** (`app_tenant_service` → `app_integrator_tenant_service`),
стену не снимал — её там не было. До D17 логин интегратора нёс `app_tenant_service`, то есть доставал
этот корень и раньше. Регрессии нет; дыра — старая.

Отдельно: комментарий в вызывающем коде утверждает обратное —
`apps/integrator/src/integrations/google-calendar/runtimeConfig.ts:36-38`: «`app.read_integrator_google_calendar_setting`
is a tenant capability — … the root matches `organization_id` exactly». Корень сверяет аргумент сам с
собой; заявленной в комментарии стены в нём нет.

### 3. `app.resolve_organization_mechanic_access(uuid,text)` — организация ИЗ КОНТЕКСТА ✅

Миграция переписывает это тело не текстом, а хирургией по якорям над `pg_get_functiondef` (:92-137).
Чтобы проверить, что при этом ничего не потеряно, я снял установленное тело **до** и **после**
применения миграции в одной транзакции и сравнил `diff -u`. Весь diff, целиком:

```diff
 DECLARE
-  v_current_organization_id uuid := app.current_org_id();
-  v_now timestamptz := statement_timestamp();
+  v_current_organization_id uuid;
+  v_now timestamptz;
 BEGIN
-  PERFORM app.require_attested_context_for_roles('app_seam_org_commerce_owner'::name, ARRAY['app_patient'::name, 'app_staff'::name, 'app_tenant_service'::name]::name[]);
+  PERFORM app.require_attested_context_for_roles('app_seam_org_commerce_owner'::name, ARRAY['app_patient'::name, 'app_staff'::name, 'app_tenant_service'::name, 'app_integrator_tenant_service'::name]::name[]);
+
+  v_current_organization_id := app.current_org_id();
+  v_now := statement_timestamp();
```

Больше не изменено **ничего**: сверка `v_current_organization_id <> p_organization_id` → 42501, все
CTE (`active_trial`, `paid_period`, `global_paid_policy`, история политики, `effective`), `RETURN QUERY`
остались побайтно. Прежние три роли в гейте сохранены, добавлена одна. Организация — из
`app.current_org_id()`, аргумент только сверяется.

### 4. `app.saas_billing_effective_tariff(uuid,uuid)` — организации в теле нет (делегат) ✅

Тот же метод, весь diff установленного тела:

```diff
 DECLARE
-  v_now timestamptz := statement_timestamp();
+  v_now timestamptz;
   v_snapshot jsonb;
 BEGIN
-  PERFORM app.require_attested_context_for_roles('app_seam_org_commerce_owner'::name, ARRAY['app_clinic_billing'::name, 'app_patient'::name, 'app_platform_settings'::name, 'app_staff'::name]::name[]);
+  PERFORM app.require_attested_context_for_roles('app_seam_org_commerce_owner'::name, ARRAY['app_clinic_billing'::name, 'app_integrator_tenant_service'::name, 'app_patient'::name, 'app_platform_settings'::name, 'app_staff'::name, 'app_tenant_service'::name]::name[]);
+
+  v_now := statement_timestamp();
```

Проверка `p_tariff_id`, snapshot-запрос, fallback, возврат — не тронуты. Гейт расширен **двумя**
ролями (`app_integrator_tenant_service` и `app_tenant_service`) — это находка 3 круга 2, и круг 3
теперь называет обе. Прямого `EXECUTE` узкая роль на хелпер не получила (замер ниже: `false`) —
контекст доходит только по `delegatesTo` от mechanic-корня.

**Итог по пункту 2: 3 из 4 тел берут организацию из контекста, 1 (calendar) — из аргумента.**

---

## Пункт 1 брифа — тест: вызов из клиники A с аргументом клиники B

Свой пробник (не тест автора): в одной транзакции материализованы три ожидающие миграции
(`…broadcast_drafts…`, `…pre_session_default_auth_otp_channel`, D17) + `privileges.bcb_webapp_dev.sql`,
посажены настоящие секреты обеим организациям, принят порт-контекст узкой роли
(`app_integrator_tenant_service` / `tenant_service` / `relation`) для организации **A**, вызовы — под
`SET LOCAL ROLE app_integrator_tenant_service`, затем `ROLLBACK`.

Артефакт при этом отработал целиком: `BCB_RUNTIME_DEFINER_GATES_VERIFIED functions=386`,
`BCB_FUNCTION_BODY_SURFACES_VERIFIED functions=420 rows=1009`.

```text
1_credential_cross_org=DENIED:42501           <-- стена держит
2_credential_own_org=OWN_SMTP                 <-- своя организация читается
3_credential_null_arg=DENIED:42501            <-- NULL-аргумент отказан
4_credential_denied_key=DENIED:42501          <-- запрещённый ключ отказан, не тихий NULL
5_calendar_cross_org=FOREIGN_GOOGLE_REFRESH_TOKEN   <-- ЧУЖОЙ СЕКРЕТ ПРОЧИТАН
6_calendar_own_org={"value": ""}
7_mechanic_own_org=1
8_mechanic_cross_org=DENIED:42501             <-- стена держит
9_credential_no_context=42501
10_calendar_no_context=42501
11_mechanic_no_context=42501
```

Строка `5_` — это блокер: значение `FOREIGN_GOOGLE_REFRESH_TOKEN` посажено организации **B**, а
прочитано под принятым контекстом организации **A**.

### Fault injection (доказательство, что пробник живой)

Во временной копии тела credential-корня сверка ослаблена до проверки только `NULL`
(`IF p_organization_id IS NULL`) и выборка возвращена на `= p_organization_id`. Тот же пробник:

```text
1_credential_cross_org=FOREIGN_SMTP_SECRET    <-- было DENIED:42501, стало утечкой
2_credential_own_org=OWN_SMTP                 (не изменилось)
3_credential_null_arg=DENIED:42501            (не изменилось)
```

Красное поймано ровно на вырезанной строке стены и ни на чём другом. Значит зелёное в чистом
прогоне даёт именно восстановленная стена, а не удача фикстуры.

---

## Пункт 3 брифа — тест: узкая роль сохранила доступ, права не откачены

Замер в той же транзакции после кандидата:

```text
13_narrow_table_privs=integrator.user_reminder_occurrences:DELETE
14_narrow_execute_app=current_actor_user_id, current_integrator_user_id, current_org_id,
                      current_patient_user_id, is_staff,
                      read_integrator_clinic_delivery_credential,
                      read_integrator_google_calendar_setting,
                      require_accepted_context, resolve_organization_mechanic_access
15_narrow_execute_tariff_helper=false
```

- Табличных привилегий во **всём кластере** — ровно одна, та же, что была до D17. Ни одной
  медицинской таблицы. Расширения стены нет.
- `EXECUTE` в схеме `app` — 9 функций: 6 контекстных/идентификационных примитивов + **ровно три
  нужные двери**. Ни одной лишней.
- Хелпер `saas_billing_effective_tariff` узкой роли напрямую **не исполняем** — как и задумано.
- Все три двери проходят с контекстом (`2_`, `6_`, `7_`) и все три отказывают `42501` без контекста
  (`9_`, `10_`, `11_`).

**Права круга 1 не откачены; цель D17 — «узкая роль не мешает доставке» — выполняется.**

---

## Пункт 4 брифа — взгляд: гейт первым оператором, `DECLARE` после него

Замер установленных тел (`substring` от первого `BEGIN`):

```text
12_gate_first_read_integrator_clinic_delivery_credential = BEGIN\n  PERFORM app.require_attested_context_for_roles('app_…
12_gate_first_read_integrator_google_calendar_setting    = \n  SELECT app.require_attested_context_for_roles('app_seam_s…
12_gate_first_resolve_organization_mechanic_access       = BEGIN\n  PERFORM app.require_attested_context_for_roles('app_…
12_gate_first_saas_billing_effective_tariff              = BEGIN\n  PERFORM app.require_attested_context_for_roles('app_…
```

Все четыре: гейт — первый оператор. `DECLARE` у трёх plpgsql-тел **без инициализаторов** (см. diff'ы
выше — круг 3 вынес `app.current_org_id()` и `statement_timestamp()` из `DECLARE` в тело после гейта;
это и было наблюдение 6 круга 2, оно закрыто). **Пункт 4 — PASS.**

Это не только «на глаз»: сам артефакт прав механически это и требует — финальный верификатор
`privileges.bcb_webapp_dev.sql:2515-2525` падает, если у plpgsql-тела за `BEGIN` не идёт сразу
`PERFORM app.require_*`, а у sql-тела первым оператором не стоит `SELECT app.require_*`.

---

## Статические гейты (прогнал сам)

| Гейт | Итог |
| --- | --- |
| `node deploy/postgres/privileges/generate-cli.mjs --all --check` | **PASS**, 4×«совпадает побайтно», `exit=0` |
| `node --test migration-order.test.mjs definer-tenant-predicate.test.mjs migrate-local-parse.test.mjs` | **PASS 44/44** |
| Артефакт поверх кандидата на живой DEV | `BCB_RUNTIME_DEFINER_GATES_VERIFIED functions=386`, `BCB_FUNCTION_BODY_SURFACES_VERIFIED functions=420` |

---

## Наблюдения (не блокеры, следующему читателю нужны)

1. **Тест автора сейчас в этом дереве не запускается — по причине окружения, не по своей вине.**
   `RUN_D17_INTEGRATOR_ROOTS_DB=1 node --test …integrator-narrow-delivery-roots.devDbProof.test.mjs`
   падает на `runtime definer gate target missing: app.pre_session_get_default_auth_otp_channel(uuid)`.
   Причина: тест кладёт в транзакцию **одну** миграцию D17 плюс артефакт прав из ветки, а артефакт
   после merge `52f3299eb` ссылается на функцию из миграции `20260823T023138`, ещё не применённой на
   DEV. Мой пробник это обошёл, домешав две ожидающие миграции. Структурно тест будет ломаться при
   каждом таком расхождении ветки и DEV — стоит заложить в него применение всех ожидающих миграций.
   К корректности круга 3 отношения не имеет: все его утверждения я подтвердил своим прогоном.

2. **Круг 3 дописал свой отчёт внутрь файла отчёта аудитора круга 2**
   (`D17_INTEGRATOR_LOST_EXECUTE_AUDIT_2026-08-23.md`, раздел «Круг 3»). Отчёт аудитора — свидетельство
   на момент проверки; дописывать в него ответ исполнителя смешивает две роли. Содержательно текст
   раздела я сверил — он соответствует коду, кроме одного (см. следующий пункт).

3. **Неточность в отчёте круга 3.** Про calendar-корень сказано: «`LANGUAGE sql`, без `DECLARE` и
   **без контекстного гейта**». Гейт там был — на роль `app_tenant_service`
   (`prod-to-target/schema-pre.sql`). Это повторяет ошибку круга 2 («у этого корня до D17 гейта не
   было вовсе»). На вывод не влияет — стены по организации не было ни тогда, ни сейчас, — но факт
   стоит поправить, чтобы следующий не считал calendar-корень «негейтованным».

4. **Легаси-оверлей по-прежнему расходится с объявлением.**
   `deploy/postgres/integrator-server-runtime-config.sql:235` пересоздаёт calendar-корень без гейта,
   `:347` переназначает владельца на `app_owner`. Это чужой предсуществующий долг (наблюдение 7 круга
   2), в границы D17 не входит; повторяю, чтобы не потерялось.

---

## Что закрыть, чтобы вердикт стал PASS

Одна правка: дать `app.read_integrator_google_calendar_setting` ту же стену, что круг 3 вернул
credential-корню — организацию брать из `app.current_org_id()`, аргумент только сверять. С учётом
того, что у корня две ветки, форма примерно такая:

- `p_organization_id IS NULL` (глобальные OAuth-ключи `google_client_id/secret/redirect_uri`) —
  оставить как есть, организации там нет по смыслу;
- `p_organization_id IS NOT NULL` (ключи клиники) — `p_organization_id <> app.current_org_id()` → `42501`,
  а `WHERE setting.organization_id = app.current_org_id()`.

Плюс в тест — кросс-арендный пробник и для calendar-корня (сейчас он есть только для credential).

**Вопрос владельцу/оркестратору для триажа:** эта дыра предсуществующая и в план D17 не входит. Если
её решают закрыть отдельным пунктом, а не внутри D17, — круг 3 по фактическому объёму D17 чистый, и
вердикт по D17 как таковому был бы PASS. Решение о границе — не аудитора.

---

## Границы прогона

DEV не изменена: всё в `BEGIN … ROLLBACK`. `--execute` не запускался, TEST/PROD/push не касался.
Чужих файлов не правил, галочек в планах не менял, ничего не чинил. Единственный записанный файл —
этот отчёт.
