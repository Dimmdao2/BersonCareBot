# Приёмка исправлений аудита единого loader

- Исходный независимый аудит: `0d005f89d`, вердикт `FAIL`, 12 достижимых пропусков.
- Исправление: `df7d96004`.
- Приёмка выполнена лидом по `AGENTS.md` §24.5–§24.6: причина и места были полностью локализованы,
  исправление механическое и не создаёт новой поверхности, поэтому повторный blind-аудит не запускался.

## Результат: PASS

Исправлены все 12 пунктов `MUST FIX`: 10 состояний загрузки в кабинете администратора и 2 состояния в формах
врача теперь используют общий `DoctorPanelLoading`, являющийся alias единственного
`AppContentLoading`. Состояния загрузки файлов, медиабуферизация и подписи кнопок не менялись.

Проверки:

- `rg -n 'Загружаем|Загрузка настроек календаря|Загрузка календаря|Загрузка графика|Загрузка сообщений' apps/webapp/src --glob '*.tsx' --glob '*.ts'` — 0 вхождений;
- `pnpm --dir apps/webapp exec eslint <9 изменённых файлов>` — exit 0;
- `pnpm --dir apps/webapp exec tsc --noEmit --pretty false` — exit 0;
- `git diff --check` перед коммитом — exit 0.

NOTE исходного аудита про пульсирующее значение KPI не входил в `MUST FIX`: полный контентный loader внутри
числового значения KPI не добавлялся.
