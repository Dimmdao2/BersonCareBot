# Independent audit brief: единый контентный loader

Тест или взгляд: это разовая UI-консолидация и качество итогового diff, поэтому основной способ проверки — независимый взгляд на код, типы и итоговый список состояний. Не писать тесты на наличие компонента, конкретный текст, количество элементов, DOM-форму, импорты или строки исходника. Поведенческий тест допустим только для отдельно названного дорогого и молчаливого отказа по `AGENTS.md` §10a/§10b; иначе тест не создавать.

Прочитай `AGENTS.md` полностью по карте заголовков и обязательно §10a, §10b, §15, §16, §17, §21, §24. Authority — этот bounded audit brief и worker brief `docs/_TODO/runs/unified-loader-20260904/WORKER_BRIEF.md`.

## Источник оракула

В `docs/_TODO/runs/unified-loader-20260904/WORKER_BRIEF.md` владелец потребовал: «Во всех кабинетах — доктор, администратор и пациент — состояния загрузки должны использовать один визуальный элемент: серый текст `Загрузка…` нормального читаемого размера и рядом один вращающийся индикатор-«лучик».» Также явно запрещено превращать в этот loader подписи действий, прогресс загрузки файлов и медиабуферизацию.

## Candidate

- Ветка: `wt/unified-loader-20260904`.
- Коммит реализации: `2c6ce8336`.
- Сравнение: `feat/doctor-ui-rebuild...2c6ce8336`.
- Scope: только изменения этого сравнения; продуктовый fix аудитор не делает.

## Что проверить

1. Существует ровно один нейтральный implementation общего контентного loader; doctor/patient совместимые имена не дублируют реализацию.
2. Компонент действительно даёт серый читаемый текст `Загрузка…`, один вращающийся индикатор рядом и центрирование по обеим осям доступной контентной области. Route-level оболочка не ломает высоту страницы.
3. Doctor/admin/patient изоляция не нарушена; нет `any`, новых зависимостей и локальных копий того же визуального loader в изменённых достижимых content-loading состояниях.
4. В diff не были ошибочно заменены action labels, upload progress/status, pending-action feedback или media buffering overlays.
5. Каждый удалённый skeleton/placeholder не был несущей частью layout или доступности; после замены состояние остаётся достижимым, валидным и без runtime/import ошибок.
6. Особо проверить `ScheduleSetupTab`: загрузка настроек календаря должна идти через общий loader, а не старую строку «Загрузка настроек календаря…».
7. Сверить объяснённые воркером остаточные `Загрузка`/spinner-вхождения с реальным кодом; MUST FIX только для достижимого нарушения authority или repo-rule, не для вкуса.

## Проверки и результат

- Инспектируй полный diff и запускай только минимальные проверки, нужные для независимого вывода. Уже зелёные worker gates можно учитывать, но не подменяй ими взгляд на diff.
- Временные поломки production-кода откати до конца хода. Продуктовый код не исправляй.
- Запиши отчёт в `docs/_TODO/runs/unified-loader-20260904/INDEPENDENT_AUDIT.md` с бинарным `PASS` или `FAIL`, перечнем проверенных классов, точным числом непойманных/найденных нарушений и evidence.
- Добавь строку вердикта по `2c6ce8336` в `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` в существующем формате с путём к отчёту и числом непойманных/найденных нарушений.
- Закоммить только audit-artifact/queue (и только намеренные acceptance-тесты, если они действительно прошли §10a/§10b) явным списком. Не пушь, не деплой, не трогай DEV/DB. Не заканчивай ход с незакоммиченными audit-artifacts.
