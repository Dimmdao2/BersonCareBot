# Третий узкий аудит — закрытие F5 (2026-09-14)

## Предмет и метод

- Candidate: `9cb57c7d0fee1dc1fa641179e32d7f7834531937` на ветке `wt/kpi-filters`.
- Authority: `docs/_TODO/DOCTOR_KPI_FILTERS_2026-09-14.md`, пункт Э7.7.
- Проверен только F5 и возможная регрессия от его двухстрочной коррекции в
  `apps/webapp/src/app/app/doctor/patients/[userId]/tabs/PatientTabRecords.tsx`.
- Классификация: **ВЗГЛЯД**. Тесты не написаны, не изменены и не запускались; сервер `:5200` не трогался.

## Проверка F5

**F5 закрыт.** `cancelsHistory` объявлен в области видимости этого же компонента до call site
(`PatientTabRecords.tsx:463-465`). Карточка получает обработчик клика только при
`cancelsHistory.length > 0` и получает `opensDetails` ровно по тому же предикату (`:807-808`). Панель
рендерится по `cancelsPanelOpen && cancelsHistory.length > 0` (`:823`): предикат наличия содержимого
совпадает буквально, без дополнительного или пропущенного условия.

Отсюда обе стороны согласованы:

- при пустом `cancelsHistory` нет обработчика, шеврона и панели;
- при непустом `cancelsHistory` есть обработчик и шеврон, а клик переводит `cancelsPanelOpen` в состояние,
  при котором панель рендерится;
- панель не может рендериться при некликабельной карточке: её собственное условие уже доказывает
  `cancelsHistory.length > 0`, а это одновременно включает `onClick`.

Путь из второго аудита тоже закрыт: при `isStale || fetchError` `displayList` принудительно пуст
(`:346-357`), следовательно пусты `historyList` и `cancelsHistory`. `cancelsCount` при этом действительно
может остаться ненулевым из `header.cancellationsCount` (`:365-370`), но карточка показывает этот счётчик
без шеврона и без клика и панель не рендерит. Ложного обещания деталей больше нет.

## Проверка регрессии в файле

Переход `DoctorStatCard` из ветки `Button` в ветку `article` при пустой истории здесь безопасен:

- `value` с `cancelsCount` и маркером `!`, `hint` и `tone` собираются до выбора корневого элемента и входят
  в общий `inner`/`shellClass` (`DoctorStatCard.tsx:75-83,103-187`); ветка `article` меняет только корневой
  интерактивный элемент (`:233-260`), содержимое и warning-оформление не отбрасывает;
- на этом call site `hasNoShows` вычисляется из `historyList`, а `cancelsHistory` включает тот же статус
  `no_show` (`PatientTabRecords.tsx:462-465`). Поэтому при реальной неявке маркер `!`, hint `есть неявка`
  и warning tone сохраняются, и карточка одновременно остаётся кликабельной; при пустой истории их отсутствие
  соответствует данным, а счётчик и обычный hint продолжают отображаться в `article`;
- `cancelsPanelOpen` может сохранить `true`, если данные временно станут пустыми, но видимой застрявшей панели
  нет: при пустом списке её блокирует `:823`. Как только панель вообще может появиться, тот же предикат включает
  `onClick`, и следующий клик закрывает её. Состояния «панель видна, закрыть нечем» нет.

Коррекция F5 других веток и данных файла не меняет; достижимой регрессии в рамках Э7.7 не найдено.

## Гейты

- `pnpm -C apps/webapp typecheck` → **EXIT=0**.
- `pnpm -C apps/webapp lint` → **EXIT=0**.

Обе команды выполнены без пайпа.

## Вердикт

**PASS**
