# Ссылки блока #1081 — перевести с удалённых файлов правил на разделы `AGENTS.md`

(run: `worker-refs-repoint`, клон `bcb-wt-987-d8`, ветка `wt/testsuite-refs`)

**План (authority):** [`docs/_TODO/TEST_SUITE_AUDIT_2026-07-29.md`](../../TEST_SUITE_AUDIT_2026-07-29.md),
раздел «ПОРЯДОК РАБОТ v2» — это его обслуживание: план и брифы блока ссылаются на файлы, которых больше нет.

**Строка плана, дающая оракул:** блок М, преамбула — «правило без гейта — документация; 30.07 решение о
Stryker-судье было принято, записано и не исполнено, потому что у него не появилось строки „чем это
исполняется“». Здесь то же самое в мелком: правило, на которое ссылается битая ссылка, исполнитель не прочитает.

**Базовая линия — не перемерять:** свод правил приземлён соседом (`ea2cb8ac1` + схлопывание), в
`.cursor/rules/` осталось три файла: `000-start-here.mdc` (маршрутная таблица), `test-execution-policy.md` и
`tests-check-behaviour-not-circumstances.mdc` (на его путь завязан механический гейт запуска агентов).
Остальные удалены, текст правил живёт в `AGENTS.md`. Это установлено, проверять заново не надо.

## Что сделать

В файлах блока — `docs/_TODO/TEST_SUITE_AUDIT_2026-07-29.md` и `docs/_TODO/runs/testsuite-v2/*.md` — сегодня
**11 ссылок на удалённые файлы**:

```
6 × .cursor/rules/host-psql-database-url.mdc
3 × .cursor/rules/dev-prod-isolation-no-real-creds.mdc
1 × .cursor/rules/webapp-tests-lean-no-bloat.mdc
1 × .cursor/rules/pre-push-ci.mdc
```

Каждую перевести на **точный раздел `AGENTS.md`** в том же виде, в каком это делает маршрутная таблица
`000-start-here.mdc`: `[`AGENTS.md` §N](путь#якорь)`. Раздел искать по содержанию правила, а не по похожему
названию: правило про `psql` и `DATABASE_URL` — это не то же, что правило про изоляцию dev/prod.

Ссылки на два **оставшихся** файла (`tests-check-behaviour-not-circumstances.mdc`,
`test-execution-policy.md`) **не трогать** — они живые, и на первый завязан гейт запуска.

## Проверка — обязательная, механическая

Написать одноразовую проверку и показать её вывод: каждая ссылка вида `AGENTS.md#якорь` из изменённых файлов
**резолвится** — то есть в `AGENTS.md` действительно есть заголовок, дающий этот якорь. Битый якорь хуже
битого файла: он выглядит рабочим. Прогнать до и после правки, показать оба вывода.

Отдельно перечислить в отчёте ссылки, для которых **не нашлось** подходящего раздела, — это находка (правило
могло не переехать), а не повод поставить ближайший похожий.

## Границы

- Только ссылки. Текст плана и брифов не переписывать, формулировки не «улучшать».
- Продуктовый код и `AGENTS.md` не трогать.
- Ветка `wt/testsuite-refs`, коммиты только в неё. **Push и merge не делать, галочки плана не ставить.**
- Отчёт: `docs/_TODO/runs/testsuite-v2/REFS_REPOINT_REPORT.md`, раздел «НЕ СДЕЛАНО» обязателен.

**Правила:** `.cursor/rules/tests-check-behaviour-not-circumstances.mdc` и `.cursor/rules/000-start-here.mdc`
(маршрутная таблица — заодно образец формы ссылки).
