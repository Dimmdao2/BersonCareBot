# Б2 — аудит закрытия чек-листа (взгляд, не поведенческий тест)

**Тест или взгляд:** взгляд. Это разовое действие — проверка, что новая строка чек-листа честно цитирует
уже свершившиеся факты (коммиты, живые проверки), а не новое поведение модуля. Поведенческих тестов не
пишем: сами гейты (`scripts/a0-greenfield-baseline.test.mjs`) уже покрыты своими самотестами в прежних
раундах (`3658752a1`/`6e853ea20`/`1480bb9d4`), это здесь не переаудируется.

**Правила:** прочитать маршрут `AGENTS.md`, CORE-правила, §24 «Оркестрация субагентов» (особенно §24.4
«тест или взгляд», §24.6 «Findings» — находка вне owner scope — вопрос, не работа).

**Authority (plan-файл владельца):** `docs/_TODO/TEST_SUITE_AUDIT_2026-07-29.md`, блок Б, пункт **Б2**
(строка со свежей правкой — `[x]` вместо `[ ]`, с блоком доказательств под ней).

**Ветка на проверку:** `wt/b2-accept`, клон `bcb-wt-b2-accept`, единственный коммит поверх текущей головы
`feat/doctor-ui-rebuild` — `git -C /home/dev/dev-projects/bcb-wt-b2-accept log -1 -p`.

## Что произошло (контекст для аудитора)

Пункт Б2 плана владельца оставался `[ ]`, хотя фактическая работа генератора эталона `a0-greenfield` уже
была сделана, слепо проаудирована и приземлена раньше, тем же диалогом лида, отдельной цепочкой коммитов на
`feat/doctor-ui-rebuild`:

- `c402f29a8` — генератор научен четырём deploy-script ролям + сужен `phone_literal_forbidden`.
- `46a88ff51` — привилегированный `refresh-a0-greenfield-baseline.mjs` выполнен лидом, `drizzle_historical_hash_drift:0175` снят.
- `3658752a1` / `6e853ea20` — слепой аудит: **FAIL**, не убито 6 из 28 (гейт не ловил дрейф в рабочем дереве,
  `validatePackage()` читал `git show HEAD:<файл>` вместо диска).
- `1480bb9d4` — фикс: `assertCleanRefreshSource()` подключена в путь проверки.
- `29f31f262` — блокер снят, приземлено. Строка вердикта в очереди:
  `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md:753`.

Моя правка (эта сессия, коммит на `wt/b2-accept`) — **только чек-лист**: `[ ]` → `[x]` на строке Б2 плюс
абзац с доказательствами, включая три вещи, которые я перепроверил САМ, не поверив прежнему отчёту:

1. `pnpm run check:saas-a0-greenfield-baseline` на чистом дереве → 8 pass / 0 fail.
2. Арбитражная поломка: дозаписана строка в конец `apps/webapp/db/drizzle-migrations/0322_*.sql`
   (последний файл каталога на момент проверки — если каталог с тех пор пополнился, это не меняет сути
   пробы, важен сам факт правки любого трекнутого файла в директории миграций), прогнан
   `node scripts/check-a0-greenfield-baseline.mjs` → `refresh_source_worktree_dirty`, затем
   `git checkout -- <файл>` вернул дерево в чистое состояние.
3. Прямой `SELECT count(*) FROM pg_proc WHERE proname IN
   ('set_saas_isolation_test_scenario','read_saas_isolation_test_scenario_fixture_counts')` на
   `bcb_webapp_dev` через `apps/webapp/.env.dev` (`DATABASE_URL`) → `0`.
4. `grep -c` по `deploy/host/deploy-prod.sh` на упоминания `test-saas-isolation-telemetry-fixtures` /
   `set_saas_isolation_test_scenario` → `0`.

## Задача аудитора

Не переаудировать сам генератор (это уже сделано раундами `3658752a1`→`1480bb9d4`→`29f31f262`, история в
`docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`), а проверить **точность моей новой строки чек-листа**:

1. Каждый упомянутый SHA (`c402f29a8`, `46a88ff51`, `3658752a1`, `6e853ea20`, `1480bb9d4`, `29f31f262`)
   существует в истории `feat/doctor-ui-rebuild` и содержание коммита соответствует тому, что о нём написано
   в новой строке чек-листа — не выдумано, не перепутано.
2. `29f31f262` действительно есть в очереди `NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` со строкой вердикта
   (не только в git log) — цитата в чек-листе не должна опираться на факт, которого нет в очереди.
3. Повторить сам мои четыре пункта независимо (те же команды из раздела выше, на своём собственном прогоне,
   не копируя мой вывод) и подтвердить дословно совпадающий результат (8/0, `refresh_source_worktree_dirty`
   при поломке и чистый откат, `0` функций, `0` упоминаний в деплое).
4. Проверить, что абзац «Не входит в это закрытие» честно называет второй, оставшийся открытым finding
   слепого аудита (`3658752a1`: механической проверки самого разреза «боевой оверлей / TEST-фикстуры» нет) —
   и что чек-лист НЕ пытается тихо закрыть или скрыть эту находку, а прямо оставляет её как открытый вопрос
   владельцу, не заводя из неё новую работу.
5. Убедиться, что мой коммит трогает **только** `docs/_TODO/TEST_SUITE_AUDIT_2026-07-29.md` — никакого кода,
   схемы, миграций (`git -C /home/dev/dev-projects/bcb-wt-b2-accept diff --name-only feat/doctor-ui-rebuild`).

## Границы

- Ничего не чинить продуктового — это чек-лист, не код. Если найдёшь неточность в тексте — исправь текст
  прямо в этой ветке (`wt/b2-accept`) и это и есть твой acceptance-artifact; не заводи отдельный fix-цикл.
- Прод не трогать. Схему DEV не менять (только `SELECT`).
- Отчёт — короткая строка вердикта для очереди `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`:
  PASS/FAIL + что именно перепроверено + числа.
