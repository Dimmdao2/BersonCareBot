Прочитал план, три исследования, синтез, канон по базе, оба правила и проверил ключевые утверждения по коду (два независимых прогона поиска). Ниже аудит.

---

# VERDICT: PASS WITH FIXES

Направление и жёсткий порядок («продуктовое → транспорт → права») верны, все девять формулировок владельца физически лежат в плане его словами. Но **D13 в текущем виде гасит напоминания о приёме на проде**, гейт D17 неполон, а D18 недооценивает собственный объём — и из-за этого D17 нельзя закрыть никогда. Ни один уже закрытый пункт не искажён.

---

## Матрица по девяти словам владельца

| # | Слова владельца | Где в плане | Вердикт |
|---|---|---|---|
| 1 | «интегратор — блок модулей по доставке сообщений и приёму вебхуков» | `WORK_ORDER.md:240-241` | **present** — цель сформулирована одной строкой, ровно как сказано |
| 2 | «вырезай весь блок lfk-diary в интеграторе» | D11, `:246-248` + D2-retirement `:154-164` | **present**, закрыт, evidence приведён |
| 3 | десять недостижимых веток: «просто вырезать; если что-то упадёт — взять из старого и перенести в вебапп» | D12, `:249-251` | **present дословно**, но без перечня десяти — см. MUST FIX 6 |
| 4 | константы записи: в вебаппе УЖЕ есть настройки и шаблоны, «вырезать нещадно» | D13, `:252-256` | **distorted** — посылка не подтверждается кодом. См. MUST FIX 1 |
| 5 | цепочка: событие → итоговые настройки из базы → планировщик → воркер → интегратор как отправитель | D14, `:258-259` | **present дословно**, включая «переносить вместе с порядком уведомлений» |
| 6 | «надо запустить сначала сильное исследование командой» | D15, `:261-265` | **present** — исследование ПЕРЕД работой зафиксировано как условие |
| 7 | очередь доставки с попытками/отступами/«мёртвой полкой» остаётся у отправителя; отдельного «воркер-шедулера» не заводим | D16, `:266-269` | **present** по смыслу; арифметика «ровно один цикл» не сходится — MUST FIX 7 |
| 8 | «приведение к одному порту бд»; семь файлов `directPublic` владелец забрал себе | D18, `:270-276` | **present, но неполно** — сырой SQL живёт далеко за пределами `directPublic`. MUST FIX 3 |
| 9 | узкая роль последней, раньше выдать нельзя | D17, `:278-282` | **present**, но гейт перечислен неверно — MUST FIX 2 |

---

## MUST FIX

**1. D13 стоит на неверной посылке, и в написанном порядке молча отключает напоминания о приёме.**

Проверено по коду:
- Настройки существуют только как запись в реестре и экран: `apps/webapp/src/modules/system-settings/registry.ts:127-128`, запись `app/api/admin/settings/route.ts:179-180,586-607`, чтение — только для отрисовки `app/app/settings/page.tsx:137-147`. **Ни один планировщик, джоб или send-путь их не читает.**
- Модуль шаблонов `apps/webapp/src/modules/notif-templates/` покрывает ровно три события: `notifTemplatesService.ts:21-22` — `['created','cancelled','rescheduled']`. **События «напоминание» в нём нет.**
- Единственный тик напоминаний в вебаппе — `modules/reminders/webPushOnlyScheduler.ts:126` — ходит по `reminder_rules`/occurrences и только web-push; записи на приём он не видит вовсе.
- Единственный живой отправитель — интегратор: `bookingLifecycleRoute.ts:365-376` (offsets 24ч/2ч и тексты) → `enqueueMessageRetryJob` `:406-413` → `infra/runtime/worker/main.ts:64-120`.

Сценарий: D13 выполняется как написано → следующая `booking.created` не ставит ни одной задачи → пациент не получает ни напоминания за 24 часа, ни за 2. Ошибки в логах нет, деплой зелёный, обнаруживается по неявкам. Это не «конкурирующая реализация, перекрывающая настроенную врачом» — это **единственная работающая**, а «настройки врача» сегодня никуда не подключены.

Требуется: разбить D13 на (а) построить потребителя в вебаппе — чтение двух per-org настроек, добавление события `reminder` в `NOTIF_TEMPLATE_EVENTS`, постановку occurrences; (б) только после этого резать константы интегратора. И поставить D13 **после** D14, а не до.

**2. Гейт D17 неполон — узкая роль уронит поддержку.**

`:280` говорит «только после D11–D16». Но после D11–D16 интегратор продолжает писать продуктовый канон: `[redacted-token].ts:133,226` (`public.support_conversations`) и `writeSupportQuestionsDirect.ts:129,143,248` (`public.support_questions`) — это D3/D4, они слиты, и в Track D-полном **на поддержку нет ни одного пункта**, хотя все три исследователя назвали её протёкшим доменом. Сценарий: роль выдана после D16 → пациент пишет в поддержку из Telegram → INSERT в `public.support_conversations` отклонён → обращение уходит в durable-outbox с инцидентом оператору либо теряется. Гейт обязан читаться «после D11–D16, D18 и переноса поддержки/настроек уведомлений».

**3. D18 недооценивает собственный воркстрим, из-за чего его проверка непроходима.**

План говорит: два доступа — `directPublic` (семь файлов) и drizzle (восемь потребителей в `infra/db/repos/`). Цифра по drizzle **точна** (8 репозиториев). Остальное — нет:
- В `directPublic/` сейчас 7 файлов, но 6 рантайм и 1 тест; сырой SQL шлют пятеро — `resolveDirectPublicActor.ts` уже на drizzle (`:10,86-91`), `mergeCandidatesDirect.ts` делегирует в `@bersoncare/platform-merge` (`:76`).
- Сырой SQL **не заперт в `directPublic`**: `writePort.ts:200-201,396-398,597-598,1114-1115`; `publicRuntimeSettings.ts:19-21`; `publicRestrictedSettings.ts:10-11`; репозитории `messageLogs.ts:77`, `outgoingDeliveryScope.ts:50`, `operatorDeliveryAttempts.ts:25`, `schedulerReminderOrganizations.ts:6-7`, `projectionHealthCore.ts:34-56`. Всего 74 не-тестовых `.query` в 21 файле.
- Есть **третий** путь, не названный вовсе: `runIntegratorSql.ts:22-48` — берёт `db.integratorDrizzle` в обход `getIntegratorDrizzleSession` и падает в компилированный сырой текст `db.query(text, params)` (`:29,:47`). Плюс отдельные пулы `integratorPoolProvider.ts:86,160`, `integratorMigrationPoolProvider.ts:15`, `operationalPoolReadiness.ts:8-12`.

Проверка D18 сформулирована как «не должно остаться второго способа ходить в базу». После переписывания только семи файлов она провалится, а вместе с ней встанет D17. Требуется: перечислить остаточный набор поимённо отдельными чекбоксами и явно записать, что остаётся сырым **по замыслу** (operational/diagnostic-пробы, миграции) — иначе пункт нефальсифицируем.

**4. Параграф «Execution order» (`:286-289`) противоречит Track D-полному и остаётся активным.**

Он заканчивается «D10 is always last». D-полный (`:243-244`, D16 `:266`, D17 `:280`) ставит после D10 ещё D16, D18 и D17. Два несовместимых активных требования прямо запрещены `plan-authoring-execution-standard.mdc:29-30` и уроком владельца 2026-07-22 в `CLAUDE.md`. Исполнитель, читающий сверху вниз, увидит стоп-правило «D10 последний». Требуется: переписать параграф под D0–D18 либо пометить `SUPERSEDED — 2026-07-30, replaced by «Track D-полный»`.

**5. D17 не называет артефакт проверки, а проверка симметричная.**

«Деплой ассертит точный набор прав» (`:282`) — правда, но конкретно: `deploy/postgres/c4-operational-runtime.sql:1009-1066`, где `unexpected = actual EXCEPT expected` и `missing = expected EXCEPT actual` сводятся в `1 / (…)::int` — **лишний грант валит выкатку так же, как недостающий**. Шелл `deploy/host/assert-c4-operational-runtime-ready.sh` даёт только 14 поимённых negative-проб (`:126-155`) и не является точной сверкой. Критично: managed-роли там — восемь (`:947-951`), и текущей рантайм-роли интегратора среди них нет (она общая с вебаппом, `DATABASE_UNIFIED_POSTGRES.md:17`). Прецедент уже был: D1 (`:135-136`) — грант «VIOLATED deploy runtime-role assertions and took TEST down». Требуется: D17 обязан назвать файл, потребовать добавления новой роли и в список managed-ролей, и в `expected`-таблицу, одним коммитом с грантом.

**6. D12 не перечисляет десять веток, а соседние ветки того же `switch` — живые и НЕ эквивалентны хендлерам.**

Проверил: недостижимых ровно **10**, потери логики нет ни в одной — `booking.event.insert:1099`, `callback.answer:1287`, `message.deliver:1301`, `message.retry.enqueue:1351`, `intent.enqueueDelivery:1388`, `notifications.get:2220`, `notifications.toggle:2237`, `reminders.rules.get:2292`, `reminders.rule.toggle:2327`, `reminders.rule.cyclePreset:2396`. Перехват — четыре `Set` на `executeAction.ts:387-390`.

Но резать «по похожести» нельзя: `message.compose:1116`, `message.edit:1226`, `message.replyMarkup.edit:1264` выглядят так же, в `DELIVERY_TYPES` (`:99-105`) их **нет**, они живые — и живой `message.compose` не эквивалентен `handlers/delivery.ts:78-150`, который дополнительно вешает основную reply-клавиатуру (`delivery.ts:114-135`). Ошибка исполнителя = у бота пропадает клавиатура. Требуется процитировать десять меток дословно как атомарный набор (`plan-authoring-execution-standard.mdc:26-28`).

**7. Арифметика D16 не сходится.**

Сегодня вечных циклов **три** в воркере — `jobQueueLoop` (`worker/main.ts:64`), `projectionOutboxLoop` (`:121`), `outgoingDeliveryLoop` (`:133`) — плюс отдельный **процесс** планировщика со своим циклом и advisory-локом лидера (`scheduler/main.ts:83-104`). D10 убирает один; остаётся два, а не «ровно один». Чтобы получился один, `message_retry_jobs` должен быть свёрнут в `outgoing_delivery_queue` — а это ровно та очередь, которой сегодня едут напоминания о приёме, и смена retry-семантики даёт дубли (риск, который назвал Sol). Требуется: назвать, что происходит с `jobQueueLoop`, и отдельным пунктом — вывод из эксплуатации процесса/юнита планировщика; либо честно написать «две очереди».

**8. У D12–D18 нет ни linked detailed plan, ни критериев приёмки, ни строк доказательств.**

D0–D10 их имеют; из новых только D11 ссылается на прогон (`:248`). Это блокирует и воркера, и аудитора: по `CLAUDE.md` (урок 2026-07-22) и `plan-authoring-execution-standard.mdc:31-36` бриф обязан содержать exact checkbox set и матрицу `code → test → runtime evidence`. Сейчас процитировать нечего. Требуется: каждому пункту — либо файл детального плана, либо встроенный атомарный чек-лист с определением доказательства.

---

## Анализ порядка

**D13/D14 против D5–D7 — коллизии по данным нет, коллизия по смыслу есть.** Напоминания о приёме идут через `message_retry_jobs` (`bookingLifecycleRoute.ts:406`), а D5–D7 — про `integrator.user_reminder_rules` / `user_reminder_occurrences`. Таблицы не пересекаются, технически D13/D14 могут идти раньше. Но приземляться им некуда: в вебаппе нет отправителя записей на приём (MUST FIX 1). Порядок внутри пары **неверен**: D13 (снять константы) стоит перед D14 (перенести решения) — это ровно тот разрыв, где напоминания исчезают. Должно быть D14 → D13.

**D10 всё ещё корректно последним среди транспорта — но уже не последним в плане.** Outbox не мёртв: счастливый путь — синхронный подписанный POST, очередь включается только при падении прямой записи; `projectionOutboxLoop` безусловен (`worker/main.ts:121-132`), и готовность деплоя специально пробует запись в `integrator.projection_outbox` под ролью воркера (`assert-c4-operational-runtime-ready.sh:115`). Гейт «только после точной переписи нулевых продюсеров» — правильный. Ловушка, которой в плане нет: шесть `scripts/stage{4,6,7,9,11,13}-release-gate.mjs` шеллят в projection-health, и снос тулинга уронит CI-гейты несвязанных этапов.

**D17 в момент, куда его поставили, проверить нельзя.** Три причины: остаются support-писатели в `public` (MUST FIX 2); D18 не покрывает весь сырой SQL, то есть «кто и как ходит» не определено (MUST FIX 3); роли интегратора нет в managed-списке точной сверки ACL (MUST FIX 5). D18 сам это признаёт (`:276`), но D17 в своём гейте D18 **не упоминает** — и это внутреннее противоречие плана. Физический порядок в файле тоже сбит: D18 напечатан перед D17.

**FK-риск живой и правильно описан.** `user_reminder_occurrences.rule_id` → `integrator.user_reminder_rules` `ON DELETE CASCADE` подтверждён и в миграции (`migrations/core/20260311_0002_create_user_reminders.sql:21-23`), и в дампе (`DB_DUMPS/integrator_bcb_webapp_dev_schema.sql:1785-1789`); `getEnabledReminderRules` (`repos/reminders.ts:213-221`) до сих пор читает интеграторскую таблицу. D5 честно записал это как заблокированное до D6. Опасность в другом: D14 («планировщик уезжает в вебапп») пересекает эту же миграцию FK, а зависимости D14 → D6 нигде не записано. Кто придёт первым, тот и получит каскадное удаление occurrences.

---

## Выдуманного объёма не нашёл

Каждый пункт D11–D18 трассируется либо в прямую фразу владельца, либо в механическое следствие. Проблема плана обратная — **недостаточность**: при заявленной цели «ingress + доставка» вне D11–D18 остались сценарный движок (163 сценария, 7 466 строк executor — крупнейший протёкший домен по всем трём исследованиям), поддержка, настройки уведомлений и обогащение Google Calendar. D12 снимает с движка 10 мёртвых веток и не трогает 44 живых типа действий. Пока движок принимает продуктовые решения, узкая роль не соответствует коду, который она охраняет.

## Развилки владельца — одним листом, до старта

`plan-authoring-execution-standard.mdc` требует выносить развилки заранее. Одна не вынесена и она главная:

**Track D (D3–D8) ведёт интегратор к тому, чтобы писать канон в `public` напрямую. Track D-полный ведёт к тому, чтобы он канон не трогал вовсе.** Ваши слова «доделать прошлую работу но уже правильно» в плане записаны, но что значит «правильно» для D3, D4, D6, D7, D8 — не зафиксировано. Выполнив D6/D7 как написано, мы напишем код, который D17 затем запретит. Рекомендация (safe-default): D6 сделать в урезанном виде — только миграция FK, разблокирующая D14; D7 и D8 не начинать до результатов исследования D15. Решение за вами.

---

## Чего я не смог проверить

- **Ничего не запускал в рантайме.** Ни базы, ни тестов, ни деплой-скриптов — только чтение кода, миграций и дампов схемы. Все выводы про «работает/не работает» — статические.
- **Не проверял живой прод и TEST:** фактические `GRANT`, имена ролей, есть ли уже сейчас pending/dead строки в `projection_outbox` и `message_retry_jobs`. Дублирующая `public.projection_outbox` из находки Opus (`public_bcb_webapp_dev_schema.sql:2825`) на проде не проверена.
- **Не проверял, есть ли сейчас в очереди отложенные напоминания о приёме** — то есть не могу сказать, сколько именно уведомлений потеряется при D13, только что канал обнулится.
- **Не открывал `docs/_TODO/runs/integrator-diary-removal/`** и не перепроверял evidence D11 — он закрыт отдельным прогоном и в этот аудит не входил.
- **Не сверялся с taskdb `#987`** и с роадмапом: не знаю, не заперты ли D3–D8 чем-то, чего нет в этом файле.
- **Не читал `docs/ORCHESTRATION_BINDINGS.md` целиком** — только разделы, на которые ссылается `CLAUDE.md` и чек-лист.
- **Смежная находка без пункта в плане** (по чек-листу оркестратора #6 — вопрос вам, не задача): `message.delete` не имеет ни `case` в switch, ни записи в `DELIVERY_TYPES` — уходит в `default: ACTION_NOT_IMPLEMENTED` (`executeAction.ts:2529-2534`), при этом реализация `delivery.ts:298-325` существует и недостижима. Заводить ли отдельным пунктом?