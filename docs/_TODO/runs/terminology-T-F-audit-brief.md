# Аудит T-F · сообщения о записи говорят словом организации

## Тест или взгляд

**Взгляд, и отдельно — живая проба дверей.** Поведение блока: шаблон сообщения произносит слово
организации там, где путь доставки может его прочитать, и честно остаётся на «приёме» там, где не
может. Автоматические UI/DOM-тесты запрещены (`AGENTS.md` §10a); тесты текста здесь уже есть и их
оракул трогать нельзя. Поэтому проверка — чтение диффа, прогон билдеров и **самостоятельная живая
проба дверей под каждым принципалом**, а не пересказ таблицы исполнителя.

Перед работой прочитать `AGENTS.md` целиком — единственный нормативный текст репозитория.

## Что проверяешь

Коммиты `2c3ecaa24` (код, 19 файлов) и `d21e87374` (план) в ветке
`wt/terminology-appointment-word` (`/home/dev/dev-projects/bcb-wt-terminology-word`).

Бриф исполнителя: [`terminology-appointment-word-T-F-brief.md`](terminology-appointment-word-T-F-brief.md).
План владельца: `docs/_TODO/MEDICAL_WELLNESS_TERMINOLOGY_MODE_2026-09-02.md`. План — единственный
оракул; находка без соответствия в нём — вопрос ведущему, а не работа (§24.6).

## Сначала kill-set ВСЛЕПУЮ, потом дифф

Выпиши, чем блок может быть сломан МОЛЧА. Назови оба числа: поймано / прошло бы незамеченным.

## Куда смотреть в первую очередь

1. **Таблица дверей — ПЕРЕПРОВЕРЬ ЖИВЬЁМ.** Исполнитель заявляет: staff, пациентский и путь
   создания записи (включая публичную, обёрнутую `withPatientOrganizationPrincipal`) — дверь
   открыта; вебхук эквайринга, web-push интегратора и материализация напоминаний — закрыта, класс
   `tenant_service`, записи `relation` нет. Прогони сам под каждым принципалом и подтверди или
   опровергни поимённо. **Ошибка в любую сторону дорога:** «закрыто» там, где открыто, оставляет
   шаблон на «приёме» без причины; «открыто» там, где закрыто, даёт 42501 в живой доставке.
2. **Новую дверь никто не открыл.** Убедись, что коммит не добавил ни capability, ни грантов, ни
   нового корня — ни в декларации, ни в миграции.
3. **Дефолт побайтно прежний.** Прогони билдеры на четырёх словах и на ОТСУТСТВУЮЩЕЙ строке
   настройки; сверь каждый шаблон со старым текстом, включая пробелы и переносы.
4. **Оракул тестов не переписан.** Существующие тесты текста (`patientMessageText.test.ts`,
   `service.d14.test.ts`, `staffBookingProjection.unit.test.ts`) не должны быть подогнаны под новый
   текст: слово обязано приходить в тест тем же путём, что в продукт. Подгонка оракула — FAIL.
5. **Один читатель.** `organizationAppointmentTerms.ts` — единственное место, читающее ключ на этом
   пути. Второй читатель, второй резолвер или подстановка по месту — FAIL.
6. **Границы.** «Запись», «визит», «посещение», «занятие» не переименовываются; название услуги
   каталога — данные клиники; «Приём платежей» — другое значение. Кабинеты закрыты прошлыми блоками.
7. **Честность списка «НЕ СДЕЛАНО».** Пройди по путям сообщений сам и проверь, что на «приёме»
   остались ровно те, у кого дверь закрыта, а не те, до которых не дошли руки.

## Инъекции обязательны

Минимум три: «что сломано → что покраснело». Непокрасившая поломка — это ответ, так и записывай.

## Отчёт

Вердикт PASS/FAIL первой строкой. Секция «НЕ ПРОВЕРЕНО» поимённо обязательна. Отчёт закоммитить.
