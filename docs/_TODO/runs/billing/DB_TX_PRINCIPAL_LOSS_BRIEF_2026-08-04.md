# Транзакция мимо порта теряет принципала — RLS молча отдаёт ноль строк

⛔ **SUPERSEDED (вердикт фиксера, тот же день).** Дефект такого класса не подтверждён — независимо перепроверено
дважды (лидом по коду 04.08, `8cd4dcda9`, и фиксером живым прогоном против клона `bcb_webapp_dev` под реальным
`locked`-контекстом и реальным FORCE RLS). Настоящая причина сбоя 04.08 — баг `FOUND` в
`app.apply_paid_saas_billing_tariff`, исправлено веткой `wt/billing-capture-fix` (`0354`, `faa715252`), уже влито
в `wt/billing-live-vat`. Подробности — `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`, запись
«fixer verdict on `1f1c73e0e`». Тест `1f1c73e0e` сохранён как exoneration-доказательство, не как регрессия.

Rules: `AGENTS.md` — Маршрут, CORE rules, **§5** (к базе только через порт своего приложения), §6, §10/§10a/§10b,
§24. Language: internal work is English.

⚠️ **Один ход, следующего не будет** (§24.2): всё на переднем плане, коммит до конца хода, держаться ~25 минут.

Authority: `docs/_TODO/SAAS_FOUNDATION/SAAS_BILLING_PLAN.md` **B0.3**, задача `#1057`; живое доказательство —
`docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`, строка `c3d576c54`.

Источник оракула: `AGENTS.md` §5 — «К базе — только через порт своего приложения на drizzle (webapp и integrator
одинаково), сырой SQL для нового кода запрещён».

## Измеренный дефект

Живой платёж 04.08: провайдер подтвердил оплату, вебхук дошёл дважды, капчер упал с
`saas_billing_tariff_apply_failed`.

Причина: `captureSaasBillingPaymentSucceeded` пишет `status='paid'` внутри drizzle-транзакции, которая берёт
соединение через **`pool.connect()`** — минуя обёртку `pool.query`, единственную, что устанавливает RLS-контекст
организации. Внутри транзакции `app.current_org_id()` всегда `NULL`, RLS **молча** фильтрует `UPDATE` в ноль
строк, и аксессор `app.apply_paid_saas_billing_tariff` корректно отказывает — с его точки зрения строки нет.

Опасен класс, а не случай: **отказ молчаливый**. Не ошибка прав, не исключение — просто ноль строк. Любой другой
код, взявший соединение так же, ведёт себя так же и выглядит как «ничего не нашлось».

## Работа

1. **Сначала перепись, потом починка.** Найти ВСЕ места, где соединение берётся мимо обёртки, устанавливающей
   принципала: `pool.connect()`, прямой `drizzle(...)` поверх сырого клиента, транзакции вне порта — в обоих
   приложениях. Список с файлами и строками — в отчёт; это половина ценности задачи.
2. **Починить капчер** так, чтобы транзакция шла через тот же порт, что и обычные чтения, и принципал
   устанавливался внутри неё. Не «выставить контекст руками рядом» — это повторит ту же ошибку в новом месте.
3. **Закрыть класс, а не случай.** Транзакция без принципала не должна тихо возвращать ноль строк: либо порт —
   единственный способ получить транзакцию, либо явная проверка, падающая заметно. Выбор обосновать: важно, чтобы
   следующий не смог повторить это случайно.
4. **Остальные найденные места** — не чинить, а перечислить с оценкой риска: какие работают с таблицами под RLS
   (то есть могут уже молча терять строки), какие нет.

## Границы

- Не трогать: проверку подписи вебхука, IP-allowlist, guard-триггер, машину состояний инвойса, сам аксессор.
- Миграция не ожидается; если понадобится — временный номер в клоне, финальный присваивает лид при сведении.
- **PROD (`135.106.162.170`) не трогать.** Деплой в этом прогоне не делать. Push не делать.

## Готово значит

- Перепись мест, берущих соединение мимо порта, — с файлами и строками.
- Капчер проходит: поведенческий тест доказывает, что транзакция сохраняет принципала и `UPDATE` находит строку;
  плюс тест на класс — транзакция без принципала не проходит тихо.
- Целевые тесты, `typecheck`, точечный ESLint, `git diff --check` чисты.
- Один коммит на своей ветке. В отчёте прямо сказать: сколько ещё мест найдено и какие из них опасны.
