# B0.2 mock deploy gate repair — 2026-08-02

Authority: `SAAS_BILLING_PLAN.md` B0.2/B0.3 and owner ruling: mocks must not deploy.

- Gate contract: `bash deploy/host/assert-no-mock-payment-deploy.sh <repo_root>` passes only with zero `*/payments/mock-complete/route.ts` files and no `apps/webapp/src/modules/payments/mockPaymentGatePolicy.ts`.
- Measured on this tree: `bash deploy/host/assert-no-mock-payment-deploy.selftest.sh` → `3/3 passed`; `bash deploy/host/assert-no-mock-payment-deploy.sh "$PWD"` → PASS.
- Caller census: `rg -l 'assert-no-mock-payment-deploy\\.sh' deploy/host/deploy-prod.sh deploy/host/deploy-webapp-prod.sh deploy/host/deploy-test-saas.sh | wc -l` → `3`; no caller passes `NODE_ENV` or `VITEST_WORKER_ID`.
- Independent behavior/inspection audit: PASS; no deploy, edits, or full CI.

B0.2 remains open; its checkbox is closed only by the lead's merge commit.
