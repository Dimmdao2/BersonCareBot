# Сверка платежей с провайдером, баланс клиники, срок жизни счёта

Владелец, 18.08: «Сверка платежей с провайдером — делаем». Плюс его решения:
расписание адекватное (не каждые две минуты); уведомление глобал-админу как критический алерт **без спама**
(повторно — не чаще раза в сутки, пока не починили); непарный платёж **зачисляется автоматически**;
у клиники появляется **баланс**, с которого можно оплатить тариф; **оплатить можно только актуальный счёт**,
срок жизни счёта — константа 30 дней в коде.

Источник практики: исследование 18.08 (ЮKassa шлёт вебхук 7 раз за 24 часа и прекращает; Stripe — баланс
выводится из неизменяемого списка операций; Alertmanager/PagerDuty — одно происшествие на отпечаток).

## Этап 1 — предусловия свёрки (без них расписание = генератор ложных тревог)

- [ ] 1.1 Фильтр `status=succeeded` в запросе списка платежей.
      Сейчас забираем все статусы, и каждый `pending`/`canceled` выглядит расхождением.
      `apps/webapp/src/infra/payments/yookassaPaymentProvider.ts` (~212-216)
- [ ] 1.2 Список платежей отдаёт те же поля, что и вебхук: `invoice_details.id ?? id`, `metadata`,
      `refunded_amount`. Сейчас отдаёт только `id` — **спящий баг**: в день включения Счетов каждый
      выставленный счёт покажется расхождением. Вебхук уже берёт ref правильно (`:459`).
      `modules/payments/providerPort.ts` (тип `PaymentProviderListedPayment`) + `yookassaPaymentProvider.ts` (~196-200, 484-495)
- [ ] 1.3 Единственный дом срока жизни счёта: `modules/saas-billing/invoiceValidity.ts` —
      `SAAS_BILLING_INVOICE_VALIDITY_DAYS = 30`, `saasBillingInvoiceExpiresAt()`, `isSaasBillingInvoicePayable()`.
      Три читателя и ни одного четвёртого: выставление счёта, кнопка оплаты в кабинете, окно свёрки по журналу.
- [ ] 1.4 Развести окна сравнения: провайдер→журнал — точечный поиск по ref без окна;
      журнал→провайдер — окно по дате оплаты. Сейчас обе стороны режутся одним окном, и счёт,
      выставленный месяц назад и оплаченный сегодня, всегда «расхождение».
      `modules/saas-billing/service.ts` (~622-711), `ports.ts`
- [ ] 1.5 Развести пространство имён происшествий: `SAAS_BILLING_RECONCILE_CADENCE_INTEGRATION` +
      параметр `integration` в open/resolve. Иначе пятиминутный тик здоровья закрывает наши происшествия,
      следующая сверка открывает заново — и алерт приходит **каждый час**. Ровно то, что владелец запретил.
      `modules/operator-health/ports.ts` (~223), `infra/repos/pgOperatorHealthWrite.ts` (~294, ~401), in-memory двойник

Гейт этапа: живой запрос к тестовому магазину подтверждает, что провайдер принимает фильтр по статусу
и возвращает `metadata` в списке. Пока не подтверждено — на этом строить нельзя.

## Этап 2 — расписание и алерт

- [ ] 2.1 Маршрут тика по образцу `api/internal/saas-billing/renewal/tick/route.ts`, параметр `windowHours`
- [ ] 2.2 Крон: раз в час `windowHours=48` + ночью `windowHours=336`. 48 ч перекрывают сутки повторов вебхука
- [ ] 2.3 Одно происшествие на расхождение (отпечаток по ref/id счёта), не на прогон

## Этап 3 — баланс

- [ ] 3.1 Таблица движений, только дозапись; баланс = сумма. Не колонка с числом
- [ ] 3.2 Ограничения в БД, а не в коде: один платёж нельзя зачислить дважды; нельзя зачислить чужой клинике
- [ ] 3.3 Автозачисление только если видно организацию; иначе — тревога и деньги не разобраны
- [ ] 3.4 Баланс виден клинике, с него можно оплатить тариф (одна транзакция, баланс не уходит в минус)

## Этап 4 — срок жизни счёта в кабинете

- [ ] 4.1 Просроченный/неактуальный счёт недоступен к оплате
- [ ] 4.2 Ссылка на оплату протухает через час — кабинет отдаёт сохранённую без проверки свежести. Чинить
- [ ] 4.3 Деньги по неактуальному счёту → на баланс, старый счёт оплаченным **не** помечать

## Ждут решения владельца

- [ ] G1 Двойное списание: у провайдера защита от повтора живёт сутки, наши ключи вечные. Повторная отправка
      черновика старше суток спишет деньги второй раз. Предложено: обновлять ключ, если черновику >24 ч
- [ ] G2 Чек при оплате с баланса (54-ФЗ). Предложено: второго чека нет — чек пробит при приходе денег
- [ ] G3 Возврат неразобранных денег: кнопка у глобал-админа (предложено) или автоматически

## Не сделано / вне скоупа

- Сверка возвратов (`GET /v3/refunds`) — отдельным заходом
