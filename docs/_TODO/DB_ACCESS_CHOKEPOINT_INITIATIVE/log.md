# Execution log — DB access chokepoint

Обязателен по `.cursor/rules/plan-authoring-execution-standard` (§12.5). По одной записи на этап:
что сделано, какие проверки (+результат), решения, что сознательно НЕ делали.

| Дата | Этап | Сделано | Проверки (результат) | Решения / пропущено |
|---|---|---|---|---|
| 2026-07-02 | S1W | Убран route-level pool bypass из `app/api/integrator/messenger-phone/bind/route.ts`: добавлен `executeMessengerPhoneHttpBindWithDefaultPool` в app-layer executor, route больше не импортирует `getPool()` и вызывает тонкий facade. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/integrator/messengerPhoneHttpBindExecute15E.test.ts src/app/api/integrator/messenger-phone/bind/route.test.ts --reporter verbose"` (17 tests); PASS: wrapped локальный `eslint` по executor/route/test; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие `getPool()`/DB imports in route. | `executeMessengerPhoneHttpBind(pool, ...)` сохранён для explicit tests/reuse; org/RLS/tenancy не менялись. |
| 2026-07-02 | S1V | Убран media preview SQL slice из `app-layer/health/collectAdminSystemHealthData.ts`: grouped preview counts и stale pending count перенесены в `infra/repos/pgAdminMediaPreviewHealth.ts`; app-layer оставляет расчёт counters/status и общий system-health orchestration. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/app/api/admin/system-health/route.test.ts src/app-layer/health/computeMediaPreviewStatus.test.ts src/infra/repos/pgAdminMediaPreviewHealth.repo.test.ts --reporter verbose"` (23 tests); PASS: wrapped локальный `eslint` по health app-layer/route test/repo; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие media preview SQL и `runWebappPgText` в app-layer health file, transport остался в repo. | Audit-log `getPool()`/`writeAuditLog` в system-health остаются separate S1/S3 candidate; org/RLS/tenancy не менялись. |
| 2026-07-02 | S1U | Закрыт dedicated-client остаток в `app-layer/integrator/messengerPhoneHttpBindExecute.ts`: `pool.connect()` + BEGIN/COMMIT/ROLLBACK завернуты в `startMessengerPhoneBindTransaction` внутри `infra/repos/pgMessengerPhoneHttpBind.ts`; app-layer больше не импортирует `PoolClient` и не управляет raw client lifecycle. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/integrator/messengerPhoneHttpBindExecute15E.test.ts src/app/api/integrator/messenger-phone/bind/route.test.ts --reporter verbose"` (17 tests); PASS: wrapped локальный `eslint` по app-layer/repo/route tests; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие `PoolClient`, `pool.connect`, `client.query`, tx wrappers in app-layer, checkout остался в repo. | Conflict/audit/relay orchestration intentionally unchanged; org/RLS/tenancy не менялись. |
| 2026-07-02 | S1T | Убран app-layer SQL/DB transport из `app-layer/media/videoHlsLegacyBackfill.ts`: legacy HLS candidate batch, status histogram и failed-reasons SELECT перенесены в `infra/repos/pgVideoHlsLegacyBackfill.ts`; app-layer оставляет backfill orchestration, dry-run, лимиты и enqueue. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/app-layer/media/videoHlsLegacyBackfill.test.ts src/infra/repos/pgVideoHlsLegacyBackfill.repo.test.ts --reporter verbose"` (11 tests); PASS: wrapped локальный `eslint` по app-layer/repo/tests; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие `@/infra/db`, `runPgPoolPgText`, `getPool()` и media SQL в `app-layer/media/videoHlsLegacyBackfill.ts`, transport остался в repo. | Public script/route imports and HLS SQL filter re-exports сохранены; org/RLS/tenancy не менялись. |
| 2026-07-02 | S1S | Завершён `adminTranscodeHealthMetrics` DB extraction: Drizzle aggregates over `media_transcode_jobs` перенесены в `infra/repos/pgAdminTranscodeHealthMetrics.ts`; app-layer metrics file теперь только объединяет repo metrics и сохраняет safe wrapper/logging. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/app-layer/media/adminTranscodeHealthMetrics.test.ts src/app-layer/media/videoHlsLegacyBackfill.test.ts --reporter verbose"` (12 tests); PASS: wrapped локальный `eslint` по metrics app-layer/test/repo; PASS: `git diff --check`; PASS: `rg` подтвердил, что `getDrizzle`, `mediaTranscodeJobs`, `runWebappPgText` находятся в repo, не в `app-layer/media/adminTranscodeHealthMetrics.ts`. | `videoHlsLegacyBackfill.ts` всё ещё содержит app-layer DB access and remains a later S1 target. Org/RLS/tenancy не менялись. |
| 2026-07-02 | S1R | Убран media_files count SQL из `app-layer/media/adminTranscodeHealthMetrics.ts`: HLS SQL filter helpers выделены в `infra/repos/mediaHlsLegacySqlFilters.ts`, counts перенесены в `infra/repos/pgAdminTranscodeHealthMetrics.ts`; `videoHlsLegacyBackfill.ts` оставляет backward-compatible re-export и локальный import helpers. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/app-layer/media/adminTranscodeHealthMetrics.test.ts src/app-layer/media/videoHlsLegacyBackfill.test.ts --reporter verbose"` (12 tests); PASS: wrapped локальный `eslint` по media app-layer/repo/tests; PASS: `git diff --check`; PASS: `rg` подтвердил, что `runWebappPgText` и media_files count SQL теперь в `infra/repos/pgAdminTranscodeHealthMetrics.ts`, не в app-layer metrics file. | Drizzle aggregates over `media_transcode_jobs` в `adminTranscodeHealthMetrics.ts` оставлены для отдельного S1 slice; `videoHlsLegacyBackfill.ts` всё ещё содержит app-layer DB access and remains a later S1 target. Org/RLS/tenancy не менялись. |
| 2026-07-02 | S1Q | Начат `app-layer/integrator/messengerPhoneHttpBindExecute.ts` без большого diff: SQL helpers (`TxQuery`, TX wrappers, canonical integrator user resolver, MAX identity ensure, identity peek, `setUserPhone`, pool adapter) вынесены в новый `infra/repos/pgMessengerPhoneHttpBind.ts`. App-layer сохранил orchestration/audit/relay и public result contract. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/integrator/messengerPhoneHttpBindExecute15E.test.ts src/app/api/integrator/messenger-phone/bind/route.test.ts --reporter verbose"` (17 tests); PASS: wrapped локальный `eslint` по app-layer/repo/tests; PASS: `git diff --check`; PASS: `rg` подтвердил, что вынесенные SQL strings/`runWebappPgText`/`runPgPoolPgText` находятся в `infra/repos/pgMessengerPhoneHttpBind.ts`, не в app-layer. | Full `executeMessengerPhoneHttpBind` TX orchestration (`pool.connect`, audit/relay side effects) ещё в app-layer и требует следующего S1 slice; org/RLS/tenancy не менялись. |
| 2026-07-02 | S1P | Закрыт layer-boundary slice для `modules/auth/channelLink.ts`: добавлен module-owned `ChannelLinkDbPort`, PG adapter `infra/repos/pgChannelLinkDbPort.ts`, binding в `ensureAuthModulePortsBound`; module больше не импортирует `@/infra/db`/`@/infra/repos` и не держит DB transport. Integrator complete route теперь гарантирует auth port binding, route tests обновлены. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/auth/channelLink.test.ts src/infra/repos/pgChannelLinkClaim.repo.test.ts src/app/api/auth/channel-link/start/route.test.ts src/app/api/integrator/channel-link/complete/route.test.ts --reporter verbose"` (22 tests); PASS: `buildAppDeps.test.ts` (20 tests); PASS: wrapped локальный `eslint` по module/port/adapter/routes/tests; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие `@/infra/db`, `@/infra/repos`, DB transport в `modules/auth/channelLink.ts`. TYPECHECK BLOCKED unrelated: `apps/webapp/src/app/api/doctor/account/timezone/route.test.ts` uses `Request` where `NextRequest` is expected; `apps/webapp/src/modules/reminders/disableReminderMessengerTopic.ts` has pre-existing `string | undefined` to `string` error. | Public route contracts and channel-link behavior unchanged; org/RLS/tenancy не менялись. `logger`/config/module-notifier imports не относились к DB bypass scope. |
| 2026-07-02 | S1O | Убран disposable-stub claim transaction transport из `modules/auth/channelLink.ts`: BEGIN/COMMIT/ROLLBACK wrapper перенесён в `infra/repos/pgChannelLinkClaim.ts`, module получает repo-level result и сохраняет прежний conflict/error mapping. Добавлен repo unit test на commit/rollback contract. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/auth/channelLink.test.ts src/infra/repos/pgChannelLinkClaim.repo.test.ts --reporter verbose"` (12 tests); PASS: wrapped локальный `eslint` по module/repo/tests; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие `client.query`/`pool.connect`/domain SQL transport в `modules/auth/channelLink.ts`. | `modules/auth/channelLink.ts` всё ещё импортирует infra repos/db напрямую; это следующий S1 layer-boundary slice, отдельно от transaction transport. Org/RLS/tenancy не менялись. |
| 2026-07-02 | S1N | Убран real-owner merge transaction helper из `modules/auth/channelLink.ts`: `tryMergeChannelLinkOwners` перенесён в `infra/repos/pgChannelLinkClaim.ts`, вместе с `platform-merge` import и domain SQL mark-used внутри merge transaction. Module сохранил orchestration, conflict audit и result mapping. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/auth/channelLink.test.ts --reporter verbose"` (10 tests); PASS: wrapped локальный `eslint` по `channelLink.ts`, `channelLink.test.ts`, `pgChannelLinkStart.ts`, `pgChannelLinkClaim.ts`; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие `runWebappPgText`/`platform-merge` в `modules/auth/channelLink.ts`. | Disposable-stub claim transaction (`pool.connect()` + BEGIN/COMMIT/ROLLBACK) сознательно оставлен отдельным S1 slice. Org/RLS/tenancy не менялись. |
| 2026-07-02 | S1M | Убран второй узкий SQL slice из `modules/auth/channelLink.ts`: token lookup, existing binding lookup, same-user mark-used, new binding insert и final mark-used перенесены в `infra/repos/pgChannelLinkStart.ts`; complete flow сохранил прежний result shape и конфликтную логику. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/auth/channelLink.test.ts --reporter verbose"` (10 tests); PASS: wrapped локальный `eslint` по `channelLink.ts`, `channelLink.test.ts`, `pgChannelLinkStart.ts`; PASS: `git diff --check`; PASS: `rg` подтвердил, что вынесенные SQL-строки находятся только в `infra/repos/pgChannelLinkStart.ts`. | Merge/claim transaction paths в `completeChannelLinkFromIntegrator` сознательно не трогались; оставшийся module SQL относится к `tryMergeChannelLinkOwners` и будет отдельным S1 slice. Org/RLS/tenancy не менялись. |
| 2026-07-01 | S1L | Убран первый узкий SQL slice из `modules/auth/channelLink.ts`: start-token cleanup/insert (`channel_link_secrets`) и phone binding info SELECT перенесены в `infra/repos/pgChannelLinkStart.ts`; public API и тексты ответов сохранены. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/auth/channelLink.test.ts --reporter verbose"` (10 tests); PASS: wrapped локальный `eslint` по `channelLink.ts`, `channelLink.test.ts`, `pgChannelLinkStart.ts`; PASS: `git diff --check`; PASS: `rg` подтвердил, что SQL-строки S1L slice находятся только в `infra/repos/pgChannelLinkStart.ts`. | `completeChannelLinkFromIntegrator` merge/claim transaction paths сознательно не трогались в этом micro-stage; `channelLink.ts` всё ещё имеет infra imports для оставшихся paths и требует следующих S1 slices. Org/RLS/tenancy не менялись. |
| 2026-07-01 | S1K | Убран app-layer SQL/transaction из `app-layer/doctor/createDoctorClient.ts`: canonical phone lookup, existing row load, email conflict check, client insert transaction и phone history transition перенесены в `infra/repos/pgDoctorClientCreate.ts`. App-layer оставляет валидацию, display fallback, trusted-phone anchor и email setup enqueue. Обновлены app-layer tests и добавлен repo transaction test. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/app-layer/doctor/createDoctorClient.test.ts src/infra/repos/pgDoctorClientCreate.repo.test.ts --reporter verbose"` (10 tests); PASS: wrapped локальный `eslint` по app-layer/repo/tests; PASS: `git diff --check`; PASS: `rg` подтвердил, что DB transport остался только в `infra/repos/pgDoctorClientCreate.ts`, не в app-layer. | Behavior/return shape сохранены; org/RLS/tenancy не менялись. |
| 2026-07-01 | S1J | Убран module-level SQL из `modules/reminders/disableReminderMessengerTopic.ts`: occurrence ownership/topic SELECT и messenger bindings SELECT перенесены в `infra/repos/pgReminderMessengerTopicDisable.ts`; module теперь получает `loadOccurrenceRule`/`loadChannelBindings` через deps. Добавлен module unit test для нового deps contract. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/modules/reminders/disableReminderMessengerTopic.test.ts src/app/api/integrator/reminders/messenger-topic/disable/route.test.ts --reporter verbose"` (7 tests); PASS: wrapped локальный `eslint` по module/test/repo/app-layer facade/route/test; PASS: `git diff --check`; PASS: `rg` подтвердил, что SQL transport остался только в новом infra repo. | Route contract и тексты ответа не менялись; route пока остаётся composition caller с `getPool()` через `app-layer/db/client` для existing canonical lookup и новых repo helpers. Org/RLS/tenancy не менялись. |
| 2026-07-01 | S1I | Убран module-level SQL из `modules/reminders/loadWarmupsSectionSlugs.ts`: helper перенесён в `infra/repos/pgWarmupsSectionSlugs.ts`, обновлены imports в reminder projection, internal web-push tick и warmup slug cache, а также тестовые моки. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/infra/repos/pgReminderProjection.pg.test.ts src/app-layer/reminders/runWebPushOnlyReminderInternalTick.test.ts --reporter verbose"` (8 tests); PASS: wrapped локальный `eslint` по helper/callers/tests; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие старого `modules/reminders/loadWarmupsSectionSlugs` import. | Query/shape сохранены; scheduler, cache TTL, projection behavior, org/RLS/tenancy не менялись. |
| 2026-07-01 | S1H | Убран page-level SQL из `app/app/settings/page.tsx`: чтение `platform_users.calendar_timezone` заменено на уже существующий `app-layer/doctor/accountTimezone.ts` facade, добавленный в S1B; SQL остаётся в `infra/repos/pgPlatformUserCalendarTimezone.ts`. | PASS: wrapped локальный `eslint` по `settings/page.tsx`, account timezone facade и repo; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие `@/infra/db`, `runWebappPgText`, raw timezone SELECT и `getPool()` в settings page. | Scope ограничен Settings page read path; запись timezone, Settings UI, system_settings, org/RLS/tenancy не менялись. |
| 2026-07-01 | S1G | Убраны route-level infra repo imports из integrator web-push subscription endpoints: `app/api/integrator/web-push/subscriptions/route.ts` и `delete/route.ts` теперь вызывают тонкий facade `app-layer/integrator/webPushSubscriptions.ts`; SQL/DB transport остаётся в существующем `infra/repos/pgWebPushSubscriptions.ts`. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run --project fast src/app/api/integrator/web-push/subscriptions/route.test.ts --reporter verbose"` (6 tests); PASS: wrapped локальный `eslint` по двум route handlers, route test и facade; PASS: `git diff --check`; PASS: `rg` подтвердил отсутствие прямого infra import в route handlers (infra import остался только в app-layer facade). | Scope ограничен route/app-layer boundary; поведение, auth, SQL, org/RLS/tenancy не менялись. |
| 2026-06-30 | S1F | Убран app-layer SQL transport из `app-layer/platform-user/recordPublicBookingMergeCandidates.ts`: поиск name-collision кандидатов публичной записи перенесён в `infra/repos/pgPublicBookingMergeCandidates.ts`; app-layer оставляет orchestration и вызов `patientMergeCandidate.upsertPending`. Обновлены unit/verify тесты. | PASS: `pnpm --dir apps/webapp exec vitest run --project fast src/app-layer/platform-user/recordPublicBookingMergeCandidates.test.ts src/infra/repos/webappPhase15E.repo.test.ts --reporter verbose` (11 tests); PASS: локальный `eslint` по app-layer wrapper/test/repo/verify; PASS: `rg` подтвердил отсутствие SQL transport в app-layer файле. | Поведение/shape сохранены; route API публичной записи не менялся. Org/RLS/tenancy не трогались. |
| 2026-06-30 | S1E | Убран app-layer SQL transport из `app-layer/platform-user/resolveOrCreateUserByPhone.ts`: canonical lookup + INSERT `platform_users` перенесены в `infra/repos/pgPublicBookingUserResolve.ts`; app-layer оставляет нормализацию телефона, display fallback и trusted-phone anchor side effect. Обновлён verify-тест `webappPhase15E.repo.test.ts` под новый invariant: app-layer без `runPgPoolPgText`, SQL transport в repo. | PASS: `pnpm --dir apps/webapp exec vitest run --project fast src/infra/repos/webappPhase15E.repo.test.ts --reporter verbose` (9 tests); PASS: локальный `eslint` по app-layer wrapper, новому repo и verify test; PASS: `rg` подтвердил отсутствие DB transport в app-layer файле и наличие SQL только в repo. | `adminTranscodeHealthMetrics` временно отложен внутри S1: перенос требует предварительно выделить SQL-фильтры из `videoHlsLegacyBackfill`, иначе возникнет обратная зависимость infra→app-layer. Org/RLS/tenancy не трогались. |
| 2026-06-30 | S1D | Убран app-layer raw SQL из `app-layer/messaging/resolvePatientTelegramUsernameMention.ts`: cross-schema Telegram username SELECT перенесён в `infra/repos/pgPatientTelegramUsernameMention.ts`, app-layer wrapper сохранил прежний fail-closed contract и форматирование mention. Добавлен unit test wrapper. | PASS: `pnpm --dir apps/webapp exec vitest run --project fast src/app-layer/messaging/resolvePatientTelegramUsernameMention.test.ts --reporter verbose` (2 tests); PASS: локальный `eslint` по wrapper/test/repo; PASS: `rg` подтвердил отсутствие SQL/DB transport в app-layer файле. | Поведение не менялось: lookup exceptions по-прежнему возвращают `null`. Org/RLS/tenancy не трогались. |
| 2026-06-30 | S1C | Убран page-level SQL из `app/app/doctor/patients/[userId]/page.tsx` и embedded program page: чтение `height_cm/weight_kg` заменено на существующий `deps.doctorClients.getPatientPhysical(userId)`, SQL остаётся в `infra/repos/pgDoctorClients.ts`. | PASS: локальный `eslint` по двум изменённым `page.tsx`; PASS: `rg` подтвердил отсутствие `runWebappPgText`/height-weight SQL в страницах и наличие SQL только в infra repo. | Новых тестов не добавлялось: поведение заменено на уже существующий сервисный метод с тем же запросом/shape. Общие webapp lint/typecheck не повторялись из-за ранее зафиксированных unrelated blockers. Org/RLS/tenancy не трогались. |
| 2026-06-30 | S1B | Убран route-level SQL из `app/api/doctor/account/timezone/route.ts`: чтение/запись `platform_users.calendar_timezone` перенесены в новый `infra/repos/pgPlatformUserCalendarTimezone.ts`, route вызывает тонкий `app-layer/doctor/accountTimezone.ts`. Добавлен route unit test. | PASS: `pnpm --dir apps/webapp exec vitest run --project fast src/app/api/doctor/account/timezone/route.test.ts --reporter verbose` (3 tests); PASS: локальный `eslint` по `route.ts`, `route.test.ts`, app-layer facade и новому repo; PASS: `rg` подтвердил отсутствие infra import/SQL в route. | `pgPatientCalendarTimezone` не переиспользован, потому что он client-only (`role='client'`), а doctor account route должен работать для doctor/admin. Общие webapp lint/typecheck не повторялись: после S1A уже зафиксированы unrelated blockers. Org/RLS/tenancy не трогались. |
| 2026-06-30 | S1A | Убран первый узкий role-precheck DB bypass из doctor-client archive/permanent-delete путей: прямые `getPool().query("SELECT role FROM platform_users ...")` в `modules/doctor-clients/clientArchiveChange.ts` и `app/api/doctor/clients/[userId]/permanent-delete/route.ts` заменены на метод `DoctorClientsPort.getPlatformUserRole`; SQL живёт в `infra/repos/pgDoctorClients.ts`. Обновлены in-memory порт и route/service тестовые моки. | PASS: `pnpm --dir apps/webapp exec vitest run --project fast src/app/api/doctor/clients/\\[userId\\]/archive/route.test.ts --reporter verbose` (3 tests); PASS: `pnpm --dir apps/webapp exec vitest run --project fast src/app/api/doctor/clients/\\[userId\\]/permanent-delete/route.test.ts --reporter verbose` (4 tests); PASS: `pnpm --dir apps/webapp exec vitest run --project fast src/modules/doctor-clients/service.test.ts --reporter verbose` (6 tests); PASS: локальный `eslint` по изменённым файлам. BLOCKED unrelated: `pnpm --dir apps/webapp typecheck` падает на stale `.next/types/validator.ts` references to missing routes/pages; `pnpm --dir apps/webapp lint` падает на существующие unrelated route infra imports + React hook/static-components errors outside S1A. Один первый Vitest-запуск с некорректным паттерном был остановлен после запуска широкого набора unrelated тестов. | Поведение сохранено: `role === null` даёт `not_found` в archive path, non-client даёт `not_client`; permanent-delete по-прежнему возвращает `not_client` для не-client. Не трогались org/RLS/tenancy, purge logic, UI и unrelated lint blockers. |
| 2026-06-30 | S0 | Старт инициативы по команде владельца. Прочитаны core docs/rules, R0/R1 планы и prior art `INTEGRATOR_DRIZZLE_MIGRATION`. Создан `db-access-map.md`: карта process trunks, runtime `.connect()`, `new Pool`, S1 layer-bypass кандидатов, `system_settings` bypassers, KEEP-зон по ADR. Исправлена базовая трактовка старого SAAS raw-SQL аудита: текущий integrator direct `db.query` в runtime — транспорт/health + мигратор/ops, не десятки repo-файлов. | Docs-only stage: `rg` inventory по `.connect()`, `new Pool`, `system_settings`, integrator `db.query`; кодовые тесты не запускались, потому что код не менялся. | Кода, миграций, DB write/read к dev/prod БД не было. Org/RLS/tenancy не трогались. KEEP-зоны не считаются кандидатами на Drizzle rewrite; они должны быть отражены в будущих guard allowlist. |
| 2026-06-17 | — | Инициатива заведена как заготовка в `_TODO/` (REQUIREMENTS + MASTER_PLAN + log). Источник scope — аудит `../SAAS_FOUNDATION/RAW_SQL_AUDIT.md`. | n/a (доки) | Кода нет (staging). Стартует по команде владельца, ДО SAAS. |
