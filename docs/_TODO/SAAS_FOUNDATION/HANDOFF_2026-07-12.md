# SaaS multitenant — HANDOFF 2026-07-12 (read this first in a fresh session)

Authoritative state lives in DOCS + taskdb, not in any chat memory. Read, in order:

1. **This file** (status + next two tasks).
2. `docs/_TODO/SAAS_FOUNDATION/TENANT_WALLS_AND_ACCESS_MODEL.md` — the CANON model (owner-confirmed).
3. `node /home/dev/brain/tools/taskdb.mjs get 708` — card status and canonical plan link; running details live
   in the linked plan/log, not in taskdb notes.
4. Commits on branch `auto/code-pg-delta` (backed up to `origin` AND `dimmdao`, HEAD `2c8cd3302`).

## DONE + PROVEN (all on `auto/code-pg-delta`, NOT pushed to main/test, NOT deployed)

- **Deploy chain #667 fixed (option-D)** — migrate as runtime table-owner with temporary auto-revoked BYPASSRLS + app_owner; independent audit SHIP; db-state 5/5 on prod-copy. Commits `be53f7d69`, `5a6440550`.
- **Enforce RLS walls** — `deploy/postgres/phase4-locked-helper-rls-policies.sql` (161 policies), un-forgeable helper predicates reading locked `app.principal_context`; audited SHIP-WITH-FIXES (core sound). Commit `2a9797bf0`.
- **Patient wall = OWN-DATA-ONLY** (owner correction) — predicate `(is_staff() AND org=current_org_id()) OR (owns via current_patient_user_id())`; clinic scoping for a patient is APP-LEVEL, not a DB wall; patient principal is identity-only (no org-resolver/409). Commit `f8ebcfe40`. **This is the canonical model.**
- **Runtime port + gate** — `getDrizzle()` single port, fail-closed in `locked` mode; `DB_PRINCIPAL_CONTEXT_MODE = legacy-guc (default,safe) | shadow | locked`; central stamping: `getCurrentSession()` stamps staff(+org)/patient principal, `verifyIntegratorGetSignature` stamps bootstrap; enterWith cross-request leak fixed+tested; full-route static gate `check-b4-locked-runtime-wiring`. Commits `466452a57`, `340ea3a03`, `33f0302d2`.
- **Specialist self-registration BACKEND** — done + audited (`/api/auth/specialist-signup/start|confirm`, `OrganizationProvisioningService`, `pgOrganizationProvisioning`). **No signup UI page yet** (API only); flag `specialist_signup_enabled` defaults false.
- **LIVE multitenant rehearsal PASSED on a real prod-copy** — `node docs/archive/2026-08-no-disposable-db-retirement/RETIREMENT.md` (host, sudo -u postgres; `--synthetic` for sandbox). Restores newest prod dump → deploy → enforce locked+FORCE → seeds clinic B (self-reg S2 + 2nd specialist S3), P2 (clinic B), P_SHARED (both clinics) → proves the who-sees-what matrix. Result (EXIT=0, all CONFIRMED):
  - Staff S1 (clinic #1): sees clinic #1 only, zero clinic B.
  - Staff S2 & S3 (clinic B, two specialists): see clinic B org-wide (incl each other), zero clinic #1.
  - Patient P_SHARED (identity-only): sees OWN rows across BOTH clinics, no other patient.
  - Patient P2: own rows only; cannot see P1/P_SHARED.
  - Plain `SET app.*` cannot forge; no-context read fails CLOSED; legacy dormant reads clinic #1.
  - Disposable copy + all roles dropped after; prod (135.x) never touched.

## Default is SAFE

`DB_PRINCIPAL_CONTEXT_MODE=legacy-guc` by default → walls installed but DORMANT, clinic #1 behavior unchanged. Enforce only bites after a deliberate strict+FORCE flip.

## Deferred to PRE-PROD (NOT needed for test/registration)

- **PII tighten** — 2 bootstrap-hybrid tables (`platform_user_contacts`, `user_phone_history`) still permit `organization_id IS NULL` globally. See Task A below.
- **Final holistic audit** — per-piece audits done; a whole-package end-to-end audit + cutover runbook review before the prod button.

## Prod policy

NO prod deploy soon. Next real target = **deploy to TEST (151.x)** for the owner to see it + register a specialist. Agents do NOT push to main/test; the owner triggers deploys.

---

## TASK A — Tighten PII bootstrap-hybrid tables (pre-prod, moderate)

Goal: `platform_user_contacts` and `user_phone_history` must not be globally readable/writable via the `organization_id IS NULL` bootstrap-hybrid branch once enforce is on. Per the P0.5b/audit findings:

1. Add `organization_id` (uuid) to both tables (Drizzle schema + migration), backfill from the owning patient's enrollment / doctor workspace where unambiguous; keep NULL only for true pre-auth/system-bootstrap rows.
2. Update the repos that write them (`pgPlatformUserContacts.ts`, `pgPhoneHistory.ts` and the booking/contact/OTP write paths) to STAMP `organization_id`.
3. Change the enforce policy for these 2 tables so NULL-org is permitted ONLY for genuine bootstrap rows (or drop the NULL-permit entirely if all rows can carry org).
4. Re-verify: `node scripts/check-saas-db-regression.mjs`, the R2 isolation smoke, and confirm no legit clinic #1 row is locked out.
   Validate on a disposable prod-copy only (never prod/test/dev). Independent audit after.

## TASK B — Test rollout + specialist registration (current priority)

B1. **Build the specialist self-registration UI** (main gap): a signup page/flow in the webapp that calls the existing `/api/auth/specialist-signup/start` + `/confirm`. Backend/provisioning is done+audited. Wire the UI, then set `specialist_signup_enabled=true` (owner-gated). Verify by running the app (per the `run`/`verify` skills) — a real specialist self-registers → own clinic appears → they log in.
B2. **Prepare the TEST deploy runbook** (agent prepares, OWNER triggers — no agent push to test). Sequencing:

- Deploy the branch code to test with walls **DORMANT** (`DB_PRINCIPAL_CONTEXT_MODE=legacy-guc`) first → nothing breaks, owner opens the app, registers a 2nd specialist (2nd clinic).
- THEN flip to **ENFORCE** on test (apply enforce artifact strict + `phase4-force-rls-cutover.sql` + `p0-5b-grants.sql` + set `locked` mode + role-switch) → owner sees real cross-clinic isolation.
- Test uses its own env/DB/units (151.x); do not load prod env on test. Restore test DB from a fresh prod dump first per `DEPLOY_667_SEQUENCE.md` / `PHASE4_ROLLOUT_RUNBOOK.md`.
  B3. Verify visually (screenshots) that clinic #1 still works and the 2nd clinic is isolated.

## Working rules (this box)

Disposable DB copies only for validation (`bcb_saas_*_(scratch|rehearsal)_*`), never prod/test/dev; NEVER `REASSIGN OWNED` (grabs the global bcb_webapp_prod database object); OS `postgres` can't read /home/dev repo files → pipe SQL via stdin; scratch role names must be ≤63 chars. Code via Codex/Sol + independent audit by a different model; owner triggers test/prod deploys.
