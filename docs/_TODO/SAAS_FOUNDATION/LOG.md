# Execution log — SAAS_FOUNDATION

Mandatory per `.cursor/rules/plan-authoring-execution-standard` (§12.5). One entry per stage:
what was done, checks run (+results), decisions, what was deliberately NOT done.

| Date | Stage | Done | Checks (result) | Decisions / skipped |
|---|---|---|---|---|
| 2026-07-10 | R2 / T0.4-pre integrator-public schema cleanup readiness | Added T0.4-pre cleanup plan, inventory, ADR, T0 cutover checklist, DB access surface map, and machine-readable integrator/public table matrix. Classified `system_settings`, reminders, Rubitime/appointments, contacts/linked-phone, support conversations/questions, and queue/log tables by canonical owner, active writer/read path, and drop posture. Added dry-run scripts under `apps/webapp/scripts/integrator-schema-cleanup/` for aggregate-only audit, mirror backfill, source reconcile, owner-gated writer switches, and drop-safety checks. Removed the unused integrator-side `setNotifTemplate` direct writer to `public.system_settings`, kept read-only template fallback, and updated the P0.11 storage guard to reject reintroducing that direct writer. Corrected architecture docs so Google Calendar/settings runtime reads are documented as canonical `public.system_settings`, with `integrator.system_settings` treated as legacy mirror/cache-invalidation compatibility. | PASS: `pnpm --dir apps/webapp exec tsx scripts/integrator-schema-cleanup/01_audit.ts --help`; PASS: `pnpm --dir apps/webapp exec tsx scripts/integrator-schema-cleanup/02_backfill.ts --help`; PASS: `pnpm --dir apps/webapp exec tsx scripts/integrator-schema-cleanup/03_reconcile.ts --repo-root ../..`; PASS: `pnpm --dir apps/webapp exec tsx scripts/integrator-schema-cleanup/04_disable_writers.ts`; PASS: `pnpm --dir apps/webapp exec tsx scripts/integrator-schema-cleanup/05_drop_deprecated.ts --repo-root ../..`; PASS: `pnpm --dir apps/integrator test -- src/infra/db/repos/notifTemplatePort.test.ts` (179 files / 1327 tests); PASS: `pnpm --dir apps/webapp typecheck`; PASS: `pnpm --dir apps/webapp run lint`; PASS: `pnpm --dir apps/integrator run typecheck`; PASS: `pnpm --dir apps/integrator run lint`; PASS: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-storage.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-storage.mjs --self-test`; PASS: `pnpm run check:saas-db-regression`; PASS: `git diff --check`. | Readiness/audit stage only. No schema/table drops, no app runtime read-source flips, no dev/prod DB writes, no prod/env access, no real external channel calls, and no migration execution. Drop-safety scripts deliberately block all reviewed candidate tables because live references remain. Future owner-gated work: `integrator_linked_phone_source=public_only`, appointment/Rubitime canonical read-source flips, reminder dispatch-from-public design, support transport migration, and retention cleanup for terminal queues/logs. |
| 2026-07-09 | R1 / T0.3.32 final doctor/admin write-path tail audit | Closed the final T0.3 aggregate tail audit after #631/#632. The audit found one remaining real write-path residual in doctor booking appointment comments: the route now uses selected workspace context instead of default organization, wraps comment creation in `withDoctorWorkspacePrincipal`, and `pgClientHistory.createAppointmentComment` now requires the DB organization principal, runs through `runDrizzleMutationTransaction`, and stamps/validates `organization_id`. Added focused route tests, source-level appointment comment audit, and final aggregate doctor/admin mutating-route audit. The aggregate audit keeps 27 currently uncovered mutating route files explicitly classified as global settings/account/channel preferences, legacy Rubitime proxy/catalog disposition, read-only POST helpers, session toggle/OAuth/SMTP helpers, or platform identity patient creation where org enrollment is later product work rather than a silent T0.3 rewrite. Full CI surfaced stale tests from earlier T0.3 principal/org contract changes; those test mocks/expectations were updated without changing production logic. | PASS direct validation per owner instruction allowing direct checks: focused aggregate/audit `vitest` set (12 files / 162 tests), focused rerun for previously failing full-CI files (4 files / 25 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/audit/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and repeated full checkpoint `pnpm run ci` PASS after stale test fixes: lint, workspace typecheck, HLS sync, integrator tests (179 files / 1328 tests), webapp tests (1293 files / 7359 tests), media-worker tests (10 files / 32 tests), builds, webapp build, and audit/registry-prod-audit. | T0.3 aggregate closure only. No schema migration, no prod/dev DB access, no RLS policy/runtime role flip, no live external calls, and no platform identity/enrollment redesign. Subagent audit could not be spawned in this chat because the subagent thread limit is reached; local source audit plus executable audit tests and full CI were used for closure. Existing Turbopack NFT trace warning in webapp build remains a warning, not a T0.3 blocker. |
| 2026-07-09 | R1 / T0.3.42 admin media file folder residual principal | Closed the residual admin media file/folder mutation slice discovered by the T0.3 aggregate audit. Mutating `/api/admin/media/[id]` handlers now require selected doctor workspace context and wrap `deleteHard`, `updateMediaFolder`, and `updateDisplayName` under `withDoctorWorkspacePrincipal`; GET keeps its existing read gate. Mutating `/api/admin/media/folders` handlers now require selected workspace context and wrap `createFolder`, `moveFolder`, `renameFolder`, and `deleteFolder` write cores. `s3MediaStorage` media file mutations now fail closed without a DB organization principal, stamp/filter `media_files.organization_id`, validate target folder organization in SQL for file moves, and run hard-delete scheduling in a principal-aware pool transaction. `mediaFoldersRepo` folder writes now run through `runDrizzleMutationTransaction`, stamp `media_folders.organization_id`, and reject folder/parent org mismatches. Added focused route/repo tests and source-level audit coverage for the admin media residual slice. | PASS direct validation per owner instruction allowing direct checks: focused `vitest` for #632 audit, admin media file route, admin media folder routes, `mediaFoldersRepo`, and `s3MediaStorage` (6 files / 56 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed admin media route/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), `git diff --check`, and manual source audit for selected-workspace route gates / principal-aware repo writes. | T0.3.42 only. No schema migration, no prod/dev DB access, no RLS policy/runtime role flip, no live S3/delete worker run, no external calls, and no full CI in this slice. Admin media GET/list/read routes keep existing read behavior unless already executed under an active principal. |
| 2026-07-09 | R1 / T0.3.41 CMS content legacy action residual principal | Closed the residual legacy CMS content server-action write slice discovered by the T0.3 aggregate audit. `saveContentPage`, `saveContentSection`, `attachArticleSectionToSystemFolder`, `renameContentSectionSlug`, and `deleteContentSection` now require selected doctor workspace context and execute content page/section write cores under `withDoctorWorkspacePrincipal`. `pgContentPages` and `pgContentSections` writes now require a DB organization principal, run through `runDrizzleMutationTransaction`, stamp `organization_id`, and reject principal/row/linked-section/linked-course org mismatches before mutating. The section update path was tightened so taxonomy reads happen inside the same mutation transaction. Added focused action tests, delete-section principal coverage, and source-level audit coverage for the legacy CMS residual slice. | PASS direct validation per owner instruction allowing direct checks: focused `vitest` for #631 audit, CMS content actions, CMS section actions, `pgContentPages`, and `pgContentSections` (5 files / 50 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed CMS action/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), `git diff --check`, and manual source audit for legacy role-only guards / non-principal transaction gaps. | T0.3.41 only. No schema migration, no prod/dev DB access, no RLS policy/runtime role flip, no live external calls, and no full CI in this slice. CMS reads and non-mutating validation/revalidation behavior remain on existing paths. Subagent audit could not be spawned in this chat because the subagent thread limit is reached; local source audit plus focused audit tests were used for this slice. |
| 2026-07-09 | R1 / T0.3.44 doctor courses references residual principal | Closed the residual doctor courses/references write slice discovered by the T0.3 aggregate audit. Doctor course create/update routes now require selected workspace context and execute `deps.courses.*` mutations under `withDoctorWorkspacePrincipal`. Doctor reference POST and reference-management server actions now require selected workspace context and wrap write cores. Admin reference archive now requires `requireAdminModeSession`, selected workspace context, and principal wrapping around lookup/archive. `pgCourses` writes now require a DB organization principal, run through `runDrizzleMutationTransaction`, stamp `courses.organization_id`, and reject linked template/page org mismatches. `pgReferences` writes now require a DB organization principal, set `app.org` inside SQL transactions, stamp `reference_items.organization_id`, and reject category/item org mismatches. Added focused route/repo tests and source-level audit coverage for the courses/references residual slice. | PASS direct validation per owner instruction allowing direct checks: focused `vitest` for #634 audit, doctor reference route, admin reference archive route, `pgCourses`, and `pgReferences` (5 files / 26 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed routes/actions/repos/tests, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. Read-only subagent audit confirmed the P0 map before implementation and found no additional same-family write routes beyond the handled files. | T0.3.44 only. No schema migration, no prod/dev DB access, no RLS policy/runtime role flip, no live external calls, and no full CI in this slice. Course usage route remains GET-only. Public/reference read routes and doctor/admin reads keep their existing read behavior unless executed under an active principal. |
| 2026-07-09 | R1 / T0.3.43 treatment template API residual principal | Closed the residual doctor treatment-program-template mutating API slice discovered by the T0.3 aggregate audit. All mutating `doctor/treatment-program-templates/**` route handlers now require selected doctor workspace context and execute `deps.treatmentProgram.*` mutations under `withDoctorWorkspacePrincipal`. `pgTreatmentProgram` template/stage/group/item writes now require a DB organization principal, execute through `runDrizzleMutationTransaction`, stamp `organization_id`, and reject principal/parent/source org mismatches across template, stage, group, item, LFK complex expansion, and test-set expansion paths. Added source-level audit coverage that discovers every `POST`/`PATCH`/`DELETE` route export under the template API folder and verifies each treatment-program mutation call is inside the balanced `withDoctorWorkspacePrincipal(...)` call range, plus repo-level transaction/principal guards. | PASS direct validation per owner instruction allowing direct checks: focused source audit (19 tests), expanded `vitest` for template audit, existing treatment-program instance/catalog audits, `pgTreatmentProgram` usage tests, and treatment-program service tests (5 files / 127 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/audit files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audits: first audit found no implementation blockers and two P2 weak-audit gaps; both were fixed. Final re-audit reported clean. | T0.3.43 only. No schema migration, no prod/dev DB access, no RLS policy/runtime role flip, no live external calls, and no full CI in this slice. Read-only template list/detail/usage routes remain on their existing read gates; this slice intentionally covers mutating template API residual principal. |
| 2026-07-09 | R1 / T0.3.40 doctor clinical recommendation test-set residual principal | Closed the residual doctor clinical-test, recommendation, test-set, and measure-kind catalog write slice discovered by the T0.3 aggregate audit. Clinical-test, recommendation, and test-set server actions now require selected doctor workspace context and run write cores under `withDoctorWorkspacePrincipal`. Matching API POST/PATCH/DELETE/PUT routes now use `requireDoctorWorkspaceApiContext` and wrap write service calls in the selected workspace principal. `pgClinicalTests`, `pgRecommendations`, and `pgTestSets` now require a DB organization principal for writes, execute mutations through `runDrizzleMutationTransaction`, stamp `organization_id`, and reject principal/row-org mismatches. `pgClinicalTestMeasureKinds` remains a global schema catalog without `organization_id`, but write-intent now requires a principal and mutations run through the same principal-aware transaction helper. Added source-level audit coverage for route/action gates, repo mutation transactions, and the global measure-kind exception. | PASS direct validation per owner instruction allowing direct checks: focused `vitest` for catalog source audit, clinical-tests/recommendations routes, test-set actions, measure-kind service, and catalog repos (8 files / 34 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/action/repo/audit files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audits: first audit found repo-layer principal gaps in clinical-tests/recommendations/test-sets; fixed. Second audit found measure-kind repo principal gap; fixed. Final scoped audit reported clean. | T0.3.40 only. No schema migration, no prod/dev DB access, no RLS policy/runtime role flip, no live external calls, and no full CI in this slice. `clinical_test_measure_kinds` is deliberately treated as current global catalog storage; org-scoped measure kinds would require a separate schema/product decision. |
| 2026-07-09 | R1 / T0.3.39 pgPhoneMessengerBind admin_audit_log org stamp | Closed the raw phone-messenger bind audit writer follow-up discovered during T0.3.37. `pgPhoneMessengerBind.recordMessengerBindBlocked` now stamps both `messenger_phone_bind_anomaly` and `messenger_phone_bind_blocked` inserts with `organization_id` from the current DB principal via the central `currentAuditOrganizationId()` helper, including the same default-organization fallback used by `adminAuditLog`. Added focused repo/audit coverage for anomaly insert fallback, blocked insert current-org stamping, and the existing-row repeat/update branch. | PASS direct validation per owner instruction allowing direct checks: focused `vitest` for `pgPhoneMessengerBind`/`adminAuditLog`/phone bind tests (initial 4 files / 46 tests; post-audit focused rerun 3 files / 43 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed repo/helper/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit after fix: no P0/P1; P2 missing repeat/update coverage was closed with an explicit regression test. | T0.3.39 only. No schema migration, no DB access, no live messenger/integrator calls, no RLS policy/runtime role flip, and no full CI in this slice. Existing open `conflict_key` dedupe remains global because `idx_admin_audit_log_conflict_open` is currently global on `conflict_key`; per-org dedupe would require explicit schema/design work and is intentionally not bundled here. |
| 2026-07-09 | R1 / T0.3.37 admin ops audit principal | Closed the admin ops audit principal slice before T0.4 readiness. Admin audit-log list/resolve routes now require selected doctor workspace context and run audit reads/resolution under `withDoctorWorkspacePrincipal`. Admin operational write routes for user profile patch, health-failure archive clear, and operator-incidents resolve-all now require selected workspace context before side effects and write `admin_audit_log` under the selected workspace principal. Central `adminAuditLog` now stamps inserted audit rows with the current DB principal organization, keeps a default organization fallback for legacy/system callers, and filters list/count/resolve operations by active principal organization when present. Added focused route, repo/helper, negative workspace-gate, and source-level audit coverage. | PASS direct validation per owner instruction allowing direct checks: focused `vitest` for admin audit-log routes, admin ops write routes, central helper/repo tests, and source audit (8 files / 66 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed admin audit/admin ops files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: no P0/P1 findings; P2 weak route-gate coverage was closed with explicit workspace-failure tests before seal. | T0.3.37 only. No schema migration, no prod/dev DB access, no RLS policy/runtime role flip, no full CI in this slice. Existing open `conflict_key` dedupe remains global because the current unique index is global; changing it per organization requires explicit schema/design work and is not silently bundled here. Raw `admin_audit_log` writes in `pgPhoneMessengerBind.ts` are tracked as required follow-up #629 before aggregate T0.3 closure. |
| 2026-07-09 | R1 / T0.3.38 legacy booking catalog Rubitime disposition | Resolved the final legacy booking-catalog/Rubitime admin mutation disposition before T0.4 readiness. `/api/admin/booking-engine/rubitime-mapping/*` was confirmed already routed through the booking-engine workspace gate with selected `organizationId`. Legacy `/api/admin/booking-catalog/*` is still a live Rubitime reference CRUD surface, not frozen-only, so it now requires selected doctor workspace context, carries `organizationId` in `AdminBookingCatalogContext`, and wraps every POST/PATCH/DELETE write path in `withAdminBookingCatalogPrincipal`. `pgBookingCatalog` admin mutations now run in `runDrizzleMutationTransaction`; side-effect syncs to `branches.timezone` and canonical `be_clinic_services` receive the same executor/transaction. Added source-level audit coverage for the admin catalog principal cutover and Rubitime mapping gate, then strengthened it to prove every mutating route call is inside the principal wrapper and every catalog write method passes `tx` into direct SQL/sync calls. | PASS direct validation per owner instruction allowing direct checks: focused `vitest` for admin booking-catalog routes/repo/audit (11 files / 97 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed booking-catalog route/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: no P0/P1 findings; two P2 weak-audit guards were closed by strengthening the source-level audit test. | T0.3.38 only. No schema migration, no prod/dev DB access, no live Rubitime calls, no RLS policy/runtime role flip, no external channel replay, and no full CI in this slice. Legacy catalog reads remain available behind the same admin + selected workspace gate; actual row ownership semantics still depend on the legacy/global catalog model until later non-legacy migration. |
| 2026-07-09 | R1 / T0.3.36Q doctor read-surface workspace containment | Closed the residual doctor read-surface slice before moving T0.3.36 forward. Doctor patient list API/page, appointment list SSR/API, Today KPI drill-down appointment lists, treatment-program instance read routes, discussion/summary/item-discussion/events/action-log/test-results reads, communications SSR comment loaders, exercise-comment/comment-metrics routes, doctor Today SSR dashboard, patient-card treatment-program preload, appointment dashboard metrics, patient dashboard metrics, support messaging unread/open conversation reads, and relevant patient/comment loaders now carry selected workspace `organizationId` or run under `withDoctorWorkspacePrincipal`. Legacy Rubitime `appointment_records` org scoping now fail-closes through BE appointment external mappings when selected org is supplied. Added/expanded focused route, loader, service, repo, and source-audit tests for the new read containment. | PASS direct focused validation per owner instruction allowing direct checks: `pnpm --dir apps/webapp typecheck`; targeted `eslint` on changed doctor route/page/repo/module/test files; `pnpm --dir apps/webapp exec vitest run ...` Q focus (19 files / 241 tests); `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests); `git diff --check`. PASS subagent audits: successive audits found real P1 SSR/Today/legacy appointment/patient metrics leaks; all were fixed. Final audit reported no P1 findings; its P2 weak-audit assertion was closed by binding the check to `getDashboardPatientMetrics({ organizationId: workspace.organizationId })`. | T0.3.36Q only. Legacy appointment records without BE native UUID or Rubitime external mapping remain hidden when selected organization scoping is required; this is intentional fail-closed SaaS containment. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI in this slice. |
| 2026-07-09 | R1 / T0.3.36P doctor client patient-bound read/write containment | Closed the residual `doctor/clients/[userId]` patient-bound endpoint slice of T0.3.36. Tasks GET/summary, notes GET/POST, support-settings GET/PATCH, client treatment-program instances GET/POST, program-day-activity, and LFK complex exercise local-comment PATCH now use selected workspace context, verify target patient membership through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, and use canonical `identity.userId` for downstream patient operations. Principal-backed reads/writes are wrapped with `withDoctorWorkspacePrincipal` where the repo honors DB principal. Added principal org filters for doctor notes reads, specialist task owner lists, treatment-program patient instance reads, and LFK local-comment update SQL. Program-day-activity now fail-closes with 404 if the principal-scoped instance lookup does not find the requested instance before diary snapshots are read. Added source-level audit coverage and tightened focused tests with distinct raw route id vs canonical patient id. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/clients/[userId]/tasks/route.test.ts src/app/api/doctor/clients/[userId]/support-settings/route.test.ts src/app/api/doctor/clients/[userId]/treatment-program-instances/route.get.test.ts src/app/api/doctor/clients/[userId]/treatment-program-instances/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts src/infra/repos/pgDoctorNotes.test.ts --reporter verbose` (6 files / 68 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audits: closure sweep classified remaining blockers; focused post-patch audit found a real P1 in program-day-activity null handling plus stale/canonical-id test gaps; all were fixed and revalidated. | T0.3.36P only. `doctor_patient_support` still has current schema uniqueness by patient, so this slice uses selected-workspace route membership plus principal-stamped mutations without a schema redesign. Broader doctor patient list, treatment-program read cluster, and exercise-comment read endpoints remain the next T0.3.36 closure slices before T0.4 readiness. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36O doctor patient adjunct read containment | Closed the doctor patient/client read-only adjunct slice of T0.3.36. Doctor patient appointments, unlinked appointments, exercise-calendar, program-activity, proactive-insights, and doctor client history routes now use selected workspace context, verify target patient membership through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, and use canonical `identity.userId`. The doctor patient card SSR preload now passes `workspace.organizationId` into appointments, program activity, and proactive insights reads. Downstream read ports now carry optional organization scope where the route/page needs selected workspace isolation: appointment history filters BE native and Rubitime mapping paths by org; LFK sessions, patient practice completions, program action log patient-wide reads, program item discussion doctor exercise-comment reads, and proactive insight SQL helpers apply the selected organization when provided. `pgPatientClinical.listLinkedAppointmentRecordIds` now requires a DB organization principal and filters clinical-visit links by exact principal org. Added focused route/helper/service tests and source-level audit coverage, including regression tests for `ProgramItemDiscussionService` preserving and validating `organizationId` through the DI-exposed service wrapper. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/patients/[userId]/appointments/route.test.ts src/app/api/doctor/patients/[userId]/appointments/unlinked/route.test.ts src/app/api/doctor/patients/[userId]/exercise-calendar/route.test.ts src/app/api/doctor/patients/[userId]/program-activity/route.test.ts src/app/api/doctor/patients/[userId]/proactive-insights/route.test.ts src/app/api/doctor/clients/[userId]/history/route.test.ts src/app/app/doctor/patients/loadDoctorPatientProgramActivity.test.ts src/modules/program-item-discussion/service.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts --reporter verbose` (9 files / 80 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/page/helper/module/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audits: first audit found appointment/activity/insight downstream org leakage; fixed by pushing org scope into read ports/repos. Second audit found the DI-exposed `ProgramItemDiscussionService` dropped `organizationId`; fixed and covered with service regression tests. Final re-audit reported no findings; its P3 caveat for invalid `organizationId` test coverage was closed in the same slice. | T0.3.36O only. Legacy `appointment_records` rows without BE native or Rubitime mapping are not surfaced in selected-workspace appointment reads when an org is supplied; this is intentional fail-closed behavior for SaaS containment. Org parameters are optional for legacy/patient flows without selected doctor workspace. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36N doctor global lifecycle containment | Closed the doctor/admin global lifecycle containment slice of T0.3.36 for email-change and permanent-delete. Admin email-change GET/POST now uses selected workspace context, verifies target patient membership through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, uses canonical `identity.userId`, and runs challenge start/read under `withDoctorWorkspacePrincipal`. Doctor client permanent-delete now requires both `requireAdminModeSession` and selected workspace context, verifies target patient membership before role/global identity/purge checks, and purges canonical `targetUserId`. Fixed `requireAdminModeSession` itself so it now rejects `session.adminMode !== true` instead of checking only `role === admin`. Added focused route tests, direct admin-mode guard tests, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/modules/auth/requireAdminMode.test.ts src/app/api/doctor/patients/[userId]/email-change/email-change.routes.test.ts src/app/api/doctor/clients/[userId]/permanent-delete/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (4 files / 66 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/helper/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit after fix: initial audit found real P1 admin-mode guard bug plus missing direct guard test; both fixed and re-audited with no findings. | T0.3.36N only. Email-change remains global account email state by product design, but route-level containment now requires selected workspace membership before challenge operations. Permanent purge remains a global destructive account operation, now bounded by adminMode + selected workspace membership. No read-only adjunct routes (`appointments`, `program-activity`, `exercise-calendar`, `proactive-insights`, `history`) and no full residual sweep closure yet. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36M doctor payments/acquiring principal | Closed the doctor patient payments/acquiring/finance preload slice of T0.3.36. Doctor patient cash payments, payment timeline, and acquiring-charge routes now use selected workspace context, verify target patient membership through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, use canonical `identity.userId`, and wrap patient-payment reads/writes in `withDoctorWorkspacePrincipal`. The acquiring route now verifies membership before provider charge creation, so an outside-workspace request cannot initiate a payment intent. `pgPatientPayments.listPayments` now fails closed without a DB organization principal and filters by exact `organization_id`; payment status updates now bind the update predicate to the organization. The doctor patient card SSR finance preload now reads patient payments under workspace principal and uses `workspace.organizationId` for booking payment history and packages instead of default organization lookup. Added focused route tests, repo-level principal tests, service webhook-status guard test coverage, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/patients/[userId]/payments/route.test.ts src/app/api/doctor/patients/[userId]/payment-timeline/route.test.ts src/app/api/doctor/patients/[userId]/acquiring-charge/route.test.ts src/infra/repos/pgPatientPayments.principal.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (5 files / 55 tests), `pnpm --dir apps/webapp exec vitest run src/modules/patient-payments/service.test.ts` (1 file / 2 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/page/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: no findings. | T0.3.36M only. Provider webhook/provider-id path remains provider-based and was not forced to require a doctor workspace principal; this preserves existing webhook semantics. In-memory patient payments remain simple and not principal-enforced; production pg repo and doctor routes are covered. No email-change, permanent-delete, or other adjacent global lifecycle hardening; those remain separate follow-up slices before T0.4 readiness. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36L doctor diagnosis catalog principal | Closed the doctor diagnosis catalog slice of T0.3.36. Doctor diagnosis catalog search/create route now uses `requireDoctorWorkspaceApiContext`, verifies the target patient through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, and wraps catalog search/create in `withDoctorWorkspacePrincipal`. `pgPatientClinical.searchDiagnosisCatalog` now fails closed without a DB organization principal and filters suggestions by exact principal `organization_id`. `pgPatientClinical.createDiagnosisCatalogEntry` now fails closed without a principal, runs through a principal-aware Drizzle mutation transaction, and stamps inserted catalog rows with the selected workspace organization. Added focused route tests, repo-level principal tests, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/patients/[userId]/diagnosis-catalog/route.test.ts src/infra/repos/pgPatientClinical.diagnosisCatalog.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (3 files / 47 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: no findings. Auditor noted a repo-level DB principal test gap; closed in the same slice with `pgPatientClinical.diagnosisCatalog.test.ts`. | T0.3.36L only. No payments/acquiring, email-change, permanent-delete, or other adjacent global lifecycle hardening; those remain separate follow-up slices before T0.4 readiness. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36K doctor patient files/media principal | Closed the doctor patient files/media slice of T0.3.36. Doctor files collection and item routes now use `requireDoctorWorkspaceApiContext`, verify the target patient through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, use canonical `identity.userId`, and wrap list/get/create/link/rename operations in `withDoctorWorkspacePrincipal`. Upload folder creation now happens only after selected-workspace membership verification and runs under the same workspace principal. The doctor patient card SSR preload now loads files under workspace principal. `pgPatientFiles` now fails closed without a DB organization principal, filters reads by exact `organization_id`, runs metadata/media dual inserts and mutations in principal-aware Drizzle transactions, stamps both `patient_files` and linked `media_files`, validates linked visits by org + patient, and rejects cross-org/cross-patient links. `pgClientMediaFolders` now applies current-org/legacy-NULL scope when ensuring/promoting client files folders and stamps new/promoted/renamed folders with the current org where a principal exists. `pgPatientClinical.listVisits` now filters visit-linked patient files by the current principal org. Added focused route tests, pg repo principal tests, updated PFI G3 tests, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/patients/[userId]/files/route.test.ts src/app/api/doctor/patients/[userId]/files/[fileId]/route.test.ts src/infra/repos/pgPatientFiles.principal.test.ts src/infra/repos/pgPatientFiles.g3.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (5 files / 57 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/page/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent exploration/audit: explorer identified folder helper principal gap; fixed. Auditor found P1 root-folder promotion could stamp another org's folder; fixed by adding org/NULL scope to promotion UPDATE predicate; re-audit no findings. | T0.3.36K only. Full per-org `media_folders` unique-index redesign remains a separate schema decision; this slice avoids route/repo principal leaks and cross-org mutation within current schema. No diagnosis catalog, payments/acquiring, email-change, or permanent-delete hardening; those remain separate slices. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36J doctor comorbidities principal | Closed the doctor patient comorbidities slice of T0.3.36. Doctor comorbidities collection and item routes now use `requireDoctorWorkspaceApiContext`, verify the target patient through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, use canonical `identity.userId`, and wrap list/add/edit/remove/restore operations in `withDoctorWorkspacePrincipal`. The doctor patient card SSR preload now loads active comorbidities under the selected workspace principal. `pgPatientComorbidities` now fails closed without a current DB organization principal, filters reads by exact principal `organization_id`, runs writes through principal-aware Drizzle transactions, stamps new rows with the current principal, and rejects cross-org existing rows with `organization_principal_mismatch`. Added focused route tests, pg repo tests, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/patients/[userId]/comorbidities/route.test.ts src/app/api/doctor/patients/[userId]/comorbidities/[comorbidityId]/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts src/infra/repos/pgPatientComorbidities.test.ts` (4 files / 50 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/page/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: initial audit found SSR comorbidity preload outside workspace principal and repo writes allowing principal-less execution; both fixed and re-audited with no findings. | T0.3.36J only. No files/media, diagnosis catalog, payments/acquiring, email-change, or permanent-delete hardening; those remain separate slices. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36I doctor clinical core principal | Closed the clinical core authorization/principal slice of T0.3.36. Doctor clinical aggregate, anamnesis, visit create/update, complaint update, diagnosis update, and diagnosis status routes now use `requireDoctorWorkspaceApiContext`, verify the target patient through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, use canonical `identity.userId`, and wrap clinical reads/writes in `withDoctorWorkspacePrincipal`. The SSR doctor patient card now resolves selected workspace membership before loading clinical state/visits/anamnesis and runs those clinical reads under the workspace principal. `pgPatientClinical` now filters principal-scoped clinical/anamnesis reads, stamps clinical/anamnesis writes with the current org principal, checks existing row organization before updates/status changes, binds diagnosis status history to patient + org, and validates visit `appointmentRecordId` using existing appointment attribution semantics (direct user, current phone without competing claim, or historical phone). Added focused route tests and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/patients/[userId]/clinical/route.test.ts src/app/api/doctor/patients/[userId]/anamnesis/route.test.ts src/app/api/doctor/patients/[userId]/visits/route.test.ts src/app/api/doctor/patients/[userId]/visits/[visitId]/route.test.ts src/app/api/doctor/patients/[userId]/complaints/[complaintId]/route.test.ts src/app/api/doctor/patients/[userId]/diagnoses/[diagnosisId]/route.test.ts src/app/api/doctor/patients/[userId]/diagnoses/[diagnosisId]/status/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (8 files / 46 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/page/repo/module/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: initial audit found missing `/clinical` + SSR clinical read principal, unbound diagnosis history, and overly strict appointment-record validation; all fixed and re-audited with no findings. | T0.3.36I only. No comorbidities, files/media, diagnosis catalog, payments/acquiring, email-change, or permanent-delete hardening; those remain separate slices. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36H doctor patient profile containment | Closed the bounded global patient profile authorization-containment slice of T0.3.36. Doctor patient header, FIO, and physical GET/PATCH routes now use `requireDoctorWorkspaceApiContext`, verify the target patient through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, use the returned canonical `identity.userId` for profile service operations, and wrap profile read/write service calls in `withDoctorWorkspacePrincipal`. Added focused route tests for workspace gate, outside-org 404, alias-to-canonical IDs, and principal-wrapped profile/FIO/physical operations; extended source-level audit coverage. Documented that these fields still live on global `platform_users`. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/patients/[userId]/route.test.ts src/app/api/doctor/patients/[userId]/fio/route.test.ts src/app/api/doctor/patients/[userId]/physical/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (4 files / 42 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/test/audit files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent exploration: profile/FIO/physical are global `platform_users` product semantics; route-level containment is safe, true org-scoped profile storage needs a separate decision. PASS subagent audit: initial audit found canonical ID propagation gap; fixed by using `identity.userId` and re-audit reported no findings. | T0.3.36H only. No DB migration, no org-scoped replacement for global `platform_users` profile fields, no email-change or permanent-delete handling, no clinical core/comorbidity/files/payments hardening, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36G doctor supplementary contacts containment | Closed the bounded supplementary-contacts authorization-containment slice of T0.3.36. Doctor supplementary contact GET/POST/DELETE routes now use `requireDoctorWorkspaceApiContext`, verify the target patient through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, and wrap contact list/upsert/delete service calls in `withDoctorWorkspacePrincipal`. Added focused route tests for workspace gate, outside-org 404, and principal-wrapped GET/POST/DELETE operations; extended source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/clients/[userId]/supplementary-contacts/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (2 files / 37 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/test/audit files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent exploration: route-only containment is bounded; full RLS/schema fix needs a separate decision because `platform_user_contacts` has no `organization_id` while P0.8.6 policy text references one. PASS subagent audit: no findings. | T0.3.36G only. No schema/repo migration for `platform_user_contacts.organization_id`, no change to upsert source-conflict semantics, no CRUD audit trail addition, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. P2 adjacent profile/clinical writes remain separate #626 follow-up slices. |
| 2026-07-09 | R1 / T0.3.36F doctor client block/archive containment | Closed the bounded block/archive authorization-containment slice of T0.3.36. Doctor `/api/doctor/clients/[userId]/block` and `/archive` now use `requireDoctorWorkspaceApiContext` and verify the target patient with `getClientIdentityForOrganization(..., gate.ctx.organizationId)` before writing the existing global `platform_users.is_blocked` / `is_archived` flags. Added a missing block route test, updated archive route tests, and extended source-level audit coverage. Documented the product/schema decision that these flags are still global account lifecycle state; a true org-scoped archive/block model requires a separate migration/design decision. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/clients/[userId]/block/route.test.ts src/app/api/doctor/clients/[userId]/archive/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts` (3 files / 36 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/test/audit files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent exploration: block/archive cannot be made truly tenant-scoped with existing storage; containment path is bounded. PASS subagent audit: no findings. | T0.3.36F only. No DB migration, no org-scoped replacement for global block/archive flags, no admin archive behavior change, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. Supplementary contacts and P2 adjacent profile/clinical writes remain separate #626 follow-up slices. |
| 2026-07-09 | R1 / T0.3.36E doctor client-card profile/schedule/symptom principal | Closed the client-card booking-profile, warmup-schedule, and symptom-tracking slice of T0.3.36. Doctor booking-profile GET/PATCH, warmup schedule GET/PATCH, and symptom-tracking POST now use `requireDoctorWorkspaceApiContext`, verify the patient through `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, and run mutating work under `withDoctorWorkspacePrincipal`. Booking profile now uses the selected workspace organization instead of default organization fallback. `pgReminderRules` now applies current DB principal org filtering for user/category reads, preserves legacy `NULL organization_id` rows, and claims legacy rows to the active org on scoped updates so another org's reminder row cannot be read/updated through patient membership alone. `pgSymptomDiary.createTracking` now stamps `symptom_trackings.organization_id` from the current DB principal. Added route, repo, and source-level audit coverage for these contracts. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp exec vitest run src/app/api/doctor/clients/[userId]/booking-profile/route.test.ts src/app/api/doctor/clients/[userId]/warmup-schedule/route.test.ts src/app/api/doctor/clients/[userId]/symptom-trackings/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts src/infra/repos/pgReminderRules.test.ts src/infra/repos/pgSymptomDiary.repo.test.ts` (6 files / 49 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: initial audit found reminder rule org-scope and symptom tracking org-stamping gaps; both fixed. Re-audit reported no findings. | T0.3.36E only. Client block/archive, supplementary contacts, and P2 adjacent profile/clinical writes remain separate #626 follow-up slices. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36D doctor online-intake principal | Closed the online-intake slice of T0.3.36. Doctor online-intake list/detail/stats/status/reply routes now use `requireDoctorWorkspaceApiContext` and run service work under `withDoctorWorkspacePrincipal`. Status/reply reject requests outside the selected workspace; reply also verifies the request patient via `getClientIdentityForOrganization` before support conversation/message writes. `pgOnlineIntake` now maps `organizationId`, filters request/list/detail/stats reads by current DB principal, stamps request/answer/attachment/status-history rows from active `org_enrollments` or current principal, filters child rows by principal, and rejects status changes when the request org mismatches the active principal. `inMemoryOnlineIntake` mirrors principal stamping/filtering/mismatch behavior. Aligned the generated schema status CHECK mirror and doctor list filter with existing `booked`/`rejected` statuses from migration `088`. Added route, service/in-memory, pg transaction, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp test src/app/api/doctor/online-intake/route.test.ts src/app/api/doctor/online-intake/[id]/route.test.ts src/app/api/doctor/online-intake/stats/route.test.ts src/app/api/doctor/online-intake/[id]/reply/route.test.ts src/app/api/doctor/online-intake/[id]/status/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts src/infra/repos/pgOnlineIntake.advisoryLock.test.ts src/modules/online-intake/service.test.ts` (8 files / 74 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/type/test/schema files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audit: no findings. Auditor additionally tried `pnpm --dir apps/webapp run db:verify-public-table-count`; it failed on an existing broad dev-DB/schema export count mismatch (`189` DB public base tables vs `133` schema exports), not treated as a T0.3.36D isolation finding. | T0.3.36D only. Client booking-profile/warmup schedule/symptom tracking, client block/archive, supplementary contacts, and P2 adjacent profile/clinical writes remain separate #626 follow-up slices. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36C doctor generic comments principal | Closed the generic doctor comments slice of T0.3.36 for the currently used `program_instance` path. `/api/doctor/comments` now uses `requireDoctorWorkspaceApiContext`, rejects unsupported generic target types explicitly, verifies the target treatment-program instance belongs to the selected workspace before list/create, and wraps comment reads/writes in `withDoctorWorkspacePrincipal`. `/api/doctor/comments/[id]` now uses the workspace gate, rejects cross-org and legacy `NULL organization_id` comments for GET/PATCH/DELETE, and wraps mutations in the workspace principal. `pgComments` now maps `organizationId`, filters `listByTarget` by current DB principal, stamps creates inside principal-aware transactions, and rejects update/delete org-principal mismatches. `inMemoryComments` mirrors principal stamping/filtering/mismatch behavior. Added focused route, service/in-memory, pg repo, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp test src/app/api/doctor/comments/route.test.ts src/app/api/doctor/comments/[id]/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts src/modules/comments/service.test.ts src/infra/repos/pgComments.test.ts` (5 files / 50 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/type/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audits: initial explorer found missing parent target guard and permissive legacy NULL handling; both fixed. Final auditor reported no findings, residual direct-pg-test risk was closed with `pgComments.test.ts`, and re-audit reported no findings. | T0.3.36C only. Generic comments beyond the currently used `program_instance` target remain explicitly unsupported at this doctor route until a full 9-target resolver is implemented. Online intake, warmup schedule, symptom tracking, block/archive/booking-profile/supplementary contacts remain separate #626 follow-up slices. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36B doctor support chat/messages principal | Closed the support chat/messages slice of T0.3.36. Doctor message GET/POST/read and conversation ensure now use `requireDoctorWorkspaceApiContext`; patient-bound ensure uses `getClientIdentityForOrganization`; message GET/POST/read verify conversation ownership against the selected workspace before returning or mutating data. Added a legacy claim path for pre-SaaS `support_conversations.organization_id IS NULL` rows: after route-level workspace verification and before principal-scoped writes, `claimLegacyConversationForOrganization` stamps the conversation and existing messages to the selected org, preserving channel-only legacy conversations while making future RLS-visible writes deterministic. `pgSupportCommunication` now maps optional org metadata, runs webapp support ensure/append/read writes through principal-aware transactions, stamps/checks conversation/message `organization_id`, and rejects principal/parent org mismatches. Added route, repo, in-memory, and source-level audit coverage. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm --dir apps/webapp test src/app/api/doctor/messages/[conversationId]/route.test.ts src/app/api/doctor/messages/[conversationId]/read/route.test.ts src/app/api/doctor/messages/conversations/ensure/route.test.ts src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts src/infra/repos/pgSupportCommunication.repo.test.ts src/infra/repos/pgSupportCommunication.test.ts src/modules/messaging/doctorSupportMessagingService.test.ts src/modules/messaging/patientMessagingService.test.ts` (8 files / 84 tests), `pnpm --dir apps/webapp typecheck`, targeted `eslint` on changed route/repo/test files, `pnpm run check:saas-db-regression` (including P0.13.3 smoke: 5 files / 25 tests), and `git diff --check`. PASS subagent audits: initial route/repo audit requirements incorporated; post-fix audit found GET read-scope + legacy RLS claim gaps; both fixed; final re-audit returned no findings. | T0.3.36B only. Generic comments, online intake, warmup schedule, symptom tracking, block/archive/booking-profile/supplementary contacts remain separate #626 follow-up slices. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.36A doctor support direct writes principal | Closed the first bounded slice of T0.3.36: doctor notes, patient support settings, and specialist tasks. Mutating doctor routes now enter `requireDoctorWorkspaceApiContext`, patient-bound writes use `getClientIdentityForOrganization(..., gate.ctx.organizationId)`, and task PATCH/DELETE/complete reject tasks outside the selected workspace before mutation. `pgDoctorNotes`, `pgDoctorPatientSupport`, and `pgSpecialistTasks` now run scoped writes inside principal-aware Drizzle transactions and stamp/check `organization_id` from the active principal or existing row. Added source-level audit coverage for the direct support/task principal cutover and updated focused route/repo tests. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm run check:saas-db-regression && pnpm --dir apps/webapp exec vitest run src/app/api/doctor/supportWorkspacePrincipal.audit.test.ts src/app/api/doctor/clients/[userId]/support-settings/route.test.ts src/app/api/doctor/clients/[userId]/tasks/route.test.ts src/infra/repos/pgDoctorNotes.test.ts src/modules/doctor-clients/service.test.ts src/infra/repos/inMemoryDoctorClients.supportFilter.test.ts && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint <changed route/repo/type/test files> && git diff --check` (6 focused test files / 33 tests, plus P0.13.3 smoke inside SaaS regression: 5 files / 25 tests). PASS subagent audit: route/IDOR auditor no findings; repo/DB-principal auditor no findings. | T0.3.36A only. Support chat/messages, generic comments, online intake, warmup schedule, symptom tracking, block/archive/booking-profile/supplementary contacts remain separate #626 follow-up slices. Read-only GET routes in this slice remain on prior role-only behavior. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-09 | R1 / T0.3.35 doctor treatment-program instance principal | Applied doctor workspace DB principal wrapping to residual treatment-program instance/editor write routes: assignment/create, instance patch, editor batch, stage/group/item create/update/delete/reorder, freeform/LFK/test-set expansion, discussion media delete/read marker/program-note reply, test attempt accept, and test result override. Added `withDoctorWorkspacePrincipal` app-layer helper. Converted the related treatment-program instance/events/action-log/discussion/test-attempt repos so write methods run inside principal-aware Drizzle transactions and new scoped inserts stamp `organization_id` only after active principal + parent orgs agree. Added org-scoped patient identity lookup for assignment/create, route ownership guards for existing-instance mutators, and parent consistency validation for `program_action_log` instance/stage/item writes. Added source-level audit coverage for mutating route principal wrappers, org ownership guards, principal-aware transaction setup, no direct repo `db.insert/update/delete` writes in the closed repo set, and action-log parent validation; updated existing route tests to mock the workspace principal boundary. | PASS targeted gate run directly per owner instruction allowing direct checks: `pnpm run check:saas-db-regression && pnpm --dir apps/webapp exec vitest run src/app/api/doctor/treatment-program-instances/doctorWorkspacePrincipal.audit.test.ts <7 doctor treatment-program-instances route test files> --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint <changed route/helper/repo/test files> && git diff --check` (8 focused test files / 74 tests, plus P0.13.3 smoke inside SaaS regression: 5 files / 25 tests). PASS post-fix subagent re-audit: route/IDOR auditor no findings; repo/DB-principal auditor no findings. | T0.3.35 only. Read-only treatment-program routes remain role-only in this slice, with prior missing-session `401` vs non-doctor `403` behavior preserved. Patient-facing route principal cutover for discussion/test/action-log remains separate patient-resource work, but touched repos now use transaction-backed writes so doctor principal can apply. No DB migration, no live DB writes, no RLS policy change, no runtime role flip, no prod/env access, no external channel replay, no full CI yet. |
| 2026-07-08 | R1 / Phase 0 integration checkpoint | Confirmed the executable Phase 0 micro-stage spine ends at P0.13.3. Ran the final integration gate after P0.13.3 and fixed three stale webapp test expectations to include the org-aware `system_settings` upsert context `{ organizationId: null }`; runtime behavior already preserved the saved SMTP/WebPush secrets. Updated the initiative README from pre-start/decomposed status to Phase 0 micro-stage spine complete. | Initial direct `pnpm run ci` failed only in the three stale settings expectations. PASS focused fix: `pnpm --dir apps/webapp exec vitest run src/modules/system-settings/service.smtpOutboundMerge.test.ts src/modules/system-settings/service.webPushVapidMerge.test.ts src/app/api/admin/settings/route.test.ts --reporter verbose` (3 files / 92 tests). PASS final gate, run directly after explicit owner instruction to remove the wrapper/limiter: `pnpm run ci` (lint, typecheck, HLS helper sync, integrator tests 179 files / 1328 tests, webapp tests 1245 files / 6945 tests, media-worker tests 10 files / 32 tests, package/integrator/webapp builds, SaaS DB regression including P0.13.3 app smoke 5 files / 25 tests, registry prod audit). Known existing Turbopack NFT trace warning from `apps/webapp/next.config.ts` / media preview worker remained non-fatal. | Integration checkpoint only. No new SaaS micro-stage after P0.13.3, no runtime roles/env/grants, no migrations, no production/dev/test application DB smoke, no `/opt/env`, no external channels, no merge, no push. Tests were run directly per explicit owner instruction for this pass. |
| 2026-07-08 | R1 / P0.13.3 app-level dormant smoke | Added `check-p0-13-app-dormant-smoke.mjs` and wired it into `check:saas-db-regression` to pin and execute the existing in-process app smoke surface: App Router RSC doctor/patient pages, doctor in-process service/component smoke, patient playback smoke, and dev-bypass development-only contracts. The checker runs the existing in-process Vitest files rather than a live dev server, with `DATABASE_URL=`/`USE_REAL_DATABASE=0` to avoid dev/test application DB access. | PASS focused gate, run directly after explicit owner instruction to remove the wrapper/limiter: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-app-dormant-smoke.mjs --self-test && pnpm run check:saas-db-regression && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-app-dormant-smoke.mjs scripts/check-saas-db-regression.mjs && git diff --check`. `check:saas-db-regression` now executes the five P0.13.3 Vitest smoke files and reported 5 files / 25 tests passed. PASS sidecar audit after fix: no blocking findings. | P0.13.3 only. No live dev server, no browser screenshots, no production/dev/test application DB access, no runtime roles/env/grants, no migrations, no app routes/services/UI, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.13.2 DB-level isolation assertions | Added scratch-only DB isolation smoke `smoke-p0-13-db-isolation.mjs` and static guard `check-p0-13-db-isolation.mjs`, wired the guard into `check:saas-db-regression`. The smoke uses the P0.13.1 synthetic fixture seed in a disposable `bcb_saas_p0_13_2_scratch_*` DB, creates a non-owner `NOBYPASSRLS` app role, enables/FORCEs RLS on representative direct-org, FK-path, patient-path, BOOTSTRAP, integrator, INFRA, TELEMETRY, and LEGACY tables, and asserts fail-closed missing/empty org, correct/wrong org isolation, same-org patient wall, bootstrap global readability, INFRA/TELEMETRY readable treatment, and LEGACY deny treatment. | PASS focused/final gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-db-isolation.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-db-isolation.mjs --self-test && pnpm run check:saas-db-regression && node docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-13-db-isolation.mjs && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-db-isolation.mjs docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-13-db-isolation.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. | P0.13.2 only. No app-level dormant smoke (P0.13.3), no dev server, no runtime role flip, no production/dev/test application DB access, no migrations, no app routes/services/UI, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.13.1 synthetic isolation fixture factory | Added deterministic P0.13 synthetic fixture IDs for two organizations, two same-org patients, one cross-org patient, and representative staff/patient principals. Added `p0-13-synthetic-fixtures.mjs` as the reusable fixture factory plus `scope-derivation/p0-13-synthetic-fixtures.tsv` as the auditable manifest. Coverage includes direct-org rows, P0.8.4 public FK-path rows, P0.8.4 denorm/path rows with patient-wall pairs, BOOTSTRAP `system_settings` global/org rows, and P0.8.5 integrator direct/denorm representatives. The generated seed SQL inserts into existing migrated scratch `public`/`integrator` target tables, writes a disposable `p0_13_fixture` manifest schema, and refuses non-`bcb_saas_*`/non-scratch DB names plus `bcb_webapp_dev/prod/test`; it does not drop or replace real target tables. The smoke script separately creates a minimal compatibility schema only inside its disposable DB before applying the same seed. Added `check-p0-13-synthetic-fixtures.mjs`, scratch smoke `smoke-p0-13-synthetic-fixtures.mjs`, and wired the checker into `check:saas-db-regression`. | PASS focused/final gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-synthetic-fixtures.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-synthetic-fixtures.mjs --self-test && pnpm run check:saas-db-regression && node docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-13-synthetic-fixtures.mjs && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/p0-13-synthetic-fixtures.mjs docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-13-synthetic-fixtures.mjs docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-13-synthetic-fixtures.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. PASS sidecar audit after fixes: no blocking findings. | P0.13.1 only. No non-bypass role assertions (P0.13.2), no app-level dormant smoke (P0.13.3), no real app schema writes, no migrations, no runtime roles/env/grants, no app routes/services/UI, no live dev/prod/test DB access, no `/opt/env`, no external channels, no full CI, no push. Scratch smoke used only a disposable `bcb_saas_p0_13_1_scratch_*` DB and dropped it. |
| 2026-07-08 | R1 / P0.12.2 JSON payload / queue PII scan | Added `scope-derivation/p0-12-json-payload-columns.tsv` documenting payload-like JSON/text columns across BOOTSTRAP, SCOPED, INFRA, and LEGACY tables without printing sample values. SCOPED payloads remain protected by materialized `organization_id`; BOOTSTRAP `system_settings.value_json` is covered by P0.11 org-aware global/override semantics; LEGACY Rubitime/appointment payloads remain frozen. INFRA queue/outbox rows (`integrator_push_outbox`, `projection_outbox`, `outgoing_delivery_queue`, `delivery_attempt_logs`) are explicitly classified as transient operational payloads with retention/scrub decisions rather than automatic re-tier. Added `check-p0-12-json-payloads.mjs` and wired it into `check:saas-db-regression`; the guard checks exact coverage, tier parity, no PII-looking sample values, and retention/scrub decisions for INFRA/TELEMETRY user-bearing payloads. | PASS focused gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-12-json-payloads.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-12-json-payloads.mjs --self-test && pnpm run check:saas-db-regression && git diff --check"`. | P0.12.2 only. No RLS policy migration, no queue/outbox schema changes, no payload scrubbing implementation, no app route/service/UI changes, no live dev/prod/test DB access, no PII samples, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.12.1 polymorphic reference resolver scan | Added `scope-derivation/p0-12-polymorphic-references.tsv` covering persisted discriminator/reference families: `comments.target_type/target_id`, `patient_home_block_items.target_type/target_ref`, template/instance treatment-program `item_type/item_ref_id`, `material_ratings.target_kind/target_id`, and `treatment_program_events.target_type/target_id`. Each target value is classified and has a resolver path to `organization_id` for SCOPED targets. Documented `admin_audit_log.target_id` as a non-polymorphic text pointer with row ownership already materialized. Added `check-p0-12-polymorphic-references.mjs` and wired it into `check:saas-db-regression`; the guard checks exact coverage, schema CHECK parity, resolved SCOPED organization paths, and no DB FK on polymorphic `item_ref_id`. | PASS focused gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-12-polymorphic-references.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-12-polymorphic-references.mjs --self-test && pnpm run check:saas-db-regression && git diff --check"`. | P0.12.1 only. No JSON payload/queue PII scan (P0.12.2), no RLS policy migration for `public.comments`, no database FK on `item_ref_id`, no runtime roles/env/grants, no app route/service/UI changes, no live dev/prod/test DB access, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.11.4 system_settings UI/rules/docs | Updated `.cursor` rules, `AGENTS.md`, `docs/ARCHITECTURE/CONFIGURATION_ENV_VS_DATABASE.md`, `docs/ARCHITECTURE/DATABASE_UNIFIED_POSTGRES.md`, and SaaS-aware rules to describe org-aware `system_settings` identity `(key, scope, organization_id)`, global `organization_id IS NULL` defaults, org-specific overrides, and mirror lockstep. Documented that current admin Settings UI writes global rows unless a future flow explicitly passes organization context. Added `check-p0-11-system-settings-docs-rules.mjs` and wired it into `check:saas-db-regression`. | PASS focused/final gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-docs-rules.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-docs-rules.mjs --self-test && pnpm run check:saas-db-regression && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-docs-rules.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. PASS sidecar read-only audit: no issues found, no files edited. | P0.11.4 only. No Settings UI implementation changes, no `ALLOWED_KEYS` changes, no route/service/repo behavior changes, no migrations, no runtime roles/env/grants, no dev/prod/test application DB access, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.11.3 system_settings org-aware write path | Added optional organization write context to the webapp `SystemSettingsPort`, pg/in-memory repos, `updateSetting`, and admin batch persistence. Global writes still target the `organization_id IS NULL` partial unique row; org writes target `(key, scope, organization_id)` rows. Mirror sync now carries `organizationId` in signed webapp-to-integrator payloads and outbox payloads, with org-aware idempotency keys. The integrator settings sync route upserts global or org-scoped mirror rows with matching partial conflict targets. Added nullable `organization_id` to `public.system_settings_audit` with FK/index, plus P0.11.3 static guard and disposable scratch smoke. | PASS static/smoke regression gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-write-path.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-write-path.mjs --self-test && node docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-11-system-settings-write-path.mjs && pnpm run check:saas-db-regression && git diff --check"`. PASS final gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-11-system-settings-write-path.mjs && pnpm --dir apps/webapp exec vitest run src/infra/repos/pgSystemSettings.repo.test.ts src/infra/repos/pgSystemSettings.audit.test.ts src/modules/system-settings/service.test.ts src/modules/system-settings/syncToIntegrator.test.ts --reporter verbose && pnpm --dir apps/integrator exec vitest run src/integrations/bersoncare/settingsSyncRoute.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/integrator typecheck && <focused eslint webapp/integrator/scripts> && git diff --check"` (43 focused tests). | P0.11.3 only. No Settings UI/scope selector, no `ALLOWED_KEYS` changes, no second route-level mirror sync, no runtime roles/env/grants, no non-system-settings app behavior changes, no dev/prod/test application DB access, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.11.2 system_settings org-aware read path | Added optional organization read context for webapp `SystemSettingsPort`, service read methods, global helper reads, and integrator `public.system_settings` accessor. When `organizationId` is present, SQL reads org rows first and falls back to the global `organization_id IS NULL` row; `getByScope` merges one row per key with org override precedence. Existing callers remain global-only by default. The in-memory system settings port accepts the same read option for interface parity. Marked media-worker direct settings reads as documented global-only by adding explicit `organization_id IS NULL`. Added `check-p0-11-system-settings-read-path.mjs` to the SaaS DB regression suite and focused tests for webapp fallback SQL, integrator fallback SQL, and media-worker global-only predicates. | PASS targeted gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-storage.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-read-path.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-read-path.mjs --self-test && pnpm --dir apps/webapp exec vitest run src/infra/repos/pgSystemSettings.repo.test.ts src/infra/repos/pgSystemSettings.audit.test.ts src/modules/system-settings/service.test.ts --reporter verbose && pnpm --dir apps/integrator exec vitest run src/infra/db/publicSystemSettings.test.ts src/integrations/bersoncare/settingsSyncRoute.test.ts src/infra/db/repos/notifTemplatePort.test.ts --reporter verbose && pnpm --dir apps/media-worker exec vitest run src/pipelineEnabled.test.ts src/watermarkEnabled.test.ts --reporter verbose && git diff --check"` (60 tests). PASS final gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && <same focused vitest set> && pnpm --dir apps/webapp typecheck && pnpm --dir apps/integrator typecheck && pnpm --dir apps/media-worker typecheck && <focused eslint webapp/integrator/scripts/media-worker> && git diff --check"`. | P0.11.2 only. No org-aware write/update path, no mirror payload org semantics, no Settings UI/scope selector, no `ALLOWED_KEYS` changes, no runtime roles/env/grants, no app route behavior changes, no dev/prod/test application DB access, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.11.1 system_settings storage shape | Added nullable `organization_id` storage shape for both `public.system_settings` and `integrator.system_settings` by inserting the DDL before the existing P0.8.6 BOOTSTRAP hybrid RLS policies in `0163_p0_8_6_bootstrap_hybrid_rls.sql`; this fixes the migration-chain ordering issue where the P0.8.6 policies already referenced `organization_id`. Replaced legacy `(key, scope)` PK semantics with partial unique indexes for global rows (`organization_id IS NULL`) and org rows (`organization_id IS NOT NULL`), added FKs to `public.be_organizations`, updated the Drizzle schema column/index shape, and kept existing rows global via nullable `NULL`. Preserved current global settings behavior by explicitly filtering current read paths to `organization_id IS NULL` and targeting global partial unique indexes in current upsert paths. Added static guard `check-p0-11-system-settings-storage.mjs`, scratch smoke `smoke-p0-11-system-settings-storage.mjs`, and focused test assertions for the global partial-index conflict target. | PASS static/focused gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-storage.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-storage.mjs --self-test && pnpm run check:saas-db-regression && git diff --check"`. PASS scratch smoke on disposable `bcb_saas_p0_11_1_scratch_*` DB: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-11-system-settings-storage.mjs"`; scratch DB dropped. PASS focused runtime tests: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-storage.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-11-system-settings-storage.mjs --self-test && pnpm --dir apps/webapp exec vitest run src/infra/repos/pgSystemSettings.repo.test.ts src/infra/repos/pgSystemSettings.audit.test.ts --reporter verbose && pnpm --dir apps/integrator exec vitest run src/integrations/bersoncare/settingsSyncRoute.test.ts src/infra/db/repos/notifTemplatePort.test.ts --reporter verbose && git diff --check"` (34 tests). | P0.11.1 only. No org-aware override API/read precedence, no UI/settings scope selector, no `ALLOWED_KEYS` changes, no second route-level sync call, no runtime roles/env/grants, no app routes/services/UI, no dev/prod/test application DB access, no `/opt/env`, no external channels, no full CI, no push. The TypeScript Drizzle monolith reflects column/index shape; the real FK is in SQL migration because `be_organizations` lives in a module that imports the monolith, so importing it back would create a schema cycle. |
| 2026-07-08 | R1 / P0.10.3 scoped tenant semantics invariant | Added static CI guard `check-p0-10-scoped-tenant-semantics.mjs` and wired it into `check:saas-db-regression`. The guard proves all 155 SCOPED descriptors have approved tenant semantics (`organization_id`, `public.be_organizations.id` self-scope, declared FK path, denorm/materialized org path, or P0.12.1-deferred polymorphic resolver), verifies all 14 P0.4 backfill batches expose no-NULL assertions for their 111 materialized-org tables, and verifies the two P0.4.BE FK-path package item declarations remain SCOPED and use parent/cross-check `organization_id`. Added in-memory `--self-test` cases for missing org column, missing FK-path metadata, and missing no-NULL assertion coverage. | PASS focused/final gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-scoped-tenant-semantics.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-scoped-tenant-semantics.mjs --self-test && pnpm run check:saas-db-regression && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-scoped-tenant-semantics.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. | P0.10.3 only. Static artifacts and migration SQL text only; no live dev/prod/test DB access, no runtime roles/env/grants, no app routes/services/UI, no migration, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.10.2 user-reference tier guard | Added artifact-based CI guard `check-p0-10-user-reference-tier-guard.mjs` and wired it into `check:saas-db-regression`. The guard reads static FK/soft-ref artifacts (`fk-edges.tsv`, `method-columns.tsv`, `all-218-signals.tsv`) plus P0.4 actor/user scoped-source rows, then fails if any `platform_users` reference is tiered INFRA/TELEMETRY or lacks allowed-tier documentation. Current guarded surface is 114 user-ref tables: SCOPED=92, BOOTSTRAP=20, LEGACY=2, INFRA/TELEMETRY=0. Added an in-memory `--self-test` that reproduces the r7 prior leak class by mutating `public.admin_audit_log`, `public.broadcast_audit`, and `public.content_section_slug_history` into unsafe tiers / no-longer-SCOPED states. | PASS focused/final gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-user-reference-tier-guard.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-user-reference-tier-guard.mjs --self-test && pnpm run check:saas-db-regression && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-user-reference-tier-guard.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. | P0.10.2 only. Static artifacts only; no live dev/prod/test DB access, no P0.10.3 null/tenant-semantics checks, no runtime roles/env/grants, no app routes/services/UI, no migration, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.10.1 tier completeness CI invariant | Added artifact-based CI guard `check-p0-10-tier-completeness.mjs` and wired it into `check:saas-db-regression`. The guard proves `tiers-218.tsv` has 219 unique tier rows matching frozen schema snapshot `all-218-signals.tsv` exactly once, pins expected tier counts, derives `needs-orgid-FINAL.txt` as exactly the `111` SCOPED non-`public.be_*` materialization tables, requires P0.4 batches to cover that set exactly, and keeps the two P0.4.BE FK-path tables outside `needs-orgid-FINAL.txt` while still SCOPED. Added an in-memory `--self-test` for duplicate tier rows, snapshot mismatch, needs-org mismatch, and accidental P0.4.BE inclusion. | PASS preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS focused/final gate: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-tier-completeness.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-tier-completeness.mjs --self-test && pnpm run check:saas-db-regression && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-10-tier-completeness.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. | P0.10.1 only. Static artifacts only; no live dev/prod/test DB access, no P0.10.2 FK/soft-ref guard, no P0.10.3 null/tenant-semantics checks, no runtime roles/env/grants, no app routes/services/UI, no migration, no `/opt/env`, no external channels, no full CI, no push. |
| 2026-07-08 | R1 / P0.9.1 default-deny enforce descriptors | Added enforce-mode descriptor metadata on top of the P0.8 RLS descriptor model. `p0-9-enforce-descriptors.mjs` resolves all 219 canonical descriptors into fail-closed enforce actions: SCOPED org match (`152`), SCOPED FK-path match (`2`), deferred polymorphic default-deny (`1`, `public.comments`), BOOTSTRAP hybrid (`4`), BOOTSTRAP global readable (`20`), INFRA/TELEMETRY explicit global (`24`), and LEGACY frozen deny (`16`). Missing/unknown descriptors also resolve to predicate `false`. Added deterministic checker `check-p0-9-enforce-descriptors.mjs`, wired it into `check:saas-db-regression`, and added scratch smoke `smoke-p0-9-default-deny-enforce.mjs` proving enforce semantics on a disposable DB. | PASS preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS focused gate: `bash /home/dev/orch/run-tests.sh "pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/p0-9-enforce-descriptors.mjs docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-9-enforce-descriptors.mjs docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-9-default-deny-enforce.mjs scripts/check-saas-db-regression.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-9-enforce-descriptors.mjs && pnpm run check:saas-db-regression && git diff --check"`. PASS scratch smoke on disposable `bcb_saas_p0_9_1_scratch_1783535995_170296` DB: `bash /home/dev/orch/run-tests.sh "P0_9_1_PSQL_AS_POSTGRES=1 SCRATCH_DATABASE_URL=postgresql:///bcb_saas_p0_9_1_scratch_1783535995_170296 node docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-9-default-deny-enforce.mjs"`; scratch DB dropped. Earlier smoke attempts failed before policy validation due cwd/permission mismatch when running node as `postgres`; fixed by keeping node as `dev` and allowing only `psql` to run through `sudo -n -u postgres`. | P0.9.1 only. Enforce descriptors are dormant/non-production proof artifacts; no production enforce activation, no runtime role switch, no app routes/services/UI, no env/grant changes, no Drizzle migration, no dev/prod/test application DB writes, no `/opt/env`, no external channels, no full CI, no push. P0.8 dormant policy migrations remain unchanged. |
| 2026-07-08 | R1 / P0.8.7 explicit exemptions + unsupported user-ref denial | Added deterministic DB-free P0.8.7 guard `check-p0-8-7-explicit-exemptions.mjs` and wired it into `scripts/check-saas-db-regression.mjs`. The guard verifies every `INFRA`, `LEGACY`, and `TELEMETRY` descriptor has `scopingKind=explicit_exemption` with a non-empty source, denies any `INFRA`/`TELEMETRY` descriptor with a static FK/soft-ref/P0.4 scoped-source signal to user data, and pins the prior leak class (`public.admin_audit_log`, `public.broadcast_audit`, `public.content_section_slug_history`) as `SCOPED` and covered by static artifacts. Updated P0.8 checklist, code facts, README status, and this log. | PASS starting gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS focused script: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-7-explicit-exemptions.mjs`. PASS final gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-7-explicit-exemptions.mjs && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-7-explicit-exemptions.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. | Static artifacts only; no scratch DB was needed or used. Unsupported user-ref findings fail/block and are not auto-scoped. LEGACY remains frozen; no legacy booking/rubitime retrofit. No P0.9 default-deny/enforce descriptors, no BOOTSTRAP/system_settings changes, no P0.8.3/8.4/8.5/8.6 policy changes, no migration, no runtime roles/env/grants, no app routes/services/UI, no dev/prod/test application DB, no `/opt/env`, no external channels, no full CI, no commit, no push. |
| 2026-07-08 | R1 / P0.8.6 BOOTSTRAP hybrid RLS migration | Ran the required branch drift check first: clean start, `git rev-list --left-right --count feat/doctor-ui-rebuild...codex/saas-roadmap-foundation` = `2 2`; SaaS branch had `2ec4aed32` P0.8.4 and `1c7b9389b` P0.8.5, while feat had unrelated booking commits `f353e13f1` and `503eca5b1`, so no merge was needed for this isolated SaaS scripts/docs/migration stage. Added deterministic P0.8.6 tooling and real Drizzle SQL policy migration `0163_p0_8_6_bootstrap_hybrid_rls.sql` after scratch smoke passed. The migration targets exactly the 4 BOOTSTRAP hybrid descriptor rows: `integrator.system_settings`, `public.platform_user_contacts`, `public.system_settings`, `public.user_phone_history`. It applies `ENABLE ROW LEVEL SECURITY`, `FORCE ROW LEVEL SECURITY`, `DROP POLICY IF EXISTS "saas_bootstrap_hybrid_p0_8_6"`, and `CREATE POLICY ... FOR ALL` with strict bootstrap hybrid predicate: global `organization_id IS NULL` rows always pass, organization rows require non-empty matching `app.org`. Updated `_journal.json`, P0.8 checklist/code facts, README status, and wired the P0.8.6 generator into `check:saas-db-regression`. | PASS preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS generator check: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-6-policy-generator.mjs`. PASS pre-migration scratch smoke on disposable `bcb_saas_p0_8_6_scratch_*` DB using `--print-sql` plus `sudo -n -u postgres psql`; scratch DB dropped. Initial post-migration gate failed only on ESLint `no-unused-vars` in the new smoke script; removed the unused variable. PASS post-migration gate after fix: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/*.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. PASS final scratch smoke again on disposable `bcb_saas_p0_8_6_scratch_*` DB; scratch DB dropped. | Predicate semantics intentionally differ from dormant permissive SCOPED policies: unset or empty `app.org` shows only global NULL rows; `app.org=orgA` shows global + orgA, not orgB. No admin Settings UI, mirror write path, `ALLOWED_KEYS`, runtime settings read/write path, app routes/services/UI, runtime roles/env/grants, P0.8.3/P0.8.4/P0.8.5 policy changes, INFRA/LEGACY/TELEMETRY/default-deny P0.8.7/P0.9 work, dev/prod/test application DB access, `/opt/env`, external channels, full CI, commit, or push. |
| 2026-07-08 | R1 / P0.8.5 integrator SCOPED RLS migration | Ran the required branch drift check first: clean start, `git rev-list --left-right --count feat/doctor-ui-rebuild...codex/saas-roadmap-foundation` = `1 1`; SaaS branch had `2ec4aed32` P0.8.4 and feat had unrelated `503eca5b1 feat(booking): canonical patient booking calendar`, so no merge was needed for this isolated SaaS scripts/docs/migration stage. Added deterministic P0.8.5 tooling and real Drizzle SQL policy migration `0162_p0_8_5_integrator_scoped_rls.sql` after scratch smoke passed. The migration targets only the strict 13 integrator SCOPED tables: I1 direct user bridge = 5, I2 identity bridge = 3, I3 parent-denorm children = 4, I4 mailings root = 1. It applies `ENABLE ROW LEVEL SECURITY`, `FORCE ROW LEVEL SECURITY`, `DROP POLICY IF EXISTS "saas_org_dormant_p0_8_5"`, and `CREATE POLICY ... FOR ALL` with dormant permissive `app.org` behavior using materialized `organization_id`. Updated `_journal.json`, P0.8 checklist/code facts, README status, and wired the P0.8.5 generator into `check:saas-db-regression`. | PASS preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS generator check: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-5-policy-generator.mjs`. PASS pre-migration scratch smoke on disposable `bcb_saas_p0_8_5_scratch_*` DB using `--print-sql` plus `sudo -n -u postgres psql`; scratch DB dropped. One first smoke wrapper attempt failed before DB creation because `$scratch_db` was expanded by the outer shell; corrected rerun passed. PASS post-migration gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/*.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. PASS final scratch smoke again on disposable `bcb_saas_p0_8_5_scratch_*` DB; scratch DB dropped. | No bridge joins recreated in policies; P0.4 materialized org columns remain authoritative. No P0.8.3/P0.8.4 target changes, no BOOTSTRAP/system_settings policies, no INFRA/LEGACY/TELEMETRY policy work, no runtime roles/env/grants, no app routes/services/UI, no dev/prod/test application DB access, no `/opt/env`, no external channels, no full CI, no P0.8.6. |
| 2026-07-08 | R1 / P0.8.4 public FK/denorm-path RLS migration | Ran the required branch drift/sync check first: after `git fetch --all --prune`, `codex/saas-roadmap-foundation`, `origin/codex/saas-roadmap-foundation`, local `feat/doctor-ui-rebuild`, and `origin/feat/doctor-ui-rebuild` all pointed at `0078d2f0d`; no merge was needed. Added deterministic P0.8.4 tooling and the real Drizzle SQL policy migration `0161_p0_8_4_public_path_rls.sql` after scratch smoke passed. The migration targets only the strict 37 public path-scoped SCOPED tables: 2 FK-path package item tables plus 35 denorm/materialized-org child tables. It applies `ENABLE ROW LEVEL SECURITY`, `FORCE ROW LEVEL SECURITY`, `DROP POLICY IF EXISTS "saas_org_dormant_p0_8_4"`, and `CREATE POLICY ... FOR ALL` with dormant permissive `app.org` behavior. FK-path predicates require both package parent and service cross-check organization to match `app.org`; denorm targets use their materialized `organization_id`. Updated `_journal.json`, P0.8 checklist/code facts, README status, and wired the P0.8.4 generator into `check:saas-db-regression`. | PASS branch/status drift check: clean start, all SaaS/feat refs at `0078d2f0d`, `git rev-list --left-right --count ...` = `0 0`. PASS preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS pre-migration scratch smoke on disposable `bcb_saas_p0_8_4_scratch_*` DB using `--print-sql` plus `sudo -n -u postgres psql`; scratch DB dropped. PASS post-migration regression/journal: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && git diff --check"`. PASS focused eslint: `bash /home/dev/orch/run-tests.sh "pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/*.mjs scripts/check-saas-db-regression.mjs"`. PASS final scratch smoke again on disposable `bcb_saas_p0_8_4_scratch_*` DB; scratch DB dropped. PASS final gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/*.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. One combined shell attempt failed before scratch DB creation due local quoting, then the same checks were rerun split and passed. | `public.comments` remains excluded because it is `polymorphic_resolver` and requires P0.12.1. No P0.8.3 direct-org target changes, no integrator policies, no BOOTSTRAP/system_settings policies, no INFRA/LEGACY/TELEMETRY policy work, no runtime roles/env/grants, no app routes/services/UI, no dev/prod/test application DB access, no `/opt/env`, no external channels, no full CI. |
| 2026-07-08 | R1 / P0.8.3 real public direct-org RLS migration | Re-ran the required branch drift check from `P0_8_CODE_FACTS.md`, then created the real Drizzle SQL policy migration `0160_p0_8_3_public_direct_org_rls.sql` from the existing deterministic P0.8.3 generator output after scratch smoke passed. The migration targets only the strict 103 `public` direct-org SCOPED tables and applies `ENABLE ROW LEVEL SECURITY`, `FORCE ROW LEVEL SECURITY`, `DROP POLICY IF EXISTS "saas_org_dormant_p0_8_3"`, and `CREATE POLICY ... FOR ALL` with the dormant permissive `app.org` predicate. Added the matching `_journal.json` entry and updated P0.8 docs to reflect execution. | PASS branch/status drift check: clean worktree at start, `git rev-list --left-right --count feat/doctor-ui-rebuild...codex/saas-roadmap-foundation` = `3 2`; `feat` had three newer commits including `0159_be_package_usages_appointment_debit_unique.sql`. PASS preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS pre-migration scratch smoke on disposable `bcb_saas_p0_8_3_scratch_*` DB using `--print-sql` plus `sudo -n -u postgres psql`; scratch DB dropped. PASS post-migration gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && <scratch smoke again> && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/*.mjs scripts/check-saas-db-regression.mjs && git diff --check"`. | Did not merge `feat/doctor-ui-rebuild`; its `0159` migration affects `be_package_usages` idempotency indexing but not the direct `organization_id` RLS predicate, so this branch used non-conflicting tag `0160`. No P0.8.4 FK/denorm/path tables, no integrator policies, no BOOTSTRAP/system_settings policies, no runtime roles/env/grants, no app code/routes/services/UI, no dev/prod/test application DB, no full CI. |
| 2026-07-08 | R1 / P0.8.3 tooling + scratch smoke | Added deterministic P0.8.3 tooling for the strict public direct-org SCOPED family: `p0-8-3-policy-targets.mjs` exports and validates the approved 103-table target list, `rls-sql-renderer.mjs` now renders quoted ENABLE/FORCE/DROP/CREATE policy DDL with stable policy name `saas_org_dormant_p0_8_3`, `check-p0-8-3-policy-generator.mjs` is wired into `check:saas-db-regression`, and `smoke-p0-8-3-direct-org-policies.mjs` creates synthetic scratch tables/roles, applies generated policies, proves owner dormant visibility plus app-role unset/empty permit and org A/B isolation, then rolls back. | PASS preflight gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS generator check: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-3-policy-generator.mjs`. PASS scratch smoke on disposable local DB `bcb_saas_p0_8_3_scratch_*`: generated SQL via `SCRATCH_DATABASE_URL=postgresql:///$scratch_db node docs/_TODO/SAAS_FOUNDATION/scripts/smoke-p0-8-3-direct-org-policies.mjs --print-sql`, executed with `sudo -n -u postgres psql -q "postgresql:///$scratch_db" -f <tmp-sql>`, then dropped the scratch DB. | Tooling/scratch smoke only. No real policy migration, no Drizzle `_journal.json`, no dev/prod/test DB access, no runtime role/env/grant changes, no application route/service/UI changes, no integrator/bootstrap/FK/denorm policies, no external deliveries. |
| 2026-07-08 | sync checkpoint / feat -> SaaS | Ran a branch sync checkpoint before continuing SaaS Foundation work. Read the required agent/rule/SaaS docs, verified a clean `codex/saas-roadmap-foundation` worktree, fetched all remotes, and compared `codex/saas-roadmap-foundation`, `origin/codex/saas-roadmap-foundation`, `feat/doctor-ui-rebuild`, `origin/feat/doctor-ui-rebuild`, `origin/main`, and `origin/test`. Local SaaS, origin SaaS, local feat, and origin feat already pointed at the same integration commit `e4eb19d31` (`Merge SaaS roadmap into doctor UI rebuild`); `origin/main` was `d09ea70c8` and `origin/test` was `a130741b0`, both behind the SaaS/feat integration branch. Executed `git merge --ff-only origin/feat/doctor-ui-rebuild` in the SaaS worktree; result: already up to date. | PASS full merge-checkpoint CI through wrapper: `bash /home/dev/orch/run-tests.sh "pnpm run ci"` (lint, typecheck, HLS helper sync, integrator tests `178 passed / 2 skipped`, webapp tests `1244 passed / 20 skipped`, media-worker tests `10 passed`, package/integrator build, webapp build, audit). Build warning noted: existing Next/Turbopack NFT trace warning for `mediaPreviewWorker.ts` via `next.config.ts`; build passed. Branch comparison before CI: `git status --short --branch`, `git fetch --all --prune`, `git for-each-ref ...`, `git rev-list --left-right --count ...`. | No conflicts. No changes from `main` or `test` were merged. No push to `main`, `test`, or `dimmdao`. No P0.8.3 implementation, no policy generator/smoke/migration work, no DB access, no prod/dev env access, no runtime role/env/grant changes, no external deliveries. |
| 2026-07-08 | R1 / P0.8 planning code-facts pass | Read repo rules for plan authoring, test policy, taskdb, full-CI gating, and SaaS guardrails. Gathered code facts through code index/search and targeted file reads: current P0.8 has descriptor model and predicate renderer, but lacks policy DDL generation, exact target export, and scratch-smoke tooling. Added `P0_8_CODE_FACTS.md`, linked it from README, expanded `P0_8_3_PREFLIGHT.md` into an execution brief with allowed files, required steps, gates, and stop conditions, and expanded `P0_8_RLS_DESCRIPTOR_CHECKLIST.md` / nightly runbook for P0.8.4-P0.8.7 preflight requirements. | PASS docs-only gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. | Docs/planning only. No policy migration, no generator implementation, no scratch DB access, no dev/prod DB access, no runtime role/env/grant changes, no full CI. |
| 2026-07-08 | R1 / P0.8.3 descriptor hygiene | Resolved the controlled P0.8.3 prerequisite for four parent-copy public rows: `content_section_slug_history`, `media_transcode_jobs`, `patient_daily_warmup_video_views`, and `reference_items`. Their P0.4 org-resolution sources are now classified as `denorm_org_column` in the RLS descriptor model, and the descriptor checker asserts both the four-row hold set and the strict `103` public direct-org P0.8.3 target. Updated `P0_8_3_PREFLIGHT.md` from unresolved hygiene gate to resolved strict-target guidance. | PASS pre-edit gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS descriptor smoke before docs: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-rls-descriptors.mjs`. PASS final gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm exec eslint docs/_TODO/SAAS_FOUNDATION/scripts/rls-descriptor-model.mjs docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-rls-descriptors.mjs && git diff --check"`. | Descriptor/checker/docs hygiene only. No policies applied, no migration created, no DB writes, no dev/prod DB access, no ENABLE/FORCE RLS, no runtime role flip, no grants/env changes, no external deliveries, no full CI. |
| 2026-07-08 | R1 / P0.8.3 preflight/design note | Added `P0_8_3_PREFLIGHT.md` as a docs-only controlled preflight for the next policy-application stage. The note defines the exact public direct-org SCOPED batching, distinguishes the current descriptor target (`107` direct-org public descriptors) from the stricter execution target (`103` tables after holding four parent-copy ambiguity rows), names the scratch-only smoke expectations, rollback gates, and owner gates. | PASS pre-edit gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS final docs-only gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. No separate docs/markdown lint script exists in `package.json`. | Design/preflight only. No policies applied, no migration created, no DB writes, no dev/prod DB access, no ENABLE/FORCE RLS, no runtime role flip, no grants/env changes, no external deliveries, no full CI. |
| 2026-07-08 | R1 / P0.8.2 SQL renderer predicate tests | Added a pure RLS SQL predicate renderer for Phase 0 policy-generation scaffolding and a focused P0.8.2 checker. The checker covers direct org predicates, patient ownership predicates, bootstrap hybrid predicates, dormant unset-GUC permit semantics, enforce-mode wrong-org deny, enforce-mode empty-GUC deny, and SQL identifier quoting for schema/table/column names. Wired the checker into `check:saas-db-regression` so the minimum SAAS DB regression gate exercises the renderer tests. | PASS preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS targeted P0.8.2 gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-sql-renderer.mjs && pnpm exec eslint scripts/check-saas-db-regression.mjs docs/_TODO/SAAS_FOUNDATION/scripts/rls-sql-renderer.mjs docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-sql-renderer.mjs && git diff --check"`. | P0.8.2 only. Pure unit/script checks only. No DB mutation, no policy migration application, no ENABLE/FORCE RLS, no runtime role flip, no prod/dev DB writes, no external deliveries, no full CI. P0.8.3 remains the next policy-application stage and requires scratch DB smoke before merge. |
| 2026-07-08 | R1 / P0.8.1 RLS descriptor model | Added the pure RLS descriptor model scaffold for all Phase 0 tier artifacts. `rls-descriptor-model.mjs` deterministically builds descriptors from the canonical artifacts (`tiers-218.tsv`, `p0-4-batches.tsv`, `p0-4-be-fk-paths.tsv`) instead of manually duplicating 219 rows. `check-p0-8-rls-descriptors.mjs` proves exact 219/219 coverage, tier counts (`SCOPED=155`, `BOOTSTRAP=24`, `INFRA=22`, `LEGACY=16`, `TELEMETRY=2`), SCOPED source classification (`111` P0.4 batch direct/denorm/polymorphic, `2` P0.4.BE FK-path, `42` `be_*` direct/self), bootstrap hybrid tables, and explicit INFRA/LEGACY/TELEMETRY exemptions. Wired the checker into `check:saas-db-regression` so the descriptor model stays guarded. | PASS initial preflight: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS standalone descriptor check: `node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-rls-descriptors.mjs`. PASS final targeted gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm exec eslint scripts/check-saas-db-regression.mjs docs/_TODO/SAAS_FOUNDATION/scripts/rls-descriptor-model.mjs docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-8-rls-descriptors.mjs && git diff --check"`. | P0.8.1 only. No SQL renderer tests, no policy migrations, no ENABLE/FORCE RLS, no runtime role flip, no DB writes, no prod/dev DB access, no payment/media/webapp writer changes, no full CI. P0.8.2 remains the next stage for renderer predicate tests. |
| 2026-07-08 | R1 / P0.7.6 payment/webhook writer context | Applied dormant DB organization principal context to payment/webhook writer paths. Entry map: booking payment runtime (`/api/payments/webhook/[provider]`, booking/member/product mock-complete and capture flows) uses the explicit `organizationId` already carried by payments service calls; `pgPayments` now runs payment intent/payment/refund/provider-event/history/payment-ref writes through `runWithDbOrganizationPrincipal(organizationId)` + webapp transaction chokepoint. Doctor patient ledger payments (`/api/doctor/patients/[userId]/payments`, `/acquiring-charge`) now require doctor workspace API context and pass `ctx.organizationId` into `patient_payment` writes. Patient acquiring webhook derives org only from the matched `patient_payment.organization_id`; legacy rows without org return `payment_org_missing` and do not invent a default. Added focused tests for booking webhook org propagation, patient acquiring webhook org/missing-org behavior, and in-memory patient-payment org fixtures. | PASS targeted P0.7.6 gate after waiting for the shared test lock held by the BCB nightly run: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm --dir apps/webapp exec vitest run src/modules/payments/service.test.ts src/modules/patient-payments/service.test.ts src/infra/repos/inMemoryPatientPayments.test.ts src/app/api/booking/payment-routes.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint src/infra/repos/pgPayments.ts src/infra/repos/pgPatientPayments.ts src/infra/repos/inMemoryPatientPayments.ts src/infra/repos/inMemoryPatientPayments.test.ts src/modules/payments/ports.ts src/modules/payments/service.ts src/modules/payments/service.test.ts src/modules/patient-payments/ports.ts src/modules/patient-payments/service.ts src/modules/patient-payments/service.test.ts src/app/api/doctor/patients/[userId]/payments/route.ts src/app/api/doctor/patients/[userId]/acquiring-charge/route.ts src/app/api/payments/webhook/[provider]/route.ts src/app/api/payments/patient-acquiring-webhook/[provider]/route.ts && git diff --check"` (13 tests). Earlier standalone webapp typecheck also passed after route gate typing cleanup. | P0.7.6 only. Boot migrations/ops remain migrator-only. No real payment provider calls, no webhook replay, no DB writes, no prod/env access, no RLS policies, no SQL renderer, no runtime role flip, no P0.8. Full CI intentionally not run for this micro-stage; backup-push uses the targeted gate. |
| 2026-07-08 | R1 / P0.7.5 media-worker writer context | Applied dormant DB organization principal context to media-worker transcode writer paths. `claimNextJob` now exposes `organizationId` on the claimed job and derives/stores it from `media_transcode_jobs.organization_id` or the parent `media_files.organization_id` without inventing a default org. `processTranscodeJob` runs the transcode flow under the optional job org principal, and the media-worker SQL runner now routes statements through `startMediaWorkerTransaction` only when a current org principal exists, so `SET LOCAL app.org` stays centralized in the P0.6 chokepoint. Missing job/media org remains dormant and uses the existing direct pool path. Added focused tests for claim org derivation, missing-org no-op behavior, direct unset execution, and principal-scoped transaction execution. | PASS targeted P0.7.5 gate: `bash /home/dev/orch/run-tests.sh "pnpm --dir packages/db-principal run build && pnpm run check:saas-db-regression && pnpm --dir apps/media-worker exec vitest run src/jobs/claim.test.ts src/processProgramSubmissionTranscode.test.ts src/withClient.test.ts src/runMediaWorkerSql.test.ts --reporter verbose && pnpm --dir apps/media-worker typecheck && pnpm --dir apps/media-worker exec eslint src/jobs/claim.ts src/jobs/claim.test.ts src/processTranscodeJob.ts src/processProgramSubmissionTranscode.ts src/persistVideoDurationSeconds.ts src/withClient.ts src/runMediaWorkerSql.ts src/runMediaWorkerSql.test.ts && git diff --check"` (13 tests). First attempt without building `packages/db-principal` failed on package resolution; rerun with the package build passed. | P0.7.5 only. No webapp enqueue changes, no payment/webhook work, no P0.7.6+, no RLS policies, no SQL renderer, no runtime role flip, no schema migration, no DB writes, no prod/env access, no dev server. Full CI intentionally not run for this micro-stage; backup-push uses the targeted gate. |
| 2026-07-08 | R1 / P0.7.5+ autonomous execution base | Added `AUTONOMOUS_NIGHTLY_RUNBOOK.md` as the operational base for nightly/autonomous passes after P0.7.4. The runbook verifies the required P0.6.1/P0.7.1-P0.7.4 branch markers, requires taskdb + `LOG.md` updates, limits execution to one micro-stage per pass, documents targeted validation through `/home/dev/orch/run-tests.sh`, and records commit/backup-push discipline without treating full CI as a per-step default. Expanded P0.7.5 media-worker and P0.7.6 payment/webhook checklists with concrete files, org-source decisions, forbidden scope, and local gate templates. Linked the runbook from README and the canonical plan. | PASS docs-only gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. A later pre-push full-CI attempt was stopped by owner direction as unnecessary for docs-only work; no full-CI result is claimed. | Docs-only planning. No runtime code, migrations, DB access, RLS policies, role flip, prod/env access, dev server, or push. |
| 2026-07-08 | R1 / P0.7.4 integrator worker/scheduler writer context | Applied dormant DB organization principal context to integrator worker/scheduler reminder and broadcast writer paths. Reminder planning now groups planned occurrence writes by `user_reminder_rules.organization_id`; reminder dispatch groups queued occurrence writes and public notification delivery attempts by occurrence/rule organization; orphaned pending occurrence expiration is bucketed per occurrence/rule organization and runs one scoped transaction per org; `user_reminder_occurrences` and `user_reminder_delivery_logs` runtime inserts copy `organization_id` from parent rows. `outgoingDeliveryWorker` now resolves `reminder_dispatch` org from occurrence/rule and `doctor_broadcast_intent` org from `public.broadcast_audit.organization_id`, applying context only around scoped reminder logs/occurrence finalization, notification delivery attempts, and broadcast audit counters. Queue claim/status writes remain INFRA/no-principal. Added focused tests for active org context, transaction promotion, and unset-source no-op behavior. | PASS targeted P0.7.4 gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm --dir apps/integrator exec vitest run src/kernel/domain/executor/handlers/reminders.tenantContext.test.ts src/kernel/domain/executor/handlers/reminders.channelFilter.test.ts src/infra/runtime/worker/outgoingDeliveryWorker.test.ts src/infra/db/repos/notificationDeliveryAttempts.test.ts src/infra/db/repos/reminders.expireOrphaned.test.ts src/infra/db/writePort.reminders.test.ts --reporter verbose && pnpm --dir apps/integrator typecheck && pnpm --dir apps/integrator exec eslint src/infra/principal/organizationPrincipal.ts src/infra/db/repos/reminders.ts src/infra/db/repos/reminders.expireOrphaned.test.ts src/infra/db/repos/broadcastAudit.ts src/infra/db/repos/notificationDeliveryAttempts.ts src/infra/db/repos/notificationDeliveryAttempts.test.ts src/infra/runtime/worker/outgoingDeliveryWorker.ts src/infra/runtime/worker/outgoingDeliveryWorker.test.ts src/kernel/contracts/reminders.ts src/kernel/domain/executor/handlers/reminders.ts src/kernel/domain/executor/handlers/reminders.tenantContext.test.ts && git diff --check"` (33 tests). Earlier focused smoke also passed for the three new/changed context test files (22 tests), and integrator typecheck passed after test typing cleanup. Review subagent finding on orphaned occurrence expiration was fixed before commit. | P0.7.4 only. `operator_alert` has no organization source and remains no-op/deferred. Queue claim/status writes remain INFRA. No P0.7.5+, P0.8, RLS policies, SQL renderer, runtime role flip, `system_settings` changes, new env vars, DB writes, prod/env access, dev server, or push. |
| 2026-07-08 | R1 / P0.7.3 integrator API/bot writer context | Applied dormant DB organization principal context to integrator API/bot writer boundaries. Added resolver helpers in `channelUsers` for a single active org from messenger identity bridge (`integrator.identities` → `public.platform_users.integrator_user_id` → active `org_enrollments` / `be_organization_members`) and direct integrator user id for M2M reminder rule upserts; both return `null` for no org or multi-org and use no default-org fallback. Threaded optional context resolvers through Telegram shared webhook/long-polling processing, MAX webhook, BersonCare reminder rules, and request-contact routes. Added a minimal `createDbWritePort` guard so plain write mutations, including bot retry enqueue, execute inside `db.tx` when a current org principal exists and the port is not already transaction-bound. Added focused resolver, write-port, Telegram/MAX, reminder-rule, and request-contact tests. Review subagent finding on `message.retry.enqueue` was fixed before commit. | PASS local gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm --dir apps/integrator exec vitest run src/infra/db/repos/channelUsers.test.ts src/infra/db/writePort.userUpsert.test.ts src/integrations/telegram/processTelegramUpdate.test.ts src/integrations/max/webhook.test.ts src/integrations/bersoncare/reminderRulesRoute.test.ts src/integrations/bersoncare/requestContactRoute.test.ts --reporter verbose && pnpm --dir apps/integrator typecheck && pnpm --dir apps/integrator exec eslint src/app/di.ts src/app/routes.ts src/infra/db/repos/channelUsers.ts src/infra/db/repos/channelUsers.test.ts src/infra/db/writePort.ts src/infra/db/writePort.userUpsert.test.ts src/infra/principal/organizationPrincipal.ts src/integrations/telegram/webhook.ts src/integrations/telegram/processTelegramUpdate.test.ts src/integrations/max/webhook.ts src/integrations/max/webhook.test.ts src/integrations/bersoncare/reminderRulesRoute.ts src/integrations/bersoncare/reminderRulesRoute.test.ts src/integrations/bersoncare/requestContactRoute.ts src/integrations/bersoncare/requestContactRoute.test.ts && git diff --check"` (50 tests). Review subagent targeted checks passed: `pnpm --dir apps/integrator test -- ...` and `pnpm --dir apps/integrator typecheck`. | P0.7.3 only. No worker/scheduler implementation, no P0.7.4+, no media-worker/payment/webhook stages, no RLS policies, no SQL renderer, no runtime role flip, no direct `system_settings` SELECT changes, no DB writes, no prod/env access, no dev server, no push. |
| 2026-07-08 | R1 / P0.7.2 webapp writer context: motivation reorder slice | Applied the dormant DB organization principal at the webapp server-action boundary for the scoped motivation quote reorder writer only. `reorderMotivationQuotes` now resolves the doctor workspace organization and runs the existing `doctorMotivationQuotesEditor.reorderQuotes` transaction under `runWithDbOrganizationPrincipal`; the repo path already uses `withPoolTransaction`, so P0.6.1 applies `app.org` through the central chokepoint. Added focused action and repo tests for action-level context cleanup and transaction-level `set_config('app.org', ...)`. | PASS before edits: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS targeted gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm --dir apps/webapp exec vitest run src/app/app/doctor/content/motivation/actions.test.ts src/infra/repos/pgDoctorMotivationQuotesEditor.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint src/app/app/doctor/content/motivation/actions.ts src/app/app/doctor/content/motivation/actions.test.ts src/infra/repos/pgDoctorMotivationQuotesEditor.test.ts && git diff --check"`. Review subagent: no findings on current motivation reorder slice. | P0.7.2 only. Kept the slice to one webapp action writer; did not change integrator, worker/scheduler, media-worker, payment/webhook, RLS policies, SQL renderer, runtime role, schema, or DB data. A broader broadcast action attempt was rejected after review because it included non-transaction SCOPED side effects. Other motivation quote writes (`upsert`, archive, active toggle) still use non-transaction statements and are deferred to later webapp P0.7.2 slices. Full CI not run; no push. |
| 2026-07-08 | R1 / P0.7.1 writer census inventory | Added `P0_7_WRITER_CENSUS.md`, an inventory-only reconciliation of SCOPED writer families against DB_ACCESS funnel coverage, `RAW_SQL_AUDIT.md`, P0.4 scope batches, and fresh code-index/`rg` scans. Covered webapp route/app-layer/port writers, integrator API/bot, worker/scheduler, media-worker, payment/webhook, packages, and boot/migration/ops paths. Linked the artifact from the SAAS README. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. | P0.7.1 only. No writer code changes, no P0.7.2+ context application, no RLS policies, no SQL renderer, no role flip, no DB writes, no prod/env access, no full CI, no push. |
| 2026-07-08 | R1 / P0.6.1 dormant DB principal context carrier | Added the dormant `@bersoncare/db-principal` carrier with AsyncLocalStorage-backed `organizationId`, UUID normalization/validation, and centralized `SET LOCAL app.org` application for transaction-scoped checked-out clients. Wired webapp, integrator, and media-worker DB chokepoints so unset context stays no-op and existing behavior is preserved; integrator `DbPort.tx` now uses the same prepare hook. Added focused tests for unset/no-SQL behavior, transaction application, nested/concurrent isolation, `buildAppDeps=cache()` not capturing org context, and rollback/release if principal setup fails after `BEGIN`. | PASS before edits: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. PASS targeted gate: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm --dir packages/db-principal run build && pnpm --dir apps/webapp exec vitest run src/infra/db/dbPrincipalContext.test.ts src/infra/db/withClient.test.ts src/app-layer/di/buildAppDeps.test.ts --reporter verbose && pnpm --dir apps/integrator exec vitest run src/infra/db/withClient.test.ts --reporter verbose && pnpm --dir apps/media-worker exec vitest run src/withClient.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/integrator typecheck && pnpm --dir apps/media-worker typecheck && pnpm exec eslint packages/db-principal/src/index.ts apps/webapp/src/infra/db/withClient.ts apps/webapp/src/infra/db/withClient.test.ts apps/webapp/src/infra/db/dbPrincipalContext.test.ts apps/webapp/src/app-layer/di/buildAppDeps.test.ts apps/integrator/src/infra/db/withClient.ts apps/integrator/src/infra/db/withClient.test.ts apps/media-worker/src/withClient.ts apps/media-worker/src/withClient.test.ts && git diff --check"`. PASS clean local lint/typecheck: `bash /home/dev/orch/run-tests.sh "pnpm --dir packages/db-principal run typecheck && pnpm --dir apps/webapp exec eslint src/infra/db/withClient.ts src/infra/db/withClient.test.ts src/infra/db/dbPrincipalContext.test.ts src/app-layer/di/buildAppDeps.test.ts && pnpm --dir apps/integrator exec eslint src/infra/db/withClient.ts src/infra/db/withClient.test.ts && pnpm --dir apps/media-worker exec eslint src/withClient.ts src/withClient.test.ts && git diff --check"`. PASS after integrator `DbPort.tx` test addition: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/integrator exec vitest run src/infra/db/withClient.test.ts --reporter verbose && pnpm --dir apps/integrator typecheck && pnpm --dir apps/integrator exec eslint src/infra/db/client.ts src/infra/db/withClient.ts src/infra/db/withClient.test.ts && git diff --check"`. PASS after review rollback fix: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm --dir apps/webapp exec vitest run src/infra/db/withClient.test.ts --reporter verbose && pnpm --dir apps/media-worker exec vitest run src/withClient.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/media-worker typecheck && pnpm --dir apps/webapp exec eslint src/infra/db/withClient.ts src/infra/db/withClient.test.ts && pnpm --dir apps/media-worker exec eslint src/withClient.ts src/withClient.test.ts && git diff --check"`. | P0.6.1 only. No RLS policies, no runtime DB role flip, no writer migration, no route/action churn, no read filtering, no DB writes, no prod/env access, no dev server, no full CI, no push. |
| 2026-07-08 | R1 / remaining Phase 0 checklist decomposition | Added executable docs-only checklists for P0.6-P0.13: dormant context, writer census, RLS descriptors/policies, default-deny descriptors, CI invariants, org-aware `system_settings`, residual polymorphic/JSON refs, and isolation fixtures. Linked the checklist set from SAAS README and `CORRECTED_PLAN.md` so future implementation stages do not execute from one-line spine rows alone. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && git diff --check"`. | Docs-only decomposition. No runtime code, no DB access, no migrations, no RLS/enforcement, no full CI, no push. Implementation should continue in a separate branch/worktree after this planning commit. |
| 2026-07-08 | R1 / pre-P0.6 DB regression guard baseline | Added root `check:saas-db-regression` script and `scripts/check-saas-db-regression.mjs`, aggregating the DB chokepoint guard, its synthetic-offender self-test, the `system_settings` accessor guard, P0.4 batch/FK-path artifact checks, and P0.5 role-split artifact check. Wired the guard into `pnpm run audit`, so full `pnpm run ci` now explicitly includes the SAAS DB regression filter. Added `UPSTREAM_SYNC_REGRESSION_CHECKLIST.md` and linked it from SAAS README / upstream sync policy. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm run check:saas-db-regression && pnpm run audit && git diff --check"`; `pnpm run audit` now runs the new guard and then `registry-prod-audit`. PASS: `bash /home/dev/orch/run-tests.sh "pnpm exec eslint scripts/check-saas-db-regression.mjs"`. | No runtime behavior changes, no DB access, no RLS/enforcement, no env changes, no full CI, no push. Existing DB_ACCESS allowlists remain unchanged; this stage packages and documents the guards for pre-P0.6/upstream/full-CI checkpoints. |
| 2026-07-08 | R1 / P0.5.1 DB role split contract/proof | Added `P0_5_DB_ROLE_SPLIT.md` defining the dormant migrator/owner vs non-bypass app runtime role contract. Added guarded scratch-only proof SQL `P0_5_DB_ROLE_SPLIT_PROOF.sql` and a local artifact checker `check-p0-5-role-split.mjs`. Updated unified-Postgres/env docs to distinguish confirmed current production (one unified `DATABASE_URL` runtime role) from the future SAAS role split contract. Privileged scratch execution passed in `bcb_saas_spike`: the proof created synthetic owner/migrator/app roles, enabled `FORCE ROW LEVEL SECURITY`, verified app-role org filtering through `app.org`, then rolled the transaction back. | PASS: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-5-role-split.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs && git diff --check"`. PASS: scratch DB sanity reached synthetic `bcb_saas_spike` without printing credentials. PASS/expected guard before privileged rerun: non-role-management scratch connection refused. PASS: privileged root/postgres scratch run ended with `P0.5.1 role split scratch proof OK.` | No runtime role flip, no production role creation, no env changes, no real DB grants, no dev/prod DB writes, no RLS policies on real tables, no full CI, no push. P0.5.1 is complete as a dormant proof/contract stage; runtime role cutover remains out of scope until later RLS gates. |
| 2026-07-08 | R1 / P0.4.BE booking package FK-path invariant | Declared the two remaining scoped `be_*` item-table tenant paths in `scope-derivation/p0-4-be-fk-paths.tsv`: `be_package_items` scopes through `be_subscription_packages.organization_id`, and `be_patient_package_items` scopes through `be_patient_packages.organization_id`. Added a local manifest checker and read-only aggregate SQL verification for parent/service same-org invariants. Updated R1 taxonomy to make the FK-path semantics explicit. | PASS: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-be-fk-paths.mjs && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs && set -a && source /home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev && set +a && psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -X -q -P pager=off -f docs/_TODO/SAAS_FOUNDATION/P0_4_BE_FK_PATH_CHECK.sql && git diff --check"`; aggregate read-only verification showed 1 `be_package_items` row, 3 `be_patient_package_items` rows, 0 missing package/service parents, and 0 service/package org mismatches. | No runtime query filtering, no RLS/enforcement, no schema migration, no org column added to the item tables, no prod DB, no external channels, no full CI, no push. The item tables remain intentionally outside the 111-table `needs-orgid-FINAL.txt` count because P0.4.BE proves declared FK-path scope instead of direct columns. First local SQL gate attempt used the current worktree env path and failed before DB connection because that worktree has no `apps/webapp/.env.dev`; corrected rerun used the documented dev env from the main worktree and passed. |
| 2026-07-08 | R1 / P0.4.I4 integrator mailings direct-org root | Added nullable `organization_id`, org index, and org FK for integrator `mailings`. Added migration `20260708_0004_p0_4_i4_integrator_mailings_org.sql` with strict default-org check, current single-org backfill, and aggregate no-NULL assertion. Applied the migration to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs && set -a && source /home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev && set +a && psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -X -q -c 'BEGIN;' -f apps/integrator/src/infra/db/migrations/core/20260708_0004_p0_4_i4_integrator_mailings_org.sql -c 'ROLLBACK;'"`; PASS: `ENV_FILE=/home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev pnpm --dir apps/integrator run migrate` through wrapper; PASS: aggregate read-only verification showed 0 `mailings`, 0 NULL `organization_id` rows, and 1 migration ledger row. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. Later writer/context stages must make new `mailings` writes carry creating org before enforcement. |
| 2026-07-08 | R1 / P0.4.I3 integrator parent-denorm child org columns/backfill | Added nullable `organization_id`, org indexes, and org FKs for 4 integrator child tables: `conversation_messages`, `question_messages`, `user_reminder_occurrences`, and `user_reminder_delivery_logs`. Added migration `20260708_0003_p0_4_i3_integrator_parent_denorm_org.sql` that copies org from the already-scoped parent (`conversations`, `user_questions`, `user_reminder_rules`, `user_reminder_occurrences`) and asserts zero NULL rows plus zero child/parent org mismatches. Added `organizationId` to the existing narrow integrator Drizzle descriptors for `user_reminder_occurrences` and `user_reminder_delivery_logs`. Applied the migration to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/integrator typecheck && pnpm --dir apps/integrator exec eslint src/infra/db/schema/integratorDomainRepos.ts && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs && set -a && source /home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev && set +a && psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -X -q -c 'BEGIN;' -f apps/integrator/src/infra/db/migrations/core/20260708_0003_p0_4_i3_integrator_parent_denorm_org.sql -c 'ROLLBACK;'"`; PASS: `ENV_FILE=/home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev pnpm --dir apps/integrator run migrate` through wrapper; PASS: aggregate read-only verification showed 30 `conversation_messages`, 18 `question_messages`, 1080 `user_reminder_occurrences`, 1007 `user_reminder_delivery_logs`, 0 NULL `organization_id` rows across all four tables, 0 child/parent org mismatches, and 1 migration ledger row. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. |
| 2026-07-08 | R1 / P0.4.I2 integrator identity-path org columns/backfill | Added nullable `organization_id`, org indexes, and org FKs for 3 integrator identity-path tables: `conversations`, `message_drafts`, and `user_questions`. Added migration `20260708_0002_p0_4_i2_integrator_identity_path_org.sql` with org resolution through `identities.user_id` → `public.platform_users.integrator_user_id`, active org enrollment/member lookup, multi-org bridge guard, explicit current default-org fallback for unresolved legacy/orphan identity-path rows, and aggregate no-NULL assertion. Applied the migration to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs && set -a && source /home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev && set +a && psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -X -q -c 'BEGIN;' -f apps/integrator/src/infra/db/migrations/core/20260708_0002_p0_4_i2_integrator_identity_path_org.sql -c 'ROLLBACK;'"`; PASS: `ENV_FILE=/home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev pnpm --dir apps/integrator run migrate` through wrapper; PASS: aggregate read-only verification showed 18 `conversations`, 17 `message_drafts`, 14 `user_questions`, 0 NULL `organization_id` rows across all three tables, 8 default-org fallback rows, 0 multi-org identity-path integrator user keys, and 1 migration ledger row. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. |
| 2026-07-08 | R1 / P0.4.I1 integrator direct-user org columns/backfill | Added nullable `organization_id`, org indexes, and org FKs for 5 integrator direct-user path tables: `contacts`, `content_access_grants`, `mailing_logs`, `user_reminder_rules`, and `user_subscriptions`. Added migration `20260708_0001_p0_4_i1_integrator_direct_user_org.sql` with direct bridge resolution through `public.platform_users.integrator_user_id`, active org enrollment/member lookup, a multi-org bridge guard, explicit current default-org fallback for unresolved legacy/orphan direct-user rows, Stage 13 bypass only for frozen `user_subscriptions` backfill, and aggregate no-NULL assertion. Added `organizationId` to the existing narrow integrator Drizzle descriptors for `content_access_grants`, `user_reminder_rules`, `mailing_logs`, and `user_subscriptions`. Applied the migration to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/integrator typecheck && pnpm --dir apps/integrator exec eslint src/infra/db/schema/integratorDomainRepos.ts src/infra/db/schema/integratorPublicProduct.ts && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of `20260708_0001_p0_4_i1_integrator_direct_user_org.sql` against dev DB; PASS: `ENV_FILE=/home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev pnpm --dir apps/integrator run migrate` through wrapper; PASS: aggregate read-only verification showed 69 `contacts`, 0 `content_access_grants`, 0 `mailing_logs`, 24 `user_reminder_rules`, 0 `user_subscriptions`, 0 NULL `organization_id` rows across all five tables, 2 default-org fallback rows, 0 multi-org direct integrator user keys, and 1 migration ledger row. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. First psql dry-run and aggregate verification attempts had shell quoting mistakes before useful SQL execution; corrected reruns passed. |
| 2026-07-07 | R1 / P0.4.RC reference categories authoritative org semantics | Added nullable `organization_id`, org index, and org FK for `reference_categories`. Added migration `0155_p0_4_rc_reference_categories_org.sql` with strict default-org check, explicit assertion that stale `owner_id`/`tenant_id` are unused, current default-org backfill for all reference categories, synchronization of `reference_items.organization_id` from its parent category, aggregate no-NULL assertion, and reference item/category org mismatch assertion. Applied 0155 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/schema.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0155 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 8 reference categories, 0 NULL `reference_categories.organization_id` rows, 0 non-NULL stale `owner_id` rows, 0 non-NULL stale `tenant_id` rows, 0 `reference_items`/category org mismatches, and Drizzle ledger/journal count matched at 156 rows. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. Legacy `tenant_id` remains readable for existing references API compatibility, but P0.4.RC makes `organization_id` the authoritative tenant column and asserts stale legacy columns are empty. |
| 2026-07-07 | R1 / P0.4.D polymorphic/denorm org resolver cases | Added nullable `organization_id` columns, org indexes, and org FKs for the 2 P0.4.D tables: `comments` and `patient_home_block_items`. Added migration `0154_p0_4_d_polymorphic_denorm_org.sql` with explicit target-type resolver coverage for `comments` (`exercise`, `lfk_complex`, `test`, `test_set`, `recommendation`, `lesson`, `stage_item_instance`, `stage_instance`, `program_instance`), author-org/default fallback for unresolved legacy comments, parent-copy backfill from `patient_home_blocks` for home block items, and aggregate no-NULL plus resolver/parent mismatch assertions. Applied 0154 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/entityComments.ts db/schema/schema.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0154 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 2 P0.4.D tables, 0 resolver/parent org mismatches, 9 total checked rows, and Drizzle ledger/journal count matched at 155 rows. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. Profiling found current `comments` target coverage is 1 `program_instance` row; P0.12 still must verify polymorphic target resolver coverage before enforcement. |
| 2026-07-07 | R1 / P0.4.P8 public catalog/content/audit org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 17 public P8 tables: admin/broadcast audit roots and recipients, clinical diagnosis/test catalogs, content sections/pages/access grants/slug history, courses, motivational quotes, recommendations/regions, reference items, tests/test sets/items. Added migration `0153_p0_4_p8_catalog_content_audit_org.sql` with current default-org backfill for clinic-owned catalog/content roots, user/member org resolution for audit actors where available, parent-copy backfill for audit recipients and catalog child tables, explicit default-org fallback for legacy unresolved audit actor rows, and aggregate no-NULL plus child/parent mismatch assertions. Applied 0153 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/schema.ts db/schema/broadcastAuditRecipients.ts db/schema/courses.ts db/schema/recommendations.ts db/schema/clinicalTests.ts db/schema/patientClinical.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0153 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 17 P0.4.P8 tables, 0 child/parent org mismatches, 708 total checked rows, and Drizzle ledger/journal count matched at 154 rows. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. `reference_items` uses explicit current default-org fallback in P8 because `reference_categories` is intentionally deferred to P0.4.RC and has no authoritative `organization_id` yet; RC must finalize/validate reference category semantics. |
| 2026-07-07 | R1 / P0.4.P7 public reminders/media/analytics org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 23 public P7 tables: reminders and delivery attempts, message/mailing/subscription logs, media folders/files/upload/transcode/playback events, product push/analytics per-user rows, material ratings/feedback, and operator health failure archive. Added migration `0152_p0_4_p7_reminders_media_org.sql` with patient/member org resolution, regex-safe integrator-user bridge for text IDs, parent-copy backfill for media/reminder child rows, explicit current default-org fallback for legacy/root rows with no reliable path, child/parent mismatch assertion, and aggregate no-NULL assertion. Applied 0152 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/schema.ts db/schema/materialRatings.ts db/schema/patientContentRatingFeedback.ts db/schema/notificationDeliveryAttempts.ts db/schema/operatorHealthFailureArchive.ts db/schema/productAnalytics.ts db/schema/webappReminderOccurrences.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0152 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 23 P0.4.P7 tables, 0 child/parent org mismatches, 23068 total checked rows, and Drizzle ledger/journal count matched at 153 rows. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. Count-only profiling found legacy/root fallback buckets, including unresolved media owner paths, null-user analytics rows, non-numeric/unresolved notification attempt integrator refs, and `content_pages` rating validation deferred until P0.4.P8 because `content_pages.organization_id` does not exist yet. |
| 2026-07-07 | R1 / P0.4.P6 public support/comms org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 8 public support/comms tables: doctor notes, doctor patient support, specialist tasks, support conversations/messages, support questions/messages, and support delivery events. Added migration `0151_p0_4_p6_support_comms_org.sql` with patient/member org resolution, strict multi-org and staff/patient mismatch guards, parent-copy backfill for support child rows, explicit current default-org fallback for unresolved legacy support rows, child/parent mismatch assertion, and aggregate no-NULL assertion. Applied 0151 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/schema.ts db/schema/doctorPatientSupport.ts db/schema/specialistTasks.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0151 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 8 P0.4.P6 tables, 0 child org mismatches, 0 multi-active patient/member users, and Drizzle ledger/journal count matched at 152 rows. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. Count-only profiling found unresolved legacy support rows with no reliable parent path: 13 support conversations with NULL platform user, 1 support conversation user with zero active enrollment, 1 support question without conversation, and 3434 delivery events without a conversation message; these use the explicit current default-org fallback. |
| 2026-07-07 | R1 / P0.4.P5 public online intake org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 4 public online intake tables: requests, answers, attachments, and status history. Added migration `0150_p0_4_p5_online_intake_org.sql` with patient-enrollment backfill for requests, guarded default-org fallback, parent-copy backfill for child rows, a multi-active patient org guard, and aggregate no-NULL assertion. Applied 0150 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/schema.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0150 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 4 P0.4.P5 tables, 0 child org mismatches, 0 multi-active request users, 0 zero-active request users, and Drizzle ledger/journal count matched at 151 rows. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. The first aggregate verification command used `pg_read_file` for journal count and failed on permission after printing successful table aggregates; the final rerun used local `jq` for the expected journal count and passed. |
| 2026-07-07 | R1 / P0.4.P4 public diary/activity org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 7 public diary/activity tables: warmup presentations/video views, diary day snapshots, patient home blocks, practice completions, symptom trackings, and symptom entries. Added migration `0149_p0_4_p4_diary_activity_org.sql` with patient-enrollment backfill for patient-owned rows, default-org backfill for `patient_home_blocks`, parent/completion-copy backfill for video views and symptom entries, a multi-active patient org guard, and aggregate no-NULL assertion. Applied 0149 to dev DB after rollback dry-run. | PASS after fixture alignment: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/schema.ts db/schema/patientDailyWarmupPresentation.ts db/schema/patientDailyWarmupVideoView.ts db/schema/patientDiarySnapshots.ts db/schema/patientPractice.ts src/infra/repos/inMemoryPatientDiarySnapshots.ts src/modules/patient-diary/aggregatePassageStatsFromSnapshots.test.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0149 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 7 P0.4.P4 tables and Drizzle ledger/journal count matched at 150 rows. Aggregate unresolved-patient check found 0 multi-org rows and 5 zero-enrollment `patient_diary_day_snapshots` rows, which use the explicit current default-org fallback. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. The nullable `organizationId` field required explicit `null` in in-memory diary snapshot support/test fixtures. A read-only subagent audit flagged the silent fallback risk; 0149 now makes the fallback guarded and explicit. |
| 2026-07-07 | R1 / P0.4.P3 public LFK/test org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 11 public LFK/test tables: LFK complexes/templates/exercises/regions/media/sessions/assignments plus `test_attempts` and `test_results`. Added migration `0148_p0_4_p3_lfk_test_org.sql` with default-org backfill for catalog/template roots, patient-enrollment backfill for patient-owned rows, parent-copy backfill for child rows, and aggregate no-NULL assertion. Applied 0148 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/schema.ts db/schema/treatmentProgramTestAttempts.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0148 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 11 P0.4.P3 tables; PASS: Drizzle ledger count matched journal count at 149 rows. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels, no full CI, no push. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. The roadmap worktree lacks `.env.dev`, so dev migration checks sourced the existing canonical dev env from `/home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev` without printing secrets. |
| 2026-07-07 | R1 / P0.4.P2 public treatment program org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 12 public treatment-program tables: templates, template stages/groups/items, instances, instance stages/groups/items, program action log, item discussion messages/reads, and treatment program events. Added migration `0147_p0_4_p2_treatment_program_org.sql` with default-org template backfill, patient-enrollment/template/default-org instance backfill, and parent-copy backfill for child rows. Applied 0147 to dev DB after rollback dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/treatmentProgramTemplates.ts db/schema/treatmentProgramInstances.ts db/schema/programActionLog.ts db/schema/programItemDiscussion.ts db/schema/treatmentProgramEvents.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0147 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 12 P0.4.P2 tables and one Drizzle ledger row for 0147. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. |
| 2026-07-07 | R1 / P0.4.P1 public clinical EHR org columns/backfill | Added nullable `organization_id` columns, org indexes, and org FKs for 12 public clinical/EHR tables: clinical visits, complaints/updates, diagnoses/status history/updates, anamnesis trauma/illness/lifestyle, comorbidities, patient files, and patient payments. Added migration `0146_p0_4_p1_clinical_ehr_org.sql` with patient-enrollment backfill and parent-copy backfill for child rows. Marked `patient_merge_candidates` as already-direct-org in the P0.4 batch manifest because it already has `organization_id NOT NULL` and org/status indexing. Applied 0146 to dev DB after dry-run. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/patientClinical.ts db/schema/patientClinicalAnamnesis.ts db/schema/patientComorbidities.ts db/schema/patientFiles.ts db/schema/patientPayments.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: rollback dry-run of 0146 against dev DB; PASS: `pnpm --dir apps/webapp run migrate` through wrapper; PASS: aggregate read-only verification showed 0 NULL `organization_id` rows across the 13 P0.4.P1 tables and one Drizzle ledger row for 0146. | No runtime query filtering, no RLS/enforcement, no prod DB, no external channels. Dev DB writes were limited to the approved migration/backfill. Verification printed aggregate counts only, no PII rows. |
| 2026-07-07 | R1 / P0.4.0 scoped-table batch map | Added `P0_4_BATCHES.md`, machine-readable `scope-derivation/p0-4-batches.tsv`, and `scripts/check-p0-4-batches.mjs`. The artifact assigns every table from `needs-orgid-FINAL.txt` to exactly one P0.4 micro-batch and records the intended org-resolution/backfill strategy for each table. | PASS: `bash /home/dev/orch/run-tests.sh "node docs/_TODO/SAAS_FOUNDATION/scripts/check-p0-4-batches.mjs"`; PASS: `git diff --check`. | Planning/artifact stage only. No `organization_id` columns added, no data backfill, no DB access, no RLS/enforcement. `P0.4.BE` remains a separate FK-path support stage for already-scoped `be_*` gaps outside the 111 `needs-orgid` table count. |
| 2026-07-07 | R1 / P0.3.3 integrator bridge report | Added read-only SQL report `P0_3_INTEGRATOR_BRIDGE_REPORT.sql` for `platform_users.integrator_user_id` ↔ `integrator.users.id` coverage, organization resolution via `org_enrollments`/`be_organization_members`, and explicit quarantine buckets. Fixed Drizzle journal timestamps for 0141-0145 so they sort after 0140; otherwise `drizzle-kit migrate` skipped them. Applied 0141-0145 to dev DB after owner approval. Count-only bridge summary: 111 platform users with `integrator_user_id`; 108 resolve to exactly one org; 0 resolve to multiple orgs; 0 linked integrator users lack org resolution; 3 platform users point to a missing integrator user row; 118 total integrator users; 10 integrator users have no platform user. | PASS: `bash /home/dev/orch/run-tests.sh "set -a && source /home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev && set +a && pnpm --dir apps/webapp run migrate"`; PASS: aggregate-only `READ ONLY` SQL report. | No PII was selected or printed; report used counts only. Dev DB was written only by approved migrations. No prod DB access, no external channels, no RLS/enforcement. |
| 2026-07-07 | R1 / P0.3.2 current client enrollment backfill | Added strict/idempotent migration `0145_seed_client_org_enrollments.sql`. It verifies the default organization exists, dynamically counts current canonical active clients (`role='client'`, `merged_into_id IS NULL`, `is_archived IS FALSE`), inserts/updates their single-org `org_enrollments` rows as `active`, and verifies aggregate enrolled count matches the client count. | PASS: `bash /home/dev/orch/run-tests.sh "bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh"`; PASS: `git diff --check`. | P0.3.2 only. No DB migration was executed against dev/prod. No PII was read or printed; verification is aggregate count-only. No runtime callers, no integrator bridge map, no RLS/enforcement, no writes outside `org_enrollments`. |
| 2026-07-07 | R1 / P0.3.1 org enrollments DDL | Added dormant Drizzle schema + migration for `public.org_enrollments`: `organization_id`, `platform_user_id`, lifecycle `status`, `created_at`, FKs to `be_organizations`/`platform_users`, org/user indexes, unique `(organization_id, platform_user_id)`, and a status check for `active`/`invited`/`discharged`/`archived`. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/bookingEngine.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh"`; PASS: `git diff --check`. | P0.3.1 only. No runtime callers, no client backfill, no integrator bridge map, no DB migration execution against dev/prod, no PII reads/printing, no RLS/enforcement. |
| 2026-07-07 | R1 / P0.2 branch sync | Pushed `codex/saas-roadmap-foundation` after the pre-push CI gate, then fast-forwarded `feat/doctor-ui-rebuild` to the same commit and pushed it. | PASS: `codex/saas-roadmap-foundation`, `origin/codex/saas-roadmap-foundation`, `feat/doctor-ui-rebuild`, and `origin/feat/doctor-ui-rebuild` all resolved to `6f5139aadd8bb2afe480a664840187a05850570b`; both worktrees were clean after sync. | No force-push. No merge commit was needed; `feat/doctor-ui-rebuild` fast-forwarded cleanly. |
| 2026-07-07 | R1 / P0.2 pre-push integration gate | Ran the repository-level pre-push gate after P0.2.2-P0.2.5 and the local P0.2 sync checkpoint were committed. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm run ci"` (lint + DB chokepoint, typecheck, HLS helper sync, integrator tests, webapp tests, media-worker tests, integrator build, webapp production build, registry audit). | One existing Turbopack NFT trace warning from `apps/webapp/next.config.ts` / media preview worker was reported during build; CI exited 0. No code changes were made after this gate before recording the docs-only log entry. |
| 2026-07-07 | R1 / P0.2 sync checkpoint | Reviewed the P0.2 diff against `origin/codex/saas-roadmap-foundation`, checked changed module boundaries, confirmed no ESLint allowlist changes, no migrations, no broad `org_id` rollout, no RLS/GUC enforcement, and no TypeScript `any` in changed TS/TSX diff. A read-only audit subagent found one P2 least-privilege issue in bound-doctor directory reads; fixed it with organization-scoped point reads before checkpointing. | PASS: `git diff --check`; PASS: `rg "from ['\\\"]@/infra/(db|repos)|import\\\\(['\\\"]@/infra/(db|repos)" apps/webapp/src/modules/organization-membership apps/webapp/src/modules/doctor-workspace --glob '!**/*.test.ts' --glob '!**/*.test.tsx'` returned no matches; PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp run lint && pnpm --dir apps/webapp exec vitest run src/modules/organization-membership/service.test.ts src/modules/doctor-workspace/service.test.ts src/app-layer/guards/requireRole.doctorWorkspaceContext.test.ts src/app-layer/guards/requireRole.doctorStaffAccess.test.ts src/app/api/doctor/booking-engine/_requireDoctorBookingEngine.test.ts src/app/api/admin/booking-engine/_requireAdminBookingEngine.test.ts src/app/api/doctor/workspace/directory/route.test.ts src/infra/repos/pgOrganizationMembership.test.ts src/app-layer/di/buildAppDeps.test.ts --reporter verbose"` (9 files / 61 tests). | No full CI yet; this was the local P0.2 phase gate. One accidental broad `pnpm --dir apps/webapp test -- ...` attempt was interrupted after path filtering proved too wide. No dev/prod DB access, no PII printing, no push, no `feat/doctor-ui-rebuild` sync yet. |
| 2026-07-07 | R1 / P0.2.5 read-only organization specialist/member listing | Extended the organization membership port with organization-scoped member and specialist directory reads, added Drizzle + in-memory implementations, introduced `doctor-workspace` directory types/service, wired the service into `buildAppDeps`, and added `GET /api/doctor/workspace/directory` over the P0.2.3 organization-aware API gate. Bound doctors receive only their own active specialist/member via point reads scoped by organization + row id; admin/owner contexts receive all active specialists and all organization members. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run src/modules/doctor-workspace/service.test.ts src/modules/organization-membership/service.test.ts src/infra/repos/pgOrganizationMembership.test.ts src/app/api/doctor/workspace/directory/route.test.ts src/app-layer/di/buildAppDeps.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint src/modules/doctor-workspace/types.ts src/modules/doctor-workspace/service.ts src/modules/doctor-workspace/service.test.ts src/modules/organization-membership/ports.ts src/modules/organization-membership/service.test.ts src/infra/repos/pgOrganizationMembership.ts src/infra/repos/pgOrganizationMembership.test.ts src/infra/repos/inMemoryOrganizationMembership.ts src/app/api/doctor/workspace/directory/route.ts src/app/api/doctor/workspace/directory/route.test.ts src/app-layer/di/buildAppDeps.ts src/app-layer/di/buildAppDeps.test.ts"`. | P0.2.5 only. No invite/create/update/deactivate flows, no specialist CRUD, no booking-catalog rewrite, no writes to `be_organization_members`, no dev/prod DB reads, no PII printing, no RLS/GUC enforcement, no full CI. |
| 2026-07-07 | R1 / P0.2.4 doctor workspace context contract | Added module-owned `DoctorWorkspaceContext` type and built it in `/app/doctor` layout from the P0.2.3 resolved workspace gate. `DoctorWorkspaceShell` now accepts the stable context prop for nested multi-specialist controls without changing rendering. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint src/modules/doctor-workspace/types.ts src/app/app/doctor/layout.tsx src/shared/ui/doctor/shell/DoctorWorkspaceShell.tsx"`. | P0.2.4 only. No page redesign, no org switcher persistence, no new DB tables, no client-side authorization decisions, no specialist/member listing yet, no full CI. |
| 2026-07-07 | R1 / P0.2.3 app-layer doctor/admin gate wiring | Added in-memory organization membership port, wired `organizationMembership` into `buildAppDeps`, added additive organization-aware doctor workspace helpers in `requireRole.ts`, and switched doctor/admin booking-engine gates from default-org lookup to resolved membership context while preserving existing role-only helpers and adminMode. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run src/app-layer/guards/requireRole.doctorWorkspaceContext.test.ts src/app-layer/guards/requireRole.doctorStaffAccess.test.ts src/app/api/doctor/booking-engine/_requireDoctorBookingEngine.test.ts src/app/api/admin/booking-engine/_requireAdminBookingEngine.test.ts src/app-layer/di/buildAppDeps.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint src/app-layer/guards/requireRole.ts src/app-layer/guards/requireRole.doctorWorkspaceContext.test.ts src/app-layer/guards/requireRole.doctorStaffAccess.test.ts src/app-layer/di/buildAppDeps.ts src/app-layer/di/buildAppDeps.test.ts src/infra/repos/inMemoryOrganizationMembership.ts src/app/api/doctor/booking-engine/_requireDoctorBookingEngine.ts src/app/api/doctor/booking-engine/_requireDoctorBookingEngine.test.ts src/app/api/admin/booking-engine/_requireAdminBookingEngine.ts src/app/api/admin/booking-engine/_requireAdminBookingEngine.test.ts"`. | P0.2.3 only. Existing `requireDoctorAccess()` / `requireDoctorApiSession()` return types unchanged. Did not migrate all routes, add RLS/GUC context, add org switcher, run DB access, or run full CI. |
| 2026-07-07 | R1 / P0.2.2 membership resolver service | Added `createOrganizationMembershipService` and `resolveOrganizationForUser` under `modules/organization-membership`. The resolver is module-only, side-effect free, uses the P0.2.1 port, returns discriminated outcomes for no membership / selection required / selected membership missing, and exposes management flags for owner/admin vs doctor/assistant. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run src/modules/organization-membership/service.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint src/modules/organization-membership/ports.ts src/modules/organization-membership/service.ts src/modules/organization-membership/service.test.ts"`. | P0.2.2 only. No DI/gate wiring, no route/page changes, no migrations, no DB access, no RLS/GUC context, no UI changes, no full CI. |
| 2026-07-07 | R1 / P0.2 planning | Added `P0_2_MEMBERSHIP_RESOLVER_CHECKLIST.md` with executable plans for P0.2.2-P0.2.5: resolver service, app-layer doctor/admin gate wiring, doctor workspace UI contract, and read-only org specialist/member listing. Updated `CORRECTED_PLAN.md` to include P0.2.4/P0.2.5 and linked the checklist from the initiative README. | PASS: `git diff --check`. | Docs-only planning. No code, tests, migrations, DB access, RLS, broad `org_id` rollout, UI redesign, push, or real external channels. |
| 2026-07-07 | R1 / P0.2.1 organization membership port | Added `OrganizationMembershipPort` under `modules/organization-membership` and a Drizzle-backed `createPgOrganizationMembershipPort` infra adapter for `public.be_organization_members`. The adapter currently exposes read-only membership lookup by platform user plus active-only lookup for the next resolver checkpoint. | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run src/infra/repos/pgOrganizationMembership.test.ts --reporter verbose && pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint src/modules/organization-membership/ports.ts src/infra/repos/pgOrganizationMembership.ts src/infra/repos/pgOrganizationMembership.test.ts"`; PASS: `bash /home/dev/orch/run-tests.sh "bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh"`; PASS: `git diff --check`. | P0.2.1 only. No resolver service, no auth gate/DI wiring, no enrollment table, no broad `org_id` rollout, no DB migration execution, no dev/prod DB access, no real external channels. |
| 2026-07-07 | R1 / P0.1.2 staff membership seed | Added Drizzle migration `0143_seed_staff_organization_members.sql`. It seeds current active staff into `public.be_organization_members` for the default organization: 1 doctor linked to specialist `518ea988-9b5e-4ad8-8194-a2d98f43bd7b`, 5 admins without specialist link. The migration is strict/idempotent: it asserts default org exists, target specialist exists/is active/in-org/has appointments, current active staff count is exactly 1 doctor + 5 admins, and final seeded membership counts match. | PASS: `bash /home/dev/orch/run-tests.sh "bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh && pnpm --dir apps/webapp typecheck && pnpm run lint"`; PASS: `git diff --check`. | No DB migration was executed against dev/prod. No PII was read or printed. No org resolver (P0.2), enrollments (P0.3), broad org columns (P0.4), RLS/enforcement, prod DB, or real external channels. |
| 2026-07-07 | R1 / P0.1.1 table taxonomy + organization members DDL | Added `R1_TABLE_TAXONOMY.md` with tenant-owned/global catalog/system/audit/integration/legacy buckets and the rule that new product tables need tenant semantics or a documented exemption. Added dormant Drizzle schema + migration for `public.be_organization_members` (`organization_id`, `platform_user_id`, `role`, nullable `specialist_id`, `status`, timestamps, FKs/indexes/checks/unique org-user). | PASS: `bash /home/dev/orch/run-tests.sh "pnpm --dir apps/webapp typecheck && pnpm --dir apps/webapp exec eslint db/schema/bookingEngine.ts && bash apps/webapp/scripts/check-drizzle-journal-sync.sh && bash apps/webapp/scripts/check-legacy-migrations-frozen.sh"`; PASS: `git diff --check`; PASS: `bash /home/dev/orch/run-tests.sh "pnpm run lint"`. Prior integration base was green via `bash /home/dev/orch/run-tests.sh "pnpm run ci"` on the same branch before R1 changes. | P0.1.1 only. No seed/backfill (P0.1.2), no org resolver (P0.2), no enrollments (P0.3), no broad `org_id` rollout (P0.4), no RLS/enforcement, no prod/dev DB access. New table has `organization_id NOT NULL` because it is empty/dormant; nullable-before-backfill rule applies to new columns on existing populated tables. |
| 2026-07-02 | branding/custom-domain roadmap | Added SaaS roadmap coverage for individual app branding and paid verified custom domains with permanent redirects for expensive tariffs/clinics. Updated `ROADMAP_TO_SAAS.md`, `REQUIREMENTS.md`, and `00_DECISIONS_AND_SCHEMA.md`. | Docs-only; `git diff --check`. | Product capability only; no Phase 0/R1 implementation, no code, no migrations. |
| 2026-07-01 | product direction update | Updated README and `ROADMAP_TO_SAAS.md` with owner decision: continue one-codebase product development while laying dormant SaaS-capable schema/access mechanisms. Future commercialization may be optional upper-tier white-label branding plus entitlement/capability bundles (store, tariffs, courses/products), not necessarily a hard product fork. Added merge gates for bringing dormant R0/R1 work into `feat/doctor-ui-rebuild`/`main` without changing live behavior. | Docs-only; `git diff --check`. | No code, migrations, DB access, tenant enforcement, branding implementation, billing, push, or main merge. |
| 2026-07-01 | upstream sync policy | Added `UPSTREAM_SYNC_POLICY.md` after merging `feat/doctor-ui-rebuild` into `codex/saas-roadmap-foundation`; updated README live docs. Purpose: define how future SaaS work should keep compatibility with upstream UI/product changes without reintroducing R0/R1 DB-access bypasses or creating a long-lived UI fork. | Docs-only; `git diff --check`. | No code, migrations, DB access, push, or R1 implementation. |
| 2026-06-30 | roadmap audit | Reviewed SAAS plan against related initiatives: `DB_ACCESS_CHOKEPOINT`, Product Platform, Courses, booking payments/memberships/products, platform identity, specialist consolidation, old multitenant sketch. Added `ROADMAP_TO_SAAS.md` and updated README/REQUIREMENTS. Main finding: tenant foundation is not the full SaaS business roadmap; `DB_ACCESS_CHOKEPOINT` is a hard prerequisite, while org lifecycle, SaaS billing/quotas, self-service onboarding, marketplace/courses and regions are later roadmaps. | Docs-only review; `rg` + targeted doc reads. | Did not pull Product Platform/Courses into Phase 0; they stay deferred until post-tenant foundation productization. |
| 2026-06-30 | pre-start decomposition audit | Reviewed canonical `CORRECTED_PLAN.md` v8 against repo plan rules and sizing standard before Phase 0 start. Found that architecture/scope are hardened, but executable labels P0.4/P0.7/P0.8/P0.11/P0.13 were still too aggregate for safe first implementation. Wrote **v9**: added sizing-risk table, executable micro-stage spine P0.1.1→P0.13.3, and Phase 0 DoD forbidding aggregate briefs for the oversized buckets. Updated README live status/source pointers. | Docs-only review; no code/tests required. Checked canonical docs and `.cursor/rules/plan-authoring-execution-standard.mdc` / `test-execution-policy.md`. | Architecture/scope unchanged from v8. P0.1 remains first, but execution starts at P0.1.1, not aggregate P0.1/P0.4 buckets. No migrations or runtime code started. |
| 2026-06-17 | — | Initiative folder + skeleton docs (README, 00 decisions+schema, 01 master-plan+compliance, 02 brief template, LOG). Read full AGENTS.md + .cursor/rules; folded conflicts (integration-config-in-DB, Drizzle-only, clean-arch, dev-PII, §24 orchestration) into the plan. | n/a (docs only) | No code yet (skeleton only, per owner). persons/directory split dropped from Phase 0. |
| 2026-06-17 | decompose | Finer decomposition (~20→~30 stages) + per-stage rules-check (02). Grounded clinical scope via read-only `information_schema`: 44 user-owned tables → classified ~18 scope / ~16 GLOBAL identity-auth (must NOT scope) / ~7 defer telemetry / 2 legacy. SCOPE+RLS surface cut ~50→18, batched B1/B2/B3. | RO SQL (counts only) | Auth/identity tables stay person-global. 5 borderline tables → owner sign-off (F0.8b). prod-parity gate on F0.12/F0.13. |
| 2026-06-17 | **LOOP CLOSED** | **Round-9 (v8) = CLEAN — zero findings → streak 2/2 → hardening loop CLOSED, plan HARDENED.** 2nd independent fresh Opus: own pg_constraint scan (89 FK→users + soft-ref union 116 tables, 0 in INFRA/TELEMETRY), independently rediscovered `platform_users.merged_into_id` (FK no heuristic catches → validates FK-based derivation), exact 219 bijection, 111≡SCOPED-non-be set-match, design/rules/P0.1 all clean. **9 rounds total (v2→v8); findings 9/3C→7/2C→5/0C→2→1→CLEAN→1HIGH→CLEAN→CLEAN.** Ready for P0.1. | RO SQL + independent FK scans | Plan hardened; execute Phase 0 on owner go. |
| 2026-06-17 | harden-loop r9 | **Round-8 (v8) = CLEAN — zero findings** (1st clean, streak 1/2). Robust: reviewer's own pg_constraint scan (97 FK→users + 118 soft-ref tables → 0 in INFRA/TELEMETRY), all 22 INFRA + 2 TELEMETRY columns manually user-less, every count reproduced, bijection exact. FK-class provably closed. v8 unchanged. Fresh **final confirmation** reviewer launched. | RO SQL + FK/soft-ref scans | Streak 1/2; 2nd consecutive clean → loop closes. |
| 2026-06-17 | harden-loop r8 | **Round-7 (confirmation) = NOT CLEAN — 1 HIGH** → streak reset 0/2. The 1st clean (r6) was a FALSE clean; r7's deeper FK-scan found a real leak: `admin_audit_log`/`broadcast_audit`/`content_section_slug_history` have FK/soft-ref (`actor_id`/`changed_by_user_id`) to platform_users but were tiered INFRA-global. **Validates the 2-consecutive rule.** **v8 fix: derivation now FK-based (`pg_constraint` FK→platform_users) + soft user-ref cols** → the 3 → SCOPED; tally 155/22/24/16/2=219; needs-orgid 108→111; P0.10 invariant adds "every FK/soft-ref→platform_users → never INFRA/TELEMETRY". Fresh reviewer relaunched on v8. | RO SQL; FK-invariant verified empty | Streak 0/2; FK-class now closed. |
| 2026-06-17 | harden-loop r7 | **Round-6 verdict: CLEAN — zero findings** (1st clean → streak 1/2). Reviewer re-scanned the r5 class via pg_constraint FK-scan (no hidden per-patient key in any non-SCOPED tier), recomputed all counts from DB, confirmed set-level artifact agreement. v7 unchanged. Fresh **confirmation** reviewer launched (2nd-consecutive-clean check). | RO SQL + FK scan | Streak 1/2; one clean from close. |
| 2026-06-17 | harden-loop r6 | Round-5 verdict **NOT CLEAN (1: MEDIUM)** — real tier-misclassification all 4 prior rounds missed: per-USER `product_analytics_events_recent`/`_user_hourly` (+`media_hls_proxy_error_events`, `operator_health_failure_archive`) were TELEMETRY/global but carry user keys → cross-tenant leak. **v7 re-tiers them → SCOPED**; TELEMETRY now = user-less rollups only (product_analytics_hourly, media_playback_stats_hourly). New tally SCOPED 152 / INFRA 25 / TELEMETRY 2 / 219 ✓; needs-orgid 104→108. Fresh reviewer relaunched on v7. | RO SQL; merge-path proof of patient-keying | Streak 0/2; long tail (single shrinking nits). |
| 2026-06-17 | harden-loop r5 | Round-4 verdict **NOT CLEAN (2: 0C/0H, both MEDIUM doc-accuracy)** — near close (9/3C→7/2C→5/0C→2/0C0H). Reviewer confirmed ALL design/scope axes clean (perfect DB↔artifact bijection over 219, every SCOPED org-resolvable, bridge exact). **v6 folds M1** (system_settings read-surface: 6 external readers named + CI-grep + mirror rule) **+ M2** (T0 counts → measured ~198 files/54 connect/48 query/28 actions + method; T0 ~5–8wk). Fresh reviewer relaunched on v6. | RO SQL | Streak 0/2 (next verdict may be 1st CLEAN). |
| 2026-06-17 | harden-loop r4 | Round-3 verdict **NOT CLEAN (5: 0C/2H)** — strongly converging (9/3C→7/2C→5/0C); reviewer confirmed C1/C2/H2 repaired, artifacts diff-identical (104≡SCOPED-non-be), all SCOPED org-resolvable. **v5 folds F1–F5 (all doc-nits):** stale 109→104; `integrator.mailings` org-orphan→direct-add (F2); 4 direct-add roots named (F3); reference_categories stale tenant_id/owner_id (F4); system_settings 3rd read-site (F5). Fresh reviewer relaunched on v5. | RO SQL | Streak 0/2 (near first clean). |
| 2026-06-17 | harden-loop r3 | Round-2 verdict **NOT CLEAN (7: 2C/3H)** but **converging** (r1 was a gross whole-schema miss; r2 = artifact reconciliation + 4 infra re-tiers + design detail). **v4 fixes:** `needs-orgid-FINAL` now generated FROM `tiers-218.tsv` (single source = **104**); outbox/queue pumps + mailing-topic taxonomy → INFRA (148 SCOPED / 27 INFRA / 219 ✓); system_settings PK `(key,scope,org_id)`+partial-unique+mirror detail (H-1); per-integrator-table org-resolution paths (H-3); be_* precise 41+self+2 (M-1); P0.10 invariant strengthened (SCOPED⟺org_id-or-path, no-NULL, artifacts-agree). Fresh reviewer relaunched on v4. | RO SQL; gate ✅ 219 | Streak 0/2. |
| 2026-06-17 | harden-loop r2 | Round-1 verdict **NOT CLEAN (9: 3C/3H)** → streak reset 0/2. Root: 218-reconciliation was asserted not performed. **Fixed in v3:** actually ran the 219-wide tier classification (`tiers-218.tsv`, recursive FK-closure + cut-sets): SCOPED 153 (**109 need org_id** + 44 be_*) / BOOTSTRAP 24 / INFRA 22 / LEGACY 16 / TELEMETRY 4 = 219 ✓; system_settings → single BOOTSTRAP tier w/ row-level org predicate (C-B); integrator bridge orphan/NULL handling (C-C); bigint↔uuid key types (H-A); telemetry-split uniform (H-B). Fresh reviewer relaunched on v3. | RO SQL; completeness gate ✅ over 219 | Streak 0/2 (need first clean). |
| 2026-06-17 | harden-loop r1 | Plan **v2** written (incorporates C-1 scope over public+integrator=218 + integrator org-bridge · C-3 classified RLS generator + 41-child denorm · C-2 chokepoint=T0 · H-4 three access tiers incl bootstrap-readable · H-1/H-2/H-3/H-5). Hardening loop started: revise→fresh cold agent→repeat until **2 consecutive clean**. Round 1 fresh agent reviewing v2. Streak **0/2**. | docs only | Defaults applied: integrator scoped via identity-bridge; store copy = later phase. |
| 2026-06-17 | plan-review | Opus re-check of CORRECTED_PLAN → **REVISION REQUIRED** (confirmed by DB queries). **C-1:** completeness on public(185) only; `integrator` schema (33 PII tables, 0 org_id) unscoped → **218** total. **C-3:** 41/84 scope tables have no ownership column → "one RLS template" unimplementable; need classified generator + child denorm. **C-2:** chokepoint is T0-weight (singleton/`cache()`/separate pools), not dormant. **H-4:** default-deny bricks login w/o bootstrap-readable tier. + H-1/H-2/H-3/H-5. | RO queries confirmed C-1/C-3/H-5 ✅ | Architecture holds; revise scope over public+integrator + RLS mechanics. 2 owner-decisions: integrator org-bridge, catalog/store copy model. |
| 2026-06-17 | corrected-plan | Wrote **CORRECTED_PLAN.md** (canonical): shared-DB+RLS on the 84, **fail-safe default-deny + explicit PUBLIC allowlist** (owner-approved), Phase 0 spine P0.1–P0.13 (incl. C3 headless-runtime context P0.7, residual-risk scan P0.12, CI fail-safe invariant P0.10). C1/C2 resolved, C3 in plan. README repointed to LIVE docs; 00/01/02 marked history. | docs only | Ready to execute Phase 0 on owner go; gates = prod-parity (P0.5/P0.8) + T0 shadow-run before default-deny flip. |
| 2026-06-17 | scope-rederive | **C1 RESOLVED.** 3 independent methods (columns/FK-closure/code-usage, 2 in worktrees, blind) + deterministic DB arbiter (recursive FK-closure) + completeness gate (185 reconciles). Result: **66 tables need org_id** (EHR core recovered), 44 be_*(already org), 8 legacy, 18 global-identity, 48 infra. Methods converged on clinical core, diverged only on borderline perimeter. Artifacts in scope-derivation/ (VERIFIED_SCOPE.md, arbiter.tsv, needs-orgid.txt, method-*.md). Worktrees cleaned. | RO metadata SQL + grep; completeness gate ✅ | Direction converged: shared-DB+RLS (silo dropped — patient↔patient needs row-level). Borderlines → owner: per-patient telemetry, content catalogs, system_settings row-level, legacy-confirm. |
| 2026-06-17 | fresh-review | Cold-start Opus adversarial review → **plan BLOCKED, REWORK before F0.1** (REVIEW_2026-06-17_FRESH.md). C1: prior classification wrong ~2–3× — heuristic matched 2/15 user-key cols, missed 18 `patient_user_id` EHR tables (clinical_diagnosis/visit, patient_files, patient_payment, treatment_program_instances…) + ~20 child tables; real scope ≈45–55. C2: D7 system_settings per-org config infeasible (scope role-based). C3: integrator/worker/scheduler/bot have no org/edge → break under RLS; resolver doesn't auto-centralize; F0.13 must hard-gate behind F0.12+shadow. H1 seed non-deterministic; H2 catalog tenancy undecided. | RO SQL confirmed C1 (18 patient_user_id tables, 185 total, 15 user-key cols) | Status flipped BLOCKED in README/00(D7)/01(classification). Required rework: (1) re-derive scope by FK reachability, (2) redesign D7 + amend rule 000, (3) add headless-runtime workstream + RLS hard-gate, (4) deterministic seed, (5) catalog-tenancy decision. F0.1 not started. |
