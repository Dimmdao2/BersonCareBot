# Execution log — SAAS_FOUNDATION

Mandatory per `.cursor/rules/plan-authoring-execution-standard` (§12.5). One entry per stage:
what was done, checks run (+results), decisions, what was deliberately NOT done.

| Date | Stage | Done | Checks (result) | Decisions / skipped |
|---|---|---|---|---|
| 2026-07-01 | upstream sync policy | Added `UPSTREAM_SYNC_POLICY.md` after merging `feat/doctor-ui-rebuild` into `codex/saas-roadmap-foundation`; updated README live docs. Purpose: define how future SaaS work should keep compatibility with upstream UI/product changes without reintroducing R0/R1 DB-access bypasses or creating a long-lived UI fork. | Docs-only; `git diff --check`. | No code, migrations, DB access, push, or R1 implementation. |
| 2026-06-30 | roadmap audit | Reviewed SAAS plan against related initiatives: `DB_ACCESS_CHOKEPOINT`, Product Platform, Courses, booking payments/memberships/products, platform identity, specialist consolidation, old multitenant sketch. Added `ROADMAP_TO_SAAS.md` and updated README/REQUIREMENTS. Main finding: tenant foundation is not the full SaaS business roadmap; `DB_ACCESS_CHOKEPOINT` is a hard prerequisite, while org lifecycle, SaaS billing/quotas, self-service onboarding, marketplace/courses and regions are later roadmaps. | Docs-only review; `rg` + targeted doc reads. | Did not pull Product Platform/Courses into Phase 0; they stay deferred until post-tenant foundation productization. |
| 2026-06-30 | pre-start decomposition audit | Reviewed canonical `CORRECTED_PLAN.md` v8 against repo plan rules and sizing standard before Phase 0 start. Found that architecture/scope are hardened, but executable labels P0.4/P0.7/P0.8/P0.11/P0.13 were still too aggregate for safe first implementation. Wrote **v9**: added sizing-risk table, executable micro-stage spine P0.1.1→P0.13.3, and Phase 0 DoD forbidding aggregate briefs for the oversized buckets. Updated README live status/source pointers. | Docs-only review; no code/tests required. Checked canonical docs and `.cursor/rules/plan-authoring-execution-standard.mdc` / `test-execution-policy.md`. | Architecture/scope unchanged from v8. P0.1 remains first, but execution starts at P0.1.1, not aggregate P0.1/P0.4 buckets. No migrations or runtime code started. |
| 2026-06-17 | — | Initiative folder + skeleton docs (README, 00 decisions+schema, 01 master-plan+compliance, 02 brief template, LOG). Read full AGENTS.md + .cursor/rules; folded conflicts (integration-config-in-DB, Drizzle-only, clean-arch, dev-PII, §24 orchestration) into the plan. | n/a (docs only) | No code yet (skeleton only, per owner). persons/directory split dropped from Phase 0. |
| 2026-06-17 | decompose | Finer decomposition (~20→~30 stages) + per-stage rules-check (02). Grounded clinical scope via read-only `information_schema`: 44 user-owned tables → classified ~18 scope / ~16 GLOBAL identity-auth (must NOT scope) / ~7 defer telemetry / 2 legacy. SCOPE+RLS surface cut ~50→18, batched B1/B2/B3. | RO SQL (counts only) | Auth/identity tables stay person-global. 5 borderline tables → owner sign-off (F0.8b). prod-parity gate on F0.12/F0.13. |
| 2026-06-17 | **LOOP CLOSED** | **Round-9 (v8) = CLEAN — zero findings → streak 2/2 → hardening loop CLOSED, plan HARDENED.** 2nd independent fresh Opus: own pg_constraint scan (89 FK→users + soft-ref union 116 tables, 0 in INFRA/TELEMETRY), independently rediscovered `platform_users.merged_into_id` (FK no heuristic catches → validates FK-based derivation), exact 219 bijection, 111≡SCOPED-non-be set-match, design/rules/P0.1 all clean. **9 rounds total (v2→v8); findings 9/3C→7/2C→5/0C→2→1→CLEAN→1HIGH→CLEAN→CLEAN.** Ready for P0.1. | RO SQL + independent FK scans | Plan hardened; execute Phase 0 on owner go. |
| 2026-06-17 | harden-loop r9 | **Round-8 (v8) = CLEAN — zero findings** (1st clean, streak 1/2). Robust: reviewer's own pg_constraint scan (97 FK→users + 118 soft-ref tables → 0 in INFRA/TELEMETRY), all 22 INFRA + 2 TELEMETRY columns manually user-less, every count reproduced, bijection exact. FK-class provably closed. v8 unchanged. Fresh **final confirmation** reviewer launched. | RO SQL + FK/soft-ref scans | Streak 1/2; 2nd consecutive clean → loop closes. |
| 2026-06-17 | harden-loop r8 | **Round-7 (confirmation) = NOT CLEAN — 1 HIGH** → streak reset 0/2. The 1st clean (r6) was a FALSE clean; r7's deeper FK-scan found a real leak: `admin_audit_log`/`broadcast_audit`/`content_section_slug_history` have FK/soft-ref (`actor_id`/`changed_by_user_id`) to platform_users but were tiered INFRA-global. **Validates the 2-consecutive rule.** **v8 fix: derivation now FK-based (`pg_constraint` FK→platform_users) + soft user-ref cols** → the 3 → SCOPED; tally 155/22/24/16/2=219; needs-orgid 108→111; P0.10 invariant adds "every FK/soft-ref→platform_users → never INFRA/TELEMETRY". Fresh reviewer relaunched on v8. | RO SQL; FK-invariant verified empty | Streak 0/2; FK-class now closed. |
| 2026-06-17 | harden-loop r7 | **Round-6 verdict: CLEAN — zero findings** (1st clean → streak 1/2). Reviewer re-scanned the r5 class via pg_constraint FK-scan (no hidden per-patient key in any non-SCOPED tier), recomputed all counts from DB, confirmed set-level artifact agreement. v7 unchanged. Fresh **confirmation** reviewer launched (2nd-consecutive-clean check). | RO SQL + FK scan | Streak 1/2; one clean from close. |
| 2026-06-17 | harden-loop r6 | Round-5 verdict **NOT CLEAN (1: MEDIUM)** — real tier-misclassification all 4 prior rounds missed: per-USER `product_analytics_events_recent`/`_user_hourly` (+`media_hls_proxy_error_events`, `operator_health_failure_archive`) were TELEMETRY/global but carry user keys → cross-tenant leak. **v7 re-tiers them → SCOPED**; TELEMETRY now = user-less rollups only (product_analytics_hourly, media_playback_stats_hourly). New tally SCOPED 152 / INFRA 25 / TELEMETRY 2 / 219 ✓; needs-orgid 104→108. Fresh reviewer relaunched on v7. | RO SQL; merge-path proof of patient-keying | Streak 0/2; long tail (single shrinking nits). |
| 2026-06-17 | harden-loop r5 | Round-4 verdict **NOT CLEAN (2: 0C/0H, both MEDIUM doc-accuracy)** — near close (9/3C→7/2C→5/0C→2/0C0H). Reviewer confirmed ALL design/scope axes clean (perfect DB↔artifact bijection over 219, every SCOPED org-resolvable, bridge exact). **v6 folds M1** (system_settings read-surface: 6 external readers named + CI-grep + mirror rule) **+ M2** (T0 counts → measured ~198 files/54 connect/48 query/28 actions + method; T0 ~5–8wk). Fresh reviewer relaunched on v6. | RO SQL | Streak 0/2 (next verdict may be 1st CLEAN). |
| 2026-06-17 | harden-loop r4 | Round-3 verdict **NOT CLEAN (5: 0C/2H)** — strongly converging (9/3C→7/2C→5/0C); reviewer confirmed C1/C2/H2 repaired, artifacts diff-identical (104≡SCOPED-non-be), all SCOPED org-resolvable. **v5 folds F1–F5 (all doc-nits):** stale 109→104; `integrator.mailings` org-orphan→direct-add (F2); 4 direct-add roots named (F3); reference_categories stale tenant_id/owner_id (F4); system_settings 3rd read-site (F5). Fresh reviewer relaunched on v5. | RO SQL | Streak 0/2 (near first clean). |
| 2026-06-17 | harden-loop r3 | Round-2 verdict **NOT CLEAN (7: 2C/3H)** but **converging** (r1 was a gross whole-schema miss; r2 = artifact reconciliation + 4 infra re-tiers + design detail). **v4 fixes:** `needs-orgid-FINAL` now generated FROM `tiers-218.tsv` (single source = **104**); outbox/queue pumps + mailing-topic taxonomy → INFRA (148 SCOPED / 27 INFRA / 219 ✓); system_settings PK `(key,scope,org_id)`+partial-unique+mirror detail (H-1); per-integrator-table org-resolution paths (H-3); be_* precise 41+self+2 (M-1); P0.10 invariant strengthened (SCOPED⟺org_id-or-path, no-NULL, artifacts-agree). Fresh reviewer relaunched on v4. | RO SQL; gate ✅ 219 | Streak 0/2. |
| 2026-06-17 | harden-loop r2 | Round-1 verdict **NOT CLEAN (9: 3C/3H)** → streak reset 0/2. Root: 218-reconciliation was asserted not performed. **Fixed in v3:** actually ran the 219-wide tier classification (`tiers-218.tsv`, recursive FK-closure + cut-sets): SCOPED 153 (**109 need org_id** + 44 be_*) / BOOTSTRAP 24 / INFRA 22 / LEGACY 16 / TELEMETRY 4 = 219 ✓; system_settings → single BOOTSTRAP tier w/ row-level org predicate (C-B); integrator bridge orphan/NULL handling (C-C); bigint↔uuid key types (H-A); telemetry-split uniform (H-B). Fresh reviewer relaunched on v3. | RO SQL; completeness gate ✅ over 219 | Streak 0/2 (need first clean). |
| 2026-06-17 | harden-loop r1 | Plan **v2** written (incorporates C-1 scope over public+integrator=218 + integrator org-bridge · C-3 classified RLS generator + 41-child denorm · C-2 chokepoint=T0 · H-4 three access tiers incl bootstrap-readable · H-1/H-2/H-3/H-5). Hardening loop started: revise→fresh cold agent→repeat until **2 consecutive clean**. Round 1 fresh agent reviewing v2. Streak **0/2**. | docs only | Defaults applied: integrator scoped via identity-bridge; store copy = later phase. |
| 2026-06-17 | plan-review | Opus re-check of CORRECTED_PLAN → **REVISION REQUIRED** (confirmed by DB queries). **C-1:** completeness on public(185) only; `integrator` schema (33 PII tables, 0 org_id) unscoped → **218** total. **C-3:** 41/84 scope tables have no ownership column → "one RLS template" unimplementable; need classified generator + child denorm. **C-2:** chokepoint is T0-weight (singleton/`cache()`/separate pools), not dormant. **H-4:** default-deny bricks login w/o bootstrap-readable tier. + H-1/H-2/H-3/H-5. | RO queries confirmed C-1/C-3/H-5 ✅ | Architecture holds; revise scope over public+integrator + RLS mechanics. 2 owner-decisions: integrator org-bridge, catalog/store copy model. |
| 2026-06-17 | corrected-plan | Wrote **CORRECTED_PLAN.md** (canonical): shared-DB+RLS on the 84, **fail-safe default-deny + explicit PUBLIC allowlist** (owner-approved), Phase 0 spine P0.1–P0.13 (incl. C3 headless-runtime context P0.7, residual-risk scan P0.12, CI fail-safe invariant P0.10). C1/C2 resolved, C3 in plan. README repointed to LIVE docs; 00/01/02 marked history. | docs only | Ready to execute Phase 0 on owner go; gates = prod-parity (P0.5/P0.8) + T0 shadow-run before default-deny flip. |
| 2026-06-17 | scope-rederive | **C1 RESOLVED.** 3 independent methods (columns/FK-closure/code-usage, 2 in worktrees, blind) + deterministic DB arbiter (recursive FK-closure) + completeness gate (185 reconciles). Result: **66 tables need org_id** (EHR core recovered), 44 be_*(already org), 8 legacy, 18 global-identity, 48 infra. Methods converged on clinical core, diverged only on borderline perimeter. Artifacts in scope-derivation/ (VERIFIED_SCOPE.md, arbiter.tsv, needs-orgid.txt, method-*.md). Worktrees cleaned. | RO metadata SQL + grep; completeness gate ✅ | Direction converged: shared-DB+RLS (silo dropped — patient↔patient needs row-level). Borderlines → owner: per-patient telemetry, content catalogs, system_settings row-level, legacy-confirm. |
| 2026-06-17 | fresh-review | Cold-start Opus adversarial review → **plan BLOCKED, REWORK before F0.1** (REVIEW_2026-06-17_FRESH.md). C1: prior classification wrong ~2–3× — heuristic matched 2/15 user-key cols, missed 18 `patient_user_id` EHR tables (clinical_diagnosis/visit, patient_files, patient_payment, treatment_program_instances…) + ~20 child tables; real scope ≈45–55. C2: D7 system_settings per-org config infeasible (scope role-based). C3: integrator/worker/scheduler/bot have no org/edge → break under RLS; resolver doesn't auto-centralize; F0.13 must hard-gate behind F0.12+shadow. H1 seed non-deterministic; H2 catalog tenancy undecided. | RO SQL confirmed C1 (18 patient_user_id tables, 185 total, 15 user-key cols) | Status flipped BLOCKED in README/00(D7)/01(classification). Required rework: (1) re-derive scope by FK reachability, (2) redesign D7 + amend rule 000, (3) add headless-runtime workstream + RLS hard-gate, (4) deterministic seed, (5) catalog-tenancy decision. F0.1 not started. |
