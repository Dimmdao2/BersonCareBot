# T0.4 integrator entrypoint organization-context map

Date: 2026-07-10

Taskdb: `#639`

Purpose: close the T0.4 source audit for how integrator ingress, workers, and schedulers obtain organization context before they write SCOPED rows. This is a source/runtime-path map only; it does not flip RLS roles, remove legacy tables, or call external channels.

## Entrypoints

| Entrypoint                              | Runtime source                                                                                                        | Organization source                                                                                                                                       | Scoped write posture                                                                                                                                                                                                                            | Guard/test                                                                                                                                              |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Telegram webhook                        | `apps/integrator/src/integrations/telegram/webhook.ts` via registered webhook route                                   | Messenger identity `externalId + telegram` resolved by `resolveActiveOrganizationIdForMessengerIdentity` in `apps/integrator/src/app/routes.ts`           | `processTelegramUpdate` wraps `eventGateway.handleIncomingEvent(event)` in `runWithOrganizationPrincipal(organizationId, handleEvent)` when a single active organization is resolved.                                                           | `apps/integrator/src/integrations/telegram/processTelegramUpdate.test.ts`                                                                               |
| Telegram long polling                   | `apps/integrator/src/integrations/telegram/longPolling.ts`                                                            | Same dependency object and resolver as webhook mode.                                                                                                      | Long polling delegates parsed updates to the shared `processTelegramUpdate` core, so it inherits the webhook org wrapper.                                                                                                                       | `apps/integrator/src/integrations/telegram/processTelegramUpdate.test.ts`                                                                               |
| MAX webhook                             | `apps/integrator/src/integrations/max/webhook.ts`                                                                     | Messenger identity `externalId + max` resolved by `resolveActiveOrganizationIdForMessengerIdentity` in `apps/integrator/src/app/routes.ts`                | MAX webhook wraps `eventGateway.handleIncomingEvent(event)` in `runWithOrganizationPrincipal(organizationId, handleEvent)` when a single active organization is resolved.                                                                       | `apps/integrator/src/integrations/max/webhook.test.ts`                                                                                                  |
| BersonCare request-contact M2M          | `apps/integrator/src/integrations/bersoncare/requestContactRoute.ts`                                                  | M2M payload recipient identity `recipientId + channel` resolved by `resolveActiveOrganizationIdForMessengerIdentity`.                                     | Dispatch is wrapped in `runWithOrganizationPrincipal(organizationId, dispatchContact)` when the recipient has a single active organization.                                                                                                     | `apps/integrator/src/integrations/bersoncare/requestContactRoute.test.ts`                                                                               |
| BersonCare reminder-rules M2M           | `apps/integrator/src/integrations/bersoncare/reminderRulesRoute.ts`                                                   | M2M payload `integratorUserId` resolved by `resolveActiveOrganizationIdForIntegratorUserId`.                                                              | Rule write is wrapped in `runWithOrganizationPrincipal(organizationId, writeRule)` when the target user has a single active organization. The repository also stamps rule/grant rows from the current principal or single-active-user fallback. | `apps/integrator/src/integrations/bersoncare/reminderRulesRoute.test.ts`; `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-integrator-reminder-org.mjs`   |
| Rubitime webhook / Rubitime M2M (retired) | Removed with Rubitime retirement 2026-07-27; surviving historical source map: [`T0_4_RUBITIME_APPOINTMENT_ORG_AUDIT.md`](../../archive/2026-07-rubitime-retirement/SAAS_FOUNDATION/T0_4_RUBITIME_APPOINTMENT_ORG_AUDIT.md) | Historical finding: the removed entrypoints had no per-request organization principal. This is not a current runtime path. | The former unscoped compatibility projections are retirement history, not a live cutover risk or SCOPED writer authority. | Archived audit only. The former checker was not retained; no executable proof is invented. |
| Scheduler tick                          | `apps/integrator/src/infra/runtime/scheduler/main.ts`                                                                 | Scheduler itself emits `schedule.tick` without a tenant principal. Reminder handlers derive org from each rule/occurrence row.                            | `reminders.planDue` and `reminders.dispatchDue` bucket writes per organization and persist each bucket through `runWithOptionalOrganizationPrincipal`.                                                                                          | `apps/integrator/src/kernel/domain/executor/handlers/reminders.tenantContext.test.ts`                                                                   |
| Runtime worker: outgoing delivery queue | `apps/integrator/src/infra/runtime/worker/outgoingDeliveryWorker.ts`                                                  | Reminder delivery writes resolve org from occurrence; doctor broadcast audit writes resolve org from audit row.                                           | Reminder dead/blocked/log writes run under occurrence org; broadcast audit counters and notification attempts run under audit org. Queue status updates are technical queue state and remain outside a business principal.                      | `apps/integrator/src/infra/runtime/worker/outgoingDeliveryWorker.test.ts`                                                                               |
| Runtime worker: projection outbox       | `apps/integrator/src/infra/runtime/worker/projectionWorker.ts`                                                        | Projection worker does not create tenant ownership; it emits pending outbox payloads and updates technical outbox state.                                  | No SCOPED business row writer was found in the projection worker.                                                                                                                                                                               | Source audit in `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-entrypoint-org-map.mjs`                                                                  |
| Runtime worker: generic retry jobs      | `apps/integrator/src/infra/runtime/worker/jobExecutor.ts`                                                             | Retry jobs dispatch already-materialized outbound intents and update technical job status.                                                                | No SCOPED business row writer was found in the generic retry executor.                                                                                                                                                                          | Source audit in `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-entrypoint-org-map.mjs`                                                                  |

## SCOPED writer trunk disposition

| SCOPED surface                                                                                                                                           | T0.4 disposition                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `integrator.conversations`, `integrator.conversation_messages`, `integrator.message_drafts`, `integrator.user_questions`, `integrator.question_messages` | Support transport writers derive org from messenger identity or copy from parent row; legacy merge re-stamps moved children. Guard: `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-integrator-transport-org.mjs`.                                                                                                                                                                                       |
| `integrator.user_reminder_rules`, `integrator.user_reminder_occurrences`, `integrator.user_reminder_delivery_logs`, `integrator.content_access_grants`   | Rule/grant writers stamp from principal or single-active target user; occurrence/log writers copy from parent. Scheduler dispatch buckets writes per organization. Guard: `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-integrator-reminder-org.mjs`.                                                                                                                                                  |
| `integrator.contacts`                                                                                                                                    | Phone fallback writes stamp from principal or single-active canonical user; conflict updates preserve existing org when unresolved. Guard: `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-integrator-contacts-org.mjs`.                                                                                                                                                                                 |
| `integrator.user_subscriptions`, `integrator.mailing_logs`                                                                                               | Writers stamp from principal or single-active canonical user; conflict updates preserve existing org when unresolved. Guard: `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-integrator-mailing-org.mjs`.                                                                                                                                                                                                |
| `integrator.mailings`                                                                                                                                    | P0.4.I4 added and backfilled `organization_id` on the table, but no live runtime insert/update writer is exported or referenced under `apps/integrator/src`. Current runtime writes only `integrator.mailing_logs`; if a `mailings` writer is introduced, it must derive organization context before T0.4 remains green. Guard: `docs/_TODO/SAAS_FOUNDATION/scripts/check-t0-4-entrypoint-org-map.mjs`. |

## Residual decisions not closed by this map

- Rubitime multi-organization ingress is still a later cutover decision; current T0.4 only documents the legacy/default-org posture.
- `integrator.contacts` fallback removal and `integrator_linked_phone_source=public_only` remain owner-gated follow-up work.
- Queue/outbox retention cleanup remains operational cleanup, not business-data migration.
