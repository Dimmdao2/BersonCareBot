# Кабинет оплаты — план

Задача: довести оплату от «платёж проходит» до **рабочего кабинета**, где деньгами управляют, а не
только принимают. Владелец 01.08: «ЮKassa — надо сделать полноценный кабинет только: с возвратами,
статистикой, ручным и авто выставлением счетов, автосписанием и тд».

Соседние планы: [`SAAS_BILLING_PLAN.md`](SAAS_BILLING_PLAN.md) — приём оплаты и снятие моков (пункты
B0.x, доводится сейчас); [`TARIFFS_PAYMENTS_ADMIN_PLAN.md`](TARIFFS_PAYMENTS_ADMIN_PLAN.md) — тарифы и
механики. Этот план — про **управление деньгами после того, как платёж научился проходить**.

---

## 1. Решения владельца (актуальные, с цитатами)

- **Р-1 (01.08).** «ЮKassa — надо сделать полноценный кабинет только: с возвратами, статистикой, ручным и
  авто выставлением счетов, автосписанием и тд». Объём задан этим перечислением.
- **Р-2 (01.08).** «моки удаляй и делай полноценное апи для юкассы — на нём и проверишь». Мок-провайдер
  снят, проверка — прогоном через тестовый магазин.
- **Р-3 (01.08).** «цель — открыть кабинет и видеть рабочие экраны. Безопасность — единый порт, стены».
  Приёмка каждого этапа — экран, который открывается и работает, а не зелёные прогоны.
- **Р-4 (01.08).** «переводи руками через поиск всё, что размазано, на один порт — единые точки импорта,
  экспорта, доступа». Новых гейтов и списков исключений не заводить.
- **Р-5 (01.08).** «тесты нужны там, где они необходимы и без них никак… если для этого надо хуй знает
  что накрутить вокруг — то проще пойти и посмотреть». Правило: `AGENTS.md` §10a.

## 2. Порядок поверхностей — решено владельцем

«Кабинет оплаты» — это две разные поверхности, они не пересекаются ни по правам, ни по данным:

- **(А) Кабинет клиники** — клиника видит платежи СВОИХ пациентов: возвраты пациенту, статистика выручки,
  счета пациенту, автосписание за абонемент. Деньги идут клинике, ключи эквайринга клиники.
- **(Б) Кабинет платформы** — мы видим, как клиники платят НАМ за тариф: счета клинике, автопродление
  подписки, статистика по клиникам, возврат клинике. Деньги идут нам, ключи наши.

**РЕШЕНИЕ ВЛАДЕЛЬЦА 01.08, дословно: «првым кабинет платформы (оплата saas-тарифов)» — то есть (Б).**
Рекомендация агента была обратной ((А), потому что оплата пациентом уже доведена до конца); владелец
выбрал (Б), и это решает порядок. (А) не отменяется, а идёт следом.

⚠️ **Следствие, которое меняет первый этап.** (Б) показывает платежи клиник за тариф — а их сегодня нет
ни одного, потому что заплатить за тариф пока нельзя: домен `modules/saas-billing`, таблицы
`saas_billing_*`, ключ провайдера и создание счёта существуют, но **ни один маршрут не зовёт
`createRenewalSaasBillingInvoice()`**, и `source="paid_subscription"` не ставит ни одна строка продуктового
кода — только `"manual"`. Поэтому первым этапом идёт не список, а **К0: клиника может заплатить за тариф**;
без него кабинет платформы будет пустым экраном по построению. Проверено по коду 01.08, см.
`SAAS_BILLING_PLAN.md` §Phase 4, метки «УТОЧНЕНО 08-01».

## 3. Что уже есть (проверено 01.08, не переделывать)

- Адаптер ЮKassa ходит в REST API v3 напрямую: `POST /v3/payments`, `POST /v3/refunds`,
  `GET /v3/payments/{id}` — Basic-авторизация `shopId:secretKey`, заголовок `Idempotence-Key`.
  Официального SDK для Node у ЮKassa нет (официальные только PHP и Python), поэтому прямые вызовы —
  это и есть мировая практика, а не самодеятельность.
- Приём уведомлений: адрес отправителя сверяется со списком подсетей ЮKassa, платёж перезапрашивается
  по API, статус и сумма берутся из ответа. Подписи у ЮKassa нет — проверять нечего.
- Ссылка на оплату доходит до клиента, запись намерения её хранит, статус отдаётся во всех четырёх
  доменах покупки.
- Тестовый магазин рабочий (`shopId 1425962`), ключ в `system_settings`, платежи помечаются `test`.

## 4. Этапы

Порядок задан решением владельца (§2): сначала поверхность платформы. К0 — новый первый этап, без него
кабинет платформы показывать нечего.

### К0. Клиника может заплатить за тариф — фундамент кабинета платформы

_Приземлено 01.08 (`ade4bc66f` → `ab66b0e09`), слепой аудит PASS. Живая проверка лидом: сессия владельца
клиники, `/app/settings` вкладка оплаты — 200, кнопка «Оплатить тариф» на экране есть._

- [x] Маршрут выставления счёта за тариф зовёт существующий `createRenewalSaasBillingInvoice()`
      (`modules/saas-billing/service.ts`) и возвращает ссылку на оплату. Сегодня эту функцию не зовёт
      никто — сама она написана и проверена, дописывать её не надо.
- [x] Экран оплаты тарифа расширяет существующую `app/settings/BillingSection.tsx` (зона MGMT-08), а не
      заводит второй компонент рядом — так записано в `SAAS_BILLING_PLAN.md` Phase 4.
- [x] Успешный захват платежа продлевает подписку с `source="paid_subscription"`; ручное назначение
      тарифа администратором платформы не перетирается истёкшей подпиской молча.
- [x] Ключи платформенного магазина живут в `saas_billing_payment_provider` (`system_settings`), отдельно
      от клинического `booking_payment_providers` — платформенный мерчант и клинический не путать.
- [x] ⛔ Инвариант владельца: оплата тарифа НИКОГДА не гейтится лестницей доступа. Заблокированная клиника
      обязана иметь возможность заплатить и разблокироваться. Здесь тест обязателен — отказ дорогой и
      молчаливый (клиника не может заплатить и не понимает почему).

### К1. Список платежей — основа кабинета

**Первым делается поверхность платформы** (решение владельца, §2): список того, как клиники платят НАМ за
тариф. Источник — счета и подписки `saas_billing_*`, которые К0 наконец начал наполнять. Поверхность
клиники (платежи её пациентов из `be_payment_intents`) — та же работа следом, отдельным этапом.

_Приземлено 01.08 (`7763d8e9a` → `c8c6941c0`), слепой аудит PASS. Живая проверка лидом: сессией
администратора платформы `/app/admin/payments` — 200, колонка «Плательщик» на странице есть; той же
страницей сессией врача — 307, данных ноль._

- [x] Экран со списком платежей: дата, сумма, статус, плательщик (клиника), за что (тариф и период),
      провайдер. Фильтры по периоду и статусу, поиск по плательщику.
- [x] Данные берутся из НАШЕГО журнала платежей, не из ЮKassa: журнал — источник истины,
      провайдер — источник подтверждения.
- [x] Стены: кабинет платформы виден только администратору платформы. Клиника не открывает его вовсе и
      не видит чужие платежи по прямой ссылке — это отказ дорогой и молчаливый, здесь тест обязателен.
      _(Для поверхности клиники то же правило читается как «список org-scoped всегда».)_

### К2. Возвраты

_Построено и приземлено 01.08 (`79c4a95e5` → `26275a108`), слепой аудит PASS по шести поломкам. Галочки
ниже НЕ проставлены сознательно: приёмка этапа — реальный повтор на оплаченном счёте, а его нельзя
провести на dev (подтверждение возврата приходит вебхуком на публичный адрес, которого у dev нет).
Закрывается на TEST одним прогоном вместе со второй половиной приёмки B0.3._

- [ ] Полный и частичный возврат с экрана платежа. Сумма возврата не превышает остаток по платежу.
      _Построено; ограничение суммы проверено поломкой на аудите._
- [ ] Возврат уходит в `POST /v3/refunds` с ключом идемпотентности; повторное нажатие не создаёт второй
      возврат — идемпотентность проверяется реальным повтором, а не только чтением кода.
      _Построено: строка возврата бронируется под уникальным ключом провайдера в одной транзакции с
      блокировкой счёта. ⛔ Реальным повтором НЕ проверено — только это и осталось._
- [ ] Статус возврата приходит уведомлением и обновляет журнал. Пока не подтверждён — «в обработке»,
      а не «возвращено».
- [ ] Право на возврат — отдельная способность, не «все, кто видит платежи». Кто именно — решает
      владелец при приёмке этапа.

### К3. Статистика

_Приземлено 01.08 (`091e24928` + круг 2 `728059800` → `3b76dff21`). Слепой аудит: цифры сводки сложены
вручную против строк списка и сошлись поштучно, включая счета ровно на границах периода; сверка выдала
все три вида расхождений, а контрольная сумма журнала до и после трёх прогонов совпала. Один дефект
(сверка отдавала пустоту вместо причины) закрыт кругом 2. Живая проверка лидом на общей ветке против
настоящего тестового магазина: сверка ответила `ok`, 7 записей журнала против 2 у провайдера,
расхождения перечислены; при намеренно сломанной настройке провайдера — названная причина отказа, а не
пустое тело._

- [x] Сводка за период: принято, возвращено, в обработке, не оплачено. Разрез по видам покупок.
- [x] Сверка с провайдером: расхождение между нашим журналом и ЮKassa показывается явно, а не
      прячется. Тянется через `GET /v3/payments` списком за период.
- [x] Выгрузка периода файлом для бухгалтерии.

### К4. Счёт вручную

_РЕШЕНИЕ ВЛАДЕЛЬЦА 01.08, дословно: «счета в тестовом не включить - потом проверим». Функция «Счета» у
тестового магазина ЮKassa не включена (`POST /v3/invoices` отвечает `403 forbidden` при рабочих ключах —
`POST /v3/payments` тем же ключом даёт `200`). Это настройка мерчанта, не код. Значит: приложение обязано
отказывать честно и НЕ подменять счёт обычным платежом; галочка про реальный `POST /v3/invoices`
закрывается позже, когда владелец включит функцию. Остальные части этапа от этого не зависят._

- [ ] Выставление счёта из кабинета: сумма, за что, кому, срок действия. Уходит в
      `POST /v3/invoices`, ссылка на оплату возвращается в ответе и отдаётся получателю.
      _⛔ Заблокировано настройкой магазина, см. решение выше. Проверяется позже._
- [x] Счёт живёт в нашем журнале со статусами: выставлен, оплачен, просрочен, отменён.
      _Приземлено 01.08 (`d2a500a09` → `2ff39cafe`, миграция 0292). Слепой аудит прогнал живьём: двойная
      отмена отвечает `409 invoice_not_cancellable`, отмена несуществующего — `404`._
- [ ] Оплата счёта подтверждается уведомлением провайдера, а не открытием ссылки.
      _⛔ Ждёт включения функции «Счета» у магазина (решение владельца выше). Путь построен и проверен по
      коду: `markSaasBillingInvoicePaid` зовётся ровно из приёмника уведомлений и ниоткуда больше._
- [x] **Повтор не создаёт второй счёт.** _Приземлено 01.08 (`57574f9fe`). Ключ идемпотентности выводится
      из самого запроса, а не рождается через `randomUUID()` на каждый вызов; защита стоит в одном месте
      (`insertSaasBillingInvoiceIdempotent`) и общая с продлением тарифа. Слепой аудит проверил живым
      повтором: три одинаковых запроса — один счёт, три разных — три счёта, гонка удерживается._
- [ ] **Залипший черновик не блокирует повтор после сбоя провайдера.** _Находка слепого аудита 01.08,
      сегодня не проявляется. Первый неудачный поход к провайдеру оставляет строку-черновик, и повторная
      отправка ТОГО ЖЕ запроса больше никогда не пытается дойти до провайдера — возвращает
      `saas_billing_checkout_unavailable`, читая старую пустую строку. Обойти можно только изменив любое
      поле, что оператору кабинета не очевидно. Проявится в первый же день после включения функции
      «Счета» — закрывать тогда же, вместе со строкой про подтверждение уведомлением._

### К5. Счёт автоматически

_Приземлено 01.08 (`08ff2a342` → `5ba5513d5`), слепой аудит PASS. Проверка шла не моком, а одноразовым
кластером PostgreSQL 16 со схемой `saas_billing_*`, дословно взятой из миграции 0259._

- [x] Правило выставления: за что и с какой периодичностью счёт выставляется сам (продление абонемента,
      продление тарифа). _Фоновая работа проходит по подпискам с истёкшим оплаченным периодом; новый
      период начинается ровно с конца оплаченного, а не с момента прогона — иначе поздний тик дарил бы
      клинике лишние дни. Граница проверена: подписка, кончающаяся ровно в момент прогона, в отбор
      попадает, кончающаяся через секунду — нет._
- [x] Выставление идёт фоновой работой через существующую очередь, а не по запросу пользователя.
      Фильтрация — на постановке в очередь, под принципалом (канон стен для фоновых работ).
      _Проверено ролями: тот же запрос без `SET ROLE app_platform_settings` получает `permission denied`
      от Postgres, а не пустой список._
- [x] Повтор выставления в тот же период не создаёт второй счёт. _Два тика подряд одним моментом:
      первый — 2 счёта, второй — 0. Держится уникальным индексом, а не проверкой «а выставляли ли уже»:
      со снятым ограничением INSERT перестаёт выполняться вовсе, то есть отказ явный, а не тихий дубль._
- [ ] ⚠️ **Не защищено проверкой:** инвариант «новый период начинается с конца оплаченного, а не с
      момента прогона». Названо аудитором: мутация кода на `now()` прошла 5/5 зелёными. Шиппед-код
      считает верно (доказано живым прогоном), но регрессия сюда пройдёт незамеченной.

### К6. Автосписание
- [ ] При первой оплате запрашивается сохранение способа оплаты (`save_payment_method: true`);
      после успеха ЮKassa отдаёт `payment_method.id`, который мы храним привязанным к плательщику.
- [ ] Последующее списание — `POST /v3/payments` с `payment_method_id`, без участия плательщика.
- [ ] Согласие плательщика на автосписание — явное, хранится с датой и текстом, отзывается им же
      в один клик. Без этого автосписание не включается.
- [ ] Неудачное списание не молчит: плательщик и клиника узнают, и есть понятный следующий шаг
      (оплатить вручную по ссылке).

### К7. Фискализация — вопрос владельцу, не наша выдумка
- [ ] 54-ФЗ требует чек на каждый платёж. ЮKassa умеет пробивать чеки сама, если магазин подключён к
      их онлайн-кассе, и тогда в платёж передаётся объект `receipt` с составом покупки и признаками.
      **Нужно решение владельца:** чеки пробивает ЮKassa или у клиники своя касса. От ответа зависит,
      передаём ли мы состав корзины в каждый платёж.

## 5. Приёмка

Каждый этап закрывается **прогоном на тестовом магазине и открытым экраном**, а не отчётом и не
зелёными тестами: платёж тестовой картой, возврат, счёт, автосписание — по своему этапу. В план
пишется, что именно проведено и что видно на экране.
