# Независимый адверсарный аудит Э5a — почта при входе в кабинет

Дата: 2026-09-16. Ветка: `wt/e5-email-gate`. Аудируемый кандидат: `2f37381dd` + текущий merge `7ee7d7bc`.

## Вердикт

**FAIL.**

### MUST FIX-1 — API и server actions пациента обходят email gate после 14 дней

Authority: `docs/_TODO/MERGE_MECHANISM_REWRITE_2026-09-14.md:177-183` — просьба, требование через 14 дней, затем отказ без почты; `docs/_TODO/E5_EMAIL_AT_LOGIN_GROUND_2026-09-15.md:194-197` — до завершения разрешены только экран привязки, повторная отправка кода, поддержка и выход, клинические данные не показываются.

Код ставит email gate только в RSC layout кабинета: `apps/webapp/src/app/app/patient/layout.tsx:98-123`. Но route handlers под `/api/patient/*` идут через `requirePatientApiBusinessAccess`, где проверяется только сессия, роль `client` и `patientClientBusinessGate`; email decision там не вызывается: `apps/webapp/src/app-layer/guards/requireRole.ts:954-980`. Пример живого patient API: `apps/webapp/src/app/api/patient/messages/unread-count/route.ts:9-16`. Server actions идут через `requirePatientAccessWithPhone`, где тоже нет email gate: `apps/webapp/src/app-layer/guards/requireRole.ts:58-70`; пример: `apps/webapp/src/app/app/patient/diary/symptoms/actions.ts:22-26`.

Достижимый сценарий: у пациента есть session cookie, `email_first_requested_at` старше 14 дней, подтверждённой почты нет. Открытие `/app/patient/messages` редиректит на `/app/patient/bind-email`, но прямой `GET /api/patient/messages/unread-count` с той же cookie вернёт данные, потому что API-гейт не знает про email deadline. Это обход требования "не заходят" вне списка исключений.

### MUST FIX-2 — текущий тест не ловит, что layout перестал заводить часы первой просьбы

Authority: `docs/_TODO/MERGE_MECHANISM_REWRITE_2026-09-14.md:185-188` — не хватает точки перехода и часов, считающих 14 дней с первой просьбы; реализованный этап обязан закрывать оба пункта.

Существующий тест `apps/webapp/src/modules/platform-access/patientEmailGatePolicy.unit.test.ts` проверяет чистую функцию, но не wiring layout → `loadPatientEmailGateState(true)`. Инъекция `apps/webapp/src/app/app/patient/layout.tsx:111` `loadPatientEmailGateState(true)` → `loadPatientEmailGateState(false)` оставила targeted test зелёным: `/home/dev/brain/host-orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run src/modules/platform-access/patientEmailGatePolicy.unit.test.ts"` → `13 passed`.

Это не доказывает текущую реализацию неверной: код сейчас часы ставит. Это finding по ложному acceptance evidence: заданная владельцем поломка "не ставить отметку часов" не поймана ничем.

## Проверки по вопросам

1. **Заперт ли человек?** В policy есть исключения для `/app/patient/bind-email`, `/app/patient/profile`, `/app/patient/support`, `/app/patient/help`, `/app/patient/install`, `/api/auth/logout`, `/legal` (`patientRouteApiPolicy.ts:28-36`). Тест ловит удаление `/app/patient/bind-email`. По коду layout не должен запереть человека вне экрана почты. Живой browser-проход на owner-пациенте не выполнен: DEV отвечает (`curl -I http://127.0.0.1:5200/app` → `HTTP/1.1 200 OK`), но доступного browser runner как Node-модуля в clone нет (`node -e "require('playwright')"` → `MODULE_NOT_FOUND`).
2. **Обходится ли гейт?** Да: `/api/patient/*` и patient server actions, см. MUST FIX-1. Поиски: `code-search "patient login doors telegram-login magic code oauth passkey redirect"`, `rg "persistNewAuthSession|setSessionFromUser|buildSession"`, `find apps/webapp/src/app/app/patient ...`, `find apps/webapp/src/app/api/patient ...`, `rg "requirePatientApiBusinessAccess|requirePatientAccessWithPhone"`.
3. **Часы.** Первая отметка в БД ставится через `COALESCE(target.email_first_requested_at, statement_timestamp())`, то есть не переставляется (`20260915T202200...sql:38-46`). При подтверждённой почте policy возвращает `none` до чтения/значения отметки. Точное 14-дневное равенство — blocking (`>=`). Время считается миллисекундами UTC; перевод летнего/зимнего времени не влияет. Будущая отметка трактуется как grace-period до наступления `future + 14 days`; отдельной защиты от clock skew нет.
4. **Права и миграция.** В миграции нет `GRANT/REVOKE/CREATE POLICY` (`rg -n "GRANT|REVOKE|CREATE POLICY|ALTER ROLE|CREATE ROLE|ALTER DEFAULT PRIVILEGES" ...` → no matches). Declaration содержит capability `patient.email-gate.state`, definer owner и relation access; generated artifacts содержат EXECUTE только для `app_patient`. `check-migration-privileges` PASS, migration order PASS, rollback-only preflight PASS с `--runtime-env-root /home/dev/dev-projects/BersonCareBot`.
5. **Не задет ли кто-то ещё.** `/app/patient/layout.tsx:76-78` сначала отсекает не-patient роли через `canAccessPatient`; `canAccessPatient` возвращает true только для `client` (`modules/roles/service.ts:3-4`). Doctor/admin/staff не попадают под email gate на своих layouts. Branded host использует тот же `/app/patient` layout, значит gate касается только patient role.
6. **Тест исполнителя по линейке 15.09.** Существующий unit проверяет выход чистой policy по независимому owner-oracle: confirmed email, first request, day 14, day 40, escape paths. Это не UI-тест и не проверка текста исходника. Он полезен, но недостаточен: не проверяет wiring layout/DB clock.

## Fault Injection

- `>=` → `>` на 14-дневной границе: **поймано** `day 14 -> requirement` (`expected requirement, received request`).
- Убрать `/app/patient/bind-email` из исключений: **поймано** `never blocks the escape path /app/patient/bind-email`.
- Не ставить отметку часов в layout (`true` → `false`): **не поймано** targeted test остался зелёным — MUST FIX-2.
- Снять проверку `session.user.role === 'client'` на email-gate блоке layout: **не поймано** targeted test остался зелёным; продуктовый blast radius ограничен соседним `canAccessPatient`, поэтому отдельного MUST FIX нет.
- Возвращать `requirement` вместо `none` при подтверждённой почте: **поймано** `confirmed email -> none`.

Все временные production-code инъекции откатаны; `git status --short` перед созданием отчёта был чистым.

## Запущенные проверки

- `/home/dev/brain/host-orch/run-tests.sh "pnpm --dir apps/webapp exec vitest run src/modules/platform-access/patientEmailGatePolicy.unit.test.ts"` → PASS, `13 passed`.
- `/home/dev/brain/host-orch/run-tests.sh "pnpm --dir apps/webapp exec node ../../scripts/check-migration-privileges.mjs"` → PASS, `check-migration-privileges: OK (229 migration files)`.
- `/home/dev/brain/host-orch/run-tests.sh "bash apps/webapp/scripts/check-drizzle-migration-order.sh"` → PASS.
- `/home/dev/brain/host-orch/run-tests.sh "bash deploy/host/migrate-dev.sh --preflight --runtime-env-root /home/dev/dev-projects/BersonCareBot"` → PASS, `pending=1`, rollback-only.

Полный CI не запускался по брифу. DEV migration execute не запускался.
