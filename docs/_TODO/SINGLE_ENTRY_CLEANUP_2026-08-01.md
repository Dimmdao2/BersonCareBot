# Единый проход к ресурсу: уборка путей в обход общей двери

## Проблема

К одному и тому же ресурсу в коде ведёт несколько путей. Пока каждый путь написан верно, это незаметно.
Ошибка появляется в момент, когда кто-то заводит новый путь и не повторяет в нём проверку — и это **не
поломка, а валидный код**, поэтому обнаружить её нечем: ни тест, ни ревью не знают, что путь должен был
пройти через дверь.

Что это стоит в этом репозитории на сегодня: один маршрут принимает файл вообще без проверки, потому что
проверка написана в других маршрутах руками; байты из хранилища можно получить в обход общего барьера;
решение об одном и том же лимите принимается по-разному в двух местах; сообщение человеку ставится в очередь
пятью независимыми способами. Ни одно из этих мест сегодня не «сломано» — они просто не защищены.

Правило, по которому это чинится, записано в [`AGENTS.md` §5](../../AGENTS.md#5-clean-architecture-изоляция-модулей)
«Один общий проход, и мимо него нельзя». Выполненным пункт считается только когда есть **единая точка** И
**механическая проверка**, роняющая сборку на попытке пройти мимо. Договорённость «все зовут общий помощник»
выполнением не является — именно она и привела к нынешнему состоянию.

## Действующие решения

- **Владелец, 01.08:** «НЕ ДЕЛАТЬ ДУБЛИ И МНОЖЕСТВЕННЫЕ ИМПОРТЫ ТАМ ГДЕ МОЖНО СДЕЛАТЬ ОДИН ОБЩИЙ и
  заблокировать проход мимо. НЕ ДЕЛАТЬ МНОГО ПУТЕЙ ОБРАЩЕНИЙ К БД — ВСЕ ПУСКАТЬ ЧЕРЕЗ ОДИН ЗАЩИЩЕННЫЙ ПОРТ.
  И Т Д — ЭТО ПРИНЦИП.»
- **Владелец, 01.08 (о форме гейта):** «не гейт подключать к каждому маршруту, а гейт сделать так, что сама
  сущность маршрута строится на нём — то есть без него маршрут вообще не работает». Общее правило блока:
  предпочитать конструкцию, при которой обход не компилируется, гейту, который перечисляет места.
- **Лид, 01.08:** порядок пунктов — по цене молчаливой ошибки, не по объёму работы. Каждый пункт закрывается
  парой «единая точка + гейт с самотестом»; гейт без самотеста не принимается.
- **Владелец, 01.08:** делать сразу как надо — обход переводится на порт в том же заходе, а не
  вписывается в список отложенного. Новые записи в аллоулисты не заводятся; существующие только снимаются.

## Источник списка

Первичный `runs/testsuite-v2/CHOKEPOINT_SWEEP_REPORT.md` в git отсутствует:
`git log --all --oneline -- docs/_TODO/runs/testsuite-v2/CHOKEPOINT_SWEEP_REPORT.md` даёт пустой вывод.
Поэтому его числа не считаются доказательством и каждый пункт перемеряется по актуальному коду до брифа. Для Ч1
команда `rg -l --glob 'route.ts' 'presignPutUrl|s3CreateMultipartUpload|request\.formData\(\)' apps/webapp/src/app/api | sort`
дала шесть intake-маршрутов. Для Ч2 точные актуальные команды и исправленная классификация записаны в брифе
`runs/briefs/CH2_MEDIA_DELIVERY_CHOKEPOINT_BRIEF.md`.

## Чек-лист

- [ ] **Ч1. Валидация загрузки файлов.** Шесть intake-маршрутов и три контекстные политики. Реальные разрывы:
      `doctor/patients/[userId]/files` принимает любой MIME/положительный заявленный размер и создаёт
      `media_files.status=ready` ДО PUT; generic/individual confirm проверяет наличие объекта, но не совпадение
      фактического размера и типа; direct-to-S3 пути доверяют header MIME без проверки сигнатуры. Следствие:
      размер можно заявить как 1 byte и обойти per-file/quota policy, повреждённый объект становится `ready`, а
      сбой presign/PUT оставляет врачу «готовый» файл без байтов и съедает квоту. Готово = одна двухстадийная
      дверь в существующем `modules/media` (intent + фактически полученный object), контекстные лимиты сохранены,
      `ready` недостижим без validated received-object result, все шесть путей на двери, structural gate с
      self-test запрещает седьмой обход.
- [ ] **Ч1б. Storage commit/cleanup после разрыва upload.** Отдельный этап, найденный проходом по тем же живым
      путям, не маскируется под валидацию: proxy пишет S3 до DB и при DB-сбое оставляет orphan; single-PUT после
      PUT/до confirm оставляет pending row + object без cleanup; patient confirm при невалидном HEAD теперь
      отвечает ошибкой, но оставляет связанные `patient_files` + `media_files.pending` + object. Готово = единый
      commit/abort lifecycle на существующих `media_files`/storage ports и
      доказанный cleanup каждого из трёх достижимых сбоев; новой таблицы или второго upload-service нет.
- [ ] **Ч2. Выдача медиа из хранилища.** Актуальный замер: семь GET-поверхностей; пять
      `/api/media/[id]/**` handler'ов копируют одну ACL-последовательность; текущего пути выдачи `media_files`
      без проверки организации не найдено, но шестую копию ничто не запрещает. Отдельно online-intake при
      нестандартной конфигурации может вернуть бессрочный public-bucket URL. Исходные «21/4» считали все S3
      imports (upload/cleanup/worker/purge), а не delivery bypasses. Готово = одна дверь ACL для пяти handler'ов,
      structural gate с self-test и отсутствие public-URL fallback; `patient_files` остаётся отдельным ресурсом.
- [x] ~~**Ч3. Постановка фоновых работ.**~~ **ОТДАНО СОСЕДНЕМУ СЕАНСУ 01.08 — не мой код.** Обе точки
      обхода общей абстракции `QueuePort` лежат в интеграторе (`integrations/bersoncare/bookingLifecycleRoute.ts`,
      `infra/db/writePort.ts`), это воркстрим интегратора. Из пяти очередей в вебаппе только перекодировка
      медиа, и обходов там нет. Пункт попал сюда механически: перепись чокпойнтов шла по всему репозиторию,
      а строки перенесены в план без разбора, чей это код.
- [ ] **Ч4. Настройки: три файла читают базу мимо общего читателя.** Канонический читатель
      `modules/system-settings/configAdapter.ts` (кэш → база → значение из конфигурации), у него 36
      потребителей. Мимо ходят `infra/repos/pgBookingEngine.ts`, `pgBookingScheduling.ts`,
      `pgProductAnalytics.ts` — читают базу напрямую, без кэша и без запасного значения. Последствие:
      обходящий код видит не то же, что остальное приложение, а при недоступности базы — ничего.
      Готово = три файла переведены на общего читателя.
- [ ] **Ч4б. Квоты: дубль логики в SQL — инженерный этап, не owner-gate.** Резолвер прав один
      (`modules/org-entitlements/service.ts`), но проверка квоты В МОМЕНТ ЗАПИСИ продублирована в SQL
      (`infra/repos/stockQuotaCheck.ts` + инлайн в `pgOrganizationInvites.ts`), и в комментариях кода это
      признано зеркалом. Причина настоящая: чтобы двое не заняли последний слот одновременно, проверка
      обязана идти внутри транзакции с блокировкой, чего резолвер на JS не умеет. Убрать дубль = потерять
      атомарность. **Уточнение владельца 01.08:** техническую архитектуру выбирает аудит/исполнитель, владелец
      задаёт продуктовое поведение. Действующее продуктовое поведение уже определено тарифным планом; задача
      этапа — свести enforcement в один transaction-aware quota-port/DB-resolver и сохранить UI read-проекцию,
      не ослабив lock/атомарность. Файлы временно не трогать, пока в них работает соседний тарифный workstream.
- [ ] **Ч7. Значение настройки живёт в базе, в коде его нет (владелец, 01.08).** Сегодня у каждой
      настройки есть значение, зашитое в исходник (`modules/system-settings/runtimeConfig.ts`, таблицы
      `*_DEFAULTS`), и оно подставляется молча, когда чтение из базы не удалось. Отсюда два свойства, оба
      плохие: «в базе нет строки» и «не смог прочитать» неразличимы, а сбой чтения тихо меняет поведение
      системы — для `auth_2fa_enabled` это означает «второй фактор выключен» без единого следа.
      **Готово =** строка на каждую настройку заводится миграцией (начальное значение — данные в базе, а не
      константа в коде); таблицы `*_DEFAULTS` удалены; читатель берёт значение только из базы.
      **Что делать при недоступной базе (владелец, 01.08, дословно): «База моргнула — мы ничего не требуем.
      Мы просто не пускаем пока не поднимется».** То есть отказ обслуживать (недоступность + алерт), а НЕ
      подмена политики. Разница существенна: подменять политику на «строгую» нельзя — включённый вслепую
      второй фактор запирает весь персонал, у кого он не настроен, такой инцидент уже был 25.07 и описан в
      комментарии `app-layer/guards/requireRole.ts`. Мы не меняем правило, мы не отвечаем.
      **Исключение, оставленное владельцем (01.08):** параметры, которые не являются продуктовым поведением
      и нужны до того, как база отвечает, — TTL и задержки в настройках безопасности. Они остаются
      константами в коде. Всё остальное продуктовое поведение — из базы.
- [x] **Ч5. Импорт репозиториев мимо DI.** Последствие — слой, которому положено получать зависимости,
      берёт их сам, и подменить их в тесте нельзя.
      ⚠️ **Замер «22» УСТАРЕЛ** (лид 01.08, перемерено по свежему feat после приземления пункта 3). Пункт 3
      (слой-импорты) и прежняя уборка сняли большинство. По факту осталось **10 value-сайтов**, из них 2 вне
      объёма: `modules/system-settings/configAdapter.ts` (аллоулист, ведёт ветка Ч7). Настоящий остаток — **8**:
      - `@/infra/repos` (5): `app/app/doctor/content/library/delete-errors/page.tsx` (`listMediaDeleteErrors`);
        `app/app/doctor/exercises/actionsShared.ts` (`pgListExerciseUsageForMediaIds`);
        `app/app/doctor/schedule/page.tsx` (`pgDoctorCalendarTimezonePort`);
        `app/app/patient/reminders/RemindersPageBody.tsx` и `app/app/patient/sections/[slug]/page.tsx`
        (`resolvePatientContentSectionSlug`);
      - `@/infra/db` (3): `runWithWebappDbOperationFamily` в `app/app/patient/diary/{page.tsx,PatientDiaryAuthenticatedMain.tsx}`
        и `app/app/patient/home/PatientHomeToday.tsx`.
      Классификация «настоящий обход vs законный порт-хелпер» (напр. `runWithWebappDbOperationFamily` может быть
      санкционированной дверью принципала, а не обходом) — за воркером, лид проверяет.
      **Приземлено `e4fd27bb6`:** три настоящих DI-обхода переведены на `buildAppDeps()`, два импорта
      `resolvePatientContentSectionSlug` — на модульный контракт; `runWithWebappDbOperationFamily` оставлен как
      санкционированная дверь принципала. Проверки и точный scope записаны в строке `5ca8860fb` общей доски.
- [x] ~~**Ч6. Настройки, которые ни на что не влияют.**~~ **ОТДАНО СОСЕДНЕМУ СЕАНСУ 01.08 (решение
      владельца).** Найденный экземпляр закрыт мной попутно (`9863f5b5b`): `auth_2fa_enabled` читался
      стражем страниц, но не маршрутом входа. Сама сверка «у каждого переключателя админки есть
      потребитель» уходит соседу.


## Порядок работ (владелец, 01.08)

1. **Сырой SQL и запросы мимо порта — вычистить полностью.** Целевое состояние названо владельцем:
   «сырого sql и запросов мимо порта не должно остаться вообще», исключения только там, где это предмет
   самого файла (миграции) или сознательное решение с названной причиной.
   - [x] **Вебапп закрыт** (`ea1611109`). Сырого `.query` вне `apps/webapp/src/infra/db/**` — ноль.
         Причина у всех трёх файлов была одна: параметр формы `.query(text, values)`, то есть вторая
         дверь к базе рядом с портом. Переходник к общему пакету `platform-merge` остался ОДИН и сам
         исполняет через порт. Гейт (решение владельца 01.08 «должен стоять гейт на query»):
         `check-no-new-raw-sql.mjs` больше не держит список файлов для вебаппа — сырой `.query`
         разрешён только внутри каталога порта. Проверен поломкой: прямой вызов и обход через
         `pool.query.bind(pool)` дают красный, внутри порта — зелёный.
   - [ ] **Осталось вне вебаппа, и это не «мелочь»:** `apps/media-worker/src` — 5 файлов, ничей
         объём работ, гейт на них не распространён. Интегратор (13 файлов) ведёт соседняя сессия.
         Пакеты `platform-merge` и `db-principal` принимают клиента формы `.query` параметром: они
         общие с интегратором и на порт вебаппа опираться не могут — это шов, но именно та форма,
         через которую обход и возможен.
   - [ ] **Отдельно и НЕ сделано: SQL как текст.** Порт исполняет `runWebappPgText`, то есть
         легаси-текст `$1..$n`, а не построитель запросов. «Запросы мимо порта» закрыты, «сырой SQL»
         как текст — нет; это другой и гораздо больший объём. Не считать пункт 1 закрытым целиком.
2. **Ч7 — значения настроек убрать из кода в базу** (решение владельца 01.08: «я не просил хардкод в
   настройках — я всё прошу перенести в базу»). Идёт сразу после SQL: это не уборка, а поведение, и оно
   про безопасность.
3. **Двенадцать импортов мимо границ слоёв** — перевести на порты (аллоулист линтера, оставшиеся записи).
4. **Ч5 — закрыт на фактическом остатке после повторного замера** (`e4fd27bb6`; исходное число 22 устарело).
5. **В9б — доказать прогоном, что без принципала данные не отдаются ни по одному пути** (план
   `TEST_SUITE_AUDIT_2026-07-29.md`).

## Порядок относительно тестового набора (лид, 01.08)

Уборка идёт **перед** тестами по той же поверхности, а не после. Причина: тест, написанный на дверь, мимо
которой можно пройти, доказывает только саму дверь — ровно та ошибка, что уже поймана на маршрутах
(В9 доказал два маршрута, а не класс).

| Пункт уборки | Идёт ПЕРЕД пунктом тестового плана | Почему |
|---|---|---|
| Ч2 (выдача медиа) | В8 (медиа и целостность карточки) | 21 потребитель; тестировать каждый бессмысленно, пока дверь обходится |
| Ч5 (импорты мимо DI) | вся масса unit-тестов | пока слой берёт зависимости сам, подменить их в тесте нельзя |
| Ч1 (валидация загрузок) | — | самостоятельная дыра, ждать нечего |
| Ч4 (настройки и квоты) | — | квоты решением владельца вне тестового набора |

## Границы

- Ведётся отдельно от тестового набора: там свой план
  [`TEST_SUITE_AUDIT_2026-07-29.md`](TEST_SUITE_AUDIT_2026-07-29.md). Пересечение одно — «маршрут строится на
  страже прав» (пункт В9б того плана), здесь не дублируется.
- Прод не трогается ничем.
- Каждый пункт: воркер → независимый слепой аудит → круг 2 по готовому списку. Приземление веткой через
  `tools/orch-launch.sh land`.
