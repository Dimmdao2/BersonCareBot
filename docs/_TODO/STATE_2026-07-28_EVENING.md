# Состояние на вечер 28.07 — точка подхвата для следующей сессии

Владелец закрыл ноут около 16:50, сессия прерывается. Здесь всё, что нужно, чтобы продолжить без него и
без моей памяти. Ветка `feat/doctor-ui-rebuild` **запушена** (`c63bcc280`), работа не потеряется.

## Где что лежит

| файл                                                             | зачем                                                                |
| ---------------------------------------------------------------- | -------------------------------------------------------------------- |
| `docs/_TODO/OWNER_PUNCHLIST_2026-07-28.md`                       | главный план. Этапы §1-§16, решения владельца, развилки              |
| `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`                | очередь аудита ночной волны, все вердикты, нарушения канона          |
| `docs/ORCHESTRATOR_CHECKLIST.md`                                 | 13 строк, перечитывать после каждого сжатия контекста                |
| `tools/orch-launch.sh`                                           | ЕДИНСТВЕННАЯ дверь запуска агентов; отказывает при нарушении условий |
| `tools/db-facts-snapshot.sh`                                     | снимок фактов живой базы для аудиторов (у них нет доступа к psql)    |
| `docs/_TODO/SAAS_FOUNDATION/QUOTAS_RESEARCH_2026-07-28.md (часть I)`  | разведка: что вообще ограничивать                                    |
| `docs/_TODO/SAAS_FOUNDATION/QUOTAS_RESEARCH_2026-07-28.md (часть II)` | разведка: механика счёта                                             |

## Что сделано 28.07

Девять слоёв независимого аудита ночной волны — **все девять FAIL**, все находки настоящие. Четыре круга
починки с независимой проверкой каждого. Сведено и запушено:

- утечка клинической переписки в консоль поддержки закрыта (страница и ручки удалены);
- слаг защищён на уровне базы (индекс по `lower(slug)`), доказано снятием индекса — смоук упал и восстановлен;
- защита входа: блокировка по перебору, и общий ограничитель чинился не в одном маршруте, а во всех двенадцати;
- §29: отдельная роль `app_clinic_billing`, у обычного персонала права на таблицы биллинга отозваны;
- квоты: недостающие гранты, без которых падало создание любой страницы вместо тарифного отказа;
- секреты перестали попадать в долговечный журнал настроек (было два расходящихся списка ключей — свёл в один);
- учётка владельца слита в одну каноническую + убраны 48 висячих ссылок, которые оставил первый заход;
- срок жизни кода из письма 10 → 30 минут (измерено по заголовкам: 16 минут письмо ждало вне нас).

## ЧТО НЕ СДЕЛАНО — начинать отсюда

1. **Выкат на TEST красный по гейту «A2 продуктовый смоук»** (§14). Окружение живое и отвечает, новый код на
   месте, но деплой по правилам НЕ считается завершённым. Это та же служебная учётка смоука, что вели весь день.
   Корень: §14.2 «деплой сам назначает пароль перед проверкой» не сделан.
2. **§16 — гейты сравнивают исходники буквально.** Владелец принял вариант B: «научить инспекторов смотреть на
   смысл, а не на начертание. главное дать правильный смысл». Работа делится надвое: механика (общий помощник
   сравнения, 33 файла) — агент; смысловая часть (что именно проверяет каждый гейт) — ВОПРОСЫ владельцу.
3. **Квоты §10.3-10.7 ЗАБЛОКИРОВАНЫ** до решения владельца по развилкам из двух разведок. Девять ключей из
   пятнадцати должны сменить класс; построенная квота на страницы считает не ту вещь. Не строить дальше.
4. **Обход экранов владельца** — 9 пунктов открыто в `docs/_TODO/OWNER_WALKTHROUGHS/2026-07-27_global-admin.md`.
5. **Уборка клонов.** Рядом с репозиторием 44 клона, около 50 ГБ. Удалять можно только те, где ноль своих
   коммитов и чистое дерево; первый замер был неверным (сравнение шло с чужим идентификатором ветки).
6. **Доставка почты.** Подтверждено по заголовкам: задержка вне нас, между почтой хостинга reg.ru и Яндексом;
   подпись домена в порядке (`spf=pass`, `dkim=pass`). Настоящее лечение — уйти с общей почты хостинга на
   сервис транзакционных писем. Это предложение владельцу, не начатая работа.

## Живые ловушки, на которые уже наступали сегодня

- Удалили маршрут → снести `apps/webapp/.next/types` перед полным CI, иначе падение выглядит как поломка кода.
- Номер миграции присваивает лид ПРИ СЛИЯНИИ; мало переименовать файл — есть ссылки в тестах и смоуках.
- После ручного разрешения конфликта гонять тесты ОБЕИХ сторон: тайпчек молчит о потерянном поведении
  (так едва не потеряли доступ админа клиники к биллингу).
- `pkill -f <шаблон>` убивает собственную команду: шаблон совпадает с её же строкой запуска. Убивать по PID.
- Наблюдатель, ждущий исчезновения процесса по строке, ловит сам себя и висит вечно. Ждать маркер в логе.
