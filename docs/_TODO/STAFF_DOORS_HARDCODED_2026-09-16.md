# Сотрудничьи двери входа задаются кодом, а не переключателями админки

Решение владельца 16.09.2026, дословно: «пусть сценарии входа сотрудников будут жестко заданы
(основа), выбирается только требование 2FA и подкобчение сотрудником входа по ТОТП или коду на
почту»; «ну и пасскей как вариант»; «и переключатели сами убрать можно»; про поверхности — «для
терапиго оно доступно а для тераписто и админ.тераписто - нет». Про прежнюю схему: «это защита
настройкой, а не устройством - вот это убрать. зря сделали», «то есть это как раз стоит
захардкодить».

Позднее уточнение владельца 16.09 по двери специалиста: «первый экран делать сразу с вводом имейл»;
passkey оставить «только как доп вариант», а подпись «вход по ключу доступа» заменить термином
Passkey. Затем владелец уточнил визуал: знак уменьшить на 30%, поднять выше отдельно от формы и
убрать верхнюю шапку; положение самой карточки не менять.

По двери платформенного администратора владелец уточнил отдельно: сразу email и пароль, без passkey,
выбора других вариантов и связи с техподдержкой; блок серый, знак маленький и наверху, верхнюю шапку
убрать.

Канон, с которым сверяется работа: `docs/ARCHITECTURE/AUTH_AND_IDENTITY_CANON.md` — таблица «Дверь |
Способы входа | Сессия» и записанный перед разделом «### 2. Текущие маршруты» блок этого решения.

## Откуда взялась работа

15.09 по прямому вопросу владельца («17 групп маршрутов — разбери их аудитором и сам») прошёл аудит
дверей входа (`docs/_TODO/AUTH_DOORS_AUDIT_2026-09-15.md`). Одна из находок: телефонная дверь не
спрашивает, на какой поверхности её позвали, — только глобально включён ли канал. Замер ведущего на
живом TEST показал, что прямо сейчас сценарий не проходит: у `test.therapysto.ru` в публичном
конфиге входа все кодовые каналы `false`. То есть сотрудника защищала НАСТРОЙКА: один переключатель в
админке — и телефонный код становится полной заменой пароля, молча и без единого красного теста.
Владелец решил убрать саму возможность.

## Этапы

- [x] **С1. Телефонный метод убран из набора сотрудничьих поверхностей.** Ведущий, `3e0a5c616`:
      `staff` и `platform_admin` больше не содержат `phone_bot` в `availableMethods`, а проверка
      канала отвечает отказом ДО чтения настройки (`surfaceAuthControlAvailable`). Тесты написаны;
      прогон — первый шаг С2, в клоне не было зависимостей.
- [ ] **С2. Переключатели каналов у `staff` и `platform_admin` сняты совсем** — ключи настроек,
      записи реестра, строки экрана админки и все чтения. У пациентской поверхности остаются.
- [ ] **С3. Состав сотрудничьей двери — константа кода:** у специалиста почта с паролем как основа
      и passkey как вариант; у платформенного администратора только почта с паролем; ни один пункт
      не зависит от записанной настройки поверхности.
- [ ] **С4. Второй фактор — выбор самого сотрудника** (TOTP или код на почту); механика
      `staffSecurity` не переделывается, только перестаёт читать снятые переключатели.
- [ ] **С5. Требование 2FA остаётся административной настройкой** («выключена / добровольная /
      обязательная») — не переносится в код.
- [ ] **С6. Приёмка:** независимый адверсарный аудит другой моделью, чем автор, затем глаза
      владельца на живом стенде. Галочка этапа не ставится до последнего.
      **Владелец 16.09 передал глаза ведущему: «С6 — проверь сам глазами на дев».** Проверка
      сделана 16.09 на живом `127.0.0.1:5200`, снимки `/home/dev/shots-c6/{doctor,admin,patient}-login.png`.
      Экраны верны: у `/app/doctor/login` и `/app/admin/login` ровно две кнопки — «Войти по ключу
      доступа» и «Войти по email», ни телефона, ни мессенджеров, ни OAuth; пациентский
      `/app/patient/login` цел — Яндекс, почта, номер телефона. Живые двери на сотрудничьей
      поверхности: `email-otp/start` → `auth_channel_disabled`, `oauth/start` → `oauth_disabled`.
      **Галочка НЕ ставится: найдена дыра — см. С7.** ⬅️ дыра закрыта (С7 выше), других препятствий у этапа не осталось: глаза владелец передал ведущему, проверка сделана 16.09 снимками.
- [x] **С7. ✅ ЗАКРЫТ (сверка ведущего с деревом 16.09).** Обе половины требования в стволе, проверено чтением самого маршрута, а не отчётом: `apps/webapp/src/app/api/auth/phone/start/route.ts` первым же действием, ДО разбора тела запроса, отказывает `auth_method_disabled` (403), если набор способов поверхности не содержит `phone_bot`; следом вход по введённому номеру отказан как класс — `purpose === 'login'` отвечает `direct_phone_login_disabled` (403). Автоматической ветки подбора канала в файле не осталось вовсе (`grep automaticPublicLogin` — пусто), вместе с ней ушёл и подбор SMS. Живой путь остался один — код после подтверждения контакта в мессенджере. Приземлено ветками `wt/c7-phone-surface` (коммиты `ded6884b1` и соседние, шесть строк вердикта в очереди аудита). Живьём с сотрудничьего хоста заново не перепроверял: заголовок поверхности подписывает `proxy`, из консоли его не подделать, а отказ стоит раньше любой ветки. Прежняя формулировка этапа — ниже, как история находки.

      Замер
      ведущего 16.09 на DEV (поверхность `staff`, публичный конфиг отдаёт `{"email":false,"sms":false,"telegram":false,"max":false}`):
      `POST /api/auth/phone/start` с одним номером возвращает `{"ok":true,"challengeId":…,"deliveryChannel":"automatic"}`.
      Причина найдена в коде: `apps/webapp/src/app/api/auth/phone/start/route.ts:88-89,143` —
      проверка канала стоит под условием `!automaticPublicLogin`, а обычный публичный вход по
      номеру идёт именно автоматической веткой и не спрашивает ни канал, ни набор способов
      поверхности. Поверхность в маршруте читается только ради брендированной организации
      (`:83-85`). То есть С1–С5 закрыли ПЕРЕКЛЮЧАТЕЛИ, а эта дверь их не спрашивает.
      Основание — прямое слово владельца 16.09: «а, ну так надо просто вырезать в тераписто
      /api/auth/phone/start (или заблокировать» — и таблица дверей канона, где у сотрудника
      телефона нет. Требуемый результат: маршрут отказывает ДО всякой ветки, если набор способов
      поверхности не содержит `phone_bot`; пациентская поверхность не задета.
      ⚠️ Файл сейчас правит соседняя сессия (#1005, вход по номеру) — этап начинается после того,
      как её работа приземлится, иначе две правки одного файла столкнутся.
      **Владелец 16.09 расширил этап, дословно: «вход по номеру идёт именно автоматической веткой -
      вырезать все подобное, это старое. Сейчас вход по номеру только по коду после подтверждения
      контактов в мессенджере».** То есть чинится не только отказ на сотрудничьей поверхности:
      автоматическая ветка публичного входа по номеру вырезается как класс. Живой путь остаётся
      один — код после подтверждения контакта в мессенджере
      (`phone/messenger-bind/{start,status,finish}`). Всё, что подбирает канал доставки само и
      обходит проверку, снимается вместе с ветками SMS-подбора.
- [ ] **С8. Почтовый код обязан доставляться там, где поверхность сама его потребовала.** Находка
      ведущего 16.09 при сверке С3–С5 с деревом: `withAuthDeliveryChannelGate` зовёт
      `isAuthChannelEnabled` без явной поверхности, ambient-резолюция единственного хоста даёт
      `staff`, а у `staff` после `d59313e18` нет `email_code` в наборе — значит на сотрудничьей
      поверхности не уходит ни один почтовый код: ни второй фактор сотрудника (С4 требует его
      выбором самого сотрудника), ни подтверждение почты при регистрации специалиста. Ни один тест
      этого не ловит. Дверь входа одним почтовым кодом при этом обязана остаться закрытой.
- [x] **С9. Дверь специалиста начинается с формы email и пароля.** Passkey остаётся вторичной
      ссылкой «Войти с Passkey» на той же форме; промежуточного экрана выбора двух равноправных
      способов нет. Доказано личной live-проверкой DEV 16.09.2026: `/app/doctor/login` сразу
      показывает поля Email и «Пароль», основную кнопку «Войти» и вторичную «Войти с Passkey»;
      промежуточного экрана и текста «Войти по ключу доступа» нет.
- [x] **С10. Дверь платформенного администратора — только email и пароль.** Нет passkey, телефона,
      других вариантов и связи с техподдержкой; шапка снята, маленький знак стоит над серой формой.
      Доказано личной live-проверкой DEV 16.09.2026: `/app/admin/login` сразу показывает Email,
      «Пароль» и «Войти»; альтернатив, поддержки и верхней шапки нет; знак имеет ширину 48 px и
      расположен над формой с фоном `#f2f3f5`.
- [x] **С11. Дверь специалиста без верхней шапки.** Вертикальный знак уменьшен на 30% — со 132 до
      92 px — и поднят выше независимо от формы; положение самой карточки не меняется. Доказано
      личной live-проверкой DEV 16.09.2026: карточка осталась на прежнем месте, вверх сдвинут только
      знак; верхней шапки нет.

## Границы

Пациентский вход не меняется ни одной строкой поведения. Двери `api/auth/*` правятся только там, где
это прямо следует из этапов выше. Прод и TEST не трогаются. Миграции из клона на DEV не
накатываются. Полный CI — только когда в `feat` приедет исполняемый код.
