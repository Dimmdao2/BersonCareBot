# Правки кабинета пациента по поручению владельца, 10.09.2026

Поручение владельца дословно (десктоп-сессия, 10.09.2026, со скриншотом главной «Сегодня»):

> надо исправить:
> 1) у меня брендированная клиника, поэтому при входе в мой кабинет у пациента должно быть скрытие блока
>    с выбором организации
> 2) иконки самочувствия - исправить верстку - должны быть полноценные эмодзи а сейчас полоски обрезанные
> 3) все иконки дизайна кабинета пациента (колокольчик, ситуации, сос и тд) - перенести в репо и грузить
>    напрямую из специальной папки, а не из s3 и не хранить натстройки в БД. Из кабинета доктора настройки
>    этих иконок убрать. Из БД тоже

Ограничение владельца: «codex не использовать», делать самому.

Пункт 3 — прямое продолжение решения владельца 18.08.2026 по иконкам самочувствия
(`35c7a4c7d`: «их нахуй из бд удалять и читать сразу из папки напрямую»), теперь на остальные
иконки дизайна кабинета.

## Замер реальности до правок (10.09.2026)

- Иконки блоков главной уже лежат в репозитории: `apps/webapp/public/patient/home/icons/*.png`
  (booking, sos, progress, next-reminder, chart-line) и `.../icons/mood/1..5.png`.
- `resolvePatientHomeBlockLeadingIconUrl` отдаёт bundled-иконку ПЕРВОЙ, CMS-значение читается только
  при её отсутствии. Настраиваемые в докторском кабинете блоки — ровно те пять, у которых bundled-иконка
  есть (`PATIENT_HOME_LEADING_ICON_BLOCK_CODES`). То есть настройка `patient_home_blocks.icon_image_url`
  уже НЕ влияет на кабинет пациента: врач меняет значение, пациент видит прежнюю картинку.
- Обрезанные «полоски» самочувствия: `patientHomeMoodOptionButtonClass` даёт `size-9 p-0 rounded-full`,
  но patient-обёртка `Button` (`shared/ui/patient/primitives/button-variants.ts`) отдаёт строку cva
  БЕЗ `tailwind-merge`, тогда как общий `shared/ui/primitives/button.tsx` оборачивает результат в `cn`.
  Из-за этого `px-3` варианта `size: 'default'` переживает наш `p-0`: содержимое кнопки шириной
  36 − 12 − 12 = 12px при высоте 36px, а `object-cover` режет эмодзи в вертикальную полоску. Порядок
  утилит в живом CSS проверен: `.p-0` на смещении 135332, `.px-3` — 136624, то есть побеждает `px-3`.
- Блок выбора организации рисует `PatientOrganizationContextBar`; поверхность запроса
  (`patient_branded` + `organizationId`) уже разрешена в `app/app/patient/layout.tsx` и до бара не доходит.

## Чек-лист

- [x] 1. Брендированная поверхность скрывает блок выбора организации у пациента этой же организации.
      `layout.tsx` даёт `brandedOrganizationSurface` (`surface === 'patient_branded'` И
      `organizationId` совпадает с организацией пациента), провайдер несёт его в контекст,
      `PatientOrganizationContextBar` на нём возвращает `null`. Коммит `6ff873d4d`.
- [x] 2. Иконки самочувствия рисуются полным эмодзи. Правка — в patient-обёртке `Button`:
      `className` проходит через `cn` (tailwind-merge) на обеих ветках, как в общей обёртке.
      Коммит `6ff873d4d`.
- [x] 3. Иконки блоков главной берутся только из `public/patient/home/icons/`; настройка иконки
      убрана из докторского кабинета, из порта/сервиса/обоих репозиториев, из реестра защищённых
      действий и из БД (миграция `20260910T103638_patient_home_block_icons_live_in_the_repo`,
      грант `UPDATE (icon_image_url)` снят из декларации). Коммит `6ff873d4d`.
- [x] 3a. Файлы иконок подогнаны под размеры отрисовки и сжаты: `sos.png` и `progress.png` 80→56px
      (рисуются максимум 28px, то есть 56px хватает при DPR 2), `mood/*.png` остаются 80px
      (рисуются 36–40px), `next-reminder.png` 80px (рисуется 40–48px) — вверх не растягиваем,
      источник и есть предел. Неиспользуемый `plan.png` удалён (карта плана ссылается на
      `chart-line.png`). Итог 85 КБ → 22 КБ.
- [x] 4. Живая проверка на DEV `:5200` — п.2 и п.3 доказаны:
      - п.2: в разметке кнопки шкалы больше нет `px-3`, остались `p-0`, `size-9`, `rounded-full`;
        на скриншоте мобильного вьюпорта — пять полных круглых эмодзи;
      - п.3: в HTML главной пациента все иконки идут с `/patient/home/icons/…`, ни одного
        `/api/media/` для иконок; на странице настроек главной у врача слова «иконка» больше нет;
      - миграция применена на именованной DEV: `migrate-dev.sh --preflight` → PASS, затем
        `--execute` → PASS с полным reconcile.
- [ ] 5. Живая проверка п.1 на брендированном адресе — **не выполнена, блокер среды.** На
      именованной DEV брендированный Host не резолвится: `app.resolve_public_organization_by_slug`
      требует одновременно ряд `organization_slug_claims.kind='current'`,
      `clinic_public_directory_entries.is_published = true` и `be_organizations.is_active = true`.
      Для проверки на DEV штатно создан слаг `tochka-zdorovya` и опубликована (ранее не
      существовавшая) публичная карточка — этого не хватило, ответ на
      `Host: tochka-zdorovya.127.0.0.1:5200` остался 404 (он приходит от нашего proxy: в ответе есть
      `x-bc-correlation-id`, то есть запрос доходит до приложения, отказывает резолв арендатора).
      Какое из трёх условий не выполнено, из приложения не видно, а чтение базы требует sudo,
      закрытого в песочнице агента. Доказательство остаётся за TEST (владелец 10.09 запретил
      деплой) либо за живой приёмкой владельца на его брендированном адресе.
      Отрицательный случай проверен: на платформенном хосте полоса организации по-прежнему на месте.
- [ ] 6. Живая приёмка владельцем.

## Что изменено в данных DEV ради проверки

- Создан слаг клиники `tochka-zdorovya` (штатный `POST /api/clinic/slug`; до этого слуга не было).
- Опубликована публичная карточка клиники пустыми полями (`POST /api/clinic/public-card`).
  Раздела карточки на странице настроек до этого не было вовсе, то есть ничего не затёрто.
- Прод не затронут; TEST не затронут.

## Пункт 4 владельца (10.09, отдельным сообщением) — не начат

> иконка брендированного приложения и сайта пациента должна настраиваться в кабинете доктора, она же
> и на фавикон должна ставиться (в идеале сразу при установке или смене и переформатируется под
> фавикон и все остальные форматы)

Это НЕ противоречит пункту 3: иконки дизайна кабинета одинаковы у всех клиник и живут в
репозитории, а иконка приложения клиники — её собственные данные, и им место в настройках и
хранилище. Работа отдельная, здесь только зафиксирована.
