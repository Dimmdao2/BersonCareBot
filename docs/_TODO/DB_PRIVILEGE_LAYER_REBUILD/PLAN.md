# PLAN v6 — слой прав БД: привести к мировой схеме

## ЦЕЛЬ (владелец, 08.08.2026)

**Отсутствие бардака в системе: чёткая структура — как, кому и когда выдаются роли и где какие
стоят стены. И чтобы это не чинилось заплатками при каждой миграции — иначе хаос, как сейчас.**

Мандат (владелец, 08.08): применить у нас все четыре принципа мировой практики. Оркестрировать,
пока система не приведена к этой схеме.

## Целевая архитектура — четыре принципа (evidence/07, проверяются в Ф0)

1. **Одна декларация — один генератор.** Истина о правах живёт в одном декларативном файле; гранты
   существуют только как вывод генератора; миграции несут только схему. Одна власть над ACL.
2. **Deny by default + стена в точке создания.** Рантайм-роли не получают ничего по умолчанию;
   новая таблица рождается закрытой; event trigger ставит RLS+FORCE при рождении org-таблицы.
   Забыл объявить = громкий 42501 на dev, никогда не тихая утечка.
3. **Двусторонняя сверка.** `объявлено EXCEPT есть` И `есть EXCEPT объявлено` — оба непусты =
   красный. Ядро уже есть (`c5a`, паттерн pg_permissions/CIS) — расширить на все роли.
4. **Стену держит движок + свип как страховка.** GRANT + RLS + FORCE применяет Postgres, не код.
   Свип = каталожные инварианты «ноль нарушений» одной командой. Стенд a1 = поведенческое
   доказательство сквозь реальный код. В нормальной жизни оба вечно зелёные; красный = ЧП, не будни.

Порядок важен: **сначала конструкция, при которой дефект не рождается (1, 2), потом детект как
страховка конструкции (3, 4).** Гейты как основной механизм = увековеченный режим «каждый ход
что-то ловим» — отвергнут владельцем.

## Правило приёмки — каждый инкремент, без исключений

Три транскрипта в сообщении коммита: **КРАСНЫЙ** (на непочиненной реальности) → **ЗЕЛЁНЫЙ** (после
починки) → **СНОВА КРАСНЫЙ** (дефект внесён обратно в откатываемой транзакции). Роли: доказыватель
(правит только код инкремента) · ломатель (Opus, не видит рассуждений доказывателя) · я (сверяю с
этим файлом). «Аудит доволен» ≠ готово. Стоп-правило: два круга без закрытой галочки → эскалация
моделей/контекста; ещё два — стоп и вопрос владельцу.
Растяжка еженедельно: строк `.md` не больше строк исполняемого кода (`FACTS.md` §7).

---

## Ф0 — верны ли исследования `[ЗАПУЩЕНО 08.08]`

Владелец: «исследования есть, верны ли они?» Дизайн опирается на evidence/07 — проверить несущие
утверждения до строительства.

- [x] В0.1 Внешние утверждения — проверены по живым первоисточникам: **14/15 подтверждены**,
      1 расхождение (OTel `SHOULD NOT`, не `MUST NOT` — вывод устоял). `FACTS.md` §5
- [x] В0.2 Механизмы Postgres — доказаны ИСПОЛНЕНИЕМ, **10/10 подтверждены** (`FACTS.md` §4.1,
      транскрипты — `evidence/12`, включая рабочий прототип event trigger). Оговорки внесены в
      Ф2/Ф3 ниже
- [x] В0.3 Ничего из опровергнутого (`FACTS.md` §9) проверки не переоткрыли

**Ф0 ЗАКРЫТ 08.08: исследование верно (В0.1 — 14/15 по первоисточникам), механизмы работают
(В0.2 — 10/10 исполнением).**

## Ф1 — SCHEME.md: точная целевая схема НАШЕЙ системы

На выходе один документ: формат декларации (роли, области из `FACTS.md` §1.5, гранты, политики,
исключения-definer с обоснованием каждого); кто владеет какими объектами; путь применения
(деплой-шаг генератора); что считается org-таблицей; судьба 61 оверлея и прав в 160 миграциях
(сжатие в baseline по протоколу Django — история остаётся, носителем истины быть перестаёт).

- [ ] Ч1.1 Черновик схемы (сильный агент, вводные: FACTS + результаты Ф0)
- [ ] Ч1.2 Адверсарный цикл по правилу владельца: свежий агент-критик → правка → повторять до
      ДВУХ чистых подряд
- [x] Ч1.3 **Приёмка владельцем — ДА (08.08): «принять сейчас, начать стройку».** Схема
      архитектурно чиста 9 кругов; остаточная точность литералов передана переписи Ф2 (снимает из
      живого каталога — устраняет класс, на котором спотыкались круги 7-9). Ф1 ЗАКРЫТ.

## Ф2 — генератор + декларация (принцип 1)

- [ ] Декларация заполнена из живого каталога (снятое текущее состояние минус известные дефекты)
- [ ] Генератор: декларация → полный идемпотентный SQL прав; применяется деплоем, руками — некуда.
      **Оговорка из В0.2:** переприменение — только ОДНОЙ транзакцией (раздельные autocommit
      ломают открытых читателей 42501 в окне; одной транзакцией — не блокирует и не ломает)
- [ ] Гейт «миграции — только схема»: GRANT/REVOKE/CREATE POLICY/CREATE ROLE в новой миграции =
      красная сборка. Старые 377 НЕ переписываются
- [ ] Приёмка: три транскрипта

## Ф3 — deny by default + event trigger (принцип 2)

Прототип и транскрипты — `evidence/12` §4-6.

- [ ] REVOKE дефолтов; новая таблица рождается закрытой для рантайм-ролей. **Оговорка из В0.2:**
      defaults живут по-создающей-роли — множество создателей закрыть и перечислить
- [ ] Event trigger: org-таблица при рождении получает RLS+FORCE или создание отклоняется.
      **Оговорки из В0.2:** включить тег `ALTER TABLE` (поздняя org-колонка) + защита от
      рекурсии; владелец триггера — только суперпользователь
- [ ] Приёмка: три транскрипта (красный = сегодняшняя реальность, где таблица рождается открытой)

## Ф4 — двусторонняя сверка на все роли (принцип 3)

- [ ] Расширить `c5a`-паттерн: table ACL + колоночные + function ACL + политики + атрибуты ролей,
      обе стороны EXCEPT, все роли
- [ ] Приёмка: красный воспроизводится на любом ручном GRANT мимо декларации

## Ф5 — свип + стенд (принцип 4)

- [ ] Свип: 9 каталожных инвариантов (RLS на каждой org-таблице; FORCE; нет RLS-таблиц без
      политик; нет `TO PUBLIC`; нет неожиданного BYPASSRLS; представления `security_invoker`;
      добавлены в Ф1.2: №7 ноль позитивных записей в `pg_default_acl` рантайм-ролям/PUBLIC;
      №8 ноль незадекларированных ролей с доступом к управляемым базам — юрисдикция по доступу;
      №9 каждый FK между org-таблицами спарен по организации — RI обходит RLS).
      Красный воспроизводится на 5 таблицах из `FACTS.md` §1.3
- [ ] Стенд a1: закрыть 5 дыр проверки (0.1 forceRls изнутри сессии · 0.2 сверка журнала миграций ·
      0.3 внешний JSON из измеренного, не подстрока · 0.4 положительный контроль строк ·
      0.5 отличать enforce от спящего) — `FACTS.md` §3.1; затем ломатель
- [ ] Обход ячеек `(роль × таблица × принципал)` — утечка/слепота/частичная (`FACTS.md` §6) — в CI

## Ф6 — первое применение генератора = чистка сегодняшних утечек

НЕ заплатки: дефекты закрываются приведением реальности к декларации.

- [ ] 5 таблиц без RLS+FORCE (`FACTS.md` §1.3) · 7 ячеек утечки (§1.2) · `platform_users` (§1.4,
      **есть развилка владельца**) · угадывание роли в Node (§1.1) · `pgEmailSetupFlowPort` (§11.7)
- [ ] Доказательство: свип зелёный, обход ячеек — ноль чужих строк по всем ролям, стенд PASS

## Развилки владельца — состояние на 08.08 (полный реестр решений — SCHEME.md §I)

**Открытая — одна: приёмка SCHEME.md (Ч1.3)** по выжимке, после двух чистых критиков подряд.

Решено владельцем 08.08: dev-база в контуре сразу («никаких прямо сейчас работ не идёт») ·
свой генератор, не Atlas Pro («денег нет») · `bcb_webapp_prod` на этом боксе — старая копия,
вне контура, удаление не назначено.

Решено инженерно 08.08 (владелец видел триаж, возражений не было; **повторно предъявить при
приёмке Ч1.3**, т.к. №1-2 закрыты в НЕ-дефолтную сторону): `platform_users` RLS — сейчас ·
7 ячеек — сузить `app_platform_settings` · BYPASSRLS у `app_owner` и `saas_system_health_owner`
— объявить-и-оставить · старый CI-гейт — оставить оба · транзиентные org-таблицы — терпеть через
фазу миграций · логины — в контуре декларации · §11.5/11.6 — явные пункты Ф6.

## Что план сознательно НЕ делает

Не переписывает 377 применённых миграций · не вводит «всегда бросать» (§9.2) · не пишет
AST-анализаторов (§9.3) · не идёт в capability-only (§9.4) · не полагается на EXPLAIN (§4) ·
не выкатывает на TEST без команды владельца.
