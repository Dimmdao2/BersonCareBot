# Doctor UI → Design DNA — миграция (план-файл владельца)

> ⛔ **ОТМЕНЕНО ВЛАДЕЛЬЦЕМ — ТОЛЬКО ИСТОРИЧЕСКАЯ ЗАПИСЬ, НЕ план-файл владельца.**
> `docs/INITIATIVES.md:18`: «полный Doctor DNA `#885` отменён владельцем и остаётся только исторической
> записью». Владелец 27.07.2026 (уточнение по разметке чекбоксов): «помечаем как "для справки если вернемся
> на доработку", частично сделано - меня устраивает». Блок ниже ("Источник истины плана") отражает статус
> файла ДО отмены и оставлен как был — не действует. Живое место работы над визуалом кабинета доктора —
> [`DOCTOR_UI_REWORK_2026-07-20/PLAN.md`](../DOCTOR_UI_REWORK_2026-07-20/PLAN.md) §UI-P. Пять боксов ниже
> размечены `[-]` (отменено, частично уже сделано в коде — доказательства в каждой строке).

> **Источник истины плана.** «Готово» = галочка в этом файле + зелёный full CI + живая проверка владельцем.
> Аудит — гейт против ЭТОГО файла, не генератор скоупа. Находка без пункта здесь = вопрос владельцу, не задача.

> 🟢 **ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07.2026: «закрывай. я сказал».** План закрыт целиком: 0 открытых пунктов.
> Четыре пункта выполнены и живут в коде (отмечены `[x]` с доказательствами), пятый снят отменой.
> Файл остаётся как справка на случай возврата к теме — исполнять из него нечего.

## Решение владельца (2026-07-19)

1. **Источник истины визуала кабинета доктора — Design DNA** (`docs/design/dna/` — v1.0 spec + v1.1 amendment + `bersoncare-theme.css`).
   `DOCTOR_APP_UI_STYLE_GUIDE.md` и shared-primitives **переписываем под DNA** (не наоборот).
2. **Спорное (строки-списки, тёплый холст, шрифт) решаем на ЖИВОМ экране**, не на бумаге — сначала предпросмотр на 2-3 экранах, потом решение.

## Установленный факт (разведка 19.07)

- DNA-токены заведены в `apps/webapp/src/app/styles/bersoncare-tweakcn-theme.css` и промаплены на shadcn-семантику,
  **но спят**: класс `theme-bersoncare-doctor-dna` нигде в рантайме не навешивается на `#app-shell-doctor`
  ([`DoctorAppShell.tsx:35,43`](../../../apps/webapp/src/shared/ui/doctor/DoctorAppShell.tsx)).
- Действующий стайл-гайд **противоречит** DNA в корне: строки-списки (заливка `bg-primary/15` + `rounded-md`
  vs плоские + полоска-акцент, заливки запрещены), холст (белый vs `#F6F4EF`), шрифт (системный vs Nunito),
  синий (`hsl(215 35% 40%)` vs `#386FBA`), радиус карточек.
- Nunito в приложении **не подключён** (импорт только в docs-версии темы).
- Аудит 13.07 (`docs/REPORTS/DOCTOR_UI_TWEAKCN_DNA_AUDIT_2026-07-13.md`): «если после токенов экран не выглядит
  как DNA — проблема в компонентной структуре (список/карточка/layout)», рекомендованный порядок
  структура → primitives → токены → шрифт → графики.

## Стадии

### S0 — Живой DNA-предпросмотр (текущая, под решение владельца)

Ветка `preview/doctor-dna`. Код-онли, обратимо. Цель — дать владельцу кликнуть и решить спорное.

- [x] **Навесить `theme-bersoncare-doctor-dna` на `#app-shell-doctor` (токен-флип: холст/синий/радиус).** —
      ✅ СДЕЛАНО (инициатива в целом отменена — `docs/INITIATIVES.md:18` + уточнение владельца 27.07.2026
      «частично сделано - меня устраивает» — но ЭТА работа выполнена и живёт в проде). Доказательство: класс навешен в обеих ветках `DoctorAppShell.tsx:35,43`; токены
      применяются через `#app-shell-doctor.theme-bersoncare-doctor-dna` в
      `apps/webapp/src/app/styles/bersoncare-tweakcn-theme.css:88-93` (проверено чтением файлов).
- [x] **Подключить и применить Nunito в doctor-scope (`--font-sans`).** — ✅ СДЕЛАНО (инициатива отменена,
      `docs/INITIATIVES.md:18` + «частично сделано - меня устраивает» 27.07.2026; эта часть выполнена).
      Доказательство:
      шрифт подключён в `apps/webapp/src/app/layout.tsx:10,23-28,76` (`next/font/google` Nunito,
      `--font-nunito`); `--bc-font-sans` использует `var(--font-nunito)` и применяется через
      `font-family: var(--bc-font-sans)` под селектором `#app-shell-doctor.theme-bersoncare-doctor-dna`
      (`bersoncare-tweakcn-theme.css:53,93`) — то есть уже активен в doctor-scope, т.к. класс темы навешен
      (см. пункт выше).
- [x] **Ввести DNA-строку списка (плоская, волосяная линия, выделение = полоска слева + вес, без
      заливки/пилюль) как новый shared-примитив — применить на: Сегодня, Клиенты,
      **Настройки\*\*. — ✅ СДЕЛАНО (инициатива отменена, `docs/INITIATIVES.md:18` + «частично сделано - меня
      устраивает» 27.07.2026; эта часть выполнена). Доказательство: примитив существует —
      `apps/webapp/src/shared/ui/doctor/DoctorDnaFlatListRow.tsx` — и используется на
      `DoctorTodayDashboard.tsx` (Сегодня), `patients/PatientsPageClient.tsx` (Клиенты),
      `settings/TeamSection.tsx` и `settings/BillingSection.tsx` (Настройки) (проверено grep по репо).
- [x] **Убрать захардкоженный холодный `#f7f9fc` в `doctor.css`, где он бьётся с холстом.** — ✅ СДЕЛАНО
      (инициатива отменена, `docs/INITIATIVES.md:18` + «частично сделано - меня устраивает» 27.07.2026;
      эта часть выполнена). Доказательство: `grep -n f7f9fc apps/webapp/src/app/styles/doctor.css` — 0 совпадений (проверено
      напрямую, файл больше не содержит этот цвет).
- [-] ~~Скриншоты 3 экранов (desktop+mobile) владельцу → его решение по строкам/холсту/шрифту.~~ — ⛔
  ОТМЕНЕНО ВЛАДЕЛЬЦЕМ. Смысл пункта — получить его решение по строкам/холсту/шрифту; решение он дал
  напрямую 27.07: «закрывай. я сказал» (и ранее «частично сделано - меня устраивает»,
  `docs/INITIATIVES.md:18`). Показывать скриншоты ради решения, которое уже принято, незачем — пункт
  закрыт отменой, а не невыполнением.

### S1 — Токены + шрифт (после S0-решения)

Промышленно включить тему, выровнять `doctor.css`-зональные токены, зафиксировать Nunito. Обновить стайл-гайд §A.2.

### S2 — Shared primitives под DNA

Переписать `doctorVisual.ts` строки/секции/выделение под DNA; `doctorClientCardChrome.ts`; каталожный стек. Обновить гайд §4–§9, §C.

### S3 — Экраны настроек и аналитики (UX-фокус владельца)

Пройти settings-hub и analytics: DNA-строки, «тихие» статусы, палитра графиков (моно-синий, зелёный только маркер).

### S4 — Сложные виджеты

Schedule/FullCalendar, patient card chart SVG, CMS/media — последним слоем.

## Открытые развилки владельца (закрыть до соответствующей стадии)

- Строки-списки: плоские DNA vs текущая заливка — **решение по S0-предпросмотру**.
- Тёплый холст `#F6F4EF` глобально vs белый — **по S0-предпросмотру**.
- Палитра аналитики: расширение моно-синего без зелёного как основного (DNA §9 open question) — к S3.

## Провенанс

Решение — чат владельца 19.07. Канон DNA — `docs/design/dna/`. taskdb bcb.
