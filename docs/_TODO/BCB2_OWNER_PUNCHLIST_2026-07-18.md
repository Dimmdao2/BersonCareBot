# BCB — пунш-лист владельца (дословно), 2026-07-18

Источник истины по доработкам. Собрано из сообщений владельца 2026-07-17/18. Каждый пункт — кандидат в чек-лист.
Владелец: «много из этого уже сделано/решено, но проверить всё нужно». Триаж проставляет статус
[DONE|PARTIAL|TODO] + evidence(file:line/commit) + правильный способ реализации + карту taskdb.

> **ТРИАЖ 2026-07-18:** независимая сверка всех пунктов с кодом → `BCB2_PUNCHLIST_TRIAGE_2026-07-18.md`.
> Итог: **26/32 уже DONE в коде**. Реальные TODO: PRG-3 (в работе), SCH-G5 (вопрос владельцу), SCH-C4/C5/C6 (проверка вьюпорта владельцем), PRG-4 (HOLD, sign-off).

Правила исполнения (владелец 2026-07-18): идти строго по чек-листам, не крутить механики/тесты ради них;
коммитить регулярно; **ПУШИТЬ обязательно в `feat/doctor-ui-rebuild`** (это бэкап, не висеть в воздухе); НИКОГДА прод;
боковые ветки регулярно мержить в основную и удалять; полный CI — в разумных пределах, не гонять туда-сюда.

---

## РАСПИСАНИЕ — График работы
- [ ] SCH-G1. Не сбрасывать выбор дней по клику на пустом месте.
- [ ] SCH-G2. Цвет филиалов — настраиваемый в настройках модуля записи (см. LOC ниже — единый пикер цвета).
- [ ] SCH-G3. При смене города — не сбрасывать выбор дней.
- [ ] SCH-G4. При выборе дней с графиком — подставлять значения установленного графика (мультивыбор — по первому дню).
- [ ] SCH-G5. В слотах для клиента слоты показываются не по графику, а по расписанию по умолчанию (хотя дни показаны только с ручным расписанием) — чинить.
- [ ] SCH-G6. Для каждой услуги — опциональная настройка «перерыв после приёма» (шаг 5 мин).

## РАСПИСАНИЕ — Календарь
- [ ] SCH-C1. Доктор настраивает отображаемый по умолчанию интервал (Расписание+Сегодня; при отсутствии записей/часов за пределом — иначе адаптивная подстройка УЖЕ ЕСТЬ, не ломать).
- [ ] SCH-C2. При изменении графика рабочие часы в сетке не обновляются без перезагрузки — чинить.
- [ ] SCH-C3. Начало/конец окна выставляется хаотично, не по ТЗ: час до/после записей и/или до/после начала/конца рабочего времени; дефолт (нет расписания/записей или всё в центре) — 09–19 или выбранный доктором. Не ужимать окно до «14–17» если одна запись 15:00 на 60 мин.
- [ ] SCH-C4. Нет отступа внизу от края календаря (упирается в пол).
- [ ] SCH-C5. Режим «лист»: контейнер списка обрезан меньше высоты окна — прокрутка не там и пустое поле внизу.
- [ ] SCH-C6. Режим «лист»: правый блок с инфо обрезается (нет внутренней прокрутки).
- [ ] SCH-C7. Статус записи confirmed — пишется системное значение, не человеко-текст.
- [ ] SCH-C8. Сегодняшний день — красный круг вокруг даты во ВСЕХ режимах (3дня/неделя/месяц); для 3дня/неделя увеличить высоту шапки.

## СЕГОДНЯ  — DONE (весь кластер, verified 2026-07-18)
- [x] TDY-1. Карточка «неделя» split Всего/Будущие — `DoctorTodayRightKpiRow.tsx:79-125` (SplitAppointmentStatCard).
- [x] TDY-2. Карточка «месяц» split — `DoctorTodayRightKpiRow.tsx:217-238`.
- [x] TDY-3. Клик по половине → свой список в модалке — `DoctorTodayRightKpiRow.tsx:200-236` (onTotalClick/onFutureClick разные items).
- [x] TDY-4. Пустую модалку не показывать — `DoctorTodayRightKpiRow.tsx:167-169` (openIfNotEmpty гейтит count>0).
- [x] TDY-5. Поиск клиента убран из модалок «Сегодня» — модалка не передаёт search-пропсы (`DoctorTodayRightKpiRow.tsx:241-256`; search в KpiPreviewModal opt-in).
- [x] TDY-6. Единый шаблон строки комментария — влит 0847697e5.
- [ ] TDY-7 (BCB2). КПИ «новые сообщения» → «открыть переписку» → конкретный диалог НЕ выбирается автоматом. (карта #812 — проверить что влито/работает)

## КЛИЕНТЫ (список + КПИ)
- [ ] CLI-1. Удалить дублирующую кнопку Клиенты/Все в левом блоке под поиском.
- [ ] CLI-2. При фильтре в карточке КПИ показывать отфильтрованное И через разделитель — ВСЕГО.
- [ ] CLI-3. Мультивыбор КПИ (С записями И с программой И с отменами) для взаимоисключающих — переключение.
- [ ] CLI-4. Фильтр «с приложением» считает ВСЕХ — чинить (факт установки, если фиксируется, либо история входа через приложение — продумать).
- [ ] CLI-5. В блок «Каналов» — фильтр «Пуш-уведомления» (у кого явно включены/был факт настройки, если возможно).
- [ ] CLI-6. Доделать переход на ФИО везде; клиенту в приветствии — имя.
- [ ] CLI-7. Кликабельные каналы (ТГ, тел, почта…).

## СТРАНИЦА КЛИЕНТА
- [ ] CLP-1. Показывать в шапке возраст.
- [ ] CLP-2. Пикер даты рождения — плохой; взять тот, что в выборе даты визита.
- [ ] CLP-3. Кликабельные каналы (ТГ, тел, почта…).

## СООБЩЕНИЯ / ЧАТЫ
- [ ] CHT-1. Не открыть профиль/карточку клиента из чата — нужна ссылка «открыть карточку» у имени в заголовке. (карта #813 — проверить)
- [ ] CHT-2. Ссылка «обзор и записи» — левый блок поверх списка чатов (активные записи + история; позже: абонемент/симптомы/диагнозы/статистика программы), закрывать крестиком, как левый блок в упражнениях. (карта #814 — проверить)
- [ ] CHT-3. Проверить: вставленные ссылки воспринимаются как ссылки и открываются в стороннем браузере.
- [ ] CHT-4. Тап/пр.клик — своё меню — добавить «ответить».

## ПРОГРАММА (у доктора)
- [ ] PRG-1. Первый добавленный этап сразу разблокирован.
- [ ] PRG-2. Не работает смена порядка групп в этапе — добавить стрелки около плюсика.
- [ ] PRG-3. Экран добавления упражнений/рекомендаций/тестов: при «добавить упражнение» — левый блок со списком/карточками как в каталоге, с той же панелью фильтров (регион/нагрузка/тип). Мультивыбор кликом (выделяется→в группу), смена фильтра не сбрасывает, без серверной подгрузки/перезагрузки (как быстрая UI-фильтрация через DOM). Каталог грузится ОДИН раз при начале правки, не тормозит при следующих открытиях. Повторный клик снимает выделение и убирает из ТЕКУЩЕЙ группы (в других остаётся).
- [ ] PRG-4. Создавать новые упражнения прямо в инстансе (которых нет в БД): либо уникальное только для инстанса пациента, либо сохранить в общую базу. В момент создания/правки таких — грузить новое видео без создания отдельного упражнения. Видео доктора → в папку куда пациент грузит фото/видео для оценки техники, отдельная подпапка `indive_program_exercises`, HLS 480+720. Режим правки упражнения в инстансе (название/перезалить видео). Из общего каталога — правка в общий каталог; в инстансе помечено индивидуальным — правка только внутри.

## СТРАНИЦА ЗАПИСИ У КЛИЕНТА (пациентская запись)
- [ ] PBK-1. Адрес — без «new».
- [ ] PBK-2. Выбор дня — сеткой месячного календарика с отметкой «Сегодня»; прошедшие недоступны; месяц листается вперёд если там есть записи; выбранный день закрашивать, под календарём — слоты.
- [ ] PBK-3. Мультизапись: при выборе времени соседнее остаётся доступным, остальные — недоступны; при двух подряд — дать выбрать следующий, пока есть слоты без разрыва.
- [ ] PBK-4. Запись другого человека (залогинен или чужое имя на публичной, а по телефону — другой): выбор «записать другого». Спросить перед записью «ФИО отличается — исправить ваши данные или записываете другого?» кнопки себя/другого. Ввод чужого ФИО НЕ влияет на учётку записывающего; в базе — запись не привязанная к пациенту (без аккаунта, с возможностью создать учётку на приёме+контакты); в записи показать ФИО того кто придёт + кто записал (кликабельно). (карта #801 — согласовать)

## АБОНЕМЕНТЫ (аудит 1)
- [ ] ABO-1. При создании — «пересчитать» не работает (не учитывает прошедшие сеансы с момента ПОКУПКИ; возможно ищет с момента создания или сломано). (карта #820/#819)
- [ ] ABO-2. После создания — блок абонемента на «Визиты» (и, вероятно, «Обзор») не обновляется до перезагрузки.
- [ ] ABO-3. Нигде нет возможности списать запись на посещение по абонементу (ни «списать на визит» в абонементах, ни «записать на абонемент» в записях).
- [ ] ABO-4. Нет создания стандартного абонемента (нет каталога нигде).
- [ ] ABO-5. В календаре даты оплаты абонемента можно выбрать будущую — запретить.
- [ ] ABO-6. В списке услуг показывает даже выключенные (закрытая для записи услуга доступна в абонементе) — не должно.
- [ ] ABO-7. «Добавить позицию» пишет «Позиций: 1» без расшифровки.
- [ ] ABO-8. При сохранении — всплывающее «Абонемент создан».
- [ ] ABO-9. Блок «списать сеанс по абонементу» в финансах: (a) не пишет рядом стоимость+коммент(начало); (b) не даёт выбрать запись для списания (вероятно задним числом от даты создания, не от даты оплаты).

## АБОНЕМЕНТЫ (аудит 2)
- [ ] AB2-1. На записи — бейдж абонемента фиолетовым.
- [ ] AB2-2. Уникальные номера абонементам; в записи «аб.#001», не полностью.
- [ ] AB2-4. Несколько активных абонементов — показать в табе «записи» все.
- [ ] AB2-5. Под активными — история закрытых (свёрнуто, стрелка, разворот; текст серее). В историю уходит если все пункты привязаны к прошедшим записям.
- [ ] AB2-6. Кнопка «глаз» в строке абонемента — подсветить тонким фиолетовым бордером привязанные записи (в истории тоже); повтор/другой — снять.
- [ ] AB2-8. На «обзор» в карточке абонемента: «Абонемент / Осталось N визитов: n×услуга… / аб #NN от Дата (несколько — через запятую)».

## РАСПИСАНИЕ И НАСТРОЙКИ ЗАПИСИ — прочее
- [ ] SET-N1. Тексты уведомлений: кнопки переменных назвать понятным словом без скобок (клик — подставляет правильно).
- [ ] SET-N2. Тексты уведомлений: сгруппировать в два контейнера сверху вниз: «Уведомления клиенту» (3 карточки в ряд) и «Уведомления специалисту» (3 карточки в ряд).
- [ ] SET-L1. Локации: пикер цвета филиала для календаря — влияет на цвет активной записи в календаре, цвет филиала в настройках графика и заливку дней в календаре графика. (= SCH-G2)

---

## Уже как карты taskdb (проверить статус, не переоткрывать)
#812 (TDY-7), #813 (CHT-1), #814 (CHT-2) — чат-фичи, влиты (51f8fd322/1e1ce5605) → проверить работу скрином.
#829 — сброс дней по клику (SCH-G1?) — влит (7d3076e63) → сверить, это ли именно.
#801 (PBK-4), #806 — инвайт/запись другого.
#818/#819/#820 — абонементы (оплата/отмена/нотификации) — пересекается с ABO/AB2.
#836 — консолидация членства (specialist_not_configured) — исправлено.
