# BCB — пунш-лист владельца (дословно), 2026-07-18

> **Статус документа: подчинённый backlog, не источник продуктовых решений.** Это отдельный технический
> punch-list по более ранней диктовке 2026-07-17/18. Последний канон решений владельца и review текущего TEST UI —
> [`SAAS_PRODUCT_UX_INITIATIVE/OWNER_REVIEW_2026-07-18.md`](./SAAS_PRODUCT_UX_INITIATIVE/OWNER_REVIEW_2026-07-18.md).
> При пересечении он побеждает этот список. Уникальные пункты punch-list сохраняются как backlog и не считаются
> автоматически подтверждёнными последним review.

Собрано из сообщений владельца 2026-07-17/18. Каждый пункт — кандидат в чек-лист. Владелец: «много из этого уже
сделано/решено, но проверить всё нужно». Триаж проставляет статус [DONE|PARTIAL|TODO] +
evidence(file:line/commit) + правильный способ реализации + карту taskdb.

> **ТРИАЖ 2026-07-18:** независимая сверка всех пунктов с кодом → `BCB2_PUNCHLIST_TRIAGE_2026-07-18.md`.
> Итог: **26/32 уже DONE в коде**. Реальные TODO: PRG-3 (в работе), SCH-G5 (вопрос владельцу), SCH-C4/C5/C6 (проверка вьюпорта владельцем), PRG-4 (HOLD, sign-off).

> **РЕКОНСИЛИАЦИЯ #2, 2026-07-27 (владелец смотрел живое приложение).** Владелец прошёл список глазами
> в работающем приложении и по большинству пунктов сказал «сделано» / «да все же вроде работает уже» —
> для пользовательски видимого поведения его живое наблюдение сильнее археологии по коду, поэтому эти
> пункты закрыты на его основании + короткая цитата `file:line` для памяти (без требования зелёного теста).
> Было (после реконсиляции #1): 29 closed / 32 open. Стало: **48 closed / 13 open** (+19 закрыто в этом
> проходе: SCH-G2, SCH-G4, SCH-G6, все 8 SCH-C, CLI-1, CLI-2, PRG-2, PRG-3, ABO-3, SET-N1, SET-N2, CHT-4).
> Каждое закрытие всё равно перепроверено по коду независимо, не вслепую под диктовку — по трём пунктам
> нашлось прямое, конкретное противоречие его словам, и они НЕ закрыты, а помечены явно: **PBK-1** (адрес
> до сих пор буквально содержит `/new/` во всех роутах), **CLI-4** (фильтра «с приложением» в коде физически
> нет), **PBK-4** (design-doc сам себя маркирует «DOCS-ONLY, no code changed»). Ещё два пункта остались
> частично открытыми как составные — только одна половина: **ABO-9** (списание записи выбрать можно, цена/
> комментарий рядом с сеансом — нет) и **PRG-4** (catalog/personal-развилка и видео-маршрутизация есть,
> отдельной подпапки `indive_program_exercises` в коде нет — задокументированное архитектурное решение).
> **SET-N1/SET-N2** владелец не смог пересказать своими словами («не понятно») — вместо закрытия/удаления
> оба пересказаны простым языком прямо в чек-листе и ОТДЕЛЬНО подтверждены кодом, оказались полностью
> реализованы. **SET-L1** оставлен open как низкий приоритет — владелец хочет не бесплатный `<input
> type="color">`, а готовый библиотечный цветовой пикер. **SCH-G1** не тронут — по нему 27.07 уже был
> отдельный инцидент с придуманной цитатой владельца, решение до сих пор не принято, ждёт его прямого ответа.

> **РЕКОНСИЛИАЦИЯ 2026-07-27** (независимая перепроверка каждого открытого пункта против живого кода/тестов/
> коммитов, не по чужому отчёту). Было: 55 open / 6 closed, без построчных доказательств. Стало: **+23 пункта
> закрыты в этом проходе** (итого 29/61 closed, 32 остаются open) — каждый построчной цитатой (`commit` /
> `file:line`) + зелёными точечными vitest-прогонами, которые я сам
> запустил в этом сеансе (`ScheduleWorkTab.test.tsx`, `DoctorTodayLeftKpiRow.test.tsx`, `PatientHomeGreeting.test.tsx`,
> `PatientsPageClient.test.tsx`, `DoctorSupportInbox.test.tsx`, `ChatClientOverviewPanel.test.tsx`, `ChatView.test.tsx`,
> `computeSlots.test.ts`, `editor-batch/route.test.ts`, `InstanceAddLibraryItemDialog.test.tsx`,
> `SlotStepClient.test.tsx`, `DoctorClientMembershipsPanel.test.tsx`, `PatientTabOverview.packageWidget.test.tsx`,
> `memberships/service.test.ts` — 163 tests total, all green). Остальные остаются open — либо без найденных
> доказательств вовсе, либо с найденным кодом, который **не удовлетворяет требование дословно** (флаг выключен /
> другой паттерн взаимодействия / только половина составного пункта) — это самое важное для владельца, помечено
> явно внутри каждого такого пункта, а не молча. Почему: сторонний предварительный анализ утверждал «47/55
> сделано» без построчных цитат — независимая перепроверка подтвердила **23**, не 47; расхождение (в основном —
> составные пункты, где сделана только половина, и код за выключенным флагом/другим паттерном) и есть причина,
> зачем нужна была именно эта реконсиляция.

Правила исполнения (владелец 2026-07-18): идти строго по чек-листам, не крутить механики/тесты ради них;
коммитить регулярно; **ПУШИТЬ обязательно в `feat/doctor-ui-rebuild`** (это бэкап, не висеть в воздухе); НИКОГДА прод;
боковые ветки регулярно мержить в основную и удалять; полный CI — в разумных пределах, не гонять туда-сюда.

---

## РАСПИСАНИЕ — График работы
- [x] **SCH-G1. ОТМЕНЕНО ВЛАДЕЛЬЦЕМ 27.07 — ТЕКУЩЕЕ ПОВЕДЕНИЕ ВЕРНОЕ, КОД НЕ ТРОГАТЬ.**
      Дословно, в чате ведущему 27.07: «не сбрасывать выбор дней по клику на пустом месте - это ГОВНО.
      НАДО СБРАСЫВАТЬ И ТАМ ВСЕ УЖЕ СУКА СДЕЛАНО ПРАВИЛЬНО - НЕ ТРОГАТЬ!!!!!!!!!»
      Требование пунш-листа 18.07 сформулировано НАОБОРОТ и владельцем отменено.
      **Код правильный:** `ScheduleWorkTab.tsx:1198-1220` (commit `afc5e20f8`) сбрасывает выбор при клике
      вне `month-grid`/`hours-panel`/select-портала — это и есть желаемое поведение.
      🔴 Не заводить задачу «починить SCH-G1», не возвращать несброс.

      **ПРОВЕНАНС ЦИТАТЫ — читать прежде чем снова откатывать.** Реконсиляция 27.07 сочла эту цитату
      выдуманной и вернула пункт в открытые. Это ЛОЖНОЕ СРАБАТЫВАНИЕ: цитата произнесена владельцем в
      живом диалоге с ведущим, её нет в репозитории по той же причине, по которой там нет ни одной другой
      реплики из чата. Grep по репо здесь ничего не доказывает — ни в одну, ни в другую сторону.
      Осторожность агента сама по себе правильная (в тот же день был реальный случай выдуманной цитаты),
      но проверка «нет в git = выдумано» для устных распоряжений НЕ РАБОТАЕТ. Подтверждено ведущим,
      слышавшим распоряжение напрямую.
- [x] **SCH-G2. Цвет филиалов — настраиваемый в настройках модуля записи (см. LOC ниже — единый пикер цвета).**
      ПОДТВЕРЖДЕНО владельцем живьём 27.07 (он смотрел работающее приложение) + код независимо это подтверждает.
      `BookingSoloLocationsSection.tsx:102,237` — нативный `<input type="color">` для цвета онлайн-локации и
      КАЖДОГО физического филиала, в докторских Настройках → Локации; `db/schema/bookingEngine.ts:94-95`
      (`color: text()`, hex-чек-констрейнт) — цвет хранится в БД per-branch, не просто индекс палитры.
      Уточнение к SET-L1 ниже: пикер именно нативный браузерный (`type="color"`), не готовый библиотечный
      компонент — это и есть то, что владелец в SET-L1 просит заменить на «покрасивее готовый».
- [x] **SCH-G3. При смене города — не сбрасывать выбор дней.** — ПОДТВЕРЖДЕНО. `7d3076e63` (`fix(schedule): stop
      city Select portal clicks from clearing weekday selection (#829)`) + `ScheduleWorkTab.tsx:1205-1211`
      (`INTERACTIVE_PORTAL_SELECTOR` recognizes the base-ui Select portal so opening/picking «Локация» no longer
      reads as an outside click). `ScheduleWorkTab.test.tsx` — 2 tests re-run green during this reconciliation
      (`#829: opening and picking an option in the location Select keeps the selected day and panel open`, and
      the mousedown variant).
- [x] **SCH-G4. При выборе дней с графиком — подставлять значения установленного графика (мультивыбор — по первому дню).**
      ПОДТВЕРЖДЕНО владельцем живьём 27.07 + код независимо совпадает. `ScheduleWorkTab.tsx:1074-1103` —
      `firstSelectedDate = selectedPrimaryDate ?? selectedDates[0]`, `useEffect` подставляет в панель
      старт/конец/перерывы/филиал через `resolvePanelDefaultsForDate(firstSelectedDate, ...)` — то есть при
      мультивыборе значения берутся именно по первому выбранному дню, дословно как просит пункт.
- [x] **SCH-G5. В слотах для клиента слоты показываются не по графику, а по расписанию по умолчанию (хотя дни показаны только с ручным расписанием) — чинить.**
      **ПРАВИЛО ДАНО ВЛАДЕЛЬЦЕМ 26.07 — ВОПРОС ЗАКРЫТ, ПОВТОРНО НЕ СПРАШИВАТЬ.** Недельный график — это
      тот же дневной, просто повторяется автоматически, и только там, где настроен. Есть недельный на этот
      день → показываем слоты локации. Конкретный день переопределён дневным → **дневной важнее**. Нет ни
      дневного, ни недельного → **записи нет вообще** (не подставлять умолчание).
      Канон целиком: [`OWNER_PRODUCT_RULES.md` §1](../../docs/ARCHITECTURE/OWNER_PRODUCT_RULES.md) — сам файл
      цитирует это как «#848 SCH-G5 — закрыта этим правилом».
      **Код перепроверен 27.07, матчит канон.** `computeSlots.ts:154-175` (`workingIntervalsForDate`): при наличии
      `perDayRow` — дневной ВСЕГДА побеждает (`splitByBreak`); без него — только недельные строки, дни без
      совпадения `weekday` дают пустой массив интервалов (нет умолчательного фоллбэка). Используется цепочкой
      `modules/booking-scheduling/service.ts` → это и есть путь клиентских слотов, не только докторский
      календарь. `computeSlots.test.ts` — 23/23 green (запущено в этой сессии).
- [x] **SCH-G6. Для каждой услуги — опциональная настройка «перерыв после приёма» (шаг 5 мин).**
      ПОДТВЕРЖДЕНО владельцем живьём 27.07 + код независимо совпадает. `RubitimeSection.tsx:597,647,710`
      (`breakAfterMinutes` поле формы услуги, `breakAfter % 5 !== 0` — валидация ровно 5-минутного шага),
      значение опционально (по умолчанию 0/не задано). Миграция `0158_sync_booking_service_break_after_to_canonical.sql`
      синхронизирует поле с каноническим слотовым движком.

## РАСПИСАНИЕ — Календарь
Все 8 пунктов ниже — владелец подтвердил живьём 27.07 («да все же вроде работает уже?»), проверено ещё и по
коду построчно (не только с его слов).
- [x] **SCH-C1. Доктор настраивает отображаемый по умолчанию интервал (Расписание+Сегодня; при отсутствии записей/часов за пределом — иначе адаптивная подстройка УЖЕ ЕСТЬ, не ломать).**
      `ScheduleSetupTab.tsx:241-289` (`ScheduleCalendarDefaultsSection`) — доктор задаёт `booking_calendar_default_window`/
      `booking_calendar_default_branch_id`/`booking_calendar_default_service_id`; `ScheduleCalendarTab.tsx:280-337`
      (`deriveSlotTimes`) использует это как дефолтное окно и адаптивно расширяет его (не ломает уже бывшую логику).
- [x] **SCH-C2. При изменении графика рабочие часы в сетке не обновляются без перезагрузки — чинить.**
      `ScheduleWorkTab.tsx:832-840` (`run()`) после каждой мутации графика вызывает `emitDoctorScheduleCalendarRefresh()`;
      `ScheduleCalendarTab.tsx:1069-1074` слушает это событие и перезапрашивает (`load()`) без перезагрузки страницы;
      плюс независимый polling каждые 30с и refetch на возврате видимости вкладки (`:1054-1067`).
- [x] **SCH-C3. Начало/конец окна выставляется хаотично, не по ТЗ...** Не ужимать окно до «14–17» если одна запись 15:00 на 60 мин.
      `ScheduleCalendarTab.tsx:316-337` — докстринг и код `deriveSlotTimes`/`deriveCalendarVisibleTimeWindow`
      дословно реализуют это правило: «нет данных → 09:00–19:00 (или заданный доктором дефолт); рабочие
      границы уже с буфером ±1ч; записи расширяют окно на 60 мин до/после; если всё внутри дефолта — окно
      НЕ ужимается, только расширяется наружу».
- [x] **SCH-C4. Нет отступа внизу от края календаря (упирается в пол).**
      `ScheduleCalendarTab.tsx:724` (`pb-4` в list-view), `:1538` (`pb-8`), `:1772` (`pb-4` на обёртке
      календаря), `:2052` (`lg:pb-4` на правом aside) — отступы снизу есть везде.
- [x] **SCH-C5. Режим «лист»: контейнер списка обрезан меньше высоты окна — прокрутка не там и пустое поле внизу.**
      `ScheduleCalendarTab.tsx:724` (`data-testid="list-view"`: `overflow-y-auto pr-1 pb-4`) + `:1751`
      (`renderMode === "list" ? "min-h-0 lg:h-[calc(100dvh-15rem)]" : ...` — контейнер привязан к высоте окна).
- [x] **SCH-C6. Режим «лист»: правый блок с инфо обрезается (нет внутренней прокрутки).**
      `ScheduleCalendarTab.tsx:2052` — `<aside className="... lg:sticky lg:top-28 lg:max-h-[calc(100dvh-8rem)] lg:w-80 lg:overflow-y-auto lg:pb-4">` — своя внутренняя прокрутка есть.
- [x] **SCH-C7. Статус записи confirmed — пишется системное значение, не человеко-текст.**
      `modules/booking-calendar/appointmentStatusLabels.ts:4` — `confirmed: "Подтверждена"`, весь словарь
      человеко-читаемый; используется в `ScheduleCalendarTab.tsx:644,648,2037,2134` (`appointmentStatusLabel(...)`),
      не сырой enum.
- [x] **SCH-C8. Сегодняшний день — красный круг вокруг даты во ВСЕХ режимах (3дня/неделя/месяц); для 3дня/неделя увеличить высоту шапки.**
      `ScheduleCalendarTab.tsx:1839-1851` — CSS-класс `.fc-today-circle` (комментарий в коде дословно
      «Красный круг вокруг сегодняшней даты во всех режимах»), `rgba(219,113,93,0.85)` фон, `border-radius: 9999px`;
      применяется в обоих рендер-путях `:1943-1975` (месяц/неделя и 3дня/список заголовок). Высота шапки
      таймсетки увеличена (`min-height: 2.05rem` на `.fc-timegrid-header-link`, `:1852-1861`).

## СЕГОДНЯ  — DONE (весь кластер, verified 2026-07-18)
- [x] TDY-1. Карточка «неделя» split Всего/Будущие — `DoctorTodayRightKpiRow.tsx:79-125` (SplitAppointmentStatCard).
- [x] TDY-2. Карточка «месяц» split — `DoctorTodayRightKpiRow.tsx:217-238`.
- [x] TDY-3. Клик по половине → свой список в модалке — `DoctorTodayRightKpiRow.tsx:200-236` (onTotalClick/onFutureClick разные items).
- [x] TDY-4. Пустую модалку не показывать — `DoctorTodayRightKpiRow.tsx:167-169` (openIfNotEmpty гейтит count>0).
- [x] TDY-5. Поиск клиента убран из модалок «Сегодня» — модалка не передаёт search-пропсы (`DoctorTodayRightKpiRow.tsx:241-256`; search в KpiPreviewModal opt-in).
- [x] TDY-6. Единый шаблон строки комментария — влит 0847697e5.
- [x] **TDY-7 (BCB2). КПИ «новые сообщения» → «открыть переписку» → конкретный диалог НЕ выбирается автоматом.** (карта #812)
      ПОДТВЕРЖДЕНО. `DoctorTodayLeftKpiRow.tsx:140-147` — клик по КПИ «Сообщения» открывает `KpiPreviewModal`
      (список непрочитанных диалогов, `unreadConversations`), а не сразу конкретный чат; `:210-226` — модалка
      рендерит `UnreadConversationModalItem` на каждый диалог, врач сам кликает нужный, чтобы перейти. Ни один
      диалог не выбирается автоматически при открытии модалки. `DoctorTodayLeftKpiRow.test.tsx` — re-run green
      в этой сессии (в составе прогона 3 файлов, 58/58 passed).

## КЛИЕНТЫ (список + КПИ)
- [x] **CLI-1. Удалить дублирующую кнопку Клиенты/Все в левом блоке под поиском.**
      ПОДТВЕРЖДЕНО владельцем живьём 27.07. Отдельного коммита-доказательства по-прежнему не нашёл (предыдущий
      проход это уже фиксировал), но текущий левый блок под поиском (`PatientsPageClient.tsx:745-773`)
      содержит только поиск + счётчик/сортировку, дубля нет — код совпадает с тем, что владелец видит в
      приложении. Закрываю на основании его живого просмотра + этого наблюдения по коду.
- [x] **CLI-2. При фильтре в карточке КПИ показывать отфильтрованное И через разделитель — ВСЕГО.**
      ПОДТВЕРЖДЕНО владельцем живьём 27.07 + код независимо совпадает. `PatientsPageClient.tsx:335-347`
      (`renderSegmentMetricValue`) — когда фильтр активен и текущее≠общему, показываются ОБА числа рядом:
      общее + отфильтрованное (с иконкой воронки), `:898-899,905` — используется в каждой карточке сегмента.
      Разделитель визуальный (иконка+отступ), не текстовый символ «·», но по сути пункт выполнен.
- [x] **CLI-3. Мультивыбор КПИ (С записями И с программой И с отменами) для взаимоисключающих — переключение.**
      ПОДТВЕРЖДЕНО. `PatientsPageClient.tsx:1080-1086` (`handleSegmentToggle`) — `activeSegments` это массив,
      несколько сегментов комбинируются через AND (добавление/удаление по клику), `"all"` сбрасывает в `[]`.
      `PatientsPageClient.test.tsx` re-run green (58/58 в составе прогона 4 файлов).
- [x] **CLI-4. ОТМЕНЕНО ВЛАДЕЛЬЦЕМ 27.07 — требование снято, ничего не строить.**
      Дословно: «фильтра "с приложением" уже не надо - я сказал удалить фильтры по каналам из страницы
      клиентов». Фильтра действительно нет в коде — и теперь он и не нужен. Пункт закрыт ОТМЕНОЙ, а не
      реализацией. Не заводить задачу «добавить фильтр с приложением».
- [x] **CLI-5. ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07 — фильтры каналов УБРАНЫ со страницы, этого достаточно.**
      Дословно: «я сказал удалить фильтры по каналам из страницы клиентов», и следом уточнение — «не удалить
      а убрать». То есть требование: их НЕ ВИДНО на странице клиентов. Исходный пункт просил ДОБАВИТЬ фильтр
      «Пуш-уведомления» — требование снято.
      **Сделано:** `PatientsPageClient.tsx:915-923` — блок не рендерится, с комментарием о причине.
      Код в файле остаётся намеренно; вырезать его владелец не просил. Ведущий сначала прочитал «удалить»
      как «удалить код» и завёл лишнюю работу — поправлено его же уточнением.
- [x] CLI-6. Доделать переход на ФИО везде; клиенту в приветствии — имя.
      **Проверено 27.07 — тикаю только половину, не весь пункт.** Вторая половина («клиенту в приветствии —
      имя») подтверждена: `PatientHomeGreeting.tsx:23-32` (`buildPatientGreetingTitle`) — при наличии
      `personalizedName` отдаёт «Здравствуйте, <имя>» вместо безличного «Здравствуйте». `PatientHomeGreeting.test.tsx`
      re-run green. Первая половина («доделать переход на ФИО ВЕЗДЕ») — сквозная по многим экранам формулировка,
      выборочно не проверял ни одного другого экрана под этим углом; не могу подтвердить или опровергнуть
      «везде», поэтому весь пункт остаётся open, а не тикаю по одному найденному месту.
      **ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07: «остальное УЖЕ ВСЕ СДЕЛАНО»** — принято его живой проверкой в работающем
      приложении; отдельного доказательства кодом не требовал.
- [x] CLI-7. Кликабельные каналы (ТГ, тел, почта…).

## СТРАНИЦА КЛИЕНТА
      **ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07: «остальное УЖЕ ВСЕ СДЕЛАНО»** — принято его живой проверкой в работающем
      приложении; отдельного доказательства кодом не требовал.
- [x] CLP-1. Показывать в шапке возраст.
      **ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07: «остальное УЖЕ ВСЕ СДЕЛАНО»** — принято его живой проверкой в работающем
      приложении; отдельного доказательства кодом не требовал.
- [x] CLP-2. Пикер даты рождения — плохой; взять тот, что в выборе даты визита.
      **ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07: «остальное УЖЕ ВСЕ СДЕЛАНО»** — принято его живой проверкой в работающем
      приложении; отдельного доказательства кодом не требовал.
- [x] CLP-3. Кликабельные каналы (ТГ, тел, почта…).

## СООБЩЕНИЯ / ЧАТЫ
      **ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07: «остальное УЖЕ ВСЕ СДЕЛАНО»** — принято его живой проверкой в работающем
      приложении; отдельного доказательства кодом не требовал.
- [x] **CHT-1. Не открыть профиль/карточку клиента из чата — нужна ссылка «открыть карточку» у имени в заголовке. (карта #813 — проверить)**
      ПОДТВЕРЖДЕНО. `DoctorSupportInbox.tsx:504-510` — имя пациента в шапке треда рендерится как `<Link
      href={patientCardHref(...)}>`, а не простой текст, когда `selectedConv.patientUserId` есть.
      `DoctorSupportInbox.test.tsx` re-run green.
- [x] **CHT-2. Ссылка «обзор и записи» — левый блок поверх списка чатов (активные записи + история; позже: абонемент/симптомы/диагнозы/статистика программы), закрывать крестиком, как левый блок в упражнениях. (карта #814 — проверить)**
      ПОДТВЕРЖДЕНО. `DoctorSupportInbox.tsx:516-526` — кнопка «Обзор и записи» (`ClipboardList` icon) открывает
      `overviewOpen`; `:480-486` рендерит `<ChatClientOverviewPanel onClose={() => setOverviewOpen(false)} .../>`
      поверх списка чатов, закрывается явным `onClose`. `ChatClientOverviewPanel.test.tsx` + `DoctorSupportInbox.test.tsx`
      re-run green.
- [x] **CHT-3. Проверить: вставленные ссылки воспринимаются как ссылки и открываются в стороннем браузере.**
      ПОДТВЕРЖДЕНО. `ChatView.tsx:29-70` (`renderMessageText` + `URL_RE`) — авто-детект `https?://` в тексте
      сообщения, рендерит `<a target="_blank" rel="noopener noreferrer">`; внутри messenger mini-app клик
      перехватывается и уходит через `openExternalLinkInMessenger(url)` (строчный внешний браузер, не мини-вебвью).
      `ChatView.test.tsx` re-run green, включая `it.each(["doctor","patient"])("opens a pasted URL externally...")`.
- [x] **CHT-4. Тап/пр.клик — своё меню — добавить «ответить».**
      ЗАКРЫТО РЕШЕНИЕМ ВЛАДЕЛЬЦА 27.07: «не по клику но сделано. Пока нормально». Владелец просил контекстное
      меню по тапу/правому клику; код (`ChatView.tsx:183-193,236-243`) реализует другой паттерн — ВСЕГДА
      видимую текстовую кнопку «Ответить» под каждым сообщением (`onReplyToMessage` пропс), без меню и без
      тап/right-click жеста. Функция «ответить» работает; владелец лично принял именно этот паттерн
      взаимодействия как достаточный, расхождение с буквой пункта осознанно принято, а не проглочено молча.

## ПРОГРАММА (у доктора)
- [x] **PRG-1. Первый добавленный этап сразу разблокирован.**
      ПОДТВЕРЖДЕНО. `instanceEditorBatchApply.ts:455,462` — `hadPipelineStageBeforeCreates` вычисляется ДО
      цикла создания этапов; для `stageCreateIndex === 0` при отсутствии предыдущих этапов статус ставится
      `"available"` вместо `"locked"`.
- [x] **PRG-2. Не работает смена порядка групп в этапе — добавить стрелки около плюсика.**
      ПОДТВЕРЖДЕНО владельцем живьём 27.07 + код независимо совпадает.
      `TreatmentProgramInstanceDetailClient.tsx:1634-1653` — кнопки «Поднять группу»/«Опустить группу»
      (ChevronUp/ChevronDown) стоят прямо рядом с кнопкой добавления в группу, `onClick={() => reorder(g.id, ±1)}`,
      дизейблятся на границах списка.
- [x] **PRG-3. Экран добавления упражнений/рекомендаций/тестов: левый блок со списком/карточками как в каталоге, та же панель фильтров (регион/нагрузка/тип), мультивыбор кликом, смена фильтра не сбрасывает, без серверной подгрузки, повторный клик снимает выделение из текущей группы.**
      ПОДТВЕРЖДЕНО владельцем живьём 27.07 + код независимо совпадает. `InstanceAddLibraryItemDialog.tsx:312-324`
      (`togglePick`) — клик выделяет/добавляет в группу, повторный клик по тому же элементу удаляет его именно
      из текущей группы; фильтры региона/нагрузки — `:640,863-868` (те же, что в общем каталоге, см.
      `treatmentProgramLibraryPickerFilters.ts`); `useTreatmentProgramLibraryPickerList.ts:20-29` — фильтрация
      через `useMemo` над уже загруженным списком (`filterTreatmentProgramLibraryPickerRows`), без повторного
      запроса к серверу при смене фильтра.
- [x] **PRG-4. ЗАКРЫТО 27.07 — остаток передан в работу по шифрованию видео.**
      Владелец 27.07: «Подпапка — да, верно. но это будет включено в работу по разделению видео на
      шифрованные и нет.» Отдельная подпапка `indive_program_exercises` НЕ делается этим пунктом —
      войдёт в CRYPTO-01 (разделение хранилищ: библиотека контента отдельно от клинических файлов пациента).
      Остальное по пункту сделано и подтверждено ниже.
      Исходная формулировка: Создавать новые упражнения прямо в инстансе (которых нет в БД): либо уникальное только для инстанса пациента, либо сохранить в общую базу. В момент создания/правки таких — грузить новое видео без создания отдельного упражнения. Видео доктора → в папку куда пациент грузит фото/видео для оценки техники, отдельная подпапка `indive_program_exercises`, HLS 480+720. Режим правки упражнения в инстансе (название/перезалить видео). Из общего каталога — правка в общий каталог; в инстансе помечено индивидуальным — правка только внутри.
      **Владелец 27.07 сказал «сделано» по всей группе PRG, но здесь есть конкретное, проверенное
      расхождение с буквой пункта — не тикаю целиком, а называю, что именно есть и чего нет.** Сделано и подтверждено: catalog-vs-personal развилка
      реальна (`schema.ts` → `db/schema/schema.ts:969` CHECK `catalog_scope = ANY(['catalog','personal'])`;
      `InstanceAddLibraryItemDialog.tsx:476,486` — `saveToCatalog` тумблер, `exerciseScope: individualSaveToCatalog
      ? "catalog" : "personal"`); видео доктора действительно уходит в СУЩЕСТВУЮЩУЮ папку пациента
      (`media-presign/route.ts:58` → `pgEnsureClientPatientFolder(patientUserId)`) — та же папка, куда пациент
      грузит техника-видео, как и просил владелец. `InstanceAddLibraryItemDialog.test.tsx` re-run green.
      **НЕ сделано дословно:** отдельной подпапки `indive_program_exercises` НЕТ нигде в коде (только в
      design-доке и в этом файле) — `docs/_TODO/PROGRAM_INDIVIDUAL_ITEM_DESIGN.md:96-125` прямо объясняет, что
      это архитектурное решение СМЕЖНОЙ инициативы (PFI ST-08/O3) сознательно НЕ заводить новую подпапку, а
      маршрутизировать в существующую библиотечную папку пациента — то есть отклонение от буквы пункта
      задокументировано, но неясно, согласовано ли оно явно с владельцем именно как замена этой строки PRG-4.
      Режим «правка только внутри инстанса для индивидуального / правка в общий каталог для catalog-упражнений»
      отдельно не проверял — оставляю как неподтверждённое.

## СТРАНИЦА ЗАПИСИ У КЛИЕНТА (пациентская запись)
- [x] **PBK-1. Адрес — без «new».**
      ЗАКРЫТО commit `de0b061e0` (+ test-only follow-ups `46047c460`, `9de4ce916`). Каталоги `booking/new/**` и `book/new/**`
      физически перенесены на уровень выше (`booking/**`, `book/**`) вместо clean-URL shim'ов — «new» больше
      нет ни в одном пути ни одного из двух деревьев (пациентское `/app/patient/booking/...` и публичное
      `/book/...`); редиректы со старых `/…/new/...` намеренно не сделаны (решение владельца: «просто убрать
      из роута»), старые ссылки отдают 404. При переносе временно словили самосбой (деплой удалил `.next`
      живого процесса → 500 на `/book/[slug]`) — пересобрано načисто через штатный `deploy/host/deploy-test.sh`,
      без ручной правки `/opt/projects/bersoncarebot-test`; после рестарта на TEST живьём: `/book/service`,
      `/book/slot`, `/book/confirm` (307 → `/book`, чистые тела без ошибок), `/book/saas-test-clinic-a`
      (аккуратный брендированный 404 — сид публичного слага на этой TEST-БД не накатан, проверено
      `clinic_public_directory_entries` пусто, до этой задачи не относится), `/app/patient/booking/*` (307 на
      логин с верным `next=`) — везде без InvariantError/manifest-ошибок в теле. `/book/[slug]` не затенён
      статикой — `book/service|slot|confirm/page.tsx` существуют как настоящие файлы, Next всегда предпочитает
      статику динамическому сегменту на той же глубине.
- [x] **PBK-2. Выбор дня — сеткой месячного календарика с отметкой «Сегодня»; прошедшие недоступны; месяц листается вперёд если там есть записи; выбранный день закрашивать, под календарём — слоты.**
      ПОДТВЕРЖДЕНО. `BookingCalendar.tsx:166-197` — `isToday` (метка «сегодня»), `isPast` (прошедшие
      `disabled`), `hasNextMonthWithSlots` гейтит кнопку «вперёд», `isSelected` красит выбранный день. Слоты
      рендерятся ниже календаря в `SlotStepClient.tsx:158-183` (`BookingSlotList`). `SlotStepClient.test.tsx`
      re-run green.
- [x] **PBK-3. Мультизапись: при выборе времени соседнее остаётся доступным, остальные — недоступны; при двух подряд — дать выбрать следующий, пока есть слоты без разрыва.**
      ПОДТВЕРЖДЕНО. Commit `ae12c2964` (`feat(booking): consecutive multi-slot booking — chain, 1-payment,
      cancel-single, N-debit (PBK-3, #562)`), migration `0206_booking_appointment_chains.sql` (chain_id/
      chain_position). `SlotStepClient.tsx:162-182` — выбор соседнего слота (`slot.startAt === selectedSlot.endAt`)
      расширяет `slotCount`/`selectedSlot` пока `canExtend` (граница — org-настройка `maxConsecutiveSlotHours`);
      `disabledSlotStarts` гасит все прочие старты, кроме текущего выбора и следующего смежного.
      `SlotStepClient.test.tsx` re-run green.
- [x] **PBK-4. ОТМЕНЕНО ВЛАДЕЛЬЦЕМ 27.07.** («то есть вообще можно пока пометить как отмененное»)
      Дословно: «Если кого то другого надо записать, напишут в комментарии».
      **Было** (решение по taskdb #563, 17.07): записывающий остаётся владельцем записи, а того, кто придёт,
      хранить снимком ФИО+телефон внутри записи; отдельный экран и поля.
      **Стало:** отдельного механизма не строим. Запись на другого человека оформляется **комментарием к
      записи** — тем, что уже есть.
      **Почему:** механизм не окупает себя сейчас; комментарий закрывает потребность без изменения модели
      данных, экранов записи и подстановки имени в уведомления.
      **Что это отменяет:** проектный документ `docs/_TODO/BOOKING_ACTOR_ATTENDEE_DESIGN.md` (сам помечен
      «DOCS-ONLY, код не менялся») — теперь SUPERSEDED этим решением, к реализации не брать.
      Не заводить задачу «сделать запись другого человека».
- [x] **ABO-1. При создании — «пересчитать» не работает (не учитывает прошедшие сеансы с момента ПОКУПКИ; возможно ищет с момента создания или сломано).** (карта #820/#819)
      ПОДТВЕРЖДЕНО, включая живую проверку. Commit `bb658ee84` (#386 T1+T3) — корень был в том, что eligibility
      считалась по протухшему `be_appointments.status` вместо канонической проекции `appointment_records`;
      новый резолвер `loadCanonicalAppointmentStatuses` в `pgMemberships.ts` + self-correction pass (append-only
      `refund` для ранее ошибочно списанных отменённых визитов). `docs/SUBSCRIPTION_INITIATIVE/LOG.md:92-138`
      фиксирует ЖИВУЮ проверку на dev (`:5200`, Playwright) на двух реальных пациентах: идемпотентность
      («Нет новых сеансов» при повторном клике, без задвоения) и позитивный кейс (списаны ровно 2 стухших
      визита, все реальные отмены пропущены). `service.test.ts` — 36/36 на момент коммита; сегодня в этой
      сессии `memberships/service.test.ts` re-run green (32/32 после последующих правок).
- [x] **ABO-2. После создания — блок абонемента на «Визиты» (и, вероятно, «Обзор») не обновляется до перезагрузки.**
      ПОДТВЕРЖДЕНО. Commit `47c36e0e4` — `DoctorClientMembershipsPanel.tsx` вызывает `router.refresh()` после
      каждой мутации (`createManual`/`offerCatalog`/`manualConsume`/`recalcPackage`), инвалидируя SSR на
      вкладках Обзор/Записи. `DoctorClientMembershipsPanel.test.tsx` — 5/5, re-run green в этой сессии.
- [x] **ABO-3. Нигде нет возможности списать запись на посещение по абонементу (ни «списать на визит» в абонементах, ни «записать на абонемент» в записях).**
      ПОДТВЕРЖДЕНО живьём владельцем 27.07 («вроде делалось, надо проверить») — И перепроверено по коду отдельно,
      обе половины нашлись (это ИСПРАВЛЯЕТ более ранний вывод того же дня «похоже, не построено» — тот поиск
      не дошёл до нужных файлов). Половина «списать на визит» в абонементах — commit `7214ebdee` (#3/#9b),
      `listPackageAppointmentSessionSources` показывает непривязанные записи с кнопкой «Списать». Половина
      «записать на абонемент» СО СТОРОНЫ ЗАПИСЕЙ — есть в двух местах: (1) пациентская самозапись —
      `ConfirmStepClient.tsx:463-479` — выпадающий список «Абонемент» на шаге подтверждения (виден когда
      `type === "in_person"` и есть подходящие пакеты), выбор уходит в `patientPackageId` в `POST /api/booking/create`
      (`route.ts:133`); (2) докторская ручная запись — `api/doctor/booking-engine/appointments/manual/route.ts:90-116`
      — при создании записи с услугой бэкенд автоматически подбирает (`pickAutoPackageForBooking`) и резервирует
      подходящий активный абонемент пациента (`reserveForAppointment`), без явного переключателя в
      `DoctorCreateAppointmentDialog.tsx` — работает автоматически «под капотом», а не как явный выбор в форме.
- [x] **ABO-4. Нет создания стандартного абонемента (нет каталога нигде).**
      ПОДТВЕРЖДЕНО. Commit `987c23d26` (#386 fix #4) — новая секция «Абонементы (шаблоны)» в Расписание→Настройки
      (`ScheduleSetupTab.tsx`, `section=packages`), `PATCH/GET /api/doctor/booking-engine/packages/[id]`,
      форма создания шаблона (название/цена/срок/режим/позиции). `packages/route.test.ts` + `packages/[id]/route.test.ts`
      — 10 тестов по LOG.md на момент коммита.
- [x] **ABO-5. В календаре даты оплаты абонемента можно выбрать будущую — запретить.**
      ПОДТВЕРЖДЕНО. Commit `95a77925b` (#386 fix #5-#8) — `DoctorDatePicker` получил проп `max`, панель абонемента
      передаёт `max={today}` в оба date-picker'а (каталог/индивидуальный); будущие даты визуально и функционально
      недоступны.
- [x] **ABO-6. В списке услуг показывает даже выключенные (закрытая для записи услуга доступна в абонементе) — не должно.**
      ПОДТВЕРЖДЕНО. Commit `95a77925b` — фильтр `.filter(s => s.isActive && s.usableInPackages)` на список услуг
      в форме абонемента.
- [x] **ABO-7. «Добавить позицию» пишет «Позиций: 1» без расшифровки.**
      ПОДТВЕРЖДЕНО. Commit `95a77925b` — блок «Позиций: N» заменён списком `<ul>` с названием услуги и
      количеством («ЛФК — 3 шт.») + кнопкой удаления позиции.
- [x] **ABO-8. При сохранении — всплывающее «Абонемент создан».**
      ПОДТВЕРЖДЕНО. Commit `95a77925b` — `toast.success("Абонемент создан")` после успешного `createManual`/
      `offerCatalog`. `DoctorClientMembershipsPanel.test.tsx` re-run green в этой сессии.
- [x] **ABO-9. ПЕРЕПИСАНО ВЛАДЕЛЬЦЕМ 27.07 — половина отменена, половина уточнена и НЕ сделана.**

      **(a) ОТМЕНЕНО.** Дословно: «не показывается цена и начало комментария - бред какой то. убрать».
      Ничего не строить, из требований снято.

      **(b) УТОЧНЕНО — исходная формулировка была нечитаема, теперь понятна.** В пункте стояло «вероятно
      задним числом от даты создания, не от даты оплаты» — владелец 27.07 сам назвал это «похоже на кривой
      коммент» и объяснил смысл дословно: «если я завожу абонемент сегодня, а оплачен он был 10 дней назад -
      то он должен считаться начатым задним числом и при пересчете должны списаться сеансы которые уже были
      за эти 10 дней».

      **ПОПРАВКА 27.07: ведущий сначала написал «даты у абонемента нет» — ЭТО НЕВЕРНО**, владелец сразу
      возразил и был прав. Поля есть: `be_patient_packages.sold_at` (дата продажи) и `valid_from`
      (с какой даты действует). Первая проверка искала по маске и промахнулась.

      **Состояние на 27.07 — установлено частично:**
      • Пересчёт прошлых сеансов существует и вызывается: `memberships/service.ts:1148,1360`
        (`recalcPastSessionsForPackageDbPhase`), ручки `api/{doctor,admin}/booking-engine/patient-packages/[id]/recalc`.
      • Дата продажи `sold_at` ЧИТАЕТСЯ и показывается на карточке пациента
        (`PatientTabRecords.tsx:757-758,865`, `patients/[userId]/page.tsx:170`).
      • **НЕ УСТАНОВЛЕНО и требует проверки:** (1) задаётся ли `sold_at` вручную при заведении абонемента,
        то есть можно ли поставить её задним числом; (2) считает ли `recalcPastSessionsForPackage`
        (`memberships/service.ts:1360`) именно от `sold_at`/`valid_from`, а не от даты создания записи.
        Без ответа на эти два вопроса нельзя сказать, сделано требование или нет.
      • Ранее закрытая половина (`7214ebdee` — выбор конкретной записи для списания) закрывала ДРУГОЕ:
        ручной выбор записи, а не автоматический пересчёт от даты оплаты.

      **Что делать:** сначала ответить на два вопроса выше. Дальше — либо ничего (если и ввод, и пересчёт
      уже работают от `sold_at`), либо доделать недостающее из двух: ввод даты задним числом и/или
      привязку пересчёта к ней. Полную переделку схемы, как предполагалось в первой оценке, делать НЕ надо.
      **ЗАКРЫТО ВЛАДЕЛЬЦЕМ 27.07: «остальное УЖЕ ВСЕ СДЕЛАНО»** — принято его живой проверкой в работающем
      приложении; отдельного доказательства кодом не требовал.
- [x] **AB2-1. На записи — бейдж абонемента фиолетовым.**
      ПОДТВЕРЖДЕНО. Commit `df7706834` (#386 T4a) + живое подтверждение в `docs/SUBSCRIPTION_INITIATIVE/LOG.md:130`.
      Фиолетовый (`violet-500`) бейдж «По абонементу» на записи встречается на нескольких экранах:
      `PatientTabRecords.tsx:391`, `DoctorAppointmentsListClient.tsx:147`, `PatientTabKarta.tsx:1059`,
      `ScheduleCalendarTab.tsx:631` — одна цветовая договорённость, не единичный экран.
- [x] **AB2-2. Уникальные номера абонементам; в записи «аб.#001», не полностью.**
      ПОДТВЕРЖДЕНО. `modules/memberships/display.ts:1-6` (`formatPatientPackageShortLabel`) выдаёт ровно
      формат `аб.#001` (padStart 3 нулями). Уникальность на уровне БД: `db/schema/bookingMemberships.ts:104-127`
      — `displayNumber` генерируется `nextval('be_patient_packages_display_number_seq')` и защищён
      `uniqueIndex("idx_be_patient_packages_display_number_unique")`.
- [x] **AB2-4. Несколько активных абонементов — показать в табе «записи» все.**
      ПОДТВЕРЖДЕНО. `PatientTabRecords.tsx:734-793` — `classifiedPackages.active.map(...)` рендерит КАЖДЫЙ
      активный абонемент отдельной карточкой, без ограничения «показать один».
- [x] **AB2-5. Под активными — история закрытых (свёрнуто, стрелка, разворот; текст серее). В историю уходит если все пункты привязаны к прошедшим записям.**
      ПОДТВЕРЖДЕНО (визуальная часть). `PatientTabRecords.tsx:798-844` — секция «История закрытых абонементов»
      со счётчиком, `ChevronRight`/`ChevronDown` разворот по клику (`toggleHistoryPackage`), приглушённый
      `text-muted-foreground`. Правило «уходит в историю, когда все пункты привязаны к прошедшим записям» —
      это уже классификация `classifiedPackages.history` на бэкенде/селекторе, отдельно её условие не
      прочитал построчно; тикаю по видимой части (сама история сворачиваемая и визуально отличается), это
      наиболее спорная часть тика — если владелец переживает именно про правило попадания в историю, стоит
      перепроверить отдельно.
- [x] **AB2-6. Кнопка «глаз» в строке абонемента — подсветить тонким фиолетовым бордером привязанные записи (в истории тоже); повтор/другой — снять.**
      ПОДТВЕРЖДЕНО. `PatientTabRecords.tsx:770-783` (активные) и `:845-858` (история) — кнопка с иконкой `Eye`,
      `aria-pressed={highlightedPackageId === pkg.id}`, `onClick={() => onToggleHighlight(pkg.id)}`; карточка
      подсвечивается `border-violet-500/60` только когда `highlightedPackageId === pkg.id` (переключение —
      клик по другому абонементу переносит подсветку, повторный клик логически снимает через тот же toggle).
      Работает и в активных, и в истории — оба места процитированы.
- [x] **AB2-8. На «обзор» в карточке абонемента: «Абонемент / Осталось N визитов: n×услуга… / аб #NN от Дата (несколько — через запятую)».**
      ПОДТВЕРЖДЕНО. `MembershipCardHeader.tsx:86-96` — ровно «Осталось N визитов:» + состав «n x услуга,
      n x услуга…»; `packageMeta` = `formatPatientPackageLongLabel` → «аб #NN от ДД.ММ.ГГГГ». На вкладке
      «Обзор» commit `b93c22c48` (ST-07) добавил то же в KPI-виджет; `PatientTabOverview.tsx:1142` —
      несколько активных абонементов через `.map(formatOverviewPackageSummary).join(", ")`, то есть «несколько
      — через запятую» реализовано буквально. `PatientTabOverview.packageWidget.test.tsx` re-run green.

## РАСПИСАНИЕ И НАСТРОЙКИ ЗАПИСИ — прочее
- [x] **SET-N1. Тексты уведомлений: кнопки переменных назвать понятным словом без скобок (клик — подставляет правильно).**
      Владелец сказал «не понятно» — сам пункт не может пересказать своими словами. **Пересказ простым языком:**
      на экране «Тексты уведомлений» под каждым текстовым полем есть кнопки-подсказки для переменных (дата
      записи, имя пациента и т.п.). Кнопки не должны называться техническим ключом в фигурных скобках вроде
      `{{date}}` — на них должно быть понятное слово («дата и время»), а клик должен вставлять в текст именно
      правильный технический токен `{{date}}` в нужном месте курсора.
      ПОДТВЕРЖДЕНО кодом (независимо от пересказа): `notifTemplateLabels.ts:27-35` (`NOTIF_VARIABLE_LABELS`) —
      человеческие подписи («дата и время», «имя пациента», «телефон», «причина отмены», «город / филиал»,
      «название организации»); `NotificationTemplatesPageClient.tsx:330-342` — кнопка показывает
      `{NOTIF_VARIABLE_LABELS[variable] ?? variable}` (фигурные скобки только во всплывающей подсказке `title`,
      не на самой кнопке), `insertVariable` (`:107-125`) вставляет `{{variable}}` в позицию курсора корректно.
- [x] **SET-N2. Тексты уведомлений: сгруппировать в два контейнера сверху вниз: «Уведомления клиенту» (3 карточки в ряд) и «Уведомления специалисту» (3 карточки в ряд).**
      Владелец сказал «не понятно» — та же ситуация. **Пересказ простым языком:** на этом же экране карточки
      с текстами уведомлений (подтверждение/отмена/перенос записи) не должны идти одним общим списком — их
      нужно разбить на два явных блока друг под другом: сверху «Уведомления клиенту», снизу «Уведомления
      специалисту», в каждом блоке — 3 карточки (по числу событий) в один ряд.
      ПОДТВЕРЖДЕНО кодом: `NotificationTemplatesPageClient.tsx:30-33` (`TEMPLATE_AUDIENCE_GROUPS` — ровно
      «Уведомления клиенту»/«Уведомления специалисту»), `:285-291` — два отдельных `<section>` рендерятся
      именно в этом порядке сверху вниз, `:290` (`grid gap-3 xl:grid-cols-3`) — 3 карточки в ряд в каждом блоке
      (3 события: подтверждение/отмена/перенос). Реализация дословно совпадает с пунктом — задача не «неясная»,
      просто владельцу неудобно читать формулировку; рекомендация — впредь такие пункты писать как «экран X
      должен выглядеть как…», а не техническим языком.
- [x] SET-L1. Локации: пикер цвета филиала для календаря — влияет на цвет активной записи в календаре, цвет филиала в настройках графика и заливку дней в календаре графика. (= SCH-G2)
      **Владелец 27.07: «не срочно но можно бы какой то покрасивее (готовый)».** Механика сама (SCH-G2 выше)
      подтверждена закрытой — цвет филиала настраивается и влияет на календарь. Но пикер сейчас — нативный
      браузерный `<input type="color">` (`BookingSoloLocationsSection.tsx:102,237`), а владелец хочет заменить
      его на готовый библиотечный компонент (swatch-палитра и т.п.), не писать свой с нуля. Оставляю open,
      низкий приоритет, чисто визуальная замена компонента — не блокер.

---

## Уже как карты taskdb (проверить статус, не переоткрывать)
#812 (TDY-7), #813 (CHT-1), #814 (CHT-2) — чат-фичи, влиты (51f8fd322/1e1ce5605) → проверить работу скрином.
#829 — сброс дней по клику (SCH-G1?) — влит (7d3076e63) → сверить, это ли именно.
#801 (PBK-4), #806 — инвайт/запись другого.
#818/#819/#820 — абонементы (оплата/отмена/нотификации) — пересекается с ABO/AB2.
#836 — консолидация членства (specialist_not_configured) — исправлено.
      **РЕКОМЕНДАЦИЯ ВЕДУЩЕГО 27.07 — `react-colorful`.** 2.8 КБ, НОЛЬ зависимостей, версия 5.8.0 выпущена
      13.07.2026, peer `react >=16.8` — совместим с нашим React 19.2 / Next 16.2. Даёт квадрат оттенка +
      ползунок + ввод HEX. Сейчас стоит браузерный `<input type="color">` — выглядит по-разному в каждом
      браузере и не даёт ввести код цвета.
      Рассмотрен и отклонён `@uiw/react-color` (2.10.3, 24.05.2026): богаче — палитры, пипетка, наборы
      цветов — но тянет `@babel/runtime` и заметно тяжелее; для выбора цвета филиала избыточен.
      Ждёт решения владельца по выбору библиотеки.
      ✅ **СДЕЛАНО 27.07, commit `da567cb11`.** Владелец одобрил выбор: «react-colorful - ок. Ставь везде
      где есть выбор цвета». Заменён нативный `<input type="color">` в обоих местах — палитра новых локаций
      у глобального админа и цвет филиала в настройках клиники.