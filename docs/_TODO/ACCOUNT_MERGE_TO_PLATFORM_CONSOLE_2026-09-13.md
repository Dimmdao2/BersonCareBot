# Перенос объединения дублей учётных записей в консоль платформы

**Заведено:** 13.09.2026, по прямому решению владельца.
**Ветка реализации:** не начата.
**Карточка taskdb:** см. первую строку тела карточки (ссылка на этот файл).

## Решение владельца (13.09, дословно)

- «объединения карточек говорил врачу — у врача его вообще не должно больше быть этого механизма»;
- «это для админа платформы»;
- «роль сессии — admin — раньше так было, теперь админ клиники это не админ платформы»;
- «убрать с карточки пациента, а перенос завести отдельной задачей».

Первая половина решения выполнена в `3255e8a8f` (приземлено в `feat/doctor-ui-rebuild` как
`c5772d413`): панель снята с обоих мест монтирования у врача. Этот план — про вторую половину.

## Что сейчас есть в репозитории

Механизм цел и рабочий, у него просто **нет ни одного места монтирования**:

| Часть | Файл | Состояние |
|---|---|---|
| UI-панель | `apps/webapp/src/app/app/doctor/clients/AdminMergeAccountsPanel.tsx` | цела, нигде не смонтирована |
| Логика подготовки решения | `apps/webapp/src/app/app/doctor/clients/adminMergeAccountsLogic.ts` | цела |
| Слияние | `apps/webapp/src/app/api/doctor/clients/merge/route.ts` | `requirePlatformOperationsApiContext` |
| Кандидаты по человеку | `apps/webapp/src/app/api/doctor/clients/[userId]/merge-candidates/route.ts` | `requirePlatformOperationsApiContext` |
| Предпросмотр слияния | `apps/webapp/src/app/api/doctor/clients/merge-preview/route.ts` | `requireAdminApiContext` |
| Поиск второго профиля | `apps/webapp/src/app/api/doctor/clients/merge-user-search/route.ts` | `requireAdminApiContext` |

Обе двери сходятся на «сессия платформенного админа» (`requireAdminApiContext` требует
`session.user.role === 'admin'`, а админ организации ходит с ролью `doctor` и `membershipRole: 'admin'`),
то есть утечки к врачу нет. Но один механизм охраняется **двумя разными охранниками** — это надо свести
к одному при переносе, иначе следующая правка одной двери разъедется со второй.

## Чего нет

В консоли платформы нет поверхности «люди»: `shared/ui/doctor/platformNavLinks.ts` содержит
организации, коммерцию, платежи, настройки, авторизацию, бронирование, шаблоны, интеграции,
уведомления, техничку, здоровье, архив и аудит-лог — раздела учётных записей среди них нет.
Карточка организации (`app/admin/clinics/[organizationId]/page.tsx`) аккаунтов тоже не показывает
(это отдельный незакрытый кусок `#1068`: у роли `app_platform_settings` нет гранта на
`be_organization_members`).

Панель принимает `anchorUserId` — она построена вокруг «вот этот человек, найди ему дубля». В консоли
платформы якоря нет: оператор начинает с поиска человека. Значит нужен экран, а не перенос компонента
один в один.

## Этапы

- [ ] **М-1. Решение владельца по месту.** Куда в консоли платформы встаёт объединение: отдельный
      пункт меню «Учётные записи» с поиском человека, либо вкладка в карточке организации, либо
      действие из аудит-лога. Рекомендация: отдельный пункт, потому что дубли по своей природе живут
      МЕЖДУ организациями, а карточка организации показывает только своих.
- [ ] **М-2. Экран.** Страница под `/app/admin/*`: поиск человека → выбор якоря → существующая панель.
      Панель переносится из `app/doctor/clients/` в платформенную папку вместе с
      `adminMergeAccountsLogic.ts`; из неё уходит `useDoctorPatientTerms` (терминология врача) и
      `doctorClientCardChrome`.
- [ ] **М-3. Одна дверь вместо двух.** Все четыре маршрута приводятся к `requirePlatformOperationsApiContext`
      и переезжают из `api/doctor/clients/*` в платформенное пространство маршрутов — сейчас механизм
      админа платформы физически лежит в API врача.
- [ ] **М-4. Тексты.** Панель уже прошла чистку 13.09 (машинные слова `merged_into_id`, hex-UUID убраны);
      при переносе проверить, что новый экран не вводит их заново, и что `pnpm lint` (гейт
      `check-notification-text-coverage.mjs`) зелёный.
- [ ] **М-5. Удалить хвост.** После переезда в `app/doctor/` не должно остаться ни панели, ни логики,
      ни маршрутов `merge*`.

## Границы

- Автоматический дедуп при регистрации/бронировании не трогаем — это другой механизм.
- Гранты БД: если экран начнёт показывать состав организации, это уже кусок `#1068` (грант
  `be_organization_members`), а не этот план.
- Тестов на формулировки не писать (решение владельца 13.09).
