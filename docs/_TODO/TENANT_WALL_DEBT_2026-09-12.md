# Стена арендатора: закрыть долг по факту, а не списком исключений

**Источник — решение владельца 12.09.2026, дословно:** «надо проверки делать, они списки исключений
делать — какая-то херня, какой-то бред… это всё надо переделывать… таблица-то есть? Надо просто их
правильно оформить, и проверки, и схему… список исключений просто убрать, у нас не должно быть никаких
исключений… не надо городить запрет ради запрета, надо просто, чтобы не было никакого списка
исключений». Найдено по дороге при работе над `EXERCISE_STORE_PLAN.md` §7 (п.5, 6в, 6г) — не про магазин
упражнений, отдельная работа, не блокирует ничей деплой.

Этот файл — единственный источник «todo»/«done» по этой работе. Ветка отдельная, в отдельном ворктри,
параллельно основной очереди.

## Что чинить

**А. 30 таблиц несут `organization_id`, но не объявлены `org: true`** в декларации прав
(`deploy/postgres/privileges/declaration.ts`) — значит инвариант арендной стены их не проверяет вообще.
Точный список (снят с `ORG_FLAG_CENSUS_BASELINE`,
`deploy/postgres/privileges/tenant-predicate-invariant.test.mjs:151`):

```
be_appointment_cancellations, be_appointment_history_events, be_appointment_no_shows,
be_appointment_reschedules, be_appointment_staff_comments, be_booking_form_submissions,
be_package_history_events, be_package_usages, be_patient_booking_profiles, be_patient_packages,
be_patient_timeline_events, be_payment_history_events, be_payment_intents, be_payments, be_refunds,
specialist_tasks, support_conversation_messages, support_conversations, support_question_messages,
support_questions, symptom_entries, symptom_trackings, system_settings, test_set_items, test_sets,
treatment_program_template_stage_groups, treatment_program_template_stage_items,
treatment_program_template_stages, treatment_program_templates, user_phone_history
```

Для КАЖДОЙ: замерить, действительно ли строка принадлежит одной клинике (арендатору) или это
легитимно платформенная/общая таблица (например `system_settings` может нести оба смысла — разобрать
по факту, не предполагать). Если да — поставить `org: true` и правильный предикат стены; если это
живая дыра (не просто пропущенный флаг, а реально читаемые чужие строки) — это САМ ПО СЕБЕ security
finding, поднимать немедленно, не через этот план.

**Б. 6 таблиц в реальной базе отсутствуют в drizzle-схеме webapp вовсе** — автоматическая перепись их не
видит в принципе (найден пример `public.broadcast_drafts`, остальные пять — найти самим: сравнить живой
список таблиц кластера со списком, который экспортирует drizzle-схема). Как минимум одна из них,
`public.system_settings_audit`, тоже несёт `organization_id` без объявленной стены. Для каждой — завести
в drizzle-схему по образцу соседних таблиц, затем применить пункт А.

**В. Убрать сам список исключений (`ORG_FLAG_CENSUS_BASELINE`) целиком.** Не «заморозить» и не
«запретить туда дописывать» — владелец прямо сказал: списка исключений быть не должно. Когда А и Б
закрыты по факту (все 36 таблиц правильно объявлены и проверены), тест
`tenant-predicate-invariant.test.mjs` должен проверять ВСЕ таблицы без списка-заглушки; `unexpected`
должен быть пуст сам по себе, не через вычитание базового множества.

**Г. Заодно (владелец 12.09, согласился «конечно да, надо делать»): триггер владения ребёнка** —
`clinical_test_regions` и `recommendation_regions` сегодня защищены только CHECK'ом, без триггера
`app.enforce_lfk_child_owner()`-подобного (у семейства ЛФК он есть, доказано инъекцией — чужой ребёнок
к платформенному родителю отбит; у этой пары — принят). Завести такой же триггер на эти две таблицы.
Заодно проверить (это и есть суть замка): клиника может свободно привязывать/отвязывать записи только
в СВОИХ собственных справочниках — чужие/платформенные трогать не должна ни при каких условиях.

## Как исполнять

- Отдельная ветка/ворктри от `feat/doctor-ui-rebuild`, не мешает текущему циклу деплоя.
- Каждая из 36 таблиц — по факту, не оптом: реальный предикат стены, живая проверка (сессия одной
  клиники не видит строк другой), а не только снятие флага.
- Независимый аудит перед приземлением (это RLS/мультитенантность — цена ошибки высокая).
- Живой прогон `tenant-predicate-invariant.test.mjs` без списка исключений — 0 расхождений.
- Отчёт: что было причиной по каждой таблице (легитимно платформенная / забыли флаг / реальная дыра),
  что сделано, чем доказано.

## Статус

- [x] Пункт А — 30 таблиц разобраны и объявлены. Отметка `org` — это ПЕРЕПИСНОЙ ФАКТ «колонку
      `organization_id` измерили» (`deploy/postgres/privileges/types.ts:188`), а не «каждая строка
      принадлежит клинике»: она кормит `orgTableAllowlist` и вводит таблицу в инвариант A1.
      Структура (замер на `bcb_webapp_dev`, `information_schema.columns` + `count(*) FILTER (WHERE
      organization_id IS NULL)` по каждой из 30): у **15** таблиц из 30 `organization_id` объявлен
      NOT NULL — платформенной строки в них быть не может в принципе; остальные **15** колонку
      допускают пустой, но реально пустые значения есть ровно у ОДНОЙ из тридцати —
      `system_settings` (146 строк из 159). `system_settings_audit` (48 из 329) тоже их несёт, но
      он НЕ входит в эти 30: он из пункта Б, и складывать их в один счёт нельзя.
      **`system_settings` действительно несёт ОБА смысла — ровно как предупреждал план.** Стена у
      неё разведена ПО ОПЕРАЦИЯМ, и это и есть правильное оформление: на SELECT арендатор видит свои
      строки ИЛИ платформенные со `scope='doctor'` (замерено живьём сессией клиники A: 13 своих +
      12 платформенных), а на INSERT/UPDATE/DELETE — только `organization_id = app.current_org_id()`,
      то есть тронуть платформенную строку не может; платформенные строки принадлежат роли
      `app_platform_settings`. Ни одна из 30 политик этим коммитом не изменилась (см. ниже — сузились
      19 ДРУГИХ таблиц). Предикат стены у всех тридцати уже стоял верный — не хватало только
      переписной отметки `org: true`, поэтому
      инвариант A1 их молча пропускал. Замер: `deploy/postgres/privileges/declaration.ts`, 30 строк
      `org: true`; живая проверка контекст-флипом (сессия клиники B считает строки клиники A):
      **29 таблиц из 30 — 0 видимых чужих строк**. Тридцатая, `user_phone_history`, показала `1`.
      Развилка ушла владельцу и ЗАКРЫТА 12.09.2026 дословно: «История принадлежит человеку» —
      см. отдельный пункт ниже.
      ЧЕСТНО О СИЛЕ ДОКАЗАТЕЛЬСТВА: контекст-флип на ПУСТОЙ таблице не доказывает ничего (на DEV
      пусты `be_appointment_no_shows`, `be_package_history_events`, `be_package_usages`,
      `be_patient_booking_profiles`, `be_patient_packages`, `be_payment_history_events`,
      `be_payment_intents`, `be_payments`, `be_refunds`, `broadcast_drafts`,
      `clinic_dedicated_bot_bindings`, `patient_comorbidity`). Для них доказательство структурное:
      `organization_id NOT NULL` + предикат стены, сверенный по тексту политики, + инвариант A1,
      который теперь их проверяет. Данными подтверждены те, где строки есть.
- [x] Пункт Б — 6 таблиц заведены в drizzle-схему + объявлены. В декларации прав они БЫЛИ — их не
      видела перепись колонок, потому что бочонок `apps/webapp/db/schema/index.ts` их не
      экспортировал. Заведены `broadcastDrafts`, `clinicDedicatedBotBindings`, `systemSettingsAudit`,
      `bookingCalendarMap`, `emailOtpLocks` (пять новых файлов, структура снята с pg_catalog
      `bcb_webapp_dev`) + возвращён в экспорт уже существовавший `patientComorbidities`.
      `system_settings_audit` дополнительно получил `org: true` (пункт А).
      `booking_calendar_map` и `email_otp_locks` организации не несут ПО ЗАМЫСЛУ — это не пропуск:
      обе служебные, прикладным ролям не открыты вовсе, ходят только названные definer-швы.
- [x] Пункт В — список исключений убран из теста целиком. `ORG_FLAG_CENSUS_BASELINE` удалён из
      `deploy/postgres/privileges/tenant-predicate-invariant.test.mjs`; `unexpected` пуст САМ ПО СЕБЕ,
      без вычитания. Добавлена недостающая половина проверки — `relationsInvisibleToCensus()`: живая
      таблица, которой нет в drizzle-схеме, теперь роняет тест, а не исчезает из поля зрения (именно
      так и прятались 6 таблиц пункта Б).
- [x] Пункт Г — триггер владения на `clinical_test_regions`/`recommendation_regions`. Миграция
      `apps/webapp/db/drizzle-migrations/20260912T110000_the_catalog_child_gets_the_same_owner_guard_as_lfk.sql`
      расширяет ОБЩИЙ `app.enforce_lfk_child_owner()` (не вторая почти такая же функция, AGENTS.md §5),
      отказ назван своим именем `catalog_child_owner_mismatch`, текст отказа ЛФК не тронут.
      Доказано инъекцией на DEV в откатываемой транзакции, ДО и ПОСЛЕ:
      до — org-ребёнок к платформенному родителю принимался (`INSERT 0 1` на обеих таблицах);
      после — G1/G2 отказ на платформенном родителе, G3 отказ клинике B на родителе клиники A,
      G4 отказ platform-ребёнку на родителе клиники, G7 отказ ПЕРЕВЕШИВАНИЮ существующей строки,
      G8 отказ подмене `organization_id`, G9 ЛФК по-прежнему `lfk_child_owner_mismatch`,
      G5/G6 законные привязки проходят, G10 все живые строки обеих таблиц проверку проходят
      (ложных срабатываний нет). Тот же набор РОЛЬЮ АРЕНДАТОРА через настоящий `begin_port_context`
      (G11a/G11b) — отказ; отцепление (G11c) — `DELETE 0` по платформенным строкам при том, что
      читает их арендатор законно (G11d: видит 3). Preflight миграции: PASS, pending=1, откачено.
- [x] Пункт Д (развилка владельца закрыта 12.09.2026: «История принадлежит человеку») —
      `public.user_phone_history` лишилась колонки `organization_id` целиком. Миграция
      `apps/webapp/db/drizzle-migrations/20260912T150000_the_phone_history_belongs_to_the_person_not_the_clinic.sql`.
      Колонка ОБЕЩАЛА стену арендатора, которой никогда не было: `B_SEES_A=1` получился не из-за
      сломанной стены, а потому что ветка персонала спрашивает НЕ КОЛОНКУ, а членство ЧЕЛОВЕКА
      (EXISTS по `be_organization_members`/`org_enrollments`) — собственный `organization_id` строки
      не проверял никто. Подтверждают код и данные: оба pre-session корня писали в колонку
      ЛИТЕРАЛЬНЫЙ NULL, а 94 живые строки заполнял единственный писатель `pgPhoneHistory.ts`,
      беря клинику из принципала, — то есть хранилось «где человек был», а не «чья запись».
      Стена tenant-service переехала с прямой колонки (D) на членство человека (M) — тот же EXISTS,
      что уже держал персонал. Побочно: FK стоял `ON DELETE CASCADE`, то есть удаление КЛИНИКИ
      стирало номера ЧЕЛОВЕКА, — это исчезло вместе с колонкой. Снят и объявленный дефект D8.
      Доказано на DEV в откатываемых транзакциях: миграция применяется чисто (колонки нет, строк
      94, индекс и FK ушли, обе настоящие стены нетронуты); человек, состоящий ТОЛЬКО в
      синтетической клинике B, персоналу клиники A невидим (0), свои 94 строки клиника A видит;
      `migrate-dev --preflight` PASS, pending=2, откачено.
- [ ] Независимый аудит — PASS.
- [ ] Живой прогон инварианта без списка исключений — зелёный.

### ИСПРАВЛЕНО ПО НЕЗАВИСИМОМУ АУДИТУ: сужение пациентской ветки было РЕГРЕССИЕЙ, а не успехом

Прежняя редакция этого раздела (и текст коммита `fbbb234ab`) подавали сужение 19 пациентских веток
как побочную пользу пункта А. Независимый адверсарный аудит показал обратное, и это подтверждено
самостоятельным перезамером. Запись оставлена целиком: она — след того, как правка сама себя
объявила успехом.

Отметка `org: true` кормит не только перепись, но и генератор политик: в `classSafe()`
(`declaration.ts`) стояла строка, вклинивавшая в ПАЦИЕНТСКУЮ ветку конъюнкт
`organization_id = app.current_org_id()`. У человека БЕЗ выбранной клиники `app.current_org_id()`
пуст ЗАКОННО — `app.install_port_context` прямо разрешает `context_class='patient'` с
`organization_id IS NULL` для `purpose='relation', function_identity IS NULL` (это читается в теле
функции на живой базе). NULL-сравнение даёт NULL и гасит ВСЮ ветку: человек теряет ВСЕ свои строки,
а не только чужеклиничную.

Это разворачивало решение владельца 2026-07-12, записанное в самом коде
(`apps/webapp/src/app-layer/principal/withOrganizationPrincipal.ts`): «The patient wall is "own data
only" and checks identity, never organisation». Мультиклинический обзор своих строк — ровно то, ради
чего это решение принято; его исчезновение и было подано как успех.

Замер на `symptom_entries` (настоящий путь `app.begin_port_context`, org-less пациентский контекст,
синтетическая вторая клиника и строка в ней — в той же откатываемой транзакции):

| политика | видно своих строк |
|---|---|
| живая в базе (до `fbbb234ab`) | **83** = 82 клиники A + 1 клиники B |
| сгенерированная `fbbb234ab` | **0** — человек ослеп на собственные данные |
| после исправления (`47db73625`) | **83** = 82 + 1, обзор восстановлен |

Обе стены при этом не дрогнули ни в одном из трёх состояний: чужих людей пациенту видно 0, строк
клиники B персоналу клиники A видно 0 (своих — 754).

ШИРЕ, ЧЕМ МЕРИЛ АУДИТ. Аудит насчитал 19 таблиц — те, что сузил `fbbb234ab`. Снятие той же ОДНОЙ
строки правит **67** политик: остальные 48 несут тот же дефект с `0e46d8302` (13.08), когда строка и
появилась. Корень один, поштучного разбора не требуется — машинной сверкой показано, что все 67
изменённых строк объясняются ровно снятием этого конъюнкта (0 необъяснённых) и что во всех 67 ветка
персонала осталась нетронутой.

Оракул требовал дефекта: `relation-access.test.mjs` проверял ровно спорную форму и был заведён ТЕМ
ЖЕ коммитом `0e46d8302`, что и сам конъюнкт, — код вписывал, тест требовал вписать, с решением
владельца пару не сверял никто. Заменён на структурное утверждение «сразу за проверкой аксессора
стоит собственность строки». Инвариант A1 этого и не требовал: `tenant-wall.mjs` прямо засчитывает
пациентскую ветку по собственному ключу человека.

### ИСПРАВЛЕНО ПО НЕЗАВИСИМОМУ АУДИТУ: ложное утверждение про `system_settings`

`fbbb234ab` утверждал, что платформенных строк среди 30 таблиц «не оказалось ни одной»,
`system_settings` в том числе — «значения с `organization_id IS NULL` в таблице нет». Это неправда:
в `system_settings` 146 платформенных строк из 159. Утверждение исправлено выше по измеренным
числам; сам замер повторён независимо. Текст коммита `fbbb234ab` не переписывается — история ветки
неизменна, исправление идёт отдельным коммитом.

## Е. Найдено, не тронуто — 16 таблиц-кандидатов на тот же дефект (владелец решает по одной)

**ИСПРАВЛЕНО ПО НЕЗАВИСИМОМУ АУДИТУ (FAIL, B2):** числа у №9 и №10 ниже были ЛОЖНЫМИ — взяты из
устаревшего комментария `declaration.ts:23661` (`patient_bookings`) и вовсе не найдены в
репозитории (`notification_delivery_attempts`). Перемерено аудитом живьём на обеих базах:
`patient_bookings` — **337 строк / 0 NULL на bcb_webapp_dev, 338/0 на bersoncarebot_test`** (не
219 из 263 — пустых значений вообще НЕТ, факт-бага не существует); `notification_delivery_attempts`
— **30926/0 на DEV, 31000/20 на TEST**. Строки №9/№10 ниже поправлены под реальные числа.

Аудит также указал (N3), что рамка всего раздела неточна: **13 из 16** таблиц ниже уже входят в
67 политик, которые чинит `47db73625` (в т.ч. как минимум `patient_diary_day_snapshots`,
`video_meetings`, `treatment_program_instances` — их живая `app_patient`-политика уже починена
этой веткой). Называть их «кандидатами на тот же дефект» неточно — RLS-часть там уже закрыта;
отдельным и НЕ закрытым остаётся только вопрос собственности колонки/`ON DELETE CASCADE`
(тот же класс, что решался у `user_phone_history`) — какие именно 3 из 16 не входят в 67, не
установлено, это отдельная точная сверка, не сделанная ни автором, ни мной.

После фикса пациентской ветки (см. выше) отдельная проверка прошлась по остальным org-таблицам с
личной колонкой (patient_user_id/platform_user_id и т.п.) в поисках того же силуэта — «у человека
без выбранной клиники организация пуста легитимно, и organization_id тихо гасит его собственные
строки». Ни одна из 16 НЕ исправлена — список открыт, это не работа, это находка для владельца.
Качественная оценка по форме таблицы и коду, не живой замер (кроме №9/№10, теперь перемеренных
аудитом).

### РЕШЕНИЕ ВЛАДЕЛЬЦА 12.09.2026 — группа «похоже, принадлежит человеку» ЗАКРЫТА, дословно

«Привязано исключительно к конкретному пациенту ВНУТРИ КОНКРЕТНОЙ КЛИНИКИ — ни в коем случае не
между клиниками. Всё что в кабинете доктора — видит только доктор внутри своей организации. Но!
документы, записи в карточке, история болезни — принадлежат ВРАЧУ. Если эта информация будет
когда-нибудь доступна клиенту — то тоже только внутри клиники. Между клиниками мы можем показывать
только его записи на приём в разные клиники и, возможно, ответы в чате от разных клиник (даже не
ответы на комментарии, а только сообщения и заявки).»

Это ПРЯМО ПРОТИВОПОЛОЖНО черновой классификации ниже («кандидат на identity-only фикс, как у
user_phone_history») — она была неверной гипотезой аудита, владелец её отклонил. Правило теперь
общее: клиническая запись (диагноз, анамнез, жалоба, документ) принадлежит ВРАЧУ/клинике, стена —
по организации, НИКОГДА по человеку через границу клиник. «Своё видно везде» касается ТОЛЬКО записи
на приём (и, возможно, сообщений/заявок в чате поддержки) — не медицинского содержания.

Проверено по факту (не только по решению): у **7 из 8** таблиц группы (##1–7 ниже) `app_patient`
СЕГОДНЯ не имеет вообще никакого гранта (ни SELECT, ни INSERT) — пациент не может прочитать их ни
при каких обстоятельствах, только `app_staff` со стеной по организации. Патентская ветка в тексте
политики — мёртвый код (`NULL::uuid IS NOT NULL`, всегда `false`), проверено на `clinical_diagnosis`
и структурно совпадает у остальных пяти. То есть по факту эти 7 УЖЕ соответствуют решению владельца
— чинить НЕЧЕГО, вопрос закрыт подтверждением, а не правкой.

Восьмая, **`patient_diary_day_snapshots`** — исключение по природе: это не врачебная запись, а
дневник самого пациента (выполнил разминку/не выполнил), в решении владельца прямо не упомянут и по
смыслу ближе к `symptom_entries`/дневнику самочувствия — уже входит в починенные 67 и работает
identity-only (своё видно из любой клиники), как и остальные дневниковые/само-отчётные таблицы.
Оставлен как есть, отдельно от группы 1–7.

Группа ЗАКРЫТА. Черновые формулировки ниже оставлены как есть (историческая находка), решение —
выше.

### ~~Похоже, принадлежит ЧЕЛОВЕКУ~~ (ЗАКРЫТО решением владельца выше — ложная гипотеза)

1. **`clinical_anamnesis_illness`** — история болезней/стрессов пациента (биографическая, не
   привязана к визиту). `organization_id` необязательна, FK на клинику `ON DELETE CASCADE` —
   удаление клиники стёрло бы историю болезни человека.
2. **`clinical_anamnesis_lifestyle`** — образ жизни пациента, тот же силуэт.
3. **`clinical_anamnesis_trauma`** — травмы/операции пациента, тот же силуэт.
4. **`clinical_complaint`** — жалобы пациента (по сути дневник симптомов), тот же силуэт.
5. **`clinical_diagnosis`** — диагнозы/сопутствующие заболевания пациента, тот же силуэт.
6. **`clinical_disease_anamnesis`** — текст истории болезни пациента. Тот же силуэт, но уникальный
   индекс завязан на `(patientUserId, organizationId)` — снятие колонки требует переложить индекс,
   как это делала правка `user_phone_history`.
7. **`patient_files`** — медицинские документы пациента (выписки/снимки/анализы). Тот же силуэт, НО
   есть настоящее двойное назначение: organization_id ещё используется для подсчёта места в
   хранилище клиники — собственность и учёт квоты надо развести, это решение владельца, не
   механическая правка.
8. **`patient_diary_day_snapshots`** — дневной снимок выполнения разминки/плана. Самое чистое дело в
   списке: первичный ключ уже `(platformUserId, localDate)` — organization_id даже не часть
   личности строки, просто прицепленная колонка с каскадным удалением.

### Похоже, принадлежит КЛИНИКЕ (org-стена скорее верна, но зафиксировать явным решением)

9. **`patient_bookings`** — старые записи на приём (до нынешнего `be_appointments`). Org-стена
   концептуально верна; перемерено аудитом живьём — **0 из 337 (DEV) / 0 из 338 (TEST)**
   organization_id пустых. Факт-бага незаполненных данных НЕТ (прежняя запись «219 из 263» была
   ложной, см. правку выше). Владелец спросил, не пропущен ли бэкфилл `organization_id`/специалиста
   в скрипте переноса дампа со старого прода — проверено: `deploy/postgres/prod-to-target-cutover-
   start.sql` и `prod-to-target-carry-legacy-appointments.sql` НИКОГДА не пишут
   `patient_bookings.organization_id` (только читают её как источник для новых строк
   `be_appointments`/`be_external_entity_mappings`, подставляя `canonical_organization_id`/
   `canonical_specialist_id`) — колонка уже приходит заполненной из исходных данных. Раз живых NULL
   нет ни на DEV, ни на TEST, пропущенного шага бэкфилла в этом скрипте не требуется и не найдено.
10. **`notification_delivery_attempts`** — журнал попыток отправки уведомлений клиники. Перемерено
    аудитом живьём — **0 из 30926 (DEV), 20 из 31000 (TEST)**. На TEST маленький хвост пустых,
    вероятно можно дозаполнить или это ожидаемо для системных уведомлений без клиники.
11. **`video_meetings`** — комната видеозвонка клиника+пациент+специалист+приём. organization_id
    обязательна (NOT NULL) — звонок физически не существует без клиники.
12. **`doctor_notes`** — личные заметки врача о пациенте (рабочий инструмент врача, не мед. запись
    и не собственность пациента — отдельное решение владельца уже есть, см. память
    `not-medical-software-practitioner-notes`).
13. **`patient_payment`** — платежи за услуги клиники.
14. **`manual_patient_commands`** — служебная запись ручных правок карточки персоналом.
    organization_id обязательна, связка с клиникой жёсткая.
15. **`treatment_program_instances`** — назначенная пациенту программа лечения ИЗ шаблона клиники.
    Стоит на всякий случай проверить пустые organization_id, живьём не мерено.
16. **`patient_specialist_links`** — связь "какой специалист видит какого пациента в какой
    клинике". В коде уже прямо написано, что это НЕ клиническая связь, а граница доступа внутри
    организации — вопрос уже решён раньше по факту, включён для полноты списка.

Не блокирует ничего в этой ветке и не входит в её приёмку.
