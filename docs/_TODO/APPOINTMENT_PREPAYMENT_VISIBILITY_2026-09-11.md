# Предоплата записи: счёт виден пациенту, живёт по таймеру, умирает честно

**Источник скоупа — диктовка владельца 11.09.2026** (чат по интерфейсу доктора). Этот файл — единственный
источник «todo» и «done» по этому workstream. Находка аудитора, которой здесь нет пункта, — вопрос владельцу,
а не задача.

Раздел `K2` в [`DOCTOR_MOBILE_UI_OWNER_ACCEPTANCE_2026-09-04.md`](DOCTOR_MOBILE_UI_OWNER_ACCEPTANCE_2026-09-04.md)
описывает ту же территорию (`PAY-APPT-08/09/11`) и остаётся вышестоящей приёмкой владельца. Здесь — рабочая
разбивка и то, что владелец добавил сверх тех формулировок.

---

## Что уже работает (проверено по коду 11.09.2026, не переделывать)

Это важно, потому что половина диктовки уже построена — и переписывать её нельзя.

- **Самозапись пациента УЖЕ создаёт счёт автоматически**, внутри той же транзакции, что и запись:
  `apps/webapp/src/modules/patient-booking/canonicalCreate.ts:505-523`. Не создался счёт — откатывается вся
  запись. Общий путь для виджета и кабинета. Требование владельца «при открытии формы „принять оплату“ счёт
  уже должен быть создан» выполнено на уровне данных.
- **Срок оплаты у записи есть**: колонка `be_appointments.payment_deadline_at`
  (`apps/webapp/db/schema/bookingEngine.ts:581`), считается один раз при создании записи из per-org настройки
  `booking_prepayment_wait_minutes` (дефолт 20 мин, `src/modules/system-settings/registry.ts:433`).
- **Автоотмена по неоплате работает**: минутный фоновый тик
  `POST /api/internal/booking-prepayment/expire` → SQL-корень `app.expire_due_booking_prepayments`
  (`apps/webapp/db/drizzle-migrations/20260905T233000_*.sql:672-748`). Слот освобождается, пишется история
  `status_changed / source: 'prepayment_expired'`.
- **Врач видит причину отмены**: красный бейдж «Отменена из-за неоплаты»
  (`src/app/app/doctor/calendar/DoctorAppointmentIndicators.tsx:35-41`). Уведомлений врачу не шлётся —
  это ровно то, что владелец подтвердил 11.09.
- QR у врача рисуется локально, без сети и без библиотеки (`calendar/localQrCode.ts`).

## Решения владельца 11.09.2026 (гейты закрыты, не переспрашивать)

1. **Истёкшая запись** — обычный отменённый статус с причиной «предоплата не внесена». Никаких уведомлений
   врачу. Видна она в сетке или нет — определяет состояние фильтра календаря врача, отдельной логики не пишем.
2. **Срок ожидания оплаты — одно значение на всю клинику/специалиста.** Переопределения на услуге нет.
3. **Промежуточный экран проверки — всегда.** Ссылка и QR ведут на нашу страницу, она проверяет, жив ли счёт,
   и либо уводит к провайдеру, либо показывает «оплата не поступила, бронирование отменено».
   **И одновременно счёт всегда отменяется у провайдера** — дословно: «если человек уже перешёл, открыл счёт,
   и он у него висит на экране оплаты — он его уже не должен оплатить в момент, когда закончилось время».
4. **Email сами не вводим и на неподтверждённый канал не отправляем.**

## Инженерные решения (приняты мной, владельцу — как факт)

- **Отмена у провайдера делается через invoice-ветку YooKassa, а не через отмену платежа.** Обычный
  `POST /v3/payments` отменить нельзя; но адаптер уже умеет `POST /v3/invoices` с полем `expires_at`
  (`src/infra/payments/yookassaPaymentProvider.ts:278-317`, флаг `supportsInvoice`), и порт уже несёт
  `invoice?: { description, expiresAt }` (`src/modules/payments/providerPort.ts:152-156`). Сегодня этой веткой
  пользуется только SaaS-биллинг. Предоплата записи переводится на неё, `expires_at = payment_deadline_at` —
  провайдер сам закрывает счёт в ту же секунду, что и мы. Новой интеграции не пишем.
- В модалке врача остаётся **одна** кнопка отправки ссылки, а не две. Нынешняя «Отправить в чат» внутри тела
  и новая «Отправить ссылку» в подвале — это одно действие; дубль убирается.

---

## Этапы

Каждый этап заканчивается живой проверкой на DEV и приземляется отдельным коммитом. «Готово» = галочка здесь
+ зелёный узкий прогон + живой запуск. «Аудит доволен» само по себе — не «готово».

### S1. Срок ожидания предоплаты редактируется в «Правилах записи»

Закрывает первую половину `PAY-APPT-08` и `PAY-APPT-23`. Ключ `booking_prepayment_wait_minutes` уже есть в
реестре и уже читается доменом — **отсутствует только дверь на запись и поле в интерфейсе**: ключа нет ни в
`ADMIN_SCOPE_KEYS`, ни в диапазонной проверке `api/admin/settings/route.ts:892-897`.

- [x] S1.1 Ключ добавлен в `ADMIN_SCOPE_KEYS` и в диапазонную проверку рядом с
      `booking_availability_horizon_days`; write-clearance на механику `booking` уже объявлен.
      — доказательство: коммит b56552495
- [x] S1.2 Поле «Сколько ждать предоплату, минут» добавлено в `BookingRulesPageClient.tsx` тем же паттерном,
      что и горизонт календаря; загрузка — в `ScheduleSetupTab.tsx` `BookingRulesLoader`.
      — доказательство: коммит b56552495
- [x] S1.3 Искусственного продуктового максимума нет (граница хранения 525600 мин остаётся).
      — доказательство: коммит b56552495
- [x] S1.4 Живая проверка: значение меняется, переживает перезагрузку, новая запись получает дедлайн от него.
      — доказательство: живая проверка 11.09: 20 → 35 пережило перезагрузку; попутно вскрыта и закрыта дыра af50c93a5 — механика booking не выдавала clearance, и писателя не было ни у одного из трёх ключей правил записи

### S2. Счёт у провайдера живёт ровно до дедлайна

- [x] S2.1 `createAppointmentPaymentIntent` передаёт `invoice: { description, expiresAt }` с `expiresAt`,
      равным `payment_deadline_at` записи.
      — доказательство: коммит 76e14380a
- [x] S2.2 Провайдер без `supportsInvoice` отваливается честно, без тихой подмены на вечный платёж.
      — доказательство: коммит 76e14380a — payment_provider_cannot_expire_invoice
- [x] S2.3 Доменный тест: дедлайн записи и `expires_at` счёта — одно и то же значение.
      — доказательство: коммит 76e14380a, проверен внесением поломки (тест краснеет)

### S3. Промежуточный экран проверки вместо прямого перехода к провайдеру

Разведано 11.09 (не искать заново): `/api/booking/payment-status` требует пациентскую сессию и для этого
маршрута не годится — ссылка приходит в мессенджер, где сессии нет.

**ПОПРАВКА 11.09, 23:4x — первая разведка была неверна, по ней работу не начинать.** Я написал, что
`PaymentsPort.findIntentById(id)` (`ports.ts:64`, `pgPayments.ts:335`) годится как вход без принципала,
потому что не требует `organizationId`. Это правда про СИГНАТУРУ порта и неправда про дверь в базу.
В декларации прав у `public.be_payment_intents` (`deploy/postgres/privileges/declaration.ts:13067`) гранты
есть только у `app_staff` и `app_tenant_service`. Ни пациентской, ни пред-сессионной роли там нет, а
`findIntentById` строит обычный `select` без organizationId — без принципала он вернёт НОЛЬ СТРОК, и это
будет выглядеть как «счёт не найден», а не как отказ. Ровно тот случай, про который у меня записано:
ноль строк без принципала ≠ пустая выборка.

Значит вход для S3 — **именованный SQL-корень под ролью `app_pre_session`**, тем же способом, каким уже
сделана анонимная витрина клиники (`app.read_anonymous_patient_surface_projection`, объяснение —
`src/app-layer/di/buildAppDeps.ts:2206-2214`, обёртка в bootstrap-принципал). Корень должен отдавать
РОВНО то, что показывается на экране проверки, и ничего сверх: жив ли счёт, сумма, дедлайн, статус записи
и ссылка провайдера. Новая дверь объявляется в декларации прав (одной миграцией `GRANT` не обойтись —
reconcile её снесёт).

Второй вход `PaymentsService.resolveIntentOrganizationId` (`service.ts:374`) сегодня **не зовёт никто** —
это мёртвый API, построенный, видимо, под этот же замысел. Либо он становится частью новой двери, либо
удаляется; висеть неиспользованным он не должен.

- [x] S3.1 Ссылка и QR ведут на наш маршрут, а не на домен провайдера.
      — доказательство: `buildAppointmentPaymentCheckUrl` — единственное правило внешнего адреса;
      `createAppointmentPaymentIntent`, `getAppointmentPaymentSummary` и
      `listAppointmentCheckoutUrls` отдают `/book/pay/{intentId}`. Fault-injection возврата сырого
      `checkoutUrl` покрасил доменные тесты.
- [x] S3.2 Счёт жив → редирект к провайдеру; счёт истёк или запись отменена → экран
      «Оплата не поступила, бронирование отменено». Ссылка на провайдера с этого экрана не даётся.
      — доказательство: живой вызов route handler на `bcb_webapp_dev` вернул для живого счёта
      `{"status":307,"location":"https://example-payments.invalid/provider/s3-live-proof"}`, а после
      переноса дедлайна того же счёта в прошлое —
      `{"status":410,"location":null,"hasProviderUrl":false,"hasFailureText":true}`. Обе отдельные
      fault-injection-поломки — редирект мёртвого счёта и утечка provider URL в HTML — покрасили тест.
- [x] S3.3 Маршрут открывается без сессии (пациент мог получить ссылку в мессенджере), но не раскрывает
      ничего, кроме суммы, срока и состояния конкретного счёта.
      — доказательство: анонимный route handler прошёл через именованный корень
      `app.read_booking_payment_check(uuid)` под `app_pre_session`; живой ответ на мёртвый счёт дал
      `{"status":410,"forbiddenValuesPresent":[]}` при проверке organization id, intent id,
      idempotency key, provider ref и provider hostname. `bash deploy/host/migrate-dev.sh --execute`
      применил owner-aware миграцию и reconcile на DEV; `pnpm run check:db-privileges-generated` зелёный.

### S4. Пациент видит счёт: ссылка, QR и крупный срок

Закрывает вторую половину `PAY-APPT-08` и `PAY-APPT-09` в части экрана.

Приземлено в `feat` слиянием `c0ec80e6b`. Галочки стоят по НЕЗАВИСИМОМУ аудиту (`00b6e6502`,
`gpt-5.6-sol high`, отчёт `docs/_TODO/runs/prepayment-s4-audit-result.md`), а не по отчёту воркера.

- [x] S4.1 Виджет (`/book/pay`) и кабинет пациента (`/app/patient/booking/pay`) показывают сумму,
      **саму ссылку**, и крупно «Оплатить до <дата, ЧЧ:ММ> · осталось N минут»; QR — только на ширине
      desktop, не на телефоне. — доказательство: воркер `6dba8d108`, правка ведущего `0b8d6d681`,
      аудит PASS. Дедлайн приходит из `payment_deadline_at` записи и на клиенте не пересчитывается;
      зоны проверены прогоном форматтера на одном мгновении в трёх зонах.
- [x] S4.2 Счётчик живой, по дедлайну экран сам переходит в состояние «оплата не поступила, бронирование
      отменено»; ссылка, QR и кнопка оплаты в этом состоянии не показываются. — доказательство: аудит
      прогнал ВСЕ 65 пар «статус записи × статус намерения»: отменённый экран появляется только у
      настоящих отмен, мёртвое намерение не даёт кнопки нигде.
- [x] S4.3 Карточка записи в кабинете показывает срок, а не только бейдж «Ожидает оплаты».
      — доказательство: `BookingUpcomingSection.tsx`; N+1 измерен на живой DEV, потолок 0-1 запрос
      на пациента.
- [ ] S4.4 Живая проверка на вьюпортах телефона и десктопа. Аудит её честно оставил в «НЕ ПРОВЕРЕНО»:
      исходник вместо экрана доказательством не считается.

**Вопрос ведущему от аудита (не FAIL, не чинится молча):** `manual_review_required` + висящее
намерение попадает в `settled`, то есть экран говорит «Запись подтверждена, оплачивать не нужно»
записи, которая на самом деле «На проверке». Денег это не трогает — кнопки оплаты нет ни в том, ни в
другом случае, — но формулировка врёт. Пункта про формулировку для проверки/переноса в этом плане
нет, поэтому новая копия не выдумывается. Развилка вынесена в конец файла.

### S5. Ссылка уходит в подтверждённые каналы сразу после записи

Закрывает вторую половину `PAY-APPT-09`. Сегодня запись в статусе `awaiting_payment` **глушит все**
уведомления пациенту: ранний `return` на `canonicalCreate.ts:540` стоит до `bookingCreatedEffects`,
`emitBookingEvent` и `sendBookingConfirmationEmail`. Пациент не получает ничего до момента оплаты.

- [x] S5.1 Для `awaiting_payment` отправляется своё сообщение со ссылкой на оплату и сроком — через
      действующий механизм выбора каналов (`resolveNotificationChannels`), только подтверждённые.
      — доказательство: воркер `ff9213d7e`, независимый аудит `1e119bccc` (FAIL с тремя находками),
      правка ведущего `8f8cdc38e`. Получатели берутся ТОЛЬКО из `targets.resolution.selectedChannels`
      (`bookingCreatedEffects.ts:79-98`), поля контактной формы к отправке не допускаются.
- [x] S5.2 Email вводить не предлагаем; на неподтверждённый адрес не шлём; пустая аудитория — не инцидент.
      — доказательство: аудитор внёс поломку в этот путь, тест покраснел (отчёт
      `docs/_TODO/audits/PREPAYMENT_S5_DELIVERY_AUDIT_2026-09-11.md`). Пустая аудитория для
      `awaitingPayment` молчит, а обычный `booking.created` по-прежнему рапортует инцидент.
- [x] S5.3 Сообщение об успешной оплате остаётся как есть, второй раз не дублируется.
      — доказательство: путь post-capture кандидат не трогал вовсе.
- [x] S5.4 (правка по FAIL аудита) Отказ уведомления не рушит уже созданную бронь: блок стоит после
      `markAwaitingPayment`, когда слот уже занят, и обёрнут в best-effort.
      — доказательство: коммит `8f8cdc38e`, приёмочный тест аудитора `canonicalCreate.d14.test.ts`
      краснел до правки и зеленеет после.
- [x] S5.5 (правка по FAIL аудита) Дедлайн очной записи рисуется в зоне филиала, а не в глобальной
      зоне приложения. Час разницы — потерянный слот и потерянные деньги.
      — доказательство: коммит `8f8cdc38e`, тот же набор тестов.

### S6. Модалка врача: кнопка счёта исчезает, пока счёт жив

- [x] S6.1 «Выставить счёт» скрыта, пока по записи есть живой неистёкший счёт.
      — доказательство: коммит b25c0a804
- [x] S6.2 При наличии подтверждённых каналов у пациента на её месте — «Отправить ссылку» (одна кнопка,
      нынешний дубль «Отправить в чат» убран). Каналов нет — кнопки нет, остаются ссылка и QR для показа.
      — доказательство: коммит b25c0a804
- [x] S6.3 Крупный срок «Оплатить до … · осталось N минут» в теле модалки.
      — доказательство: коммит b25c0a804
- [x] S6.4 Счёт истёк → ссылка и QR не показываются, вместо них «Оплата не поступила, бронирование отменено»;
      «Выставить счёт» снова доступна.
      — доказательство: коммит b25c0a804
- [ ] S6.5 Живая проверка на DEV на мобильном вьюпорте.

### S7. Причина отмены видна пациенту

**ПОПРАВКА 11.09, 23:5x — состояние хуже, чем описывал этот этап.** Формулировка ниже исходила из того,
что пациент видит отмену и не видит причину. По коду это не так: пациент НЕ ВИДИТ И САМОЙ ОТМЕНЫ.
`app.expire_due_booking_prepayments` (миграция `20260905T233000_*.sql:672-748`) пишет ровно в три места:
`be_appointments`, `be_appointment_history_events`, `be_patient_timeline_events`. Проекция
`patient_bookings`, из которой строится список записей пациента, не трогается ничем: тик
(`api/internal/booking-prepayment/expire/route.ts`) больше ничего не зовёт, зеркалящего триггера на
`be_appointments` нет (единственный триггер там — `be_appointments_confirmed_overlap_occupancy_guard`),
а в `PatientBookingsPort` вообще нет метода отмены по канонической записи — есть только
`markConfirmedByCanonicalAppointment` и пациентский `cancelBooking`. То есть у пациента бронь остаётся
«Ожидает оплаты» навсегда, со ссылкой, которая уже не платится.
(Это чтение кода, не живой замер: `psql` на DEV идёт через агента-порт. Первый шаг работы по этапу —
доказать это на живой `bcb_webapp_dev`, а не поверить мне.)

У врача причина уже выводится, и второго правила заводить не надо: признак считается из истории по
`payload.source = 'prepayment_expired'` (`pgBookingCalendar.ts:396`, `pgDoctorClients.ts:842`) и
показывается бейджем «Отменена из-за неоплаты» (`DoctorAppointmentIndicators.tsx:34-41`).

- [ ] S7.1 Истечение предоплаты доводится до проекции пациента: бронь становится отменённой в том же
      корне и в той же транзакции, что и сама запись, — иначе пациент видит живое «Ожидает оплаты» над
      мёртвым счётом.
- [ ] S7.2 Пациенту показывается причина «Предоплата не внесена», а не отмена клиникой. Признак берётся
      из того же источника, что и у врача (`source = 'prepayment_expired'`), второго правила не заводим.

---

## НЕ СДЕЛАНО (на 11.09.2026, 23:0x)

- **S4** — экраны пациента: ссылка, QR (владелец 11.09: QR только на широком экране, на телефоне — сумма,
  срок и кнопка), живой обратный отсчёт. Не начат.
- **S5** — код готов воркером в `wt/prepayment-s5-notify` (`ff9213d7e`), **идёт независимый аудит**, в `feat`
  не приземлён. Галочки не ставятся до вердикта.
- **S7** — причина отмены пациенту. Не начат.
- **Живая проверка S2 и S6** не делалась: на DEV нет записи в статусе «ожидает оплаты» с настроенным
  платёжным провайдером, поэтому ни срок жизни счёта у провайдера, ни новые состояния модалки врача
  глазами не проверены. Код готов, галочки S2/S6 стоят по коду и внесённой поломке, не по живому прогону.

## Найдено аудитом, но НЕ входит в этот план (вопрос владельцу, не задача)

- **Мультислотовая бронь, оплаченная одним платежом, шлёт пациенту по сообщению на каждый слот.**
  Нашёл независимый аудит S5 (`1e119bccc`, находка F3). Дефект настоящий и давний: цикл
  `for (const appointmentId of settled.confirmedAppointmentIds)` в `src/modules/payments/service.ts:649`
  зовёт обработчик на каждую запись, а обработчик каждый раз кладёт `patientMessageText` в событие
  `booking.payment_captured` (`appointmentPaymentConfirmedHandler.ts:68`). Кандидат S5 этот код не
  трогал — находка к S5 не относится, и пункта под неё в этом плане нет.
  Приёмочный тест аудитора снят (`e1f4a63a7`): он требовал ровно одно событие на платёж, а событие
  несёт ещё и календарную синхронизацию со своим `bookingId` — схлопывание оставило бы второй слот
  без обновления календаря. Правильная починка: событие на каждую запись, но `patientMessageText`
  только у первой (поле optional, `bookingLifecycleSchema.ts:33`).
  **Вопрос владельцу:** пациент, записавшийся на три слота одним платежом, должен получить одно
  сообщение или три? Пока ответа нет — работу не начинаю.

## Открытые развилки владельца

- [x] Нужен ли QR **пациенту** на его собственном экране оплаты — закрыто владельцем 11.09:
      **QR только на широком экране**; на телефоне сумма, срок и кнопка.
- [x] Порядок этапов — закрыто владельцем 11.09: «мне насрать, лишь бы быстрее и не в ущерб качеству»,
      параллелить где можно, мелкие правки делать самому без агентов.
- [ ] **Мультислотовая бронь, оплаченная одним платежом: одно сообщение или по одному на слот?**
      Разбор — в разделе «Найдено аудитом, но НЕ входит в этот план».
- [ ] **Что пациент должен читать про запись «На проверке» (`manual_review_required`), пока по ней
      висит неоплаченный счёт?** Сейчас экран говорит «Запись подтверждена, оплачивать не нужно» —
      кнопки оплаты нет (это верно), но слово «подтверждена» неправда. Варианты: оставить как есть
      (состояние редкое, деньгам ничего не грозит), или завести отдельную формулировку «Запись на
      проверке». Нашёл независимый аудит S4, сам чинить не стал — это копия, а не инженерия.
