# Слепой kill-set: публичная визитка клиники и разворот адреса (19.08)

Составлен по authority ДО чтения реализации и тестов (§24.5, §10b).
Authority: `docs/_TODO/CLINIC_PUBLIC_PAGE_AND_URL_FLIP_2026-08-19.md` §3.2, §3.3, §3.5, §4, §5, §6, §11;
AGENTS.md §5, §1b, §17, §1 «Миграции после baseline B0».

## A. Публичная отдача медиа `/{clinic}/media/{uuid}`  (§3.5 п.1, п.3)
- K1  файл ТОЙ ЖЕ клиники, существующий и конвертированный, но НЕ упомянутый в карточке → отдаётся.
- K2  файл ЧУЖОЙ клиники (uuid подставлен в маршрут клиники А) → отдаётся.
- K3  файл, не прошедший конвертацию (status != ready) → отдаётся битым/сырым.
- K4  удалённый (soft-deleted) файл → продолжает отдаваться.
- K5  медиа отдаётся, когда сама карточка/клиника не опубликована.
- K6  общий чокпоинт `/api/media/[id]` ослаблен: аноним получает файл.
- K7  ответ различает «нет такого uuid» и «есть, но не твой» → перечисление.

## B. Что уходит анониму со страницы (§3.2)
- K8  в разметку попадает внутренний идентификатор: organization_id, branch_id, tariff_id.
- K9  на странице ФИО специалистов (specialists_json).
- K10 контакты сотрудника из platform_users вместо контактов организации.
- K11 в locations_json уехало лишнее: timezone, color, sort_order, is_active, uuid филиала.
- K12 описание рендерится как HTML/markdown → XSS анонимной страницы (план: plain text).

## C. Два выключателя (§3.3)
- K13 `card_is_published=false` → страница всё равно открывается.
- K14 клиника не опубликована в каталоге / организация неактивна → страница открывается.
- K15 сочетание (карточка да, клиника нет) → открывается.
- K16 404 отличим от «нет такой клиники» → аноним перечисляет клиники.
- K17 отказ ЧТЕНИЯ (42501 / БД недоступна) отдаёт 404 или пустую карточку вместо кода ошибки.

## D. Редирект (§2)
- K18 `/book/{slug}` не 308 (302/307/200) → ссылка из письма теряет вес/ломается.
- K19 алиас `/{old}` и `/{old}/booking` не редиректят на current.
- K20 цепочка длиннее одного прыжка (`/book/{alias}` → ... → ... ).
- K21 редирект-маршрут перехватил глубокие шаги мастера (`/book/service`, `/book/slot`, `/book/done`, `/book/embed.js`).

## E. Запись из кабинета (§4)
- K22 админ клиники А прикрепляет в карточку медиафайл клиники Б.
- K23 админ клиники А меняет карточку клиники Б (org берётся из тела запроса, а не из принципала).
- K24 отказ приходит только из route-гейта, а тело функции пускает (обход мимо маршрута).
- K25 не-staff (пациент/аноним) пишет карточку.

## F. Права (§5 репо, §3.1 плана)
- K26 фактические гранты `app_seam_public_clinic_card_owner` шире декларации.
- K27 bootstrap-роль (`app_pre_session`) получила права сверх EXECUTE на корень чтения.
- K28 `app_staff` получил новые поколоночные/табличные права на проекции вместо записи корнем.
- K29 создана новая роль / выдан BYPASSRLS / отключён FORCE RLS.
- K30 корень чтения читает арендаторские таблицы напрямую, а не проекцию.

## G. Первый экран филиалов (§5, §6)
- K31 второй филиал города остаётся недостижим (филиал по-прежнему выбирается за человека).
- K32 протухший `branch`/`specialist` даёт пустой список вместо экрана §6.3.
- K33 `branch` чужой клиники под slug А показывает данные клиники Б.

---

# Результат слепого аудита — ВЕРДИКТ: **FAIL для приземления**

Ветка `wt/clinic-card-page-20260819` на `189395685`, база сравнения `a45c46597`
(`feat/doctor-ui-rebuild`; `git rev-list --count HEAD..feat` = 0 — ветка НЕ отстаёт).
Все проверки — на DEV (`bcb_webapp_dev`), собственный dev-сервер на :5310, сосед на :5200 не тронут.
ПРОД не трогался. Фикстуры DEV созданы и удалены (см. «Уборка»).

## Классификация по §24.4 (тест или взгляд)

| Пункт брифа | Природа | Способ доказательства |
| --- | --- | --- |
| 1. Публичное медиа | повторяемое поведение | живой HTTP по kill-set A + фикстуры в БД |
| 2. Что уходит анониму | повторяемое поведение | живой HTTP + разбор разметки |
| 3. Два выключателя | повторяемое поведение | живой HTTP, матрица 5 состояний |
| 4. Редирект | повторяемое поведение | живой HTTP, `%{num_redirects}` |
| 5. Запись из кабинета | повторяемое поведение | живой POST под сессией админа клиники |
| 6. Права нового шва | разовое действие | interspection каталога БД (`pg_proc`, `information_schema.column_privileges`, `pg_roles`) |
| 7. Первый экран филиалов | повторяемое поведение | живой HTTP |

## Kill-set: что поймано

| ID | Что ломали | Результат | Команда |
| --- | --- | --- | --- |
| K1 | файл ТОЙ ЖЕ клиники, не входящий в карточку | **404** | `curl /dmitryberson/media/aa…c003` |
| K2 | файл ЧУЖОЙ клиники (в т.ч. вписанный в `photo_media_ids` мимо двери записи) | **404** | `curl /dmitryberson/media/bb…c006` |
| K3 | не сконвертированный файл (`status='pending'`) | **404** | `curl /dmitryberson/media/aa…c004` |
| K4 | удалённый файл (`status='pending_delete'`) | **404** | `curl /dmitryberson/media/aa…c005` |
| K5 | медиа при выключенной карточке / клинике / организации | **404** | матрица ниже |
| K6 | общий чокпоинт `/api/media/{uuid}` анониму | **401** | `curl /api/media/aa…c001` |
| K7 | несуществующий uuid неотличим от «есть, но не твой» | одинаковый **404** | `curl /dmitryberson/media/9999…` |
| K8 | внутренние идентификаторы в разметке | только uuid медиа (нужны для URL); ни `organization_id`, ни `branch_id`, ни `tariff_id` | grep по HTML |
| K9 | ФИО специалистов на визитке | нет | grep по HTML |
| K10 | контакты сотрудника вместо контактов организации | нет, только введённые владельцем | взгляд + HTML |
| K11 | `timezone`/`color`/`sort_order`/`is_active`/uuid филиала в снимке | нет | HTML + тело `save_public_clinic_card` |
| K12 | HTML/скрипт в описании | экранировано React (`&lt;img …&gt;`), `javascript:` в сайте отбит `website_invalid` | POST + HTML |
| K13 | `card_is_published=false` | **404** (страница и медиа) | матрица |
| K14 | `is_published=false` при `card_is_published=true` | **404** | матрица |
| K15 | оба выключены / организация `is_active=false` | **404** | матрица |
| K16 | 404 неизвестной клиники отличим от 404 невыпущенной | тела и заголовки совпадают (разница только в эхо slug в RSC-потоке) | `diff` двух ответов |
| K17 | отказ ЧТЕНИЯ отдаёт 404/пустую карточку | отдаёт **500** + «Страница клиники временно недоступна» + лог с категорией; отклонение 500 вместо 503 названо в плане вслух | наблюдалось живьём при отсутствующей capability |
| K18 | `/book/{slug}` не 308 | **308** | `curl -w '%{http_code}'` |
| K19 | алиас не редиректит | `/{old}`→308→`/{current}`, `/{old}/booking`→308→`/{current}/booking` | `curl` |
| K20 | цепочка длиннее одного прыжка | `num_redirects=1` на всех четырёх формах | `curl -L -w '%{num_redirects}'` |
| K21 | перехвачены глубокие шаги мастера | `/book/service|slot|confirm|pay|done` → своё 307 на `/book`; `/book/embed.js` → 200 | `curl` |
| K22 | админ клиники Б цепляет медиа клиники А | **400 `media_not_owned`** — приходит из ТЕЛА функции (`ERRCODE 22023`, маршрут только переводит код) | POST |
| K23 | подмена организации в теле запроса | **400 `invalid_body`** (`.strict()`), плюс backstop `42501` в теле функции по `app.current_org_id()` | POST |
| K24 | не-staff пишет карточку | **401** аноним, **403** платформенный админ без `organization.management` | POST |
| K31 | второй филиал города недостижим | достижимы ВСЕ активные филиалы, город стал группировкой | см. ниже |
| K32 | протухший `branch` даёт пустой список | «Этот филиал больше не принимает записи» + список действующих | см. ниже |
| K33 | `branch` чужой клиники показывает чужое | тот же экран «филиал больше не принимает», ничего чужого | см. ниже |

Матрица выключателей (страница / медиа):
`card=1,pub=1,org=1` → 200/307 · `card=0,pub=1` → 404/404 · `card=1,pub=0` → 404/404 ·
`card=0,pub=0` → 404 · `card=1,pub=1,org.is_active=0` → 404/404 · неизвестный slug → 404.

## Права нового шва (взгляд в каталог DEV)

`app_seam_public_clinic_card_owner`: `rolsuper=f`, `rolbypassrls=f`, `rolcanlogin=f`, `rolinherit=f`,
ноль членств в обе стороны; `USAGE` на `app`/`public` без `CREATE`. Табличных грантов — **ноль**,
все 40 привилегий поколоночные и совпадают с декларацией. `EXECUTE`: чтение — только `app_pre_session`,
запись — только `app_staff`, пересечения нет. Обе функции `SECURITY DEFINER`,
`search_path=pg_catalog`, чтение `STABLE`, запись `VOLATILE`. `pnpm run test:db-privileges` → 99/99.

Единственный излишек: у шва есть `UPDATE` на `clinic_public_directory_entries.organization_id`,
которого тело функции не пишет (артефакт модели декларации — операции применяются ко всему списку
колонок). Недостижим: роль без логина и без членств, её привилегии исполняются только внутри двух тел.
Записан как рекомендация, не finding.

## НАХОДКИ

### F1 (критично, блокирует приземление) — новый канонический адрес записи мёртв для посетителя

`/{clinic}/booking` отдаёт «Каталог временно недоступен» ЛЮБОМУ анониму, для любой клиники.
Причина названа в самом репозитории — `apps/webapp/src/app-layer/principal/publicBookingPrincipal.ts:14-16`:
новая публичная точка входа обязана добавить свою строку в `PUBLIC_BOOKING_PRINCIPAL_SOURCES`,
иначе попадёт на реляционный путь, которого у класса `tenant_service` нет. Ветка этого не сделала:
`loadBookingEntry.ts:79` ставит `source: 'app/[clinicSlug]/booking:entry'`, которого в списке нет.
Вторая половина той же причины: `loadBookingEntry.ts` читает каталог **кабинетным** листером
`listInPersonServicesForBranch` (`db.select()` напрямую) и `catalog.listSpecialists()`, тогда как
публичный путь обязан идти через объявленную дверь `listPublicBookableServicesForBranch`
(так делает старый `publicOrganizationBooking.ts:127`).

Доказательство причинности (временные правки внесены и откачены):
добавление строки в список → падение уезжает с `listBranches` на `listServices`;
плюс подмена листера на `listPublicBookableServicesForBranch` → первый экран рисуется полностью.
С параметром `?specialist=` экран остаётся мёртвым и после обеих правок, потому что
`listSpecialists` — тоже реляционное чтение без публичной двери.

Последствие для живого человека: `publicBookPaths.forSlug` переведён на новый адрес, поэтому
**старая ссылка из письма подтверждения записи** (`/book/{slug}`) теперь честным 308 уводит человека
на страницу, где вместо услуг написано «Каталог временно недоступен». Публичная запись перестаёт
работать целиком. Конструктор ссылки (Ш7) при этом штампует ссылки с `?specialist=`, которые мертвы
даже после починки первых двух шагов.

Команда:
```
curl -s http://127.0.0.1:5310/dmitryberson/booking          # → «Каталог временно недоступен»
curl -s -L -w '%{url_effective}\n' http://127.0.0.1:5310/book/dmitryberson   # 308 → тот же мёртвый экран
```
В логе: `Missing declared webapp port capability: tenant_service`.

Почему это не поймали: `bookingEntryScreen.unit.test.ts` (6/6 зелёные) подменяет `deps` целиком и
принципала не ставит; ни одного route/live-теста на новый адрес нет. Прогнанные мной 7 файлов /
33 теста этой поверхности зелёные при мёртвом продукте.

### F2 (высоко) — в ветку закоммичены неразрешённые маркеры merge-конфликта в двух deploy-артефактах

`deploy/postgres/generated/port-context-capabilities.bcb_webapp_dev.sql:179-184` и
`…bersoncarebot_test.sql:179-184` содержат `<<<<<<< HEAD` / `=======` / `>>>>>>> a8c2d3d59…`.
Файл — не текст, а SQL: применение даёт синтаксическую ошибку. Внутри конфликта сторона «theirs»
несёт две capability соседней ветки (`booking.public-catalog.read`, `booking.public-slot-snapshot.read`),
то есть любое «разрешить в свою пользу» их потеряет.

Вторая половина находки — гейт этого не ловит: `node deploy/postgres/privileges/generate-cli.mjs --check`
(на который план Ш2 ссылается словами «побайтно») проверяет только `privileges` и `allowlist`
и выходит **0**. Артефакт port-context сверяется лишь под отдельным флагом:
```
node deploy/postgres/privileges/generate-cli.mjs --check --port-context-only   # exit 1, «расхождений 2»
```
Ни один скрипт деплоя и ни один CI-шаг этот флаг не зовёт (`grep -rn "port-context-only"` — только
сам generate-cli). Последствие: заявленное «побайтно» неверно для этого артефакта, и корректность
объявленного каталога capability сегодня ничем не удерживается.

### F3 (средне) — «готово» в плане подписано доказательствами, которых нет

`docs/_TODO/CLINIC_PUBLIC_PAGE_AND_URL_FLIP_2026-08-19.md` §8: Ш6 и Ш7 помечены `[x]` с
доказательством «`bookingEntryScreen.unit.test.ts` 6/6», Ш8 перечисляет поведенческие тесты как
пройденные. Живьём (F1) обе ступени не работают. Ш2 ссылается на `generate-cli --check` «побайтно» —
для затронутого артефакта это неверно (F2). Правило §24.7 — `milestone done` требует живой проверки,
а не зелёного набора.

## Проверил, нарушений НЕ нашёл

Публичная отдача медиа (kill-set A целиком, включая чужой uuid, вписанный в проекцию мимо двери
записи), состав анонимной страницы, оба выключателя и их сочетания, неотличимость 404, вечный
308-редирект с одним прыжком и алиасами, стена принадлежности медиа на записи (из тела функции),
гейт роли, права нового шва, доступность всех филиалов города и три экрана протухшего параметра.

## Что проверить НЕ смог

1. **Первый экран и параметры ссылки — только под временной правкой.** Из-за F1 экраны G
   (`K31`–`K33`) наблюдались на дереве с двумя временными правками, которые откачены. После
   исправления F1 их надо перепроверить на настоящем коде.
2. **`?specialist=` не проверен ни в одном сценарии** — путь мёртв до починки `listSpecialists`.
3. **TEST не трогал.** Всё измерено на DEV; поведение TEST после деплоя (в т.ч. последствия F2)
   не наблюдал.
4. **Реальная отдача байтов логотипа не проверена** — на DEV S3 не сконфигурирован под фикстуры,
   маршрут доходит до `307` на presigned-URL, дальше не ходил.
5. **Полный `pnpm run ci` не гонял** (§9 требует общий замок хоста и сосед держит :5200/:5202).
   Прогнаны точечно: 7 файлов / 33 теста затронутой поверхности, `tsc --noEmit` (0),
   `test:db-privileges` (99/99), eslint по изменённым файлам (свои файлы чисты; 14 ошибок
   `no-html-link-for-pages` — существующий долг в `AppEntryLoginContent.tsx` и `AuthBootstrap.tsx`,
   веткой не тронутых).

## Рекомендации (НЕ задачи — §24.6)

- `UPDATE` на `organization_id` у шва — сузить, если модель декларации это позволяет.
- В снимок адресов попадает встроенный псевдофилиал «Онлайн» и сырой `city_code`, когда у него нет
  перевода (`titleForBookingCityCode` отдаёт код как есть) — на публичной странице видно
  «Кабинет на Невском · nevsky-office».
- `/{clinicSlug}` теперь ловит любой несовпавший корневой путь, то есть каждый мусорный запрос
  стоит одного обращения к БД. Раньше это был статический 404.

## Уборка

- Временные правки продукта (`publicClinicCard.ts`, `loadBookingEntry.ts`, `publicBookingPrincipal.ts`)
  откачены — `git status` чист, кроме этого файла.
- Фикстуры DEV удалены: 6 строк `media_files` (`AUDITCARD-%`), филиал `aa…00b2`, обе карточки
  возвращены в `card_is_published=false`, `is_published=true`, поля очищены, `be_organizations.is_active=true`.
- Dev-сервер :5310 остановлен, порт свободен; сосед на :5200/:5202 не тронут.
- **Изменено вне git:** `apps/webapp/.env.dev` и `.env` (оба gitignored) синхронизированы с декларацией
  штатным `node --experimental-strip-types deploy/host/update-dev-port-context-env.mjs` — DEV отставал
  на 6 capability, включая обе двери визитки, из-за чего страница отдавала 500. Оставлено
  синхронизированным намеренно: возврат сломал бы DEV следующему агенту.

---

# Аудит ФИКСА F1/F2/F3 (19.08, второй проход) — ВЕРДИКТ: **PASS для приземления**

Ветка `wt/clinic-card-page-20260819` на `6fa0f7bf4` (фикс — `7c84fe835`), база сравнения
`feat/doctor-ui-rebuild` = `d5310d9e5`: `git rev-list --count HEAD..feat` = **0** (ветка не отстаёт,
подтягивать нечего). Всё измерено на DEV (собственный dev-сервер `:5310`, `bcb_webapp_dev`).
ПРОД не трогался, TEST не трогался, гранты не менялись, ролей не создавалось.

## Классификация по §24.4

| Пункт брифа | Природа | Способ доказательства |
| --- | --- | --- |
| точка входа в `PUBLIC_BOOKING_PRINCIPAL_SOURCES` | повторяемое поведение | поведенческий тест + слепые поломки + живой HTTP |
| каталог читается публичной дверью | повторяемое поведение | то же |
| маркеры конфликта убраны | разовое действие | взгляд на итоговое состояние (`git grep`) |
| гейт `--port-context-only` сведён в один проход | гейт → самотест по §10a «Гейт с самотестом» | вернул маркеры руками → красное; откатил → зелёное |
| галочки плана | разовое действие | взгляд на §8/§16 плана против замеренной реальности |
| фикс не открыл дыру анониму | повторяемое поведение | живой HTTP + разбор разметки/RSC-потока |

## F1 — закрыт

Живой HTTP на DEV `:5310`, анонимно (без cookie):

| Запрос | Что пришло |
| --- | --- |
| `GET /dmitryberson/booking` | **200**, «Филиал · Москва. Точка Здоровья · Санкт-Петербург · Онлайн-приём» — настоящие филиалы, текста «Каталог … недоступен» в ответе нет |
| `GET /dmitryberson/booking?branch=54432236-…72ad` | **200**, «Сеанс 60 мин», «Сеанс 90 мин» |
| `GET /dmitryberson/booking?branch=00000000-0000-4000-8000-000000000000` | **200**, «Этот филиал больше не принимает записи» + список действующих филиалов |
| `GET /dmitryberson/booking?specialist=<uuid>` | **200**, каталог настоящий; в журнале `[clinic-booking] specialist scoping ignored` (Ш6b, по брифу не находка) |
| `GET /book/dmitryberson` | **308** → `/dmitryberson/booking` (один прыжок) |

В журнале dev-сервера за весь прогон — ни одного `Missing declared webapp port capability`,
ни одного `42501`, ни одного `entry catalog read failed`.

Чтением подтверждено, что на этом пути реляционных чтений не осталось вовсе:
`listBookableBranchesForOrganization` → `catalog.listBranches` (публичная ветка есть),
`resolveBookableOnlineLocationForOrganization` → `listBranches` + переданный
`listPublicBookableServicesForBranch`, а тот → `catalog.getBranch` (публичная ветка) +
`services.listPublicBookableServicesForBranch` (дверь `app.read_public_booking_catalog`).
Автоматически это же держит утверждение `expect(getDrizzle).not.toHaveBeenCalled()` в новом тесте.

## F2 — закрыт, гейт прошёл САМОПРОВЕРКУ

- `git grep -e '^<<<<<<< ' -e '^>>>>>>> '` по всему дереву — **пусто**.
- Обе стороны бывшего конфликта на месте в обоих артефактах: `clinic.public-card.read`,
  `booking.public-catalog.read`, `booking.public-slot-snapshot.read`.
- `generate-cli.mjs --all --check --port-context-only` → **exit 0**, побайтно, обе базы.
- **Самотест гейта.** Восстановил в `port-context-capabilities.bcb_webapp_dev.sql` ровно
  доконфликтное содержимое (`git show 7c84fe835^:…`, 3 маркера) и запустил общий чокпоинт
  `node deploy/postgres/privileges/reconcile-access.mjs --env dev --db bcb_webapp_dev
  --admin-socket /var/run/postgresql --admin-port 5432`:
  → упал **на строке 90** (второй `--check`, `--port-context-only`) с
  «КРАСНЫЙ bcb_webapp_dev/portContext … строка 179: закоммичено `<<<<<<< HEAD`», **до** psql.
  После отката файла тот же вызов проходит оба `--check` и доходит до psql (падает уже на
  peer-аутентификации `postgres` — прав нет, это ожидаемо и к гейту не относится).
- Оба вызывающих чокпоинта проверены глазами: `deploy/host/migrate-dev.sh:209` и
  `deploy/host/deploy-test.sh:184` зовут именно `reconcile-access.mjs`.

## F3 — закрыт

`§8` плана: Ш6 несёт живое HTTP-доказательство, Ш6b стоит `[ ]` «НЕ РЕАЛИЗОВАНО», Ш7 несёт пометку
о неотработанном `?specialist=`, Ш8 больше не ссылается на несуществующее доказательство. Всё это
совпадает с замеренной реальностью. Расхождения, найденные и помеченные ЭТИМ проходом:
`§16.1` (строка про старый `bookingEntryScreen.unit.test.ts` протухла — файл переписан) и `§16.2`
(симптом «`GET /{clinic}` отдаёт 500» больше не воспроизводится, сегодня **404**) — помечены прямо
в плане, а не только здесь.

## Дыры фикс не открыл

Анонимный `GET /dmitryberson/booking`: в ответе ровно **три** UUID — два физических филиала и
встроенный «Онлайн», все три нужны для ссылок следующего шага и стоят в них по дизайну §6.1.
Ни `organizationId`, ни `fullName`, ни имён специалистов, ни `tariff`. На экране услуг — только
`branchId` + два `serviceId`; `publicWidgetVisible`/`adminManualOnly`/`organizationId` в поток не
попадают. Фикс здесь строго СУЖАЕТ выдачу: прежняя редакция резолвила специалиста и клала его
`fullName` в `specialistName`, теперь это поле всегда `null`, а `listSpecialists` на публичном пути
не зовётся вовсе.

404: `GET /dmitryberson` (карточка выключена) → **404**; `GET /no-such-clinic-xyz` → **404**;
`GET /no-such-clinic-xyz/booking` → **404**; `GET /dmitryberson/media/<чужой uuid>` → **404**.
Полная матрица пяти состояний выключателей не перемерялась: фикс не трогает ни
`app/[clinicSlug]/page.tsx`, ни `publicClinicCard.ts`, ни `media/` (diff — 6 файлов, см. ниже),
а на этом же коде матрица снята первым проходом выше. Все карточки на DEV сейчас выключены уборкой
первого прохода, поэтому направление «карточка включена → 200» живьём не переснималось — для этого
нужна запись в DEV-БД, доступа к которой у роли `dev` нет и расширять его брифом запрещено.

## Новый тест поверхности входа (§24.5 — писал аудитор)

`apps/webapp/src/app/[clinicSlug]/booking/bookingEntryScreen.unit.test.ts` переписан целиком.
Подменена ТОЛЬКО граница базы: `getDrizzle` бросает (как класс `tenant_service` в проде — сквозной
`purpose: 'relation'` ему не объявлен), `runWebappNamedRoot` отвечает по точной идентичности корня.
DI-контейнер отдаёт настоящий `createPgBookingEnginePort()`; принципал
(`withExplicitOrganizationPrincipal`), маршрутизация «дверь vs реляция» и загрузчик экрана —
продуктовые. 4/4 зелёные.

Слепые поломки (каждая внесена руками в продуктовый код и откачена):

| Что сломано | Какое утверждение покраснело |
| --- | --- |
| строка `app/[clinicSlug]/booking:entry` удалена из `PUBLIC_BOOKING_PRINCIPAL_SOURCES` | «без параметров показывает ФИЛИАЛЫ, взятые ДВЕРЬЮ» (+ ещё 2 из 4: `unavailable` вместо экрана) |
| `listPublicBookableServicesForBranch` → кабинетный `listInPersonServicesForBranch` | «филиал в ссылке даёт услуги ЭТОГО филиала, и тоже только дверью» (`unavailable` вместо `services`) |
| снята стена `branch.organizationId !== organizationId` | «филиал ЧУЖОЙ клиники не показывает ничего чужого» (`services` вместо `stale`) |
| отказ чтения возвращает пустой список филиалов | «нечитаемый каталог — это слышимый отказ, а не „филиалов нет“» |

`git status` после инъекций чист — все временные правки продукта откачены.

**Решение по старому файлу (§10a):** переписан на месте, а не подправлен. Критерий: ни одно его
утверждение не относилось к пути живого посетителя — он ставил вместо
`withExplicitOrganizationPrincipal` сквозную заглушку и подменял `deps` целиком, поэтому различить
публичную дверь от кабинетной было физически нечем; сохранять было нечего, а зона ответственности
и имя файла остаются теми же.

## Проверил, нарушений НЕ нашёл

Живой первый экран и все три экрана параметра `branch` на НАСТОЯЩЕМ коде (первый проход видел их
только под временными правками — п.1 его «что проверить не смог» закрыт); отсутствие реляционных
чтений на публичном пути; чистота обоих deploy-артефактов и их побайтное совпадение с декларацией;
самотест гейта; вечный `308` с одним прыжком; состав анонимной разметки; 404 на выключенной
карточке, неизвестном слаге и чужом медиа-uuid.

## Прогоны

`tsc --noEmit -p apps/webapp/tsconfig.json` → 0. `eslint` нового файла → 0.
`vitest run 'src/app/[clinicSlug]' 'src/app/book' 'src/infra/repos/publicBookingDoors'
'src/app-layer/principal' 'src/modules/patient-booking'` → **21 файл / 86 тестов зелёные**.
`pnpm run test:db-privileges` → **99 pass, 0 fail, 9 skip** (без изменений).

## Рекомендации (НЕ задачи — §24.6)

1. **`listInPersonServicesForBranch` после фикса не зовёт НИКТО** (проверено:
   `grep -rn` по `apps/webapp/src` — ни одного продуктового вызова). При этом функция осталась
   **значением по умолчанию третьего параметра** `resolveBookableOnlineLocationForOrganization`.
   Сегодня оба вызывающих (`loadBookingEntry.ts:83`, `publicOrganizationBooking.ts:126`) передают
   дверь явно, так что дефекта нет. Но умолчание — это ровно та дверь, через которую F1 вернётся
   молча: следующий публичный вызывающий, забывший третий аргумент, получит кабинетный листер и
   зелёные тесты. Дешевле сделать параметр обязательным (или убрать мёртвую функцию), чем ловить
   это ещё раз. Это НЕ работа этого хода — в плане владельца такого пункта нет.
2. Гейт `--port-context-only` работает **по одной базе за проход**: `migrate-dev.sh` сверяет
   `bcb_webapp_dev`, `deploy-test.sh` — `bersoncarebot_test`. Репозиторного шага, который сверял бы
   ОБА артефакта (`--all --check --port-context-only`), по-прежнему нет ни в `pnpm ci`, ни в
   workflow. Хуже, чем было, не стало; но «побайтно» в §8 Ш2 держится на том, что обе среды
   когда-то деплоились.
3. Рекомендации первого прохода (`UPDATE` на `organization_id` у шва; сырой `city_code` без
   перевода; `/{clinicSlug}` ловит любой несовпавший корневой путь ценой обращения к БД) не
   пересматривались — фикс их не касается.

## НЕ СДЕЛАНО

1. **`?specialist=` не проверен** — по брифу это открытый пункт Ш6b, двери под идентичность
   специалиста нет; проверено только то, что параметр не роняет страницу.
2. **Полная матрица пяти состояний выключателей визитки не перемерялась** — обоснование выше
   (фикс не трогает эти файлы; для направления «включено → 200» нужна запись в DEV-БД).
3. **TEST не трогал**, деплоя не делал; поведение TEST после приземления не наблюдал.
4. **Полный `pnpm run ci` не гонял** — §9 требует общий замок хоста. Прогнано точечно (см. выше);
   `pnpm build`/`build:webapp`/`audit` не запускались.
5. **Реальная отдача байтов медиа** по-прежнему не проверена (S3 на DEV под фикстуры не настроен) —
   не регрессия этого хода, тот же пункт первого прохода.
6. **Реляционный отказ класса `tenant_service` смоделирован**, а не снят с живого Postgres:
   в тесте `getDrizzle` бросает. Живое доказательство того же — HTTP-прогон выше и первый проход;
   DB/RLS-матрицу до owner-go §10b строить запрещено.

## Уборка

- Временные правки продукта (`publicBookingPrincipal.ts`, `loadBookingEntry.ts` ×2,
  `inPersonServicesCatalog.ts`, `port-context-capabilities.bcb_webapp_dev.sql`) откачены,
  `git status` чист, кроме намеренных файлов этого хода.
- Dev-сервер `:5310` остановлен, порт свободен; соседние `:5200`/`:5201`/`:5202` не тронуты.
- Фикстур в БД не создавал и не удалял — записи в DEV-БД в этом проходе не было вовсе.
