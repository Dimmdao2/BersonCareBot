# Докупка объёма файлов — пакеты места (поручение владельца 10.09.2026)

Владелец, 10.09.2026, дословно:

> «Докупка места в настройках должна быть у всех. Пакеты с количеством места должны настраиваться в
> кабинете администраторов… то есть какие пакеты можно докупать? Какой объём? Сколько стоит?
> Собственно, модель расчёта у нас уже была прописана… докупленный пакет, то есть если надо
> увеличить место, приобретается пакет, стоимость рассчитывается с момента покупки до конца периода
> подписки основного тарифа. Доплачивается вот эта вот рассчитанная сумма. Предоставляется пакет
> места, со следующего [периода] счёт выставляется».

Повод: 10.09 подсчёт объёма свели к одному числу на весь аккаунт (`b8ac751d1`), и стало видно, что
упереться в лимит человек может, а купить место — нет. Текст отказа при переполнении владелец
оставил с советом «увеличьте объём» именно под эту работу.

## 0. Ответы владельца на развилки (10.09.2026, четыре вопроса одним листом)

| Развилка | Решение владельца |
|---|---|
| Модель денег | **Один в один как докупка места специалиста** — Р-15/Р-18 реестра `SAAS_FOUNDATION/TARIFFS_PAYMENTS_ADMIN_PLAN.md` §5а-0: объём даётся СРАЗУ, отдельный счёт на пропорцию от момента покупки до конца оплаченного периода, со следующего периода цена входит в общий счёт |
| Сколько пакетов | **Один пакет за раз**, увеличение — переход на пакет большего объёма с пересчётом |
| Отказ от пакета | **Отклоняется, пока объём занят**: сначала освободить место, потом отказываться (подтверждено владельцем 10.09 повторно, после разведки рынка — см. §0б) |
| Цена | **За период тарифа**, та же сетка, что у цены дополнительного места |

Из «один в один как места» следует, что новой денежной формулы в работе нет: цена перехода — это
`proratedRemainingPeriodAmountMinor({ currentPriceMinor: цена текущего пакета, targetPriceMinor:
цена нового, … })`, который уже умеет ровно этот случай (переход с одной цены на другую внутри
периода), а срок счёта — конец периода (Р-19), долг переносится в следующий период (Р-18).

## 0a. Разведка «за что берут деньги другие» (поручение владельца 10.09, вопрос про HLS и оригинал)

Владелец: «человек может загружать два гигабайта, а если мы храним только HLS… либо хранить файлы и
давать скачать и брать за объём, либо брать стоимость только за HLS, а файлы удалять после
транскодирования. Как это делается? Надо изучить».

**Три модели, которые реально работают на рынке:**

| Кто | Мера | Что с производными | Что с оригиналом |
|---|---|---|---|
| Mux | **минуты** хранимого видео, «regardless of file size» | не считаются: хранится один нормализованный мастер, варианты собираются на выдаче | сырой файл в исходном виде НЕ хранится; «мастер» — эквивалент по качеству, скачивается по временной ссылке (24 ч) |
| Cloudflare Stream | **минуты**, «rounded up to the second of video duration, regardless of file size» | прямо сказано: «Multiple quality levels that Stream generates» объём НЕ потребляют; MP4-загрузки тоже не потребляют хранение (они в трафике) | «You cannot download the _exact_ input file that you uploaded» |
| Vimeo | **байты ОРИГИНАЛА** («a 200MB source file will use 200MB of your total storage») | не считаются | оригинал хранится и скачивается, «хранить исходники» — отдельная функция аккаунта |
| bunny.net Stream | **байты ВСЕГО, что лежит** | считаются: каждое разрешение добавляет файлы, MP4-fallback и хранение оригинала «увеличивают объём в разы» | по выбору |

**Общее у всех, кроме Bunny:** за наш собственный энкодерный веер клиента не считают. Веер — деталь
реализации платформы, её кладут в цену, а не в счётчик. Bunny — исключение, и он же единственный,
кому приходится предупреждать пользователя, что число «умножится в разы».

**Наши собственные числа (замер по живым данным DEV 10.09.2026, 151 видео):**

- оригиналы — **21.1 ГБ**, HLS-деревья к ним — **8.31 ГБ**, отношение **0.39×** (по отдельным
  роликам 0.23–0.53×);
- то есть оригинал дороже своего HLS примерно в 2.5 раза, а сейчас в счётчик попадает ровно
  оригинал и не попадает HLS: считаем 21.1 ГБ при фактически лежащих 29.4 ГБ;
- у КАРТИНОК этой развилки уже нет: `mediaPreviewWorker` перекодирует загруженное, заменяет объект
  своим стандартным рендишном, **обновляет `size_bytes`** и удаляет оригинал (SECURITY_CANON §5).
  Видео так не делает — оно оставляет и сырой MP4, и HLS.

**РЕШЕНИЕ ВЛАДЕЛЬЦА 10.09.2026, дословно: «давай хранить и считать только размер исходников» + «и
давать их скачать».** То есть модель Vimeo: исходник лежит у нас, в счётчик идёт ЕГО размер, наш
энкодерный веер (HLS-варианты, превью, иконки) в счётчик не попадает и живёт в цене пакета. Для
видео это ровно нынешнее поведение — сырой MP4 остаётся, `size_bytes` равен размеру загруженного,
HLS не считается; менять счётчик не требуется.

Одно место, где это расходится с буквой решения, — КАРТИНКИ: `mediaPreviewWorker` по
SECURITY_CANON §5 (решение владельца 19.08) перекодирует загруженное, заменяет объект своим
стандартным рендишном, обновляет `size_bytes` и УДАЛЯЕТ оригинал. Поэтому у картинки исходника
физически нет: считается и скачивается наш стандартный рендишн. Хранить и картиночные оригиналы —
это отмена решения 19.08 и рост объёма; отдельным вопросом вынесено владельцу, по умолчанию НЕ
делаем.

## 0б. Разведка «а что делают там, где библиотеку загружает сам специалист» (10.09, вторая волна)

Повод — вопрос владельца: «человек загрузил 60 ГБ, отказался от оплаты объёма, новых файлов не грузит,
а эти 60 ГБ прекрасно раздаёт пациентам. Мы храним и платим за трафик, он не платит».

| Кто | Как продаётся объём | Что при переполнении |
|---|---|---|
| SimplePractice | не мерят вообще: «unlimited file storage» на всех планах | нечего ограничивать; но у них хранятся документы (загрузка клиента ≤ 11 МБ), видео — живая телемедицина, которая нигде не лежит |
| Practice Better | 1 ГБ на входном платном плане, до 30 ГБ на Professional, безлимит на Plus/Team; сверх плана — **1 $ за ГБ в МЕСЯЦ** | «won't be able to upload additional files until you free up space or purchase additional storage»; выдачу не режут |
| GetCourse | место — отдельные заказы со своим сроком, 100 ГБ ≈ 1580 ₽/мес (9700 ₽/год), каждый продлевается сам | дословно: «Если в файловом хранилище не осталось места, то владелец и сотрудники аккаунта не смогут загружать новые файлы никаким способом»; рычаг у них не на диске, а на тарифе — превышение лимита ПОЛЬЗОВАТЕЛЕЙ блокирует аккаунт через 7 дней |
| Vimeo | байты оригинала | единственный, кто режет ВЫДАЧУ: видео сверх лимита перестают проигрываться, исходники удаляются через 60 дней |

**Главное из разведки:** объём везде, кроме SimplePractice, — это ЕЖЕМЕСЯЧНАЯ плата за занятое, а не разовая
покупка. И никто, кроме чистой видеоплатформы, не режет выдачу пофайлово.

**Почему пофайловое отключение отклонено (владелец, 10.09):** «какой из них оплачен и какой неоплаченный?
Мы будем вспоминать по датам загрузки?». Ранжировать файлы (по дате, по размеру — как угодно) значит
объяснять пациенту, что его упражнение не открылось, потому что оказалось сорок первым гигабайтом. Единица
воздействия — АККАУНТ, а не файл.

**Итоговая модель (владелец, 10.09, после разведки):**

1. В тарифе есть включённый объём. Нужно больше — **докупается пакет, без перехода на более дорогой тариф**.
2. Пакет — ежемесячная услуга за занятое место: пока байты лежат, они в счёте.
3. **Отказ от пакета отклоняется, пока место занято.** Выход — освободить место (в том числе скачав
   исходники, М6), тогда пакет снимается со следующего периода.
4. Перестал платить — это уже не «отказ», а неоплаченный счёт, и его отрабатывает СУЩЕСТВУЮЩАЯ лестница
   (льготный период → только чтение → блокировка кабинета). Заблокированный кабинет не раздаёт ничего —
   рычаг появляется сам, без единой строчки про «какие файлы оплачены».
5. Заморозка загрузки при переполнении остаётся повседневным мягким рычагом. Сами мы не удаляем ничего.

Открыто у владельца: **безлимитный объём в тарифе** — «не уверен, что готов на безлимит, надо посчитать,
сколько это всё будет стоить». До расчёта себестоимости безлимит в тарифах не предлагаем.

## 1. Границы работы

Строим: каталог пакетов у глобального админа · покупку и переход в кабинете арендатора (у ВСЕХ
композиций — и соло, и клиника) · влияние пакета на лимит объёма · счёт и его попадание в следующий
период · отказ от пакета.

НЕ строим и не трогаем: правила доступа при неоплате (они уже есть — Р-18 + лестница тарифа),
докупку мест специалистов (она работает, мы её переиспользуем как образец, но не переписываем),
учёт производных нашего энкодера (превью, HLS-варианты, иконки — их не загружали, отдельных строк
в журнале у них нет), докупку SMS (§25.1 `OWNER_PRODUCT_RULES.md`, владелец: «не к спеху»).

## 2. Чек-лист

### М1. Каталог пакетов у глобального админа

- [x] Таблица пакетов (объём в байтах, цена за период, валюта, активность, порядок) + миграция и
      объявление прав в `deploy/postgres/privileges/declaration.ts` — `saas_storage_packages` и
      `saas_storage_package_period_prices`, миграция
      `20260910T173000_storage_packages_are_a_platform_catalog.sql`, применена на DEV.
- [x] Чтение/запись каталога в существующей админской двери коммерции (`/api/admin/commercial`), без
      второго API-корня — `storagePackages` в GET, действия `create_storage_package` /
      `update_storage_package` в POST; полнота матрицы цен проверяется ТЕМ ЖЕ
      `assertCompleteTariffPeriodPriceMatrix`, что и у тарифа, второй копии правила нет.
- [x] Секция «Пакеты объёма» в конструкторе (`CommercialConstructorClient.tsx`) рядом с тарифами —
      отдельная вкладка: список пакетов, объём в ГБ (в байты пересчитывает экран), цена за каждый
      продаваемый период, «Продаётся» вместо удаления.

Живая проверка на DEV 10.09.2026: неполная матрица цен отклонена (400), полный пакет создан
(53 687 091 200 байт = 50 ГБ, 199/999/1799 ₽), правка цены и снятие с продажи прошли, снятый пакет
остался в каталоге; вкладка отрисована (скрин передан владельцу).

Удалять пакет каталогу НЕЛЬЗЯ, поэтому `deleteStoragePackage` не существует: снятие с продажи — это
`isActive = false`. Это не упрощение, а требование денег — строка нужна счёту уже купившей
организации.

### М2. Право: пакет увеличивает лимит

- [ ] Купленный пакет живёт на подписке организации (по образцу `paid_additional_seats`: счётчик и
      ЕСТЬ доступ, двигается из одного места).
- [ ] `fileStorageLimitFromSnapshot` — единственное место, где лимит объёма складывается из тарифа и
      пакета; вторая реализация не заводится.
- [ ] То же число видно в «Использовано из включённого» и в решении о понижении тарифа.

### М3. Дверь покупки

- [x] `modules/saas-billing/storagePackage.ts` — единственная дверь «можно ли купить/перейти, почём,
      на какой отрезок услуги, до какого момента живёт счёт»; цена наружу отдельно не экспортируется
      (`decideStoragePackagePurchase`, коммит `50e2027f4`).
- [x] Котировка сервера по образцу `seatOverageQuote.ts`: в запросе покупки денег нет. Общая
      HMAC-сердцевина вынесена в `signedPurchaseQuote.ts`, оба вида покупки — тонкие обёртки над ней,
      подпись связана с предметом покупки (`sub`), коммит `60b594cfd`.
- [x] Счёт создаётся в репозитории под замком организации; идемпотентность — по личности покупки
      (`createStoragePackageInvoiceIfNeeded`, `pgSaasBilling.ts`).
- [x] Со следующего периода цена пакета входит в счёт продления (как `additionalSeatQuantity`):
      миграция `20260910T223000`, `storagePackageForNextPeriod` + обязательный
      `storagePackagePriceMinor` в `saasBillingPeriodAmountMinor`, коммит `1c3bd8393`.
- [x] Структурный гейт на вторую дверь: `scripts/check-prorated-purchase-single-door.mjs` — НЕ вторая
      копия гейта мест, а тот же разбор, где вид покупки стал строкой таблицы `DOORS`. Живая инъекция
      второй двери объёма краснит гейт (2 находки), self-test отклоняет 11 форм обхода.

### М4. Переход и отказ

- [x] Переход на больший пакет — доплата разницы по пропорции от момента перехода
      (`decideStoragePackagePurchase`, `proratedRemainingPeriodAmountMinor` с `currentPriceMinor`
      действующего пакета; тест «за остаток периода берёт пропорцию, а не полную цену пакета»).
- [x] Отказ отклоняется, пока занято больше, чем останется без пакета; отказной текст говорит, что
      именно освободить (владелец 10.09 подтвердил дословно: «пока он места не освободит, этого не может
      произойти — отказ на отключение доппакета»). Пофайловое отключение выдачи РАССМОТРЕНО И ОТКЛОНЕНО —
      §0б. Дверь: `releaseStoragePackage` (оба репозитория) через `quota.resolveStoragePackageRelease()`
      под замком механики `files`; занятое берётся из общего шва учёта, а не отдельным подсчётом.
      Маршрут: `PATCH /api/clinic/billing` `action: 'release_storage_package'` → 409
      `storage_package_occupied` с `freeBytes`.
- [x] Возврат/понижение не отбирают уже оплаченный период задним числом (Р-18): отказ гасит только
      продление, `paid_storage_package_id` остаётся до конца периода (тест «принимается, когда место
      позволяет, но объём не отбирают до конца периода»).
- [x] Покупка после отказа гасит отказ: иначе оплаченный пакет молча не дожил бы до следующего
      периода. Три колонки состояния пишет ОДИН писатель `setStoragePackageAllowance`.

### М5. Кабинет арендатора

Владелец 10.09 (вечер), дословно: «в настройках в кабинете, там, где будет вкладка про тариф,
информация, там нужно сделать… отображение заполненного пространства в расчёте от приобретённого. И
там же кнопка увеличить место — она позволяет выбрать дополнительные пакеты гигабайтов. И если там
уже пакет приобретён, он указывается, что он входит в тариф. Ну и отключить: кнопка отключить, если
место позволяет, переполнения не будет — отключение происходит сразу, ну, как сразу — в конце
оплаченного периода».

- [x] Блок в «Тариф и биллинг»: занято из доступного, текущий пакет, выбор пакета, покупка, отказ —
      `StorageSpaceBlock.tsx:79`, встроен в ту же секцию вкладки `BillingSection.tsx:143`. Витрина
      приезжает с сервера ТОЙ ЖЕ функцией, что отдаёт её маршруту (`page.tsx:34,650`): второго
      расчёта цены и второго места выпуска котировки нет.
- [x] ЗАПОЛНЕННОСТЬ показывается полосой заполнения от ДОСТУПНОГО (тариф + купленный пакет), а не
      только строкой «X из Y» — `StorageSpaceBlock.tsx:247` (та же идиома `role="progressbar"` +
      `bg-muted`/`bg-primary`, что в `DoctorClientOverviewCarePlan.tsx`, без графиков и библиотек).
      Число берётся из проекции механики `files` (`BillingSection.tsx:69`), то есть из
      `fileQuotaWithPurchasedStorage` — из того же потолка, который разрешает загрузку; своего
      «доступно» блок не считает. Живьём на DEV 10.09: «Занято 19.7 ГБ из 75.0 ГБ» при тарифных
      25 ГБ и пакете 50 ГБ.
- [x] Купленный пакет подписан прямо в блоке — «Входит в тариф», `StorageSpaceBlock.tsx:268`;
      отдельной страницы нет.
- [x] Кнопка «Отключить» рядом (`StorageSpaceBlock.tsx:271-280`); отказ по переполнению называет
      число из ответа двери (`freeBytes` → `StorageSpaceBlock.tsx:210`), успех говорит про конец
      оплаченного периода (`StorageSpaceBlock.tsx:194`). Оформленный отказ виден как «Отключён с
      <дата>» (`StorageSpaceBlock.tsx:262`).
- [x] Виден у ВСЕХ композиций, включая соло: блок не различает композиции по построению — в
      `page.tsx` фильтр `hideTeamCapacity` трогает только `clinic_team`. Проверено живьём 10.09 на
      DEV-кабинете СОЛО (вкладки «Организация · Онлайн-запись · Профиль специалиста · Тариф и
      биллинг», без «Команды»), скрин передан.
- [x] Отказ при переполнении ведёт в этот блок, а не в тупик: текст отказа загрузки файла пациента
      получил ссылку «Увеличить место» на `?tab=billing` (`PatientTabFiles.tsx:795`). Новых экранов
      не заведено — ссылка встала в существующее сообщение об отказе.
- [x] Ничего нового в дизайне: `primitives/button`, `primitives/select` (с `displayLabel` по §22),
      `primitives/badge`, `doctorDnaFlatList*` и внутренние блоки той же `DoctorSection`; новых
      токенов, графиков и зависимостей нет. Форматтер байт не продублирован — общий
      `billingQuotaFormat.ts` на список чисел и на блок; денежный формат сведён в
      `shared/lib/formatMinorAmount.ts` (было три копии).

**Живая проверка на DEV 10.09.2026** (свой `next dev` из worktree на `127.0.0.1:5310`, обычный вход
врача `dimmdao@yandex.ru`): вкладка отрисована, полоса и обе подписи на месте, консольных ошибок нет.
НЕ проверено живьём и не выдаётся за проверенное: панель выбора пакета и покупка. У DEV-организации
`billing_period_code` подписки пуст (тариф бесплатный, период никогда не оплачивался), поэтому цена
пакета за период не подтягивается и КАЖДЫЙ пакет каталога приходит `not_sold` — покупать на этих
данных нечего. Проверено, что это состояние данных, а не кода: заведённый в каталоге второй пакет
«+200 ГБ» (`create_storage_package`, 199/999/1799→599/2999/5399 ₽) вернулся клинике как `not_sold`
при живой цене на все три периода. Отказ покупки по подделанной котировке при этом отвечает 402
`storage_package_quote_expired` — цену из браузера дверь не читает. Отказ от пакета (`Отключить`)
живьём НЕ нажимался намеренно: на этих данных он бы прошёл (занято 19.7 ГБ < 25 ГБ без пакета) и
оставил бы DEV в состоянии «отключён» без обратного пути, потому что покупка тут невозможна.

### М6. Скачивание исходника (владелец 10.09: «и давать их скачать»)

- [ ] Кнопка «Скачать исходник» у видео в медиатеке и в карточке пациента — отдаёт тот самый объект,
      размер которого посчитан, через существующую дверь выдачи (`authorizeMediaDelivery`), а не
      новым маршрутом.
- [ ] Файлы ПАЦИЕНТА скачиваются наравне с медиатекой (владелец 10.09: «надо чтобы и файлы
      пациентов тоже можно было скачивать»). Уточнение к его же вопросу «мы их после
      транскодирования удаляем»: проверено по `mediaPreviewWorker.ts` — удаляется оригинал ТОЛЬКО у
      картинок (`applyStandardImageRendition`, решение 19.08); видео сохраняется как загружено, HLS
      лежит рядом производной. То есть у видео пациента есть что отдать уже сейчас.
- [ ] У картинок скачивается стандартный рендишн (оригинала нет — SECURITY_CANON §5); текст кнопки
      не обещает «оригинал» там, где его нет.
- [ ] Право на скачивание — у той организации, которой файл принадлежит; пациент скачивает только
      своё (существующие стены, без новых).
- [ ] **Владелец 10.09 (вечер), дословно:** «исходник отдаём, и, естественно, только тому
      специалисту, который это загрузил, и только как вложение… только как загружаемый файл,
      неисполняемый в браузере, несмотря на то, что это видео. Это надо обязательно закрыть
      максимально жёстко». Отсюда три обязательных условия выдачи исходника, каждое проверяемо:
      1. загрузивший специалист (а не любой сотрудник организации);
      2. `Content-Disposition: attachment` + `Content-Type: application/octet-stream` +
         `X-Content-Type-Options: nosniff` — браузер обязан скачать, а не проиграть;
      3. отдельная ссылка с коротким сроком, не переиспользуемая для встроенного проигрывания.

### М7. Отдельный бакет сырых загрузок (решение владельца 10.09.2026, вечер)

Владелец, дословно: «мы можем хранить оригиналы, загруженные специалистом, и их перепаковывать уже
для отдачи… а всё, что загрузил специалист, просто лежит, и оно не исполняется ни при каком
варианте, снаружи к нему доступа тоже нет. Может быть, нам вообще нужен отдельный bucket, куда
именно загрузки складываются, чистые, и там размер считать тогда просто будет — по объёму этого
бакета, и там по папкам внутри него, каждая папка — организация. И тогда нам не надо ставить
антивирус на сервер: в России сигнатуры не скачать, мы решили сейчас этого не делать».

Модель: **сырое отдельно, отдаваемое отдельно.** Загруженный файл кладётся в бакет загрузок и
оттуда НИКОГДА не отдаётся наружу; наружу идёт только то, что перепаковал наш энкодер (HLS,
стандартный рендишн картинки). Перекодирование и есть санитизация — вредоносный файл не
исполняется, потому что до браузера доезжает наш собственный вывод, а не чужие байты.

- [ ] Отдельный бакет (или выделенный префикс с собственной политикой) под сырые загрузки; папка
      верхнего уровня — организация, чтобы объём читался и «снаружи», простым обходом бакета.
- [ ] Загрузка пишет ТУДА; выдача (`authorizeMediaDelivery`, HLS-прокси) физически не умеет туда
      ходить — из сырого бакета не выдаётся ничего, кроме явного скачивания исходника (М6).
- [ ] Картинки перестают затирать оригинал: `mediaPreviewWorker` кладёт рендишн в бакет выдачи, а
      исходник ОСТАВЛЯЕТ в сыром. Это отменяет решение 19.08 (SECURITY_CANON §5) — фиксируем как
      осознанную замену: защита теперь не «оригинала нет», а «оригинал не отдаётся и не исполняется».
- [ ] Перенос уже лежащих оригиналов в сырой бакет + сверка счётчика.

**Изоляция перекодировщика (владелец 10.09, вечер).** Дословно: «наш собственный перекодировщик —
ну да, классно, я только не знаю, насколько это уже сейчас реализовано правильно… если это нормальная
история, она работает и не очень дорого по разработке сейчас — надо сделать. А если что, мы туда
пишем потом». То есть: замерить, как сегодня устроен наш энкодер (кто его запускает, с какими
правами, есть ли у него сеть и ключи), и либо закрыть дёшево, либо записать сюда с оценкой.
Причина, по которой это вообще стоит в плане: стена «сырое не отдаётся» закрывает браузер клиента, но
НЕ закрывает нас самих — ffmpeg и картиночные библиотеки разбирают враждебный файл у нас на сервере.

- [x] **Замер сделан 10.09.2026.** Перекодировщиков ДВА, и они изолированы по-разному:
      * **Видео → HLS: отдельный процесс** `apps/media-worker` (`src/main.ts:19`, в новом проде —
        контейнер `media-worker` в `deploy/docker/docker-compose.yml:137`). Учётных данных БД у него
        НЕТ вовсе (`env.ts:67-81` падает, если они появятся), HTTP-порта нет, ffmpeg зовётся только
        argv-массивом (`ffmpeg/runFfmpeg.ts:17`, `shell:true` в путях кодирования нет ни разу),
        `cap_drop: ALL`, `read_only: true`. То есть базовая изоляция УЖЕ есть.
      * **Картинки/HEIC/постеры: ВНУТРИ процесса вебаппа** (`mediaPreviewWorker.ts:354`, sharp +
        ImageMagick + ffmpeg). Тот же процесс держит пулы БД, `SESSION_COOKIE_SECRET` и отвечает на
        запросы врачей и пациентов. Это и есть настоящая дыра: разбор враждебных байт идёт там, где
        лежат данные пациентов.
      Что найдено сверх этого, в порядке дешевизны исправления:
      1. контейнеру энкодера смонтированы сокет postgres и четыре клиентских ключа mTLS, которые ему
         не нужны ни для чего (общий якорь compose, `docker-compose.yml:36-42`);
      2. лимита памяти/CPU у него нет вообще; таймаут одного вызова ffmpeg — 2 часа, и он
         применяется к каждому из трёх рендишенов;
      3. сеть наружу не ограничена (`harden-network-and-ssh.sh:99`), а ключи S3 у него с правом
         удаления в ОБОИХ бакетах;
      4. вебапп передаёт ffmpeg подписанный HTTPS-URL как `-i` без `-protocol_whitelist`.
- [x] **Дешёвое сделано 10.09.** Владелец: «давай сделаем сейчас дёшево, а потом фоном отдельно, в
      отдельной ветке, запусти дорого». Из контейнера энкодера убраны сокет PostgreSQL, каталог
      клиентских ключей mTLS и группа доступа к ним (`deploy/docker/docker-compose.yml`, служба
      `media-worker`: `volumes: []`, `group_add: []`) — ни для чего из этого у воркера нет
      применения, а держал он их в процессе, который парсит враждебное видео. Поставлен
      `pids_limit: 1024`.
      Пределы памяти и CPU НЕ поставлены осознанно: временный каталог у контейнера — tmpfs, то есть
      HLS-нарезка занимает ту же память, и наугад выбранный `mem_limit` убивал бы законную
      перекодировку большого файла. Это отдельный шаг, ему нужен замер живого хоста.
- [ ] **Дорогое — отдельной веткой, фоном.** Вынести разбор картинок (sharp + ImageMagick + ffmpeg)
      из процесса вебаппа в media-worker через существующий типизированный шов
      `mediaWorkerControl.ts`. Сегодня враждебные байты разбираются там же, где живут пулы к базе и
      сессионный секрет, — это единственная оставшаяся настоящая поверхность.

Что надо назвать вслух и решить отдельно (не выдумываю за владельца):

1. **Перекодируется не всё.** Документы (PDF, docx) в файлах пациента наш энкодер не трогает.
   Для них санитизация — это ровно «лежит и не отдаётся»; в момент, когда мы такой файл всё-таки
   отдаём человеку, он получает чужие байты как есть. Поэтому скачивание исходника (М6) стоит
   держать за сотрудниками организации, которая его и загрузила, а не открывать пациенту.
2. **Счётчик остаётся в базе.** Обход бакета — это сверка (раз в сутки), а не живая проверка под
   замком организации: загрузка обязана решать «влезает ли» в той же транзакции, где считает.
   Объём бакета по папкам — независимая проверка того, что база не врёт.

### М6a. Приёмка

- [ ] Живой прогон на DEV: покупка, переход, отказ, переполнение, счёт в следующем периоде.
- [ ] Владелец проходит маршрут сам.

## 3. Что переиспользуется, а не пишется заново

- `modules/saas-billing/proration.ts` — вся арифметика пропорции и потолок цены;
- `modules/saas-billing/seatOverage.ts` — образец единственной двери (структурный гейт при этом НЕ
  повторён копией: `scripts/check-prorated-purchase-single-door.mjs` проверяет оба вида покупки одним
  разбором — скопированный гейт разошёлся бы с оригиналом молча и был бы зелёным впустую);
- существующий конвейер счетов: провайдер, чек, перенос долга (Р-18), автосписание;
- `transactionQuotaPort` — замок организации и место, где решение о квоте принимается в транзакции.
