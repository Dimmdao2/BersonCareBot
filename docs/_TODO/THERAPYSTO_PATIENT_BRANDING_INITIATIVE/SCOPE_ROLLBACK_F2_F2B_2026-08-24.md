# Откат `F2`+`F2b` из ветки брендирования — работа вне объёма владельца (24.08.2026)

Что произошло: ведущий (`bersoncarebot-3d`) запустил и принял работу по пунктам `F2` и `F2b`, не сверив их
с более поздним решением владельца §1.2j от 23.08.2026. Оно сужает этап `F` дословно:

> `F2`, `F2b`, `F5` кодом НЕ делаем: это значения переключателей, владелец выставляет их сам в настройках.
> Механики, маршруты и UI остаются на месте — вырезать по-прежнему нечего.

Текст пунктов `F2`/`F2b` в §2 старше (22.08) и §1.2j его перекрывает. Аудит был проведён против устаревшего
текста и дал `PASS` — вердикт верен по своему оракулу, но оракул был не тот. Ошибка ведущего, не исполнителя
и не аудитора.

Что сделано:

- код `F2`+`F2b` (`6dc5b260f`) в ветке `wt/therapysto-night-20260823` отменён коммитом-revert;
- вместе с ним снят аудиторский тест `auditF2SurfaceMechanicGate.route.test.ts`: он проверяет именно
  отменённый гейт и без него станет красным;
- отчёт аудита `AUDIT_F2_F2B_2026-08-24.md` оставлен на месте как запись факта;
- сама работа НЕ потеряна: ветка `wt/therapysto-f2-20260824` (`6dc5b260f`, аудит `741316be9`) цела, из неё
  всё возвращается одним `git cherry-pick`, если владелец решит, что резолверный отказ ему всё-таки нужен.

Вопрос владельцу (не работа): §1.2j говорит «выставлю сам в настройках», а сегодня OAuth и passkey
отключаются значением, которое НЕ проверяется на уровне резолвера — выключенная механика скрыта в UI, но
маршруты входа остаются достижимыми напрямую. Нужен ли отказ в резолвере, или достаточно того, что есть?
