# Закрывающий независимый аудит `S-1`, круг 2 — `PASS` с оговорками, FOR LAND

**Вердикт: `PASS` — снятие `app.email_auth_enqueue_otp_delivery` полное, корректное и безопасное; вход по коду
на почту не изменился. Приземлять можно.** Две оговорки НЕ блокируют этот коммит и НЕ относятся к его скоупу —
это открытые пункты `TPB-15`, вынесенные владельцу вопросом (см. «Вне скоупа»).

Под аудитом: `0f8cf4af6` на `wt/therapysto-s1-20260824` (в дереве `7800b3f13`, слитом с `feat/doctor-ui-rebuild`).
Автор: Codex `gpt-5.6-sol`/high. Аудитор: Claude Opus 5/high, бриф `S1_AUDIT2_BRIEF_2026-08-24.md`.
Оракул: `IMPLEMENTATION_PLAN.md`, `TPB-15`. Круг 1 — `AUDIT_S1_2026-08-24.md`.

**Инъекций посажено `5`, убито `4`, не поймано `1`.**

## Построчно по брифу

| Пункт | Вердикт | Чем доказано |
| --- | --- | --- |
| `R2-1` вызывающих у снятого пути не было | **PASS** | Свой обход, не сверка со списком автора: `git grep` по дереву `0f8cf4af6^` в `apps/webapp/src` + `apps/integrator` даёт **ровно 4 места**: объявление `enqueueEmailOtpDelivery` (`modules/auth/emailOtpDeliveryQueuePort.ts:42`), адаптер `pgAuthEmailOtpDeliveryQueue.ts:15`, DI-привязка `bindAuthModulePorts.ts:19,41` и два комментария. **Ни одного вызова.** Путь действительно осиротел до правки. |
| `R2-2` вход по коду на почту работает как прежде | **PASS** | Слепой kill-set построен ДО открытия авторских тестов, по живому коду (`emailAuth.ts:284`, `pgEmailAuth.ts:43`, `mailProfile.ts:31`), свои значения. База 16/16 зелёные. `I1` (staff-поверхность получает клиентский профиль вместо `Therapysto`) — **убита**, 2 падения. `I2` (открытый код не доезжает до атомарного корня, `code: ''`) — **убита**, 1 падение. `I3` — см. ниже, не поймана. Активный путь идёт через `app.email_auth_start_challenge(11 арг.)`, снятая функция в нём не участвует. |
| `R2-3` снятие полное и последовательное | **PASS по снятому объекту**, оговорка по соседней половине | Взгляд на diff: сняты порт, DI-привязка, адаптер, функция (миграция), записи в переписи (`function-census.ts`), `relation-access.test.mjs`, overlay-гранты (`d3-4-*`, `dev-c1-*`, `organization-member-invites-rls.sql`), гейт владения на TEST (`deploy-test-saas.sh:1054`) и все сгенерированные артефакты. **Висящих ссылок в правах и гейтах не осталось** — проверил отдельно счётные гейты: `expected_db_owner_anon_secdef=21` не меняется (функция была `app_owner`-owned, не `$DB`-owned), живого пина на число `app_owner`-secdef в скрипте НЕТ (только в комментариях), гейт `assert_login_fix_definer_owners_pinned` сохранил сиблинг `set_email_challenge_delivery_code(uuid,text)` — снят только исчезнувший подпись-ряд, иначе гейт упал бы с `<missing>`. `prod-to-target/schema-pre.sql` функцию сохраняет — это снимок ПРОДА, где миграция ещё не применена, расхождения тут нет. |
| `R2-4` миграция без операций с правами, гейты проходят | **PASS** | Тело миграции — один `DROP FUNCTION IF EXISTS` + `BCB-MIGRATION-OWNER: app_seam_email_otp_owner` + `BCB-MIGRATION-VERIFY`. Свой grep по `GRANT/REVOKE/CREATE ROLE/ALTER ROLE/ALTER DEFAULT PRIVILEGES/CREATE POLICY` — **0 совпадений**. Свои прогоны: `migration-order.test.mjs` + `function-census.test.mjs` + `relation-access.test.mjs` = **80/80, 0 fail**; `check-c4-migration-owned-function-bodies` OK; `check-queue-port-boundary` OK. |
| `R2-5` артефакты пересобираются байт-в-байт | **PASS** | Свой прогон `node deploy/postgres/privileges/generate-cli.mjs --all --check` → `EXIT=0`, четыре артефакта (`privileges`/`org-allowlist` × `bcb_webapp_dev`/`bersoncarebot_test`) «совпадает побайтно». |
| `R2-6` авторская правка не «зелёная всегда» | **PASS** | Коммит новых тестов не добавлял, поэтому проверял обратным ходом — что снятые строки держали живые гейты. `I4`: вернул удалённый ряд в `relation-access.test.mjs` → **красный** (`# fail 1`). `I5`: вернул удалённую запись функции в `function-census.ts` → генератор **отказал** (`декларация неполна … must name exact delegated roots`). Плюс `I2` краснит `emailAuth.durableQueue.d27c.test.ts` — тест не всегда зелёный. |
| `R2-7` «BersonCare» не собирается в живом пути аутентификации | **FAIL по оракулу `TPB-15`** (не по этому коммиту) | Свой исчерпывающий `rg`. В `deploy/postgres/*.sql` — **0 совпадений**, чисто. В живом коде аутентификации осталось **два места**: `apps/webapp/src/modules/auth/passkeyAuth.ts:31` `rpName: 'BersonCare'` (видно человеку в диалоге создания passkey браузера/ОС) и `apps/webapp/src/modules/staff-security/totp.ts:51` `issuer = 'BersonCare'` (видно в приложении-аутентификаторе, плюс попадает в `otpauth://` label). Само письмо с кодом собирается корректно из `mailProfile.senderDisplayName` (`apps/integrator/src/integrations/email/mailProfile.ts:98-99`), старой строки в нём нет. |

## Инъекции

| # | Куда | Что ломал | Результат |
| --- | --- | --- | --- |
| `I1` | `modules/auth/mailProfile.ts:32` | staff/platform_admin отдают клиентский профиль вместо `Therapysto` | **убита** (2 падения) |
| `I2` | `modules/auth/emailAuth.ts:345` | открытый код не передаётся в `app.email_auth_start_challenge` | **убита** (1 падение) |
| `I3` | `modules/auth/emailAuth.ts:347` | отказ по rate-limit (`challengeId=null`) выдаётся наверх как `ok:true` с выдуманным `challengeId` | **НЕ ПОЙМАНА** |
| `I4` | `privileges/relation-access.test.mjs:290` | вернул снятый ON CONFLICT-ряд снятой функции | **убита** (`# fail 1`) |
| `I5` | `privileges/function-census.ts:1917` | вернул снятую запись функции в перепись | **убита** (генератор отказал) |

`I3` не поймана даже расширенным прогоном: `src/modules/auth` + `src/app/api/auth` целиком — **33 файла, 164/164
зелёные**, пока пользователю отвечают «код отправлен» на запрос, который база отвергла по кулдауну и по которому
ничего не поставлено в очередь. Это ровно тот класс «маскировки отказа», против которого строился D27-C.
**Дыра предсуществующая, кругом 2 не внесена** (эта ветка кода не менялась) — называю, не чиню.

## Вне скоупа круга 2 — вопросы владельцу, не работа

1. **Снята одна половина устаревшей пары, вторая жива.** Атомарный корень `app.email_auth_start_challenge`
   пишет `pending_delivery_code = NULL, delivery_token = NULL`
   (`20260823T043206_deliver_c4_mail_profile_tenant_binding.sql:120-122`). Значит у второй половины —
   `app.email_auth_set_email_challenge_delivery_code(uuid,text)` и её единственного вызывающего
   `insertEmailChallenge` (`pgEmailAuth.ts:128`, привязан к порту на `:416`) — **тоже нет ни одного
   продуктового вызывающего**: `insertEmailChallenge` встречается только в объявлении порта, реализации,
   привязке и тесте. При этом функция остаётся `SECURITY DEFINER`, достижимой из bootstrap-роли (гранты в
   `d3-4-bootstrap-base-login-read-grants.sql` и `dev-c1-bootstrap-schema-app-grants.sql` сохранены) и пишущей
   в чужую строку `email_challenges` — та самая поверхность, на которой круг 3 D27-C ловил перезапись кода на
   чужой попытке входа. Это **тот же класс дефекта**, который круг 2 только что снял, — но снятие второй
   половины в бриф круга 2 не входило. Вопрос владельцу: снимать ли её тем же приёмом отдельным кругом.
2. **`TPB-15` не закрыт:** `passkeyAuth.ts:31` и `totp.ts:51` (см. `R2-7`). Смежно, вне пути аутентификации:
   `modules/patient-booking/sendBookingConfirmationEmail.ts:105,116` («С уважением, BersonCare» в письме
   подтверждения записи) и `apps/integrator/src/integrations/web-push/deliveryAdapter.ts:133` (fallback-заголовок
   пуша `'BersonCare'`). Пункт `TPB-15` в плане стоит `- [ ]` — эти места закрывают его остаток.
3. **Мелочь, не дефект:** `apps/integrator/src/infra/runtime/worker/outgoingDeliveryWorker.authEmailOtp.d27c.test.ts:35,37`
   держит фикстуру `'Ваш код BersonCare: 123456'` / `'Код подтверждения BersonCare'`; воркер payload только
   релеит, поведение не пинится, но строка устарела. `email-otp/start/route.route.test.ts:295` ссылается
   комментарием на удалённый `pgAuthEmailOtpDeliveryQueue.ts`.

## Границы соблюдены

Прод не тронут, `--execute` на DEV не запускался, в `feat` не вливалось, реальным адресатам не слалось,
`app.read_integrator_clinic_delivery_credential`, миграции `20260823T030000…`/`20260823T043206…` и
`app.enqueue_outbound_message` не трогались. Все инъекции откачены, дерево после них чистое
(`git status --porcelain` пуст по `apps/` и `deploy/`).

## НЕ СДЕЛАНО

- Живой прогон на DEV/TEST (`--execute`, применение миграции, реальная отправка кода) — запрещён брифом.
- Full CI целиком — гонялись только затронутые гейты и scoped-тесты.
- Тело `app.email_auth_set_email_challenge_delivery_code` на предмет проверки владения не разбиралось —
  вне скоупа круга 2, вынесено вопросом выше.
