# Независимый аудит слайса `B3`, круг 3 — однохостовость по умолчанию

**Дата:** 23.08.2026 · **Аудитор:** Claude Opus 5 / high (автор — Codex `gpt-5.6-terra`/high, коммит сделал
ведущий после обрыва порта)
**Оракул:** `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, пункт `B3` и `Gate B`
**Кандидат:** `517c53aab` (плюс `1e8a86b4b`, `6aeab03c4` того же слайса) на `wt/night-b3-20260823`
**Дерево аудита:** `/home/dev/dev-projects/bcb-wt-night-b3-20260823`, `HEAD` = `572dadfe5`
**Круг 1:** `FAIL`, 3 блокера, непойманного 4 · **Круг 2:** `FAIL`, блокер `R-1`, непойманного 3

## Вердикт: **FAIL, NOT FOR LAND**

**Блокирующих `1`. Неблокирующих `3`. Инъекций посажено `6`, убито `5`, не поймана `1`.
Непойманного (дефектов мимо проверок автора) — `2`.**

Продуктовая правка сделана правильно и закрывает блокер круга 2 по-настоящему: незаданный
`PATIENT_APP_ORIGIN` теперь означает `APP_BASE_URL` в единственном config-seam, и на конфигурации, которая
реально существует на DEV и TEST, пациентское дерево живёт целиком. Явно заданный origin по-прежнему
побеждает, гейт при двух Host работает в обе стороны, `500` на манифесте закрыт. Всё это я перемерил своими
руками на своей production-сборке — цифры ниже мои, не переписанные из отчёта автора.

**Но цена, которой это куплено в тестах, — блокер.** Чтобы набор позеленел при новом дефолте, автор удалил
единственный тест, проверявший, что гейт аудитории маршрутов ОТБИВАЕТ чужое дерево, и заменил параметризацию
поверхностей утверждением `expect(PATIENT_DEFAULT_SURFACE.origin).toBe(STAFF_SURFACE.origin)`. Результат
измерен, а не предположен: **весь гейт можно выключить намертво, и все 2027 тестов вебаппа остаются
зелёными**; а стоит выставить `PATIENT_APP_ORIGIN` — то есть перейти в конфигурацию TEST и PROD, ради которой
эта правка и делалась, — как набор краснеет. Это ровно то, что `AGENTS.md` §10 запрещает словами «тесты
подгоняются под код, а не код под тесты», и прямой промах против `Gate B` плана владельца, который требует
host-матрицу тестов по `staff`, `platform_admin`, `patient_default`, `patient_branded`, unknown.

---

## 1. Что проверено и как

| Вопрос брифа | Способ | Результат |
| --- | --- | --- |
| Блокер `R-1` круга 2: шесть путей при отсутствующем `PATIENT_APP_ORIGIN` | **живой прогон** production-сборки, порт `6320` | **закрыт** — §2 |
| Обратная сторона: гейт работает при двух различимых Host | живой прогон, один сервер, два Host, порт `6322` | **PASS** — §2 |
| Явно заданный `PATIENT_APP_ORIGIN` побеждает | живой прогон в этой самой конфигурации | **PASS** — §2 |
| `FB-2`: тест спрашивает окружение, а не подменяет присваиванием | 3 инъекции в шов `config/env.ts` | **PASS** по букве, но см. блокер `A-1` |
| `FB-3`: `/manifest.webmanifest` на `platform_admin` — определённый ответ | живой прогон на трёх конфигурациях + инъекция | **PASS**, но фикс односторонний — `A-3` |
| Единственность резолвера и места решения пациентского origin (`TPB-16`) | обход дерева `grep` по всем читателям Host | **PASS** — §5 |
| Соответствие пункту `B3` плана владельца | сверка текста | по букве выполнено, приёмка `Gate B` — нет |
| Полный CI по ветке | `pnpm run ci` через общий замок хоста | §6 |

**Как гонялся живой прогон.** Сборка в этом дереве через общий замок:
`NODE_ENV=production ALLOW_DEV_AUTH_BYPASS=false ENV_FILE=…/BersonCareBot/apps/webapp/.env.dev
/home/dev/brain/host-orch/run-tests.sh "pnpm run build:webapp"` → **rc=0, 128 s,
`BUILD_ID = cLmWT5yDTAP6CIW2KOoTy`**. Затем standalone `node .next/standalone/apps/webapp/server.js` на
портах `6320`/`6321`/`6322` (порт `5200` не занимался, `6300` чужой — не трогался; все три сервера погашены
за собой). Окружение — реальный `apps/webapp/.env.dev` главного дерева, `PATIENT_APP_ORIGIN` из него снят
явно; секреты не печатались. Продуктовый код на время замеров не менялся.

Три конфигурации:

- **A — сегодняшние DEV и TEST:** `APP_BASE_URL=http://127.0.0.1:5200`, `PATIENT_APP_ORIGIN` **отсутствует**.
- **B — то же, но доменный origin:** `APP_BASE_URL=http://staff.local:6321`, `PATIENT_APP_ORIGIN` отсутствует
  (нужна, чтобы админский Host вообще был достижим — см. `N-8` круга 1).
- **C — два различимых Host:** `APP_BASE_URL=http://staff.local:6322`,
  `PATIENT_APP_ORIGIN=http://patient.local:6322`.

---

## 2. Живые замеры

### Конфигурация A — блокер `R-1` круга 2 закрыт

Host `127.0.0.1:5200` (тот самый, на котором круги 1 и 2 получали пустые `404`):

| путь | круг 2 (до правки) | круг 3 (эта правка) |
| --- | --- | --- |
| `/app/patient/login` | 404, 0 байт | **200, 40202 байт** |
| `/app/patient/cabinet` | 404, 0 байт | **307**, 50 байт → `/app/patient/login?next=%2Fapp%2Fpatient%2Fcabinet` |
| `/book` | 404, 0 байт | **200, 21802 байт** |
| `/book/embed.js` | 404, 0 байт | **200, 1774 байт** |
| `/join/start` | 404, 0 байт | **200, 16685 байт** |
| `/clinic-a` | 404, 0 байт | **404, 15541 байт** — app-level, не гейт |
| `/manifest.webmanifest` | 404, 0 байт | **200, 668 байт** |
| `/maintenance.html` | 404, 0 байт | **200, 2923 байт** |
| `/api/health` | 200 | **200**, `{"ok":true,"db":"up"}` |
| `/` · `/app` · `/app/doctor/login` · `/legal/terms` | 200 | 200 — 103727 / 38478 / 40241 / 25247 байт |

Пустой гейт-`404` отличён от app-`404` по размеру тела ровно как в кругах 1 и 2: `/clinic-a` даёт
15541 байт отрендеренной страницы, а не ноль. Контракт установленных PWA цел побайтно:
`id="/app"`, `scope="/app"`, `start_url="/app/patient"`, `name="Therapygo — забота о твоём здоровье"`.
`/manifest-staff.webmanifest` на том же Host — отдельный `200`, 527 байт.

Хард-`404` без отката на платформу не пострадал: неизвестный Host `evil.example` — `404, 0 байт` на
`/`, `/api/health`, `/app/patient/login`. Админский Host `admin.127.0.0.1:5200` при IP-образном
`APP_BASE_URL` недостижим (`404, 0 байт` на всём) — это `N-8` круга 1, артефакт окружения, не дефект.

### Конфигурация C — обратная сторона не сломана, явный origin побеждает

Один и тот же сервер, два Host. `PATIENT_APP_ORIGIN` задан явно — то есть это же и есть доказательство
пункта 3 брифа:

| путь | Host `staff.local:6322` | Host `patient.local:6322` |
| --- | --- | --- |
| `/app/patient/login` | **404, 0 байт** | 200, 40275 байт |
| `/app/patient/cabinet` | **404, 0 байт** | 307, 50 байт → login |
| `/book` | **404, 0 байт** | 200, 21878 байт |
| `/book/embed.js` | **404, 0 байт** | 200, 1774 байт |
| `/join/start` | **404, 0 байт** | 200, 16761 байт |
| `/clinic-a` | **404, 0 байт** | 404, 15617 байт (app-level) |
| `/manifest.webmanifest` | **404, 0 байт** | 200, 668 байт |
| `/maintenance.html` | **404, 0 байт** | 200, 2923 байт |
| `/app/doctor/login` | 200, 40241 байт | **404, 0 байт** |
| `/api/health` | 200 | 200 |

Пациентское дерево отбивается на staff-хосте, staff-дерево — на пациентском, общие маршруты живут на обоих.
Админский Host `admin.staff.local:6322`: `/app/doctor/login` `200`, `/app/patient/login` и
`/manifest.webmanifest` — `404, 0 байт`.

### Конфигурация B — `FB-3` закрыт, `500` не воспроизводится

Host `admin.staff.local:6321`, доменный `APP_BASE_URL`, `PATIENT_APP_ORIGIN` отсутствует — то есть тот самый
путь, на котором круг 2 получал `500` и `⨯ Error: patient_manifest_requires_patient_surface`:

| путь | круг 2 | круг 3 |
| --- | --- | --- |
| `/manifest.webmanifest` | **500, 0 байт** | **404, 0 байт** |
| `/manifest-staff.webmanifest` | 200 | 200, 527 байт |
| `/app/patient/login` · `/app/doctor/login` · `/` · `/api/health` | 200 | 200 (`R-2`, ниже) |

В логах всех трёх серверов за весь прогон — **ни одной строки `Error`/`⨯`**. Ответ выбран `404`, и это
правильный выбор: `/manifest.webmanifest` принадлежит пациентской аудитории по той же таблице
(`config/surfaceRoutes.ts:137-139`), а `platform_admin` в неё не входит.

---

## 3. Блокирующая находка

### `A-1` (БЛОКЕР). Гейт аудитории маршрутов остался без единого теста, а набор привязан к однохостовому окружению

Правка дефолта имеет побочный эффект, которого в отчёте автора нет: в vitest `APP_BASE_URL` не задаётся
(`vitest.setup.ts` его не пишет), значит берётся дефолт `http://127.0.0.1:5200`
(`config/env.ts:32`), а `PATIENT_APP_ORIGIN` теперь падает на него же (`config/env.ts:222`). Итог —
**в тестовой среде `PATIENT_DEFAULT_SURFACE.origin` и `STAFF_SURFACE.origin` стали одной и той же строкой**,
`arePlatformSurfaceHostsDistinct()` всегда `false`, и гейт `canSurfaceEnterRoute` в `proxy.ts:58-62`
не выполняется ни в одном тесте репозитория.

Автор привёл набор в соответствие с этим состоянием, а не наоборот. В `517c53aab` из
`apps/webapp/src/proxy.route.test.ts` **удалён** единственный тест, проверявший отбой:

```
-  it.each(['/app/patient/profile', '/book', '/manifest.webmanifest'])(
-    'hard-404s patient route %s on the staff Host when surface Hosts are distinct',
```

и `[PATIENT_ORIGIN, '/app', 'patient_default']` заменено на `[PATIENT_ORIGIN, '/app', 'staff']`, а
подстановка origin через `Object.defineProperty` — на утверждение
`expect(PATIENT_DEFAULT_SURFACE.origin).toBe(STAFF_SURFACE.origin)` (`proxy.route.test.ts:187`).
Тестов в файле было 18, стало 17.

**Нога первая — гейт не защищён ничем. Измерено.** Инъекция `I-4`: `arePlatformSurfaceHostsDistinct()`
всегда возвращает `false`, то есть гейт маршрутов мёртв при ЛЮБЫХ Host, включая два домена после `B7`/`B8`.

```
scoped-набор (6 файлов):   Test Files 6 passed (6)   Tests 67 passed (67)
весь вебапп:               Test Files 431 passed | 4 skipped (435)
                           Tests 2027 passed | 12 skipped (2039)   rc=0, 130 s
```

**Ни один из 2027 тестов не краснеет.** В круге 2 эта же инъекция краснила три теста — их больше нет.
Оставшиеся в файле `404`-проверки (`proxy.route.test.ts:315,322`) держат другую ветку — хард-`404` для
неизвестного и неактивного арендаторского Host, где резолвер возвращает `null`; гейта аудитории они не
касаются. `canSurfaceEnterRoute` продолжает проверяться как чистая функция
(`config/surfaceRoutes.unit.test.ts`), но её подключение к choke point — нет.

**Нога вторая — набор краснеет в целевой конфигурации. Измерено.** Тот же scoped-набор, единственное
изменение — `PATIENT_APP_ORIGIN=https://therapygo.ru` в окружении прогона, то есть ровно то, что владелец
выставит на TEST и PROD, когда домен появится («я сразу дам оба домена и мы переедем», 22.08):

```
FAIL  proxy.route.test.ts > keeps the patient portal on the patient Host
      AssertionError: expected null not to be null
FAIL  proxy.route.test.ts > keeps both route trees and health reachable when staff and patient share one Host
      AssertionError: expected 'https://therapygo.ru' to be 'http://127.0.0.1:5200'
FAIL  proxy.route.test.ts > stamps https://therapygo.ru/ once for /app
      AssertionError: expected { surface: 'patient_default', … } to match object { surface: 'staff', … }
Test Files 1 failed | 5 passed (6)    Tests 3 failed | 64 passed (67)
```

Набор зелёный ТОЛЬКО в переходной однохостовой конфигурации и краснеет в день переезда — то есть merge-gate
сломается тогда, когда он нужнее всего.

**Против чего это.** `Gate B` плана владельца дословно требует «host matrix tests (`staff`,
`platform_admin`, `patient_default`, `patient_branded`, unknown)». После кандидата через choke point
резолвятся ровно две из пяти: `staff` (он же теперь и «пациентский» Host) и unknown/`patient_branded`
(через инъектируемый tenant-шов). `patient_default` и `platform_admin` не резолвится ни один тест — проверено
обходом: `patient_default` встречается только как аргумент чистой функции
(`surfaceRoutes.unit.test.ts:72-78`) и в `staffPwaManifest.unit.test.ts:16`; `platform_admin` через proxy —
нигде, только в `manifest.webmanifest/route.route.test.ts`, где `getResolvedSurface` замокан.
`AGENTS.md` §10: «тесты подгоняются под код, а не код под тесты».

**Чего это НЕ значит.** Продуктовое поведение верное — я его измерил живьём в §2 по обеим сторонам. Блокер
не в поведении, а в том, что после приземления это поведение не держит ничто, кроме моего разового прогона.
Правильную форму теста выбирать не аудитору; замечу лишь, что бриф `FB-2` требовал «тест обязан спрашивать
окружение, а не подменять поверхность присваиванием» применительно к НОВОМУ тесту дефолта — и он написан
верно (`envDatabaseRuntime.unit.test.ts`, инъекции `I-1`…`I-3` ниже его убивают). Распространение этого
требования на host-матрицу, где две конфигурации по определению нужно ЗАДАВАТЬ, — это уже не `FB-2`.

---

## 4. Неблокирующие находки

**`A-2`. Фикс `FB-3` применён к одному из двух симметричных манифестов.**
`app/manifest.webmanifest/route.ts:20-22` получил явный `404` для `platform_admin`, а соседний
`app/manifest-staff.webmanifest/route.ts` остался без всякой защиты: он зовёт `buildStaffPwaManifest()`,
который бросает `staff_manifest_requires_staff_surface` для `patient_default`/`patient_branded`
(`shared/lib/pwa/staffPwaManifest.ts:12-14`). Сегодня недостижимо: при выключённом гейте общий Host всегда
резолвится в `staff`, а `patient_branded` в production не строится вовсе (`NO_TENANT_SURFACE`,
`proxy.ts:33,41-44` — это `N-3` круга 1). Станет достижимо ровно тогда же, когда `R-2`: как только `B1`
включит резолв арендатора при всё ещё общем origin, брендированный Host на `/manifest-staff.webmanifest`
даст `500` вместо `404`. Тот же класс, что закрытый `R-3`, только с другой стороны.

**`A-3`. `R-2` не изменился и теперь живёт на штатной конфигурации.** Измерено на B (админский Host,
`PATIENT_APP_ORIGIN` отсутствует — то есть настройка по умолчанию после этой правки):
`/app/patient/login` `200`, `/book` `200`, `/app/doctor/login` `200`, `/` `200`.
`arePlatformSurfaceHostsDistinct()` — предикат конфигурации, не запроса, поэтому выключается глобально.
Бриф круга 3 прямо вывел `R-2` из скоупа, и я его не завожу; фиксирую только, что после этой правки
однохостовость стала дефолтом, значит и `R-2` — дефолтом, а не редким случаем.

**`A-4`. Имена в тесте обещают покрытие, которого нет.** Константа `PATIENT_ORIGIN`
(`proxy.route.test.ts:16`) теперь тождественна `STAFF_ORIGIN`, и три теста с ней в названии
(«keeps the patient portal on the patient Host», «does not redirect … public booking route»,
«stamps … once for /app») читаются как пациентская host-матрица, а проверяют staff-Host. Именно этим
механизмом `A-1` и прячется от следующего автора.

**Унаследованное, в скоуп не заводится:** `R-4` (пациентская страница на общем Host объявляет
staff-манифест) — закрыто решением владельца 22.08. `N-2` (гейта единственности резолвера нет) — вопрос
владельцу; я его не пересаживал, круг 2 уже показал, что lint его не видит. `N-6` круга 1 (абсолютные
ссылки переиспользуют резолв лишь частично) — без изменений.

---

## 5. `TPB-16`: границы целы

Обход всего дерева `apps/webapp/src` вне тестов:

- `resolveRequestSurface` определён один раз (`shared/lib/surface/requestSurface.ts:85`) и вызывается из
  production-кода один раз — `proxy.ts:53`;
- `request.headers.get('host')` встречается дважды, обе — в `proxy.ts`: строка 54 (резолвер) и строка 71
  (существовавший до `B3` CSRF-шов `decideCsrfOrigin`, план `B6` его сохраняет);
- место, где решается пациентский origin, одно: `config/env.ts:222` → `config/productSurfaces.ts:25`.
  Второго геттера нет; `parseWebappEnv()` — это тот же самый шов, экспортированный для теста, а не второй;
- потребители результата читают его через `readResolvedSurface`/`requireResolvedSurface`/`getResolvedSurface`
  (8 файлов) и Host заново не резолвят.

Второго резолвера, второго геттера поверхности и второго места решения пациентского origin не появилось.

---

## 6. Полный CI по ветке

Прогон только через общий замок хоста: `/home/dev/brain/host-orch/run-tests.sh "pnpm run ci"`.
Из worktree он действительно работает — маркер заморозки пишется в общий каталог git
(`scripts/ci-record.mjs:42`, `resolve(gitCommonDir(), 'bcb-feat-freeze')`, коммит `cee651692` в этой ветке;
`b81a82614` из брифа — тот же фикс на соседней ветке, предком `HEAD` он не является).

**Результат: зелёный, `rc=0`, 583 s.** `HEAD` до прогона `572dadfe5`, после прогона `572dadfe5` — не
двигался. `runs/ci-last.json` после прогона:

```json
{
  "sha": "572dadfe5d5558516eee65be7a829f5bfbe5163a",
  "headAfter": "572dadfe5d5558516eee65be7a829f5bfbe5163a",
  "movedDuringRun": false,
  "stepsExit": 0,
  "exitCode": 0
}
```

| шаг | итог |
| --- | --- |
| `lint` (корень + webapp, структурные гейты и их self-test'ы) | OK, 0 errors, 2 warnings (`react-hooks/exhaustive-deps`, `@next/next/no-img-element`) |
| `typecheck` (`tsc --noEmit` по всем пакетам + `tsconfig.type-tests`) | OK |
| `test` (integrator) | 109 файлов / 561 тест, 2 expected-fail, 1 skipped |
| `test:scripts` | pass 36 / fail 0 |
| `test:db-principal` | pass 31 / fail 0 |
| `test:db-privileges` | pass 162 / fail 0 |
| `test:webapp` | 431 файл / 2027 тестов, 4 файла и 12 тестов skipped |
| `test:media-worker` | 5 файлов / 16 тестов |
| `build` (integrator) + `build:webapp` | OK, «Compiled successfully in 52s» |
| `audit` (`check-saas-db-regression`, `registry-prod-audit`) | OK, уязвимостей нет |

**Известной нестабильности не было:** `PhoneMessengerAuthFlow.ui.test.tsx` в логе прогона не упоминается ни
разу, красных файлов нет вовсе. Отдельно перепроверять его нечего — он не краснел.

**Зелёный CI здесь ничего не опровергает, и это ровно суть `A-1`.** Те же 2027 тестов остаются зелёными,
когда гейт аудитории маршрутов выключен намертво (§7, `I-4`).

---

## 7. Фолт-инъекции

Каждая — временная правка, откатанная сразу после прогона; `git status --porcelain` проверялся после каждой
и был пуст. Базовая линия — **67 passed** в шести файлах: `proxy.route.test.ts`,
`app/manifest.webmanifest/route.route.test.ts`, `config/surfaceRoutes.unit.test.ts`,
`config/envDatabaseRuntime.unit.test.ts`, `shared/lib/pwa/staffPwaManifest.unit.test.ts`,
`modules/auth/sessionCookieHostOnly.unit.test.ts`.

| # | Инъекция | Результат |
| --- | --- | --- |
| `I-1` | Вернуть `.default('https://therapygo.ru')` и убрать fallback в transform (возврат `R-1`) — требование брифа | **поймана** — 4 red |
| `I-2` | Оставить `.optional()`, но подставлять литерал `'https://therapygo.ru'` вместо `APP_BASE_URL` — та же ложь, тише | **поймана** — 4 red |
| `I-3` | `PATIENT_APP_ORIGIN: config.APP_BASE_URL` — явно заданный origin молча проигрывает | **поймана** — 1 red |
| `I-4` | `arePlatformSurfaceHostsDistinct()` всегда `false` — гейт аудитории мёртв при любых Host | **НЕ поймана** — 67/67 scoped и **2027/2027 всего вебаппа зелёные** |
| `I-5` | `arePlatformSurfaceHostsDistinct()` всегда `true` — гейт заклинен включённым (возврат `F-1`) | **поймана** — 4 red |
| `I-6` | Снять ветку `platform_admin → 404` в маршруте манифеста (возврат `R-3`) | **поймана** — 1 red |

**Посажено 6, убито 5, не поймана 1.** Три инъекции в шов `config/env.ts` (`I-1`…`I-3`) — это то, что
требовал `FB-2`: две мои сверх обязательной. Все три убиты, шов дефолта закрыт честно.

Отдельно — не инъекция, а замер окружения: тот же набор с выставленным `PATIENT_APP_ORIGIN` даёт
**3 red из 67** (§3, нога вторая).

**Непойманного (реальных дефектов мимо проверок автора): 2** — `A-1` и `A-2`. Причина у обоих одна и та же,
что называли круги 1 и 2, только развернувшаяся в другую сторону: тесты никогда не задают ДВЕ конфигурации
одновременно. Раньше они видели только «два хоста», теперь только «один».

---

## 8. Итог трёх кругов

| | круг 1 | круг 2 | круг 3 (этот отчёт) |
| --- | --- | --- | --- |
| Вердикт | `FAIL` | `FAIL` | `FAIL` |
| Блокеров | 3 (`F-1`…`F-3`) | 1 (`R-1`) | 1 (`A-1`) |
| Закрыто | — | `F-2`, `F-3` | `R-1`, `R-3`, обе стороны гейта живьём |
| Инъекций | 8, убито 7 | 5, убито 4 | 6, убито 5 |
| Непойманного | 4 | 3 | 2 |

Блокеры кругов 1 и 2 закрыты, и продуктовая часть `B3` теперь ведёт себя правильно на обеих конфигурациях —
это измерено. Слайс не приземляется по одной причине: проверок, которые удержат это поведение после
приземления, не осталось.

---

## 9. Вопросы ведущему и владельцу (НЕ задачи)

Аудит — гейт против плана владельца, а не источник работы. Ни одна находка ниже не заводится в скоуп
аудитором.

1. **`A-1` — ведущему, инженерный вопрос.** Host-матрица `Gate B` обязана прогоняться в ОБЕИХ
   конфигурациях (общий origin и два различимых), иначе одна из них всегда без покрытия. Как именно —
   параметризацией origin внутри теста, вторым прогоном набора с выставленным `PATIENT_APP_ORIGIN` или
   иначе — решать не аудитору; в плане владельца такого пункта нет, есть только требование самой матрицы.
2. **`A-2`** — распространять ли решение `FB-3` на `/manifest-staff.webmanifest`. Сегодня недостижимо,
   станет достижимо вместе с `R-2` на `B1`.
3. **`R-2`/`A-3`** — прежний вопрос круга 2 без изменений: должен ли предохранитель быть пер-запросным.
   После этой правки однохостовость стала конфигурацией по умолчанию, так что вопрос подорожал.
4. **`N-2`** — без изменений с круга 1.

---

## НЕ СДЕЛАНО

- **Инъекцию `N-2` (второй Host→surface резолвер) не пересаживал** — бриф вывел её из скоупа как
  унаследованную и классифицированную «взглядом»; круг 2 уже показал, что весь `lint` её не видит.
- **`R-2`, `R-4` и `N-2` не проверялись на предмет исправления** — бриф прямо запретил заводить их в скоуп.
- **TEST не трогал.** Файл окружения TEST (`/opt/env/bersoncarebot/webapp.test`) не читал: он требует
  привилегированного доступа, а бриф запрещает трогать TEST. Утверждаю только то, что доказал: ни один
  артефакт репозитория `PATIENT_APP_ORIGIN` туда не пишет, значит после этой правки TEST попадёт в ветку
  «один хост». Это соответствует замыслу, но живой проверкой на TEST не подтверждено.
- **PROD, БД, deploy, `push` не трогал; порт `5200` не занимал.** Свои dev-серверы (`6320`, `6321`, `6322`)
  погашены за собой, порты освобождены.
- **Продуктовый код не менял.** Все правки — временные инъекции, откатанные до конца прогона; дерево между
  ними и после них чистое.
- **Живой UI-прогон в браузере не делался** — бриф просил HTTP-замеры, а не клик-through.
