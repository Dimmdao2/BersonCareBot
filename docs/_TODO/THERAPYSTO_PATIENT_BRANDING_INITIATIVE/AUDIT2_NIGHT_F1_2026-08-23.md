# Закрывающий аудит `F1`, круг 2 — «способы входа как свойство поверхности»

**PASS. Блокер круга 1 закрыт, вход не изменился ни у кого.**

- **Что проверялось:** `42b65cecc` «fix(auth): align surface policy with runtime settings» — фикс по блокеру
  `AUDIT_NIGHT_F1_2026-08-23.md` §1.2. Проверка велась на `HEAD` = `51869a25e` (в него влит `feat/doctor-ui-rebuild`;
  к `F1` из этого мержа не относится ничего).
- **Оракул:** `IMPLEMENTATION_PLAN.md`, пункт `F1`.
- **Клон:** `/home/dev/dev-projects/bcb-wt-night-f1-20260823`, ветка `wt/night-f1-20260823`.
- **Дата:** 23.08.2026.

Полный скоуп `F1` на ветке — 8 файлов: `ae8cd73f8` (6) + `42b65cecc` (2). Продуктового кода в нём один файл:
`apps/webapp/src/shared/lib/surface/requestSurface.ts`; плюс `app/manifest.webmanifest/route.ts` (одна строка,
разбор ниже). Остальное — тесты.

---

## 1. Все 18 ячеек сверены самостоятельно, из источника

Значения брались **не из теста автора**, а из тех же мест, откуда их берёт продукт.

**Источники живых значений**

| механика | где живёт истина | что там сегодня |
| --- | --- | --- |
| `password` | `modules/auth/passwordEligibility.ts` — ось **роли**, тумблера нет вообще: `role !== 'client'` (решение владельца 04.08.2026, `AUTH_AND_IDENTITY_CANON.md` §9) | staff/admin — да, пациент — нет |
| `email_code` | `auth_email_enabled` в `deploy/postgres/generated/prod-to-target/runtime-settings.sql:66` | `true` |
| `phone_bot` | `auth_sms_enabled:72` / `auth_telegram_enabled:110` / `auth_max_enabled:68` | `false` / `false` / `false` → механика выключена |
| `totp` | второй фактор, `app/api/auth/email-password/login/factor/route.ts` — вход только через `staffLoginContinuation`, тумблера нет | staff/admin — да, пациент — нет |
| `oauth` | `isOAuthProviderEnabled` (`authChannelPolicy.ts`) = тумблер AND configured; тумблеры `auth_oauth_yandex/google/vk/apple_enabled` (`:79, :106, :108, :95`) | все четыре `false` → выключен у всех, несмотря на `oauth_yandex_enabled = true` (configured) |
| `passkey` | `isIndependentAuthMethodEnabled('passkey')` → `auth_passkey_enabled:97` | **`true`** |

Дамп — снимок прод-базы. Проверено, что он не устарел: `git log` по файлу даёт три правки (`5dd2fde90`,
`9f4b98c25`, `f72b38853`, последняя 21.08), и `git show` каждой по маске `auth_(email|sms|telegram|max|passkey|
oauth|pin)` **пуст** — ни один auth-тумблер после cutover не двигали. Последняя запись самих тумблеров —
`updated_at = 2026-08-17 02:59`. То есть дата в комментарии кода описывает состояние верно.
`deploy/postgres/test-settings-override.sql` ни одного из этих ключей не переопределяет (там только три
`*_oauth_redirect_uri`) — на TEST значения те же.

**Сверка 18 ячеек `enabledMethods`** (3 строки × 6 механик):

| механика | живое | `staff` в коде | живое | `platform_admin` | живое | `patient` |
| --- | --- | --- | --- | --- | --- | --- |
| password | вкл | вкл ✅ | вкл | вкл ✅ | выкл | выкл ✅ |
| email_code | вкл | вкл ✅ | вкл | вкл ✅ | вкл | вкл ✅ |
| phone_bot | выкл | выкл ✅ | выкл | выкл ✅ | выкл | выкл ✅ |
| totp | вкл | вкл ✅ | вкл | вкл ✅ | выкл | выкл ✅ |
| oauth | выкл | выкл ✅ | выкл | выкл ✅ | выкл | выкл ✅ |
| passkey | **вкл** | **вкл ✅** | **вкл** | **вкл ✅** | **вкл** | **вкл ✅** |

**18 из 18 совпали.** Три расхождения круга 1 (phone_bot и oauth стояли включёнными, passkey — выключенным,
все три на всех трёх строках) устранены.

`availableMethods` фикс не трогал и трогать не требовалось: staff/admin — все шесть механик (passkey остаётся в
`available` при любом значении тумблера, механика не вырезана — §1.6 плана); patient — `email_code`, `phone_bot`,
`oauth`, `passkey` без `password` (ось роли) и без `totp` (у пациента нет второго фактора). Совпадает.

**Одна оговорка, не находка.** `email_code` эффективен как `enabled AND configured`
(`getClientVisibleAuthChannelPolicy`), где `configured` для email = наличие `smtp_outbound`. Это
credential-производная величина, в дампе настроек её нет; косвенное доказательство — почта на проде и TEST
реально ходит. На ячейку это не влияет: обе половины истинны.

## 2. Тест краснеет при расхождении — 21 инъекция в матрицу

Дерево перед инъекциями чистое; после каждой файл восстанавливался, финальная сверка с `BASE` — побайтовая.
Прогон — 4 затронутых тест-файла.

Покрыты **все 18 ячеек `enabledMethods` поимённо**, каждая — в ту сторону, в которую она может разойтись с
живым значением, плюс `availableMethods` всех трёх строк:

| строка | инъекции | результат |
| --- | --- | --- |
| `staff` | `+oauth`, `+phone_bot`, `-passkey`, `-password`, `-email_code`, `-totp`, `available -passkey` | 7/7 **поймано** |
| `platform_admin` | `+oauth`, `+phone_bot`, `-passkey`, `-password`, `-email_code`, `-totp`, `available -oauth` | 7/7 **поймано** |
| `patient` | `+phone_bot`, `+oauth`, `+password`, `+totp`, `-passkey`, `-email_code`, `available -phone_bot`, `available +password`, `available +totp` | 7/7 **поймано** (две из перечисленных — комбинации `enabled ⊄ available`) |

Ни одна подмена ячейки не прошла молча. Тест — `proxy.route.test.ts` → `surface auth policy` → «matches the
2026-08-17 live runtime-settings snapshot on all three surfaces»; сравнение идёт `toEqual` по целому объекту
политики, поэтому мимо него не проходит ни добавление, ни удаление, ни перестановка.

## 3. Утверждение в коде честно (§24.4 — «взгляд»)

Комментарий после фикса:

> Values snapshot live production settings from `deploy/postgres/generated/prod-to-target/runtime-settings.sql`
> on 2026-08-17: passkey is on; OAuth and phone/bot are off.

Проверено поимённо: путь существует; passkey `true`; все четыре OAuth-тумблера `false`; sms/telegram/max `false`;
дата соответствует `updated_at` этих строк и после неё тумблеры не менялись (§1). **Утверждение верное.**

Ложное утверждение круга 1 («These F1 defaults describe today's behavior … except globally-disabled passkey»)
из кода удалено, заголовок теста переименован с «carries the current behavior» на честный «matches the
2026-08-17 live runtime-settings snapshot». Формулировка коммита `ae8cd73f8` «defaults match previous behavior»
осталась в истории, но она теперь **правда** — значения приведены именно к прежнему поведению.

**Рекомендация (не находка).** Комментарий называет источник для трёх механик из шести. `password` и `totp`
в `runtime-settings.sql` не описаны вовсе — они держатся на роли (`passwordEligibility.ts`) и на
staff-continuation второго фактора. Дописать эти два источника одной строкой было бы полнее; достижимого
нарушения тут нет, продуктовый fix аудитор не делает (§24.6).

## 4. Ничей вход не изменился

**Потребителей `authPolicy` в продуктовом коде по-прежнему ноль.** Перепись на текущем `HEAD` (шире, чем в
круге 1 — по `authPolicy`, `authPolicyConfig`, `DEFAULT_SURFACE_AUTH_POLICY_CONFIG`, `enabledMethods`):

- `requestSurface.ts` — сам резолвер и его санитайзер;
- `app/manifest.webmanifest/route.ts:27` — **единственное появление вне резолвера**: на переходном общем Host
  роут собирает синтетический `ResolvedSurface` пациентской поверхности, и `authPolicy` там стоит только чтобы
  удовлетворить обязательное поле типа. `buildPatientPwaManifest` читает из него `surfaceDisplayName` и больше
  ничего — `authPolicy` в тело манифеста не попадает. Строка добавлена самим `ae8cd73f8`; в круге 1 её пропустил
  мой же grep по `\.authPolicy` (там нет ведущей точки) — на вывод «потребителей ноль» это не влияет, но
  поправка в источнике нужна.

Экран входа по-прежнему целиком строит `publicAuthSnapshot.ts` из глобальных тумблеров; резолвер он не
спрашивает. `proxy.ts` не передаёт `authPolicyConfig` — production работает на `DEFAULT_SURFACE_AUTH_POLICY_CONFIG`.
Заголовок `x-bc-resolved-surface` внутренний: входящий удаляется, ставится свой.

**`F2`–`F5` не заехали.** Диффом подтверждено: продуктовый код `F1` — один файл `requestSurface.ts` плюс
одна строка в манифест-роуте; `authChannelPolicy.ts`, `publicAuthSnapshot.ts`, `AuthFlowV2.tsx`, админские
настройки и OAuth start/callback не тронуты; ничего не вырезано; чек-боксы плана не двигались. `F1` остаётся
несущей конструкцией: типизированная матрица заведена, включение механики настройкой доказано инъекцией в шов,
но потребитель появится только на `F2`/`F4`.

## 5. Находка A круга 1 закрыта — два гейта получили самотест

Круг 1 оставил низкую находку: два защитных инварианта правки не имели теста «сломай специально» (`AGENTS.md`
§10a). Это поведение модуля, поэтому по §24.5 недостающие acceptance-тесты пишет аудитор — написаны один раз,
в существующий `describe('surface auth policy')`, без правки продуктового кода:

- `refuses a surface whose enabled methods escape its available methods` — политика с `enabled ⊄ available`
  обязана уронить резолвер в `null`, а не уехать в заголовок;
- `rejects a resolved-surface header whose auth policy is invalid` — заголовок с испорченной `authPolicy`
  (выход за `available`, незнакомая механика, дубль, не-объект) обязан отвергаться; в тесте есть положительный
  контроль, чтобы он не мог пройти «всегда null».

Fault injection по этим гейтам после добавления тестов:

| инъекция | до | после |
| --- | --- | --- |
| `K5` санитайзер перестал требовать `enabled ⊆ available` | не поймана | **поймана** (2 failed) |
| `K5b` санитайзер перестал отсеивать незнакомые механики | не проверялась | **поймана** |
| `K6` `readResolvedSurface` берёт `authPolicy` из заголовка без санитайза | не поймана | **поймана** |

## 6. Инъекции — итог числами

- **посажено 28** (21 в матрицу + 4 комбинации `enabled ⊄ available` + `K5`, `K5b`, `K6`);
- **убито 26**;
- **не поймано 2** — `K5` и `K6` на состоянии `42b65cecc`; после дописанных мной acceptance-тестов оба
  перепосажены и убиты. **Незакрытых инъекций не осталось.**

## 7. Проверки

| проверка | результат |
| --- | --- |
| 4 затронутых тест-файла (step-level, §10) | ✅ `Test Files 4 passed (4) · Tests 78 passed (78)` (было 76 + 2 моих) |
| scoped ESLint по изменённым файлам | ✅ `exit 0` |
| `pnpm --dir apps/webapp typecheck` | ✅ `tsc --noEmit exit 0` |
| дерево после всех инъекций | ✅ чистое, сверка с `BASE` побайтовая |

**Отдельно, чтобы не выглядело замолчанным.** Первый прогон случайно ушёл во весь webapp-набор (446 файлов) и
дал одно падение — `shared/ui/patient/auth/PhoneMessengerAuthFlow.ui.test.tsx`, «Получить код повторно». Файл
к `F1` отношения не имеет (в скоуп из 8 файлов не входит), в одиночном прогоне зелёный: `1 passed · 7 passed`.
Флейк под параллельной нагрузкой, не регрессия `F1`.

## 8. Открытые вопросы владельцу (работой не становятся, §24.6)

Все четыре — из круга 1, ни один фиксом не закрыт и закрыт быть не мог:

1. `enabledMethods` — статическая константа, а «включено» сегодня динамично (тумблер в БД). Сейчас это снимок
   живого состояния; при первом же переключении в админке он разойдётся. Матрица должна стать хранилищем
   (`F4`) или остаться снимком?
2. Нарушенный инвариант политики роняет **всю поверхность** в `404` (`policyFor` → `null` → `proxy.ts:64`), а не
   выключает одну механику. Сегодня недостижимо (значения — литералы под `as const satisfies`, ловится
   компилятором); после `F4`, когда значения поедут из админки, опечатка кладёт вход клиники целиком. Приемлемо?
3. `F5`/`OG-4` говорят «Яндекс у пациентов остаётся включённым как есть», но в проде
   `auth_oauth_yandex_enabled = false` с 17.08.2026 при заполненных кредах. Матрица зафиксировала живое
   значение (выкл) — это и есть «как есть», или Яндекс задумывался включённым?
4. Запрет пароля у пациента живёт по оси роли (`passwordEligibility`), а матрица вводит его же по оси
   поверхности. У доктора, зашедшего на пациентский Host, оси разойдутся — какая главнее? Развилка `F2`/`F4`.

## 9. НЕ СДЕЛАНО

- `F2`–`F5` не проверялись и не требовались — вне брифа;
- full CI не гонялся: скоуп круга 2 — значения в одном файле и два теста, признаков repo-уровня нет (§10);
- живая проверка на TEST не делалась: у `authPolicy` нет ни одного продуктового потребителя, менять в
  интерфейсе нечего;
- чек-бокс `F1` в `IMPLEMENTATION_PLAN.md` **не отмечен** — галочку ставит лид после собственной проверки
  полноты (§24.7), не аудитор;
- рекомендация из §3 (дописать в комментарий источник для `password` и `totp`) не применена — продуктовый fix
  аудитор не делает.
