# Медицинский и оздоровительный режим терминологии

Статус: очередь после текущего системного пакета Track D.

## Решение владельца 02.09.2026

Текущая настройка одного слова «клиент / пациент» должна стать единым режимом приложения:

- медицинский режим использует медицинскую терминологию;
- оздоровительный режим заменяет медицинские слова и фразы согласованными немедицинскими аналогами во всём пользовательском интерфейсе;
- переключение должно действовать системно, а не как набор независимых настроек отдельных полей;
- до реализации нужен полный инвентарь видимых терминов и предложение пар формулировок для обоих режимов.

Примеры направления, данные владельцем: «диагноз» → «результат диагностики» или «предположение»; «клинические тесты» → «функциональные тесты». Окончательные пары определяются после исследования контекста каждой фразы.

## Уточнение владельца 07.09.2026

- явный выбор «Клиенты / Пациенты» в настройках специалиста сохраняется и должен работать во всём интерфейсе;
- одна существующая группа `onSupport` получает выбираемое название «Избранные / На сопровождении»; второй
  механики, свойства или группы не создаётся;
- оба выбора подключаются через один общий terminology layer, без hardcode и page-level подстановок;
- это уточнение заменяет несовместимое предложение MWT-03 удалить `patient_label` в пользу одного общего режима.
  Решения MWT-04 по остальной медицинской терминологии остаются открытыми и не блокируют эти два выбора.

## Решение владельца 12.09.2026 — скоуп сузился до ДВУХ выбираемых слов

MWT-04 был листом из 26 спорных пар и девяти развилок. Владелец ответил не по пунктам, а сужением
скоупа. Дословно: «Сразу говорю, блять, что вообще нихуя не понимаю. Короче, мы меняем там клиент и
пациент. Просто у нас уже есть такая штука, да? Надо будет просто добавить выбор названия, типа,
сеанс, прием, тренировка, сессия… Вы записаны на сеанс, вы записаны на прием… Меняю-то клиент,
пациент и, как бы, сеанс, прием, все».

Что это значит по механике:

1. **Слово о человеке** — «клиент / пациент». Механизм уже есть (`patient_label`,
   `DoctorPatientTermsContext`), решение 07.09 в силе.
2. **Слово о событии записи** — НЕ пара «медицинский / оздоровительный», а такой же ВЫБОР названия
   организацией из набора: **приём · сеанс · тренировка · сессия**. Дословно: «Прием или сеанс
   тогда, окей, пока». Работает по тому же принципу и через тот же слой, что клиент/пациент, «и
   внутри кабинета специалиста, и внутри кабинета клиента».
3. **«Занятие» — не участвует в выборе.** Дословно: «Занятие, занятия, занятия, оно как бы как
   есть, так и есть. Занятие всегда». Слово остаётся закреплённым за самостоятельной практикой
   (отметка ЛФК), как и было; D7-рекомендация «Встреча» ОТКЛОНЕНА дословно: «Встреча, блядь, ну,
   встреча не уверен я. Встречу не надо».
4. **Медицинские шаблоны не трогаем.** Дословно: «Медицинские шаблоны менять точно не надо».
5. Остальные 24 пункта D-матрицы и развилки Q1-Q7, Q9 владелец не решал и в этот скоуп не входят.
   Их не реализуем и по рекомендациям агента не додумываем.

Как это делается — тоже решение владельца, дословно: «всё, что касается переименования, там, сеанс,
сессия, вот это вот всё точно делать не в основной ветке, параллельно отдать воркеру механическую
работу, пусть берёт принцип клиника… пусть берёт принцип просто, как бы, пациент, клиент и вот
делает всё, проход за проходом… просто делит весь интерфейс на блоки. Все сообщения на блоки, как
бы и по очереди их делает… В отдельной ветке просто». И: «И то не срочно».

### Блоки прохода (по одному за проход, каждый — свой коммит)

| # | Блок | Что входит |
| --- | --- | --- |
| T-A | Слой и хранение | второй per-org ключ рядом с `patient_label`: набор `приём·сеанс·тренировка·сессия`, дефолт `приём` (сегодняшнее поведение); дверь чтения и записи, экран настройки специалиста |
| T-B | Кабинет специалиста: запись и расписание | «Очный приём», «Формат приёма», «Новое время приёма», «Журнал прошедших приёмов», карточки записи |
| T-C | Кабинет специалиста: остальные экраны | списки, фильтры, заголовки, где слово о событии записи произносится |
| T-D | Кабинет клиента | «Вы записаны на …», история, ближайшая запись, домашний экран |
| T-E | Публичная запись `/book/**` | шаги формата и подтверждения, `patientBookingLabels.ts`, третий синоним «Онлайн консультация» |
| T-F | Сообщения: почта, push, боты TG/MAX | шаблоны уведомлений и напоминаний о записи |

Границы прохода: медицинские шаблоны справочников не трогаются; «занятие» не переименовывается ни
в одном блоке; кроме двух названных ключей никакие слова не переключаются.

**T-A — механизм и хранение построены, ветка `wt/terminology-appointment-word`.** Второй per-org ключ
`appointment_label` (scope=doctor, audience `authenticated_client`) живёт в том же `system_settings`
рядом с `patient_label`; резолвер один и тот же — `resolvePatientTerms`
(`apps/webapp/src/modules/system-settings/patientTerms.ts`), он отдаёт готовые падежные формы и род
слова, а не одну строку; контекстов тоже два прежних, а не новые:
`DoctorPatientTermsContext` и `PatientOrganizationContext`. Дверь записи — общая
`/api/admin/settings` (тот же атомарный батч рабочего пространства), дверь чтения кабинета клиента —
тот же seam `app.read_authenticated_runtime_setting`, расширенный миграцией
`20260912T012000_the_organization_names_the_appointment_word` вместе с платформенной строкой-дефолтом
«приём». Выбор стоит на экране настройки специалиста рядом с «Клиенты / Пациенты» тем же `Select`.
Доказано на DEV: `migrate-dev.sh --preflight` PASS и `--execute` применён; под настоящим принципалом
пациента seam отдаёт слово своей организации, классифицирует его `authenticated_client` и возвращает
ноль строк на чужую организацию; без строки клиники приходит платформенный «приём».
Живая приёмка в кабинете (переключить и перезагрузить страницу) выполняется после landing на общем
`:5200` по §1a AGENTS.md — до неё блок готовым не объявляется. Следующий проход — T-B.

**T-A прошёл независимый аудит и приземлён в `feat/doctor-ui-rebuild` (`e87db394d`, merge --no-ff).**
Вердикт PASS: [`runs/MEDICAL_WELLNESS_TERMINOLOGY_T_A_INDEPENDENT_AUDIT_2026-09-12.md`](runs/MEDICAL_WELLNESS_TERMINOLOGY_T_A_INDEPENDENT_AUDIT_2026-09-12.md),
run-id `audit-t-a-1094`. Слепой kill-set 31 отказа, поймано тестами 5, закрыто конструкцией 24,
открытыми остались две рекомендации. Первая (дефолт резолвера не закреплён ничем: подмена
«приём» → «сеанс» оставляла прогон зелёным) закрыта правкой ведущего `18fc6319d` по §24.1 —
одна строка теста с оракулом «решение владельца», целевая инъекция её красит.

**T-B — экраны записи и расписания кабинета специалиста переведены, коммит `77244cc7f`.** Слово о
событии записи берётся из `DoctorPatientTermsContext` (клиентские экраны) и из
`resolvePatientTerms(shell.patientLabel, shell.supportGroupLabel, shell.appointmentLabel)` с явным
третьим аргументом (серверные страницы `visits/new`, `visits/[visitId]`). 21 файл: календарь и
панель записи, индикаторы, диалог переноса, экраны «Сегодня», запуск приёма из карточки клиента,
страница и модалки визита, подпись переменной шаблона расписания.

Род согласуется ДАННЫМИ резолвера, а не склейкой в компоненте: добавлены `agreeWithAppointment`
(выбор между двумя написанными формами определения по `appointmentGender`),
`appointmentDeliveryFormatLabels` (одна пара «Очный приём»/«Онлайн-приём» вместо четырёх копий) и
`capitalizeAppointmentForm`. Падежи по-прежнему приходят готовыми из `APPOINTMENT_TERMS_BY_LABEL`.

Замер по резолверу (`tsx`-прогон помощников на всех четырёх значениях): «тренировка» даёт «Очная
тренировка», «Начать тренировку», «Сейчас на тренировке», «Следующая тренировка», «История
тренировок», «Тренировок пока нет», «Тренировка сохранена», «Будет создана новая тренировка без
записи»; «сессия» — женский род так же; «сеанс» — мужской. При дефолте все надписи совпадают с
прежними литералами дословно, **кроме двух**: `DoctorTodayNextAppointment` раньше писал «Сейчас на
приеме» и «Следующий прием» без «ё» — теперь «приёме»/«приём», как во всём остальном кабинете.

`tsc --noEmit` зелёный, eslint по изменённым файлам чистый, `clinicOwnerSettingsBatch.unit.test.ts`
6/6. **Живая приёмка на общем `:5200` не выполнена и остаётся обязательной** — она идёт после
landing (AGENTS.md §1a и `LOCAL_DEV_AND_AGENT_TESTING.md` §3b: второй Next server и доп. порт
запрещены, worker берёт `:5200` только в окно live-приёмки после landing).

За границей прохода намеренно оставлены (T-C): «записаться на приём» в `SubscriberBlockPanel`,
подсказка фильтра «Были на приёме» в `PatientsPageClient`, заголовок «Приём» в
`DoctorAnalyticsAppointmentsSection`. Не трогается «Приём платежей»/«Приём оплаты» — это другое
значение слова.

**Замер остатка 12.09 после T-B — кто из оставшихся блоков стоит отдельного воркера, а кто нет.**
Считано по репозиторию, не по памяти (`grep` по «приём/прием» с вычетом «приём платежей» и
комментариев):

| Блок | Мест | Кому |
| --- | ---: | --- |
| T-C кабинет специалиста, остальные экраны | 3 | ведущему самому, §24.1 — это подсказка фильтра «Были на приёме», заголовок раздела аналитики и строка `SubscriberBlockPanel`; ставить воркера, передавать контекст и собирать отдельную приёмку ради трёх строк дороже самой правки |
| T-D кабинет клиента | 18 | воркер |
| T-E публичная запись `/book/**` | 2 | складывается в проход T-D: те же формы, тот же контекст, отдельная постановка не окупается |
| T-F сообщения (`modules/patient-booking`) | 15 | воркер |

**Закрывающий гейт прохода (вторая рекомендация аудита, R2).** Третий аргумент
`resolvePatientTerms` сегодня необязателен, и колл-сайты зовут резолвер без значения
`appointment_label`: клиника выберет «тренировка», а такой экран останется на «приёме», и не
покраснеет ничего. Делать аргумент обязательным СЕЙЧАС нельзя — компиляция сломается в файлах
блоков, до которых проход ещё не дошёл. Поэтому:

- [ ] **T-G (закрывающий, после T-F).** Сделать третий аргумент `resolvePatientTerms` обязательным
      и убедиться, что `tsc` зелёный без единого нового `?? 'приём'` по месту вызова: обход
      перестаёт компилироваться, а не остаётся на совести следующего прохода.

      Замер аудитора T-B по дереву скобок: таких колл-сайтов **18** (17 продуктовых + 1 в тесте),
      а не 13, как было записано раньше. Цена обхода измерена инъекцией того же аудита: провайдер,
      лишённый `appointmentLabel`, оставляет ВЕСЬ кабинет специалиста на «приёме» при зелёных
      `tsc`, eslint и тестах. Тестом это не закрывается — только обязательным аргументом.

**T-C закрыт правкой ведущего (§24.1), воркера на него не ставили.** Замер дал три строки, и
постановка отдельного прохода стоила бы дороже самой правки. Переведены: подсказка сегмента
«Недавние» в `PatientsPageClient` (форма берётся из терминологии на месте отрисовки — `SEGMENTS`
модульная константа, контекста там нет, литерал остался запасным значением типа), строка блокировки
учётной записи в `SubscriberBlockPanel` и заголовок раздела записи в
`DoctorAnalyticsAppointmentsSection` (хук, как у соседнего `ClientContactPieChart` на той же
странице). При дефолте все три строки побайтно прежние — проверено прогоном резолвера:
«Были на приёме в текущем месяце.», «записаться на приём.», «Приём». `tsc` и eslint по трём файлам
чистые.

**Пять находок аудита T-B — куда они отнесены (§24.6: находка не становится работой сама).**

1. Три из четырёх примеров в строке блока T-B («Формат приёма», «Новое время приёма», «Журнал
   прошедших приёмов») физически лежат в `app/app/patient/**`, то есть это **T-D**, а не T-B.
   Аналоги на стороне специалиста закрыты. Примеры в таблице блоков остаются как были — они
   описывают НАДПИСИ, а не файлы; переносить их в строку T-D незачем, достаточно этой отметки.
2. Число колл-сайтов исправлено выше: 18, не 13.
3. `DoctorCurrentAppointmentCard.tsx` и `tabs/karta/EncounterSummary.tsx` не импортируются ниоткуда
   и на экран не попадают — мёртвыми они были и до этого прохода. Файлы не удаляются (решение
   владельца 12.09: «не трогаем файлы»); отмечено здесь, чтобы их не приняли за живой экран.
4. Подпись «— файлы, прикреплённые к визиту» остаётся рядом с заголовком «Тренировка»: «визит» —
   третье слово, а владелец 12.09 сузил скоуп ровно до двух. Меняется только по его слову.
5. **Для живой приёмки:** «Сейчас на приёме» и «Следующий приём» смотреть в
   `DoctorTodayNextAppointment`, а не в мёртвом двойнике из п. 3.

**T-D — кабинет клиента переведён; T-E — публичная запись НЕ переводится, дверь не пускает.**
20 файлов (19 изменённых + один новый). Слово берётся из ЕДИНСТВЕННОГО контекста кабинета
`PatientOrganizationContext` там, где компонент клиентский, и приходит пропом от того, кто уже
прочитал настройку, там, где он серверный. Второго резолвера, контекста и хранилища не заведено.

Клиентские экраны (хук `usePatientTerms`): шаги формата и подтверждения мастера записи
(`booking/FormatStepClient`, `booking/confirm/ConfirmStepClient`), предстоящие записи и история
посещений (`booking/BookingUpcomingSection`, `booking/BookingPastHistorySection`), карточки записи
кабинета (`cabinet/CabinetActiveBookings`, `cabinet/CabinetPastBookings`,
`cabinet/CabinetUpcomingAppointments`, `cabinet/BookingFormatGrid`), категория напоминаний
(`reminders/ReminderRulesClient`) и кнопка записи в программе
(`treatment/program-detail/PatientProgramControlCard`). Общий `cabinet/patientBookingLabels.ts`
получил ОБЯЗАТЕЛЬНЫЙ второй аргумент `terms` — необязательный оставил бы карточку на «приёме» при
зелёном `tsc`, то есть ровно тот обход, который закрывает T-G.

Серверные экраны — слово приходит туда, где организация уже прочитана, без новой двери:
экран техработ и главная (`layout.tsx` → `PatientMaintenanceScreen`, `home/PatientHomeToday` →
`home/PatientHomeBookingCard`) получают его пропом, заголовок шага переноса — из проекции
`bookingCatalogRsc`, которую страница слота уже читает. Для трёх серверных мест без своего
загрузчика (`help/page.tsx`, `bind-phone/page.tsx`, `shared/ui/patient/guestAccess.tsx`) заведён
один клиентский лист `shared/ui/patient/organization/PatientAppointmentWord.tsx`: он берёт готовую
форму из того же контекста, падеж — его параметр. Новых запросов к базе он не делает.

**Найдено и исправлено по дороге:** на главной и в загрузчике мастера первым вариантом стояло
чтение через `deps.systemSettings.getSetting`. Под пациентским принципалом этот путь уходит во
ВТОРУЮ пациентскую дверь настроек `app.read_current_patient_ui_setting`, allowlist которой знает
`patient_label`, но НЕ знает `appointment_label`: слово не пришло бы вовсе, а на раскрытом пути —
упёрлось бы в отсутствующий грант (SQLSTATE 42501). Оба места переведены на ту дверь, которую
открыл T-A, — `runtimeConfig.getAuthenticatedString` → `app.read_authenticated_runtime_setting`.
Заодно чтение `patient_label` на главной перестало быть вторым путём к тому же значению.

**T-E не сделан, и это не пропуск, а закрытая дверь.** Публичная запись анонимна: `/book/**` и
`/{clinic}/booking` работают под принципалом организации (`withExplicitOrganizationPrincipal`), а
`appointment_label` объявлен `authenticated_client` (`registry.ts`) и читается seam-функцией
`app.read_authenticated_runtime_setting`, которая требует аттестованную роль `app_patient` или
`app_staff`. Публичное чтение настроек (`app.read_public_runtime_setting`) организацию вообще не
принимает — оно глобальное по построению. Проекция, которую публичная страница уже читает
(`loadBookingEntryScreenRsc`), настройки не содержит. Значит слово там взять НЕОТКУДА без новой
двери или смены audience ключа, а это решение владельца, не механическая правка. Поэтому
`app/book/PublicFormatStepClient.tsx` (2 надписи) и `app/[clinicSlug]/booking/BookingEntryClient.tsx`
(«Онлайн-приём», живой первый экран публичной записи) остались на «приёме».

**Вопрос владельцу (не работа):** должно ли слово организации звучать на публичной записи? «Да»
означает отдельный проход: либо публичный per-org seam для этого ключа, либо перевод
`appointment_label` в audience, доступную анонимной воронке. «Нет» закрывает T-E как есть.

**За границей прохода намеренно:** «запись», «визит», «посещение» и «занятие» не переименовываются
(владелец сузил скоуп до двух слов) — поэтому «История посещений», «Активные записи», «Ближайшие
записи» и статусы «Отменена/Завершена/Перенесена» (они согласуются с «записью», а не с событием)
остались как были. Строка «Вы записаны на приём» физически живёт в
`modules/patient-booking/patientMessageText.ts` и `modules/web-push/pushNotificationCopy.ts` — это
T-F, в этот проход не входит. «Онлайн консультация» в `patientBookingLabels.ts` — имя категории из
каталога, а не слово о событии записи.

При дефолте «приём» ВСЕ тронутые надписи побайтно прежние — сверено построчно прогоном резолвера
на всех четырёх значениях (`npx tsx`), расхождений нет ни одного. `tsc --noEmit` зелёный, eslint по
изменённым файлам чистый, целевые тесты `src/app/app/patient`, `src/app/book`,
`src/app/[clinicSlug]`, `src/modules/system-settings`, `src/modules/patient-booking` — 40/41 файлов
зелёные. Единственный красный — `src/app/app/patient/layout.branding.test.ts`
(`port.resolveMechanicAccess is not a function`): падает ТАК ЖЕ на HEAD без этих правок, к проходу
отношения не имеет. **Живая приёмка на общем `:5200` не выполнена и остаётся обязательной** — она
идёт после landing (AGENTS.md §1a).

## Этапы

- [x] **MWT-01 — Инвентарь.** Найти все видимые пользователю медицинские термины и фразы во всех поверхностях приложения, включая тексты из БД/CMS, письма, уведомления, ботов, экспорт и печатные материалы; отделить доменные идентификаторы и юридически обязательные формулировки, которые нельзя механически переименовывать. — [`MEDICAL_WELLNESS_TERMINOLOGY_INVENTORY_2026-09-02.md`](MEDICAL_WELLNESS_TERMINOLOGY_INVENTORY_2026-09-02.md) §1-§3: 13 поверхностей, DB/CMS и четыре исполняемых кодовых baseline/fallback-источника; два audit-pass добавили doctor «Осмотр»/«Травмы и операции», `/book/**`, legal, TG/MAX, категории файлов, platform reminder «бады и лекарства» и реальную многоточечную синхронизацию справочников. Три класса разведены по фактическому владельцу строки. Lead correction N1-N2 принят по тому же kill-set.
- [x] **MWT-02 — Матрица терминов.** Для каждого найденного контекста предложить согласованную пару «медицинский режим / оздоровительный режим», без потери смысла и без механической замены частей слов. — там же §4: 9 однозначных пар + 26 спорных понятий с 2-3 вариантами каждое (D1-D26); D26 покрывает живой multi-channel reminder, Q1 учитывает SQL и кодовые fallback seed; D/Q-census `26/26`.
- [x] **MWT-03 — Единый механизм.** Спроектировать один типизированный слой терминологии для всех поверхностей, включая поведение по умолчанию и границы арендатора. Архитектурный слой, wiring и инвентарь §5-§6 сохраняются; по более позднему решению 07.09 он принимает явные per-org choices `patient_label` и `support_group_label`, а не удаляет `patient_label`.
- [x] **MWT-04 — Решение владельца.** Закрыт 12.09 сужением скоупа, а не построчными ответами — см. раздел «Решение владельца 12.09.2026» выше: переключаются ровно два слова (человек и событие записи), остальные 24 пункта D-матрицы и развилки Q1-Q7/Q9 владелец не решал и в работу не входят. Исходный лист вопросов: Представить владельцу матрицу спорных терминов и варианты только там, где продукты имеют разный смысл; записать выбранные формулировки до реализации. — вопросы сведены в один лист: [`MEDICAL_WELLNESS_TERMINOLOGY_INVENTORY_2026-09-02.md`](MEDICAL_WELLNESS_TERMINOLOGY_INVENTORY_2026-09-02.md) §7 (Q1 baseline справочников · Q2 глобальный админ · Q3 лендинг · Q4 дефолт новой клиники · Q5 арендаторство уведомлений · Q6 шаблоны ботов · Q7 фискальный чек · Q8 подтверждение **каждого** из D1-D26 поимённо · Q9 тенантлес-вход `/app`). По каждому дана рекомендация; ответов владельца пока нет.
- [ ] **MWT-05 — Реализация.** Идёт В ОТДЕЛЬНОЙ ВЕТКЕ воркером, блок за блоком (таблица T-A…T-F выше), не в основной. Прежняя редакция пункта: C3M реализует системные `Клиенты / Пациенты` и `Избранные / На сопровождении` без ожидания MWT-04; перевод остальных медицинских терминов начинается после решений MWT-04. Параллельных resolver и hardcoded обходов не оставлять.
- [ ] **MWT-06 — Приёмка.** Проверить обе конфигурации на настоящих ролях и каналах; смена режима должна менять весь согласованный словарь и не менять медицинские данные, права или бизнес-поведение.

## Граница текущего запуска

Первый агент выполняет только MWT-01–MWT-03 и готовит MWT-04. Код продукта до решения владельца по матрице не меняется.

02.09.2026: первый проход по MWT-01–MWT-03 получил вердикт FAIL от независимого аудита
([`runs/MEDICAL_WELLNESS_TERMINOLOGY_MWT01_04_INDEPENDENT_AUDIT_2026-09-02.md`](runs/MEDICAL_WELLNESS_TERMINOLOGY_MWT01_04_INDEPENDENT_AUDIT_2026-09-02.md),
`dc0491aec`) — пропуски инвентаря, решения агента выданы за owner-факты, механизм не собирался для
интегратора. Тем же днём correction-pass закрыл весь kill-set F1-F4 в
[`MEDICAL_WELLNESS_TERMINOLOGY_INVENTORY_2026-09-02.md`](MEDICAL_WELLNESS_TERMINOLOGY_INVENTORY_2026-09-02.md).
Продуктовый код, миграции, БД, env, UI и тесты не менялись ни разу. MWT-05 и MWT-06 не начинались.
Повторный аудит `6c81a7596` подтвердил F2-F4 и нашёл два новых F1-пропуска: живой platform reminder
«бады и лекарства» и четыре кодовых baseline/fallback-источника. Лид исправил их тем же bounded
kill-set без третьего blind-аудита по §24.5–§24.6; correction принят, поэтому MWT-01–03 закрыты.
Следующий вход — ответы владельца по §7 (Q1-Q9, D1-D26).
