# Хендофф оркестрации — 20.08.2026, вечер

Кто писал: ведущий Opus, сессия перехвата работы кодекса. Состояние на момент передачи:
`feat/doctor-ui-rebuild` = **`803cfed74`**, `origin/feat/doctor-ui-rebuild` = **`16601d8a3`** (origin отстал,
пуш ещё не делался — он идёт по порядку ПОСЛЕ полного CI).

## Порядок владельца (20.08), переставлять нельзя

1. Свести ВСЕ ветки в feat. Каждая — через порт `land`, с независимым аудитом и строкой вердикта в очереди.
   Мимо порта руками не сливать.
2. Миграции: все применены на DEV, леджер без сирот, гейт имени зелёный и ОРЁТ при плохом имени.
   Поднять dev-сервер, убедиться, что ошибок нет.
3. A→B с прода на DEV. Свежий дамп с 135.х ТОЛЬКО штатным скриптом.
4. Раскатка заново: дамп → `p0-data-fix-doctor-admin-split` → бэкфилл ФИО → миграция. БЕЗ обезличивания.
   Строка `4ff57819` → `exceptions.preserveCurrent`, остальные 169 строк байт-в-байт.
5. Проверка на TEST: всё поднялось, права наложились, с обнулением.
6. Старые миграции в корзину — ТОЛЬКО после снятой и проверенной A→B.

Уточнённая владельцем последовательность хвоста (20.08, дословно): «**потом full ci, пуш и потом уже код
деплой на test**» и «**не забудь применить миграции на DEV если они есть и проверить что они проходят**».

## Что закрыто

Шесть пунктов порядка были закрыты и проверены ранее в этой же сессии (сведение, миграции, A→B, раскатка ФИО
`total 170 / eligibleUpdates 161 / preservedCurrent 5 / drift 0 / missing 0`, TEST 5/5 служб, чистка старых
миграций). Телеграм починен (`1de419c8a`, аудит sol/high PASS 7/7), приземлён и **запушен** — это последнее,
что уходило на origin.

### Сведено этой рукой, всё через порт, 0 ручных слияний

| ветка | feat после |
|---|---|
| `wt/d10-transport-stage1-20260820` | `7ae0d8a4b` |
| `origin/wt/port-context-latency-audit-20260816` | `33bcebb34` |
| `wt/d5-deadcode-c4-20260820` | `40955951b` |
| `wt/doctor-patient-header-tabs2-20260820` | `7da8379b8` |

## Что осталось сделать — по порядку

### A. Досвести ветки

- **`wt/global-admin-video-access-20260820`** — аудит `gav-audit-20260820` (sol/high) ИДЁТ. Ветка несёт две
  работы: лимиты длительности видео GA-L-01/02 и #1069 глобальный платный доступ с миграцией
  `apps/webapp/db/drizzle-migrations/20260820T175432_paid_period_global_access_authority.sql` (164 строки).
  Аудит проверяет три обхода гейта, единственность chokepoint, миграцию и что права никому не расширены.
- **`wt/video-duration-20260820`** — **НЕ приземлять как самостоятельную**: она делает ровно ту же работу
  GA-L, что и ветка выше (12 общих файлов). Записать как вытесненную. Если аудит покажет, что реализация
  GA-L в `global-admin-video-access` слабее — тогда наоборот, брать эту, а из той выкидывать media-часть.
  Здесь уже прошёл цикл: аудит sol нашёл 3 обхода (MUST FIX), фиксер terra их закрыл (`31a816030`, 20/20
  тестов зелёные).
- **`wt/ui-gate-exhaustive-20260816`** — вердикт по остатку записан (PASS, 2 файла, +7/−6). НО при слиянии
  с текущим feat даёт **конфликт в 5 файлах** `runs/dev-interactive-audit/`: ветку архивировали 18.08, feat
  по этим файлам с тех пор **0 коммитов**, а размеры разошлись (`run.mjs` 1468 против 1485, `scenarios.mjs`
  305 против 404, `gate-utils.mjs` 168 против 281). То есть ветка НЕ вытеснена — она содержит больше, и
  конфликт нужно разрешать содержательно. Отдать воркеру (terra) на пересадку и разрешение, потом аудит sol.
- **`wt/d20-level4-remainder-20260820`** — уже в feat (`4bbbda02f`), приземлилась мимо аудита. См. ниже.

### B. Убрать из feat неверный грант (следствие FAIL-аудита)

`deploy/postgres/privileges/declaration.ts:6694` — две строки, дающие `app_patient` колоночный INSERT на
`integrator.direct_public_write_retries`. Независимый аудит `d20-remainder-audit-20260820` (sol/high):
**FAIL, убито 5, не поймано 0**. Право не работает вовсе — временный GRANT под `app_patient` на живой
`bcb_webapp_dev` дал **SQLSTATE 42501**, FORCE RLS отверг INSERT, потому что политика `rev10_direct_business_10`
пускает только `app_integrator_request` и `app_operational_delivery_worker`. Настоящий enqueue и так идёт под
`app_integrator_request`. Если кто-то «дочинит» политику — `app_patient` сможет указать чужой
`organization_id` (FK 0, триггеров 0, policy-связей 0). **Две строки удалить.**
Отчёт: `docs/REPORTS/AUDIT_4BBBDA02F_D20_PRIVILEGE_PRINCIPAL_2026-08-20.md`.

### C. Разблокировать полный CI (в работе)

Замер на чистом feat: `pnpm --dir apps/webapp exec eslint src --max-warnings=0` → **EXIT=1, 2 problems
(0 errors, 2 warnings)**, оба в `apps/webapp/src/app/app/doctor/calendar/AppointmentPaymentSection.tsx`
из чужого коммита `542c1eb35`. Во всём остальном `apps/webapp/src` чисто. Воркер `lintwarn-20260820`
(terra/medium) снимает: `useCallback` для `load` с сохранением счётчика гонки `requestVersion` и точечный
`eslint-disable-next-line` для локального QR-data-URI (там `next/image` не к месту по смыслу коммита).
**Пока это красное — полный CI зелёным быть не может.**

### D. Дальше по порядку владельца

1. Миграции на DEV: применить, если есть новые, и проверить, что проходят.
2. Полный CI — ОДИН раз, после последней влитой ветки. Перед запуском обязательно `pgrep -af agent-run.mjs`.
   Механика: `pnpm run ci` = `scripts/ci-record.mjs`, он ставит маркер `.git/bcb-feat-freeze`, и хук
   `tools/git-hooks/reference-transaction` на время прогона отказывает В ЛЮБОМ обновлении feat, включая land.
   Итог ложится в `runs/ci-last.json`; `movedDuringRun: true` = прогон недействителен.
3. Пуш.
4. Code-only деплой на TEST: `bash deploy/host/deploy-test.sh feat/doctor-ui-rebuild`.

## Живые агенты на момент передачи

- `gav-audit-20260820` — мой, sol/high, аудит `global-admin-video-access`.
- `lintwarn-20260820` — мой, terra/medium, снятие lint-блокера.
- `d10a-journal-worker2-20260820` — **ЧУЖОЙ**, не трогать.

## Запреты, действующие прямо сейчас

- **`wt/d10-transport-stage2-20260820`** — владелец: не трогать вообще, ни land, ни чтение. Вливает сам.
- **`wt/d10a-journal-nonqueue-20260820`** — в ней работает воркер; результат владелец примет и вольёт сам.
- **`wt/d10a-scope2-20260820`** — отклонена навсегда, ветка уже удалена из дерева. Ломает релиз-гейт:
  подменяет источник сверки, после неё гейт считал бы ~77% расхождения вместо реальных 1.2%
  (старый источник 6214 id / 6137 совпадений, новый 2305 / 531). Вердикт в очереди.

## Открытое, что ждёт владельца

- **Безопасность отправки с TEST.** Перенаправление доставки выключено: в `/opt/env/bersoncarebot/api.test`
  все 12 `DEV_REDIRECT_*` пусты при `NODE_ENV=production`, поэтому
  `apps/integrator/src/shared/devDeliveryRedirect.ts:71-72` возвращает false. Включённых правил напоминаний
  45, MAX/SMS/почта настроены. Замер после рестарта служб 19:53: отправок третьим лицам **0**, была одна —
  мой собственный OTP на мою почту в 19:58. Владелец о развилке знает, решение не давал.
- **Данные TEST после раскатки.** `be_organizations` = 1, `be_specialists` = 1, `be_organization_members` = 1
  при 308 `platform_users`; `app_ext.variant_a_identity_refs` = 2 строки, и **ни одна не указывает на этого
  единственного участника** — джойн даёт 0. Поэтому пред-проверка выкатки честно отказывает: «нет ни одного
  действующего сотрудника — доказывать нечего». Многоарендных фикстур (три клиники) на стенде больше нет,
  их снёс сброс. Не чинил — это приёмка владельца.
- **D4 не доказан.** `writeReminderRulesDirect.rls.integration.test.ts` пропускается: skip-gate смотрит 4
  переменные, а тест требует пятую (`DB_PRINCIPAL_CONTEXT_MODE=locked`), тогда как DEV работает в
  `port-context`. Обычный CI-suite его не гоняет.
- **Побочная мертвечина.** `buildIntegratorEventsHttpBody` в
  `apps/integrator/src/infra/adapters/jsonStableStringify.ts` остался без вызывающих — наследство
  `7ae0d8a4b` (D10 stage 1), не чинилось намеренно.

## Уроки этой смены, которые стоят дороже кода

1. **Аудит «взглядом» там, где нужен тест, врёт.** По `doctor-patient-header-tabs2` аудит дал 14/14 PASS,
   а проаудированный код звал таблицу `clinical_visits`, которой не существует (живая база знает только
   `public.clinical_visit`). Поймано перепроверкой ведущего с инъекцией: возврат старого имени даёт EXIT=1.
2. **Аудитор обязан быть НЕ СЛАБЕЕ автора.** «Не та же модель» ≠ «любая другая». Код пишет terra, проверяет
   sol. Если задачу отдать sol — корректного аудитора не остаётся вовсе.
3. **За аудитором нужно убирать.** Аудитор `video-duration` оставил в клоне ДВЕ неоткаченные инъекции
   (снятую проверку `media.kind !== 'video'` и снятый гейт из трёх файлов вместе с удалённым модулем).
   Порт отказал на «незакоммиченном»; откатывал ведущий руками. Проверять `git status --short` после
   каждого аудитора.
4. **PASS протухает при движении feat.** У `d5-deadcode-c4` старый PASS был снят с прежнего feat; после
   приземления `d10-transport-stage1` он перестал действовать, и слияние дало конфликт встречных удалений.
   Правило: PASS действителен только против того feat, на котором мерился.
