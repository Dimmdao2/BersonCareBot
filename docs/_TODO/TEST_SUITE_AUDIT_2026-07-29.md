# Ревизия тестового набора — решение владельца 29.07

> **⛔ ПЕРЕД СТАРТОМ ЭТАПА — перечитать, не по памяти:** `AGENTS.md` (§24 оркестрация, §7-9 коммит/CI/пуш feat),
> `docs/ORCHESTRATION_BINDINGS.md`, `docs/ORCHESTRATOR_CHECKLIST.md`, релевантные `.cursor/rules/*.mdc`.
> Агентов запускать только через `tools/orch-launch.sh`. **НЕ ИЗОБРЕТАТЬ:** сначала искать готовое.

## ЗАЧЕМ ЭТО ВООБЩЕ — прочитай, прежде чем что-то удалять

Проблема не в том, что тестов «много». Проблема в том, что **они врут в обе стороны**, и каждая сторона стоит
нам реальных денег и времени.

**Врут зелёным.** 28.07 мы построили первую тарифную квоту, и все тесты вокруг неё были зелёные. А в живой базе
у функции пересчёта не было права на чтение таблицы — то есть при включённой квоте падало бы создание ЛЮБОЙ
страницы, с невнятной ошибкой вместо честного «лимит исчерпан». Тесты этого не видели, потому что работали на
подставленных числах. Нашлось это не тестами, а запросом к базе. В тот же день так же выяснилось, что расход
не доходил до витрины и что обычный сотрудник дотягивался до таблиц биллинга. **Зелёный прогон означал ровно
ничего.** Вот это и есть главная цена: владелец принимает решение о выкате, глядя на зелёное, а зелёное не
связано с работоспособностью.

**Врут красным.** 28.07 полный прогон трижды падал не потому, что что-то сломалось, а потому что кто-то
поменял кавычки, перенёс аргументы на другую строку или переименовал функцию. Каждый разбор — от получаса до
часа: надо доказать, что паника ложная. Хуже другое: когда красное в половине случаев ничего не значит, к
красному перестают относиться серьёзно, и правка теста «чтобы позеленело» становится нормальной операцией.
Так набор тестов деградирует целиком, включая ту часть, что ловит настоящее.

**Что мы платим за это каждый день.** Полный прогон — восемь минут перед каждым слиянием и каждым выкатом.
258 тысяч строк тестов надо сопровождать: любой рефакторинг ломает те из них, что привязаны к внутренностям, и
их чинят руками. Плюс сами тесты пишут агенты, для которых объём выглядит доказательством работы, — так
появляются файлы на 255 утверждений о том, какие имена встречаются в исходнике.

**Как выглядит успех.** Не «тестов стало меньше». Успех — это когда **красный означает поломку, а зелёный
означает, что можно выкатывать**. Тест имеет право на жизнь, если можно назвать конкретную ошибку, которую он
поймает, и эта ошибка правдоподобна. Всё остальное — не защита, а налог.

**Чего делать НЕЛЬЗЯ.** Удалять тест потому, что он мешает, падает или непонятен. Единственное основание для
удаления — «названная поломка отсутствует или неправдоподобна». Если сомневаешься — не удаляй, вынеси в отчёт.
Потеря настоящей проверки хуже, чем сохранение лишней.

## Решение владельца, дословно

- «тесты ради тестов - явно нет»;
- «мой вывод - удалять все эти тесты прямо сейчас» (о тестах, пиннящих текст исходников);
- «пройти фильтром «назови ошибку»» → **«отлично. запускай»**;
- о карте: «нужна карта - разбить файлы с тестами на группы либо по смыслу либо по модулям, потом по каждому
  составить файл с тестами, потом решить какие там вообще полезны. Работы много, но надо делать».

Владелец также сформулировал, каким тест должен быть: «дали на вход это — получили это; что внутри функции
тесту должно быть насрать», плюс отдельные классы — поведение (нажали → получили), безопасность (неверный
пароль → отказ, повтор → задержка) и доступ к данным в БД.

## Измеренная картина (29.07, лид)

| показатель                | значение                                      |
| ------------------------- | --------------------------------------------- |
| строк кода приложения     | 112 765                                       |
| строк тестов              | 258 698 (соотношение 2.3 : 1) |
| тестовых случаев          | 10 179                                        |
| проверок `expect`         | 27 339                                        |
| медиана проверок на файл  | 10                                            |
| в 50 самых тяжёлых файлах | 18% всех проверок                             |
| в 200 самых тяжёлых       | 40% всех проверок                             |

Классификация 1797 тестовых файлов по типу проверки: поведение — 1041, только заглушки — 381, отрисовка UI —
235, **текст исходника — 112**, живая база — **28**.

⚠️ **Про «381» и «34» — это ДВА РАЗНЫХ признака, не опечатка** (расхождение поймано аудитом плана 29.07):

- **381** — широкий признак карты: в файле три и более заглушки, нет живой базы, нет отрисовки UI, нет чтения
  файлов. Это просто «тест на заглушках» — само по себе НЕ дефект, так пишут нормальные тесты портов.
- **34** — узкий признак: более 80% утверждений в файле — про то, что **наша собственная заглушка была
  вызвана** (`toHaveBeenCalled*`). Вот это подозрительно: тест может проверять сам факт вызова, ничего не
  зная о результате. Список: `docs/_TODO/testsuite-mock-echo.txt`.
- Выборочная проверка узкого списка показала, что часть из 34 — **законные проверки контракта на границе**
  (например: при ошибке интегратора отправка в учёт ошибок вызвана с нужными аргументами, при успехе — не
  вызвана). Поэтому этап 4 — разбор глазами, а не удаление по признаку.

Ключевой перекос: про права, изоляцию и запросы мы почти всё «знаем» **не проверив** — живую базу трогают 28
файлов из 1797, а текст исходника — 112. Сегодняшний дефект прав (функция пересчёта квоты без права на чтение
таблицы) был зелёным во всех тестах именно поэтому.

## Фильтр «назови ошибку» — процедура

Для КАЖДОГО теста ответить одной строкой: **какую конкретную поломку он поймает**, в форме
«подали такое — получили неправильное такое». Абстракции («проверяет корректность») не принимаются.

- ответ вида «поймает переименование / смену форматирования / что мы не вызвали свою же заглушку» → **удалить**;
- ответ называет правдоподобную поломку (лимит не сработал, чужой пациент виден, счётчик попыток не вырос) →
  **оставить**;
- спор → **объективный арбитр**: внести названную поломку в код руками и посмотреть, покраснел ли тест. Не
  покраснел — тест не ловит то, ради чего написан.

**БРЕМЯ ДОКАЗАТЕЛЬСТВА — НА СОХРАНЕНИИ (решение владельца 29.07: «сначала доказать пользу мне надо будет»).**
Умолчание — удалить. Проверка, упавшая от преобразования с нулевым изменением поведения (форматтер, кавычки,
переименование, разрезание литералов), считается ловящей формат и идёт в мусор БЕЗ обсуждения. Исполнитель и лид
НЕ имеют права оставить её своим решением и НЕ имеют права «сделать нечувствительной к кавычкам» — это лечение
симптома.

Оставить можно только так: принести владельцу доказательство — арбитром показать, какую конкретную поломку
проверка ловит и что ломается в продукте, если инвариант нарушить. **Решает владелец, не лид.**

Проверено на практике 29.07 — правило выведено из фактов, а не из осторожности: из четырёх проверок, которые лид
счёл нужными, три сверяли строку и все три оказались лишними (гейт U9A удалён через несколько часов вместе с
зеркалом — починка была выброшенной работой; PR-03A0 и текстовая половина сканера прав S4 лечились заплаткой
вместо проверки поведения). Уцелела одна — `pagePrincipalCensus`, и она единственная **не строковый матчер**:
разбирает дерево TypeScript, ни одного `toContain`.

**Если под текстовой проверкой правда стоит живой инвариант — правильная форма не «сверить текст мягче», а
проверить ПОВЕДЕНИЕ:** запустить команду и убедиться, что она отказала; сломать инвариант и убедиться, что
покраснело. Текст исходника не проверяет ничего.

**Фильтр применяет НЕ автор теста.** Автор всегда найдёт оправдание — 28.07 исполнитель закрепил тестом
собственную ошибку.

## Этапы

> ### 📍 СТАТУС ЭТАПОВ (сведение двух исследователей 29.07 — ни одного «открытого сироты»)
>
> Разведено по требованию владельца «прошлые этапы должны быть выполнены или отменены как неактуальные».
> Полное обоснование и метод исполнения — в разделе **«Как это исполнять (HOW)»** ниже.
>
> | этап | статус | куда |
> |---|---|---|
> | 0, 0б, 0в, 0г | **СДЕЛАНО** | форматтер+переименование+покрытие+пилот мутаций отработаны, см. «ЗАМЕРЫ 29.07» |
> | 0д (ИИ и глаза) | **ВЛИТО** | → HOW-A, шаг A7 (арбитр на единственной субъективной границе) |
> | 1, 2 | **СДЕЛАНО** | чисто-текстовые и смешанные текстовые сняты (#1074) |
> | 3 (мутации по 200) | **ВЛИТО** | → HOW-A (A2–A9) — это и есть mass-cut конвейер |
> | 4 (mock-echo 34) | **ВЛИТО** | → HOW-A (A5–A7) |
> | 5 (три инварианта) | **ВЛИТО** | → HOW-B, тир 5 |
> | 6 (гейт на новый код) | **ВЛИТО** | → HOW-C (C1–C4) |
> | 7 (a0-greenfield) | **ВЛИТО** | → HOW-C (C5); дрейф 0175 — блокирующий пред-шаг |
> | 8 (env-файлы) | **ОТМЕНЁН из скоупа тестов** | не тест; работа владельца остаётся, но вне «почистить тесты» |
>
> Ниже этапы 1–8 сохранены как справочный источник (их содержание — авторитет, на который опирается HOW).

> ### ⛔ ПРИНЦИП, ДЕЙСТВУЮЩИЙ НА КАЖДОМ ЭТАПЕ БЕЗ ИСКЛЮЧЕНИЙ
>
> **Умолчание — УДАЛИТЬ. Не «переделать правильно», не «сделать дешевле», не «сделать нечувствительным
> к кавычкам».** Проверка, упавшая от преобразования с доказуемо нулевым изменением поведения
> (форматирование, кавычки, переименование, разрезание литералов, вставка комментариев), ловит ФОРМУ
> ЗАПИСИ, а не поведение — и идёт в мусор без обсуждения.
>
> **Оставить может только владелец.** Исполнитель и лид НЕ вправе сохранить проверку своим решением.
> Хочешь оставить — принеси владельцу доказательство арбитром: какую конкретную поломку она ловит и что
> ломается в продукте, если инвариант нарушить. Решает владелец.
>
> **Резать можно тестами, не обязательно файлами** (владелец 29.07). Если в файле есть и мусор, и живая
> проверка — снять мусорные `it()`, файл оставить.
>
> **Если под текстовой проверкой правда стоит инвариант — правильная форма не «сверить текст мягче», а
> проверить ПОВЕДЕНИЕ:** запустить и убедиться, что отказало; сломать и убедиться, что покраснело.
>
> **Почему так жёстко (факты 29.07, а не осторожность):** из четырёх проверок, которые лид счёл нужными,
> три сверяли строку и все три оказались лишними — одна была удалена через несколько часов вместе с кодом,
> который стерегла. Уцелела одна, и она единственная не строковый матчер: разбирает дерево, а не текст.
> В тот же день тем же признаком слегли ПЯТЬ гейтов CI подряд — все они сверяли текст исходника.


- [x] **0. Механический отсев форматтером** (идея владельца 29.07; встаёт ПЕРЕД 1–4, ничего не вытесняет).
      Прогнать по репо `pnpm format` (prettier уже стоит и настроен, ставить нечего). Форматирование —
      **мутация с гарантированно нулевым изменением поведения**, поэтому любой покрасневший после неё тест
      покраснел доказуемо не от поломки: он ловит кавычки и переносы строк, то есть ровно то, что раздел
      «Врут красным» называет налогом. Это тот же «объективный арбитр» из процедуры фильтра, но применённый
      разом ко всем 1797 файлам и без спора с автором теста.
      **Обязательный порядок (иначе улов не доказателен):** 1. базовый прогон тестов ДО форматирования → список уже красного; 2. `pnpm format` **отдельным коммитом, где нет ничего кроме формата**; хеш — в `.git-blame-ignore-revs`,
      чтобы `git blame` продолжал показывать настоящих авторов; 3. второй прогон → **разница с базой и есть искомый список**; 4. каждый новый красный: пиннинг формата → удалить; сломалось по существу (правдоподобный класс —
      шаблонная строка, где значим перенос) → **это найденный баг кода**, чинить код, а не тест.
      **Граница метода:** красный под форматтером — доказательство бесполезности; зелёный под форматтером
      **не доказывает ничего** и никого не спасает от этапов 1–4. Не покрывает переименования (prettier имён не
      трогает) и `.sql`/`.sh` (850 и 52 файла; миграции — применённая история, не переписывать ни ради чего).

  **РЕЗУЛЬТАТ ПРОГОНА 29.07.** База до форматирования — 1792 файла / 10 815 тестов, ноль падений (поэтому
  каждое падение после доказуемо от форматтера). После `pnpm format` (`68bfcbeda`, 6327 файлов): **23 файла,
  53 теста**. Разбор всех 53:
  - **52 — утверждения о тексте исходника.** Классы: подстрока (`toContain('вызов("литерал")')`), регулярка по
    файлу, счётчик вхождений («не меньше девяти»), и самое дикое — **сравнение позиций символов**
    (`confirm-page.test.ts`: одна строка обязана встречаться в файле раньше другой). Отдельно стоит
    `check-s4-entitlement-coverage`: не найдя перенесённый вызов, он отрапортовал **десять несуществующих дыр
    в правах** — то есть ложная тревога не «впустую», а прямо в тему безопасности.
  - **1 — `pagePrincipalCensus`,** и он оказался единственным настоящим гейтом. Его анализ идёт по дереву
    разбора TypeScript, форматирование его не заметило: список нарушителей после прогона тот же, файл в файл.
    Красил только вмороженный рядом ответ вида `файл:строка`. **Решение владельца 29.07: номер строки не может
    быть частью ожидаемого значения нигде** — он двигается от любой правки. Починено в `7a7e0993c`: сверка по
    путям, номера считаются анализом в момент падения и печатаются в тексте ошибки. Проверено арбитром: снят
    `await` у guard на живой странице → гейт покраснел и назвал `app/doctor/appointments/page.tsx:31`.
    Проверено по всему набору: других тестов с `файл:строка` в ожидаемом значении нет, место было одно.
  - **5 красных файлов не значились ни в одном списке классификатора** — метод нашёл то, что разметка проглядела.

  **НАСКОЛЬКО ШИРОКО ПРОШЁЛ ЗОНД (замер 29.07, вопрос владельца «форматтер же не все строки менял»).**
  Форматтер переписал **84.3% всех строк кода**: 5079 файлов, 166 307 строк из 197 326. По строкам
  непройденных углов практически нет, повторять прогон незачем — он идемпотентен.
  **Настоящее ограничение не в строках, а в наборе символов:** prettier меняет только кавычки, пробелы,
  переносы, висячие запятые и скобки. Пиннинг, где ничего этого нет, переживёт любое форматирование —
  `toContain('runWebappPgText')` не умрёт никогда, а `toContain('fn(workspace, "cms_pages")')` умрёт.
  Отсюда вывод: **текстовая ось закрыта**, добирать нечего. Прогон с другими настройками (ширина 80/120,
  двойные кавычки) переставит те же пять символов в других местах — стоит один прогон, улов ожидается малый.
  До чего форматирование не дотянется принципиально: пиннинги ИМЁН (ось переименования, прогнана: 27 красных,
  настоящих 2 — ось тупая) и проверки ЗНАЧЕНИЙ (это работа мутаций).

  **Поправка к разметке (найдена 29.07).** Список «смешанных» завышен: классификатор считал
  `expect(src.match(/X/g).length).toBeGreaterThanOrEqual(9)` проверкой значения, хотя это тот же пиннинг текста
  со счётчиком. В 21 файле, удалённом сверх списка чистых, было 803 утверждения и **все текстовые**.

  **РЕЗУЛЬТАТ #1074 (29.07).** Из CI-gates в `scripts/`, `apps/*/scripts/`, `deploy/` и
  `docs/_TODO/SAAS_FOUNDATION/scripts/` удалены проверки текста исходников: общий
  `source-text-guard`, чисто текстовые gate-файлы и текстовые части смешанных gate-файлов. В смешанных
  проверках оставлены только исполняемые команды/тесты, запросы к БД, сверки генератора с артефактом и
  проверки структурированных реестров/классификаций. Удалённые gate-файлы исключены из агрегаторов,
  package scripts и deploy orchestration. Доказательство после очистки:
  `node scripts/check-saas-db-regression.mjs` — PASS (5 test files / 30 tests в app smoke);
  `pnpm lint` — PASS; `pnpm typecheck` — PASS.

- [x] **1. Удалить чисто текстовые.** 45 файлов, 717 проверок, ни на один нет внешних ссылок.
      Список: `docs/_TODO/testsuite-pure-textpin.txt`.
- [x] **2. Вычистить смешанные.** 21 файл, где текстовые пиннинги соседствуют с настоящими проверками
      (значения, «лишний вызов не сделан»). Удалять файлом НЕЛЬЗЯ — снять только текстовые утверждения.
      Список: `docs/_TODO/testsuite-mixed-textpin.txt`.
- [ ] **3. Мутации по 200 тяжёлым файлам — вместо ручного фильтра.** *(переписано 29.07 после сверки с
      практикой; прежняя редакция «разобрать 200 файлов фильтром назови ошибку» SUPERSEDED.)*
      Почему переписано: в этих файлах 40% всех проверок, это тысячи утверждений — вручную или ИИ это не
      разбирается за разумное время, а ИИ вдобавок **всегда** назовёт правдоподобную поломку, даже когда её
      нет (проверено 29.07 на себе: лид поверил комментарию в шапке теста, опроверг только арбитр).
      Мутационный анализ отвечает на тот же вопрос **фактом**: убитый мутант — это и есть «названная поломка»,
      выведенная механически, а не мнением. Списки партий: `testsuite-batch1.txt`, `testsuite-batch2.txt`.
      Порядок: прогнать Stryker по файлам партии, затем разбирать **только выживших мутантов** —
      их и предъявлять фильтру «назови ошибку». Выживший мутант, для которого нельзя назвать правдоподобную
      поломку, означает бесполезное утверждение; эквивалентный мутант (не меняющий поведения) означает, что
      трогать нечего. Результат — таблица «файл → оценка → выжившие мутанты → вердикт». Удаление — отдельным
      шагом после разбора лидом.
- [ ] **4. Решить по 34 файлам «утверждения только про вызов заглушки».** Список:
      `docs/_TODO/testsuite-mock-echo.txt`. Признак узкий (см. предупреждение выше), выборка показала законные
      проверки контракта на границе — поэтому разбор глазами тем же фильтром «назови ошибку», а не удаление
      по признаку. Один файл (`apps/webapp/src/infra/repos/pgBookingScheduling.principal.test.ts`) попал и в
      партию 2 этапа 3 — разбирается ОДИН раз, вердикт пишется в таблицу партии 2.
      **Критерий переписывания — из книги Google по инженерии:** проверять через ПУБЛИЧНЫЙ интерфейс, а не
      через внутренности. Избыточное подмешивание заглушек «связывает тест с реализацией вместо поведения:
      тест проходит, даже когда логика неверна, и ломается при любом рефакторинге внутренностей» — это ровно
      обе наши беды в одной фразе.
- [ ] **5. Три инварианта, оставшиеся без охраны.** Аудит признал удаление формально верным, но защиты у них
      теперь нет ни одной (решение владельца 29.07: «если его надо охранять — построй нормально»):
      удержание ссылочного UUID пациента между клиниками (изоляция арендаторов), приоритет структурного ФИО
      над разбором `displayName`, `onDelete: set null` для ссылки на медиафайл. Классы разные: первые два —
      поведенческими тестами, третий — живой базой (удалить медиафайл, убедиться, что строка карточки цела,
      а ссылка обнулилась). Каждый сдаётся с доказательством арбитра: сломать инвариант руками — тест обязан
      покраснеть.
- [ ] **6. Гейт на НОВЫЙ код, а не только уборка старого.** *(добавлено 29.07 по сверке с практикой.)*
      Разовая чистка убирает накопленное, но через месяц агенты напишут столько же: сегодня видели, как
      правило с `alwaysApply` заставляло их отстраивать удалённое зеркало настроек. В Google мутационный
      анализ встроен в КОД-РЕВЬЮ и гоняется на изменённые строки каждого диффа (6000 инженеров, ~30% всех
      диффов), а не разовым аудитом. Нам нужен тот же принцип в CI: мутации по diff-строкам, а не по репозиторию.
      **РЕШЕНИЕ ВЛАДЕЛЬЦА 29.07: принято** («я согласен с решением про мутацию»). Гейт по мутациям на
      изменённые строки становится частью плана, а не предложением. Разовая уборка без него закрывает
      прошлое и не мешает набору зарасти заново.

- [ ] **7. Эталон схемы `a0-greenfield` — сделать настоящим.** *(РЕШЕНИЕ ВЛАДЕЛЬЦА 29.07: принят вариант 1.)*
      **Находка:** эталон закоммичен ОДИН раз 21.07 и с тех пор не обновлялся, а миграций легло 65. При этом
      `verify-a1-rls-conformance.mjs` **восстанавливает его в одноразовую базу** и проверяет на ней изоляцию
      по RLS — и этот гейт гоняется в GitHub CI (`ci.yml:129`). То есть проверка безопасности всю неделю
      зелёная не потому, что всё хорошо, а потому что проверяет схему недельной давности.
      **Что делать:** пересобирать эталон (`refresh:saas-a0-greenfield-baseline`) ТЕМ ЖЕ коммитом, что и
      миграция, и вписать `check:saas-a0-greenfield-baseline` в CI, чтобы расхождение роняло сборку. Именно
      этих двух условий и не хватает: в Rails снимок схемы переписывает сам `db:migrate`, а у GitLab стоит
      отдельный тест «Check for changes in structure.sql». Без механизма снимок — декорация.
      **Сначала разобрать, а не замазать:** гейт сейчас красный с причиной
      `drizzle_historical_hash_drift:0175_p0_8_b4_roles_1_is_staff_wall_rls`. Причина настоящая — уже
      применённую миграцию 0175 правили ТРИЖДЫ после снятия эталона (`15d9748be`, `9f95bdfab`, `f1fe3e943`),
      то есть нарушено правило «миграции только дописываются»: на контурах, где 0175 уже накатилась, эти
      правки не применятся никогда. Пересборка эталона это скроет — поэтому сперва выяснить, что реально
      стоит на тесте и на проде.
      **Побочная выгода, ради которой владелец и согласился:** одноразовая база поднимается из снимка, а не
      накатом 65+ миграций на каждый прогон — это ускоряет CI.
- [ ] **8. Env-файлы: общий слой вместо дублирования.** *(РЕШЕНИЕ ВЛАДЕЛЬЦА 29.07: делать, физически руками
      лида, доступ есть в том числе через `su deploy`.)*
      **Замер:** в `api.prod` 21 ключ, в `webapp.prod` 14, дублируются 7. Из них `HOST` и `PORT` обязаны
      РАЗЛИЧАТЬСЯ (3200 против 6200) — их не трогать. Остальные пять обязаны совпадать байт в байт:
      `DATABASE_URL`, `INTEGRATOR_SHARED_SECRET`, `TELEGRAM_BOT_TOKEN`, `APP_BASE_URL`, `NODE_ENV`.
      Расхождение любого из них — это отвалившаяся межсервисная авторизация, две базы вместо одной или
      ссылки, ведущие не туда.
      **Форма:** НЕ один файл на оба сервиса (у них разные порты, и наименьшие привилегии: webapp незачем
      держать ключи СМС и MAX, интегратору — `SESSION_COOKIE_SECRET`). Общий `common.<контур>` с пятью
      значениями плюс частные файлы; systemd допускает несколько строк `EnvironmentFile=` в юните.
      **Порядок:** сначала тест, живая проверка обеих служб, только потом прод. Заодно проверить
      `RUBITIME_API_KEY` и `RUBITIME_WEBHOOK_TOKEN` в `api.prod` — если Рубитайм отключён, это мёртвые секреты.

## Механический отсев ДО аудита ИИ — порядок принят владельцем 29.07

Почему не «аудит ИИ по каждому тесту сразу»: ИИ, которого спрашивают «что ловит этот тест», всегда назовёт
что-нибудь правдоподобное. 29.07 лид сам поверил комментарию в шапке теста и назвал его ценным — решило дело
только внесение поломки руками. На 10 тысячах тестов это 10 тысяч возможностей соврать, и каждый ответ всё
равно пришлось бы проверять арбитром. Поэтому ИИ — последним и по остатку.

**Асимметрия, на которой всё держится:**

- красный под **форматтером** → тест доказуемо мусорный (сделано, 52 штуки);
- красный под **мутацией кода** → тест доказуемо ценный, **и он сам называет, что ловит** — это убитая им
  мутация. То есть колонка «что ловит» для выживших заполняется фактом, а не мнением;
- зелёный в обоих случаях → не доказано ничего, сюда и нужны глаза.

**Порядок (дёшево → дорого):**

- [x] **0б. Массовое переименование.** Тот же приём, что с форматтером, добирает третью причину из раздела
      «Врут красным», которую prettier не берёт: переименование. Переименовать внутренние символы по репо —
      поведение не меняется, красные = тесты, пиннящие имена. Один прогон.
- [x] **0в. Покрытие.** В репо не стоит ничего для замера (`c8`/`@vitest/coverage-v8` отсутствуют). Поставить и
      найти тестовые файлы, не исполняющие ни строки кода приложения — механическая замена гадательному
      признаку «только заглушки» (381 файл).
- [x] **0г. Мутации.** Готовое — Stryker (раннер под vitest есть, в репо не стоит). Гоняет не всю сюиту на
      каждую мутацию, а только тесты, покрывающие изменённую строку. Всё равно часы, поэтому: сперва пилот на
      одном модуле для замера времени, потом по приоритету — вход/пароли, изоляция клиник и принципалы, квоты и
      биллинг (туда же смотрит раздел «Врут зелёным»).
      **Оговорка:** бывают мутации, не меняющие поведение, поэтому зелёный под мутацией — очередь на разбор, а
      не приговор. Приговор без разбора даёт только красный.
- [ ] **0д. ИИ и глаза** — только по тем, кого не решили 0б–0г.

**Условие для каждого прогона:** база перед ним обязана быть зелёной, иначе новые красные не отличить от
старых. Это цена, которую 29.07 уже заплатили один раз и не будем платить вслепую второй.

## Куда писать результат и кто применяет фильтр

- **Результат этапа 3** пишется в `docs/_TODO/testsuite-verdicts-batch1.md` и `...-batch2.md`, по одной строке
  на тестовый случай: `файл:строка | название теста | какую поломку поймает | оставить/удалить/переписать`.
  Строка без графы «поломка» считается неразобранной и не засчитывается.
- **Применяет фильтр отдельный агент-ревизор**, не тот, кто писал тест, и не тот, кто чинил этот модуль в
  ночной волне 28.07. Лид назначает его через `tools/orch-launch.sh` и НЕ принимает вердикты по отчёту:
  выборочно перепроверяет спорные строки арбитром (внести поломку руками → тест обязан покраснеть).
- **Удаление после этапа 3 — отдельный шаг** и отдельный коммит: сначала таблица вердиктов, потом правки.

## Критерий «готово» по этапам

| этап | готово, когда                                                                                                                                         |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | 45 файлов удалены (или обоснованно оставлены поимённо), `pnpm --dir apps/webapp test` и `--dir apps/integrator test` зелёные, числа до/после в отчёте |
| 2    | в 21 файле не осталось утверждений по тексту прочитанного файла, все прочие проверки сохранены, опустевшие файлы удалены и названы                    |
| 3    | обе таблицы вердиктов заполнены целиком: строк ровно столько, сколько тестовых случаев в 200 файлах; спорные перепроверены арбитром                   |
| 4    | по каждому из 34 файлов записан вердикт в тех же таблицах                                                                                             |

Общий гейт на всё: **зелёный полный `pnpm run ci` и живая проверка на TEST** — «тесты стали чище» само по себе
не «готово» (канон: `docs/ORCHESTRATOR_CHECKLIST.md`, пункт 10).

## ЗАМЕРЫ 29.07 — всё, что измерено за день (разбор владельца завтра)

Записано как есть, без выводов и без работы из находок. Каждая цифра воспроизводима командой рядом.

### 1. Форматтер как мутация с нулевым изменением поведения (шаг 0а)

| | до | после |
|---|---|---|
| тестовых файлов | 1792 | 1737 |
| тестов | 10 815 | 10 423 |
| падений | 0 | 0 |

Прогон `pnpm format` (`68bfcbeda`, 6327 файлов) при зелёной базе уронил **23 файла / 53 теста**.
Разбор всех 53: **52 — утверждения о ТЕКСТЕ исходника** (подстрока, регулярка, счётчик вхождений,
сравнение позиций символов). 53-й — `pagePrincipalCensus`, единственный настоящий гейт: его анализ по
дереву разбора форматирование пережил, красил только вмороженный `файл:строка`.
**5 красных файлов не значились ни в одном списке классификатора** — метод нашёл то, что разметка
проглядела.

### 2. Переименование символов как мутация (шаг 0б)

Зонд: 1188 имён, которые тесты упоминают строками И которые экспортирует код; переименованы только
идентификаторы, строки и комментарии не тронуты; ноль ошибок типов в коде приложения (проверка, что
поведение сохранено). Результат — **19 файлов / 27 тестов**, из них настоящих **2**.
Остальные 25 — нормальные тесты, называющие экспорт строкой в `vi.spyOn`. **Вывод: ось тупая**, у неё
нет гарантии форматтера («красный ⇒ мусор»). Зонд выброшен, в репозиторий не лёг.

### 3. Покрытие (шаг 0в)

`@vitest/coverage-v8`, полный прогон с `--coverage.all`: **операторы 61.94%** (56 495 / 91 198),
ветви 53.96%, функции 59.19%.

**90 файлов кода не исполняет ни один тест** (2002 оператора), 32 из них по чувствительным темам.
Худшее: `pgOrganizationProvisioning.ts` (создание клиник, 65 операторов), `pgPatientInvites.ts` (90),
`pgPatientHomeBlocks.ts` (67), `registryAcquiringGateway.ts` (эквайринг, 28),
`notifyDoctorPatientMessage.ts` (23).
**У двух из них тест БЫЛ** — `pgOrganizationProvisioning.test.ts` и `pgWarmupFeelingCompletion.test.ts`;
оба оказались текстовыми и удалены сегодня. То есть отчёт показывал покрытие там, где код не исполнялся
ни разу.

Статический срез того же шага: файлов, где ВСЕ импорты приложения заглушены, — **ноль**. Признак
«381 только заглушки» из карты 29.07 оказался артефактом подсчёта `vi.fn()`, а не фактом.

### 4. Мутации, пилот (шаг 0г)

Stryker 9.6 + раннер vitest. Модуль `apps/webapp/src/modules/auth/sessionCookie.ts` — срок жизни,
потолок возраста, подпись и проверка формы сессии.

| мутантов | убито | выжило | без покрытия | оценка | время |
|---:|---:|---:|---:|---:|---:|
| 224 | 8 | 207 | 9 | **3.57%** | 261 с |

Читается так: почти любое изменение поведения в самом чувствительном к безопасности модуле проходит
мимо тестов. Оговорка: часть выживших мутантов эквивалентна (не меняет поведения), поэтому 3.57% —
нижняя граница, а не приговор каждому мутанту. 207 из 224 эквивалентными быть не могут.
Прикидка стоимости: ~4.4 минуты на файл при запуске только покрывающих тестов.


### 4б. Мутации по приоритетному модулю прав (этап 3, первый настоящий заход)

Модуль `app-layer/guards/requireEntitlement.ts` + `modules/org-entitlements/service.ts` — квоты и
механики, тот самый узел, из-за которого квота 28.07 была зелёной при неработающей функции.

| мутантов | убито | выжило | без покрытия | оценка |
|---:|---:|---:|---:|---:|
| 382 | 11 | 274 | 97 | **2.88%** |

Выжившие сидят на строках, ПРИНИМАЮЩИХ РЕШЕНИЯ (каждая = «поменяли логику, ни один тест не заметил»):
- `requireEntitlement:29` `if (access==='read') return ok` — страж может пустить куда нельзя;
- `requireEntitlement:31/34` — ветки `read_only`/`blocked`: заблокированная клиника может продолжать писать;
- `requireEntitlement:112` `assertMechanicEnabled(...)` — выключенная механика может остаться доступной;
- `service:28/36` — валидация квоты: кривой тариф и отрицательный лимит проходят;
- `service:165` `currentUsage >= limit` — САМО сравнение квоты: ни один тест не проверяет, что лимит срабатывает.

`:165`, `:112` и ветки `blocked`/`read_only` эквивалентными быть не могут — это реальные дыры.
**Вывод: здесь не тесты чистить, здесь тестов НЕ ХВАТАЕТ на самом чувствительном.** Ложится в этап 5 и в
замер «живую базу трогают 28 файлов из 1797». Отчёт мутаций: `runs/stryker-entitlements/report.json`.
Блокировала прогон сверка текста миграций в `service.test.ts` (35 утверждений, сняты).
### 5. Тесты, привязанные к обстоятельствам запуска

Поиск по 1755 тестовым файлам (`tools/testsuite/env-coupling.mjs`). Кандидаты, НЕ приговор:

| кандидатов | класс |
|---:|---|
| 155 | сеть или живой хост |
| 153 | часовой пояс в тексте |
| 102 | текущее время без подмены таймеров |
| 17 | текст `.sql` деплоя или миграции |
| 13 | `process.cwd()` |
| 6 | переменная окружения без установки |
| 5 | абсолютный путь машины |
| 3 | `Math.random()` |

Проверка руками показала ложные срабатывания даже в «острых» классах: `/tmp/...` в
`mediaPreviewWorker.test.ts` — возвращаемое значение заглушки, `Math.random` в `phoneOtpLimits.test.ts` —
генератор уникального номера фикстуры. Триаж нужен по каждому классу.

### 6. Что починено, а не удалено

- `pagePrincipalCensus` — сверка по путям, номера строк считаются в момент падения (`7a7e0993c`).
  Арбитр: снят `await` у guard → гейт назвал `app/doctor/appointments/page.tsx:31`.
- Сканер прав S4 — сверка границы регуляркой вместо `includes` с двойными кавычками (`e2f85d46c`);
  из-за кавычек он рапортовал **10 несуществующих дыр в правах**. Арбитр: подменён механик в
  `lifecycleActions.ts` → сканер назвал `cms-pages.lifecycle` пропущенным.
  Проверка реестра, удалённая воркером по ошибке, возвращена (`6bb0d0215`) — аудит дал по ней FAIL,
  подтверждено арбитром: подставленный дубль в реестре ловится.
- Срок жизни сессии — с текста на поведение (`c422e2d5c`). Арбитр: подменена ветка выбора срока →
  новый тест упал, старый текстовый остался бы зелёным.

### 7. Открытое — на завтра

- **Три инварианта без охраны вообще** (аудит признал удаление формально верным): удержание ссылочного
  UUID пациента между клиниками, приоритет структурного ФИО над разбором `displayName`,
  `onDelete: set null` для ссылки на медиафайл. Владелец 29.07: «если его надо охранять — построй
  нормально». Классы разные: два поведенческими тестами, третий живой базой.
- **Воркер по 17 SQL-пиннингам** — работа в клоне `bcb-wt-fmtcut`, независимого аудита ещё не было.
- **90 файлов без покрытия** — материал для решения, куда ставить настоящие проверки.
- **Мутации по приоритетным модулям** — стоимость измерена, объём не выбран.

## Что НЕ делаем

- Не гонимся за числом тестов: медиана в 10 проверок на файл здорова, перекос сидит в десятках гигантов.
- Не переписываем 40 SQL-пиннингов в интеграционные тесты: **деплой уже проверяет то же самое против живой
  базы** и строже (права ролей, число защищённых функций, опись политик, владелец таблиц). Дублировать незачем.
- Не трогаем 28 файлов с живой базой — это самое ценное, что есть.

## Как это исполнять (HOW) — сведённый вердикт двух исследователей 29.07

> Заказано владельцем: «нужны исследователи — как делать». Два независимых исследователя (Claude и Sol
> `gpt-5.6-sol`) отвечали на ОДИН вопрос, не видя друг друга — как прошлый вердикт Opus+Sol по «что должны
> проверять тесты». **Сошлись независимо** — это сведение. Расхождений было ровно два, выбор оркестратора
> сделан и помечен ниже. Это ДОПИСКА к плану выше (этапы 3–7 влиты сюда), **а не новый план; новых карточек
> не заводится**. Провенанс и оба отчёта: `docs/_TODO/runs/testcut-how/` (`sol-report.md`, `PROVENANCE.md`).
>
> **Две проблемы владельца, обе первого класса:** (1) огромный объём БЕССМЫСЛЕННЫХ тестов (исполняют строку,
> не проверяют ничего — замерено 2.88–3.86%); (2) ДЕФИЦИТ грамотных тестов там, где важнее всего (90 файлов с
> нулевым покрытием, 32 чувствительных; строки-решения прав без единого теста; живую базу в CI трогает ~0).
> HOW-A лечит первую, HOW-B — вторую, HOW-C не даёт зарасти заново.

### Опора: механизмы УЖЕ в репозитории — строить поверх, не рядом (сверено обоими в коде)

- **Stryker 9.6.1 + `@stryker-mutator/vitest-runner` уже стоят.** Рабочие конфиги: `stryker.pilot.json`
  (sessionCookie), `stryker.entitlements.json` (права/квоты). Оба `coverageAnalysis: perTest`,
  `disableTypeChecks: true`, `concurrency: 4`, `timeoutMS: 60000`.
- **Живая матрица A1 УЖЕ в CI.** `scripts/verify-a1-rls-conformance.mjs` поднимает одноразовый PostgreSQL на
  приватном сокете, восстанавливает `a0-greenfield`, накатывает миграции через `migrate-all.sh`, сеет две
  организации и доказывает own-org / cross-org-denied / missing-principal / unsigned-direct-DB через реальный
  `createWebappPoolProvider`. Job `saas-rls-conformance`. **HOW-B РАСШИРЯЕТ этот контур, а не строит второй.**
- **28 живых-БД тестов тёмные в merge-гейте.** `describe.skipIf` за `RUN_*_DEV_DB=1 && USE_REAL_DATABASE=1`,
  которых CI не ставит; и смотрят в записываемый `bcb_webapp_dev`, не в одноразовый кластер.
- **`check:saas-a0-greenfield-baseline` есть пакетным скриптом, но НЕ в `ci.yml`.** Только
  `check:saas-a1-rls-conformance` подключён — потому схема недельной давности проходила гейт безопасности.
- Merge-гейт = `test:webapp:fast`, шардирован 3×. Замер стоимости мутаций: sessionCookie 224 мутанта / 261 с ≈
  **4.4 мин/файл** при `perTest`.

### HOW-A — резать бесполезное (одна фикс-последовательность, НЕ «аудит пока не устанет»)

Каждый шаг механический и опровержимый; единственные суждения — два названных человеко/владелец-гейта в конце.

- **A1. Зелёная база — предусловие, энфорсит сам инструмент.** Перед каждой партией мутаций покрывающие тесты
  обязаны быть зелёными, иначе новый красный не отличить от старого. Энфорс бесплатный: Stryker гоняет
  **initial dry-run** и падает, если база красная. Красный dry-run = «чини базу», НИКОГДА «мутант выжил».
  Фиксировать базовый SHA + версии Node/pnpm/Stryker/Vitest + перепись файлов/случаев + хеш конфига мутаций +
  хеш манифеста A0. Полный CI — один раз на финальном merge-чекпоинте, не между мелкими партиями.
- **A2. Стабильная опись + узлы решений из AST.** `test_id = путь + полный заголовок describe/it + хеш AST-узла`
  (**НЕ номер строки** — решение владельца 29.07). Код бить по ПУБЛИЧНОМУ контракту, не по файлу теста. Узлы
  решений из AST: условия и case, валидационные return и доменные throw, границы сравнений, логические связки,
  проверки прав/принципала, переходы состояния, арифметика квот/сумм/сроков/повторов. Импорты, типы, логи,
  статические подписи, JSX-копирайт, wiring-литералы — «пустые» (arid), если контракт явно не делает их
  наблюдаемыми. Так и делает Google: мутирует изменённый код, опускает пустые узлы, лимитирует мутантов на
  строку, выбирает полезные операторы, а не брутфорс всего репо
  (https://research.google/pubs/practical-mutation-testing-at-scale-a-view-from-google/).
- **A3. ДВА режима Stryker, не один:**

  | параметр | офлайн-уборка/аудит | PR-ратчет |
  |---|---|---|
  | scope | фикс-батч модуля | диапазоны изменённых строк-решений |
  | `coverageAnalysis` | `perTest` | `perTest` |
  | `disableBail` | `true` (полная kill-матрица: кто кого убил) | `false` (скорость) |
  | `incremental` | `true` | `false` (для корректности) |
  | `--force` | на рероне после смены конфига/среды | всегда |
  | concurrency | 2 на общем хосте; 4 на выделенном | 4 |

  Тонкости (сверено с докой): vitest-runner ВСЕГДА per-test (конфиг `coverageAnalysis` игнорится) и гоняет
  каждый vitest-воркер однопоточно — внешнюю параллель даёт Stryker, поэтому в среде батча ставить
  `VITEST_MAX_WORKERS=1`, иначе оверсабскрайб 8 ядер. `disableBail` НЕ включает per-test — он заставляет
  убитого мутанта показать ВСЕ убившие тесты (нужно для атрибуции при прунинге). `incremental` — для офлайна и
  восстановления прерванного прогона, но **не безопасная граница корректности для PR** (не видит смену
  зависимостей/среды/снапшотов; vitest сообщает изменённые тесты только по файлам). PR гоняет точные диапазоны
  `file:начало-конец` + `--force`. `disableTypeChecks: true` держать (мутанты создают TS-невалидные формы;
  типы проверять на немутированной базе отдельно).
- **A4. Обуздать тяжёлые import-графы Next (фикс-эскалация).** (1) Мутировать доменный сервис/роут-хендлер, не
  `page.tsx`, когда оба выражают одно решение. (2) Использовать проект vitest `fast` для pure/module/API (Browser
  Mode vitest-runner не поддерживает). (3) `vitest.related=true` выбирает связанные тесты; для API/серверного
  контракта, достающего код косвенно, — явный allowlist тест-файлов + `related=false`, иначе Stryker молча
  пропустит нужный тест. (4) **РЕШЕНИЕ ОРКЕСТРАТОРА (расхождение №1 между исследователями): `ignoreStatic`
  ГЛОБАЛЬНО НЕ включать.** Claude предлагал включить ради 2× экономии; Sol возразил — TTL сессии или константа
  прав вычисляются на import-time и это РЕАЛЬНОЕ решение; глобальный `ignoreStatic` ослепил бы ровно самый
  чувствительный к безопасности модуль. Глушить только AST-классифицированный НЕ-решающий статик-узел. (5)
  Дробить батч, если dry-run > 90 с или прогноз мутаций > 15 мин. (6) Не гонять Stryker одновременно с полным
  Next-билдом или полным прогоном на общем 8-ядернике.
- **A5. Фикс-батчинг и порядок** (сюда влиты этапы 3 и 4). Партиция по владеющему контракту (не по тест-файлу),
  чтобы покрывающие наборы не пересекались между батчами (пересечение = повтор тех же тестов = потерянные часы).
  Порядок по ценности тира: (1) уже сделанный форматтер; (2) 200 тяжёлых файлов; (3) 34 mock-echo (пересечение
  разбирается один раз); (4) tenant/RLS; (5) вход/сессии; (6) квоты/биллинг; (7) 32 непокрытых; (8) остальное
  лексически. Цель — 75–150 мутантов-решений или ≤15 мин прогноза на батч.
- **A6. Фактическая kill-матрица, минимизация ПО СРЕДАМ.** Для каждой среды отдельно: `test → {убитые
  НЕэквивалентные мутанты-решения, воспроизведённые исторические дефекты}` и `мутант → {убившие тесты}`. Классы:
  `KEEP_PROVEN` (убил ≥1 неэквивалентный мутант-решение или воспроизвёл дефект), `REDUNDANT_PROVEN_SET` (убивает
  только уже убитое другим тестом в том же контракте/среде), `UNPROVEN` (не убил ничего значимого),
  `ARBITER_REQUIRED` (заявляет последствие, которого операторы не выразили), `DELETE_PROVEN_FORM` (падает от
  преобразования с нулевым поведением / сверяет текст). Внутри `модуль × среда × последствие` — детерминированный
  минимум покрывающего набора. **Юнит-тест НЕ вытесняет живой-БД тест — они про разные среды.** 28 живых-БД
  файлов держать, пока их поведение не представлено в новой БД-матрице.
- **A7. Арбитр — на ЕДИНСТВЕННОЙ субъективной границе (сюда влит этап 0д).** ИИ НЕ решает, полезен ли
  `UNPROVEN`-тест. Ревизор заявляет последствие C (вход/состояние → наблюдаемо-неверный результат) → вносит
  ровно эту поломку руками → гоняет немодифицированный тест → оставить, только если покраснел по заявленной
  причине → откатить, убедиться что зелёный. Одна инъекция арбитрирует целую дубль-группу. Под дефолтом-удаление:
  нет последствия → удалить; заявлено, но арбитр зелёный → удалить; арбитр красный → владелец МОЖЕТ оставить;
  эквивалентная мутация → классифицируем мутацию, а не тест. Три человеко-чекпоинта: **калибровочный** (после
  трёх репрезентативных батчей — pure-auth, тяжёлый Next, живой-БД — один раз утвердить конфиг и стоимость);
  **исключений** (владелец смотрит ТОЛЬКО предлагаемые keeps без обычной мутационной улики + новые подавления
  эквивалентных); **финальный cut** (один сводный before/after + независимая проверка улик удаления — НЕ второй
  ручной обзор 10 000 тестов).
- **A8. Дедуп выживших по ПОСЛЕДСТВИЮ.** Выживший мутант — не задача и не карточка. Строка worklist в этом же
  плане по ключу `тир + модуль + среда + public_boundary + decision_id + наблюдаемое последствие`. Пример:
  `quota | org-entitlements | pure | requireEntitlement | переход write-доступа | заблокированная клиника всё
  ещё пишет`. Все мутации `blocked`/`read_only`/связок/снятия guard с тем же последствием = ОДНА строка;
  id-мутантов и непокрытые файлы — уликами под ней. ИИ-истории про баги не принимаются — последствие называет
  человек один раз на чекпоинте.
- **A9. Применение удалений — малыми обратимыми партиями.** Сохранить вердикт-отчёт → удалить только
  классифицированное → прогнать те же немутированные контракт-тесты → один арбитр-инъекцией из каждой оставленной
  группы → откатить инъекции → коммит удаления + галочки-улики вместе → после логической группы модулей —
  прогон уровня приложения. Зелёный после удаления доказывает только что база ещё бежит; улика ценности — это
  до-удаления kill-матрица и арбитры.
  **Уборка дешёвого без мутаций (параллельно A5):** этап 1 (`testsuite-pure-textpin.txt`, 45 файлов) — первым
  коммитом; этап 2 (`testsuite-mixed-textpin.txt`, 21 файл) — снять текстовые `it()`, пустые файлы удалить.

**Стоимость HOW-A (Sol, инженеро-дни + машина):** калибровочная выборка ~1500 мутантов — 40–90 мин (80–180 при
2× графах); 5 000–15 000 отобранных мутантов-решений — ~2–10 машино-часов; брутфорс всего репо — 30–60+ ч, не
рекомендуется. Инструментарий описи/классификатора — 2–3 дня; разбор 200+34 — 3–5 дней; остаток свипа+удаления —
3–7 дней. **Итого HOW-A ≈ 8–15 инженеро-дней + машина.** Главная неопределённость — не машинное время, а число
арбитр-групп: один арбитр на мутант = неэкономично, один на дедуплицированное последствие = обязателен.

### HOW-B — строить недостающее (тиры по порядку; сюда влит этап 5)

**Рецепт авторинга (каждый тир):** один поведенческий контракт на `модуль × среда`, гонит модуль через ПУБЛИЧНЫЙ
интерфейс, проверяет РЕЗУЛЬТАТ, не внутренности. Среды раздельно, не в один гигант-файл: `*.contract.test.ts`
(pure на fakes/фиктивных часах), `*.postgres.integration.test.ts` (одноразовый Postgres 16), route/UI — только
если поведение не наблюдаемо ниже. Заглушать только недетерминированное/внешнее; не проверять вызов внутреннего
коллаборатора, если сам вызов не является внешним контрактом. Приёмка каждого теста: **инъекция поломки руками →
красный** (арбитр), и где был мутант — **обязан его убить**. Это дословно Google, гл. 11–12: «тестируй через
публичные API», «поведение, а не методы»; оверзаглушки «привязывают тест к реализации: проходит при неверной
логике, ломается на рефакторинге» — ровно обе наши беды
(https://abseil.io/resources/swe-book/html/ch12.html, https://abseil.io/resources/swe-book/html/ch11.html).

- **Тир 1 — изоляция клиник / RLS / принципал (высшая ценность для мед-SaaS).** Расширить существующий A1, не
  ставить второй фреймворк. Матрица **принципал × организация × операция**:
  оси — принципалы {staff A/B, patient A/B, missing/unsigned principal, owner-оракул}; орг {A, B}; операции
  {SELECT/INSERT/UPDATE/DELETE}; таблицы RLS-набора. Каждая ячейка проверяет РЕЗУЛЬТАТ через ОТДЕЛЬНОЕ
  oracle-соединение: cross-org SELECT → пусто/отказ; foreign UPDATE → 0 строк **И оракул подтверждает, что
  строка не изменилась** (0 затронутых сам по себе недостаточно); no-principal → отказ до scoped-доступа.
  Диагностика (`row_security_active`, `pg_has_role`, `session_user`, SQLSTATE) — В ДОПОЛНЕНИЕ к результату, не
  вместо. Non-owner логины обязательны: суперюзер и `BYPASSRLS` всегда обходят RLS, владелец таблицы обходит без
  `FORCE ROW LEVEL SECURITY`, enabled-таблица без политики = default-deny
  (https://www.postgresql.org/docs/16/ddl-rowsecurity.html).
  **Инъекции в саму базу (это и делает матрицу доверенной — БД-аналог Stryker):** после чистого прохода
  перегнать матрицу по ОДНОЙ инъекции на одноразовой БД, требуя красного на каждой: (1) `REVOKE` права нужной
  операции → падает позитивная own-org ячейка; (2) ослабить политику до `USING(true)`/`WITH CHECK(true)` →
  падает cross-org негатив; (3) снять `FORCE RLS` где идёт owner-путь; (4) сменить владельца `SECURITY DEFINER`
  функции; (5) выдать логину лишнюю привилегированную роль → падают и топология, и матрица. **Инъекция,
  оставившая матрицу зелёной = недостающий тест.** `SECURITY DEFINER` исполняется с правами владельца, поэтому
  смена владельца — реальный поведенческий дефект, не каталожная мелочь. pgTAP-хелперы прав — вторичны к
  проверке результата. **Также поднять 28 тёмных живых-БД тестов** в этот job: перевести изоляционное
  подмножество с записываемого `bcb_webapp_dev` на одноразовый кластер (каждый проверить на зависимость от
  dev-сида отдельно — не все переедут как есть). Стоимость: 3–6 инженеро-дней.
- **Тир 2 — вход/пароли/сессии.** Pure-контракты для `modules/auth/sessionCookie.ts` (пилот 3.57% — самый
  чувствительный модуль почти без проверок): верный пароль → успех; неверный → generic-отказ; повтор →
  инкремент устойчивого счётчика и точная граница задержки/локаута (`>=` vs `>`); истёкший/подделанный/битый
  cookie → отказ; TTL и max-age на фиктивных часах, ОБЕ стороны границы; logout → старая сессия непригодна.
  Инъекции: инвертировать проверку пароля, снять инкремент счётчика, сдвинуть порог, снять проверку подписи,
  выбрать неверную ветку TTL, принять реплей. Реальный код хеширования; НИКОГДА не хранить открытые креды в
  фикстурах. Приёмка — убить конкретных выживших из `runs/stryker-pilot/report.json`. 2–4 дня.
- **Тир 3 — квоты/биллинг.** Pure: `usage = limit-1` пускает; `usage = limit` блокирует; отрицательный/битый
  лимит отвергнут; выключенная механика блокирует; `read_only` — только чтения; `blocked` — отказ записи;
  отсутствие entitlement → документированный результат. Живая-БД: функции пересчёта МОГУТ читать все нужные
  таблицы под реальной ролью (дефект 28.07 — недостающий грант, невидим на подставленных числах); обычные роли
  НЕ читают внутренности биллинга; расход через публичный интерфейс = факту в БД; конкурентные резервации не
  превышают квоту; отказ не оставляет частичного состояния. Стартовый список инъекций — 7 названных выживших на
  `requireEntitlement`/`org-entitlements/service` (`runs/stryker-entitlements/report.json`). 2–3 дня.
- **Тир 4 — 32 непокрытых чувствительных файла (фикс-порядок последствий).** Первыми три критических шва:
  **провижининг клиник** (`createOrganizationProvisioningService` + реальный порт: валидный intent = ровно одна
  клиника+владелец+специалист; ретрай идемпотентен; коллизия slug → `slug_unavailable` без частичных строк;
  чужой принципал не читает/не провижинит; владелец букабелен только в новой клинике — только живой Postgres);
  **инвайты пациентов** (новый инвайт вытесняет прежний pending; revoked/expired/exchanged/superseded не
  погашаются; cross-org lookup/revoke/redeem падает; конкурентное погашение — ровно один победитель; провал
  доставки письма отменяет proof; sender — заглушка на границе, но проверять lifecycle+БД, не факт вызова);
  **эквайринг** (`registryAcquiringGateway` + фейк реестра провайдеров: глобально выключенные платежи падают;
  сумма/валюта/пациент/return-url доходят до провайдера; ошибка провайдера → публичный отказ без ложного
  success-record; refund хранит idempotency-key; неверный webhook-метод направляет в booking-webhook; API-авториз
  не даёт зарядить чужого пациента). Остальные 29 — тот же рецепт в лексическом порядке; нулевое покрытие выбирает
  очередь, не диктует число тестов. Стоимость всех 32: 8–16 дней.
- **Тир 5 — три инварианта без охраны (этап 5).** (1) Референсный UUID пациента между клиниками — две связи
  одного канонического пациента, публичный путь построения ссылки в обоих принципалах, канонический UUID
  сохранён без подмены орг-локальным; инъекция подмены → красный. (2) Приоритет структурного ФИО над
  `displayName` — конфликтующие поля, структурное побеждает, `displayName` только при отсутствии структурного;
  инверсия → красный. (3) Медиа-FK `ON DELETE SET NULL` — в одноразовом Postgres удалить медиа-строку, карточка
  цела, FK обнулился; пересоздать как `CASCADE`, затем `RESTRICT` — контракт обязан упасть на обеих. 1–2 дня.

**Вход HOW-B = выход HOW-A:** worklist выживших (A8) и список нулевого покрытия — входная очередь HOW-B,
дедуплицированная по последствию. Строить в порядке worklist, не файлов. **Итого HOW-B ≈ 16–31 инженеро-дней**;
БД-харнесс и fault-injection сериализуются вокруг общих артефактов.

### HOW-C — удержать гейтом, чтобы не заросло заново (сюда влиты этапы 6 и 7)

- **C1. Диф-мутационный merge-job** — обязательный, ПАРАЛЛЕЛЬНО lint/typecheck/tests/build/A1 (никогда не в
  8-минутной критической цепи). Шаги: посчитать merge-base → распарсить изменённые TS по AST → пересечь
  изменённые строки с узлами решений → сгенерить диапазоны Stryker → **≤2 высокоценных мутанта на узел**
  (граница равенства, условие/булево, логическая связка, return/throw, арифметика денег/квот/времени) → кап
  **100 мутантов на PR** (больше → «раздели изменение») → dry-run → Stryker `--force`, bail on, concurrency 4 →
  парсить JSON, не только агрегатный порог. Урок Google: мутировать только изменённое и капать/фильтровать —
  внимание разработчика тоже конечный ресурс.
- **C2. Точные условия падения:** dry-run упал; изменены строки-решения, но набор мутантов пуст; мутант на
  изменённой строке `Survived` или `NoCoverage`; инфра-ошибка мешает классификации; job > 6 мин; новое
  `Stryker disable` без аппрува/причины; JSON отсутствует или от другого base/head/config-хеша; рерон дал другой
  базовый исход (флейк). `Killed` принят; `TimedOut` — только при щедром фикс-таймауте от зелёного dry-run;
  повторные load-таймауты = инфра-провал, не kill. **Порог = 100% валидных неигнорируемых мутантов на изменённых
  строках** — бинарно по строке, а НЕ агрегатный %. `high=low=break=100`, но проверять статус JSON, т.к.
  агрегат прячет ошибки классификации.
- **C3. Эквивалентные мутанты — РЕШЕНИЕ ОРКЕСТРАТОРА (расхождение №2 между исследователями).** Score — нижняя
  граница; выживший блокирует merge, но не всегда требует нового теста. Claude предлагал файл-бейслайн
  `mutation-baseline.json` (как eslint). **Взято решение Sol: файл по номерам строк = ещё один тест на текст
  исходника, поедет от любой правки — отвергнут.** Подавление живёт ИНЛАЙНОМ у AST-узла:
  `// Stryker disable next-line <Mutator>: <причина>` + аппрув владельца/кодовладельца, едет с узлом и
  пересматривается при правке кода. Игнор-мутанты остаются видимыми в отчёте. Глобальное исключение оператора —
  «дробовик», недопустимо (https://stryker-mutator.io/docs/stryker-js/disable-mutants/).
- **C4. Бюджет 6 мин** (в параллель, внутри 8-мин merge): restore 45 с, AST-диф 15 с, dry-run 60–90 с, ≤100
  мутантов 2–4 мин (с 2× риском графа), отчёт 15 с, жёсткий таймаут 6 мин. A1/A0 БД-jobs параллельно с 7-мин
  таймаутом. Текущий A1-workflow позволяет 15 мин — цель мерить на ≥20 реальных PR перед энфорсом. Если A1 > 7
  мин — оптимизировать restore/setup, НЕ выносить изоляцию из merge-гейта. Авто-retry-до-зелёного запрещён.
- **C5. Сделать `a0-greenfield` настоящим (этап 7).** Добавить инварианты к существующему механизму A0: любой
  диф каталога миграций требует дифа baseline/manifest; фронтир манифеста = фронтиру миграций (ноль pending);
  любая изменённая историческая миграция падает ДО refresh; baseline без миграции требует явного аппрува
  ремонта; CI пересобирает нормализованную схему на одноразовом Postgres 16 и byte/hash-сверяет с коммитнутым
  baseline. Текущий генератор пишет source-commit и требует чистого дерева — расширить refresh **staged-режимом**:
  читать миграции из индекса Git → собрать БД из прошлого baseline + staged-миграций → дамп+нормализация →
  писать хеши содержимого миграций (не самоссылочный финальный SHA) → застейджить schema.sql+manifest → коммит
  миграции и baseline ВМЕСТЕ. CI повторяет генерацию во временном каталоге, сверяет zero-diff/hash. **Дрейф 0175
  разобрать против TEST/PROD ДО любого refresh — пересборка его замаскирует** (уже применённую 0175 правили
  трижды, нарушив append-only). Так делают зрелые экосистемы: Rails обновляет `schema.rb`/`structure.sql` из БД
  после миграции и коммитит (https://guides.rubyonrails.org/v7.2/active_record_migrations.html); GitLab
  `db:check-migrations` сверяет структуру и требует `structure.sql` в коммите миграции
  (https://docs.gitlab.com/development/database/dbcheck-migrations-job/).

**Стоимость HOW-C ≈ 4–8 инженеро-дней** (2–3 диф-мутационный инструмент, 1–3 staged-A0+дрейф, 1–2 тайминг/флейк).

### Разведение этапов 0–8 (совпало у обоих исследователей)

| этап | вердикт | причина |
|---|---|---|
| **0 / 0б / 0в / 0г** форматтер, переименование, покрытие, пилот | **СДЕЛАНО** | база 1792→1737, 52 текстовых снято, ось переименования признана тупой, 61.94% покрытие, 90 непокрытых, два пилота мутаций замерены; см. «ЗАМЕРЫ 29.07». Не повторять. |
| **0д** ИИ и глаза | **ВЛИТО → HOW-A A7** | арбитр на единственной субъективной границе, последним, только по выжившим |
| **1** чисто-текстовые (45) | **СДЕЛАНО** | набор 1737/10 423 без падений, класс удалён; остаток — механически в HOW-A |
| **2** смешанные (21) | **СДЕЛАНО** | 803 текстовых утверждения сняты, исполняемые сохранены |
| **3** мутации по 200 | **ВЛИТО → HOW-A A2–A9** | прозаические вердикты заменены фикс kill-матрицей, покрывающим набором по средам, арбитром и worklist |
| **4** mock-echo (34) | **ВЛИТО → HOW-A A5–A7** | одна фикс-партия после снятия пересечения; state/output-контракты и арбитр решают, без ИИ-обзора |
| **5** три инварианта | **ВЛИТО → HOW-B тир 5** | два поведенческих + один живой-БД, каждый с обязательной инъекцией |
| **6** гейт на новый код | **ВЛИТО → HOW-C C1–C4** | одобренное владельцем требование = строгий диф-мутационный ратчет с явными статусами и бюджетом |
| **7** a0-greenfield | **ВЛИТО → HOW-C C5** | same-commit staged-пересборка + сверка дрейфа; расследование 0175 — блокирующий пред-шаг |
| **8** общий слой env-файлов | **ОТМЕНЁН как этап тестов** | дедуп секретов деплоя не удаляет бессмысленные тесты и не строит контракты. Аппрув владельца на саму работу НЕ отменяется — она уходит из скоупа «почистить тесты», чтобы не блокировать его завершение. Новых карточек не заводится. |

«Влито» = старая галочка вытеснена названным HOW-разделом и НЕ остаётся вторым независимо открытым этапом.

### Честная неуверенность (сведено, оба)

- **3% с двух модулей на весь репо не переносятся** — число мутантов по узлам-решениям репо-широко не замерено;
  брутфорс = десятки машино-часов.
- **Тяжёлые Next-графы могут превысить 2×** — пилоты были на лёгких leaf-модулях; `infra/repos/**`,
  `app-layer/**` могут быть минуты-на-мутант; ~15-часовая оценка HOW-A может быть ×2–3. Мерить на первом реальном
  батче до расписания.
- **Доля эквивалентных мутантов неизвестна** — строгий гейт переносит эту неопределённость в явные узкие
  человеко-аппрувнутые подавления вместо снижения порога; подавления надо периодически аудировать (ручное).
- **Мутации не моделируют все дефекты** — изоляция транзакций, недостающие гранты, владение, конкурентность, RLS
  требуют отдельной БД-матрицы инъекций (HOW-B тир 1).
- **A1 сейчас доказывает одну границу (be_appointments), 2 роли, 2 орг** — расширение до полного CRUD по
  чувствительным границам может занять дольше 2–4 дней.
- **28 тёмных живых-БД тестов** переезжают на одноразовый кластер не гарантированно — часть зависит от dev-сида;
  каждый проверять отдельно.
- **8-минутный merge-бюджет зависит от планировщика GitHub-раннеров и старта Postgres** — калибровать на ≥20
  реальных PR перед энфорсом.
- **Same-commit A0** требует смены семантики генератора (сейчас source-commit) — просто добавить чекер в CI
  недостаточно: он пока пропускает pending-хвост миграций.
- **0175** — если TEST и PROD по 0175 разошлись, «пересобрать baseline» неверно; развилку не решить отсюда,
  нужен живой взгляд на оба контура.
