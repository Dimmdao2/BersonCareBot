# Двери входа: разбор находок аудита 15.09 в работу

Владелец 16.09.2026, дословно: «короче - у тебя чинить все эти двери и пути авторизации работы снова
есть. Все по найденному списку», «дубли, лишнее и тд». Это и есть разрешение брать находки в работу —
до него список был ответом на вопрос, а не скоупом.

Источник находок: `docs/_TODO/AUTH_DOORS_AUDIT_2026-09-15.md` (аудитор `gpt-5.6-sol high`, прогон
`auth-doors-audit-20260915`, 47 файлов `route.ts` в 17 группах). Строка вердикта и то, что ведущий
перепроверил своей рукой, — в `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md` (`e68111c9e`).

Предметный канон: `docs/ARCHITECTURE/AUTH_AND_IDENTITY_CANON.md`. Правило разбора: находка без
строки канона или без слова владельца — не работа. Ниже у каждого этапа названо его основание.

## Порядок и состояние

- [ ] **Д1. Сотрудничьи двери задаются кодом.** Закрывает находки F1 (глобальный админ входит кодом
      на почту мимо 2FA) и F2 (пациентская телефонная дверь выдаёт сессию сотруднику без пароля).
      Ведётся отдельным планом — `docs/_TODO/STAFF_DOORS_HARDCODED_2026-09-16.md`, этапы С1…С6.
      Основание: решение владельца 16.09 + таблица дверей канона.
- [x] **Д2. Мёртвая пара `messenger/start` + `messenger/poll` удаляется.** ✅ **ЗАКРЫТ (сверка ведущего с деревом 16.09):** каталога `apps/webapp/src/app/api/auth/messenger` в дереве НЕТ — пара снята раньше, галочка отставала от реальности. Находки U1 и D1:
      вызовов в исходниках нет (ведущий проверил сам — поиск по пути даёт только артефакты сборки
      `.next`), при этом `poll` умеет выдать сессию, а `start` раскрывает `user_not_found`. Живую
      привязку телефона к мессенджеру обслуживает `phone/messenger-bind`. Основание: слово владельца
      «лишнее» + принцип канона «вход стоит ровно столько, сколько самый слабый способ попасть
      внутрь».
- [x] **Д3. Двери пароля перестают различать состояние учётки до проверки кода.** ✅ **ЗАКРЫТ (сверка ведущего с деревом 16.09):** правка приземлена как `ad74c1af3`, `email-password/forgot` отвечает единственным `PASSWORD_RECOVERY_REQUEST_ACCEPTED` независимо от состояния учётки. Находка F4:
      `email-password/forgot`, `setup-access` и `setup-code/complete` отвечают по-разному на
      неизвестный адрес, contact-only и учётку с паролем. Основание — прямая строка канона:
      существующий и несуществующий адрес не различаются ни текстом, ни кодом ответа.
- [x] **Д4. Дубли-двойники сводятся к одной двери каждый.** ✅ **ЗАКРЫТ (сверка ведущего с деревом 16.09), все четыре пары:** D2 — `api/auth/login/alternatives-config` и `api/auth/oauth/providers` в дереве отсутствуют, единственным источником остался `publicAuthSnapshot.ts`, а отдельное имя бота Login Widget по-прежнему живёт своим маршрутом `api/auth/telegram-login/config` (капкан ниже не сработал); D3 — `api/auth/logout/route.ts` экспортирует только `POST`, состояние меняет не GET; D5 — password-setup challenge запускает ровно один маршрут `email-password/setup-code/complete`; D6 — каталог `api/auth/oauth/callback` содержит один `yandex`. Находки D2 (три проекции одного
      публичного конфига входа), D3 (`logout` отдельно GET и POST, GET меняет состояние), D5 (две
      двери запускают один и тот же password-setup challenge), D6 (два URL одного яндексовского
      callback). Основание: слово владельца «дубли». Для каждой пары в отчёте указать, какая дверь
      остаётся и кто переучен на неё.
      **Q1 ЗАКРЫТ ВЛАДЕЛЬЦЕМ 16.09, дословно: «два HTTP-адреса — /api/auth/login/alternatives-config
      и /api/auth/oauth/providers - удаляй, лишние дыры».** Оба маршрута удаляются совсем; замер по
      живым журналам это подтверждает — на новом проде за окно `14.09 10:22 — 15.09 22:42 UTC`
      обращений к ним НОЛЬ, на TEST два и оба наши собственные `curl` с хоста стенда. Прежняя
      редакция этапа («не удалять, обернуть») ОТМЕНЕНА словом владельца. Единственный источник —
      `publicAuthSnapshot.ts`, экран получает снимок через RSC.
      ⛔ **Капкан при сведении (сосед по репозиторию 16.09, проверено ведущим по коду):** в
      `alternatives-config` поле `telegramBotUsername` — имя бота, который шлёт КОДЫ, и маршрут
      намеренно гасит его в `null`. Имя бота Login Widget — ОТДЕЛЬНАЯ настройка, её отдаёт только
      `/api/auth/telegram-login/config` через `getTelegramLoginWidgetBotUsername()`. Возьмёт единый
      источник имя из `alternatives-config` — вернётся «левый бот»: кнопка позовёт одного, код
      придёт от другого. Проекций три, третья — `publicAuthSnapshot.ts`, она и есть настоящий
      единый источник; сводить все три, сохранив РАЗДЕЛЬНОСТЬ двух имён.
- [x] **Д5. `oauth/start` проверяет явно названную дверь, а не только Host.** ✅ **ЗАКРЫТ ПО СУЩЕСТВУ
      (сверка ведущего с деревом 16.09), с ОДНОЙ оговоркой, которую надо знать:** маршрут зовёт
      `canSurfaceEnterRoute(surface.surface, …)` и `authPolicyNameForRequestSurface(…)`, то есть дверь
      названа явно, а не выведена из Host. Оговорка: проверка стоит под условием
      `arePlatformSurfaceHostsDistinct()` — там, где поверхности живут на ОДНОМ хосте, она не
      выполняется вовсе. Это осознанная развилка, а не дефект: разделять ли поверхности на одном хосте —
      вопрос настройки стенда, и он НЕ поставлен владельцем. Вынесен в Д9 как вопрос, работой не делается.
      F6 закрыт фактом: подсказываемый экраном адрес `…/api/auth/oauth/callback/vk` в дереве СУЩЕСТВУЕТ
      (`apps/webapp/src/app/api/auth/oauth/callback/vk`), несуществующего URL админка не предлагает.
- [ ] **Д6. Contact-only пациенту не предлагается setup пароля, который завершение обязано
      отклонить.** Находка F7.
- [x] **Д7. Две пациентские регистрации по почте, одна из которых всегда запрещена.** ✅ **ЗАКРЫТ
      (сверка ведущего с деревом 16.09):** мёртвая парольная регистрация пациента снята и приземлена
      (`d26b1c0e1`, аудит `8e2b7e1f5`, публичный контракт поправлен `c8454b512`); в дереве осталась одна
      почтовая регистрация — `api/auth/email-otp/register`.
- [ ] **⛔ Д8. Канонически снятые входы: deep-link обмен токена и Mini App.** Находка F3. Канон
      требует убрать обмен токена как класс и временно отключить мини-приложения целиком, но эта
      правка **режет живой вход той самой когорте, у которой нет почты** (замер 15.09: 91 человек
      приходит через мессенджер). Поэтому этап стоит ПОСЛЕ требования почты при входе (Э5) и
      начинается только по отдельному слову владельца. Сам по себе список «чинить всё» его не
      открывает: слишком дорогая цена ошибки в порядке действий.
      **⛔ ЭТАП СНОВА ЗАКРЫТ ВЛАДЕЛЬЦЕМ 16.09, позже в тот же день: «пока не трогай миниапп, я не
      знаю как сделать и это сейчас не так важно».** Это отменяет открытие ниже — ни Д8, ни Д10 в
      работу не берутся, пока владелец не вернётся к теме сам. Всё, что записано дальше, сохранено
      как разобранная развилка, чтобы не разбирать её заново.

      **Замер ведущего 16.09, который сильно уменьшает работу.** Проверено по дереву, а не по памяти:

      1. **В боте мини-приложения УЖЕ НЕТ.** Кнопки собираются как обычные inline-кнопки с полем
         `url`; это закреплено тремя наборами — `executeActionBookingMiniAppRemoval`,
         `executeActionHomeMiniAppRemoval`, `executeActionDiaryReminderMiniAppRemoval`, — и каждый
         прямо утверждает `replyMarkup` **не содержит** `web_app` и `open_app`. Значит «вырезаем
         миниапп» в части БОТА уже сделано раньше.
      2. **Мини-приложение живо в ВЕБАППЕ, и это и есть настоящая работа Д8:** страницы
         `app/app/tg/page.tsx` и `app/app/max/page.tsx` плюс двери входа по `initData` —
         `api/auth/telegram-init` и `api/auth/max-init`. Вместе с ними снимается диплинк-обмен токена
         (находка F3).
      3. **Меню платформенного бота сегодня — ОДНА кнопка** «📅 Запись на приём»
         (`content/telegram/user/menu.json` и `replyMenu.json`, действие `booking.open`). Это не
         мини-приложение и не `web_app`: нижняя кнопка шлёт текст, бот отвечает сообщением с
         inline-ссылкой.
      4. **Платформенный бот и выделенный бот клиники — два РАЗНЫХ маршрута** в интеграторе:
         платформенный вебхук/long-polling и отдельный маршрут выделенного бота, который привязан
         точным отпечатком учётных данных (`resolveDedicatedClinicBotOrganization`).

      **⚠️ Замер в самой `main` (владелец спросил «ты смотрел в майн?» — не смотрел, измерил после
      вопроса). Он меняет постановку, потому что «нынешнее поведение» живёт там, а не в `feat`:**

      - в `main` нижнее меню — ДВЕ кнопки: `menu.book` («📅 Запись на приём») и `menu.app`
        («Приложение») с `webAppUrlFact: links.webappHomeUrl`. Вторая — это кнопка `web_app`, то есть
        контейнер мини-приложения **по построению**. В `feat` осталась одна первая;
      - в `main` есть ещё подменю дневника (`diary.symptom`, `diary.lfk`), которого в `feat` нет;
      - шире меню: сценариев бота в `main` — **75**, в `feat` — **23**. В нашей линии сняты дневник,
        кабинет, помощник, привязка контакта и телефонная авторизация в боте.

      То есть `feat` уже реализует решение канона «в боте только предоставление контактов и получение
      кода», а `main` — это старый богатый бот. **«Оставить для моего бота как в `main`» и «вырезаем
      миниапп» сталкиваются ровно на его боте:** кнопка «Приложение» в нижнем меню иначе, чем через
      `web_app`, не открывается — поля `url` у нижней кнопки в Bot API нет.

      **Вопрос владельцу, без него правка не делается:** что именно обязано сохраниться у
      `BersonCareBot` после переезда — только две кнопки нижнего меню (тогда для его бота остаётся
      контейнер мини-приложения ради второй кнопки), или всё поведение старого бота целиком (дневник,
      кабинет, помощник)? Это его решение, а не инженерная развилка.

      **Как это складывается (моё прочтение, владелец поправит одной строкой).** Своими же словами
      он сказал 16.09: «пациентский бот на платформе один - это ПОКА». Значит `BersonCareBot` — это и
      есть платформенный пациентский бот, а не выделенный бот клиники. Тогда решение раскладывается
      без единого исключения в коде: **меню не строится у ВЫДЕЛЕННЫХ ботов клиник, а платформенный
      пациентский бот сохраняет нынешнюю единственную кнопку** — то есть ровно то, что владелец и
      просит для своего бота. Если же `BersonCareBot` заведён как выделенный бот клиники `berson`,
      понадобится явная привязка исключения к конкретному боту, и это надо знать ДО правки.
      Порядок остаётся прежним: правка режет живой вход когорте без почты (замер 15.09: 91 человек
      приходит через мессенджер), поэтому этап идёт ПОСЛЕ требования почты при входе (Э5), а Э5
      отложен владельцем. До снятия этой отсрочки Д8 не берётся в работу.
      **Измеренное, что пригодится при исполнении:** у нижней кнопки Telegram поля `url` нет
      (проверено по установленным типам Bot API) — она открывает адрес только через `web_app`, то
      есть запуском мини-приложения; инлайн-кнопка в сообщении поле `url` несёт. Именно поэтому
      «гасим миниапп» и «кнопка открывает виджет в один тап» были взаимоисключающи — вопрос снят
      решением выше: кнопок не будет вовсе.
- [ ] **Д9. Четыре вопроса аудита без строки канона** (Q1–Q4: удаление двух незваных config-адаптеров;
      привязка OAuth `state` к начавшему браузеру и одноразовость; нужен ли state-changing
      `GET /api/auth/logout`; двойное создание сессии при первом specialist signup). Инженерные
      решаю сам по мировой практике, продуктовые выношу владельцу одним листом, а не по одному.

## Границы для всех этапов

Пациентский вход не ломается: каждый этап обязан назвать, что именно продолжает работать как сейчас.
Прод и TEST не трогаются. Миграции из клона на DEV не накатываются — только откатываемый preflight.
Полный CI гоняется, только когда в `feat` приедет исполняемый код, а не markdown. Галочка этапа не
ставится, пока не пройден независимый адверсарный аудит другой моделью, чем автор.

## Перепись «а где ещё» (ведущий, 16.09, по прямой просьбе владельца)

Владелец 16.09: «может лучше если гейт не работает поискать где еще есть маршруты - разными
поисками по коду?». Сделано четырьмя независимыми проходами по `apps/webapp/src/app`, не одним
поиском. Команды и цифры:

- всего HTTP-маршрутов: `find apps/webapp/src/app/api -name route.ts | wc -l` → **483**;
- кто зовёт пациентскую сессию напрямую, минуя общий проход
  (`grep -rln "getOptionalPatientSession\|patientClientBusinessGate\|canAccessPatient" --include=route.ts`
  минус те, где есть `requirePatientApiBusinessAccess`): **5 файлов** — три объявленных исключения
  (`patient/support`, `patient/email-change/confirm`, `patient/messenger/request-contact`),
  `patient/organization-context` (уже переведён на общий проход в ветке Э5a, на `feat` ещё старый) и
  две двери входа `auth/phone/{start,messenger-bind/start}`, которые по устройству работают до сессии;
- остальные маршруты без общего прохода разложены по первому сегменту пути: `doctor` 176, `admin` 60,
  `auth` 40, `integrator` 29, `internal` 18 — это сотрудничьи и служебные поверхности, другой класс
  двери; `booking/public` 6 и `references`/`brand` — публичные по устройству; `payments/*-webhook` —
  входящие от провайдеров; `account/security/**` — те самые «привязка, повтор кода, поддержка, выход»,
  которые оракул Э5 прямо разрешает;
- страницы (не API): пациентских `page.tsx` **43**, и все они лежат под ОДНИМ швом
  `apps/webapp/src/app/app/patient/layout.tsx:85` (`patientClientBusinessGate`), поэтому «страница
  мимо гейта» отдельным классом не существует. Вне `app/patient/**` пациентских экранов нет: `app/tg`,
  `app/max` и `app/page.tsx` рендерят вход, `app/account` — учётная запись любой роли.

**Вывод переписи:** класс «пациентский маршрут мимо общего прохода» на `feat` больше нигде не
повторяется, кроме уже названных. Это НЕ значит, что гейт лишний: он держит класс закрытым на
будущее — три круга аудита нашли три разных таких маршрута подряд.

**Найдено попутно и ЗАКРЫТО 16.09 — вопрос снят вместе с дверью.** Было: маршрут
`api/references/[categoryCode]` закрывал приватные справочники СПИСКОМ ЗАПРЕЩЁННЫХ
(`PRIVATE_REFERENCE_CATEGORY_CODES = new Set(['visit_manipulation'])`), то есть был дверью,
открытой по умолчанию — новый приватный справочник отдавался бы публично, пока кто-то не вспомнит
дописать его в список. Спрашивать у владельца, переделывать ли список в обратную форму, больше не
нужно: маршрут снят целиком вместе со списком по его же слову «резать нещадно»
(`docs/_TODO/PUBLIC_DOORS_CENSUS_2026-09-16.md`, D1) — вызывающих у него не было ни одного.
