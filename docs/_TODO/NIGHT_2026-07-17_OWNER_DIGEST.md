# Ночь 17.07 — дайджест владельцу (черновик, дополняется до утра)

Собрано автономным лидом за ночь. Задачи-источники — в общей базе (taskdb, проект bcb).
Ниже только то, что требует твоего взгляда/решения. Всё остальное закрыто или в работе.

## 🔴🔴 ПУНКТ №1 — тест НЕ полностью «owner-ready», хотя отвечает 200 (#821, #815)
**Проверил сам, руками в БД (read-only).** «Owner-ready strict» включил FORCE RLS на графике и записях.
Под рабочей ролью персонала БЕЗ установленного принципала `current_org_id` = NULL → политика ложна для
всех строк → **любое «простое» чтение молча возвращает ПУСТО**, хотя данные есть (4 графика, 320 записей).
Последствия, которые ты и видел:
- **График работы «исчезает» при выставлении недели** — запись сохраняется, но чтение расписания врёт пусто.
- **Двойная запись проходит** — проверка занятости слота видит 0 занятых (жёсткое DB-ограничение ловит только
  точное пересечение одного специалиста, но не кабинеты/блоки/буферы).
Прогоны приёмки были зелёные, потому что били по пациентским экранам и обёрнутым чтениям, а не по этим.
**Не чинил** — это RLS/принципал, самая опасная зона: неверная правка = дыра в стенах изоляции. Нужен
аккуратный системный проход в ОДНОМ чокпоинте (#821) днём, с независимым аудитом. Это НЕ на коленке ночью.
Старый прод (заморожен, 1 клиника) — НЕ затронут.
**Но план правки уже готов** — `docs/_TODO/SAAS_FOUNDATION/RLS_UNPRINCIPLED_READ_FIX_PLAN.md`: рекомендация —
правка в ОДНОМ файле (`drizzle.ts`), доказательство что изоляция не ломается, план тестов, 4 вопроса тебе (§8).
Тебе останется сказать «го» — не начинать с нуля. Плюс там же отдельный баг: «Очистить шаблон» графика молча
не работает (перепутаны аргументы, коммит 460b7c8ce) — чинится в одну строку, отдельным диффом.

---

## 0. Статус приёмки теста (на 08:38)
Отдельная сессия приёмки (#783/#803) с ~06:46 без коммитов/смены статуса — простаивает (жива, но 0 артефактов
90+ мин). Похоже, ждёт тебя. Тест при этом ЖИВ (webapp/public = 200, 0.016с), деплой на свежем `7f0f6c11d`.
Я НЕ трогал её сессию и НЕ лез в основную ветку. Независимо я проверку по сути уже сделал: здоровье теста ✓,
прогоны ролей зелёные (03:31) ✓, плюс нашёл RLS-проблему, которую приёмка пропустила (см. пункт №1). Логины
демо-клиник A/B для показа изоляции — есть. Три готовые UX-ветки (#812/#813/#814) волью, как только сессия
приёмки точно освободит ветку — они не связаны с RLS-багом и не влияют на твою главную цель.

## A. Сделано и отаудировано ночью (ждёт вливания после приёмки теста)
- **#805** `/book/{имя-клиники}` — публичная запись клиники. Ветка отаудирована (жёсткий секьюрити-аудит, 0 critical/high). Хвост: нужен grant #817 перед живым тестом.
- **#812/#813** — КПИ «новые сообщения» открывает конкретный диалог; ссылка «открыть карточку» в шапке чата. Отаудировано, баг namespace-коллизии пойман и починен.
- **#814** — панель «обзор и записи» слева в чате (по образцу упражнений). Отаудировано SAFE.
- Вливание всех трёх — сразу после того, как приёмочный агент добьёт финальный деплой теста (#783/#803), чтобы не мешать ему.

## B. Спроектировано ночью (планы на твоё ревью, реализация после «го»)
- **#751** Тарифы+оплата+админ-сетка — план заморожен, прошёл ДВА независимых аудита (второй: «безопасен к исполнению, 0 опровергнутых утверждений»). Магазин упражнений исключён из волны (как ты сказал).
- **#807** Лендинг + два входа — дизайн-дельта против UX-канона. Нашёл: жёсткий PWA-редирект = баг против уже принятого канона (чинится без тебя); хардкод домена всего в 3 местах.
- **#808** Базовая админка + чат техподдержки — вердикт: строить узкую новую модель тредов (старый support_conversations архитектурно не подходит).
- **#801/#806** Инвайты + создание пациента из календаря — дизайн-контракт против roadmap-стадии U3B; ключевая находка: таблица org_enrollments уже есть, но её никто не пишет.

## C. 🔴 Риски, найденные ночью
- **#818** Абонементы: mock-оплата БЕЗ env-гейта. Подтверждено на ветке прода (main, заморожен 01.07): любой авторизованный пациент с выставленным счётом может активировать абонемент бесплатно. Прод не трогал (только читал). Фикс = гейт в коде, деплой — с твоего разрешения.
- **#821** ~44 роута без re-stamp DB-принципала под locked RLS — гейтит ПРОД-катовер (#708). Нашлось в собственной заметке #725, карточки не было.

## D. Вопросы к тебе (сведённый список, отвечать по номерам)
1. **MAX API — дедлайн 19.07 (2 дня):** код готов. Нужно на проде integrator добавить `MAX_API_BASE_URL=https://platform-api2.max.ru`, сертификат Минцифры, `NODE_EXTRA_CA_CERTS`, рестарт, тест-отправка. (#65)
2. Rubitime-инцидент застрял (0 потерь, guard сработал): чистить `resolved_at` на проде? Делать durable-фикс (авто-resolve + graceful 23P01)? (#737)
3. Disposable prod-copy DB для финальной репетиции мультитенанта перед катовером — когда дашь? Плюс включить в приёмку аудит #821. (#708)
4. Пациенты (моб.): превью выбранного уходит ниже экрана — авто-скролл к панели или bottom-sheet? (#130)
5. Пациенты: сбрасывать значки-фильтры при смене сегмента/категории — да/нет? (#131)
6. Напоминания по времени суток: какие дефолтные времена и переносить ли bulk существующих клиентов? (#191)
7. Рассылки: приоритет из 3 подзадач (отложенная отправка / фикс-темы / подписки / форс)? (#90)
8. Opt-in согласие на рекламные рассылки: дашь юр.текст оферты или разрешишь драфт технического флоу на ревью юристу? (#213)
9. Онлайн-приём: когда надиктуешь подробное ТЗ (предоплата, отбор врачом, расписание/MVP)? (#215)
10. Курсы: база уже в коде — пауза актуальна или что конкретно доделать? (#26)
11. Триаж (#815): жду точные шаги воспроизведения багов «график удаляется при недельном расписании» и «проблема с созданием записей».

## E. Почищено ночью (бэклог)
Закрыты как уже сделанные в git: #24 (ФИО), #27 (вкладка Финансы), #734 (clinic_admin gate), #735 (isolation verified), #738 (invite accept). Дубль #800→#811. Осталось подтвердить 1 под-баг в #54 (legacy telegram_users).
