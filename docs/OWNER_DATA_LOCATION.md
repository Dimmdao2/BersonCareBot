# Где лежат данные владельца, которых нет в репозитории

Здесь только **адрес и назначение**. Содержимого тут нет и быть не может: это персональные данные.

## `/opt/bersoncarebot-data/` (владелец `dev`, группа `deploy`, режим 0750/0640)

Перенесено 19.08.2026 из `.tmp/` рабочего дерева. Причина переноса: `.tmp` — временный каталог вне
git, любая уборка сносила бы его целиком, а восстановить эти файлы неоткуда. Проверено перед
удалением: все 72 файла сверены по контрольным суммам, расхождений нет.

| каталог | что это | почему ценно |
|---|---|---|
| `fio-backfill/owner-reviewed-test-20260719.{manifest,payload}.json` | Решения владельца по разбору ФИО, просмотренные вручную, с хешем источника и решением приёмки | Повторить этот просмотр = заново глазами пройти список людей |
| `fio-backfill/reports/fio-decisions.json` и отчёты качества | Итоговые решения «фамилия/имя/отчество» по каждому человеку + сухие прогоны и резервная копия применённого | То же самое: ручная работа, не выводимая из данных |
| `specialist-consolidation/applied-*.json` | Что именно слито при устранении дублей специалистов: первичный, дубли, пропущенные | Единственная запись о том, что мы сделали с живыми данными |
| `reschedule-count-sanitation/applied-*.json` | Что поправлено в счётчиках переносов | То же |
| `placeholder-purge/applied-*.json` | Что вычищено из записей-заглушек | То же |

## Что НЕ перенесено и почему

`.tmp/fio-backfill/russiannames/` — 51 МБ публичного справочника русских имён (архив + jsonl).
Скачивается заново, ручного труда в нём нет. Удалено вместе с `.tmp`.

## Что с этим делать дальше

- [ ] Включить каталог в резервное копирование — сейчас он не в бэкапе, а второй копии нет нигде.
- [ ] При переезде прода `applied-*.json` — источник ответа на вопрос «что мы меняли в данных
      и когда». Не удалять до завершения переезда.

> ⚠️ В git эти файлы класть нельзя: там ФИО живых людей.
