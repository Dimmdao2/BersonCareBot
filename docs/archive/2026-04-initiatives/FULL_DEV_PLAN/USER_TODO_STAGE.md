# USER_TODO_STAGE

Дата: 2026-03-24  
Контекст: `FULL_DEV_PLAN` + текущее состояние реализации.

---

## Важно по нумерации этапов

- Нумерация синхронизирована с актуальным планом:
  - `Stage 14` = Settings/Admin,
  - `Stage 15` = PWA (отложен).

---

## Статус заполнения

- **Заполнено**: большая часть Stage 13, Stage 3, часть Stage 12 и выбор варианта по relay.
- **Требует уточнений**: STUB-02 contract детали, часть Stage 14 (права/флаги), часть Stage 12 (retry/dedup/метрики).
- **Безопасность**: секреты в этом документе не хранятся. Все ключи/пароли должны оставаться в env/секрет-хранилище.

## Принятые решения (итерация 2)

Ответы владельца в формате `1..8`:

1. Max уже практически реализован, бот работает. Дополнительно изучить проектную документацию по MAX перед финализацией.
2. Для relay использовать **один endpoint**.
3. Idempotency key в предложенном формате — **да**.
4. Retry — **да**, дополнительно сделать последнюю попытку через `5 минут`.
5. `debug_forward_to_admin` включается админом и изменения флагов логируются (audit) — **да**.
6. Для admin-mode авто-таймаут — **да** (значение уточняется отдельно).
7. Нужна детализация: для каких именно типов событий применяется policy partial-failure/fallback.
8. Для метрики "прочитано" принять рекомендованный подход (`open/click`).
9. Partial-failure policy уточнена владельцем по типам событий (см. раздел ниже).

---

## 1) Stage 13 + Stage 3

## 1.1 Stage 13 (Интеграции)

### 13.1 Email adapter (integrator)

**Ответ пользователя (подтверждено):**

- Почтовый домен: `dmitryberson.ru`.
- SMTP уже используется (`reg.ru`).
- DKIM/DMARC уже настроены.
- Отправитель: `no-reply@...` (используется).

**Что еще нужно от владельца:**

- Подтвердить окончательный SMTP-провайдер для production (оставляем `reg.ru` или меняем).
- Подтвердить, что SMTP-логин/пароль/API-key остаются только в env.

### 13.2 Email verification flow

**Ответ пользователя (подтверждено):**

- OTP длина: `6`.
- TTL: `20` минут.
- Ограничения: `5` повторов с паузой `1` мин, затем блок `10` мин.
- Нужен механизм редактирования шаблонов сообщений (email/sms/messenger) в настройках.

### 13.3 Deep-link Telegram

**Ответ пользователя (подтверждено):**

- Ключи/username/webhook уже есть в env.
- TTL link-secret: `10` минут.
- Одноразовость: `да`.
- Повторная привязка: конфликт + уведомление + предложение обратиться в поддержку.

### 13.4 Deep-link Max

**Ответ пользователя (частично):**

- Канал не обязательный для пользователя (как и TG/email).
- Доступы есть в env.
- Если в текущей библиотеке чего-то нет, нужно опираться на официальную документацию Max API.

**Нужна фиксация:**

- Max в текущем этапе: обязательный к поддержке (как канал привязки) или fallback-only.

### 13.5 Google Calendar sync

**Ответ пользователя (подтверждено):**

- Модель: `Rubitime master`, `Google Calendar` как проекция (read-only на данном этапе).
- Аккаунт для подключения есть.
- GCP/OAuth данные уже заведены.

**Важно:**

- Client secret и другие чувствительные данные хранятся только в env/secret-store.

### 13.6 Rubitime reverse API

**Ответ пользователя (подтверждено):**

- Контракт/ключи уже используются в текущем integrator и env.

### 13.7 Auto-bind email из Rubitime payload

**Ответ пользователя (подтверждено/частично):**

- Email есть в webhook payload.
- При конфликте email: как у мессенджеров, плюс уведомление пользователю; желательно уведомление админу.
- Невалидный email: не принимать.
- Неподтвержденный email: хранить как unverified и просить подтвердить в профиле.

---

## 1.2 Stage 3 (Профиль и auth-данные)

**Ответ пользователя (подтверждено):**

- Смена телефона/email без повторной верификации: `нет`.
- TTL сессии браузера: `90` дней.
- Инвалидация сессии: минимум при смене телефона.
- SMS-провайдер: текущий (`SMSC`) подтвержден.

---

## 2) Контракт STUB-02 (webapp -> integrator relay outbound)

## 2.1 Выбранный вариант

**Ответ пользователя:** выбирать `Вариант B`, если без чрезмерного усложнения.  
**Итог для реализации:** `Вариант B (рекомендуемый)`:

- HTTP endpoint,
- HMAC подпись,
- idempotency key,
- async delivery events,
- retry/backoff,
- явная error model.

## 2.2 Что уже решено

- Аутентификация: `HMAC`.
- Общий подход: надежно, но без overengineering.

## 2.3 Что нужно уточнить до кодинга

- Endpoint: один универсальный или несколько по каналам.
- Idempotency:
  - какой ключ используем (`message_id` + `recipient` + `channel` или `correlation_id`),
  - сколько хранить dedup-ключи.
- Retry:
  - сколько попыток,
  - интервалы,
  - что считается финальной ошибкой.
- Delivery status:
  - какие статусы обязательны (`accepted/sent/delivered/read/failed`).
- SLA:
  - целевое время доставки (например 95% < 30 сек).

---

## 3) Stage 14: Settings/Admin

## 3.1 Роли и права (ответы пользователя)

- Label "клиент/пациент": `admin`.
- `dev_mode`: `doctor`.
- Тестовые ID каналов: `admin`.
- Интеграционные ключи: `admin` (хранить в БД).
- SMS fallback: `admin` + подтверждение.
- Архивация пользователя: `admin`.
- Контент рассылок: `doctor`.
- Записи на прием (удаление/скрытие): `doctor` + подтверждение (нужна детализация, см. вопросы ниже).
- Выбранная защита admin-mode: `Вариант A` (toggle + confirm dialog).

## 3.2 Неуточненные места

- Что такое `debug_forward_to_admin` и кто его включает.
- Что считать `bulk operations` в вашем контексте.
- Нужен ли `audit required` для каждого опасного флага.
- Нужен ли авто-таймаут admin-mode.
- Нужен ли флаг `allow_test_delivery_in_prod`.

---

## 4) Stage 12 (Reminders)

## 4.1 Уже определено

- Выбран `Вариант B` (мультиканал + fallback + retry + quiet hours + история доставки).
- Логика каналов:
  - напоминания/статусы записи — по каналам, выбранным пользователем,
  - напоминания по разминке — через привязанные мессенджеры,
  - push добавить позже (после PWA/mobile).
- Тихие часы:
  - управляются настройками (с учетом timezone пользователя).
- Лимит напоминаний в день:
  - предложено `20` (нужно уточнение scope).
- Пользователь может отключать все, кроме системных/важных сообщений.

## 4.2 Нужно уточнить

- Что делать при partial failure (по каждому типу напоминаний).
- Retry policy (число попыток/интервалы/стоп-условие).
- Что считать "прочитано/выполнено" для статистики.
- Что именно понимается под dedup и антиспам (уровень ключа, окно времени).
- Как агрегировать совпадающие по времени правила.
- Срок хранения истории delivery/read событий.

## 4.3 Утвержденная policy partial-failure/fallback (ответ владельца)

### 4.3.1 Запись на прием

- Статус: **утверждено**.
- Правило: fallback включен (`7.1 = да`).
- Практика: отправка в выбранные каналы; при ошибках применяем fallback по принятой цепочке.

### 4.3.2 Reminders ЛФК

- Статус: **утверждено**.
- Правило: fallback включен (`7.2 = да`).

### 4.3.3 Важные сообщения

- Статус: **утверждено отдельной логикой** (`7.3 = нет` для обычного fallback).
- Выбранный вариант: **B**.
- Правило отправки: сразу во все мессенджеры + email.
- Если нет подтвержденного прочтения, запускается SMS fallback.
- Время до SMS fallback:
  - по умолчанию `1 час`,
  - должно настраиваться админом в settings (например ключ `important_fallback_delay_minutes` в `system_settings`).

### 4.3.4 Чат (переписка)

- Статус: **утверждено**.
- Правило: fallback включен (`7.4 = да`).

### 4.3.5 Остальные уведомления / рассылки по темам

- Статус: **утверждено**.
- Правило: только в выбранные пользователем каналы, **без fallback**.
- Примеры: новости о событиях (Москва/СПб), онлайн-события, будущие темы рассылок, создаваемые доктором.

---

## 5) Список вопросов к владельцу (короткий чеклист)

Отвечать в формате `Q1: ...`.

### Stage 13 + Stage 3

- Q1: Оставляем SMTP-провайдер `reg.ru` как production default? 
- Q2: Max deep-link в этой итерации — обязательный канал или fallback-only? 
- Q3: Подтверждаете policy конфликтов email: блок автопривязки + уведомление пользователю + уведомление админу?
- Q4: Инвалидировать сессию только при смене телефона или также при смене PIN/email?
  
Ответы владельца:
- Q1: `да` (оставляем `reg.ru`).
- Q2: Max уже практически реализован и должен поддерживаться как рабочий канал (для пользователя при этом остается опциональным).
- Q3: `да`.
- Q4: только при смене телефона.

### STUB-02 relay contract

- Q5: Endpoint: один универсальный `/relay-outbound`?
- Q6: Idempotency key: `message_id + channel + recipient` подходит?
- Q7: TTL dedup-ключа: `24 часа` подходит?
- Q8: Retry: `3` попытки (`0s`, `10s`, `60s`) подходит?
- Q9: SLA: `95% < 30 сек` подходит?
  
Ответы владельца:
- Q5: `да` (один endpoint).
- Q6: `да`.
- Q7: `да`.
- Q8: `да`, плюс дополнительная последняя попытка через `5 минут`.
- Q9: принять рекомендуемое значение для проекта (`95% < 30 сек`).

### Stage 14 Settings/Admin

- Q10: `debug_forward_to_admin` кто может включать: `admin`? да
- Q11: Нужен ли `audit log` для изменений флагов/ключей?
- Q12: Нужен ли авто-таймаут admin-mode (например 30 минут)?
- Q13: Что у вас считается `bulk operations` (массовые рассылки, массовая архивация, массовое отключение каналов)?
  
Ответы владельца:
- Q11: `да`.
- Q12: `нет`.
- Q13: использовать техническое определение в реализации:
  - массовая рассылка по теме/сегменту (много получателей),
  - массовая архивация/разархивация пользователей,
  - массовое включение/отключение каналов/флагов.
  Для всех bulk-операций: `admin-only` + подтверждение + audit log.

### Stage 12 Reminders

- Q14: Partial failure policy: если TG не доставлен, идем в MAX/email/sms сразу или по задержке?
- Q15: Что считать "прочитано": `delivery`, `open`, `click` или `explicit confirm`?
- Q16: Лимит `20/день` — на пользователя, на канал или на все уведомления суммарно?
- Q17: Срок хранения reminder истории: `90 дней` или `180/365`?
- Q18: Если два правила совпали по времени, подтверждаете отправку по очереди с паузой `30` секунд?
  
Ответы владельца:
- Q14: по утвержденной policy из раздела 4.3.
- Q15: принять рекомендованный подход (`open/click`).
- Q16: лимит `20/день` на **пользователя**; количество каналов пользователя не влияет на лимит.
- Q17: по умолчанию `180 дней`.
- Q18: по умолчанию `да` (очередь с паузой `30` секунд).

### Доп.уточнение по важным сообщениям

- Q19: Для "важных сообщений" фиксируем какой вариант:
  - `A`: сразу во все каналы,
  - `B`: мессенджеры+email, через 1 час без confirmed-read -> SMS.
- Ответ владельца: `B`; время fallback должно быть настраиваемым у админа.

---

## 6) Что можно сразу отдавать агентам

После ответов на Q1-Q19 можно автономно запускать:

- Stage 13 (код + тесты + документация, кроме ручных действий с внешними кабинетами),
- Stage 12 по варианту B,
- Stage 14 (Settings/Admin, включая права/флаги),
- STUB-02 relay outbound (вариант B),
- финальные e2e/CI и итоговые отчеты.

