# Telegram и MAX: роль ботов, меню и возможное мини-приложение

**Статус 02.09.2026:** code-runtime и bot-action сверены; мёртвые ответы врача через кнопки бота удалены и
приняты аудитом. Живая TEST-проверка дошла до провайдера и установила внешний блокер: текущий TEST-токен
Telegram отвечает `401 Unauthorized`. Будущее минимальное меню отложено владельцем; удалённый Mini App SDK не
возвращается.

## Действующие решения владельца

- Обычный бот Therapysto подтверждает телефон средствами мессенджера, доставляет коды входа и обычные
  уведомления: о записи, занятии, сообщении клиники и других выбранных событиях.
- Брендированный бот клиники делает всё то же и дополнительно принимает входящие сообщения пациента и доставляет
  рассылки клиники.
- Ни команда, ни webhook бота не создают новую учётную запись. Регистрация начинается в webapp; бот только
  доказывает владение телефоном и доставляет код.
- Рассылки доступны только брендированному боту. Обычные уведомления доступны обоим.
- Встроенная библиотека Mini App намеренно удалена. Если мини-приложение вернётся, это отдельная поверхность
  записи на приём, а не второй кабинет, второй auth flow или способ создавать аккаунты.

## Что уже видно в текущем коде

- Telegram на старте очищает slash-команды и ставит стандартную кнопку меню без Web App
  (`setupTelegramMenuButton.ts`).
- MAX очищает список команд; там используется inline-меню, потому что постоянная reply-клавиатура не
  поддерживается. Техническая способность отправить `open_app` не означает, что Mini App продуктово включён.

## Оставшаяся проверка

- [x] Проверить на текущем TEST настройку Telegram menu button. Доказательство 02.09: provider-вызовы
  `deleteMyCommands` и `getUpdates` оба вернули `401 Unauthorized`; это один и тот же недействительный
  TEST-токен, а не ошибка меню или транспорта внутри приложения. Повторная живая проверка ждёт валидный токен.
- [x] Снять фактическую матрицу Telegram/MAX: команды, стандартная кнопка, inline/reply меню, deep links и
  различия обычного и брендированного бота. Доказательство 02.09: read-only run
  `bot-menu-research-20260902` прочитал обе интеграции, весь живой content и единый executor/dispatch; обычные
  уведомления используют клиниковый канал с платформенным fallback, рассылки — только `clinic_required`,
  dedicated inbound привязан к точной клинике, Mini App в живом content не включён.
- [x] Сверить каждый текущий bot-action с каноном выше: phone proof, login code, ordinary notifications,
  branded inbound messages и branded broadcasts; отдельно доказать отсутствие account creation. Доказательство:
  исследование подтвердило, что бот завершает только заранее созданный browser claim и возвращает код, а
  create-or-bind происходит в webapp после ввода кода. Мёртвые callback/M2M-ответы врача и вводящие в заблуждение
  кнопки удалены в `02c9ecca2`/`7acd848e1`; независимый audit `audit-systemic-bot-cleanup-final-20260902` принят
  после локализованного исправления уведомления пациенту в `8daaa6f32`.
- [x] Закрыть три разрыва, найденные фактической матрицей 02.09: экран записи снова ведёт browser URL на
  `/app/patient/booking`; свободный текст админа больше не предлагает удалённую кнопку ответа; прямой ответ
  обычного бота принудительно остаётся на платформенном отправителе, а ответ dedicated webhook — на боте
  клиники, без fallback между ними. Доказательство: `executeActionBookingMiniAppRemoval.unit.test.ts`,
  `deliveryPolicy.unit.test.ts`, `dispatchPort.test.ts`, integrator typecheck и scoped ESLint.
- [ ] После сверки определить минимальное меню пациента и сотрудника. **Отложено владельцем 01.09.2026:** сейчас
  только исследование; Mini App и новые продуктовые пункты не добавлять до отдельной команды.

Техническая справка возможностей MAX ведётся в `docs/ARCHITECTURE/MAX_CAPABILITY_MATRIX.md`; этот план определяет,
что из возможностей разрешено продуктом.
