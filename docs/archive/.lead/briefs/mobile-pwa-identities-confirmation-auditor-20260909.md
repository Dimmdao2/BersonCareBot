# Тест или взгляд — #915 PWA identity correction

Повторяемые manifest-builder outputs and documented runnable package command are checked by targeted behavioral
tests/command execution; generated PNG byte stability and retained legacy assets are one-time inspection.

# Confirmation auditor-live brief

Authority: `docs/_TODO/NATIVE_MOBILE_APP_INITIATIVE/MASTER_PLAN.md` M1-01…M1-06 and owner §1.5; product candidate
`93f2d7b34`; audit authority `a9e8c4f08`; independent audit/tests `dccaef384`; correction authority `b88cae29a`;
saved report `.lead/runs/mobile-pwa-identities-audit-20260909/90-final-audit-report.md`. Audit the final correction
commit produced after that authority. Product code is read-only.

Before every action run the `AGENTS.md` heading map. Read the global decision method; §1/§1b; §5; §9;
§10/§10a/§10b; §12; §15–§17; §21 and §24. Read the active plan, original worker/auditor/correction briefs, saved
report, blind kill-set, exact product/audit/correction diffs, manifest builders/routes, surface metadata resolver,
package scripts and mobile-shell README. Use code-search before blind grep.

First persist a fresh bounded blind failure list in
`.lead/runs/mobile-pwa-identities-confirmation-audit-20260909/00-blind-killset.md`. Confirm all retained M1 tests
remain green, then prove these two corrected behaviors at public boundaries:

1. `buildPatientPwaManifest(patient_default)` uses only Therapy Go `any`/`maskable` current-brand assets while
   `buildPatientPwaManifest(patient_branded)` preserves legacy blue `/pwa-icon-192.png` and
   `/pwa-icon-512.png`, contains no Therapy Go icon or false clinic maskable asset, and preserves the resolved
   clinic name plus stable id/scope/start URL. Extend the existing manifest output suite with this missing branded
   manifest oracle; do not test source strings or implementation structure.
2. Every active README command for regenerating brand assets invokes the existing `derive:brand-assets` package
   script and actually succeeds. This is command execution/one-time inspection, not a permanent source-text test.

Run explicit fault injections for default/branded icon cross-wiring and omission of the runnable command,
fully restore product mutations, and report `убито N / непойманных M` with exact failed assertions/commands.
Re-run targeted PWA/metadata/install tests, webapp typecheck and scoped lint through the host lock; run the asset
generator twice and compare hashes; run `git diff --check`. Do not modify production, Android native plugins,
M1-07, push/video/media paths, DB, deploy, TEST or PROD.

Writable scope is existing relevant PWA behavioral test files plus
`.lead/runs/mobile-pwa-identities-confirmation-audit-20260909/**`. Save the report as
`.lead/runs/mobile-pwa-identities-confirmation-audit-20260909/90-final-audit-report.md`. Commit only audit-owned
tests/artifacts with explicit paths and `#915`, never `git add -A`, do not push, finish clean, and report exact
SHA/verdict/commands/fault tally. Findings require a reachable owner-visible failure and violated M1 line; style or
alternative architecture is not a finding.
