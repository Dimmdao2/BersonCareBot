# Закрывающий аудит `B4a`, круг 2 — блокер круга 1 закрыт

**PASS. Требование `B4a` выполнено; блокирующих находок нет.**

Проверяемый коммит: `d41e5c1ec` «fix(branding): normalize core clinic names (#787)». Клон
`/home/dev/dev-projects/bcb-wt-night-b4a-20260823`, ветка `wt/night-b4a-20260823`. Оракул —
`IMPLEMENTATION_PLAN.md`, пункт `B4a`. Круг 1 — `AUDIT_NIGHT_B4A_2026-08-23.md` (`FAIL`, находка
`F-1`). Дата: 23.08.2026.

**Одной строкой.** `F-1` закрыт по существу, а не по границе: имя ядра теперь проходит через ту же
нормализацию, что и платное, поэтому активная клиника получает живой адрес при **любой** длине
названия, которую принимает регистрация — проверено моими значениями `1 / 119 / 120 / 121 / 199 / 200`,
эмодзи и обрамляющими пробелами. Три случая `404` не размылись, купленный бренд по-прежнему
побеждает, сверка происхождения цела. Семь посаженных поломок — семь пойманных. Один недосмотр в
доказательствах автора я нашёл и закрыл своим тестом: **вторая из двух строк фикса не была защищена
ни одним тестом** — полный набор автора оставался зелёным при её откате.

---

## 1. Что изменилось после круга 1

`apps/webapp/src/modules/org-branding/service.ts`:

- `normalizeDisplayNameOverride` → `normalizeDisplayName`, а дублирующий алиас
  `normalizePatientAppName` **удалён**: у платного и бесплатного пути теперь физически одна функция;
- `platformOnly` (`service.ts:162`) и ветка `applied` (`service.ts:238`) прогоняют
  `core.displayName` через неё же перед тем, как отдать в `effectiveDisplayName` /
  `effectivePatientAppName`.

Нормализация — усечение по `MAX_DISPLAY_NAME_LENGTH = 120` после `trim`, то есть ровно тот предел, на
котором `sanitizeEffectivePatientBrand` (`requestSurface.ts:92,94`) раньше отбрасывал бренд целиком и
превращал хост клиники в `404`. Продуктовый код изменён только в этом файле.

---

## 2. Что доказано

| Обязательство брифа | Способ (§24.4) | Итог |
| --- | --- | --- |
| Живой адрес при любой валидной длине названия — **своими значениями** | тест | **PASS** |
| Три случая `404` не размылись | тест | **PASS** |
| Нормализация одна, не две копии | взгляд | **PASS** (с рекомендацией `R2-3`) |
| Купленный бренд побеждает | тест | **PASS** |
| Сверка происхождения бренда цела | тест | **PASS** |
| Инъекции | 7 посажено / 7 убито / 0 не поймано | **PASS** |

### 2.1 Живой адрес при любой валидной длине — собранным швом, своими значениями

Проверял не ручными значениями с двух сторон, а собранным швом: реальный
`resolvePatientSubdomainOrganization` (B1) → реальный `createOrgBrandingService(…, 'anonymous')` (B4)
→ реальный `proxy` / `resolveRequestSurface` (B3). Заглушены только внешние границы (порт БД,
резолвер тарифа). Организации, метки и названия — свои
(`4a4a4a4a-…` / `5b5b5b5b-…`, «Я»/«Ж»/«Ю»/«Ц»/«Ш», метка `audit2`), автором не бравшиеся.

Автор брал `120 / 121 / 200`. Я взял другие и добавил классы, которых у него нет:

| Случай | Название | Результат |
| --- | --- | --- |
| нижняя граница | 1 знак | `200`, имя целиком |
| под порогом | **119** знаков | `200`, имя целиком |
| ровно порог | 120 знаков | `200`, имя целиком |
| порог + 1 | **121** знак | `200`, имя = первые 120 |
| почти максимум | **199** знаков | `200`, имя = первые 120 |
| максимум регистрации | ровно **200** знаков | `200`, имя = первые 120 |
| срез попадает на пробел | 200 знаков словами, `title[119] === ' '` | `200`, имя 119 знаков — хвостовой пробел срезан санитайзером |
| обрамляющие пробелы | сырое 210, значимых 200 | `200`, имя = 120 знаков без пробелов |
| эмодзи | `'🌿'.repeat(100)` — 200 UTF-16 единиц, срез рвёт суррогатную пару | `200`, поверхность цела |
| **ветка `applied`**: тариф есть, опубликована только палитра, имя ядра 200 знаков | `#0F766E` без имён | `200`, имя = первые 120, акцент купленный |
| купленное имя 199 знаков поверх ядра 200 знаков | | `200`, имя = первые 120 **купленного**, логотип на месте |

Последние две строки — не украшение. Ветка `applied` берёт имя ядра **другой строкой кода**
(`service.ts:238`), чем платформенный фолбэк (`service.ts:162`); её отдельно никто не проверял (см.
`R2-1`).

Значение проекции в двух случаях сверено точным равенством всего объекта, а не `toMatchObject`:
лишних полей, диагностик и плашек в нём нет.

### 2.2 Три случая `404` держатся

Каждый — своим прогоном на том же шве, с **длинным** названием, чтобы `404` не мог прийти от
нормализации:

| Случай оракула | Как воспроизведён | Результат |
| --- | --- | --- |
| метка неизвестна | `takoy-metki-net.therapygo.ru`, в каталоге название 200 знаков | `404`, заголовка поверхности нет |
| организация неактивна | `isActive: false`, полный доступ к механике, название 200 знаков | `404` |
| организация удалена | ядро исчезло между резолвом метки и чтением ядра (`getCoreContext → null`) | `404` |
| (сверх трёх) чужой бренд на хосте | валидный бренд организации `5b5b…` на хосте организации `4a4a…`, название 200 знаков | `404` |

Четвёртого достижимого `404` я не нашёл. Единственный оставшийся отказ проекции — название из одних
пробелов; он недостижим через продуктовый код, разбор в `R2-2`.

### 2.3 Нормализация одна (взгляд)

- Функция нормализации имени в продуктовом коде **одна**: `normalizeDisplayName`
  (`service.ts:134`). Её вызовы — `service.ts:162, 238, 239, 240, 330, 332, 333`; второй реализации
  усечения/`trim` имени в `apps/webapp/src` нет. Алиас `normalizePatientAppName`, который был вторым
  именем той же функции, коммитом удалён.
- Производитель анонимной проекции **один**: `anonymousPatientBrand` (`service.ts:174`); других мест,
  конструирующих `AnonymousPatientBrand`, нет — `requestSurface.ts:17` только реэкспортирует тип.
- Платный и бесплатный путь сходятся в одну функцию, но **выражение** фолбэка
  `normalizeDisplayName(core.displayName) ?? core.displayName` записано дважды (`162` и `238`) —
  семантика идентична, риск — расхождение при будущей правке. Рекомендация, не находка (`R2-3`).

### 2.4 Купленный бренд побеждает, происхождение сверяется

Купленное имя (199 знаков) вытесняет имя ядра (200 знаков) и само режется по 120; логотип и акцент
купленные, платформенный акцент не подставляется. Сверка происхождения
(`requestSurface.ts:158-162`) коммитом не тронута и краснеет от снятия (инъекция `I-D`).

---

## 3. Инъекции: посажено 7, убито 7, не поймано 0

Каждая — отдельный класс отказа, один раз на класс (§24.5). Все временные правки продуктового кода
откачены через `git checkout --`, дерево после прогонов чистое.

| # | Что сломано | Что покраснело | Итог |
| --- | --- | --- | --- |
| `I-A` | `platformOnly`: нормализация имени ядра убрана (возврат обхода) | 121 / 199 / 200 / эмодзи / пробелы — **6 из 16** моих проверок; `1`, `119`, `120` остались зелёными | убито (6 failed / 10 passed) |
| `I-B` | ветка `applied`: `coreDisplayName` = сырое имя ядра | «оплаченный тариф, опубликована только палитра, имя ядра 200 знаков» | убито (1 failed / 15 passed) |
| `I-C` | снят гейт неактивной организации (`if (!effective.core.isActive) return null`) | «неактивная организация с длинным названием остаётся `404`» | убито (1 failed / 15 passed) |
| `I-D` | снята сверка происхождения бренда (`requestSurface.ts:158-162`) | «бренд ЧУЖОЙ организации остаётся `404`» | убито (1 failed / 15 passed) |
| `I-E` | неизвестная метка резолвится в организацию вместо `NOT_FOUND` | «неизвестная метка остаётся `404`» | убито (1 failed / 15 passed) |
| `I-F` | предел санитайзера разведён с нормализацией (`120` → `100` в `requestSurface.ts`) | **10 из 16**, включая 119 и 120 | убито (10 failed / 6 passed) |
| `I-G` | отсутствующее ядро синтезирует контекст вместо `throw CORE_CONTEXT_UNAVAILABLE_ERROR` | «удалённая организация остаётся `404`» | убито (1 failed / 15 passed) |

`I-F` — не проверка утверждения теста, а проверка связи: она доказывает, что расхождение предела
санитайзера с пределом нормализации немедленно краснит набор, а не возвращает `404` тихо.

**Честно про ход работы:** первая попытка `I-E` была no-op — подставляемая строка не совпала с кодом,
и прогон «16 passed» ничего не значил. Заметил по `AssertionError` собственного скрипта, нашёл
настоящую строку (`patientSubdomainOrganization.ts:30`) и повторил инъекцию как надо.

---

## 4. `R2-1`. Вторая строка фикса не была защищена ни одним тестом (закрыто моим тестом)

Класс: отсутствие защиты от отката (§10a «конструкция → тест → защита от отката») на строке, которую
коммит и вводит.

**Замер, не суждение.** С откаченной нормализацией в ветке `applied` (`I-B`) полный набор автора —
`proxy.route.test.ts` + `org-branding/service.unit.test.ts` — остался **`85 passed`, ноль красных**.
То есть половина фикса `d41e5c1ec` не имела регрессионного покрытия: параметризация автора
`[120, 121, 200]` бьёт только по `sosny`, у которой механика брендинга выключена тарифом, а значит
идёт исключительно через `platformOnly`.

**Закрыто здесь же.** Аудиторский acceptance-тест написан один раз (§24.5) и добавлен в существующий
`describe` вместо нового файла (§11 «Файлы и дубли»):
«оставляет живой адрес, когда тариф есть и опубликована только палитра, а имя ядра длинное». После
добавления `I-B` краснит его, `I-A` краснит 4 проверки. Продуктовый код я не менял — по §24.6 аудитор
своего продуктового фикса не принимает; здесь его и не потребовалось: поведение уже верное,
недоставало только защиты.

Заодно в тот же `describe` добавлены мои границы (`119`, `199`) и класс с эмодзи.

---

## 5. Неблокирующее

### `R2-2` (вопрос владельцу, не работа). Фолбэк `?? core.displayName` возвращает ненормализованное значение

`normalizeDisplayName` отдаёт `null` в двух случаях: значение не строка и строка пустая после
`trim`. В обоих `?? core.displayName` возвращает сырое значение обратно, и санитайзер поверхности
отдаёт `404`. То есть **название из одних пробелов сегодня даёт `404`** — я это замерил, тест держит
цифру.

Достижимость проверена, а не предположена:

- запись идёт единственным живым путём — регистрация специалиста:
  `organizationTitle: z.string().trim().min(1).max(200)`
  (`app/api/auth/specialist-signup/start/route.ts:46`) → `normalizeTitle`
  (`organization-provisioning/service.ts:12`) → `INSERT INTO public.be_organizations`
  (`deploy/postgres/specialist-owner-provisioning-rls.sql:229`). Пустое имя не проходит;
- второй писатель `title` в коде — `upsertOrganization` (`infra/repos/pgBookingEngine.ts:735`),
  и его **не зовёт ни один route**: вхождения только порт, фасад сервиса и тесты. Валидации имени в
  фасаде нет — если этот путь когда-нибудь подключат к админ-UI без проверки, класс станет
  достижимым;
- данные DEV-базы: `5 orgs | empty=0 | over120=0 | maxlen=19`.

Вывод: сегодня это не четвёртый случай `404` — он недостижим. Пункта «ограничить/подставить название
организации» в плане владельца нет, поэтому работой не становится. Владельцу — одной строкой вместе с
границей записи (см. §7).

### `R2-3` (рекомендация). Предел `120` живёт в двух местах

`MAX_DISPLAY_NAME_LENGTH = 120` (`service.ts:96`, приватная для модуля) и литерал `120` в
`sanitizeEffectivePatientBrand` (`requestSurface.ts:92,94`). Одно и то же продуктовое правило,
две записи в разных модулях, связь держится только тестами (`I-F` это подтверждает). По принципу
владельца «один chokepoint» предел стоит экспортировать из `org-branding` и импортировать в
санитайзер — тогда расхождение станет невозможным по конструкции, а не только заметным. Правку не
делал: в плане владельца такого пункта нет.

Того же рода — дважды записанное выражение фолбэка (`service.ts:162` и `238`, см. §2.3).

### `R2-4` (уточнение к плану, для ведущего). Строка доказательства называет не тот прогон

В `IMPLEMENTATION_PLAN.md:683-685` написано: «`apps/webapp/src/proxy.route.test.ts` … целевой
прогон — `87 passed`». Замерено:

| Команда (из `apps/webapp`) | Результат |
| --- | --- |
| `npx vitest --run src/proxy.route.test.ts` (до моих тестов) | `77 passed (77)` |
| `+ src/modules/org-branding/service.unit.test.ts` | `85 passed (85)` |
| `+ src/modules/clinic-directory/patientSubdomainOrganization.unit.test.ts` | `87 passed (87)` |
| тот же набор из трёх файлов **после** моих тестов | **`91 passed (91)`** |

То есть `87` — это набор из трёх файлов, а не названный один файл. После добавления моих проверок
число — `91`. Строку доказательства правит ведущий вместе с галочкой (§24.7); сам план я не трогал.

### `R2-5` (перенос из круга 1, остаётся верным). `B4a` закрывает резолв, а не живой адрес

`proxy.ts:34` — `const NO_TENANT_SURFACE: TenantSurfaceLookup = async () => ({ status: 'unknown' })`,
и другого `TenantSurfaceLookup` в продуктовом коде по-прежнему нет. Сегодня `<slug>.therapygo.ru`
отдаёт `404` любой клинике, потому что метка вообще не резолвится. Оракул `B4a` прямо ставит проверку
«на резолве поверхности», поэтому объёму пункта это не противоречит, но живым адрес станет на `B5`.

### `R2-6` (процедурное). Галочку `B4a` по-прежнему перевернул исполнитель

`[x]` стоит с коммита `dd4fd3cae`; по §24.7 галочки ставит ведущий после собственной проверки
полноты. Сегодня требование по существу выполнено, так что это уже только порядок, а не неверная
отметка.

---

## 6. Прогоны

| Команда (из `apps/webapp`) | Результат |
| --- | --- |
| `npx vitest --run src/proxy.b4a-audit2.route.test.ts` (мой временный набор, 16 проверок) | `16 passed` |
| `npx vitest --run` ×3 файла (`proxy.route`, `org-branding/service.unit`, `clinic-directory/patientSubdomainOrganization.unit`) | **`91 passed (91)`** |
| `npx tsc --noEmit -p tsconfig.json` | `exit=0` |
| `npx eslint src/proxy.route.test.ts` | `exit=0` |

Уровень — targeted по §10: продуктовый код изменён в одном модуле, затронуты его прямые потребители;
полный `pnpm run ci` нового сигнала здесь не даёт, общий замок хоста не занимался.

Временный файл `src/proxy.b4a-audit2.route.test.ts` (собранный мной независимый шов для инъекций)
после прогонов удалён; постоянными остались три класса проверок, дописанные в существующий
`describe` `B4a` в `proxy.route.test.ts`.

---

## 7. Вопросы владельцу (одним листом, работой не становятся)

1. **Граница записи.** Регистрация принимает название до 200 знаков, а пациентская поверхность
   показывает 120 и молча режет остальное. Оставляем усечение на показе или ограничиваем название на
   вводе (тогда клиника видит предел в форме, а не потерянный хвост в адресе)? По брифу — открытый
   вопрос, не чинил.
2. **Пустое название.** Сегодня недостижимо (`R2-2`), но конструкция это допускает: подставлять
   «Организация», как уже делает кабинет пациента (`patient-organization/service.ts:46`), или
   оставить как есть?
3. **`B5`.** `B4a` закрыт на резолве; живым адрес клиники станет только после подключения боевого
   шва арендатора (`R2-5`).

---

## 8. НЕ СДЕЛАНО

- **Живая проверка в браузере не проводилась** — боевой шов арендатора заглушка (`R2-5`), поднять
  `<slug>.therapygo.ru` нечем. Всё доказано прогоном настоящего `proxy`.
- **Данные TEST-базы не смотрел** — TEST живёт на другом боксе; замер `empty=0 / over120=0 /
  maxlen=19` относится к DEV-базе (`bcb_webapp_dev`, 5 организаций).
- **Границу записи не чинил и `R2-3` не правил** — по брифу и §24.6 это вопросы владельцу и
  рекомендация, а не работа аудитора.
- **`B5`, `B5a`, `B8`** не проверялись: вне объёма `B4a`.
- **Продуктовый код не менялся** — ни строки; артефакт аудита состоит только из тестов и этого
  отчёта.
