PASS, FOR LAND

# Независимый аудит `F2` + `F2b`, круг 1 — 24.08.2026

Под аудитом: commit `6dc5b260f` (`fix(auth): gate OAuth and passkey by surface matrix`), ветка
`wt/therapysto-f2-20260824`, клон `/home/dev/dev-projects/bcb-wt-therapysto-f2-20260824`.
Уровень `app` (только `apps/webapp`), full CI не гонялся — repo-уровневых признаков нет.
Аудитор: Claude Opus 5, независимо от автора (Codex `gpt-5.6-terra`/high).

Оракул — `IMPLEMENTATION_PLAN.md`, `F2` и `F2b` дословно. Kill-set составлен по оракулу **до** чтения
реализации и авторских тестов; проверки написаны на собственных значениях, авторские фикстуры не
переиспользовались.

## Вердикт по пунктам

| ID | Вердикт | Evidence |
| --- | --- | --- |
| `F2` отказ OAuth на staff/admin на уровне резолвера, а не скрытием в UI | **PASS** | Свой поведенческий тест `apps/webapp/src/modules/auth/auditF2SurfaceMechanicGate.route.test.ts`: `POST /api/auth/oauth/start` с резолвнутой поверхностью `staff` → `501 oauth_disabled`, при этом спрошенный ключ — `auth_surface_staff_oauth_yandex_enabled`; та же сборка с поверхностью `patient_default` и включённым патиентским ключом отдаёт `200` и `oauth.yandex.ru`. То же для `platform_admin`+google. Callback: `POST /api/auth/oauth/callback/apple` на staff отвечает `307 …oauth_disabled` **до** разбора тела формы. Отказ приходит из настройки поверхности, не из UI и не из глобального тумблера. |
| `F2` включение настройкой возвращает путь в строй БЕЗ правки кода | **PASS** | Тот же файл: единственное изменение — `auth_surface_staff_oauth_yandex_enabled=true` в хранилище настроек → `POST /api/auth/oauth/start` на staff отдаёт `200` с `authUrl`. Код между двумя прогонами не менялся. |
| `F2` ничего не удалено: UI, start/callback, provider config | **PASS** (взгляд, теста нет) | `git show 6dc5b260f --diff-filter=D --stat` — пусто, ни одного удалённого файла; в diff только замены импорта/вызова. На месте: `app/api/auth/oauth/start`, все четыре callback + легаси `callback/route.ts` (делегирует в тот же гейтованный yandex-handler), `oauth/providers`, UI `app/app/settings/AuthProvidersSection.tsx` и `shared/ui/patient/auth/AuthFlowV2.tsx`, все геттеры кредов в `system-settings/integrationRuntime.ts`. |
| `F2b` passkey — механика матрицы, дефолт выключен у докторов | **PASS** | Дефолт: `SYSTEM_SETTING_REGISTRY.auth_surface_staff_passkey_enabled.defaultValue === 'false'` (реестр берёт значение из скомпилированной матрицы через `defaultSurfaceAuthControlEnabled`). Поведение: `POST /api/auth/passkey/login/options` на staff → `403 auth_method_disabled`, `beginPatientPasskeyAuthentication` не звался; на patient той же сборкой → `200`. Включение: только `auth_surface_staff_passkey_enabled=true` → staff получает `200`. |
| `F2b` код и маршруты passkey сохранены, PIN не вернулся | **PASS** (взгляд, теста нет) | Все пять маршрутов на месте и гейтованы одним и тем же вызовом: `passkey/login/options`, `login/verify`, `register/options`, `register/verify`, `credentials`. UI жив: `StaffPasskeySection.tsx`, `patient/profile/PasskeySection.tsx`, `admin/auth/PlatformAuthChannelPolicySection.tsx`. PIN: `rg -niE '\bpin[_ ]?(code|login|auth)\b|auth_pin'` по `src` (без тестов) — 0 совпадений. |
| Регрессия: вход больше ни у кого не изменился | **PASS** | Свои значения, не авторские: в матрице тронута ровно одна ячейка (`staff.enabledMethods` минус `passkey`). Проверено на дефолтах реестра — `staff_email=true`, `platform_admin_passkey=true`, `patient_email=true`, `patient_passkey=true`; OAuth у staff и platform_admin `false` — таким он был и до коммита, это значение поставил `F1`, `F2` его не менял. Отдельный тест на fail-closed: запрос без резолвнутой поверхности → `501`, а не проход. |

## Инъекции (§24.5, по одной на независимый класс поломки)

Посажено **6**, убито **6**, не поймано **0**. После каждой — `git checkout` затронутого файла;
продуктовое дерево чистое.

| # | Класс поломки | Результат |
| --- | --- | --- |
| I1 | снят гейт OAuth в `oauth/start` (`Promise.resolve(true)`) | red, 2 теста |
| I2 | общий резолвер потерял surface-awareness (жёстко `patient`) | red, 7 тестов |
| I3 | снят гейт passkey в `passkey/login/options` | red, 1 тест |
| I4 | матрица `staff.enabledMethods` снова с `passkey` | red, 1 тест |
| I5 | снят гейт в `oauth/callback/apple` | red, 1 тест |
| I6 | fail-open: нерезолвнутая поверхность считается разрешённой | red, 1 тест |

## Что проверено взглядом дополнительно

- **Границы брифа соблюдены**: `app.read_integrator_clinic_delivery_credential` и обе миграции
  `20260823T030000…`/`20260823T043206…` коммитом не тронуты (`git show --stat`).
- **Авторские тесты не ослаблены.** Правка `publicAuthPolicy.unit.test.ts` сужает инвариант `F1`
  («дефолты повторяют легаси») ровно на одну ячейку `staff.passkey` и продолжает утверждать все
  остальные; `independentAuthMethodToggle.route.test.ts` не потерял ни одного утверждения, а
  добавил `toHaveBeenCalledWith('passkey')`.
- **Обходных входов не осталось.** Прямые вызовы `isOAuthProviderEnabled`/`isIndependentAuthMethodEnabled`
  мимо `isAuthMechanicEnabled` остались только в проекциях для показа (`oauth/providers`,
  `publicAuthSnapshot`, `account/page.tsx`, `patient/profile/page.tsx`) — это чтение политики для
  отрисовки, а не вход. Все входные маршруты идут через гейт.
- **Вклад коммита по существу.** `isOAuthProviderEnabled`/`isIndependentAuthMethodEnabled` были
  surface-aware уже после `F1`; этот коммит добавляет (а) единый fail-closed chokepoint
  `isAuthMechanicEnabled` (отказ резолвера/настроек превращается в `false`, а не в 500) и
  (б) снимает `passkey` со staff в скомпилированной матрице. Заявка автора подтверждена.

## Owner question (не работа, §24.6)

**Раздел `1.2j` плана (владелец, 23.08) выводит `F2`/`F2b` из кодового объёма:** «`F2`, `F2b`, `F5`
кодом НЕ делаем: это значения переключателей, владелец выставляет их сам в настройках», и про passkey —
«и это я уже выключил». Коммит `6dc5b260f` всё-таки правит код (fail-closed gate) и скомпилированный
дефолт. Технически он корректен и ничего не вырезает, но это объём, который владелец снял.
Вопрос владельцу: оставить как есть или откатить до «только значения в настройках»?

**Практическое следствие того же:** гейт читает **сохранённое** значение настройки, а скомпилированная
матрица даёт только дефолт для отсутствующей строки. Миграция `F4` (`20260823T173446_split_auth_settings…`)
уже записала по 9 строк на поверхность на DEV — там `auth_surface_staff_passkey_enabled=true`
(скопировано с легаси). То есть на DEV/TEST/PROD докторский passkey останется включённым, пока владелец
не выключит его переключателем; «дефолт выключен» действует на чистой базе. Это согласуется с решением
владельца «выставляю сам», но на живых средах коммит фактического значения не меняет — знать об этом
надо до приёмки на TEST.

## Recommendation (не блокер)

При выключенном staff-passkey закрывается и маршрут `passkey/credentials` — доктор с уже
зарегистрированным ключом не сможет ни войти по нему, ни удалить его из кабинета, пока механика
выключена. Вход остаётся по паролю и коду на почту, поэтому это не блокер; если владелец захочет
«выключить вход, оставив управление ключами», это отдельная строка плана, а не правка сейчас.

## НЕ СДЕЛАНО

- Живая проверка на DEV/TEST (реальный HTTP через proxy с настоящим заголовком поверхности) — в бриф
  круга не входила и не проводилась; доказательства выше — маршрутные тесты уровня `app`.
- Full CI не гонялся (уровень `app`, признаков repo-уровня нет — §10 «Уровни»).
- `F2c`, `F3`, `F5` не проверялись: коммит их не трогает.
