# FIX_PLAN — этап 3 (профиль клиента)

## 1. Статус этапа

**Выполнен в основном:** в репозитории есть `InlineEditField`, `ProfileForm` с единым паттерном для ФИО и телефона, миграции **016** (`phone_challenges` OTP), **017** (email), **018** (channel link), API под `apps/webapp/src/app/api/auth/email/*`, `phone/*`, расширения каналов. Точное закрытие каждого подэтапа **3.3–3.5** (лимиты OTP, таймеры, все мессенджеры) требует построчной сверки с `PLAN.md` и тестами.

---

## 2. Найденные проблемы

### Критичные

Нет (по выборочному просмотру).

### Средние

| Файл / модуль | Суть | Шаг плана | Тип |
|---------------|------|-----------|-----|
| `apps/webapp/src/app/app/patient/profile/ProfileForm.tsx` | Телефон: для смены используется `BindPhoneBlock` в режиме раскрытия — соответствует **3.1**; для первичной привязки — переход по `Link` на `bind-phone` (допустимо, но должно быть явно согласовано с UX-паттерном «всё в профиле»). | 3.1 | фронт |
| Модули `auth` + миграции `016–019` | Большой объём логики; риск рассинхрона документации `PLAN.md` (примеры номеров миграций) с фактическими файлами — при найденных противоречиях обновлять только **описательные** блоки плана в отдельной задаче (не в этом файле). | 3.2–3.4 | документация / архитектура |

### Мелкие

| Файл / модуль | Суть | Шаг плана | Тип |
|---------------|------|-----------|-----|
| Тесты | Убедиться, что для `email/start|confirm` и OTP-лимитов есть покрытие `route.test.ts` и repo-тесты — в рамках этого аудита список тестов не выгружался полностью. | 3.2, 3.3 | тесты |

---

## 3. Пропущенные шаги

- Не подтверждено закрытие **всех** веток **3.4** (Max/VK), если в плане они обязательны для MVP — сверить с фактическими сценариями в `ConnectMessengersBlock` и integrator.

---

## 4. Лишние изменения

Не выявлены.

---

## 5. План исправлений

1. Пройти чеклист подэтапов **3.1–3.5** в `PLAN.md` и для каждого отметить файл-якорь в репозитории; зафиксировать расхождения в задаче.
2. **`ProfileForm.tsx`**: при необходимости единообразия — встроить первичную привязку телефона без ухода на отдельный URL (опционально, продуктовое решение).
3. **Тесты**: дополнить критичные ветки email/OTP, если `grep` по `*.test.ts` показывает пробелы рядом с `apps/webapp/src/app/api/auth/email`.
4. `pnpm run ci` после изменений.

---

*Аудит: `PLAN.md` этапа 3, `ProfileForm.tsx`, список `apps/webapp/migrations/016*.sql` … `019*.sql`, дерево `apps/webapp/src/app/api/auth`.*
