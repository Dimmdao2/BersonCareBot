# FIX_PLAN — этап 0 (Tailwind + shadcn)

## 1. Статус этапа

**Выполнен полностью** по факту репозитория: в `apps/webapp` есть `postcss.config.mjs`, `@import "tailwindcss"` в `src/app/globals.css`, `components.json`, `src/lib/utils.ts` (`cn`), набор `src/components/ui/*`, скрипт `analyze` и `@next/bundle-analyzer` в `package.json`.

**Документация:** чеклист в `PLAN.md` может оставаться с неотмеченными пунктами при уже сделанной установке.

---

## 2. Найденные проблемы

### Критичные

Нет.

### Средние

- `apps/webapp/src/app/globals.css` — большой слой legacy (`.button`, `.panel`, …) параллельно Tailwind; ожидаемо по плану 0, но техдолг миграции (тип: стили / фронт; шаги 0.3–0.4 по смыслу «не ломать старое»).

### Мелкие

- Сверка полного списка shadcn из подэтапа 0.4 с фактическим `src/components/ui/` при добавлении новых UI-фич.

---

## 3. Пропущенные шаги

Не выявлено по структуре файлов.

---

## 4. Лишние изменения

Не анализировались.

---

## 5. План исправлений

1. При редактировании `PLAN.md` этапа 0 — синхронизировать чеклист с репозиторием.
2. Постепенно переносить экраны с legacy-классов на Tailwind/shadcn в следующих этапах; при касании файла — убирать инлайн-стили.
3. `pnpm run ci` после изменений кода.

---

*Аудит статический: репозиторий + PLAN этапа 0.*
