# FIX_PLAN — этап 5

## 1. Статус этапа

Выполнен в значительной части: в `apps/webapp/src/app/api/auth/` есть маршруты check-phone, pin, messenger, oauth и потоки этапа 3. Полное соответствие 5.1-5.8 не подтверждалось построчно.

## 2. Найденные проблемы

### Критичные

Нет без полного security-ревью.

### Средние

- Рассинхрон номеров миграций в описании плана и файлов в `apps/webapp/migrations/`. Шаг: общий контекст 5. Тип: документация.
- Подэтапы 5.2, 5.5, 5.8: сверка с e2e. Тип: тесты.
- Подэтап 5.4 (integrator): не проверялся. Тип: backend.

### Мелкие

- Подэтап 5.7: сверить TTL с `apps/webapp/src/modules/auth/service.ts`. Тип: логика.

## 3. Пропущенные шаги

Матрица подэтапов 5.x и тестов может требовать заполнения.

## 4. Лишние изменения

Не анализировались.

## 5. План исправлений

1. Таблица соответствия ID 5.1-5.8 из `PLAN.md` к файлам и `*.test.ts`.
2. Zod на всех POST в `apps/webapp/src/app/api/auth/`.
3. Негативные кейсы для OAuth и PIN в `route.test.ts` где отсутствуют.
4. Обновить описательную документацию по миграциям без секретов.
5. `pnpm run ci` и e2e при изменениях auth.

---

Аудит: `PLAN.md` STAGE_05, листинг `apps/webapp/src/app/api/auth`.
