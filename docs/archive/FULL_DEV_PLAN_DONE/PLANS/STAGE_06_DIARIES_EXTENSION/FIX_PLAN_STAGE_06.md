# FIX_PLAN — этап 6

## 1. Статус этапа

Миграции `022_reference_tables_and_seed.sql`, `023_symptom_tracking_extension.sql`, `024_lfk_extension.sql` присутствуют в `apps/webapp/migrations/`. Полная сверка микрошагов 6.1-6.7 с DoD не выполнялась в этом аудите.

## 2. Найденные проблемы

### Критичные

Нет без полного прогона тестов.

### Средние

- Для каждого ID из DAG в `PLAN.md` этапа 6 подтвердить unit, route tests, e2e. Тип: тесты.
- Дублирование форм симптомов/ЛФК: вынести общее при рефакторинге. Тип: архитектура.

### Мелкие

- Удалить мертвый код в формах дневника после миграции стилей. Тип: мертвый код.

## 3. Пропущенные шаги

Подтверждение 6.5-6.7 UI без чтения всех `actions.ts` и компонентов.

## 4. Лишние изменения

Не анализировались.

## 5. План исправлений

1. Пройти DAG в `PLAN.md` этапа 6 сверху вниз; зафиксировать файл и тест на каждый ID.
2. Проверить `buildAppDeps.ts` на порты справочников и дневников.
3. Не менять миграции 001-021; только новые файлы при доработках.
4. `pnpm run ci` и `pnpm --dir apps/webapp run test:e2e`.

---

Аудит: `PLAN.md` STAGE_06, наличие 022-024 migrations.
