# FIX_PLAN — этап 7

## 1. Статус этапа

API `symptom-stats` и `lfk-stats` с `route.test.ts` есть в `apps/webapp/src/app/api/patient/diary/`. План 7.0 фиксирует recharts и индексы. Пункты 7.3-7.5 по UI/a11y не проверялись полностью в этом аудите.

## 2. Найденные проблемы

### Критичные

Нет при успешном CI.

### Средние

- 7.1.1: единый dynamic import recharts, без дублирования. Тип: фронт.
- Владение trackingId/complexId в API статистики; расширить тесты при изменениях. Тип: backend, логика.
- 7.5.2: навигация MiniStats -> дневник. Тип: фронт.

### Мелкие

- 7.3.3: a11y осей графиков. Тип: фронт.

## 3. Пропущенные шаги

Опциональный 7.1.2 ChartShell при дублировании оберток.

## 4. Лишние изменения

Не анализировались.

## 5. План исправлений

1. Grep по `modules/diaries/components` на прямой import recharts; унифицировать.
2. Проверить e2e на импорт страниц дневника и `routePaths`.
3. Дополнить тесты пустых данных при необходимости.
4. `pnpm run ci` и `test:e2e`.

---

Аудит: `PLAN.md` STAGE_07, файлы `symptom-stats`, `lfk-stats`.
