# Этап 1: Исправление критических багов

> Приоритет: P0 (первоочередной)
> Зависимости: Этап 0 (Tailwind + shadcn/ui уже установлены)
> Риск: низкий

## Важно

- **Tailwind + shadcn/ui уже установлены** (Этап 0). Все CSS-исправления делать через Tailwind-классы, не добавлять новые классы в `globals.css`.
- При касании компонента: по возможности переводить его стили на Tailwind.
- Перед пушем: `pnpm run ci`.

---

## Контекст для агента

**Перед началом работы** прочитать:

- `README.md` — запуск, команды, endpoints.
- `docs/ARCHITECTURE/SERVER CONVENTIONS.md` — порты, пути, env.
- `apps/webapp/ARCHITECTURE.md` — структура webapp.
- `docs/FULL_DEV_PLAN/ROADMAP.md` — общие правила и стек (секция «Ключевые правила для агентов»).

**Проект:** монорепо pnpm, два приложения:

- `apps/integrator` — Fastify, Telegram/Max/Rubitime webhooks, grammY.
- `apps/webapp` — Next.js 16, React 19, PostgreSQL через pg, Zod.

**Правила этапа 1:**

1. **НЕ рефакторить** код, не относящийся к багу. Минимальные, точечные изменения.
2. **Коммит после каждого подэтапа.** Одна задача = один коммит. Сообщение: `fix(webapp): <описание>` или `fix(integrator): <описание>`.
3. **Перед пушем:** `pnpm run ci` должен проходить.
4. **Если баг требует расследования** (1.2, 1.6) и причина непонятна после изучения кода — описать находки, предложить гипотезы, запросить логи/данные с сервера у владельца.

---

## Достаточно ли для автономного агента?

**Да для направления работ**, но часть пунктов **нельзя полностью верифицировать без прод/staging и реальных мессенджеров** (1.2, частично 1.3, 1.6). Ниже — якоря в коде и явные подсказки, чтобы агент не гадал пути и не противоречил фактической реализации.

### Обязательный порядок работы

1. Прочитать перечисленные в «Контекст» документы (хотя бы `apps/webapp/ARCHITECTURE.md` для путей).
2. Для каждого подэтапа открыть **все** перечисленные в нём файлы + блок «Якоря в репозитории» ниже.
3. После правок: `pnpm run ci` (как в корневом `package.json`).
4. Если критерий требует «мобильный браузер / Telegram / Max» — написать в отчёте, что проверено статически/локально, а что нужно человеку.

### Когда остановиться и запросить человека

| Подэтап | Без человека обычно не проверить |
|---------|----------------------------------|
| 1.2 | Реальный диалог с Max-ботом, webhook с прод-секретами, корректность deep-link в прод-конфиге |
| 1.3 | Поведение внутри Telegram/Max Mini App на устройстве |
| 1.4 | Физическая клавиатура на телефоне (можно частично эмулировать DevTools) |
| 1.6 | Долгая сессия «в течение дня», сценарии с несколькими вкладками |

### Якоря в репозитории (отправная точка)

| Задача | Файлы / модули |
|--------|----------------|
| Профиль, телефон | `apps/webapp/src/app/app/patient/profile/ProfileForm.tsx`, `.../profile/actions.ts`, страница привязки `.../patient/bind-phone/` |
| Max UI | `apps/webapp/src/shared/ui/ConnectMessengersBlock.tsx`; карточки каналов собираются выше по дереву (искать `channelCards` / preferences) |
| Max backend | `apps/integrator/src/integrations/max/webhook.ts`, `connector.ts`, `mapIn.ts`, сценарии: `apps/integrator/src/content/max/user/scripts.json`, `.../admin/scripts.json`, справка `max.md` |
| FAB «вопрос» | `apps/webapp/src/shared/ui/AskQuestionFAB.tsx`, вызов в `apps/webapp/src/shared/ui/AppShell.tsx` (проп `visible`) |
| Канал | `apps/webapp/src/modules/auth/channelContext.ts` (типы; фактическая установка контекста — искать по репо `ChannelContext` / `channelContext`) |
| Клиенты врача | `apps/webapp/src/app/app/doctor/clients/page.tsx`, `DoctorClientsPanel.tsx`, `ClientsFilters.tsx`, данные: `apps/webapp/src/infra/repos/pgDoctorClients.ts` |
| Сессия | `apps/webapp/src/modules/auth/service.ts` — `SESSION_COOKIE_NAME`, `SESSION_TTL_SECONDS` (сейчас **12 ч**), `apps/webapp/src/config/env.ts` |
| CMS | `apps/webapp/src/app/app/doctor/content/ContentForm.tsx`, `.../content/actions.ts`, домен `apps/webapp/src/modules/content-catalog/` |
| Статистика отмен | SQL: `apps/webapp/src/infra/repos/pgDoctorAppointments.ts` → `getAppointmentStats` (фильтр `status = 'canceled'`) |
| Дневник симптомов | `apps/webapp/src/app/app/patient/diary/symptoms/AddEntryForm.tsx` |

---

## Подэтап 1.1: Fix — кнопка «изменить» телефон перезагружает страницу

**Проблема:** при нажатии на кнопку «изменить» около телефона страница мигает (полная перезагрузка).

**Файлы:**

- `apps/webapp/src/app/app/patient/profile/ProfileForm.tsx`
- `apps/webapp/src/app/app/patient/profile/actions.ts` (server actions)
- при необходимости логика из `apps/webapp/src/app/app/patient/bind-phone/` (переиспользовать или вынести общее)

**Подсказка по коду:** сейчас «Изменить» у привязанного телефона — это **`Link` на `/app/patient/bind-phone?...`**, т.е. уход со страницы профиля (полная навигация). Исправление по смыслу плана: **не уводить пользователя**, а дать смену номера inline (toggle + тот же сценарий, что на bind-phone) или эквивалент без ощущения «перезагрузки» всего UI.

**Действия:**

1. Прочитать `ProfileForm.tsx`, найти обработчик кнопки «изменить» для телефона.
2. Проверить, не используется ли `<form>` без `event.preventDefault()` (если появится форма).
3. Превратить в клиентский toggle состояния (useState) для переключения view → edit.
4. Инпут должен содержать текущее значение по умолчанию.
5. Кнопка «сохранить» вызывает server action без перезагрузки (useFormState / useActionState / startTransition — без лишнего `router.refresh()`, если он даёт мигание).

**Тесты:**

- `pnpm run ci` проходит.
- На странице профиля нажатие «изменить» переключает поле в режим редактирования без перезагрузки.

---

## Подэтап 1.2: Fix — привязка Max не работает

**Проблема:** при нажатии «подключить Max» бот открывается, но привязка не происходит.

**Файлы:**

- `apps/webapp/src/shared/ui/ConnectMessengersBlock.tsx`
- `apps/integrator/src/integrations/max/webhook.ts`
- `apps/integrator/src/integrations/max/connector.ts`
- Сценарии Max: `apps/integrator/src/content/max/user/scripts.json`, при необходимости `.../admin/scripts.json`
- Конфиг и переменные: `apps/integrator/src/integrations/max/config.ts`, `.env.example` в этой папке
- Справка по интеграции: `apps/integrator/src/integrations/max/max.md`

**Действия:**

1. Проверить URL для открытия Max-бота (правильный ли deep-link): откуда берётся `openUrl` для карточки Max (данные канала — искать по репо `openUrl`, `ChannelCard`, API меню/настроек).
2. Проверить, что Max webhook получает событие при `/start`.
3. Проверить `connector.ts` / `mapIn.ts` — маппинг входящего сообщения и идентификаторов.
4. Проверить сценарий привязки в `scripts.json` для Max (ветки пользователя).
5. Сравнить с рабочим flow для Telegram (`apps/integrator/src/integrations/telegram/` или аналог в репо).
6. Исправить причину (вероятно: неправильный deep-link URL или отсутствующий сценарий привязки).

**Выход, если упёрлись:** зафиксировать в отчёте цепочку «клик → URL → сообщение в Max → webhook → БД» и запросить у владельца **фактический** `openUrl` с прод/стенда и лог строки webhook (без секретов в репозитории).

**Тесты:**

- `pnpm run ci` проходит.
- При нажатии «подключить Max» бот открывается, привязка выполняется, статус обновляется.

---

## Подэтап 1.3: Fix — кнопка «задать вопрос» — логика видимости

**Проблема:** кнопка пропадает после привязки мессенджера. Мигает при перезагрузке.

**Файлы:**

- `apps/webapp/src/shared/ui/AskQuestionFAB.tsx`
- `apps/webapp/src/modules/auth/channelContext.ts`
- **Родитель:** `apps/webapp/src/shared/ui/AppShell.tsx` (здесь задаётся проп `visible` для FAB)

**Подсказка по коду:** в `AppShell` FAB сейчас получает проп **напрямую от наличия пользователя** (`visible={user !== null}`), а не от `channelContext`. Перед правками **сверить** продуктовое требование этого подэтапа с текущей логикой: возможно, нужно комбинировать «залогинен» + «не мини-приложение» + опционально данные канала.

**Действия:**

1. Прочитать `AskQuestionFAB.tsx` и `AppShell.tsx` — определить текущее условие показа и источник мигания (SSR vs client, смена пропа после `useEffect`).
2. Изменить логику показа: **не в мессенджере** (например, нет `window.Telegram?.WebApp` и аналога Max) — кнопка доступна; в Mini App — скрыта. Уточнить роль `channelContext.ts` (типы/привязка) — использовать, если есть готовое определение «в канале».
3. Чтобы убрать мигание: держать узел в DOM и переключать **`opacity` + `pointer-events` + `visibility`** (или аналог) вместо кратковременного `display: none` ↔ появление; либо задать начальное состояние до гидрации так, чтобы не было скачка.
4. Проверить, что контекст канала / флаги Mini App не дублируются во вредоносные cookie (если применимо).

**Тесты:**

- В браузере (без мессенджера): кнопка видна, не мигает при загрузке.
- В мессенджере (Telegram/Max Mini App): кнопка скрыта.

---

## Подэтап 1.4: Fix — поиск клиентов сбрасывает клавиатуру

**Проблема:** при вводе в поле поиска на странице клиентов (doctor) после каждой буквы клавиатура на мобильном закрывается.

**Файлы:**

- `apps/webapp/src/app/app/doctor/clients/DoctorClientsPanel.tsx` (раньше — `ClientsSearchBar.tsx`)
- `apps/webapp/src/app/app/doctor/clients/page.tsx`

**Подсказка по коду (важно):** в `page.tsx` у `ClientsSearchBar` стоит **`key={q ?? ""}`**, где `q` из `searchParams`. При debounced обновлении URL **`q` меняется на каждый ввод** → React **пересоздаёт** весь `ClientsSearchBar` → поле ввода теряет фокус (на мобильном «сбрасывается клавиатура»). Агенту: убрать эту привязку `key` к запросу или сделать ключ стабильным; плюс реализовать план ниже.

**Действия:**

1. Определить причину: при вводе выполняется server-side rerender через `searchParams` / `router.push`, что пересоздаёт input (см. подсказку выше).
2. **Решение A (предпочтительное):** сделать фильтрацию client-side:
  - Загрузить полный список клиентов на страницу.
  - Фильтровать в client-компоненте через `useState` + `useMemo`.
  - Поиск начинать только после 3 символов.
3. **Решение B:** если client-side невозможно из-за объёма данных — debounce 500ms на input перед отправкой запроса.
4. Убедиться, что `<input>` не пересоздаётся (key стабилен).

**Тесты:**

- На мобильном: клавиатура не сбрасывается при вводе.
- Фильтрация работает корректно после 3 символов.

---

## Подэтап 1.5: Fix — фильтры клиентов глючат

**Проблема:** фильтры «с Телеграм», «с Max», «есть запись» работают нестабильно, сбрасываются.

**Файлы:**

- `apps/webapp/src/app/app/doctor/clients/ClientsFilters.tsx`
- Загрузка списка: `apps/webapp/src/infra/repos/pgDoctorClients.ts` (`listClients`), порт/сервис `apps/webapp/src/modules/doctor-clients/`

**Действия:**

1. Прочитать `ClientsFilters.tsx` и `page.tsx` — как `defaults` и ссылки передаются из `searchParams`.
2. Вероятная причина: фильтры через URL `searchParams`; при обновлении одного параметра теряются остальные (или сбрасывается `q`).
3. Переписать на управляемое состояние (useState / общий объект в клиентской обёртке) с **мержем** всех фильтров и поиска при каждом изменении.
4. Фильтрация — client-side (если данные уже на странице) или server-side с **одной** сериализацией query-string (все флаги и `q` вместе).
5. Проверить фильтр «есть запись» в SQL: `hasUpcomingAppointment` / join с `appointment_records` — сверить с `pgDoctorClients.ts`.

**Тесты:**

- Каждый фильтр можно включить/выключить независимо.
- Комбинации фильтров работают корректно.
- Фильтр «есть запись» показывает только пользователей с `appointment_records`.

---

## Подэтап 1.6: Fix — авторизация доктора слетает

**Проблема:** сессия доктора периодически сбрасывается.

**Файлы:**

- `apps/webapp/src/modules/auth/service.ts`
- `apps/webapp/src/config/env.ts`

**Подсказка по коду:** в `service.ts` заданы **`SESSION_COOKIE_NAME = "bersoncare_webapp_session"`** и **`SESSION_TTL_SECONDS = 60 * 60 * 12`**. Продление при активности нужно искать по репо (`expiresAt`, `refresh`, `setCookie`, `decodeSession`).

**Действия:**

1. Проверить TTL сессии (**сейчас 12 ч**) — для доктора может быть мало.
2. Проверить, не истекает ли cookie раньше (Max-Age / Expires) и совпадает ли с `expiresAt` в payload сессии.
3. Проверить, корректно ли работает продление сессии при активности (если отсутствует — добавить).
4. Если TTL = 12 ч: увеличить до 7 дней **для роли doctor** или добавить механизм refresh (не ломая пациентов — отдельное решение по ролям).
5. Добавить структурное логирование при отклонении сессии (истёк срок / неверная подпись / пустой cookie) — **без PII** в прод-логах.

**Тесты:**

- После входа доктором сессия не слетает в течение рабочего дня.
- При активности сессия продляется.
- Юнит-тесты на `decodeSession` / обновление cookie — по возможности.

---

## Подэтап 1.7: Fix — CMS-страницы не сохраняются

**Проблема:** при создании/редактировании контента в кабинете доктора страницы не сохраняются.

**Файлы:**

- `apps/webapp/src/app/app/doctor/content/ContentForm.tsx`
- `apps/webapp/src/modules/content-catalog/`
- Действия: `apps/webapp/src/app/app/doctor/content/actions.ts`

**Действия:**

1. Прочитать `ContentForm.tsx` и `content/actions.ts`, проследить вызов `deps.contentPages.upsert` / аналог.
2. Проверить SQL-запрос INSERT/UPDATE в репозитории content pages (внутри `content-catalog` или `infra/repos`).
3. Проверить валидацию (Zod schema) — возможно, отклоняет данные.
4. Добавить обработку ошибок и отображение в UI (toast / текст под формой).
5. Проверить, что раздел (section) и slug корректно формируются и не перезаписывают чужие строки.

**Тесты:**

- Создание новой страницы → страница появляется в списке.
- Редактирование → изменения сохраняются.
- Ошибки валидации отображаются пользователю.

---

## Подэтап 1.8: Fix — статистика — неверный подсчёт отмен

**Проблема:** удалённые записи (remove_event_record) считаются как отмены.

**Файлы:**

- `apps/webapp/src/modules/doctor-stats/`
- SQL: **`apps/webapp/src/infra/repos/pgDoctorAppointments.ts`** — метод `getAppointmentStats` (сейчас отмены: `COUNT(*) FILTER (WHERE status = 'canceled')`).

**Действия:**

1. Найти запрос подсчёта отмен (см. файл выше) и схему `appointment_records` (миграции или `grep` по репо).
2. Проверить доменную модель: удаление записи (`remove_event_record` / `last_event`) — какой **`status`** в БД; не попадает ли оно в `status = 'canceled'`.
3. Добавить фильтр: исключить удалённые записи из метрики «отмены» (по `last_event`, отдельному полю или статусу `removed`).
4. При необходимости завести явный статус `removed` или документировать различие в коде.

**Тесты:**

- Юнит-тест на агрегат с фикстурами строк `appointment_records` (если есть ин-мемори порт — расширить).
- Статистика отмен не включает удалённые записи.
- Общее число записей за период корректно.

---

## Подэтап 1.9: Fix — дневник симптомов — кнопки

**Проблема:** кнопки выбора балла не круглые, не подсвечиваются цветом при выборе.

**Файлы:**

- `apps/webapp/src/app/app/patient/diary/symptoms/AddEntryForm.tsx`

**Действия:**

1. Прочитать AddEntryForm.tsx — найти рендер кнопок 0–10.
2. Стилизовать через Tailwind: `rounded-full w-9 h-9 aspect-square` (идеальный круг).
3. Цвета через inline style или Tailwind arbitrary values — градиент от зелёного к бордовому:
  - 0: `hsl(120, 60%, 40%)` (зелёный)
  - 5: `hsl(45, 80%, 50%)` (жёлтый)
  - 10: `hsl(0, 70%, 35%)` (тёмно-бордовый)
  - Вычислять через функцию `getScoreColor(score)` → HSL.
4. При выборе (active): фон = цвет балла, текст белый (`text-white`).
5. Без выбора: обводка цветом (`border-2`), фон прозрачный.
6. Удалить старые CSS-классы для этих кнопок из `globals.css` если есть (это **исключение** к правилу этапа «не раздувать globals» — только удаление мёртвых правил после переноса на Tailwind).

**Тесты:**

- Кнопки — идеальные круги.
- При нажатии: фон окрашивается в цвет от зелёного (0) до бордового (10).
- Выбранная кнопка визуально отличается.

---

## Общий критерий завершения этапа 1

- Все 9 багов исправлены.
- `pnpm run ci` проходит без ошибок.
- Визуальная проверка: профиль, дневник, список клиентов (doctor), статистика.
- Нет регрессий в существующих страницах.

### Отчёт агента (рекомендуется)

Кратко: что изменено по файлам; для 1.2 / 1.6 — что проверено локально и что осталось на стороне владельца (логи, прод-URL).

