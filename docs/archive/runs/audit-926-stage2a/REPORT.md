# Слепой аудит этапа 2a #926 — `auditor-live`

Ветка `wt/clinic-specialist-page-926`, аудируемый коммит `fc2361bb8`.
Плановый authority: `docs/_TODO/CLINIC_PUBLIC_PAGE_AND_URL_FLIP_2026-08-19.md` §17.2 — 17.G,
17.B (в части специалистов), 17.H (в части специалиста).

Слепой kill-set — [`KILLSET-blind.md`](KILLSET-blind.md), составлен по канону и плану ДО `git show`
и ДО чтения тестов. 23 названные поломки.

## Итог по kill-set

| | |
|---|---|
| убито конструкцией | 19 |
| убито существующим тестом | 1 (K11 — маршрут не имеет второй ветки; `clinicCardMedia.route.test.ts`) |
| убито новым тестом | 3 (K1, K2, K3 — предикаты публикации и организации в теле двери; плюс состав набора медиа) |
| **НЕ УБИТО** | **0** |

Ни одна названная поломка не осталась непойманной. Новый тест — один файл,
`deploy/postgres/privileges/clinic-public-card-specialists.devDbProof.test.mjs`, opt-in, в CI не идёт.

## Fault injection — «что сломал → что покраснело»

Все семь инъекций внесены в тело `app.read_public_clinic_card(text)` на живой DEV, после каждой
дверь возвращена и сверена `diff` с сохранённым `pg_get_functiondef` — побайтно.

| Поломка в продуктовом коде | Покрасневшее утверждение |
|---|---|
| снят `specialist.card_is_published` в списке специалистов | «клиника не публиковала этого человека, а дверь его отдала» |
| снят `specialist.organization_id = entry.organization_id` в списке | «на визитке клиники оказался специалист ЧУЖОЙ организации» |
| снят `avatar.organization_id = entry.organization_id` в джойне аватара | «дверь отдала аватаром файл ЧУЖОЙ организации» |
| снят `embedded.organization_id = entry.organization_id` в ветке markdown | «файл ЧУЖОЙ организации попал в публичный набор этой клиники» |
| снят `card_is_published` в ветке аватаров набора медиа | «аватар неопубликованного специалиста попал в публичный набор» |
| снят `card_is_published` в ветке markdown набора медиа | «файл из описания НЕопубликованного специалиста попал в публичный набор» |
| снят `embedded.status = 'ready'` в ветке markdown | «неготовый файл попал в публичный набор» |

Сохранённые тесты, проверенные лично:

| Поломка | Покрасневшее утверждение |
|---|---|
| `resolvePublicCardMedia` переведена с членства в наборе на «файл этой клиники» (`service.ts`) | `clinicCardMedia.route.test.ts`: «файл той же клиники, которого нет в карточке, не отдаётся» + «файл чужой клиники не отдаётся» |
| `ALTER FUNCTION app.read_public_clinic_card OWNER TO app_seam_public_slug_owner` | `clinicPublicCard.devDbProof.test.ts`: `expected 'app_seam_public_slug_owner' to be 'app_seam_public_clinic_card_owner'` |

Первая инъекция в фикстуру теста НЕ покрасила ничего (снятый `card_is_published` в ветке markdown):
у неопубликованного специалиста в фикстуре не было описания, и красить было нечем. Фикстура
исправлена — у него появилось описание со ссылкой на свой готовый файл, — после чего та же инъекция
красит. Записано прямо, потому что это и есть цена слепого протокола: непокрасившая инъекция
означает дыру в проверке, а не исправность кода.

Продуктовый код после инъекций возвращён: `git diff HEAD` по `apps/` и `deploy/` пуст.

## Живое доказательство на DEV (`bcb_webapp_dev`)

TEST и PROD не трогались. Все данные фикстуры сняты; финальная сверка — счётчики шести таблиц,
`be_specialists` построчно и `pg_get_functiondef` двери совпадают с состоянием до аудита побайтно.

Сводка первого прогона (настоящая анонимная роль через `app.read_public_clinic_card`):

```
специалисты в ответе двери: опубликованный+активный — ЕСТЬ
                            неопубликованный, неактивный, чужой — НЕТ
страница специалиста:       неопубликованный / неактивный / чужой / несуществующий → null (404)
                            свой специалист по ЧУЖОМУ слагу → null; чужой по своему → null
набор медиа:                аватар опубликованного + файл из его описания — ЕСТЬ
                            файл организации вне набора, аватар неопубликованного,
                            аватар неактивного, неготовый файл, файл чужой организации — НЕТ
аватар = uuid ЧУЖОГО файла: avatarMediaId вернулся null, файл в набор не попал
выключатели:                card_is_published=false | is_published=false | организация неактивна
                            → карточка, страница специалиста и отдача медиа гаснут все три разом
```

Гранты, живая интроспекция: у `app_seam_public_clinic_card_owner` табличного `SELECT` на
`public.be_specialists` НЕТ (`has_table_privilege` = false), колоночный — ровно девять объявленных
колонок; `select created_at` от имени шва даёт `permission denied for table be_specialists`.
`generate-cli --check` — побайтное совпадение по трём базам; `--census` зелёный.

## Вердикты

- **17.G (отдельная публичная страница специалиста) — PASS.**
- **17.B в части специалистов (превью на визитке) — PASS.**
- **17.H в части специалиста (аватар, короткое и полное описание) — PASS.**

## НЕ СДЕЛАНО

- Живой UI на `:5200` не смотрел — §1a запрещает второй `next dev`, живая приёмка после приземления
  и это роль ведущего.
- Предсуществующие 6 падений `pnpm test:db-privileges` не чинил. Проверено: тот же самый набор из
  шести падает и на декларации `feat/doctor-ui-rebuild`.
- Этап 2b (услуги на визитке, параметр `specialist` в мастере, описание самой КЛИНИКИ, корень
  клиники) не проверялся — вне authority.
