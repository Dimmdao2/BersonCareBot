# Общий taskdb — справка

Правила использования находятся только в `AGENTS.md` §1. Интерфейс порта:

```bash
node /home/dev/brain/tools/taskdb.mjs --help
```

Проект BersonCareBot: `bcb`. Ограничения доступа и правила постановки — в `AGENTS.md` §1.
