# Orchestration bindings — BersonCare (привязки этого репо к generic-канону)

> **Generic-мастер канона** (метод + роль-промпты) живёт в `/home/dev/brain/host-orch/` (проект-агностичный shared-дом, НЕ в этом репо). Этот файл — тонкий указатель + привязки именно BersonCare. Другие репо делают свой такой файл со своими значениями.
>
> `/home/dev/orch/` на текущем сервере не является актуальным источником канона. `/home/dev/brain/orch/` — это cron/index/runtime-скрипты brain, не host-orch master.

## Мастер (читать оттуда)
- Метод: `/home/dev/brain/host-orch/round3/AGENT_AUTORUN_SCHEME.v3.md` (⇄ копия в `docs/AGENT_AUTORUN_SCHEME.md`)
- Роль-промпты: `/home/dev/brain/host-orch/roles/ROLE_PROMPTS_v3.md`
- Изменения/история: `/home/dev/brain/host-orch/CANON_v3.1_CHANGELOG.md`

## Привязки BersonCare (значения плейсхолдеров мастера)
| Плейсхолдер | Значение для BersonCare |
|---|---|
| `{REPO_ROOT}` | `/home/dev/dev-projects/BersonCareBot` (главный интеграционный worktree на `feat/doctor-ui-rebuild`). Текущая сессия может работать в изолированном worktree, например `/home/dev/dev-projects/bcb-walls`; всегда проверять `pwd`, `git status -sb`, `git worktree list`. |
| `{MAIN_BRANCH}` (интеграционная) | `feat/doctor-ui-rebuild` (⛔ НЕ `main` без команды владельца) |
| `{SERVER}` | `http://127.0.0.1:5200` (один постоянный Next dev на feat) |
| `{DEV_LOGIN}` | `curl -s -c /tmp/r3.cookies -L "{SERVER}/api/auth/dev-bypass?token=dev%3Adoctor&next=/app/doctor"` (admin: `dev%3Aadmin`) |
| `{SEAL_LEDGER}` | `/home/dev/brain/host-orch/round3/SEAL_LEDGER.md` (+ `/home/dev/brain/host-orch/round3/verify-seals.sh`) |
| `{RUN_TESTS}` | Прямой запуск нужной команды (`pnpm ...`) разрешён; `/home/dev/brain/host-orch/run-tests.sh "<cmd>"` только опциональный throttling/fallback для тяжёлых прогонов, если нужен mutex |
| `{SCREENSHOTS}` | В интеграционном worktree: `{REPO_ROOT}/.claude/screenshots/<ITEM>/` (owner-facing история). В изолированном worktree каталог может отсутствовать; создавать его только для визуального acceptance или указывать явный screenshot path в плане. |
| `{QUEUE}` / `{STATUS}` / `{ESCALATIONS}` | `/home/dev/brain/host-orch/round3/{QUEUE.md, STATUS.json, ESCALATIONS.md}` |
| `{ETALON_UI}` | страница упражнений врача + `apps/webapp/src/shared/ui/doctor/*` |
| `{RULES}` | `AGENTS.md` + `.cursor/rules/*` (clean-arch, drizzle-only, no-dup) |
| `{HEARTBEAT}` | Legacy host-orch heartbeat: `{REPO_ROOT}/.r3-heartbeat` в интеграционном worktree. В изолированном Codex-worktree может отсутствовать; не считать это ошибкой локального этапа. |
| `{CODE_SEARCH}` | точное/лексическое: `node /home/dev/brain/tools/code-search.mjs "<query>" --repo bcb [-k N]`; смысловое: `bash /home/dev/brain/tools/codeq.sh "<query>" --repo bcb [--k N]` |
| `{TASKDB}` | `node /home/dev/brain/tools/taskdb.mjs <cmd>`; не писать в taskdb сырым SQL |
| `{SUBAGENTS}` | В Codex использовать MCP/tool `multi_agent_v1.spawn_agent` / `wait_agent` при доступности. Актуальные model overrides: `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5`, `gpt-5.4`; старые формы `gpt-5-5` и `gpt-5-6-sol` не использовать. Host legacy fallback — `/home/dev/brain/tools/lead-delegate.mjs`, только если работа идёт через host-orch loop. |
| `{ORCH_VERIFY}` | `node /home/dev/brain/tools/orch-verify.mjs <TID> [--dry]` для независимой проверки taskdb-задачи, где применимо |
| `{CRON}` | только `node /home/dev/brain/tools/cronport.mjs ...`; прямой `crontab` запрещён |
| `{VALIDATION}` | Полноценные локальные проверки доступны напрямую через `pnpm`: targeted tests, `pnpm run typecheck`, `pnpm run lint`, affected builds и `pnpm run ci` для крупных/final gate. `run-tests.sh` — только optional mutex/throttling, не обязательный wrapper. |
| `{PROMPT_CONTEXT}` | В промпт исполнителю/аудитору давать не только план: `AGENTS.md`, `.cursor/rules/*`, релевантные `.claude/*`/`.codex/*` если есть, `docs/README.md`, server conventions, architecture docs, module-local `*.md`, initiative/audit/roadmap docs по теме. |

## Особенности репо
- Главный worktree `/home/dev/dev-projects/BersonCareBot` — интеграционный и может быть эксклюзивно за контролёром; воркеры лупа и другие чаты работают в своих изолированных worktree. Для локальных правок использовать фактический текущий worktree, а не слепо `{REPO_ROOT}`.
- Прод: `main` = автодеплой ОТКЛЮЧЁН (ручной), Telegram-интегратор в `long_polling` для РУ-сервера. feat в main не мержим без команды владельца.
- Текущий dev/backup push разрешён в рабочую ветку, если это не `main`/`test` и владелец не запретил. Перед коммитом проверять чужие незакоммиченные изменения и не трогать их.

## Обязательная схема исполнения плана

Эта схема применяется к планам в `docs/_TODO/**`, если владелец не дал более узкое указание. Цель оркестратора — довести работу по плану до конца и сдать её по главному финальному чек-листу, а не просто запустить несколько агентов.

### Единица прохода
- Один исполнительский проход = один нормальный этап плана: не микрослайс на один мелкий фикс и не огромная фаза, которую нельзя полноценно проверить.
- У каждого этапа должен быть свой чек-лист. Если чек-листа нет, оркестратор сначала добавляет или требует добавить его в план, иначе исполнителю нечего сдавать, а аудитору нечего проверять.
- После каждого второго этапа оркестратор запускает быстрого аудитора процесса с вопросом: "Корректно ли идёт оркестрация?". Этому аудитору обязательно дать перечитать этот документ и текущий план.

### Цикл этапа
1. **Исполнение:** запустить субагента `worker` через `multi_agent_v1.spawn_agent` на один полноценный проход по выбранному этапу. По умолчанию model override не задавать; для сложных этапов можно явно выбрать `gpt-5.5`. В промпте дать ссылки на план, чек-лист этапа, финальный чек-лист, `AGENTS.md`, `.cursor/rules/*`, `.claude/*` при наличии, `.codex/*` при наличии и этот документ. Исполнитель обязан вести лог, фиксировать проверенные пункты чек-листа и не расширять scope без явного разрешения.
2. **Аудит:** запустить отдельного независимого агента на аудит результата по чек-листу этапа и фактическому коду. Аудит должен быть не поверхностным: трассировка кода, проверка запросов/границ/тестов/миграций/доков по смыслу этапа. Если этап сложный, связан с безопасностью, tenant-isolation, миграциями, интеграциями или есть сомнение в полноте проверки — запускать `gpt-5.6-sol` с повышенным reasoning effort.
3. **Фиксы и тесты:** запустить агента на исправления по аудиту и релевантные проверки. Простые фиксы — `gpt-5.4` или `gpt-5.6-luna`; средние и крупные — `gpt-5.5` или `gpt-5.6-terra`; для сложных security/tenant этапов — `gpt-5.6-sol`. Агент фиксит конкретные замечания, сдаёт обновлённый чек-лист и результаты тестов.
4. **Модель выбирается вместе со scope:** сильную/дорогую модель нельзя тратить на узкий patch по одному finding. Узкий локализованный фикс делает текущий оркестратор или дешёвый fixer (`gpt-5.4` / `gpt-5.6-luna`). Если зовём `gpt-5.6-sol`, ему дают цельный stage/convergence scope: перечитать план, текущий diff, все audit findings, найти соседние причины, исправить систему целиком и сдать проверки. Повышенный reasoning effort не использовать как замену нормальной постановке задачи.
5. **Эскалация после повторного провала:** если два coder/fixer-прохода подряд не закрыли один и тот же этап, класс замечаний или audit-fail, оркестратор не запускает третий такой же узкий круг и не подменяет ремонт запуском "ещё одного аудитора". Сначала расширить постановку до цельного stage/convergence scope. Дальше лестница: `gpt-5.6-sol` на широкий convergence; если Sol не справился или недоступен — другой сильный исполнитель как свежий взгляд; если и это не закрыло проблему — полностью остановиться, записать blocked note с путями к логам/аудитам и ждать владельца.

### Обязанности оркестратора
- Писать точные промпты: что делать, где план, где чек-листы, какие правила читать, что считать готовым, куда писать лог, какие проверки запустить.
- Выбирать модель и reasoning effort оптимально под задачу. `xhigh` не запрещён, но не должен быть дефолтом "на всякий случай": обычно достаточно среднего или высокого effort в зависимости от риска, размера и неясности этапа.
- Контролировать, что агенты пишут лог, не уходят от темы, не зависают и не остаются зомби-процессами или забытыми сессиями.
- Проверять, что исполнитель сдал работу по чек-листу этапа, а аудитор проверил этот же чек-лист в коде и артефактах, а не только по словам исполнителя.
- Не допускать накопления трёх и более незамердженных и забытых worktree. Перед новым этапом проверять активные worktree/сессии; после завершения этапа мержить нужное, удалять ненужное, реапить процессы.
- Держать коммиты небольшими и осмысленными, регулярно бэкапить ветку пушем в интеграционную/текущую рабочую ветку. `main` и `test` не трогать без прямой команды владельца.
- Не гонять full CI на каждый мелкий чих, но и не давать накопиться длинной цепочке изменений без полного прогона. Практическое правило: targeted tests на каждом этапе, full CI на крупных гейтах, перед финальной сдачей и когда накопился существенный пакет коммитов.
- Если полный или длинный тестовый прогон упал в середине, а предыдущие шаги этого же прогона точно уже прошли на том же коде, после локального фикса можно начинать с релевантной упавшей части, а не обязательно с самого начала. Перед финальной сдачей всё равно нужен достаточный полный gate по риску плана.

### Очистка зомби-субагентов
- Если UI/лимит показывает зависшие Codex-субагенты, а `wait_agent` уже вернул по ним `completed`/`failed` или они явно больше не участвуют в текущем этапе, чистить их через CLI, а не оставлять "до потом".
- Сначала проверить форму команды: `codex delete --help`. Для точечных завершённых сессий использовать только точные UUID:
  ```
  codex delete --force <subagent-session-uuid>
  ```
- Удалять только известные subagent-session UUID текущей оркестрации. Не удалять текущую основную Codex-сессию, app-server/proxy/code-mode-host процессы и чужие процессы из других проектов без отдельного явного решения владельца.
- После удаления проверить, что не остались рабочие процессы по текущему плану:
  ```
  ps -eo pid,ppid,stat,lstart,cmd | rg 'codex|multi_agent|subagent|<plan-key>|vitest|tsx'
  ```
  В этой проверке отличать системные процессы Codex Desktop (`app-server`, `proxy`, `codex-code-mode-host`) от реально запущенных worker/audit/fixer процессов.
- Если `rg` по `.codex` всё ещё находит UUID только в основном логе текущей сессии, это не означает живую subagent-сессию: логи родительского чата сохраняют исторические упоминания. Живой проблемой считать отдельную сохранённую сессию или процесс.
- Результат очистки обязательно записать в taskdb через `{TASKDB}`: какие UUID удалены, что процессы проверены, запускались ли новые агенты после очистки.

### Минимальный отчёт агента
Каждый исполнитель, аудитор и фиксер должен вернуть:
- `run_id` / имя сессии или агента;
- путь к логу;
- какие пункты чек-листа проверены;
- какие файлы/модули реально смотрел или менял;
- какие проверки запускал и их результат;
- явные остаточные риски или блокеры.

### Финальный gate плана
Перед финальной сдачей оркестратор сверяет:
- все этапные чек-листы закрыты исполнителем и проверены аудитором;
- главный финальный чек-лист плана закрыт;
- фиксы по аудитам либо выполнены, либо явно вынесены в согласованный follow-up;
- нет забытых worktree, зомби-процессов и неподтверждённых агентских веток;
- изменения закоммичены и запушены в разрешённую ветку;
- уровень тестов соответствует риску накопленных изменений.
