# Фаза 2 — Кнопка «Установить» и installability (Chrome-ориентир)

**Цель:** осмысленная установка PWA с главной там, где платформа позволяет; безопасное поведение в Telegram/MAX при появлении SW.

**Пререквизиты:** [фаза 1](PHASE_01_ROOT_LANDING.md) (на странице есть блок установки и контент лендинга).

## Scope (код)

- Клиентский компонент на лендинге: `beforeinstallprompt`, кнопка, fallback-текст, iOS-блок.
- **Минимальный** service worker — `public/sw.js` + регистрация **только** вне Mini App (`isMessengerMiniAppHost()`).

## Чеклист (код — закрыто)

- [x] Обработать **`beforeinstallprompt`**: сохранить событие, по кнопке вызвать `prompt()`; если события нет — fallback (меню Chrome «Установить» / «Добавить…»).
- [x] Блок **iOS** — короткая инструкция (Поделиться → На экран «Домой»), **без** программного `prompt()` установки PWA.
- [x] **Минимальный** SW без перехвата `fetch` (сеть как без SW) и регистрация с лендинга с **`scope: "/app"`** (как manifest); в **`install`/`activate`** нет кэша HTML/API.
- [x] **Manifest** **`scope: "/app"`** — совпадает с областью регистрации SW (см. [фаза 3](PHASE_03_MANIFEST_AUDIT.md)).
- [x] **Не** регистрировать SW при `isMessengerMiniAppHost()` (`messengerMiniApp.ts`).
- [x] Событие **`appinstalled`**: сброс промпта + короткое UX‑подтверждение при уже установленном / standalone.
- [ ] **Ручной smoke:** Telegram/MAX WebView — первый экран без зависаний после выкладки (код гарантирует только отсутствие регистрации SW в Mini App).

## Проверки

### Автоматические (репозиторий)

- [x] `pnpm --filter @bersoncare/webapp lint` проходит.
- [x] `pnpm --filter @bersoncare/webapp typecheck` проходит.
- [x] SW: нет обработчика `fetch` и нет кэширования API/HTML в скрипте.

### Ручные (устройства / стенд)

- [ ] **Chrome** (Android или desktop) — установка через кнопку или меню.
- [ ] **iOS** — только инструкция, без программной установки; текст читаем в Safari и понятен в Chrome на iOS.
- [ ] **Мини‑апп** (`/app/tg`, `/app/max`) — запуск и авторизация без регрессии.

## Закрытие фазы

### В репозитории

- [x] Запись в `LOG.md`: SW включён с лендинга; путь `public/sw.js`; регистрация SW и manifest с **`scope: "/app"`**; не регистрировать в Mini App.

### На стенде

- [ ] Пункты §«Ручные» и чеклист **«Ручной smoke: Telegram/MAX WebView»** выше.
