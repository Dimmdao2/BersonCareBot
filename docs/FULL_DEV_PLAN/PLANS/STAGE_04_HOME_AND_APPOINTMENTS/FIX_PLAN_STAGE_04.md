# FIX_PLAN — этап 4 (главная и записи)

## 1. Статус этапа

Частично выполнен. Полное сопоставление микрошагов 4.1a-4.1f и 4.2-4.4 с `page.tsx` и `cabinet/page.tsx` в этом аудите не выполнялось.

## 2. Найденные проблемы

### Критичные

Нет без полного прохода по подэтапам.

### Средние

- `apps/webapp/src/modules/menu/service.ts` и `apps/webapp/src/app/app/patient/page.tsx`: сверить с таблицей раздела "План и код" в `PLAN.md` этапа 4 (уроки, меню, карточки). Шаг 4.1. Тип: фронт, логика.
- `apps/webapp/src/app/app/patient/cabinet/page.tsx`: критерии подэтапа 4.2 (Rubitime, статусы, история) требуют построчной проверки. Тип: фронт, логика.

### Мелкие

- Обновить при необходимости `docs/WEBAPP_STRUCTURE_AND_UI_REPORT.md` при смене секций главной. Тип: документация.

## 3. Пропущенные шаги

Подтверждение 4.1b-4.1e (новости, рассылки, мотивашка, заглушка статистики) после чтения актуальных компонентов главной.

## 4. Лишние изменения

Не анализировались.

## 5. План исправлений

1. Пройти таблицу "План и код" в `PLAN.md` этапа 4 и отметить статус по каждой строке.
2. Реализовать или добить микрошаги 4.1a-4.1f со стабильными `id` для e2e.
3. Экран 4.2: сверить статусы и виджет Rubitime с планом; fallback-ссылка при CSP.
4. Подэтапы 4.3-4.4: зависимости от этапов 3 и 7.
5. `pnpm run ci` и при смене маршрутов `pnpm --dir apps/webapp run test:e2e`.

---

Аудит: `PLAN.md` этапа 4.
