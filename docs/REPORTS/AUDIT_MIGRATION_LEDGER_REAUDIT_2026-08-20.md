# Повторный независимый гейт — журнал миграций после починки FAIL (20.08)

Гейт, не фиксер. Прошлый гейт (Opus, `AUDIT_MIGRATION_LEDGER_2026-08-20.md`) дал **FAIL**; фиксер (Sonnet)
отчитался коммитами `e9ac1d234` (`git mv` 0051-0053 → таймштамп + `--relabel` + отчёт) и `6078daf6b`
(регистрация в очереди). Проверял ЗАКРЫТИЕ FAIL против реальности, не по отчёту фиксера. Все числа сняты
живьём против `bcb_webapp_dev` на этом боксе (`sudo -u postgres psql -h /var/run/postgresql`) или получены
названной командой. ПРОД и TEST не открывал.

Клон: `/home/dev/dev-projects/bcb-wt-migration-timestamp-20260819`, ветка `wt/migration-timestamp-20260819`,
HEAD `6078daf6b`.

## Вердикт: **PASS** — прежний FAIL по `232616725` СНЯТ

Живой КРАСНЫЙ блокер (0051/0052/0053) закрыт и доказан независимо; журнал согласован с каталогом живьём;
раннабл-тропы для повтора ручного DML мимо обёртки в дереве НЕТ (утверждение фиксера перепроверено и верно).
Ручной ledger-DML в `232616725` — совершённое прошлое действие на живом DEV: его нельзя «отменить», но все
устранимые последствия чисты, оно честно раскрыто, повторяемого следа в дереве не оставило, а правильная
форвардная правка (down-операция в обёртке + наблюдаемость журнала) — owner-scoped, и фиксер верно НЕ стал
чинить её новым ручным DML. Две отдельные НЕ-блокирующие находки для владельца — в конце.

Ниже каждое утверждение — с командой или `файл:строка`.

## 1. Живой блокер 0051/0052/0053 — закрыт (PASS)

### Байт-в-байт — доказано на уровне git-блобов, не на глаз
Переименование — чистое (`git show e9ac1d234 --stat -M`: три `.sql` по `0` insertions/deletions). Сверил
SHA блоба ДО (родитель `e9ac1d234^`) и ПОСЛЕ:

```
0051…_client_when_identified → 20260819T170216_… : c39468bd… == c39468bd…  IDENTICAL
0052…_must_not_leave_a_client → 20260819T163536_… : 5542cd15… == 5542cd15…  IDENTICAL
0053…_spends_no_tariff_seat   → 20260819T182039_… : ac7d4159… == ac7d4159…  IDENTICAL
```

Одинаковый blob-SHA = ни один байт не изменён. Это строже, чем сверка diff.

### Ни одна миграция не применена дважды; ни одна не стала pending-но-применённой
```
# tag == новое имя файла == sha256(файла), для всех трёх:
20260819T170216_… file=ba4a6912…  ledger=ba4a6912…  MATCH
20260819T163536_… file=efdb857d…  ledger=efdb857d…  MATCH
20260819T182039_… file=556c698a…  ledger=556c698a…  MATCH
# старые числовые теги в леджере ОТСУТСТВУЮТ:
SELECT tag … WHERE tag LIKE '0051%' OR '0052%' OR '0053%' → (пусто)
```

Хеши в леджере — те же самые, что до релейбла (`ba4a6912/efdb857d/556c698a` из прошлого аудита), значит
`--relabel` только переклеил `tag` (`UPDATE … SET tag`), ни один statement миграции не выполнился повторно.
Дублей тегов нет (`GROUP BY tag HAVING count(*)>1` → пусто), pending нет.

### Полная сверка леджер↔каталог живьём
```
total ledger rows=58, null-tag=1  ·  files on disk=57  ·  tagged rows=57
orphan tags (леджер без файла): (пусто)
pending (файл без строки):      (пусто)
```

### Гейты зелёные — моими руками
```
check-drizzle-migration-order.sh → OK (exit 0)
migrate-dev.sh --preflight       → PASS: pending=0 total=57 verified-objects=89 foreign-ledger-rows=1
```

`verified-objects=89` — preflight спросил у базы каждый объект каждой применённой миграции против каталога
(живая проба, не по леджеру). Совпадает с замером лида (`pending=0 total=57 verified=89 foreign=1`).

### Гейт имени орёт на плохое имя — живая инъекция во все три прогонщика
Создал `0099zz_reaudit_probe_BADNAME.sql`, прогнал три прогонщика:
```
check-drizzle-migration-order.sh → "…0099zz… is not named YYYYMMDDTHHMMSS_… frozen snapshot does not know it"
migrate-local.mjs --rollback-only → та же строка, ДО BEGIN (ни один statement не дошёл до базы)
run-webapp-drizzle-migrate.mjs    → "[migrate] migration_name_violation 0099zz…"
```
Все три отказали и назвали файл. Инъекцию снял, `git status --short` чист, гейт снова OK.

### Тесты и хеш-гейты
`node --test migration-order.test.mjs migrate-local.test.mjs migrate-local-parse.test.mjs` → **47 pass / 0
fail**. Адверсарные ассерты реальны (не форма): `migrate-local.test.mjs:475` relabel отказывает при content
drift; `:486` — на не-foreign теге; `:516-517` drop-foreign отказывает, когда файл ещё держит хеш строки
(«this is a rename, not a dead row»); `:528` — на не-foreign теге; `:539` — оба без `--drizzle-folder`.

### Права в переименованных файлах — чисто (правило ⛔ «миграция не выдаёт прав»)
Прицельный греп по трём файлам (`^\s*(REVOKE|GRANT)`, `FROM PUBLIC`, `ALTER DEFAULT PRIVILEGES`,
`CREATE POLICY|ROLE`) — пусто. Совпадения по подстроке `revoke` — это ИМЯ функции
`app.revoke_public_booking_enrollment` (бизнес-логика отзыва зачисления) и комментарии, не привилегия.
Историческое `REVOKE ALL … FROM PUBLIC`, которым прошлый аудит опасался за `0051`, в текущих файлах нет.

## 2. Тропа для повтора ручного DML — НЕ найдена (утверждение фиксера верно)

Полный список исполняемых мест раздачи `UPDATE`/`DELETE`/`INSERT` по `drizzle.__drizzle_migrations`
(`git grep`, исключая тесты/`.md`/generated):

| место | что | вердикт |
|---|---|---|
| `deploy/postgres/privileges/migrate-local.mjs:245/262/399/402` | `--relabel`/`--drop-foreign`/reapply | сама санкционированная обёртка |
| `deploy/postgres/privileges/migration-order.mjs:174` | бэкфилл легаси-тегов | импортируется и зовётся ТОЛЬКО из обёртки |
| `apps/webapp/scripts/run-webapp-drizzle-migrate.mjs:325` | `INSERT` применённой миграции | сам санкционированный раннер |
| `apps/webapp/db/drizzle-migrations/0000_b0_baseline.sql:8` | bootstrap `DELETE` | statement миграции, идёт ВНУТРИ транзакции обёртки, не в обход |

`.sh`/`.sql` вне обёртки, упоминающие таблицу: `deploy/postgres/generated/privileges.*.sql` — только
`GRANT/REVOKE/ALTER TABLE/DROP POLICY` (реконсиляция ПРАВ, не row-DML); `tools/db-facts-snapshot.sh` —
только `SELECT` (read-only). Untracked-скриптов нет (`git status --ignored` чист от посторонних).

`.md`-отчёты (`MIGRATION_TIMESTAMP_FIX`, `MIGRATION_TIMESTAMP_NAMES_AUDIT`, `DEV_LEDGER_REPAIR…_2026-08-04`)
цитируют сырой `DELETE` как РАССКАЗ о прошлом действии — один раздел прямо озаглавлен «⛔ Обход wrapper'а —
назван прямо». Это записи, не рансбуки («сделай так снова»); по гигиене документации история не переписывается.
Агент, скопировавший `DELETE` из отчёта об инциденте, нарушил бы и явное предупреждение, и канон
(`AGENTS.md:415-416`, non-negotiable №1) — защита стоит в каноне + отказе обёртки, а не в отсутствии строки в
прозе. Исполняемой тропы обхода в дереве нет; вычищать было нечего — совпадает с фиксером.

## 3. Необъяснённая мутация журнала — суждение гейта: приемлемый остаточный риск (DEV), + находка

Причину прежней мутации двух строк установить по-прежнему нельзя, и это НЕ халтура: `log_statement=none` на
этом postgres — успешные `UPDATE`/`DELETE` в лог не пишутся в принципе. Раскрыто честно. При этом:

- текущий журнал ДОКАЗУЕМО согласован с ЖИВЫМ каталогом (`verified-objects=89`, `comm`-сверка пуста, хеши
  трёх релейбл-строк побайтно сходятся) — не «сам с собой», а с реальными объектами базы;
- DEV — общий бокс с параллельными ветками (`git worktree list` многолюден), ПРОД на отдельном сервере,
  прод-риск нулевой;
- остаточного дрейфа нет.

Блокировать ветку навсегда за неустранимое-по-конфигурации прошлое событие, когда текущее состояние
доказуемо чистое, — это негейтящийся гейт. Поэтому для DEV — **приемлемо**. Но наблюдаемость реальна как
пробел, и я выношу её отдельной находкой ниже (не чиню, скоуп не завожу — по брифу).

## 4. Оставшаяся одна чужая строка

- `tag = NULL`, `hash = c13927102c54…`, `created_at = 1800000070000`, `id = 598`. Это и есть весь
  `foreign-ledger-rows=1`.
- **Откуда:** чужая немаркированная запись, появившаяся от конкурентной активности на общем DEV-боксе;
  однажды ошибочно подписанная тегом `0050_the_transcode_queue_dispatcher_had_no_door` легаси-бэкфиллом
  старой (не зафиксированной) версии журнала, затем разподписанная в `NULL`. Её hash не совпадает ни с
  одним файлом ни в одной ветке (`MIGRATION_TIMESTAMP_FIX_2026-08-20.md:287-289`,
  `MIGRATION_LEDGER_ORPHANS_2026-08-20.md:287`).
- **Правильно ли оставлять — да.** Строка инертна: файла нет → не pending; тега нет → не сирота при сверке;
  `--drop-foreign <tag>` её адресовать не может (нужен тег), а ручной `DELETE` был бы тем самым запрещённым
  обходом. Задокументирована, вне скоупа этого воркстрима. Убирать её сейчас — либо нарушить канон, либо
  завести новый скоуп; оставить — единственный безопасный и канон-совместимый выбор.

## Non-blocking находки для владельца (НЕ чинил, скоуп НЕ заводил — по брифу)

1. **В обёртке нет down/rollback-операции — это активный драйвер ручных DML.** Именно поэтому воркер F3 в
   `232616725` откатывал миграции сырым `sudo psql -f`, и НЕЗАВИСИМО тем же путём пошёл аудитор в
   `MIGRATION_TIMESTAMP_NAMES_AUDIT_2026-08-20.md:191` (ручной `DELETE` для снятия своей пробной строки) —
   два разных агента уперлись в одну стену и оба обошли обёртку. Канон однозначен («не хватает операции →
   допиши САМУ утилиту, а не лезь в БД мимо неё»). Пока down-операции в `migrate-local.mjs` нет, паттерн
   будет повторяться. Это ВОПРОС/скоуп владельцу, не моя правка.
2. **Мутации `__drizzle_migrations` на DEV сейчас невидимы** (`log_statement=none`). Для воркстрима, чья цель
   — целостность леджера, и учитывая, что TEST — репетиция прода, стоит закрыть наблюдаемость журнала
   (audit-триггер на `drizzle`-схему или прицельный `log_statement` для таблицы) ДО cutover на TEST/PROD.
   Не блокер для DEV-закрытия, но должно быть закрыто до прода.

## Что доказано, что нет

- Живой блокер 0051-0053: закрыт, все проверки брифа пройдены независимо. **PASS.**
- Ручной DML в `232616725`: совершённое прошлое действие; устранимых последствий не осталось (журнал чист,
  тропы повтора в дереве нет), правильная форвардная правка owner-scoped — фиксер верно её не выдумал.
  Прежний FAIL **СНЯТ**.
- Причину мутации двух строк — НЕ установил (неустранимо конфигурацией лога); вынесено находкой №2.
- TEST/PROD живьём не проверял — DEV-only скоуп, туда никто и не ходил.
