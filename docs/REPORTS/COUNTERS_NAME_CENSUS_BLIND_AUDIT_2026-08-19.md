# Слепой аудит замены счётчиков-чисел сверкой имён — 19.08

**Происхождение файла.** Аудитор `audit-counters-20260819` (Opus/high) записал вердикт целиком в строку
очереди `docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`, отдельного отчёта не создав. Текст ниже — его
слова дословно, перенесённые ведущим в ожидаемое каноном место; ни одно утверждение не дописано и не
изменено. Причина переноса: порт требует у тестовых коммитов путь к отчёту о поломках, и требование верное —
доказательства инъекций должны лежать файлом, а не только в ячейке таблицы.

**Предмет:** ветка `wt/counters-20260819`, коммиты `f1afd4a60`, `96e976a4d`, `4f085da6d`.
**Метод аудитора:** слепой проход, 19 инъекций в проверяемое, сверка с предветочным деревом.

---

**FAIL, не приземлять без одной правки; ядро работы здоровое.** Замена делает ровно то, что обещает: 11 инъекций (переименование корня в миграции, DEFINER→INVOKER, подмена подписи в артефакте при неизменном числе, вычерк курируемого корня, вырождение именованного корня в relation-wide, потеря возможности резолвером, TEST-only дверь, осиротевший владелец шва, третий шов на таблице, вторая динамическая обёртка, владелец миграции вне кластера) — каждая краснеет и НАЗЫВАЕТ появившееся/исчезнувшее имя. П.3 брифа PASS: сверка `BCB_FUNCTION_BODY_SURFACES_VERIFIED` усилена, а не ослаблена — подмена одного тела другим при том же числе теперь краснеет поимённо (`app.accept_org_invite` → `app.legacy_accept_org_invite`), раньше была зелёной. **F-1 (единственная блокирующая):** новая SECURITY DEFINER дверь, тело которой лежит НЕ в пронумерованной миграции (`deploy/postgres/port-context/contract.sql`, `test-saas-isolation-telemetry-fixtures.sql`), проезжает с нулём красного — инъекция `app_ext.zz_new_backdoor()` + запись в декларацию даёт 103 pass / 0 fail, тогда как предветочное дерево (`f1afd4a60^`) краснело трижды (`410 !== 409`, `397 !== 396`, `functions=${expectedDefiners}`). Именно этим путём приехали три последние двери — их называют сами удалённые комментарии (`expire_accepted_port_context`, `begin_port_context`, `assert_port_context_claim`). Двери из миграций гейт сохранили (`b0ForwardArtifactRoots`), двери из `contract.sql` — потеряли; чинится одним ключом переписи. **F-2 полнота:** заявлено 18 утверждений, фактически заменено 23 (в них 26 вписанных руками чисел); не тронуты два того же класса — `relation-access.test.mjs:1395-1396` (132/62; `REV10_CLINICAL_ACCESS` правится постоянно, а соседний `deepEqual(actualEdges, expectedEdges)` уже называет расхождение) и `function-census.test.mjs:354-361` (`BUSINESS_SEAM_STATS`, 12 строк ВЫШЕ заменённого; инъекция даёт `dev: 229 !== 230` — голое число). **F-3 честный риск:** `BCB_UPDATE_NAME_CENSUS=1` — глобальная кнопка «согласен со всем»: вычерк курируемого pre-session корня + перегенерация = 19 pass / 0 fail и одна строка в json; тормоза нет ни одного, один прогон переписывает все 6 ключей разом. **F-4 мелочь:** `parseOwnerStatements(...).length === statements.length` краснеет (доказано парсером, глотающим пустую статью), но печатает `4 !== 5` — не называет. Удалённое сверх нужного не найдено: заморозки формы миграций 0016–0019 сняты законно (сам разбор бросает, называя тег и номер статьи), счётчики 226/124/287 и гистограмма форм полностью покрыты соседними поимёнными сверками (доказано инъекциями `search_path` и `returnsSet`). Прогон: 112 тестов, 103 pass, 0 fail, 9 skipped; eslint по изменённым файлам чист; перепись в синхроне (перегенерация на чистом дереве даёт пустой diff).
