# Разбор дня 19.08 — что сделано, что на стенде, что на бумаге, что открыто

Замер: `git log --since="2026-08-19 00:00" feat/doctor-ui-rebuild` → **253 коммита**, из них 62 слияния
веток, 45 `fix`, 13 `feat`, 5 `perf`, 26 `audit`, 79 `docs`, 8 `test`.

Граница дня одна и важная: **на стенде стоит `c89b5d40b` (18:47)**, служба поднята 18:51:31. Всё, что влито
после, на стенде НЕ живёт. `git rev-list --count c89b5d40b..feat` → **43** (33 без слияний).

---

## 1. Четыре ошибки живого прохода владельца — закрыты и проверены против реальности

Проверка велась не по галочкам плана, а против логов и базы (`docs/_TODO/OWNER_PATIENT_WALKTHROUGH_BUGS_2026-08-19.md`,
раздел «Независимая сверка ведущего»).

| Ошибка | Что было | Что стало | Чем доказано |
|---|---|---|---|
| Медиа в комментарии к упражнению | файл не прикреплялся | правка `1dae8918f` | коммит — предок выкаченного `c89b5d40b`. Оговорка: на TEST 1 строка комментария и 0 медиа при ней, свежими данными не подтверждено |
| Долгое подтверждение записи | 3.4 с — фиксированная лестница повторов ждала интегратора, результат выбрасывался | `54c20db12`: событие ушло в `after()` в `createBookingSyncPort` | замер на живом TEST: **0.19 с** |
| Каналы уведомлений — отказ прав | 42501 на `user_notification_topic_channels` | запись идёт через `app.set_current_patient_notification_topic_channel`, EXECUTE = t | `journalctl --since "today 12:00" \| grep -c 42501` → **0**; 315 строк в таблице, у `app_patient` только SELECT — так и задумано |
| Часовой пояс без подтверждения | сохранение молчало | `app.set_current_patient_calendar_timezone`, EXECUTE = t | в `platform_users.calendar_timezone` живые значения: Москва 63, плюс Берлин, Белград, Ереван, Амстердам, Рим, Екатеринбург, Владивосток, Лос-Анджелес |

## 2. Правки кода, которые уже стоят на стенде (00:08–18:47)

45 `fix` + 13 `feat` + 5 `perf`. По смыслу это шесть кустов:

- **Публичная запись довели до конца человеческого пути.** Адрес развёрнут в `/{clinic}/booking`; у клиники
  появилась визитка `/{clinic}`; филиал перестал выбираться за человека; телефон и почта из формы перестали
  теряться; экран «готово» стал таким же, как в приложении, с добавлением в календарь; мёртвая запись и
  публичный каталог перестали молчать о своём отказе и отвечают кодом ошибки.
- **Отказ базы перестал притворяться пустотой.** В пяти местах 42501 показывался человеку как «данных нет».
  Плюс перепись текстового SQL и три горящие находки из неё.
- **Оператор и здоровье очереди.** Оповещение интегратора было мертво на 9 местах из 14; сторож видел
  инциденты и не мог открыть ни одного; критический тик падал целиком из-за одного канала; два машинных
  тика не имели своей двери — один врал об успехе, второй не шёл вовсе.
- **Деньги.** Место посреди периода считается по дням до конца оплаченного, счётчик мест умеет вниз; цена
  дополнительного места выпускается сервером подписанной котировкой, число из браузера к деньгам не
  прикасается.
- **Медиа.** Изображение хранится в стандартном размере, оригинал удаляется; непересобранный файл говорит
  «готовится»; каталог перестал отдавать первичный файл мимо конвертации; видео можно приложить ссылкой.
- **Скорость и стены.** Установка контекста — 3 обращения к базе вместо 9; страница 51 → 45 транзакций;
  безусловные ветки общих политик получили область клиники; заявленную организацию сверяет база, а не код.

## 3. Влито после стенда — ждёт выкатки (19:19–21:13)

Кода здесь мало, и он весь про правила, не про экраны:

- `dc9be0010` — права вычищены из миграций, гейт `check-migration-privileges.mjs` встал в дверь (260 строк).
- `6225fe265` — снят владелец шва визитки, перепись швов публичной витрины.
- `fd40d30e5` — кабинет показывал клинике отменённый порядок частей адреса. Починено удалением дубля:
  форма адреса строилась вручную во втором месте, теперь зовёт канонический `publicBookPaths.forSlug`.
- `08c68b159` — решения владельца Т11 (домен = брендирование = платная механика) и Т12 (лимит клиентов убрать).

## 4. Бумага, а не код — шесть исследований (2 200+ строк)

Ни одно из них ничего не чинит и не заводит карточек. Это входы для решений владельца:

| Документ | Строк | Зачем |
|---|---:|---|
| `SEAM_OWNERS_CENSUS_2026-08-19.md` | 1169 | 43 владельца швов, 40 родились из двух коммитов 09.08; рекомендация 43 → 37 |
| `CLINIC_CUSTOM_DOMAIN_PROPOSAL_2026-08-19.md` | 589 | как домен устроен у нас и у других, экран настройки |
| `SEAT_UNPAID_PRACTICE_2026-08-19.md` | 520 | что делают другие с неоплаченным местом |
| `CLINIC_SHOWCASE_WORLD_PRACTICE_2026-08-19.md` | 480 | практика витрины клиники |
| `SEAT_INVOICE_WORLD_PRACTICE_2026-08-19.md` | 337 | практика по счёту за место |
| `SILENT_CODE_CENSUS_2026-08-19.md` | 144 | 26 разрывов «код есть, экрана нет» |

## 5. Открытое — по убыванию важности

1. **Роль `app_seam_public_clinic_card_owner`.** Снята из декларации коммитом `cfa4e45df`, в кластере жива и
   держит 54 объекта в `bersoncarebot_test`. DEV-reconcile падает в любом дереве с этим коммитом. Держит
   приземление ветки публичной записи. Два маршрута развязки описаны, ни один не выполнялся.
2. **Из 26 тихих находок реально болят четыре:** буфер между записями считается на каждый слот, но задать
   его негде; блокировку времени (отпуск, ремонт) нельзя поставить ни с одного экрана; новый кабинет нельзя
   завести; часовой пояс филиала API принимает, поля нет — у всех филиалов Москва.
3. **Т12 не исполнено** — решение записано, лимит клиентов из кода не убран.
4. **Расписания оператора на TEST не установлены** — критический тик продления подписок.
5. Двойная бронь на занятый слот; прод-алертинг не ловит отказ доставки; карточка пациента хардкодит Москву.
