# Platform Identity & Access — журнал выполнения

Вести по мере реализации (по образцу других инициатив в `docs/`).

| Дата | Действие | Ссылка PR / коммит | Примечание |
|------|----------|-------------------|------------|
| — | Созданы план и спецификация | — | `MASTER_PLAN.md`, `SPECIFICATION.md`, `SCENARIOS_AND_CODE_MAP.md` |
| 2026-04-10 | Ужесточение плана по ревью: порядок фаз B→C→D, DoD, три модуля, API=UI policy, onboarding на сервере, tg:… как арх. решение, observability | — | Синхронно обновлены `SPECIFICATION.md` (§3–§5, §14–§15), `README.md`, `SCENARIOS_AND_CODE_MAP.md` |
| 2026-04-10 | **Фаза A (EXEC):** типы access context, `resolvePlatformAccessContext`, trusted phone policy (`TrustedPatientPhoneSource`), колонка `patient_phone_trust_at` + writers (OTP, integrator projection, OAuth Yandex, merge); `isPlatformUserUuid` в shared; обновлён §8 `SCENARIOS_AND_CODE_MAP.md` | — | Маршрутные guards пока не переведены на tier (фаза D). |
| 2026-04-10 | **Фаза A (уточнение контракта):** в `PlatformAccessContext` поле **`tier`** вместо `clientTier` (выравнивание с SPECIFICATION §3 / MASTER §5); `trustedPatientPhoneWriteAnchor` у доверенных writers + re-export | — | Поведение tier на чтении без изменений; якоря только для связи кода с enum. |
| 2026-04-10 | **Фаза A (FIX):** `isTrustedPatientPhoneActivation` в `trustedPhonePolicy` (единственная read-side проверка §5); `GET /api/me` добавляет **`platformAccess`** из `resolvePlatformAccessContext` (SPEC §6); Mini App `patientMessengerContactGate` при наличии `platformAccess` снимает гейт только при `tier === "patient"` | — | Меняет поведение UI гейта при телефоне в сессии без `patient_phone_trust_at`. |
| 2026-04-10 | **Фаза B (EXEC):** до INSERT в `findOrCreateByChannelBinding` — резолв канона по подсказкам из signed webapp-entry token (`sub` UUID, `integratorUserId`, телефон только при совпадении с каноном с `patient_phone_trust_at`); merge кандидатов через `mergeCanonicalPlatformUserCandidates`; `contact.linked` без дублирующего `updatePhone`; опциональное поле `integratorUserId` в `contracts/webapp-entry-token.json`; тесты `exchangeIntegratorToken.resolutionHints` | — | Tier-политика на web не меняется; `pnpm run ci` обязателен перед пушем. |
| 2026-04-10 | **Фаза B (FIX по аудиту):** `integratorUserId` в webapp-entry токене при best-effort lookup из integrator DB (`getLinkDataByIdentity`) в Telegram/MAX webhook; `resolveCanonicalPlatformUserId` для diary-событий + wiring в `events/route.ts`; обогащение `display_name` при привязке канала к существующему канону (если было пусто); комментарий к merge по телефону в `collectCandidateIds` | — | Mini App / Login Widget без `?t=` по-прежнему без server-side hints (намеренно: нельзя доверять client-controlled `start_param` для UUID). |
| 2026-04-10 | **Фаза B — повторный AUDIT после FIX:** таблица остатков **B-R1–B-R8** в `SCENARIOS_AND_CODE_MAP.md` §10; порядок разделов §9 (наблюдаемость) → §10 (остатки) → §11 (чек-лист); ссылка из `MASTER_PLAN.md` §5 (фаза B) | — | Итог: обхода tier на web через integrator-события не выявлено; закрытие B-R* — по плану в C/E. |
| 2026-04-10 | **Фаза B — досылка (закрытие хвостов):** `start_param` / `webappEntryToken` → те же signed hints, что `?t=`; `tg:`/`max:` в `sub` без `bindings`; merge-ветка перезаписывает `display_name`; логи `[identity_resolution]` / auth hints; §10 переписан как «статус закрыта» | — | Cookie/сессия для всех входов и полный DoD §8 — фазы C/E. |
| 2026-04-10 | **Фаза B — повторный независимый AUDIT:** сверка кода с MASTER_PLAN §5 и DoD §3 п.5; §10 обновлён (таблица «пункт §5 → код», границы C/E); MASTER_PLAN §5 — уточнение статуса; **правок кода нет** (P0/P1 по B не найдены) | — | — |
| 2026-04-10 | **Релиз в main:** фаза B (канал↔канон, signed hints, integrator `integratorUserId` в entry-токене, diary→канон, тесты) + доки §10/журнал; `pnpm run ci` перед пушем | — | Остатки чек-листа §11 — фазы C/D/E |
| 2026-04-10 | **Фаза C (EXEC):** `sessionCanonicalUserIdPolicy.ts` (арх. решение `tg:`/не-UUID = onboarding-only); `exchangeIntegratorToken` — UUID `sub` без bindings → `pgUserByPhone.findByUserId`; лог `client_session_transport=legacy_non_uuid_onboarding_only`; комментарии init/widget; `LEGACY_NON_UUID_SESSION_RESOLUTION` в `resolvePlatformAccessContext`; JSDoc `SessionUser` / `setSessionFromUser`; SPEC §6, MASTER §5 C, SCENARIOS §2/§11; тест `exchangeIntegratorToken.uuidSubWithoutBinding.test.ts` | — | `pnpm run ci` зелёный; route/API whitelist (DoD §2 полностью) — фаза D |
| 2026-04-10 | **Фаза C (FIX по аудиту):** закрыта лазейка «только `session.user.phone`» для patient-бизнеса: `requirePatientApiSessionWithPhone` и `requirePatientAccessWithPhone` при `DATABASE_URL` требуют **`tier === "patient"`** (`patientClientBusinessGate` → `resolvePlatformAccessContext`); при ошибке БД — fallback на телефон в сессии; `POST /api/patient/question` переведён на общий gate; `guards.md`; тест `requireRole.patientTier.test.ts`; ссылка в `sessionCanonicalUserIdPolicy.ts` | — | `pnpm run ci` зелёный; middleware/layout allowlist по телефону — вне этого FIX (фаза D при необходимости) |
| 2026-04-10 | **Фаза C.02 — закрыта:** `patientClientBusinessGate` (`platform-access`); `/api/booking/*` + `/api/patient/*` → `requirePatientApiBusinessAccess` (алиас `requirePatientApiSessionWithPhone` в `requireRole.ts`); layout: tier + `resolvePatientLayoutPathname` (Referer fallback); RSC: напоминания, журнал, сообщения, intake LFK/nutrition — `requirePatientAccessWithPhone`; тесты booking 403; `guards.md`, `SCENARIOS_AND_CODE_MAP.md` §7/§11, `MASTER_PLAN` §5 C.02 | — | К фазе D: единый whitelist-модуль route & API policy |
| 2026-04-10 | **C.02 — финальная верификация:** сводка доков, унификация импортов API patient на `requirePatientApiBusinessAccess`, доп. тест `POST /api/booking/create` 403; `pnpm run ci`; push в main | — | — |
| 2026-04-10 | **Готовность к фазе D:** в `SCENARIOS_AND_CODE_MAP.md` §11 — отдельный чекбокс «скоуп C+C.02», блок предусловий D, явный перечень остаточных UI-паттернов (profile allowlist, guest-страницы); в `MASTER_PLAN.md` §5 D — предусловия; `guards.md` + комментарии profile/layout/policy; лог при `need_activation` и неразрешённом pathname | — | Следующий шаг — EXEC фазы D (единый whitelist-модуль) |
