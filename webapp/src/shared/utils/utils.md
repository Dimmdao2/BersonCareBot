# utils

Вспомогательные функции общего назначения.

Например кодирование/декодирование base64url для работы с токенами и подписями. Используются в модулях auth и в инфра (вебхуки) и не привязаны к доменной логике.
