-- Drizzle journal entry 0000 (phase 1 baseline).
-- The first `drizzle-kit introspect` snapshot was previously stored here as a block comment;
-- retrieve it from git history if needed. Base `public` tables are created by the legacy
-- SQL runner (`apps/webapp/migrations/` via `pnpm run migrate`); incremental Drizzle SQL starts at 0001.
SELECT 1;
