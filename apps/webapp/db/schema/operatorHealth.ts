/** Канон таблиц — пакет `@bersoncare/operator-db-schema` (webapp + integrator, Drizzle). */
export {
  operatorIncidents,
  operatorJobStatus,
  integrationWebhookLastStatus,
  integrationWebhookErrorEvents,
} from "@bersoncare/operator-db-schema";
