# Webapp env: dev — webapp/.env.dev; prod — /opt/env/bersoncarebot/webapp.prod. See webapp/src/config/env.ts
NODE_ENV=development
HOST=127.0.0.1
PORT=5200
APP_BASE_URL=http://127.0.0.1:5200
# Optional in dev: set both to the same value to avoid empty layout meta baseline on first load.
# BUILD_ID=dev-local
# NEXT_PUBLIC_BUILD_ID=dev-local
# Production: совпадает с integrator (api.prod) — одна база, одна роль PostgreSQL, схемы public + integrator.
# Dev: часто одна dev-база с обеими схемами; иначе — отдельный URL только до выравнивания локального окружения.
DATABASE_URL=postgres://bcb_webapp_dev_user:password@127.0.0.1:5432/bcb_webapp_dev
# Required in dev and prod (min 16 chars). Never use repo-known or example values in production.
SESSION_COOKIE_SECRET=replace-with-long-random-secret
# Either set both below, or one shared secret for both entry and webhook.
INTEGRATOR_WEBAPP_ENTRY_SECRET=replace-with-entry-secret
INTEGRATOR_WEBHOOK_SECRET=replace-with-webhook-secret
# INTEGRATOR_SHARED_SECRET=optional-fallback-if-entry-and-webhook-not-set
# Для отправки SMS с кодом через интегратор: URL API интегратора (dev: http://127.0.0.1:4200). Нужен вместе с INTEGRATOR_SHARED_SECRET (или entry/webhook).
# INTEGRATOR_API_URL=http://127.0.0.1:4200
# В dev при true на странице /app показываются кнопки «Как пациент» и «Как врач/админ» для входа в браузере без Telegram. В production игнорируется.
ALLOW_DEV_AUTH_BYPASS=true
# Публичный username бота без @ (как в адресе t.me/<username>). НЕ числовой id бота из BotFather.
# По умолчанию в коде: bersoncare_bot. Канон для прод — лучше задать telegram_login_bot_username в админке.
# TELEGRAM_BOT_USERNAME=bersoncare_bot
# Ник бота MAX для диплинка channel-link (канон — max_login_bot_nickname в админке). Пример: id123456789_1_bot
# MAX_LOGIN_BOT_NICKNAME=
# Кто может войти из Telegram: через запятую — числовые user id аккаунтов (людей), не @username и не id бота
ALLOWED_TELEGRAM_IDS=7924656602
# Числовой user id админа в Telegram (человек), не username бота
ADMIN_TELEGRAM_ID=364943522
# Whitelist Max (вход); роль — только ADMIN_PHONES / DOCTOR_PHONES
ADMIN_MAX_IDS=
DOCTOR_MAX_IDS=
# Роль admin и doctor только по номеру (через запятую; нормализация как при SMS-входе)
ADMIN_PHONES=+79643805480
DOCTOR_PHONES=
# Дополнительные номера, которым разрешён вход (клиент), без повышения роли
ALLOWED_PHONES=

# Каталог файлов CMS (upload /api/media). Пусто — `var/media` относительно cwd.
# MEDIA_STORAGE_DIR=

# --- MinIO / S3 (CMS медиа): endpoint + ключи + S3_PRIVATE_BUCKET — presigned PUT в приватный бакет; чтение через GET /api/media/:id (редирект на presigned GET).
# S3_PUBLIC_BUCKET — опционально (будущие прямые публичные URL / CDN), для включения библиотеки не требуется.
# S3_ENDPOINT=https://fs.example.com
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_PRIVATE_BUCKET=bersonservices-private
# S3_PUBLIC_BUCKET=bersonservices-public
# S3_REGION=us-east-1
# S3_FORCE_PATH_STYLE=true
# Фоновая очистка media_files (pending_delete): Bearer для POST /api/internal/media-pending-delete/purge
# INTERNAL_JOB_SECRET=
# Уровень логов pino (info, warn, error). По умолчанию в коде: info
# LOG_LEVEL=info

# --- Этап 5 (авторизация): новый поток входа (check-phone, PIN, мессенджер, OAuth) ---
# Ранний интерактивный login на `/app` + prefetch публичных конфигов (AuthBootstrap / AuthFlowV2); см. messengerAuthStrategy.
# NEXT_PUBLIC_AUTH_BOOTSTRAP_EARLY_UI_V2=1
# NEXT_PUBLIC_AUTH_V2=1
# Яндекс OAuth (все три пусты — режим отключён, API вернёт 501)
# YANDEX_OAUTH_CLIENT_ID=
# YANDEX_OAUTH_CLIENT_SECRET=
# YANDEX_OAUTH_REDIRECT_URI=
