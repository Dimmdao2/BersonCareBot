import { cn } from "@/lib/utils";

/** Единый вид карточек главной «Сегодня» (README Phase 3.4). */
export const patientHomeCardClass = cn(
  "rounded-2xl border border-border bg-card p-4 shadow-sm",
);
