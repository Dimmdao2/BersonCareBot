# media-library

Экран библиотеки файлов в CMS (`/app/doctor/content/library`).

**Точка загрузки для врача:** полноэкранная библиотека — основной поток для пакетной загрузки, presigned / multipart в MinIO по возможностям клиента (`MediaLibraryClient`). Дополнительно в модалках выбора медиа (`MediaPickerShell` + `MediaPickerPanel`, см. `MediaLibraryPickerDialog` / `MediaLibraryInsertDialog`) есть вкладка **«Загрузить с устройства»**: один файл уходит в `POST /api/media/upload` в папку, согласованную с фильтром пикера (корень или выбранная папка в режиме упражнений). Атрибут `accept` на поле файла согласован с `kind` пикера; после успешной загрузки тип строки (`image` / `video` / …) проверяется перед вызовом `onPick` — несовпадение с полем (например видео в обложку «только картинка») даёт сообщение на русском без подстановки в форму (запись в библиотеке уже создана). В формах медиа по-прежнему не вводится произвольный URL вручную (кроме legacy `https://` в сохранённом контенте/упражнениях).

**Markdown:** вставка из модалки использует [`markdownMediaSnippet.ts`](../../../../../shared/ui/markdown/markdownMediaSnippet.ts) — решение `![alt](url)` vs ссылка по `kind`/`mimeType` и расширению имени файла.

**Поиск в picker (модалка):** общая логика `useMediaPickerFilteredList` (базовый пул страницами по 50 + локальный поиск + серверный fallback при 0 локальных совпадений, debounce ~300 ms). При **≥1** локальном совпадении сервер **не** вызывается. Серверная выдача — отдельный список с подписью «По всей библиотеке» (`MediaPickerListFooter`). Используется в `MediaPickerPanel`, `AutoCreateExercisesClient`.

**Экран библиотеки** (`MediaLibraryClient`): по умолчанию **«Все папки»**; при входе в конкретную папку — `includeDescendants=true` (файлы из подпапок). В режиме «Все папки» и в корне **не показываются** файлы из системного поддерева **«Файлы клиентов»** (загрузки пациентов в обсуждение программы и будущие врачебные клиентские файлы). Отдельная область списка **«Файлы клиентов»** (`MediaLibraryFolderScopeSelect`) — листинг всего поддерева; внутри — подпапки по клиентам (имя клиента; при коллизии — суффикс `· uuid8`). Загрузка врачом в клиентское поддерево: только в **подпапку клиента**, не в корень «Файлы клиентов» (кнопка загрузки отключена с подсказкой). Системные папки `client_files_root` / `client_patient` нельзя переименовать, переместить или удалить. Серверный `q`, пагинация по 24, «Показано X из Y», кнопка «Загрузить ещё». Сортировка по имени: цифры → латиница → кириллица. В меню «⋯» — сводка использования (`GET /api/admin/media/[id]/usage-summary`); удаление с подтверждением при использовании в CMS **или** в обсуждении программы (`confirmUsed`).

Функции:
- просмотр загруженных файлов;
- редактируемое отображаемое название (без изменения исходного имени файла в хранилище);
- фильтрация по типу;
- сортировка по типу, дате загрузки и размеру;
- метаданные в списке: кто загрузил (ссылка на профиль) и разрешение image/video;
- загрузка новых файлов;
- удаление через меню действий (`...`) с предупреждением о местах использования в CMS.
