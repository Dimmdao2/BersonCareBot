/** DOM id строки результата теста на экране инстанса (deep link `focusItemId`). */
export function doctorProgramTestResultDomId(resultId: string): string {
  return `doctor-program-test-result-${resultId}`;
}
