/**
 * Builders are intentionally a thin re-export: tests define domain defaults next to
 * the contract they exercise, without creating a second application model here.
 */
export { Factory } from 'fishery';
