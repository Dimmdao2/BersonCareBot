# platform-analytics

Агрегаты кабинета глобального админа. Без карточек пациентов, без регистраций и push.

Порт: `ports.ts`. Страница `/app/doctor/analytics` (layout global-admin) → `GET /api/admin/platform-analytics`.
