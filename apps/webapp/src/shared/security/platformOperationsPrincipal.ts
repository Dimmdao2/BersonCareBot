/**
 * Exact dedicated-platform source used by authenticated platform-only surfaces.
 *
 * The principal deliberately carries no organization: cross-organization commercial operations
 * are authorized by `platform.operations` and must never inherit a clinic membership.
 */
export const PLATFORM_OPERATIONS_DB_SOURCE = 'platform.operations:authenticated';
