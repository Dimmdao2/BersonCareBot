// М3 круг 2 (#1081): существует ТОЛЬКО для CI-джобы stryker-diff-scoped-selftest
// (.github/workflows/ci.yml) — не продуктовая логика. Джоба временно правит эту строку решения двумя
// способами (RED: добавляет непокрытую ветку; GREEN: переписывает покрытую ветку в другую форму,
// как в M3-2 слепого аудита) локальным коммитом, который никогда не пушится, и откатывает его же шагом.
// Не удалять и не переносить без переноса шагов джобы, которые ссылаются на этот путь буквально.
export function classifySelftestProbe(value: string): 'empty' | 'other' {
  if (value === '') return 'empty';
  return 'other';
}
