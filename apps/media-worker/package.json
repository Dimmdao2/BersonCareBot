{
  "name": "@bersoncare/media-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm --dir ../../packages/error-tracking run build && pnpm --dir ../../packages/shared-contracts run build && pnpm run clean && tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run --passWithNoTests"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.1107.0",
    "@bersoncare/error-tracking": "workspace:*",
    "@bersoncare/shared-contracts": "workspace:*",
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "dotenv": "^17.4.2",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  }
}
