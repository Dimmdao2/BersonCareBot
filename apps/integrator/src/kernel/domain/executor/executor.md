# executor

Исполнитель действий: по типу действия выполняет одну операцию (message.send, user.state.set, diary.symptom.*, webapp.event.emit и др.), возвращает intents и writes. Доставку выполняет dispatcher.