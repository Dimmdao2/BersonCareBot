---
description: Select (выпадающий список) — подпись выбранного значения в триггере не должна быть «сырым» ключом
alwaysApply: true
---

Правило живёт в `AGENTS.md` §22 «UI: Select — displayLabel». Текст здесь не дублируется.
