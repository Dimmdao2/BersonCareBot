---
description: Единая база задач — только через утилиту taskdb, НИКОГДА сырым SQL
globs:
alwaysApply: true
---

Правило живёт в `AGENTS.md` §1 «Онбординг и server conventions», подраздел «Задачи — только через
taskdb-порт, не сырой SQL». Текст здесь не дублируется.
