dist
node_modules
coverage

# Документация форматтером НЕ трогается — решение владельца 29.07.
# Причина: prettier переставляет кавычки, выравнивает таблицы и нормализует маркеры списков.
# В документах это чистый шум: он ничего не чинит, зато ломает ручную вёрстку планов и
# конфликтует с параллельной работой по уборке планов. Один прогон 29.07 задел 1165 .md.
*.md
