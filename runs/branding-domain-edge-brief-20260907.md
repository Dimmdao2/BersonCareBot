# Worker brief: automatic custom-domain TLS edge (#787)

Read `AGENTS.md` by its mandatory header-map route before every action. In particular read and obey §1/§1b (host identity and PROD prohibition), §3, §7, §9, §10/§10a/§10b, §12, §14a and §24. Read `README.md`, `docs/ARCHITECTURE/SERVER CONVENTIONS.md`, `docs/ARCHITECTURE/LOCAL_DEV_AND_AGENT_TESTING.md`, `deploy/HOST_DEPLOY_README.md`, the full authority `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`, `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/SURFACE_AND_DOMAIN_MAP_2026-08-22.md`, `docs/_TODO/CUSTOM_DOMAIN_TLS_RESEARCH_2026-07-26.md`, and `docs/_TODO/CLINIC_CUSTOM_DOMAIN_PROPOSAL_2026-08-19.md`. Workstream is taskdb #787.

Implement the repository-managed infrastructure portion of B7/B8/C5a for automatic custom-domain TLS, without touching any live host. The required result is a reusable common edge using Caddy `on_demand_tls` with a fail-closed application `ask` URL, automatic issuance/renewal, and reverse proxying that preserves the original `Host` (`header_up Host {hostport}` or the exact correct equivalent). There must be no per-tenant nginx edit, per-tenant Certbot command, or process reload in the clinic workflow.

First reconcile the existing documented topology: the new trial production host is `135.106.187.95`, blue/green Docker lives under `/opt/bersoncarebot`, nginx currently owns public 80/443, and no second public IP is documented. Do not silently assume a second IP. Choose and implement only a topology that can coexist with the documented host and preserve the existing nginx-owned platform domains. If Caddy cannot own arbitrary custom-host TLS without a documented port-443 routing/cutover mechanism, make that exact missing infrastructure fact an explicit named blocker in the runbook instead of inventing an address. A valid repository change may include Caddy config/template, compose/service wiring, nginx stream/SNI routing or an explicit atomic edge cutover mechanism, safe validation/install scripts and rollback/runbook, but it must be one coherent design and must not execute it.

Expose parameterized non-secret configuration (ask URL, upstream, email/storage paths, canonical platform DNS target, stable edge IP) through the project’s established env/template conventions. Do not put critical product configuration in a new ad-hoc env key if the application/database owns it; edge-operational values may be env/template inputs per AGENTS §3. Include readiness/renewal observability compatible with C5a: edge health and a concrete certificate-expiry failure signal, while the application remains the authority for whether a hostname is eligible.

Preserve current TEST VPN-locked `test.bersoncare.ru` and existing production platform hosts. Do not SSH, sudo, install packages, alter firewall/DNS, deploy, reload nginx, request a real certificate, or probe either PROD host. Do not edit application source, DB/schema/migrations, or React UI.

Per AGENTS §10b, product workers do not write tests. Validate static configuration with already available tools only; if Caddy is absent, do not install it. Run relevant shell/static checks and document exact unexecuted live gates. Commit only intentional scoped files with explicit paths, no `git add -A`, one coherent product commit referencing #787, and do not push.
