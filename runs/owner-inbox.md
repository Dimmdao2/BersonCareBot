сосед влил всё, ветка свободна — можно коммитить и публиковать
[x] прочитано лидом 31.07 03:3x — подтверждено: все 6 коммитов из прошлой записи orch-wakeup уже в HEAD. Блокер read-only bcb-wt-* остаётся (это отдельная проблема песочницы, не про слияние). Осталось: независимый аудит RLS-коммита 61e644c6d (Track D) — не запущен, порт недоступен из этой сессии.
[x] подъём лида 31.07 (следующий проход) — перепроверил: bcb-wt-* всё ещё read-only (touch на tariff и 987-d8 → EROFS), живых процессов нет (все логи *.log завершены, spawned pid мёртв). Аудит RLS-коммитов 61e644c6d/0d1d8dd47/1cf06cb7f (Track D #987) по-прежнему не запускался — не подменял независимую проверку своим чтением. Записал долг явной строкой в NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md (статус «ОЖИДАЕТ АУДИТА», без коммита, только правка файла). Дальше нужен: (1) кто-то снаружи чинит read-write bind для bcb-wt-* в orch-revive-сессиях, (2) после этого — auditor через tools/orch-launch.sh именно на этот кусок (RLS/гранты, self-assessment не считается).
[x] подъём лида 31.07 (ещё проход) — нашёл ТОЧНУЮ причину блокера в коде, не только симптом: `tools/orch-revive.sh` поднимает лида без `--persona`, `/home/dev/brain/lib/rights-contract.mjs:22` без персоны отдаёт ровно один writable root (`cwd`=BersonCareBot). Это контракт прав, не недосмотр в bwrap. Персона `devlead` тоже не решает — она даёт один внешний repo за раз, а лиду нужны BersonCareBot + bcb-wt-* клон одновременно. Подробности и два варианта фикса (новая персона с несколькими writable roots, либо orch-launch.sh пишет бриф-лог не внутри клона) — в runs/orch-wakeup.md, последняя запись. Сам /home/dev/brain не трогал (не мой репозиторий, риск слишком высокий без ведома). Долги по аудиту Track D (RLS+снятие проекции) без изменений, оба ждут этого фикса.
