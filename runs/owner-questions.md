
- [ ] **31.07, D10a — «убрать `public.outgoing_delivery_queue`» опирается на неверное описание.** В плане она
  названа «не журнал, вторая очередь доставки на стороне вебаппа». По коду это НЕ дубль: таблицу определяет
  вебапп (drizzle/миграции), но обрабатывает её воркер ИНТЕГРАТОРА
  (`apps/integrator/src/infra/runtime/worker/outgoingDeliveryWorker.ts` через
  `infra/db/repos/outgoingDeliveryQueue.ts`: `claimDueOutgoingDeliveries` / `markOutgoingDeliverySent` /
  `markOutgoingDeliveryDead`). Это ЕДИНСТВЕННАЯ очередь доставки; рядом у интегратора только
  `message_retry_jobs` (отложенные и повторы, другой цикл). Убрать её сейчас = снести доставку.
  Проверено мной лично (grep по `apps/integrator/src`, разбор циклов `worker/main.ts`), независимо от агента,
  который на это наткнулся и правильно остановился, а не выдумал миграцию в несуществующую таблицу.
  **Вопрос:** что именно вы имели в виду 30.07 под «`outgoing_delivery_queue` убирать»? Три чтения:
  (а) убрать ВЛАДЕНИЕ вебаппа — таблица переезжает в схему интегратора, поведение то же;
  (б) вы имели в виду `integrator.delivery_attempt_logs` (тот действительно дубль-журнал и сносится);
  (в) очередь и правда заменяется на новую в рамках D16 «один цикл» — тогда снос идёт только вместе с ней.
  **Мой safe-default, пока ответа нет:** ничего не сношу, наблюдение оставляю на месте — оно уже смотрит на
  ту очередь, которую воркер реально обрабатывает. D10a часть 1 оказалась пустой работой по факту.

- [ ] **31.07, D16 — «один цикл»: один на весь интегратор или один ЦИКЛ ДОСТАВКИ?** Перепись циклов
  (`docs/_TODO/runs/integrator-cleanup/D16_LOOP_CENSUS.md`, три независимые стратегии поиска) показала: у
  интегратора не один резидентный процесс, а два — `worker` и отдельный `scheduler` (собственный systemd unit со
  своим вечным циклом), плюс внешние cron-триггеры (периодические HTTP-вызовы `/api/internal/**/tick`), которые
  тоже являются точками планирования. Формулировка плана «у воркера должен остаться один вечный цикл — доставка»
  и «отдельного модуля-планировщика не заводим» запрещает заводить НОВЫЙ планировщик, но не говорит, сносить ли
  СУЩЕСТВУЮЩИЙ `scheduler/main.ts`.
  **Вопрос:** целевое состояние — (а) один вечный цикл на весь интегратор, то есть `scheduler` как отдельный
  процесс исчезает; или (б) один цикл ДОСТАВКИ внутри `worker`, а `scheduler` остаётся отдельным процессом со
  своей задачей?
  **Мой safe-default, пока ответа нет:** ничего не сношу. Перепись сделана, вердикты по каждому циклу записаны,
  блокирующие пункты названы — работа не стоит, стоит только сам снос, который и так упирается в D10 (а тот
  ждёт вашего ответа про `outgoing_delivery_queue`).

- [ ] **31.07, D15a — СЕМЬ продуктовых развилок по идентичности, одним листом.** Исследование готово:
  `docs/_TODO/runs/integrator-cleanup/D15A_IDENTITY_RESEARCH.md` (476 строк, перепись пяти предметов + схема
  переноса + порядок шагов D15b с риском). Развилки продуктовые, я их за вас не закрываю. По каждой указана
  рекомендация и безопасное умолчание — до вашего ответа действует умолчание, работа не стоит.
  1. **Какой телефон из мессенджера считать доверенным** — рекомендация: только подтверждённый (владение
     контактом в Telegram, валидная подпись MAX, пройденный код в вебаппе). Умолчание: неподтверждённый телефон
     не даёт доверия и не участвует в сопоставлении людей.
  2. **Когда разрешено сливать людей автоматически** — рекомендация: только поглощение пустой технической
     заготовки при непротиворечивых признаках. Умолчание: не сливать, возвращать конфликт.
  3. **Общий или переиспользованный номер** — рекомендация: телефон доказывает владение номером, но не тождество
     человека; настоящая карточка сохраняется, конфликт на разбор. Умолчание: не сливать и не переносить привязку.
  4. **Создаёт ли первый контакт в мессенджере зачисление в клинику** — рекомендация: не создаёт до приглашения,
     записи или действия клиники. Умолчание: без зачисления.
  5. **Что делать с существующими людьми из мессенджеров без зачисления** — рекомендация: добирать только по
     явным признакам, остальные остаются без клиники. Умолчание: все остаются без клиники до явного действия.
  6. **Уведомления по умолчанию после новой привязки канала** — рекомендация: служебные сообщения отдельно,
     необязательные темы выключены до согласия. Умолчание: необязательные выключены, существующий выбор
     человека не перезаписывается.
  7. **Кто владеет привязками каналов** — рекомендация: вебапп, интегратор хранит только свою техническую
     привязку. Умолчание: будущей узкой роли интегратора прямая запись в общие привязки не даётся.

  **Отдельно, важное из исследования:** интегратор СЕГОДНЯ владеет частью канонической идентичности — первый же
  входящий вебхук создаёт человека в `platform_users`, привязывает канал, включает предпочтения, а при нескольких
  кандидатах сам выбирает, кого с кем слить. Поэтому выдать узкую роль без права записи (пункт D17) НЕЛЬЗЯ, пока
  это не перенесено: сломается первый вебхук нового пользователя. Оставить права — значит не выполнить цель
  Track D. Порядок остаётся D15a → D15b → D17.

- [ ] **31.07, D12b — перепись исполнителя готова, девять вопросов о ГРАНИЦАХ переноса.** Документ:
  `docs/_TODO/runs/integrator-cleanup/D12B_REACHABLE_CENSUS.md`. Числа: 105 достижимых сценариев, из них
  **30 «канал/доставка — остаётся», 62 «продуктовое решение — уезжает», 13 спорных**; плюс 21 недостижимый тип
  действия, найденный заново. То есть «интегратор = приём и доставка» сегодня выполнено меньше чем наполовину.
  **Важная поправка, которую внёс исполнитель (и он прав):** D13/D14 перенесли решения подсистемы «жизненный
  цикл записи на приём». Есть ВТОРАЯ, отдельная подсистема — напоминания о самопомощи (`handlers/reminders.ts`):
  собственное планирование, собственные расчёты, своя таксономия причин пропуска, русские тексты. D13/D14 её
  вообще не касались, и в плане она отдельным пунктом не стоит.
  Девять вопросов сгруппированы так (по каждому нужен ваш ответ «остаётся / уезжает»):
  1–2. Опознание человека по внешнему ID и привязка телефона через мессенджер: интегратору остаётся только
     доставка входа, а создание учётки, доверие к телефону и синхронизация личности — вебаппу? (Определяет
     статус 13 спорных сценариев сразу.)
  3. Старая ветка ответа администратора в обращениях поддержки: сама владеет состоянием обращения и решает,
     закрывает ли ответ связанный вопрос. Оставляем намеренно или переносим/списываем?
  4. Три недостижимых действия по правилам напоминаний: удалять как мёртвые или планируется новый экран,
     который снова до них дойдёт?
  5–8. Напоминания о самопомощи: выбор заголовка и канала, ссылка перехода, расчёт «отложить», расчёт
     «выключить до», таксономия и русские тексты причин пропуска — это целевое состояние или тоже уезжает?
  9. Таксономия категорий уведомлений (города/онлайн/записи) захардкожена в интеграторе — если подсистема
     оживёт, она должна жить в вебаппе?
  **Пока ответов нет, работа не стоит:** перепись сделана, она сама по себе результат пункта D12b; следующие
  шаги переноса упираются в эти границы, и гадать за вас я не буду.

- [ ] **31.07, НАХОДКА (не задача, требует вашего решения): у исполнителя интегратора НЕТ ТЕСТОВ ВООБЩЕ.**
  Как поймал: агент вырезал 1189 строк «недостижимых» веток и отчитался «41 тест зелёный». Среди вырезанного
  оказался **живой** `message.edit` — он используется в сценариях MAX (`content/max/user/scripts.json`, 14
  вхождений) и на живом пути `telegram/deliveryAdapter.ts:217`; там же было всё семейство доставки
  (`message.deliver`, `message.retry.enqueue`, `intent.enqueueDelivery`). Коммит я отменил, в ветку не попало.
  Проверил отдельно: отключение живой ветки рассылки напоминаний (`reminders.dispatchDue`) **не роняет ни одного
  теста**. В каталоге `kernel/domain/executor/handlers/` тестовых файлов нет ни одного.
  **Что это значит:** любая правка исполнителя сегодня проходит без страховки — «зелёные тесты» там не значат
  ничего. А исполнитель это ровно то, что Track D разбирает: 105 достижимых сценариев, из них 62 продуктовых
  решения на перенос.
  **Вопрос:** заводить ли отдельную работу «покрыть исполнителя тестами хотя бы по живым сценариям» ПЕРЕД тем,
  как продолжать переносы из него? В вашем плане такого пункта нет, поэтому сам не завожу.
  **Мой safe-default:** каждый следующий рез в исполнителе требую доказывать не тестами, а тремя источниками
  (контент сценариев, код обоих приложений, смысловой поиск) — так уже переписан повторный бриф D12.

- [ ] **31.07, дыра в защите D3/D4 (не блокирует, но знать нужно).** Механизм передачи владения поддержкой
  (`supportCanonicalWriteHandoff.ts`) тестом закрыт — отключение приёма канонического ответа роняет тест.
  **Но его ПОДКЛЮЧЕНИЕ в `writePort.ts` не покрыто**: я отключил приём ответа прямо в месте вызова
  (`accepts: () => false`) — ни один из 47 тестов интегратора не покраснел. То есть если кто-то сломает
  использование, а не сам модуль, интегратор снова начнёт писать канон сам, и это пройдёт молча.
  Причина та же, что я уже записывал: у исполнителя интегратора тестовой сети почти нет.
  **Что предлагаю:** отдельная небольшая работа — тест на уровне `writePort`, что после подтверждённого
  канонического ответа прямой писатель НЕ вызывается. Заводить не стал: это моя находка, а не пункт вашего плана.
