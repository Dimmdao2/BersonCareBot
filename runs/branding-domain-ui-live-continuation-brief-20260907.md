# Test or view classification — live-view continuation for clinic domain settings UI (#787)

Read the `AGENTS.md` header map first, then §§1a, 9, 10, 10a, 10b, 16, 17, 21, 22 and 24 in full; `README.md`; `docs/ORCHESTRATION_BINDINGS.md`; `docs/ARCHITECTURE/LOCAL_DEV_AND_AGENT_TESTING.md`; `docs/ARCHITECTURE/DOCTOR_APP_UI_STYLE_GUIDE.md`; exact TPB-14/B8/C5a requirements in `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md`; and the existing candidate audit `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/AUDIT_CUSTOM_DOMAIN_UI_2026-09-07.md`.

Continue the independent audit on synchronized candidate `58cdd83c1ad5ab1b50325f0a1c63398ea92092c5` (it contains audited UI/test commit `b48ae835a7ceb70c4584945b881bdfb254012ed1` plus the current integration head). Do not repeat its blind kill-set, tests or fault injections. Its only open gate is the live desktop/mobile view; it reports no product finding.

Run the candidate on one free port in `5210..5219` without touching shared `5200`. The sanctioned runtime source is the canonical named DEV env at `/home/dev/dev-projects/BersonCareBot/apps/webapp/.env.dev`: source it into the candidate process without printing or copying values, then override only non-secret local launch identity (`NODE_ENV=development`, `HOST=127.0.0.1`, selected `PORT`, `APP_BASE_URL=http://127.0.0.1:<selected-port>`). Do not read `/opt`, TEST or PROD env. Start Next directly as the local runbook requires; wait for readiness in the foreground workflow and stop only your candidate process at the end.

Use the existing registered clinic-owner account through the ordinary login contract in `AGENTS.md` §1a. Do not create fixtures and do not change/clear/replace the clinic slug or custom-domain intent: those claims are intentionally persistent/quarantined and a visual audit must not damage named DEV data. Inspect the current settings state only.

At desktop and mobile viewport widths, verify the existing domain section is reachable for the owner and record actual facts about overflow/cut-off, base-domain field, both human placement choices, readable current hostname/DNS instruction/status, recheck reachability, technical `<slug>.therapygo.ru` address, booking link, and neighbouring slug/branding/settings usability. UI text, element counts/order, DOM/source shape and screenshot pixels are view evidence, never automated test oracles. Do not add another test.

Append a short continuation section to the existing audit artifact with the exact candidate SHA, selected port, launch/readiness facts without secret values, both viewport observations, whether any current-data limitation remained, cleanup proof, and binary final verdict. A finding must name a reachable owner-requirement failure; otherwise mark PASS after live continuation. Update/add the audit-queue verdict accordingly. Commit only the artifact and queue line with explicit paths; no product fix, no push, no land, no TEST/PROD/DNS/TLS operations.
