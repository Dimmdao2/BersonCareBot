# Слепой kill-set — этап 2a #926 (auditor-live)

Составлен ДО `git show fc2361bb8` и ДО чтения любых тестов, только по:
AGENTS.md §3/§5/§10a/§10b/§17/§24.4/§24.5 и
docs/_TODO/CLINIC_PUBLIC_PAGE_AND_URL_FLIP_2026-08-19.md §3.1/§3.2/§3.3/§3.5, 17.B, 17.G, 17.H.

Оракул: решение владельца 11.09 — «отдельная страница специалиста… показывает его описание, его
фотографию… привьюшка есть на визитке клиники… его данные там должны быть»; «что именно публикуется —
решает клиника, а не платформа»; «неактивный или удалённый специалист даёт тот же отказ, что и
выключенная визитка, без возможности перебрать клиники по форме ответа»; «набор карточки И ЕСТЬ
авторизация» (§3.5); «короткое описание — просто текст, подробное — markdown-материал с медиа».

Форма: «что сломать в продуктовом коде → какое наблюдаемое последствие для человека».

## A. Кто попадает в публичную выдачу специалистов (17.B, 17.G)

K1. Убрать из предиката двери условие «специалист опубликован» →
    посетитель видит на визитке человека, которого клиника публиковать не просила (решение о
    публикации переходит от клиники к платформе).
K2. Убрать `is_active` из предиката →
    на визитке остаётся уволенный/скрытый специалист; клиника его выключила, а он висит публично.
K3. Убрать связывание по организации (джойн/предикат org) →
    в выдаче клиники A появляются специалисты клиники B: ФИО сотрудника чужого арендатора наружу.
K4. Убрать проверку «визитка опубликована / организация активна» на пути выдачи специалистов →
    выключенная визитка перестаёт быть выключенной: по её адресу аноним всё равно перечисляет штат.

## B. Страница специалиста и форма отказа (17.G)

K5. Сделать отказ по несуществующему/неопубликованному/неактивному специалисту ОТЛИЧНЫМ от отказа
    выключенной визитки (разный статус, разный текст, разный редирект) →
    аноним перебирает id и слаги: «этот есть, но скрыт» vs «этого нет» — перечисление людей и клиник
    по форме ответа. Молчаливо и дорого.
K6. Убрать сверку «специалист принадлежит клинике из слага» (читать специалиста только по его id) →
    `/{clinicA}/specialist/{id-из-B}` рисует специалиста чужой организации: межарендаторская стена.
K7. Отдать на странице специалиста поля, которых владелец не просил и которые являются внутренними
    идентификаторами (organization_id, user_id, uuid филиала) →
    §3.2 «ни одного внутреннего идентификатора»; материал для перебора.

## C. Анонимная отдача медиа — «набор двери И ЕСТЬ авторизация» (17.H гр.1, §3.5)

K8. Заменить проверку «uuid входит в набор медиа этой двери» на проверку «файл принадлежит этой
    организации» →
    любой файл клиники (скан паспорта пациента, документ, неопубликованное фото) отдаётся анониму по
    `/{clinic}/media/{uuid}`, если он угадал/узнал uuid. Самая дорогая и самая молчаливая поломка этапа.
K9. Снять публикацию со специалиста, а его аватар продолжает отдаваться →
    отзыв публикации не отзывает файл; клиника считает, что убрала человека, а фото живёт.
K10. Отдать по `/{clinicA}/media/{uuid}` файл ЧУЖОЙ организации →
    межарендаторское чтение файлов.
K11. Добавить в маршрут ветку-исключение (fallback «если не нашли в наборе — проверим владельца»,
    флаг, отдельный публичный список) →
    авторизация деградирует из членства в наборе во владение — это K8 обратно, но уже структурно.
K12. Ослабить подписанный `/api/media/{uuid}`, чтобы пускал анонима →
    падает не визитка, а весь медиа-чокпоинт: файлы пациентов доступны без сессии.

## D. Полное описание — markdown-материал (17.H)

K13. Убрать/обойти санитайзер, разрешить сырой HTML в markdown →
    хранимый XSS на публичной анонимной странице: скрипт клиники A выполняется у любого посетителя.
K14. Разрешить рендер ссылки на медиа-uuid, которого нет в наборе двери (в т.ч. uuid ЧУЖОЙ
    организации, вписанный в markdown руками) →
    публичная страница превращается в примитив чтения произвольных файлов; заметить нечем, картинка
    просто «работает». Дорого и молчаливо.
K15. Рендерить КОРОТКОЕ описание как markdown/HTML вместо plain text →
    тот же класс инъекции, но через превью на визитке клиники, которое видят все.

## E. Двери, права, миграция (§5, §24.2, §1, §3.1)

K16. Завести вторую публичную дверь (новая SQL-функция / новый грант рабочей роли) под специалистов
    или под их медиа вместо расширения `app.read_public_clinic_card(text)` →
    §5 «один общий проход»: обход появляется в варианте, о котором забыли; вторая дверь разъедется
    с первой по предикатам публикации.
K17. Дать шву табличный `SELECT` вместо колоночных грантов на ровно нужные колонки →
    шов читает колонки, которых ему не положено (в т.ч. будущие), и никто этого не заметит.
K18. Не объявить новые колонки в `declaration.ts` (только `GRANT` в миграции) →
    reconcile снимет грант, публичная страница ляжет с 42501 — громко, но чинится не там, где сломано.
K19. Внести `GRANT/REVOKE/POLICY/ROLE` в миграцию →
    нарушение §1; права разъезжаются между декларацией и живой базой.
K20. Читать `be_specialists` напрямую из публичного маршрута мимо объявленной двери →
    §3.1: анонимный принципал касается арендаторских таблиц.

## F. Владение аватаром (17.H, граница записи)

K21. Записать в аватар специалиста uuid ЧУЖОГО файла (валидации владения в момент записи нет) и
    проверить, отдаст ли его дверь чтения →
    если предикат чтения не фильтрует по владению — админ клиники публикует наружу любой файл
    платформы одним UPDATE. Если фильтрует — это решение, а не дыра.

## G. Зоны UI (§17)

K22. Внести в общий `ClinicPublicCardView`/страницу специалиста импорт из `shared/ui/doctor/**` или
    `@/components/ui/**` в патиент-ветке (и наоборот) →
    правка стиля одной зоны перекрашивает другую; ловится eslint — громко, не тест.

## H. Единственный источник (повтор класса 17.A)

K23. Развести предпросмотр в кабинете и публичную страницу по разным источникам специалистов →
    владелец видит один состав, посетитель другой, и расхождение молчит. Ровно тот дефект, который
    слепой аудит этапа 1 поймал на адресах.

---
Число названных поломок: 23.
