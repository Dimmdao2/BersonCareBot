# Worker brief: branding domain core (#787)

Read `AGENTS.md` by its mandatory header-map route before every action. In particular read and obey §1/§1b (server, migrations, privileges), §4a, §5, §7, §9, §10/§10a/§10b/§11, §12, §14a and §24. Read `README.md`, `docs/ARCHITECTURE/LOCAL_DEV_AND_AGENT_TESTING.md`, `docs/ARCHITECTURE/SERVER CONVENTIONS.md`, `deploy/postgres/privileges/README.md`, and the full authority `docs/_TODO/THERAPYSTO_PATIENT_BRANDING_INITIATIVE/IMPLEMENTATION_PLAN.md` plus its `SURFACE_AND_DOMAIN_MAP_2026-08-22.md`. Workstream is taskdb #787.

Implement the application/domain-core portion of the reopened B1/B2/B3/B4a/B8/C5a requirements. The production Next request path must resolve the real request `Host` through a production-wired tenant lookup; the second `proxy` argument is `NextFetchEvent` and must never be used as a test-only dependency seam. Reuse the existing `RequestSurfaceResolver` and public slug/branding ports. Add the smallest narrow, pre-session-safe hostname→organization DB port/root required by the architecture and declare exact DB privileges only in `deploy/postgres/privileges/declaration.ts`, regenerating owned artifacts through the generator.

Build the server-owned custom-domain binding lifecycle required by B2/B8/C5a: base domain plus placement (`apex` or existing-site mode), exact hostname computed server-side, global uniqueness, immutable organization ownership, explicit pending/readiness/active/failed/suspended/quarantine semantics sufficient for the plan, and only `active` participating in Host resolution or canonical redirects. Pending or failed custom domains must never damage `<slug>.therapygo.ru`. When active, the technical subdomain returns 308 to the custom hostname while preserving path and query. Unknown/inactive/deleted organizations and unknown hosts fail closed. A known clinic without paid/published branding remains a live Therapygo surface using its core display name as required by B4a.

Extend the existing settings/API choke point rather than creating a duplicate generic settings store. Implement the internal fail-closed Caddy `ask` HTTP endpoint against the binding state, using the existing `/api/internal/*` authentication convention. Do not perform DNS/network readiness probing in request middleware. If a deterministic application-side transition service/route is needed for the later verifier/edge integration, add it narrowly and document its contract; do not claim a certificate exists from DNS alone.

Preserve current `test.bersoncare.ru` and all current staff/platform-admin/patient-default behavior. No BersonCare-specific branch. Do not touch production or TEST hosts, do not deploy, do not apply migrations to a live database, do not start/stop shared dev servers, and do not edit Caddy/nginx deployment files or the React settings UI in this worker.

Per AGENTS §10b, product workers do not write tests. You may run existing targeted tests, typecheck, lint, privilege generation/checks and migration static/preflight checks that do not mutate shared DB state. Report which required observable behaviors still lack proof so the independent auditor can create the acceptance tests blind. Commit only intentional scoped files with explicit paths, no `git add -A`, one coherent product commit referencing #787, and do not push.
