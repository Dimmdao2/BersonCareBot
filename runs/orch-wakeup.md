# Подъём лида 31.07 01:5x — блокер инфраструктуры, работа НЕ запущена

Сторож поднял лида (`tools/orch-revive.sh`), живых зависших прогонов не найдено: все логи в
`bcb-wt-*/docs/_TODO/runs/**/*.log` за последние сутки заканчиваются `result`/`status: completed`,
ни один `spawned pid=` не жив (`kill -0` — dead на всех проверенных).

## Блокер: в этой сессии все `/home/dev/dev-projects/bcb-wt-*` смонтированы read-only

Проверено на нескольких клонах (`bcb-wt-987-d8`, `bcb-wt-tariff`, `bcb-wt-docs3`,
`bcb-wt-1070-plan-refresh`): `touch <клон>/.write-test` → `Read-only file system`, причём ошибка
не снимается `dangerouslyDisableSandbox` — то есть это реальный read-only mount на уровне ОС в
этой сессии, а не ограничение инструмента. Первичный репозиторий (`BersonCareBot`) и `/tmp`
пишутся нормально.

Следствие: `tools/orch-launch.sh` в этой сессии выполнить нельзя ни для worker, ни для auditor —
скрипт сам пишет лог-файл рядом с брифом ВНУТРИ клона (`$(dirname "$BRIEF")/$RUN_ID.log`) ещё до
спавна агента, а это тоже запись в read-only дерево. Значит и обычный аудит/фикс, и слияние клона
обратно в `feat/doctor-ui-rebuild` из этой сессии недоступны.

Это не решение владельца, которое можно записать вопросом и жить дальше, — это отличие рантайма
данной поднятой сессии от обычных интерактивных lead-сессий (у них клоны явно писались весь день,
судя по коммитам). Разбираться нужно на стороне того, что раздаёт монтирование `bcb-wt-*` подъёмным
(`orch-revive.sh`) сессиям.

## Что нашёл по состоянию очереди (для следующего, у кого будет запись)

Не сведены с очередью аудита (`NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md`) свежие коммиты в двух клонах:

- `bcb-wt-tariff` (карточка #1069, §5a): `ed33c6a02`, `2ea832962`, `f1eee78b6` — без строки-вердикта
  в очереди. Клон также не содержит текущую голову `feat` в первичном репо (`919147d14`) — нужен
  `fetch+merge` перед следующим воркером (гейт `orch-launch.sh` проверка 2).
- `bcb-wt-987-d8` (Track D #987): `61e644c6d`, `0d1d8dd47`, `1cf06cb7f` — без строки-вердикта.
  `61e644c6d` трогает `deploy/postgres/{p0-5-role-split,p0-5b-grants,phase4-force-rls-cutover,
  phase4-locked-helper-rls-policies}.sql` (RLS/гранты) — по правилам ночной волны такое не
  засчитывается самооценкой исполнителя, нужен независимый аудитор до следующей работы в этом
  клоне. `0d1d8dd47`/`1cf06cb7f` — правки слияния той же ретирки (contracts/projectionEventTypes).

Обе строки — кандидаты на следующего auditor/auditor-live, как только запись в клоны снова
доступна. Ни то, ни другое не запускал — не было возможности создать бриф внутри клона.

Работа НЕ остановлена по решению владельца — остановлена окружением. Сообщить владельцу утром.

## Повторная проверка 31.07 02:41 MSK — блокер тот же, живых прогонов нет

Перепроверил заново на этом подъёме: `touch` на все 27 каталогов `bcb-wt-*` даёт `Read-only file system`
без исключений (список полный, не только 4 клона из прошлой записи). Живых процессов нет — по всем логам,
изменённым за последние сутки (`bcb-wt-987-d8`, `bcb-wt-docs3`, `bcb-wt-portsmoke`, `bcb-wt-tariff`), последний
`spawned pid=` мёртв (`kill -0` → dead), в хвосте логов `result`/`err":""` — все завершились штатно.
`tools/orch-launch.sh` не трогал — по коду (`governor/host-sandbox.mjs:13`) он всё равно пишет лог рядом с
брифом внутри клона до спавна, а это та же read-only точка. Хаков в обход (копирование клона в /tmp, симлинки,
отключение сэндбокса) не делал — это подмена доверенного пути слияния, не решение владельца, которое можно
записать и жить дальше. Строки-кандидаты на аудит из прошлой записи (`bcb-wt-tariff`, `bcb-wt-987-d8`) актуальны,
но записать их вердикт в очередь без прогона независимого аудитора через порт нельзя — самооценка воркера не
заменяет аудит, особенно для `61e644c6d` (RLS/гранты).

Вывод не изменился: чинить нужно снаружи (кто раздаёт монтирование `bcb-wt-*` для `orch-revive.sh`-сессий),
не из самой сессии. Сообщить владельцу утром.
