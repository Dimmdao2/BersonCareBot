# Fill only after the owner approves exact TEST names. Do not commit actual TEST values.
STAFF_HOST=
PLATFORM_ADMIN_HOST=
PATIENT_DEFAULT_HOST=
PATIENT_BRANDED_HOST=
CLINIC_CUSTOM_HOST=
# A TEST certificate bundle whose SAN explicitly covers every host above.
TLS_CERTIFICATE_PATH=
TLS_CERTIFICATE_KEY_PATH=
