# СГЕНЕРИРОВАНО из apps/webapp/src/modules/operator-health/backgroundJobManifest.ts. Руками не править.
# Перегенерировать: node deploy/host/background-jobs-cli.mjs --write
# Сверить (гейт deploy и CI): node deploy/host/background-jobs-cli.mjs --check
#
# Автопродление тарифа (счета клиникам) — среда test.
# Счёт продления тарифа истёкшим подпискам; шаблон ходил без публичного Host и получал 404 (B1).
# id=saas_billing_renewal_tick tick=saas_billing/saas_billing_renewal.tick POST /api/internal/saas-billing/renewal/tick?limit=50
# principal=internal_job_bearer surface=app_public_origin timeout=300s stale_after=10800s
# Host/Origin/X-Forwarded-Proto и env-файл (/opt/env/bersoncarebot/webapp.test) строит общий transport
# deploy/host/run-internal-job.sh — cron-строка их не копирует и не знает про branding proxy.
0 * * * * root /opt/projects/bersoncarebot-test/deploy/host/run-internal-job.sh test saas_billing_renewal_tick
