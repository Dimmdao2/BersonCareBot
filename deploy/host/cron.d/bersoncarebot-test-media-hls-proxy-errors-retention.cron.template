# СГЕНЕРИРОВАНО из apps/webapp/src/modules/operator-health/backgroundJobManifest.ts. Руками не править.
# Перегенерировать: node deploy/host/background-jobs-cli.mjs --write
# Сверить (гейт deploy и CI): node deploy/host/background-jobs-cli.mjs --check
#
# Retention HLS proxy errors — среда test.
# Окно хранения media_hls_proxy_error_events; реестр, права и API были, будильника не было (B2).
# id=hls_proxy_retention tick=media/media.hls_proxy_errors.retention POST /api/internal/media-hls-proxy-errors/retention
# principal=internal_job_bearer surface=app_public_origin timeout=600s stale_after=691200s
# Host/Origin/X-Forwarded-Proto и env-файл (/opt/env/bersoncarebot/webapp.test) строит общий transport
# deploy/host/run-internal-job.sh — cron-строка их не копирует и не знает про branding proxy.
20 4 * * 1 root /opt/projects/bersoncarebot-test/deploy/host/run-internal-job.sh test hls_proxy_retention
