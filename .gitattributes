# Очередь аудита — журнал: обе стороны только ДОПИСЫВАЮТ строки в конец, поэтому обычное
# трёхстороннее слияние даёт конфликт на каждом мердже клона (поймано трижды 31.07).
# union сохраняет обе стороны автоматически — ровно то, что я делал руками.
docs/_TODO/NIGHT_WAVE_AUDIT_QUEUE_2026-07-28.md merge=union
